# Portier Claude Code Guide

## Project Identity

- App name: Portier
- Repository name: portier-port-forwarding
- Purpose: local TCP/UDP port forwarding manager for development and LAN testing

## Important Naming Conventions

- Use `sources`, not `src`.
- Use `build`, not `dist`.
- Use `scripts` for executable automation.
- Use lowercase filenames for normal markdown docs.
- Keep tool-required files uppercase: `AGENTS.md`, `CLAUDE.md`, and `SKILL.md`.
- Keep `README.md` file in the root uppercase.
- React component and view files under `client/sources/` use **CamelCase** filenames (e.g., `ForwardRuleList.tsx`, `StatCard.tsx`, `Sidebar.tsx`).
- Non-component files (utilities, types, config) keep the existing repo convention (e.g., `format.ts`, `nav.ts`, `portierApi.ts`).
- Go CLI command files under `tools/cli/sources/commands/` are named by **responsibility**: each command file is a bare noun matching its command (`config.go`, `diagnose.go`, `diagnostics.go`, `runtime.go`, `list.go`, `status.go`, `start.go`, `stop.go`, `activity.go`), plus cross-cutting helpers named for what they own (`url.go` = connection-target/URL resolution, `resolver.go` = rule id/name resolution, `root.go` = dispatch/help). Do **not** add a `cmd` suffix (the historical `configcmd.go`/`diagnosticscmd.go` suffix was only collision-avoidance and was removed in v1.7 Slice 2), and do **not** name a file for a type it merely returns when its real job is something else (the old `config.go` held URL resolution, not config commands). Renames are `git mv` only — never change the `commands`/`commands_test` package, exported symbols, command names, flags, output, or exit codes for a cosmetic rename. The CLI stays a pure API client.
- For canonical Portier terminology (forward rule, config plan/apply/import, runtime/server/service, advisory vs warning, diagnose vs diagnostics export, live connection vs UDP session), see `docs/glossary.md`. New docs/API/CLI/UI wording should use glossary terms, or update the glossary if a genuinely new concept is introduced. Do not rename public API fields or REST paths (e.g. `/api/forwards`, `listenHost`/`targetHost`, `clientAddress`/`targetAddress`) for cosmetic consistency; document any exception in the glossary.

## Architecture

- `server/sources` handles the Node.js service, REST API, config persistence, and forwarding lifecycle.
- `service/sources` handles the native Go service runtime focused on smaller binaries and service deployment.
- `client/sources` handles the React UI only.
- `shared/sources` owns types, validation, port constants, and port advisory logic.
- `scripts` contains executable scripts; each platform subdir (`windows/`, `macos/`, `linux/`) also contains its platform docs and templates.
- `tools` contains user-facing and developer-facing project tools. `tools/cli/` is the v1.3 portier CLI (a Go-based API client for the management API). Future possible tools: `tools/bench/`, `tools/replay/`. Do not place tools in `scripts/` or `service/`.

## Coding Rules

- Prefer small, readable changes.
- Do not rewrite the entire app unless explicitly asked.
- Keep TypeScript simple and explicit.
- Keep TCP and UDP forwarding logic understandable and testable.
- Keep runtime config external.
- Do not bake `rules.json` into packaged executables.
- Do not expose the management UI/API on `0.0.0.0` by default.
- The management UI/API defaults to `127.0.0.1:47831`.
- Recommended forward listen port range: `48000-48999`.
- Update docs when behavior changes.

## Security And Safety Rules

- Treat `0.0.0.0` as LAN exposure and warn clearly.
- Do not silently change firewall, service, or system-level behavior.
- Do not add telemetry.
- Do not add remote update/download behavior.
- Do not store secrets in repo files.
- Do not modify user config files unless explicitly requested.
- Do not change service scripts casually.
- Do not edit generated outputs or build artifacts directly.

## Files Claude Should Generally Avoid Editing

Do not edit these unless explicitly asked:

- `node_modules/`
- `build/`
- `server/build/`
- `service/build/`
- `client/build/`
- `shared/build/`
- `coverage/`
- `.git/`
- `.env`
- `.env.*`
- `*.log`
- `rules.json`
- `C:\ProgramData\Portier` examples except docs/templates
- generated package outputs under `build/windows/`
- `test-results/` (Playwright artifacts — gitignored, auto-generated)
- `playwright-report/` (Playwright HTML reports — gitignored)

## Preferred Validation Commands

Run the narrowest relevant validation first, then broaden before finishing:

```powershell
npm run lint
npm run typecheck
npm run test
npm run build
npm run check
```

For E2E tests (browser-level validation of user flows):

```powershell
npm run build:client       # required before running E2E
npm run test:e2e           # runs Playwright against TypeScript server on port 47890
npm run test:e2e:fresh     # build:client + test:e2e combined
```

For CLI (Go — tools/cli/):

```powershell
npm run test:cli              # go test ./... inside tools/cli (uses httptest; no running service needed)
npm run build:cli             # builds tools/cli/build/portier[.exe]
npm run validate:cli          # test:cli + build:cli
```

For coverage (reporting and gate validation):

```powershell
npm run coverage:shared     # shared TypeScript (vitest + v8)
npm run coverage:server     # server TypeScript (vitest + v8)
npm run coverage:client     # client TypeScript/React (vitest + v8)
npm run coverage:service    # Go service sequential coverage (~30s)
npm run coverage:cli        # Go CLI reporting only (no gate)
npm run coverage:baseline   # all five in sequence (reporting only)
npm run validate:coverage   # runs all + enforces gates; exits 1 if any gate fails
npm run validate:coverage:shared   # shared only
npm run validate:coverage:server   # server only
npm run validate:coverage:client   # client only
npm run validate:coverage:service  # service only
npm run validate:coverage:cli      # cli only
```

Coverage outputs written to `coverage/` (gitignored). Vitest writes json-summary per workspace; Go profiles are written and removed per run. Gates are defined in `scripts/validate-coverage.js`.

Baseline (v1.6-pre, recalibrated at Slice A; cli updated at Coverage Slice C and again at v1.10 Slice 1, service at Coverage Slice D, server at Coverage Slice E): cli 97.8% (gate 97%), client ~95%/~89-90%/~78-80% (gate 94/89/78), service 90.3% (gate 90%), shared 100% (gate 100/100/100), server 98.9%/93.6%/100% (gate 95/92/99). Client numbers vary ±1% due to Windows vitest ghost-entry deduplication — both runs pass.

Current gates (canonical in `scripts/validate-coverage.js`): shared `{stmts:100, branch:100, funcs:100}`, server `{stmts:95, branch:92, funcs:99}`, client `{stmts:94, branch:89, funcs:78}`, and the Go components service `{stmts:90, funcs:95}`, cli `{stmts:95, funcs:95}`, replay `{stmts:95, funcs:95}`. v1.13 added **function** gates to the Go components (gated at 95% statements + functions, except service statements which stays at 90 — platform/test-less packages hold its statement actual ~90.2%); the cli statement gate was deliberately set 97 → 95 in that standardization (cli actual 97.8%). Go tooling reports no branch coverage. Policy: shared 100%, server ~100%, client ≥90% statements, Go 95%. Replay is gated module-wide (its own Go module) via `npm run validate:coverage:replay`.

v1.6 Coverage Slice E (TypeScript server error paths): raised server coverage 95.4/92.2/100 → 98.9/93.6/100 (gates 89/91/99 → 95/92/99) with tests-only additions — no product code changed; `validate:contract` 167/167. `diagnose.test.ts` 85.9% → 98.0%: TCP listen-bind failure (occupied port), target-connect success (local server) / refused (unbound port) / DNS-resolution failure (`.invalid` host → target-connect skip), one-way udp-mode, and 0.0.0.0 / privileged-port / common-port advisory warns. `udp-forwarder.test.ts` 86.3% → 99.4%: the four send-callback error branches (one-way target send, multi-client session send on reuse, last-client return send, multi-client return send) emit `udp.packet.error` + record lastError — driven by clean instance-level `.send` injection (matching the existing `socket.emit("error")` tests), with a `waitUntil` deadline poll (no fixed sleeps). `api.test.ts`: apply-with-drift persist failure → 500 (parity with Go Slice D, via an inline failing `RuleStore`). Structurally-hard branches documented (not chased): the 2 s diagnose bind/connect **timeout** branches (`tryTcpBind`/`tryUdpBind`/`tryTcpConnect` timeout), the UDP `if (!this.listenSocket)`/`if (!this.lastClient)` post-stop/pre-client race guards, the `tcp-forwarder.ts` optional-registry guard (90% branch, by-design), and the api.ts platform-normalize branches (Windows-only env). Source: `audits/v1.6-coverage-audit-1.md`, `audits/v1.6-testing-audit-1.md`.

**Durable testing rule (Coverage Slice E):** Server coverage must prioritize meaningful runtime error paths — UDP send/socket errors, diagnose timeout/error aggregation, API rejection mapping, and cleanup/idempotency — over line-hits. Socket and timeout tests must use deadline polling and Test-A helpers, never fixed sleeps. Forcing send-callback error branches via a clean instance-level `.send`/`.emit` override on a specific socket is allowed; brittle prototype/`dgram` monkeypatching is not. Structurally-unreachable Node socket internals (real send-buffer timeouts, post-stop reply races) are documented, not force-covered.

v1.6 Testing Slice Test-E (client/E2E meaningful coverage): added a populated Live Connections TCP-table E2E (real forwarded connection held open → populated row) and an API-failure error-banner E2E (`page.route` aborts `/api/connections` → `role="alert"`), and cleaned brittle selectors (removed `label.auto-refresh-toggle`, `.diag-panel-title`, `.diag-panel-body`). Tests-only; E2E 34/34 (run twice, no fixed sleeps); client coverage/gates unchanged; `validate:contract` 167/167. Source: `audits/v1.6-testing-audit-1.md`.

**Durable testing rule (Test-E):** E2E must cover critical user-visible workflows, not only empty states — keep at least one Live Connections **populated-state** test (real held connection → populated table row) and one **API-failure** regression (`page.route` abort → `role="alert"` banner, app stays usable). Prefer role/label/visible-text selectors over CSS-class selectors; reserve exact-copy assertions for short stable empty-state messages and contractually meaningful strings, and use `{ exact: true }` / role scoping to avoid strict-mode ambiguity. Socket/timing E2E uses real runtime + Playwright auto-waiting/deadline polling, never fixed sleeps; route interception is allowed only to force otherwise-undeterministic failure states (document it). When the UI lacks an accessible hook a test needs, prefer adding a minimal accessibility attribute over asserting a styling class.

v1.6 Coverage Slice D (Go service error paths): raised service coverage 88.5% → 90.3% (gate 88 → 90) with tests-only additions — no product code changed; `validate:contract` 167/167. New white-box tests (`service/sources/api/errorpaths_test.go`) cover API rejection/error paths: empty/failed body reads → 400, malformed JSON → 400, unknown rule PATCH → 404, generic manager error → 500 (via a `manager.NewWithStore` handler whose persist always fails — `os.MkdirAll` under a regular file, cross-platform), config-apply ImportConfig failure → 500, `NewHandler` option defaults, and `tryTCPBind`/`tryUDPBind` success+failure. Plus `forwarders/udp_errorpaths_test.go` (public `NewUDPForwarder` + `udpMode` nil-default via the Test-A `startUDPForwarderOnFreePort` helper), `static/static_test.go` (`ServeClient` asset vs SPA-fallback, `HasClient`), `manager/errorpaths_test.go` (`StartEnabled` skip-disabled, `hasIDIn` merge-ID-collision regeneration), `configplan/udpmode_test.go` (`udpModeEqual`/`udpModeVal` nil and non-nil branches), and one `options` positional-arg case. Structurally-unreachable/seam-requiring branches documented (not chased): crypto/rand fallbacks (`randomEventID`/`NewRuleID`/`generateConnectionID`), `config.Save` write/sync/rename injection (Go `os.Rename` replaces atomically, so the recovery path is dead), and the UDP forwarder read-loop send-error branches (`emitPacketError`, `handleMultiClientPacket`/`sessionReadLoop` write errors) which need a `net.Conn` injection seam not worth adding now. Source: `audits/v1.6-coverage-audit-1.md`.

**Durable testing rule (Coverage Slice D):** Service coverage must prioritize correctness/error paths — persistence failures, forwarder start/stop failures, API rejection paths (400/404/409/500), and rollback consistency — over line-hits. Do not chase structurally-unreachable socket internals (read-loop send errors, crypto/rand fallbacks, atomic-rename recovery) with invasive production-only seams; add a seam only if it improves production design, otherwise document the gap. Socket tests must use the Test-A stabilized port helpers and deadline polling — never fixed sleeps. Generic (non-typed) manager errors are reproduced cleanly via `manager.NewWithStore` with a persist-failing store path, not by mocking.

v1.6 Coverage Slice C (CLI command edge cases): raised CLI coverage 93.2% → 97.7% (gate 93 → 95) by adding failure-path/exit-code tests — no CLI behavior change, CLI stays a pure API client. New tests: a shared `failingWriter` exercising the `output.PrintJSON → "Error encoding JSON" → exit 1` branch in every JSON-emitting command (`commands/json_err_test.go`); white-box tests for `formatChangeValue` (nil/int-float/non-int-float/string/bool) and `opEndpoint` (current/desired/both-nil) in `commands/helpers_internal_test.go`; and a `run()` dispatch table asserting an invalid `--url` exits 2 for every subcommand (`main_test.go`). Structurally-unreachable CLI branches left uncovered and documented: `main()` os.Exit wrapper, `client.do` http.NewRequest/read-body errors, `client.doWithBody` and `writePrettyJSON` `json.Marshal(Indent)` errors (impossible with current DTOs — do not add fake unmarshalable types). Source: `audits/v1.6-coverage-audit-1.md`.

**Durable testing rule (Coverage Slice C):** CLI command coverage must include failure-path behavior, not only happy paths. Every JSON-emitting command must keep its `output.PrintJSON` encode-failure path tested (exit 1 + "Error encoding JSON"), and the config plan/diff/apply/export/validate commands must keep exit-code priority tested: local file errors (2), connection/API errors (3/1), plan errors (1), destructive-without-`--yes` (1 via 400), dry-run (0, no mutation), and drift with `--fail-on-drift` (4). Do not chase structurally-unreachable branches (os.Exit wrappers, `json.Marshal` errors on concrete DTOs) — document them instead.

v1.6 Slice B (post-Slice-B): service 88.6% (gate raised to 88%). 9 new Go tests for manager rollback and config error paths.

v1.6 Architecture Slice Arch-A (contract drift guard): aligned the Go `LAN_EXPOSURE` advisory message (`service/sources/advisory/advisory.go`) to the canonical TypeScript wording in `@portier/shared` (`shared/sources/index.ts`); advisory code/severity unchanged. `validate:contract` now also asserts advisory/plan **content** (message text), not just codes/shape: in-runtime canonical assertions plus a cross-runtime `compareParity` pass diffing normalized advisory and config-plan payloads field-by-field (timestamps/generated ids excluded). `validate:contract` 166/166 against both runtimes (was 156/156). No API shape/behavior change, no plan/apply semantic change, no coverage gate change. Treat `@portier/shared` as canonical for advisory wording; the Go service must match it. Source: `audits/v1.6-architecture-audit-1.md`.

v1.6 Architecture Slice Arch-B (apply orchestration extraction): config apply transformation moved out of the HTTP handlers into tested plan-engine helpers — `buildApplyImportFromPlan` (`server/sources/config-plan.ts`) and `BuildApplyImportFromPlan` (`service/sources/configplan/plan.go`). Behavior preserved exactly; `validate:contract` 166/166; server coverage 95.2/91.6 → 95.3/92.0, service unchanged; no gate change. Source: `audits/v1.6-architecture-audit-1.md`. Next: Arch-C (Go service safe dedupe — unify UUID generation, collapse repeated manager activity-emission blocks).

**Durable architecture rule (Arch-B):** Config apply orchestration must live beside the plan engine (`server/sources/config-plan.ts`, `service/sources/configplan/plan.go`), not inside HTTP handlers. HTTP handlers may enforce request/response concerns, confirmation (`yes`), dry-run, and status codes, but plan/apply transformation logic (deriving the desired replace list, id injection/preservation, applied counts) must remain in the tested config-plan/configplan helpers. The TypeScript and Go helpers must stay semantically mirrored; new apply behavior must add unit tests in both runtimes plus `validate:contract` parity assertions where externally observable. `validate:contract` is the parity guard for observable behavior.

v1.6 Architecture Slice Arch-C1 (Go ID dedupe + dead code): replaced the duplicate UUID generators `manager.randomID` and `api.newApplyRuleID` with one shared `domain.NewRuleID()` (`service/sources/domain/id.go`); removed a no-op `name` assignment in `api.go` `buildRuleLiveSummary`. Behavior preserved; `validate:contract` 166/166; coverage neutral; no gate change. Source: `audits/v1.6-architecture-audit-1.md`. Next: Arch-C2 (collapse repeated Go manager activity-emission blocks into one helper).

**Durable architecture rule (Arch-C1):** Go service rule ID generation must have one shared implementation (`domain.NewRuleID`). New rule IDs for manager-created rules and config-apply-created rules must use the same generator so the two paths cannot silently drift in format. Do not reintroduce per-package UUID helpers.

v1.6 Architecture Slice Arch-C2 (manager activity-emission dedupe): collapsed the 10 repeated rule-scoped emission blocks in `manager.go` into one `emitRuleEvent(eventType, severity, rule, message)` helper (create/update/delete/start/stop/error). Config-level events (export/import/import-failed) keep their own `emitActivity` calls (they carry `details`, no rule fields). Payloads preserved byte-for-byte; full-payload regression tests added in `manager_test.go`. `validate:contract` 166/166; service 88.6% → 88.5% (no gate change). Arch-C complete. Source: `audits/v1.6-architecture-audit-1.md`. Next: Arch-D (CLI DTO parity guard) or resume coverage push.

**Durable architecture rule (Arch-C2):** Rule-scoped Go manager activity events must be emitted through `emitRuleEvent` (so ruleId/ruleName/protocol shape cannot drift between call sites); non-rule-scoped config events stay on `emitActivity` with their own details. Activity payloads are user-visible diagnostics — any change must be covered by tests asserting type, severity, ruleId, ruleName, protocol, message, and details.

v1.6 Architecture Slice Arch-D (CLI DTO live-runtime parity guard): `validate:contract` now captures live JSON from both runtimes and strictly decodes it into the CLI DTOs via `TestCLIDTOContractParity` (`tools/cli/sources/client/contract_decode_test.go`, env-gated by `PORTIER_CLI_CONTRACT_FIXTURES`). Proves the CLI's third contract copy matches real runtime output, not just httptest mocks. `validate:contract` 167 passed; no coverage/gate change; CLI stays a pure API client. Arch-D completes the audit's contract-parity remediation. Source: `audits/v1.6-architecture-audit-1.md`. Next: resume coverage push.

**Durable architecture rule (Arch-D):** The CLI DTOs (`tools/cli/sources/client/client.go`) are a third copy of the REST contract and must be guarded against **live** runtime responses, not only httptest mocks. The CLI must stay a pure API client — it must not import server/service internals or become a second runtime. When a new API response family is added, update BOTH the `validate:contract` live capture (`scripts/validate-contract.js`) AND the CLI decode guard (`contract_decode_test.go`). `/api/connections` is out of CLI scope (no CLI DTO/command).

v1.6 Testing Slice Test-A (port-allocation stabilization): removed the EADDRINUSE/TOCTOU flake in socket-binding tests. `free*Port` helpers allocate→close→return a port; another parallel test/process can grab it before the forwarder/manager binds. Fix is test-only (Pattern C: bounded retry around the *bind step only*, only on EADDRINUSE, never the whole test). Helpers: `isAddrInUse` + `startTCPForwarderOnFreePort`/`startUDPForwarderOnFreePort` (`service/sources/forwarders/portretry_test.go`), `startRuleStable` (`service/sources/manager/portretry_test.go`, rebinds via `UpdateRule`), `startForwarderOnFreePort` (`server/sources/test-helpers.ts`). `scripts/validate-contract.js` `startServer` retries child bind on a fresh port and `waitForReady` bails on child exit. Migrated: Go `tcp_test.go`/`udp_test.go`/`manager_test.go` happy-path binds, TS `tcp-forwarder.test.ts`. Residuals (follow-up): TS `udp-forwarder.test.ts`, the api HTTP `/start` sites, E2E `port.ts` (E2E is serial — `workers:1` — so no intra-suite race). No coverage/gate change. Source: `audits/v1.6-testing-audit-1.md`.

**Durable testing rule (Test-A):** Tests must not rely on allocate-close-rebind port helpers when a listener handoff or bind-retry can be used. Socket readiness/ordering must use deadline polling, not fixed sleeps. EADDRINUSE flakes must be fixed at the helper/bind-operation level (bounded retry on a fresh port, only on EADDRINUSE) — never hidden by retrying whole tests or whole suites, and never by arbitrary sleeps. New socket-binding tests should build+start through the `startForwarderOnFreePort`/`startTCPForwarderOnFreePort`/`startUDPForwarderOnFreePort`/`startRuleStable` helpers (or an equivalent bounded bind-retry), and intentional bind-failure tests (which reserve a port to force EADDRINUSE) must stay outside those helpers.

v1.6 Fix Slice 3 (finish Test-A residual migration): consumed the bind-retry/live-handoff helpers in the two named TS residual suites. New TS helpers in `server/sources/test-helpers.ts`: `startRuleStable(manager, ruleId, freePort)` (mirrors the Go helper — retries `manager.startRule` on EADDRINUSE by rebinding the rule's `listenPort` via `updateRule`), `startTcpServerOnFreePort()` and `bindUdpSocketOnFreePort()` (bind to port 0 and return the LIVE listener/socket + actual port — no close-rebind window). Migrated: `server/sources/forwarders/udp-forwarder.test.ts` (forwarder listen binds → `startForwarderOnFreePort` via a local `startUdpForwarder`; UDP target/echo sockets → `bindUdpSocketOnFreePort`; several fixed-sleep session-count waits → `waitUntil` deadline polls) and `server/sources/forward-manager.test.ts` (manager `startRule` sites incl. the Test-D rollback tests → `startRuleStable`; TCP target servers → `startTcpServerOnFreePort`; removed the local allocate-then-bind `startTcpTarget`). Intentional-conflict tests left bypassing the helpers (UDP "start() rejects when port is already bound" with a live blocker; manager "logs rule.error … when startRule fails" with a live occupier; the merge-import rollback test already tolerates the race by asserting not-running). Tests-only; 58 migrated tests pass ×3; server 354 tests, 98.9/93.8/100 unchanged; `validate:contract` 171/171, `validate:config` 71/71, all gates unchanged. **Remaining accepted residuals (documented, bounded):** the HTTP `/start` sites in `server/sources/api.test.ts` and `service/sources/api/api_test.go` (the bind happens behind the HTTP handler and each test asserts the start-response shape, so a robust fix needs an HTTP-level recreate-on-EADDRINUSE helper — a larger, lower-value follow-up), and the E2E `tests/e2e/helpers/port.ts` allocate-close helpers (E2E is fully serial — `workers:1`, `fullyParallel:false` — so no intra-process race is possible; only a rare cross-process race remains, against a single dedicated server). Revisit trigger: if an HTTP `/start` or E2E EADDRINUSE flake is actually observed. Source: `audits/v1.6-audit-synthesis-and-fix-plan.md`.

v1.6 Fix Slice 4 (validate-contract.js scenario registry): the ~1090-line monolithic `runScenarios` in `scripts/validate-contract.js` is now 12 named scenario-group functions (`runRuntimeScenarios`, `runForwardsScenarios`, `runForwardLifecycleScenarios`, `runActivityScenarios`, `runConfigExportImportScenarios`, `runPortAdvisoryScenarios`, `runForwardDeleteScenarios`, `runConnectionsScenarios`, `runConfigPlanScenarios`, `runConfigApplyScenarios`, `runErrorEnvelopeScenarios`, `runDiagnoseScenarios`) driven by an ordered `scenarioGroups` registry; `runScenarios` is a slim wrapper building `ctx = { api, runtime, fixtureDir, state }` and iterating the registry, then the Arch-D CLI fixture capture last. Cross-group rule ids (`tcpId`/`udpId`/`udpDefaultId`) flow via `ctx.state`; group-local values (`exportedConfig`) stay local. Added `expectedRuntimeApiValue(label)` for the runtime-label→API-value mapping (Naming-D). Script-only; assertions verbatim; `validate:contract` 171/171 unchanged (the file uses intentional `\x00` sort-key separators in the `normalize*` helpers — preserve them on any future edit). Parity comparison (`compareParity`), CLI DTO guard, and child cleanup unchanged. Source: complexity/SOLID/pattern/duplication/readability audits.

**Durable rule (Fix Slice 4):** Contract scenarios must live in named `run<Area>Scenarios(ctx)` groups registered in the ordered `scenarioGroups` array — not in a monolithic `runScenarios` body. A new API endpoint family adds or extends a focused group + a registry entry; cross-group state flows via `ctx.state`, group-local state stays local. `validate:contract` scenario-count changes must be intentional and documented (171 at Slice 4; **183 at Slice 5**). `scripts/validate-contract.js` contains intentional embedded `\x00` bytes (sort-key separators) — edit with tools that preserve them (the file reads as "binary" to ripgrep).

v1.6 Fix Slice 5 (activity value parity guard): `validate:contract` now guards the activity taxonomy at the value level, not just `ActivityEvent` shape. Canonical `EXPECTED_ACTIVITY_TYPES` (17) / `EXPECTED_ACTIVITY_SEVERITIES` (4) in `scripts/validate-contract.js`; `runActivityValueSetParityChecks()` reads the TS union (`shared/sources/activity.ts`) and Go consts (`service/sources/activity/activity.go`) from source (binary-independent) and asserts both declared sets equal the canonical set; `runActivityScenarios` asserts every emitted type/severity ∈ allowed set + the representative `rule.*` pairs; the config-import group asserts the `config.imported`/`config.import.failed` (success/error) emission. Count 171 → 183. No product/API-shape/gate change; no event names changed.

**Durable rule (Fix Slice 5):** Activity event-type and severity values are contract values, not cosmetic labels. The canonical set is 17 types + 4 severities (`info`/`success`/`warning`/`error`); there is no `config.reordered`. Any new type/severity must be added consistently across `@portier/shared` (`shared/sources/activity.ts`), the Go consts (`service/sources/activity/activity.go`), `docs/api-contract.md`, the emitting code in both managers, and the `validate:contract` `EXPECTED_ACTIVITY_*` sets — together, in one change. `validate:contract` must keep asserting value-level membership AND cross-runtime source-set parity, not only shape. The CLI stays a pure API client (no activity-type logic).

v1.6 Fix Slice 6 (TS emitRuleEvent helper): the TypeScript `ForwardManager` now emits the 6 rule-scoped events (`rule.created`/`updated`/`deleted`/`started`/`stopped`/`error`) through a single private `emitRuleEvent(type, severity, rule, message)` helper (`server/sources/forward-manager.ts`), mirroring the Go `manager.emitRuleEvent` (Arch-C2) — payloads byte-identical, `activity?.add` optional-chaining preserved, `ActivityStore` still owns id/timestamp. The 5 config-level events (`config.exported`/`config.imported`/3× `config.import.failed`) stay on direct `activity?.add` (carry `details`, not rule-scoped). 3 full-payload regression tests added. No API/type/severity/message/order/count change; server branch 93.8 → 94.0, gates unchanged; `validate:contract` 183/183.

**Durable rule (Fix Slice 6):** Rule-scoped activity events must be emitted through the dedicated `emitRuleEvent` helper in BOTH runtimes (`server/sources/forward-manager.ts`, `service/sources/manager/manager.go`) so their `{type, severity, ruleId, ruleName, protocol, message}` payload cannot drift between call sites. Config-level events that carry a `details` map (or are not rule-scoped) stay on the direct `activity?.add`/`emitActivity` path unless a dedicated config-event helper is introduced in both runtimes. Activity payloads are user-visible diagnostics — any change to a rule-scoped emission must keep full-payload tests (type/severity/ruleId/ruleName/protocol/message) green in both runtimes, and value changes follow the Fix Slice 5 rule.

v1.6 Fix Slice 7 (config apply importConfig result.errors invariant): both apply handlers (`server/sources/api.ts`, `service/sources/api/api.go`) now inspect the `ImportResult` instead of discarding it. After import, if `result.errors` is non-empty → `200 ok:false` with the errors surfaced through the existing `plan.errors` field (code `IMPORT_ERROR`) + `plan.summary.hasErrors:true` + zero applied counts (no new `ConfigApplyResponse` field). Currently no `result.errors` path is reachable from apply (duplicate bindings pre-blocked by the plan engine's `detectDuplicateKeys`, invalid rules by plan validation, persist throws → 500, merge errors N/A in replace) — this is a belt-and-suspenders guard. Go construction extracted to the unit-tested `applyImportErrorResponse` helper (handler guard structurally unreachable without the deferred manager-interface seam; documented). `validate:contract` 183 → 185 (duplicate-binding apply parity). CLI unaffected (`applyExitCode` already maps `ok:false`→1). No API-shape/valid-behavior change; gates unchanged.

**Durable rule (Fix Slice 7):** Config apply must never report `ok:true` when the underlying import operation reports errors. Plan-level errors (including duplicate desired listen bindings — `protocol+listenHost+listenPort` — flagged by the plan engine) must stop apply BEFORE import and stay contract-covered. Import-level errors that surface after plan validation must be reported as `ok:false` via the existing `plan.errors`/`summary.hasErrors` envelope with zero applied counts, byte-for-byte parity across TS and Go — never a new response field, never a silent success. Both apply handlers must inspect the `ImportResult`, not discard it.

v1.6 Testing Slice Test-D (ForwardManager persist-failure rollback parity): the TypeScript `ForwardManager` previously mutated its in-memory rule map before `persist()` with no rollback, so a failed `store.save()` left in-memory rules inconsistent with `rules.json` and divergent from the Go manager (which rolls back). Fixed in `server/sources/forward-manager.ts`: `addRule`/`updateRule`/`deleteRule`/`reorderRules`/`importConfig` now restore prior state on a persist failure (and restart a forwarder that was stopped for a forwarding-field update, best-effort so a restart error cannot mask the persist error). Rollback parity is proven by `ControllableStore`-driven tests in `server/sources/forward-manager.test.ts` mirroring the Go `Test*PersistFailureRollsBack` tests. No API/forwarding/plan-apply change; rollback only triggers on disk-write failure. Source: `audits/v1.6-testing-audit-1.md`.

**Durable testing rule (Test-D):** Persist-failure paths are correctness paths, not optional edge cases. Both runtimes' rule managers (`server/sources/forward-manager.ts` and `service/sources/manager/manager.go`) must roll back so that a failed persist leaves NO partial in-memory or running-state mutation, for create / update / delete / reorder / import. Any new mutating manager method that persists must snapshot-and-restore on persist failure, and must have rollback tests in BOTH runtimes (TS via a controllable failing store; Go via `failingStore`). Runtime parity for these paths is mandatory — if the two runtimes ever intentionally diverge, document it explicitly in both test suites and here.

v1.6 Fix Slice 1 (TS replace-import duplicate-binding parity): TypeScript `ForwardManager.importConfig` now rejects duplicate listen bindings within the imported set for BOTH replace and merge modes via a module-level `ensureNoDuplicateBindings(rules)` mirroring the Go `manager.ensureNoDuplicateBindings` (same message wording), run after validation and before any mutation — no in-memory mutation, no persist, no forwarder start/stop, one `config.import.failed` (severity error). The HTTP layer maps `result.errors` → 422 `{errors, result}` in both runtimes. Config **apply** was already protected (the plan engine's `detectDuplicateKeys` flags intra-desired duplicate bindings → `summary.hasErrors` → apply returns `ok:false` before calling `importConfig`), resolving the duplicate-binding half of Resilience-C. `validate:contract` 167 → 171 (duplicate-binding parity scenario, both modes × both runtimes). No valid-import or merge-vs-existing behavior change; no gate change. Source: `audits/v1.6-audit-synthesis-and-fix-plan.md`.

**Durable rule (Fix Slice 1):** Config import (replace AND merge) must enforce the same duplicate listen-binding rule in both runtimes. A listen binding is `protocol + listenHost + listenPort`; two rules in the imported set with that key conflict even if their IDs differ, and the whole import is rejected (422, no mutation, no persist). Import parity changes require unit coverage in BOTH runtimes (TS `forward-manager.test.ts`, Go `manager_test.go`) plus a `validate:contract` parity scenario. Keep the TS and Go duplicate-binding message wording aligned. The CLI stays a pure API client (no duplicate-binding logic).

v1.6 Fix Slice 2 (TS ConfigStore atomic write parity): TypeScript `ConfigStore.save` (`server/sources/config-store.ts`) now writes crash-safely — unique same-directory temp file (`.portier-forwards-<pid>-<ts>-<rand>.tmp`) → `fsync` via FileHandle → atomic `rename` over `rules.json`, mirroring Go `config.Store.Save`. The previous file stays intact until rename; the temp is removed on any pre-rename failure (best-effort, never masking the original error); the persistence error propagates so `ForwardManager` rollback (Test-D) still runs. `load` unchanged. No remove-and-retry recovery branch (Node `fs.rename` replaces atomically on POSIX `rename(2)` and Windows `MoveFileExW REPLACE_EXISTING`); directory fsync omitted (not portable on Windows — same limit Go accepts). A minimal test-only `ConfigStoreFileOps` seam (optional 2nd constructor arg, defaults to real fs) allows forcing write/sync/rename failures without a real disk-full event; not exposed through any product/API path. `config-store.ts` 100/100/100; gates unchanged. Source: `audits/v1.6-resilience-audit-1.md`.

**Durable rule (Fix Slice 2):** Config persistence must be crash-safe in BOTH runtimes. Rules-config writes use same-directory temp file + `fsync`/flush where practical + atomic `rename`; a failed save must not corrupt the previous `rules.json`, and the original persistence error must reach the caller so manager rollback can execute. New persistence durability behavior needs failure-path tests in the owning runtime (TS via the `ConfigStoreFileOps` seam, Go via a temp/rename seam). Do not add a broad filesystem abstraction; keep the seam minimal (mkdir/open-write-sync-close/rename/remove). Directory-fsync metadata durability on Windows is an accepted documented limit in both runtimes.

v1.7 Slice 1 (validate-contract.js outer child-cleanup guard): `scripts/validate-contract.js` now tracks every spawned runtime child in a module-level `activeChildren` set (added in `startServer`, removed on the child's `exit` event and in `cleanup`) and kills any survivors via a synchronous `killAllChildren()` wired to `process.on("exit")`, `SIGINT`/`SIGTERM` (kill then exit 130), and the outer `main().catch` (kill then exit 1). This is the deferred Resilience-F outer-guard that Fix Slice 4 left out. The primary ordered `cleanup(ts)`/`cleanup(go)` `try/finally` path is unchanged — the guard only adds the failure-path net (unexpected throw, early exit, never-ready startup, interrupt signal) and makes per-runtime cleanup idempotent. Script-only; `\x00` sort-key separators preserved; `validate:contract` 185/185 unchanged; no product/API/scenario/gate change. Source: `audits/v1.6-resilience-audit-1.md` (Resilience-F).

**Durable rule (v1.7 Slice 1):** Any script that spawns runtime child processes (`validate-contract.js`, and future `validate:*` runners) must terminate them on ALL exit paths, not only the happy-path `try/finally`. Track live children in a set and kill them from a synchronous guard wired to `process.on("exit")`, the termination signals (`SIGINT`/`SIGTERM`), and the top-level promise `.catch`. The ordered per-child `cleanup()` stays the primary path; the guard is the belt-and-suspenders net and must be idempotent. Do not regress this into a single un-guarded `try/finally`.

v1.7 Slice 3 (Go Forwarder interface + StartRule dedupe): the Go manager starts/stops/inspects rules through a small shared `forwarders.Forwarder` interface (`Start() error`, `Stop()`, `Status() domain.ForwardStatus`) built by the `forwarders.NewForwarder(rule, log, onEvent, tcpReg, udpReg)` factory (`service/sources/forwarders/forwarder.go`). `*TCPForwarder`/`*UDPForwarder` already satisfy it — the interface adds shape, not behavior, so `tcp.go`/`udp.go` were untouched. `manager.runtimeState` holds one `forwarder forwarders.Forwarder` (not separate `tcp`/`udp` pointers); `StartRule`/`stopRuntime`/`statusForRule`'s running branch are single-path. The protocol switch lives ONLY in `NewForwarder` (unknown protocol → nil → manager no-op). The not-running synthetic status in `statusForRule` keeps its per-protocol shape (that is API-response shape, not lifecycle). Behavior/contract unchanged; `validate:contract` 185/185; `NewForwarder` 100% covered; service 90.3→90.1 (gate 90 PASS). Source: `docs/roadmap.md` v1.7.

**Durable rule (v1.7 Slice 3):** Go rule lifecycle in the manager must go through the `forwarders.Forwarder` interface + `NewForwarder` factory — the manager must not reacquire per-protocol forwarder pointers or duplicate per-protocol start/stop construction. Protocol dispatch belongs in `NewForwarder` (one switch); a new forwarding protocol adds a case there and a type satisfying `Forwarder`, not another branch in `StartRule`/`stopRuntime`. Keep the interface minimal (start/stop/status) — do not grow it into a framework or push API-response-shape concerns into it. The TypeScript server is not required to mirror this internal structure (no cross-runtime parity obligation for it), but any new observable forwarding behavior still needs `validate:contract` parity.

v1.7 Slice 4 (TS UDP emit facade): the TypeScript UDP forwarder builds all activity events through a small module-private `UdpEventEmitter` facade (`server/sources/forwarders/udp-forwarder.ts`), constructed once from `rule` + `onEvent`. Methods: `packetError(message)` (no details), `packetForwarded`/`packetReturned`/`sessionOpened`/`sessionClosed`(message, details); one private `emit(type, severity, message, details?)` stamps `ruleId`/`ruleName`/`protocol:"udp"` and dispatches via the optional `onEvent`. Throttle timing, `status.lastError` mutation, and session/registry bookkeeping stay in the forwarder body. Payloads byte-identical (same fields/order, `details` omitted when absent). Facade is NOT exported (UDP-specific; TCP has its own events) — tested through the public forwarder, not by importing the class. 2 full-envelope regression tests added; `validate:contract` 185/185; server 98.9/94.0/100 → 98.9/94.1/100 (gates unchanged). Source: `docs/roadmap.md` v1.7.

**Durable rule (v1.7 Slice 4):** Repeated UDP (and similarly repetitive per-protocol) activity-event construction must go through a small co-located emit facade so the `{type, severity, ruleId, ruleName, protocol, message, details?}` envelope cannot drift between call sites — do not re-spell the full event literal at each emission. Keep such a facade module-private and close to its one forwarder unless it is genuinely reused (TCP and UDP have distinct event sets — do not force a shared cross-protocol emitter or an event bus/observer framework). The facade owns ONLY event construction + dispatch; throttle timing, `status`/`lastError` mutation, and session/registry bookkeeping stay in the forwarder. Emitted payloads must stay byte-identical (fields, order, `details` presence) and value changes follow the Fix Slice 5 activity-taxonomy rule. Prove facade output with full-envelope tests through the public forwarder; do not couple tests to the private facade name. This is a TS-internal structural choice — no Go parity obligation, but observable event changes still need `validate:contract`.

v1.7 Slice 5 (diagnose check-phase helper split): `diagnoseRule` in BOTH runtimes (`server/sources/diagnose.ts`, `service/sources/api/diagnose.go`) is reduced to an explicit ordered phase list of one-check helpers — `checkListenHost`/`checkLanExposure`/`checkPrivilegedPort`/`checkCommonPort`/`checkListenBind`/`checkTargetHost`/`checkTargetConnect`/`checkUdpMode` (UDP only) — each returning one `DiagnosticCheck` built verbatim. `targetResolved` derives from the `target-host` check's pass status (not a separate mutable flag). I/O helpers and `buildSummary` unchanged. Output byte-identical (IDs/labels/severities/order/messages/details); `validate:contract` 185/185. Added one ordering regression test per runtime (the prior find-by-id tests + contract ID-membership did NOT guard exact order). Source: `docs/roadmap.md` v1.7.

**Durable rule (v1.7 Slice 5):** The diagnose flow must stay a flat, explicit ordered list of small named per-check phase helpers in BOTH runtimes, kept structurally mirrored (split/extend in lockstep — they cannot drift). A new diagnostic check is a new `checkX` helper inserted at the right position in the `diagnoseRule` phase list in both `diagnose.ts` and `diagnose.go`, plus its `validate:contract` assertion and the per-runtime ordering test — NOT inline appended logic, and NOT a generic rule-engine/registry abstraction. Each phase helper returns exactly one `DiagnosticCheck` and must keep its ID/label/severity/message/details byte-identical across runtimes; check order is observable contract — keep the ordering regression tests (`diagnose.test.ts` "check ordering", Go `diagnose_phases_test.go`) and the contract ID assertion green. Do not derive `targetResolved` from anything other than the target-host check's pass status.

v1.7 Slice 6 (CLI config loader/mapper cleanup): the CLI config commands (`tools/cli/sources/commands/config.go`) share the read→parse→validate prelude via `loadConfigForCommand(filePath, verb, errExit, stderr) ([]rawConfigRule, int, bool)` (used by import/plan/diff/apply — import errExit 1, plan/diff/apply errExit 2; stderr wording unchanged) and the rule→DTO mapping via `toConfigRules([]rawConfigRule) []client.ConfigRule` (used by `buildPlanRequest`/`buildApplyRequest`/`buildImportRequest`). `RunConfigValidate` keeps its own JSON-aware parse-error flow (does not use the helper). Output formatters and `writePrettyJSON` already isolated. CLI behavior/exit codes/output byte-identical; CLI stays a pure API client; `validate:contract` 185/185; CLI coverage 97.7→97.9 (gate 95 PASS); all 5 helpers 100% covered by existing black-box tests. Source: `docs/roadmap.md` v1.7.

**Durable rule (v1.7 Slice 6):** CLI config commands must share their file load+parse+validate prelude (`loadConfigForCommand`) and their local-rule→`client.ConfigRule` mapping (`toConfigRules`, via the `build*Request` helpers) — do not re-inline `os.ReadFile`+`parseLocalConfig`+`validateLocalConfig` or the field-by-field DTO loop per command. New config commands reuse these helpers (pass the command's verb + error exit code). `RunConfigValidate` is the documented exception (JSON-aware parse-error output) — leave it bespoke unless a JSON-aware variant is added deliberately. Per-command exit-code semantics (import/plan/diff/apply local-input file errors → 2 — see Slice 7; plan errors → 1; drift+`--fail-on-drift` → 4; connection → 3) and exact stderr/stdout wording are observable CLI behavior and must stay byte-identical through any refactor — guard with the existing black-box `commands_test` behavior tests; do not add brittle private-helper tests. The CLI stays a pure API client (no server/service imports, no DTO changes).

v1.7 Slice 7 (CLI exit-code normalization review): audited all CLI command exit codes and fixed one clear inconsistency — `config import` local file read/parse/validation errors `1`→`2`, matching `config apply`/`plan`/`diff` (one-line change: `loadConfigForCommand(…, "import", 2, …)`). Documented exit-code policy in `tools/cli/readme.md`. Three import local-error tests tightened `!= 0`→`== 2`. No other command changed; `validate`'s `1`-for-invalid/unreadable is the documented validator-semantics exception. CLI behavior otherwise unchanged; `validate:contract` 185/185; coverage gate 95 PASS. Source: `docs/roadmap.md` v1.7.

**Durable rule (v1.7 Slice 7) — CLI exit-code policy:** `0` success; `1` API/runtime/operation error (server rejection/error, local output-**write** failure) and `validate`'s invalid/unreadable result; `2` invalid arguments/usage AND invalid local **input** config file (unreadable/malformed/validation failure) for `config import`/`plan`/`diff`/`apply`; `3` connection failure (service unreachable); `4` drift with `--fail-on-drift`. Intentional (documented, not to be "fixed"): local **input** errors are `2` but local **output**-write failures are `1`; `config validate` reports invalid/unreadable as `1` (its exit code is its result) while missing the path arg is `2`; a rule `<id|name>` matching nothing is `1` (target absent, like a 404) while an ambiguous name is `2` (fixable selector). New CLI commands MUST follow this policy; route API/connection errors through `exitWithError` (→ `1`/`3`); use `2` for bad local input and `1` for output/operation failures. Changes to a command's exit codes are observable behavior — update `tools/cli/readme.md` + the per-command help and add/adjust a black-box `commands_test` assertion in the same change.

v1.7 Slice 8 (SettingsView decomposition): `client/sources/features/settings/SettingsView.tsx` (was 736 lines) is now a ~90-line orchestrator composing per-panel components (`ExportConfigSection`/`PlanApplySection`/`ImportConfigSection`/`RuntimeEnvironmentSection`/`DiagnosticsExportSection`) over a shared `SettingsSection` wrapper, with logic in hooks (`useRuntimeInfo`/`useClipboardCopy`/`useConfigExport`). Each stateful panel owns its own state; the one cross-panel concern (config export, shared by the Export panel + the Import backup button via the `exporting` flag) is created once in `SettingsView` via `useConfigExport()` and passed to both. DOM byte-identical; JSX/classes/aria/onClick moved verbatim; `SettingsView.test.tsx` (63 tests) unchanged and green; client gates 94/89/78 unchanged; settings E2E 6/6. Source: `docs/roadmap.md` v1.7.

**Durable rule (v1.7 Slice 8) — settings/feature-view decomposition:** A feature view that has grown multiple stateful concerns must be a thin orchestrator composing focused panel components, NOT one giant component. Each panel owns its own state; lift state to the parent ONLY for a genuine cross-panel concern (e.g. the shared config-export `exporting` flag) and pass it down — do not prop-drill an entire view's state. Extract a hook (`useX`) when logic + state is cohesive and reusable/testable on its own (runtime fetch, clipboard, export flow); extract a small presentational primitive (`SettingsSection`) for repeated wrapper markup. Decomposition must be DOM-preserving: move JSX, class names, `aria-label`/`role`/`id`/`htmlFor`, and `onClick` expressions **verbatim** — no relabeling, no structure change, no accessibility regression. Because the tests render the composed top-level view and assert via role/label/text, a correct decomposition needs NO test changes; if a test must change to pass, the DOM changed — re-check. Keep new component files CamelCase and hooks camelCase under the feature folder. This is client-internal — no API/contract impact, but run the feature's E2E spec when it has one.

v1.7 Slice 9 (UI wording consistency pass): audited client UI copy against `docs/glossary.md`; fixed one glossary-backed inconsistency — the sidebar nav label for the Live Connections view was "Connections", now "Live Connections" (`client/sources/app/NavItem.ts`), matching the view title + glossary. Updated the `tests/e2e/connections.spec.ts` `goToConnections` helper (nav button name → "Live Connections"; arrival now confirmed via the unique `tablist`/"Connection views" role, since the title text is no longer unique). Rest of the UI already glossary-aligned (Listen/Target Host/Port; Running/Stopped/Error; TCP Connections/UDP Sessions; Active/Idle; Diagnose action vs Diagnostics Export bundle; Autostart label for `enabled`). "API Docs" (nav/header) vs "API Reference" (page heading) intentionally left (no glossary anchor; label-vs-heading is acceptable). No behavior/API/DTO change; `validate:contract` 185/185; client gates unchanged; full E2E 34/34. Source: `docs/roadmap.md` v1.7, `docs/glossary.md`.

**Durable rule (v1.7 Slice 9) — UI wording:** User-facing client labels (nav, titles, field labels, buttons, status, help text) must use `docs/glossary.md` canonical terms. A view's nav label should match its title/canonical term when the glossary anchors it (e.g. "Live Connections", not bare "Connections"); a short nav label MAY differ from a more descriptive page heading only when no glossary term applies and both are unambiguous. Keep observed vs desired state distinct (running/stopped/error vs enabled; "Autostart" = the UI label for `enabled`), TCP "connection" vs UDP "session", and "Diagnose" (per-rule action) vs "Diagnostics export" (bundle). Do NOT rename frozen public surfaces for cosmetic UI consistency (`/api/forwards`, `listenHost`/`targetHost`, `clientAddress`/`targetAddress`, the `connections` view *id*). A visible label change is observable behavior: update any test/E2E that selects by that text in the same change, and prefer role-based selectors when a renamed label becomes non-unique (e.g. nav button + view title sharing text → assert a unique `role`).

v1.8 Slice 1 (rule group metadata contract): added an optional `group` label to forward rules as the v1.8 Operator Power Tools data foundation — `group?: string` on the shared `ForwardRule`/`ConfigPlanRuleSnapshot` (`shared/sources/index.ts`, `plan.ts`), `Group *string json:"group,omitempty"` on Go `domain.ForwardRule`/`ForwardRuleResponse`/`configplan.RuleSnapshot`, and `group` on the CLI `ConfigRule`/`ConfigPlanRuleSnapshot`/`ForwardRuleResponse` DTOs (all `omitempty`). Validation is identical and parity-tested in both runtimes (`validateGroup` in `@portier/shared`; `validateGroup`/`normalizeGroup` in `service/sources/validation`): optional; trimmed; empty/whitespace → absent; ≤ 64 chars (`PORTIER_GROUP_MAX_LENGTH`/`domain.GroupMaxLength`); no control chars (C0 + DEL); non-string rejected. PATCH: `""` clears, non-empty sets, absent/`null` unchanged (Go can't distinguish JSON `null` from absent, so TS matches it). `group` is a plan **material** field (group-only change = `update`) but **not** a forwarding field (non-destructive, no restart) — added to `MATERIAL_FIELDS` (TS) and the Go `diffMaterialFields` group branch, kept OUT of `FORWARDING_FIELDS`/`forwardingFields`. Preserved through export/import and plan/apply (snapshot builders + `buildApplyImportFromPlan`/`BuildApplyImportFromPlan` + `toRuleResponse`). Runtime forwarding/lifecycle/duplicate-binding/status unchanged. `validate:contract` **185 → 204** (self-contained `rule-group` scenario group + `config import→export` roundtrip + cross-runtime `plan:GROUP_CHANGE` parity; CLI DTO guard green). No UI group editing yet, no CLI group commands/filtering yet (deferred). No coverage gate changed. Source: `docs/roadmap.md` v1.8, `docs/api-contract.md`, `docs/glossary.md`.

**Durable rule (v1.8 Slice 1) — rule group metadata:** Rule `group` is **optional, behavior-neutral metadata** until group operations are introduced — it must NOT affect forwarding, lifecycle, start/stop/reorder, duplicate-binding (still `protocol + listenHost + listenPort` only), or status. Config import/export/plan/apply must **preserve** `group` in BOTH runtimes; an ungrouped/legacy rule omits the field entirely and must produce no drift. `group` validation (optional, trimmed, empty→absent, ≤ 64 chars, no control chars, non-string rejected, same error wording) must stay **parity-tested** across TypeScript (`@portier/shared`) and Go (`service/sources/validation`) — add cases in both plus a `validate:contract` scenario when the rule changes. In a plan, `group` stays a material but non-destructive field (in `MATERIAL_FIELDS`, never in `FORWARDING_FIELDS`). PATCH semantics are frozen: `""`/whitespace clears, non-empty sets, absent/`null` leaves unchanged. The CLI stays a pure API client (forward `group`, light local pre-validation, no group-specific logic). Do NOT widen this into tags/profiles/multi-group/colors or rename the frozen `group` field for cosmetic reasons.

v1.8 Slice 2 (rule group UI editing and display): surfaced `group` in the web UI — client-only, no shared/server/service/CLI code touched, `validate:contract` 204/204. Rule form (`client/sources/features/forwards/RuleForm.ts` + `ForwardRuleForm.tsx`): optional **Group** input under Name; `group: string` added to `RuleFormState`/`emptyForm`/`ruleToForm`/`formToPayload`; `formToPayload` always sends raw `group` so the Slice 1 PATCH contract handles set (non-empty) / clear (empty → `""`) / create-normalize; `maxLength={PORTIER_GROUP_MAX_LENGTH}`; explicit `aria-label="Group"` because the always-present hint `<span>` inside the `<label>` would otherwise pollute the accessible name (same fix the Listen Host field uses). Client-side `validateForwardRule` already rejects over-long/control-char groups on submit (server stays authoritative). Rule list (`ForwardRuleList.tsx`): subtle `.rule-group-label` chip beneath the name when present (no placeholder when absent), and the search box now matches `group`. Tests: 5 form + 3 list unit tests, 1 E2E (`tests/e2e/portier.spec.ts`: create-with-group → chip + API, edit-clear → gone + API). Client gates 94/89/78 unchanged. Source: `docs/roadmap.md` v1.8.

**Durable rule (v1.8 Slice 2) — rule group UI:** When surfacing an optional metadata field in the rule form, drive it through `RuleForm.ts` (`RuleFormState` + `emptyForm`/`ruleToForm`/`formToPayload`), not ad-hoc component state, and **always send the raw value** in `formToPayload` so the Slice 1 PATCH contract does the work (empty string clears on edit / normalizes to absent on create; non-empty sets) — do not special-case "clear" in the client. A form input whose `<label>` also contains a persistent hint/help `<span>` MUST carry an explicit `aria-label` (the label's full text content, hint included, otherwise becomes the accessible name and breaks `getByRole("textbox", { name })` and screen-reader output) — mirror the Listen Host field. Display group as a subtle, secondary chip (`.rule-group-label`) that does not overpower status/protocol/endpoints, render nothing when absent (no empty placeholder), and keep server-side validation authoritative (the client `maxLength`/`validateForwardRule` checks are convenience only). Reuse the existing rule search for group matching; do NOT add group filtering/grouping/bulk UI in this slice. This is client-internal — no API/contract/coverage-gate change.

v1.8 Slice 3 (group filtering and grouping groundwork): added a **Filter by group** `<select>` to the Forward Rules list (`ForwardRuleList.tsx`) — client-only, read-only navigation, `validate:contract` 204/204. Options derived from the current rules (distinct trimmed group names, `localeCompare` `sensitivity:"base"` sort) + `All Groups` + `Ungrouped` (only when ungrouped rules exist); the control renders only when ≥1 rule has a group. Sentinels `ALL_GROUPS`/`UNGROUPED` (wrapped `__…__` tokens, cannot collide with real groups). Filter applies in the same `filteredRules` pass as search+status (AND-combine); a derived `activeGroupFilter` clamps a stale selection back to All Groups (no out-of-range `<select>` value, never hides all rules from a deleted group); drag-reorder disabled while a group filter is active. Empty states unchanged ("No rules match the current filter."). Tests: 8 new list unit tests + 1 E2E; one Slice 2 chip assertion scoped to the table since the group name now also appears as an `<option>`. Client gates unchanged. Source: `docs/roadmap.md` v1.8.

**Durable rule (v1.8 Slice 3) — rule list group filter:** Group filtering is **read-only client navigation** — it must not mutate rules or touch the API/runtime. Derive group options from the live `rules` prop (distinct, trimmed, locale-insensitive sorted); never persist them separately. Apply the group filter in the same single `filteredRules` predicate as the existing search/status filters so all three AND-combine; keep the existing empty-state messages. Always **clamp a stale specific-group selection** to "all" via a derived value (a controlled `<select>` must never hold a value with no matching `<option>`, and a vanished group must not silently filter out every rule). Offer "Ungrouped" only when an ungrouped rule exists, and render the whole control only when at least one group exists (no dead single-option control). Use sentinel tokens that cannot collide with a real (non-empty, trimmed) group for the non-group selections. Keep it a native labelled `<select>` (keyboard-accessible, `aria-label`); do not add visual grouping headers, group start/stop, bulk actions, colours, or filter persistence in this slice. When a list value (like a group) now appears in BOTH a row chip and a filter option, scope chip assertions to the table (e.g. `.rule-group-label` / `within(table)`) so they stay unambiguous. Client-internal — no API/contract/gate change.

v1.8 Slice 4 (group operations API): added `POST /api/forwards/groups/:group/{start,stop}` in BOTH runtimes — start/stop all rules sharing a `group`. Behaviour over existing metadata: never mutates rule definitions/order/`enabled`/`group`/duplicate-binding; per-rule lifecycle + activity events identical to single-rule start/stop (no new event type). `ForwardManager.startGroup`/`stopGroup` (TS) + `Manager.StartGroup`/`StopGroup` (Go) iterate the matched rules in **rule order**; start ignores `enabled` (matches single-rule start), skips already-running (`already_running`), partial-fails one rule without aborting the rest (`failed` + reason); stop skips not-running (`not_running`). No match → **404**; invalid group → **400** (group-operation targets must be non-empty: `validateGroupName` in `@portier/shared`, `validation.ValidateGroupName` in Go, both add `group is required.`). Response `GroupActionResponse {group,action,total,succeeded,skipped,failed,results:[{ruleId,ruleName,status,reason?}]}` — shared `groups.ts` + `summarizeGroupAction`, mirrored by Go `domain.GroupActionResponse` + `buildGroupActionResponse`. Route nests under `/api/forwards/groups/:group/...` (4 segments, no collision with `:id/start`); Go parses the escaped path so an encoded `/` in a group stays one segment. Client `setGroupRunning` added as groundwork (NO UI wiring); `ApiDocsView` documents both endpoints. `validate:contract` **204 → 225** (`group-action` group + `group:start` parity). No UI/CLI group commands. Source: `docs/roadmap.md` v1.8.

**Durable rule (v1.8 Slice 4) — group operations:** Group start/stop are **behaviour over existing rule metadata, NOT config mutation** — they must never change rule definitions, order, `enabled`/autostart, `group`, or duplicate-binding, and must reuse the existing single-rule start/stop path (same per-rule activity events; do NOT invent a group-level event type unless the activity model gains one deliberately in both runtimes). Result ordering MUST follow the manager's **rule order** (deterministic); the summary is `{group, action, total, succeeded(=started/stopped), skipped, failed, results[]}` with per-rule `status` + optional `reason` (`already_running`/`not_running` skip tokens, or the error message for `failed`). Start ignores `enabled` (parity with single-rule start); a single rule's start failure is a `failed` result, never an aborted operation (partial success). An empty match is a **404**, not a silent success. Group-operation path targets must be validated as **non-empty** group labels (`validateGroupName`/`ValidateGroupName`, message `group is required.`) in addition to the normal length/control-char rules. TS and Go group-operation behaviour + response shape + error behaviour MUST stay contract-tested (`validate:contract` `group-action` group and `group:start` parity); add scenarios in lockstep when behaviour changes. Keep the shared `summarizeGroupAction` and Go `buildGroupActionResponse` counting mirrored. The CLI stays a pure API client (no group commands/DTOs this slice); the client `setGroupRunning` method is groundwork only — no UI wiring until a later slice. Ungrouped bulk actions are intentionally deferred.

v1.8 Slice 5 (CLI group commands): exposed the Slice 4 group API through the Go CLI — `portier group list|start|stop <group>` (singular `group`, matching `config`). CLI-only, pure API client, `validate:contract` 225/225. `group list` derives groups from `GET /api/forwards` + `GET /api/status` (alphabetical, rule+running counts; `No rule groups configured.` exit 0 when none); `group start`/`stop` call `POST /api/forwards/groups/:group/{start,stop}` and print a summary + `RULE/STATUS/REASON` table (`--json` = full `GroupActionResponse`). Exit codes (v1.7 Slice 7 policy): 0 success incl. skips, 1 API error / no-match 404 / `failed>0`, 2 missing/invalid group, 3 connection. Group arg trimmed + locally validated (non-empty/≤64/no-control via `validateGroupArg` reusing `groupMaxLength`/`hasControlChar`), server authoritative. New `client.StartGroup`/`StopGroup` + `GroupActionResponse`/`GroupActionResult` DTOs (`url.PathEscape` for the group segment); `group` wired into `main.go` + root help. Arch-D guard extended: CLI fixture capture starts a dedicated grouped rule → `group-start.json` strictly decoded into the CLI DTO (main capture rule stays ungrouped so plan/apply stay drift-free). Black-box `commands_test` for all paths; `main_test` dispatch table + `group`. CLI gate 95% holds. Source: `docs/roadmap.md` v1.8.

**Durable rule (v1.8 Slice 5) — CLI group commands:** The CLI group commands are a **pure API client** over the Slice 4 endpoints — they MUST NOT reimplement group semantics, mutate rules, or talk to anything but `GET /api/forwards`, `GET /api/status`, and `POST /api/forwards/groups/:group/{start,stop}`. `group list` is derived client-side from the live rules+status (alphabetical, distinct non-empty trimmed groups, rule+running counts) — never persisted; an empty result prints a clear message and exits **0**. Exit codes follow the v1.7 Slice 7 policy and the Slice 4 semantics: a **skip** (`already_running`/`not_running`) is success (**0**); `failed>0` OR the API's no-match **404** is **1**; a missing/invalid local group argument is **2** (validate non-empty/≤`groupMaxLength`/no-control locally with `validateGroupArg`, but let the server stay authoritative — do not duplicate server-only checks); unreachable is **3** (via `exitWithError`). `--json` must emit the full `GroupActionResponse` for start/stop and `{groups:[{group,total,running}]}` for list. New CLI DTOs are a contract copy — keep them guarded by the Arch-D `TestCLIDTOContractParity` (capture a live `group-start.json`; keep the main CLI-contract capture rule **ungrouped** so plan/apply fixtures stay drift-free). Prefer black-box `commands_test` over private-helper tests. Do not add group rename/delete, ungrouped bulk, profiles, tags, or colours.

v1.8 Slice 6 (UI group action buttons): exposed group start/stop in the web UI — client-only, `validate:contract` 225/225. A compact toolbar (`Start group "<name>"` / `Stop group "<name>"`) appears under the rule-list header (`ForwardRuleList.tsx`) only when **Filter by group** is set to one concrete group (hidden for All Groups + Ungrouped, and only when an `onGroupAction` prop is supplied). Click → `App.handleGroupAction` → `setGroupRunning` (`POST /api/forwards/groups/:group/{start,stop}`) → `refreshAll`; per-action loading (`Starting…`/`Stopping…`, both buttons disabled = no double-submit); inline summary `Started group "web": N succeeded, N skipped (N total)` (`role="status"`), warning when `failed>0` (`role="alert"`), server error message on rejection/404 (`role="alert"`). Result cleared when the group selection changes. 11 `ForwardRuleList` + 2 `App` unit tests + 1 E2E. Client gates unchanged. Source: `docs/roadmap.md` v1.8.

**Durable rule (v1.8 Slice 6) — UI group actions:** Operator bulk-by-group actions in the web UI are a thin client over the Slice 4 endpoints (via `setGroupRunning`) — no reimplemented group semantics, no rule mutation, no API/DTO change. Only surface group start/stop for **one concrete group** (the active `Filter by group` selection) — never for All Groups or Ungrouped (there is no ungrouped bulk endpoint). After a successful action, **refresh rule/status data through the existing `refreshAll`/`onRefresh` pattern** (do not hand-mutate local state). Show a **scoped inline result** near the buttons (`role="status"` for success/skip, `role="alert"` for `failed>0` or an error) using existing error conventions — do NOT add a new global notification system; clear the result when the group selection changes. Prevent double-submit by disabling both buttons while a request is in flight. Group-action buttons must carry the **group-scoped accessible name** (`Start group "web"`) so they stay unambiguous. Keep the rule list otherwise unchanged (no redesign). The component keeps the action behind an optional `onGroupAction` prop so non-action usages render no toolbar; the API call + refresh live in the parent (`App`), the local busy/result UI lives in the list. No profiles/tags/colors/rename/management screen; this is client-internal — no API/contract/coverage-gate change.

v1.8 Slice 7 (rule health basics): added an operator-facing `health` (`healthy`/`warning`/`error`) on `ForwardStatus`, derived deterministically from existing runtime state — `validate:contract` 225 → 234 (`status:health` parity). Shared `RuleHealth` + required `health` on `ForwardStatus` + `deriveRuleHealth({enabled,running,lastError})` (`@portier/shared`); Go `domain.RuleHealth` + `DeriveRuleHealth` + `Health` on `domain.ForwardStatus`. The **manager** is the single place that derives health (it owns `enabled`): TS `ForwardManager.getStatus` (forwarders now return a health-less `ForwarderStatus` = `Omit<ForwardStatus,"health">`), Go `Manager.statusForRule`. Priority: `error` if `lastError` present; else `warning` if `enabled && !running`; else `healthy`. UI: `RuleHealthBadge` dot (green/amber/red, `role="img"` + label/tooltip) — since v1.8 Slice 9 in its **own `Health` column** on the rule list (dot + one-word `healthShortLabel`, `aria-hidden`), distinct from the unchanged Status column. CLI: `portier status` HEALTH column + `health` in the DTO/`--json`. ~30 existing `ForwardStatus` fixtures gained `health`. No probing, no background check, no rule mutation; `lastError` lifecycle unchanged. Source: `docs/roadmap.md` v1.8.

**Durable rule (v1.8 Slice 7) — rule health:** Rule `health` is a **derived, deterministic interpretation** of existing runtime state (`enabled`/`running`/`lastError`) — it must NEVER imply active target probing or background monitoring unless such probing is explicitly implemented (this slice implements none). Keep `health` and `status`/`running` **distinct**: status is lifecycle, health is the operator reading; do not collapse them or replace the status badge with health. The single derivation helper is `deriveRuleHealth` (`@portier/shared`) / `domain.DeriveRuleHealth` (Go) — keep the two byte-identical in logic and **parity-tested** (`validate:contract` `status:health` + the `rule-health` scenario); the priority is fixed (`error` > `warning` > `healthy`). Health is computed by the **manager** (which has the rule's `enabled`), not by forwarders — TS forwarders return `ForwarderStatus` (`Omit<ForwardStatus,"health">`) and the manager adds `health`; Go forwarders leave `Health` zero and `statusForRule` sets it. Do not move health derivation into the forwarders, the API handlers, or the client. The client/CLI must **read** `health` from the status response, never recompute it (one source of truth). Health adds no network I/O and never mutates a rule. Do not add health history, a metrics endpoint, target reachability polling, auto-restart, or retry in this slice.

v1.8 Slice 8 (duplicate rule action): client-only duplicate-rule convenience — `validate:contract` 234/234 unchanged (no shared/server/service/CLI/API/DTO/config change, no backend endpoint). Each rule row has a **Duplicate** action (`Copy` icon, accessible name `Duplicate rule <name>`) that opens the existing drawer in **create mode** pre-filled from the source. `ForwardRuleForm` gained an optional `duplicateSource` prop (used only when not editing; edit wins), title "Duplicate Rule" + "New rule copied from …" subtitle, drawer aria-label "Duplicate Forward Rule"; `ForwardRuleList` gained optional `onDuplicate` (button renders only when provided); `App` tracks `duplicateSourceId`, derives the source rule, clears it on every form open/close path, and keys the drawer `dup-<id>`. Prefill via `ruleToDuplicateForm` (`RuleForm.ts`): copies editable fields (protocol, listen/target host+port, udpMode, **group**), name → `<name> copy` (`duplicateName`), **drops id** (saves via create `POST`, not PATCH) and **forces `enabled:false`** (a created enabled rule auto-starts via `addRule`→`startRule`; a duplicate must not auto-start). Runtime-only state (status/`lastError`/health/active counts) is inherently excluded (never in the form). Validation unchanged — duplicate-binding caught by the normal create path if the user doesn't change listen host/port/protocol. Four pre-existing E2E selectors that matched action buttons by loose substring name (`{name:"Edit"}` ⊂ "Duplicate rule E2E Edit Original") were tightened to `exact:true`. Source: `docs/roadmap.md` v1.8.

**Durable rule (v1.8 Slice 8) — duplicate rule:** Duplicate-rule is a **client-only create-flow convenience** — it must NOT add a backend duplicate endpoint, mutate the source rule, or change any API/DTO/config shape (the duplicate saves through the existing create `POST /api/forwards`, `id` undefined). A duplicate prefill (`ruleToDuplicateForm`) copies only the editable definition fields **including `group`**, sets the name to `<name> copy`, **drops the id**, and **forces autostart (`enabled`) off** — never copy runtime-only state (id/status/`lastError`/health/active connections/sessions), and never let a duplicate auto-start on save (a created `enabled` rule starts immediately). The duplicate must NOT bypass or special-case validation: it passes the same client + server validation as any new rule, so an unchanged listen binding surfaces the normal duplicate-binding error — do not pre-resolve or auto-increment ports. Open it through the existing rule drawer in **create mode** (distinct title/subtitle so it's clearly a new rule, not an edit) via the optional `duplicateSource` prop, ignored when `editingRule` is set; keep the row action behind an optional `onDuplicate` prop and give it the **rule-scoped** accessible name `Duplicate rule <name>`. When a new always-present row action's accessible name embeds the rule name, audit existing tests/E2E that select sibling action buttons by **loose** substring name (`{name:"Edit"}` etc.) and make them `exact:true` — a rule named after an action word will otherwise collide. No rule templates/saved templates/profile cloning/group-level duplicate/management screen. This is client-internal — no contract/coverage-gate change.

v1.8 Slice 9 (config preview UX polish): client-only polish of the Settings **Plan & Apply** preview (`PlanApplySection.tsx` + new helpers in `planHelpers.ts` + scoped CSS) — `validate:contract` 234/234, no API/DTO/config or plan/apply semantics change. Summary gained a labelled `Destructive: N` count. Each operation renders as a card: action badge (Add/Update/Remove/Unchanged), rule name + protocol, a plain-language impact note (`describeOperationImpact`), and text tags `Destructive` (from `op.destructive`) / `Metadata only` (from `isMetadataOnlyUpdate`). Each change row shows a friendly field label (`formatFieldLabel`), a `forwarding`/`metadata` tag (`changeImpact`), and `before → after`. Impact for updates is derived from the **actual changed fields** via a client-side mirror of the server's `FORWARDING_FIELDS` (`protocol`/`listenHost`/`listenPort`/`targetHost`/`targetPort`/`udpMode`) — a group/name/autostart-only edit reads "Metadata only — the forwarder is not restarted" and never implies a restart. Server stays authoritative for the `destructive` Apply-gating; no-drift/error/warning/confirmation/apply states unchanged. Source: `docs/roadmap.md` v1.8.

**Durable rule (v1.8 Slice 9) — config preview UI:** The config plan/apply preview is a **read-only presentation of the existing plan response** — it must NOT change plan/apply semantics, the `destructive` gating, or any API/DTO/config shape, and must keep the server authoritative for `destructive` (the Apply button still gates on `summary.destructive` + the confirmation checkbox). Destructive and metadata states must be conveyed by **text** (badges/tags + a plain-language impact note), never colour alone (accessibility). A group-only (or any non-forwarding) update must be shown as **metadata-only** and must **not imply a socket restart** — derive an update's restart/impact from the **actual changed fields**, using a client-side mirror of the server's `FORWARDING_FIELDS` set (`protocol`/`listenHost`/`listenPort`/`targetHost`/`targetPort`/`udpMode`); if that set changes server-side, update the `planHelpers.ts` copy in the same spirit (it is display-only — do not import server internals, and do not move classification into shared/server for this). Keep the preview's pure logic in `planHelpers.ts` (`formatFieldLabel`/`changeImpact`/`isMetadataOnlyUpdate`/`describeOperationImpact`) with unit tests, and keep `PlanApplySection` rendering thin. Preserve the existing no-drift/in-sync, plan-error (apply disabled), warning, destructive-confirmation, and apply success/failure/`ok:false` states and their `role="alert"`/`role="status"` semantics. No full redesign, no diff-viewer, no new config workflow. Client-internal — no contract/coverage-gate change.

**Durable rule (v1.8) — Forward Rules row action menu:** The Forward Rules row keeps **only Start/Stop as an inline icon button**; the remaining per-row actions — **Edit, Duplicate, Diagnose, Activity, Delete** (in that order, Edit first) — live in a per-row **overflow ("kebab", `MoreVertical`) menu** (`ForwardRuleList.tsx`) opened by a `More actions for <rule name>` trigger (`aria-haspopup="menu"` + `aria-expanded`). The trigger is **seamless — styled like the reorder drag handle** (`.row-menu-trigger`: no button border/background, muted glyph brightening on hover / `aria-expanded`), NOT a boxed `btn-icon`. The dropdown is `role="menu"` (labelled `Actions for <rule name>`) of `role="menuitem"` buttons; **Delete keeps the red `row-menu-item--danger` styling** and still triggers the existing inline Confirm/Cancel delete prompt (do not replace that confirm flow with a bare menu click). Only one row menu is open at a time; it closes on **outside click and Escape** (a single document listener gated on the open id); the trigger is disabled while the row is busy or mid delete-confirm. When adding or moving a row action: keep Start/Stop inline, give menu items plain text names (`Edit`/`Duplicate`/`Diagnose`/`Activity`/`Delete`) with their icon, and remember that **tests must open the menu first** and target `role="menuitem"` (not a row button) — both unit (`openRowMenu` helper) and E2E (click the `More actions for <name>` trigger, then the `menuitem`). This is client-internal — no API/contract/coverage-gate change.

v1.10 Slice 2 (policy finding explanations): extended the policy toolkit with deterministic explanations for policy finding codes, reusing the v1.9 `explain`/inline-`--explain` UX — CLI-only, `validate:contract` 234/234, CLI coverage 97.8% (gate 97). Architecture: extracted the shared `Explanation` type + render helpers (`For`/`Sorted`/`SortedCodes`/`ForReport`/`PrintInline`/`Merge`) out of `doctor` into a new **leaf** package `tools/cli/sources/explain` (imports nothing internal). Each domain owns its explanation DATA keyed by its own code constants — `doctor` keeps its 20-code registry (`doctor.Explanations()`), `policy` adds its 6-code registry (`policy.Explanations()`, in `policy/explain.go`); the `explain` command merges them via `explain.Merge(doctor.Explanations(), policy.Explanations())`. `portier explain <code>` and `explain --list` now cover all **26** codes (sorted: `config.*` < `policy.*` < `rules.*`/`runtime.*`); doctor explanations are unchanged. `portier policy check --explain` adds an inline `Code:`/`Meaning:`/`What to do:`/`Related:` block per emitted finding (human, via `explain.PrintInline`) and an additive top-level `explanations` map (deduped by code, via `explain.ForReport`) under `--json` through a new `policy.Emit`/`EmitOptions`/`reportJSON` (mirrors `doctor.Emit`; `Explanations` is `omitempty` so non-`--explain` JSON is byte-identical). `--explain` never changes findings/summary/result/exit code. Policy explanations frame each finding as a **policy choice** (the operator's `allow*`/`require*` guardrail), offer both resolutions (change the rule OR adjust the policy), and never claim auto-enforcement/remediation. Doctor `--explain` stays byte-identical (same `explain.Explanation` JSON shape + `explain.PrintInline` rendering = old `printCheckExplanation`); support-bundle `explanations.json` stays doctor-scoped (`doctor.SortedExplanations()` → 20). Registry parity guarded per-domain (`doctor/explain_internal_test.go`, `policy/explain_internal_test.go`: every code explainable, no orphan/dup, Code==key, fields/severity valid, no dangling/self `related`). `commands` stays handlers-only. `explain` added to `coverpkg`. Added offline AI prompt `prompts/policy.md`. Source: `docs/roadmap.md` v1.10.

**Durable rule (v1.10 Slice 2) — policy/doctor explanations:** Operator-facing **codes must have explanations** — every stable doctor/check code AND every policy finding code MUST have a registry entry, guarded per-domain (`doctor/explain_internal_test.go`, `policy/explain_internal_test.go`); adding a code without an explanation must fail a test in the SAME slice. There is **one** `Explanation` type and **one** set of render helpers, in the leaf package `tools/cli/sources/explain` (`For`/`Sorted`/`SortedCodes`/`ForReport`/`PrintInline`/`Merge`) — do NOT introduce a second explanation schema or a duplicate inline renderer. Each domain OWNS its explanation **data** keyed by its own code constants (no string duplication); the `explain` command composes domains via `explain.Merge(...)` (this is dispatch-level composition in `commands`, not domain logic — domain explanation data/logic must NOT live in `commands`). `explain` is a **leaf**: it must import nothing internal (so `doctor`/`policy` can import it without a cycle). Inline `--explain` (doctor, config doctor, policy check) must reuse the canonical registry via `explain.PrintInline`/`explain.ForReport`; it must **not change** which checks/findings run, the report, the summary, the result, or any **exit code** — the JSON `explanations` map is additive (`omitempty`, set only with `--explain`, byte-identical output without it), and human inline blocks come after each finding. Explanations are static offline reference text — they must **never claim Portier enforces a policy or fixes/probes anything**, and a policy explanation must frame the finding as a **policy choice** (an `allow*`/`require*` guardrail), offering both resolutions (change the rule OR adjust the policy), not necessarily a product defect. Doctor `--explain` output (human + JSON shape) must stay byte-identical through this refactor; the support bundle's `explanations.json` stays doctor-scoped (`doctor.SortedExplanations()`), not the merged 26. Codes sort alphabetically (`config.*` < `policy.*` < `rules.*`/`runtime.*`). New CLI domains that emit codes add their own registry + a parity guard and register with the `explain` command's merge — do NOT grow this into a central registry framework. CLI stays a pure API client; no API/DTO/contract change. (Prior `doctor` references to `Explanation`/`ExplanationFor`/`SortedExplanationCodes` are **superseded** — the type lives in `explain`; doctor exposes `Explanations()`/`SortedExplanations()`.)

v1.10 Slice 3 (policy report export): added `--out <file>` to `portier policy check` — writes the policy report to a file as deterministic pretty JSON for CI/reviews/support handoff, reusing the v1.9 doctor `--out` convention. CLI-only, fully offline, `validate:contract` 234/234, CLI coverage held 97.8% (gate 97, not raised — ~0.8% buffer too tight to reach 98 safely). The exported file uses the SAME `reportJSON` shape as `--json` (`findings`+`summary`+`result`, plus the additive `explanations` map under `--explain`) — no second schema. `policy.EmitOptions` gained `OutPath`; `policy.Emit` now builds the payload once, prints to stdout (JSON if `--json`, else human with a `Report written to <file>` confirmation), then if `OutPath != ""` writes the same payload via `output.WritePrettyJSON` — so `--json --out` and `--json --explain --out` stdout/file are byte-identical (same bytes by construction; `output.PrintJSON` encoder + `WritePrettyJSON` MarshalIndent+`\n` match). `commands/policy.go` parses `--out` (flag-parse error on a missing value → exit 2) and threads it through; `commands` stays handlers-only. Exit: 0 passed, 1 violations OR `--out` write failure (operation failure, `Error writing` on stderr, overrides the report code), 2 usage/missing-`--out`-value/unreadable-or-malformed config or policy. A malformed/unreadable config or policy still exits 2 and writes NO file (parse fails before `Emit`); the file is only written after a successful marshal (no partial writes); parent dirs not created; config/policy never mutated; no runtime access/probe/telemetry. Covered by black-box `policy_export_test.go` (write+result, `--json`/`--explain` file byte parity, write-failure→1, missing-`--out`→2, malformed-config/policy-no-file, no-mutation). Source: `docs/roadmap.md` v1.10.

**Durable rule (v1.10 Slice 3) — policy report export:** Policy report export (`policy check --out`) must write the **same JSON schema as `policy check --json`** (the `reportJSON` `{findings, summary, result}` shape, plus the additive `explanations` map under `--explain`) — never invent a second policy-report schema, and reuse `output.WritePrettyJSON` (pretty, create/truncate, no parent-dir creation, no partial write on marshal failure). Exporting a report must **not mutate** the config or policy file, must **not contact the runtime** or probe targets, and adds no telemetry/upload. A **file-write failure is an operation failure → exit 1** (`Error writing` on stderr) that overrides the report's own exit code; a missing `--out` value is a usage error → exit 2; an unreadable/malformed config or policy still exits 2 and **writes no file** (no success-looking report — parse must fail before any write). `--out` writes the file regardless of `--json`; keep stdout valid JSON in `--json` mode (no confirmation line) and only print the `Report written to <file>` confirmation in human mode — and the **same bytes** that go to stdout under `--json` must be byte-identical to the file (build the payload once, write it both places: `--json --out` and `--json --explain --out` parity). Report payload construction stays in the `policy` package (`policy.Emit`/`EmitOptions.OutPath`), file writing in `output`, and `commands` stays handlers-only. This mirrors the v1.9 doctor `--out` rule; the two `--out` conventions must stay aligned. The CLI stays a pure API client; no API/DTO/contract change, no support-bundle/redaction scope in this slice.

v1.10 Slice 4 (policy templates): added built-in policy templates so operators can generate/inspect common safe-operations policies without hand-writing the schema — CLI-only, `validate:contract` 234/234, CLI coverage 97.8% → 97.6% (gate 97 PASS, not lowered). New `portier policy template <name>` / `--list` (`RunPolicyTemplate` in `commands/policy.go`, dispatched offline from the existing `RunPolicy`). Registry + metadata + rendering live in the `policy` package (`policy/template.go`): `Templates()` (sorted copy), `FindTemplate(name)`, `TemplateNames()`, `Template{Name,Title,Description, file}` with `PolicyValue()` (bare policy file = exact `Parse` schema) / `DetailValue()` (`{name,title,description,policy}` wrapper), and `PrintTemplateList(jsonOutput, w)`. Three templates, sorted: `local-safe` (allowLan/allowPriv off, autostart on, no group), `managed` (requireGroup on; allowLan/allowPriv/autostart off), `permissive` (Portier's default — only forbidDuplicateBindings on). All `schemaVersion:1`, only the five boolean guardrails — **no protocol allowlist/denylist, no `allowUdp`** (UDP first-class). Output: `template <name>` → bare policy JSON; `--json` → wrapper; `--list` → human list (name col + title + indented description) / `--list --json` → `{templates:[{name,title,description}]}` (no embedded policy); `--out <file>` → writes the **bare** policy JSON regardless of `--json` (via `output.WritePrettyJSON`) so the file is directly usable by `policy check` (human mode prints `Policy written to <file>`, `--json` keeps stdout the pure wrapper). Flags work before AND after the name via an **interspersed flag-parse loop** (`template managed --out f`), unlike the flags-before-file commands. Exit: 0 success, 1 `--out` write failure, 2 usage (unknown template — lists available — / missing name / too many / missing `--out` value / `--list` with a name or `--out`). Fully offline; modifies nothing but `--out`. Guarded by white-box `policy/template_internal_test.go` (each renders to a schema-1 policy `Parse` accepts + `Evaluate` runs; sorted/unique; no udp/protocol token; guardrail booleans locked to purpose) + black-box `commands/policy_template_test.go`. `template.go` 100% covered; the dip is the unreachable `json.Marshal`-on-concrete-DTO encode branches in `RunPolicyTemplate`. Source: `docs/roadmap.md` v1.10.

**Durable rule (v1.10 Slice 4) — policy templates:** Built-in policy templates must render to the **exact same policy schema `policy check` accepts** (`schemaVersion: 1` + the five boolean guardrails) — a rendered/`--out` template must be directly usable by `policy check` with no editing. Templates must be **deterministic and sorted by name** (`Templates()` returns a sorted copy regardless of definition order) and live in the `policy` package (`policy/template.go`: registry, metadata, `PolicyValue`/`DetailValue`/`PrintTemplateList`); **template definitions must NOT live in `commands`** (it stays handlers-only — it only parses flags, resolves the template, and calls `output` for file writing). Templates must **never introduce a protocol allowlist/denylist or an `allowUdp` rule** (UDP is first-class) and must not add new guardrails — they are starter files over the existing schema, guarded by a registry test (each parses+evaluates, sorted/unique, no `udp`/`protocol` token, guardrail booleans locked to each template's documented purpose). The `--out` file is the **bare** policy JSON (no metadata wrapper) regardless of `--json`, written via `output.WritePrettyJSON` (no parent-dir creation, no partial write); a write failure → exit 1, a missing `--out` value / unknown or missing template / `--list`+name or `--list`+`--out` → exit 2. Fully offline — no runtime contact, no probing, modifies nothing but `--out`. Adding a template = a registry entry + its locked-booleans test case; do NOT grow this into a template-file loader, user-supplied template directory, or parameterized generator. CLI stays a pure API client; no API/DTO/contract change.

v1.10 Slice 5 (policy-aware config review): added `portier policy review --current <file> --candidate <file> --policy <file>` — a fully offline, dry-run review that compares a current config with a candidate config and evaluates ONLY the candidate against a policy. CLI-only, `validate:contract` 234/234, CLI coverage 97.6% (gate 97 PASS, not lowered). Model/eval/render live in the `policy` package (`policy/review.go`): `Review` (`Change ReviewSummary json:"review"` + embedded `Report` so `findings`/`summary`/`result` stay top-level — no second finding schema), `ReviewSummary{Current,Candidate config.Summary, Delta ReviewDelta}`, `ReviewDelta` (scalar candidate−current: ruleCount/enabledRuleCount/disabledRuleCount/groupCount/ungroupedRuleCount), `BuildReview(current, candidate, pol)` (reuses `config.BuildSummary` + `Evaluate`), `PrintReviewHuman`, and `EmitReview` (mirrors `Emit`: reuses `EmitOptions`, additive `explanations` via `explain.ForReport`, `output.WritePrettyJSON` for `--out`, `--json`/`--out` byte-identical). Policy-check and review human renderers now share extracted `printFindings`/`printSummaryAndResult` in `policy.go` (policy check output byte-identical). The handler `RunPolicyReview` (+ `loadReviewConfig` helper) in `commands/policy.go` is dispatched offline from `RunPolicy`; `commands` stays handlers-only. Human output: `Portier Policy Review` + `Config changes:` block (`Rules: 2 → 3 (+1)` using the existing CLI `→` arrow, all five count lines with `%+d` deltas) + the candidate's findings/summary/`Result:`. Exit: 0 candidate passes, 1 candidate violates (or JSON-encode / `--out` write failure), 2 usage / unreadable-or-malformed current/candidate config or policy. Never mutates configs/policy, never contacts the runtime, never probes, no import/apply. Covered by black-box `policy_review_test.go`. Source: `docs/roadmap.md` v1.10.

**Durable rule (v1.10 Slice 5) — policy-aware config review:** `policy review` must be **dry-run and fully offline** — it reads the current config, candidate config, and policy file, and must NEVER mutate any of them, contact the runtime, probe targets, or use config import/apply. It evaluates **only the candidate** against the policy and must **reuse the `policy check` finding/report semantics** (`policy.Evaluate` + the shared `printFindings`/`printSummaryAndResult` rendering + the embedded `Report`) — do NOT introduce a second finding/report/explanations schema (the JSON is `{review, findings, summary, result}` with an additive `explanations` map under `--explain`, exactly like `policy check`). The change summary must stay **compact and deterministic** — scalar `current`/`candidate` (`config.BuildSummary`) + a scalar `delta`; this slice is explicitly **not** a full config diff engine (do NOT add per-rule/field diffing, rename detection, or a diff DSL here). Review model/build/render live in the `policy` package (`policy/review.go`); config summaries reuse `config.BuildSummary`; file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only (parse flags, load files, call `policy.BuildReview`/`EmitReview`). Exit codes follow the policy-check family: 0 candidate passes, 1 candidate violates OR a JSON-encode/`--out` write failure, 2 usage error or unreadable/malformed config or policy. `--explain`/`--out`/`--json` behave exactly as in `policy check` (additive explanations never change findings/summary/result/review/exit; `--json --out` byte-identical). CLI stays a pure API client; no API/DTO/contract change, no apply/import/enforcement/remediation.

v1.10 Slice 6 (runtime policy check): added `--runtime` to `policy check` so operators can evaluate the **live runtime config** against a policy without exporting it first — read read-only via the existing `client.ExportConfig()` (`GET /api/config/export`), evaluated with the SAME `policy.Evaluate` + report shape as offline `--config`. CLI-only, **no new endpoint**, `validate:contract` 234/234, CLI coverage 97.6% (gate 97 PASS, not lowered). Exactly one config source: `--config` (offline) XOR `--runtime`; both/neither → exit 2; `--policy` required and validated BEFORE any runtime I/O. Offline `--config` mode stays fully offline (never resolves/contacts the runtime URL) — runtime mode lazily resolves the URL from a new `commands.ConnFlags{URL,Host,Port}` (passed from `main.go`'s `policy` dispatch) and builds the client only when `--runtime` is set. Added an additive `source` field to the policy-check report: `Source string json:"source,omitempty"` on `reportJSON` + `Source` on `policy.EmitOptions`, with `policy.SourceConfigFile`/`SourceRuntime` consts; `PrintHuman` gained a `source` param (prints `Source: <x>` under the title). Offline `policy check --json` WITHOUT a source is byte-identical to before (omitempty); `policy review` is unaffected (`source` lives only on the policy-check wrapper, not on `Report`). The command owns the source selection: `loadCheckRules(useRuntime, configPath, conn, stderr)` returns `(rules, source, code)` and `exportedRules(*client.ConfigExportResponse) []config.Rule` maps the export 1:1 into config domain rules (so a runtime config evaluates identically to the same file). Runtime-unreachable → exit 3 and config-export/API failure → exit 1 via the existing `exitWithError` (ConnectionError→3, else→1) — NEVER a fabricated policy finding/report. `commands` stays handlers-only; the `policy` package stays free of live-client behavior (the offline/runtime split). Covered by black-box `policy_runtime_test.go` (httptest export server). Source: `docs/roadmap.md` v1.10.

**Durable rule (v1.10 Slice 6) — runtime policy check:** `policy check` must support exactly one config source — `--config <file>` (offline) XOR `--runtime` (live) — with both/neither a usage error (exit 2). Runtime mode is **read-only and dry-run**: it reads the runtime config ONLY through the existing read-only config-export client path (`client.ExportConfig()` / `GET /api/config/export`) — **no new API endpoint**, no target probing, no enforcement/remediation, and no mutation of runtime/config/policy. It must **reuse the exact offline evaluator and report semantics** (`policy.Evaluate` + the shared rendering + the same `reportJSON`) — runtime and offline produce identical findings/summary/result/explanations for equivalent config contents; do NOT add new policy codes or a second report schema for runtime mode. **Offline `--config` mode must stay fully offline** — it must NOT resolve or contact the runtime URL (lazily resolve `ConnFlags` ONLY in the `--runtime` branch); the policy package must stay free of live-client behavior (the command layer owns the runtime fetch via `loadCheckRules`/`exportedRules` and lazy client construction; `commands` stays handlers-only). The `--policy` file is validated BEFORE any runtime I/O (invalid input → 2 takes precedence over runtime errors). A runtime-unreachable condition exits **3** and a config-export/API failure exits **1** (the standard live-CLI `exitWithError` convention) — a runtime read failure is an operation/runtime error, **never** wrapped as a policy finding, and no report is fabricated when the config could not be read. The additive `source` field (`"config file"`/`"runtime"`) is `omitempty` and must keep offline `policy check --json` byte-identical when no source is set, and must not leak onto `policy review` (`source` lives on the policy-check `reportJSON` wrapper, not on `Report`). `--json`/`--out`/`--explain` behave exactly as in offline check. CLI stays a pure API client; no API/DTO/contract change.

v1.12 Slice 6 (workflow history toolkit consistency audit): focused audit of the v1.12 workflow history/observability toolkit (Slices 1–5) — audit/cleanup only, no new features. **Result: PASS, v1.12 history toolkit ready for finalization.** Verified the command surface (built-binary battery against a redirected `APPDATA`/`XDG_CONFIG_HOME` config dir + the white-box suite): `workflow run --record-history` (passed/failed/invalid-plan-no-record) and `workflow history list/stats/show/export/prune/clear` dispatch consistently and are all OFFLINE (a malformed `--url` is ignored — `RunWorkflowHistory` takes no `ConnFlags`). JSON schema consistency (list `{schemaVersion,filters?,shown,totalStored,runs}`; stats `{schemaVersion,filters?,totalStored,shown,results,steps,workflows[],stepTypes[],codes[]}`; export `{schemaVersion,createdAt,source,runCount,runs,safety}` with 8 all-false safety flags + `--json --out` byte parity; prune `{schemaVersion,removed,kept,totalBefore,totalAfter}`; arrays non-nil/`[]`-when-empty; `filters` only when active; no full reports/messages/raw configs/runtime URLs/tokens in stored/exported entries). Exit codes (list/stats 0 incl. empty; show 0/unknown-1/missing-2; export 0/usage-2; prune 0 incl. removed-0 / no-mode·`--keep`+filter·missing-`--yes`-2; clear 0/missing-`--yes`-2; NO exit 3 anywhere — history never contacts the runtime). Safety/architecture (code-inspection): `workflow/history.go` imports ONLY `output` — no `client`/`commands`/`os/exec`/`net/http`/`os.Getenv`; only `run --record-history` writes history (after a completed run); prune/clear require `--yes`; mutations touch only the compact store or the explicit export `--out`; no workflow execution / referenced-file reads / raw-report scanning / logs-env-process collection / telemetry / upload / sync; `commands` stays handlers-only with model/projection/filter/stats/prune/store in the `workflow` package; no import cycles; `go vet` clean. Four small fixes (no behavior change): (1) stale `workflow history` help usage line `<list|show|clear>` → `<list|stats|show|export|prune|clear>`; (2) `--json` options note now lists `list/show/stats/prune`; (3) `--yes` options note now reads "the destructive operation (prune, clear)"; (4) the `workflow run` `--record-history` help + the glossary "Workflow run history" row now list the full subcommand set. `validate:contract` 234/234; CLI coverage 97.8% (gate 97 PASS, none lowered, not raised — buffer over 97 meaningful but still short of a stable 98%); validate:cli/lint/typecheck/gofmt/vet clean. No release blockers, no deferred non-blockers. No new durable rule — the audit confirms the Slice 1–5 rules. Source: `docs/roadmap.md` v1.12.

v1.14 Slice 9 (volatile read-endpoint parity pattern — migrate `GET /api/runtime`): migrated the **first endpoint with volatile fields** (`uptimeSeconds`, process `pid`/`platform`/`arch`) into Nest — `server/sources/nest/api/runtime/` — establishing the time/process provider/parity strategy before the deferred volatile endpoints `/api/connections` (`generatedAt`) and `/api/config/export` (`exportedAt`). CLI/UI/Go/replay untouched; `validate:contract` **234/234**. **Endpoint choice:** `GET /api/runtime` (the slice's preferred candidate) — read-only, compact, no runtime-manager dependency, and it forces the migration to solve volatile/process metadata. **Shared pure builder (no drift):** extracted `buildRuntimeInfo(input)` + `normalizePlatform`/`normalizeArch` + `RuntimeInfoOptions` into a NEW framework-free module `server/sources/runtime-info.ts`; **both** the Express route AND the Nest `RuntimeService` call this single builder, so the runtime-info shape cannot diverge. The builder is PURE — its volatile inputs (`now`, `pid`, `platform`, `arch`) are passed in by the caller (Express passes `new Date()`/`process.*`; Nest passes provider values) — and it preserves the historical Express defaults (`"unknown"` version, `PORTIER_DEFAULT_HOST`/`PORTIER_DEFAULT_PORT`, empty paths, `serviceMode:false`) and the exact contract field order (`name`→`version`→`runtime`→`platform`→`arch`→`uptimeSeconds`→`startedAt`→`managementHost`→`managementPort`→`configPath`→`staticDir`→`serviceMode`→`pid`). The Express change is behavior-preserving: the route now delegates to `buildRuntimeInfo`, `runtimeStartedAt` is STILL resolved once at app creation (so the no-`runtimeInfo` `startedAt` fallback stays app-creation time, not request time — the builder takes the resolved `startedAt` + `now` as SEPARATE inputs, it does NOT re-resolve the fallback), and `api.ts` re-exports `normalizePlatform`/`normalizeArch`/`RuntimeInfoOptions` for backward compatibility (only `api.ts`/`api.test.ts` referenced them). **Volatile provider/token pattern** (three narrow readers in `runtime.reader.ts`): `ClockReader`/`CLOCK_READER` (`now(): Date`, deliberately GENERIC — designed to be reused by `/api/connections` `generatedAt` and `/api/config/export` `exportedAt`), `ProcessReader`/`PROCESS_READER` (`pid()`/`platform()`/`arch()`), and `RuntimeInfoReader`/`RUNTIME_INFO_READER` (`options(): RuntimeInfoOptions | undefined` + `startedAt(): Date` — the scaffold default returns no options and a start time captured ONCE at construction, mirroring Express's app-creation `runtimeStartedAt`). Production providers (`defaultClockReader`/`defaultProcessReader`/`createDefaultRuntimeInfoReader`) read the real clock/process; the scaffold has no runtime info wired. `RuntimeService` composes the three readers → `buildRuntimeInfo`; `RuntimeController` maps via `toRuntimeInfoResponseDto` (response DTO = `RuntimeInfo`, structural copy). **NO request DTO** — the endpoint has no query/route/body input (documented exception). **Exact parity strategy (no normalization, no field stripping):** added a MINIMAL, optional, production-invisible clock seam to Express `AppOptions` — `now?: () => Date`, defaulting to `() => new Date()` (the same kind of test-only seam as the existing `runtimeInfo.startedAt`; production never passes it, so behavior is unchanged). The parity test boots BOTH the real Express app (`createApp(manager, { runtimeInfo: FIXED_INFO, now: () => FIXED_NOW })`) AND the Nest app (overriding `CLOCK_READER` → fixed `now`, `RUNTIME_INFO_READER` → fixed options+startedAt, leaving `PROCESS_READER` as the REAL default), and asserts `diffApiResponses(express, nest) === []`. Because `pid`/`platform`/`arch` are the real process values (identical in-process for both apps) and the clock + start time are pinned, `uptimeSeconds` is fully deterministic (`30`) and the booted-Express-vs-booted-Nest diff is byte-for-byte empty — NO field is stripped or normalized before comparison, NO fake timers, NO global `process` mutation. The scaffold-default Nest app returns real-process runtime info (`version:"unknown"`, real `pid`/`platform`/`arch`, live `uptimeSeconds >= 0`, parseable ISO `startedAt`). `GET /api/ports/advisory`, `GET /api/activity`, `DELETE /api/activity`, `GET /api/status`, `GET /api/forwards` parity, `/health`, the `/api/*` 404 envelope, and non-API no-leak all re-confirmed; the existing Express `GET /api/runtime` tests pass unchanged (the refactor is output-identical). **Every new/changed file is 100% covered** — `runtime-info.ts` (builder all-branches + `normalizePlatform`/`normalizeArch` all-branches, via `runtime-info.test.ts`) and the runtime reader/service/controller/module/response DTO (statements/branch/functions); 144 nest tests (11 added) + 6 `runtime-info` unit tests. Server coverage 99.0%/95.6%/100% (gate 95/92/99 PASS — no gate lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 540, client 449, all 12 Go packages). **Replay validation was NOT run** — no replay/shared-validation files were touched (per the v1.14 validation-scope correction). **Still scaffold-only:** served only under `npm run start:nest`; Express remains the default active runtime and its `GET /api/runtime` serves identical output. Deferred: `/api/connections`/`/api/config/export` (now have a proven volatile provider/parity pattern to reuse), rule mutation/lifecycle write endpoints, config import/export, static serving (Milestone 3+). Source: `docs/roadmap.md` v1.14.

**Durable rule (v1.14 Slice 9) — volatile read-endpoint migration & exact parity:** A migrated Nest endpoint with **volatile fields** (timestamps, uptime, pid, `generatedAt`/`exportedAt`) must get those values from **narrow injected readers** — `ClockReader`/`CLOCK_READER` (keep it GENERIC and reuse it across timestamp endpoints — do NOT make a per-endpoint clock), `ProcessReader`/`PROCESS_READER`, `RuntimeInfoReader`/`RUNTIME_INFO_READER` — never read `Date`/`process` inline in the service (the service must stay pure and deterministically testable). Pick the SMALLEST interface the endpoint needs. Parity MUST be **byte-for-byte with NO field normalization and NO field stripping** — never delete/ignore a volatile field before comparing, never assert only the non-volatile subset, and never claim parity if it requires ignoring fields. Prefer the first parity path: **inject the same fixed volatile values into BOTH Express and Nest** so a booted-Express-vs-booted-Nest HTTP diff is deterministic. To do that without changing production behavior, (a) extract a **shared pure builder** that both the Express route and the Nest service call (e.g. `buildRuntimeInfo` in `server/sources/runtime-info.ts`) so the two runtimes cannot drift on shape/defaults/field-order, AND (b) add a **minimal, optional, production-invisible seam** to Express `AppOptions` (e.g. `now?: () => Date`, default = the real clock — the same pattern as `runtimeInfo.startedAt`) so the otherwise-volatile field can be pinned in tests; leave process metadata as the REAL default in both (identical in-process). Do NOT use fake timers or mutate global `process` state. When extracting a shared builder out of Express, the refactor MUST be behavior-preserving (preserve the historical defaults, the once-at-app-creation `startedAt` resolution — pass resolved `startedAt` AND `now` as separate builder inputs, never re-resolve the fallback per request — and re-export any moved symbols from `api.ts` for back-compat). Every migrated endpoint still follows the DTO rule: a **response DTO + mapper always**; **no request DTO when there is no input** (documented exception). If exact parity genuinely cannot be achieved without normalization for some future volatile endpoint, **defer it and document why** — do NOT introduce parity normalization. The CLI/Go/Express runtime behaviour and the public contract are unchanged; `validate:contract` stays 234/234; migrated endpoints stay shadow-only under `start:nest`; all new/changed Nest (and shared-builder) code is **100% covered** (no gate lowered, no broad exclusion). Do NOT import Express route handlers/internals into Nest, couple to Go-service internals, or touch CLI/replay.

v1.14 Slice 8 (first NestJS write endpoint — `DELETE /api/activity`): migrated the **first mutation endpoint** into Nest, establishing the write-endpoint pattern on the lowest-risk write — `server/sources/nest/api/activity/`. CLI/UI/Go/replay untouched; Express untouched; `validate:contract` **234/234**. **Endpoint choice:** `DELETE /api/activity` (the slice's preferred candidate) clears the in-memory activity log and returns `204` with an empty body. It is safe because it is the paired mutation for the already-migrated `GET /api/activity`, reuses the `ACTIVITY_STORE` token, and touches **NO** forwarder lifecycle, sockets, rule persistence, config import/export, or manager restart — so it proves the write pattern without any high-risk rule mutation (rule create/update/delete/start/stop/reorder/import + config import/export stay with Express, deferred to Milestone 3). **Narrow write capability (provider pattern):** added an `ActivityClearer { clear(): void }` interface alongside the existing `ActivityReader` (both in `activity.service.ts`); the `ACTIVITY_STORE` token's bound value satisfies BOTH (the domain `ActivityStore` already has `list` + `clear` + `add`), and `ActivityService`'s constructor narrows to `ActivityReader & ActivityClearer` (the injected string token is unchanged — only the TS type annotation widened). No new provider/token, no runtime manager, no sockets. `ActivityService.clear()` calls `store.clear()`. **NO DTO** — the endpoint has **no query or body input**, so per the slice guidance no DTO/validation pipe is added (DTOs are for endpoints with input). `ActivityController.clear()` is `@Delete()` + **`@HttpCode(204)`** returning `void` — the `@HttpCode(204)` is REQUIRED because Nest defaults a `DELETE` handler to `200`, whereas the Express route is `res.status(204).end()` (204, empty body); a `void` return → no body. The controller delegates to `ActivityService.clear()` and holds no logic. **Parity byte-for-byte:** `DELETE` on a populated store and on an empty store both return `{ status: 204, body: null }` identically to Express (the integration test shares the SAME `ActivityStore` instance between the Nest app — via `app.get(ACTIVITY_STORE)` — and the Express app — via `createApp(manager, { activity: store })` — so a clear from either runtime is observable in both, and each runtime's DELETE is exercised against a freshly-seeded populated store); a subsequent `GET /api/activity` returns `{ events: [] }` in BOTH runtimes, proving the clear took effect. `fetchApi` reads a `204` empty body as `body: null` (matching Express). `GET /api/activity` (10 query variants), `GET /api/ports/advisory`, `GET /api/status`, `GET /api/forwards` parity, `/health`, the `/api/*` 404 envelope, and non-API no-leak all re-confirmed. **Every new/changed nest file is 100% covered** (activity service `clear` + controller `clear` — statements/branch/functions); 125 nest tests (5 added): `ActivityService.clear` (a `RecordingStore` fake implementing `ActivityReader & ActivityClearer`, asserting `clear` delegated), `ActivityController.clear` (stub service, returns `undefined`/`void`, `clear` called once), and 3 integration DELETE tests (populated → 204 + cleared GET in both; populated parity; empty-store parity). Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS — no gate lowered). lint/typecheck/build clean; full `npm run test` green (shared 105, server 515, client 449, all 12 Go packages). **Replay validation was NOT run** — no replay/shared-validation files were touched (per the v1.14 validation-scope correction). **Still scaffold-only:** served only under `npm run start:nest`; Express remains the default active runtime and its `DELETE /api/activity` is unchanged. Deferred: `/api/runtime` (volatile uptime/pid), `/api/connections`/`/api/config/export` (volatile timestamps), rule mutation/lifecycle write endpoints, config import/export, static serving (Milestone 3). **(Corrected mid-slice to add explicit request/response DTOs):** per a correction, EVERY migrated endpoint now has an explicit **request DTO** where input exists and an explicit **response DTO** always. Added `*ResponseDto` types + pure mappers as the domain→HTTP boundary, one per feature: `ports-advisory.response.dto.ts` (`PortsAdvisoryResponseDto`/`toPortsAdvisoryResponseDto`), `activity-list.response.dto.ts` (`ActivityListResponseDto`/`toActivityListResponseDto`), `status-list.response.dto.ts`, `forwards-list.response.dto.ts`. The mappers do a structural copy (`.map(x => ({...x}))`, the wrapper `{ events: events.map(...) }`) that preserves the Express JSON byte-for-byte (array-vs-object, property names, empty arrays/objects); the `@portier/shared` types ARE the REST contract (no internal-only fields), so a structural copy at the boundary is correct (it would become an explicit field pick only to hide a future internal field). Controllers now `return to*ResponseDto(this.service.method(...))` — a fresh copy, NOT the raw domain object (so `toBe` differs but `toEqual` matches); services return domain data (`ActivityService.list` now returns `ActivityEvent[]`, not `{events}` — the controller wraps via the mapper; the old `ActivityListResult` type was removed). Request DTOs: `GET /api/ports/advisory` keeps `PortsAdvisoryQueryDto` + `ApiValidationPipe` (Slice 7); `GET /api/activity`'s query stays endpoint-local coercion (the documented no-validation parity exception — pure coercion-with-fallback, always 200); `GET /api/status`/`GET /api/forwards`/`DELETE /api/activity` have no request input → no request DTO (documented). `DELETE /api/activity` returns `204` with NO body → no response DTO (the absent body IS the response, matching Express — the response-side analogue of the no-input rule). Each mapper is unit-tested byte-for-byte (`toEqual(source)`), for empty + populated input, that it does NOT mutate the source (`structuredClone` snapshot compare), and that it returns a fresh object/array (`not.toBe`). The read-endpoint integration parity suites (ports/activity/status/forwards) confirm the mappers produce byte-identical wire output. Total 133 nest tests; server coverage 99.0%/95.5%/100% (gate PASS, no gate lowered). Source: `docs/roadmap.md` v1.14.

**Durable rule (v1.14 Slice 8b) — explicit request & response DTOs for every endpoint:** EVERY migrated Nest `/api` endpoint must have **explicit DTO coverage**: a **request DTO** for any query/route-param/body input (validated via `ApiValidationPipe(Dto)`, Slice 7), and an **explicit response DTO ALWAYS**. The response DTO is the **boundary between domain/runtime data and HTTP JSON** — the controller maps the service result through a small **pure mapper** (`to*ResponseDto`) that returns a **fresh copy** preserving the Express JSON shape byte-for-byte (property names, nesting, array-vs-object, empty arrays/objects), and must NOT leak domain-only/internal fields. Do NOT casually `return` a raw domain object from a controller — only when the raw type is explicitly the response DTO shape AND documented as such (for Portier the `@portier/shared` types ARE the REST contract, so the mapper is a structural copy; it becomes an explicit field pick to hide any future internal field). Keep the mapping **testable and 100% covered**: every mapper has a direct unit test asserting byte-for-byte output (`toEqual(source)`), empty + populated cases, **no mutation of the source** (snapshot compare), and a fresh return reference; DTO `@Transform` callbacks are covered through pipe behavior. **Documented exceptions (the only ones):** an endpoint with **no request input** needs no request DTO; a **`204`-empty response** has no body → no response DTO; and a query that is **pure silent coercion-with-fallback** (always `200`, never rejects — e.g. `GET /api/activity`) keeps its coercion endpoint-local in the service rather than adding a transform-only DTO (a DTO would add ceremony + risk drift) — document each. Services return domain data (or an internal result); controllers own the response-DTO mapping; services never touch Nest/HTTP objects. Express parity stays byte-for-byte and `validate:contract` 234/234. When a future endpoint is migrated, add BOTH its request DTO (if input) AND its response DTO + mapper in the same slice — there is no "retrofit later" for newly migrated endpoints.

**Durable rule (v1.14 Slice 8) — write/mutation endpoint migration:** A migrated Nest write endpoint reuses the read endpoint's provider/token and adds a **narrow WRITE interface** (e.g. `ActivityClearer { clear(): void }` beside `ActivityReader`) — keep the read and write capabilities as separate interfaces so each consumer narrows to what it needs; the shared token's value satisfies the intersection (the domain object already does), and the SERVICE injects `Reader & Writer` (don't broaden to the concrete class). **Match the Express HTTP status exactly with `@HttpCode(...)`** — Nest defaults `POST` to `201` and every other verb (incl. `DELETE`) to `200`, so a `204`-empty Express route needs `@Delete() @HttpCode(204)` + a `void` return (empty body; `fetchApi` reads it as `body: null`). **No DTO when the endpoint has no query/body input** — document that explicitly; DTO + `ApiValidationPipe(Dto)` is only for endpoints that validate input (Slice 7). Controllers stay transport adapters (delegate, return nothing for 204); services own the mutation; never hand-roll responses, never touch the runtime manager/sockets for these low-risk writes. **Prove mutation parity against the SAME seeded store instance** shared between Express (`createApp(manager, { activity: store })`) and Nest (`app.get(TOKEN)`), so a clear/mutation from either runtime is observable in both; test the mutation on populated AND empty state, assert the exact status+empty body byte-for-byte, AND assert a **subsequent read reflects the mutation** in both runtimes. Migrate only **low-risk, side-effect-contained writes** in this phase (an in-memory log clear) — rule create/update/delete/start/stop/reorder/import and config import/export are deferred (they involve persistence, forwarder lifecycle, sockets, and rollback semantics) to a later milestone. Express stays the default runtime; the migrated write stays shadow-only under `start:nest`; `validate:contract` stays 234/234; all new/changed Nest code is **100% covered** (no gate lowered, no broad exclusion). Do NOT import the Express app factory/internals, couple to Go-service internals, or touch CLI/replay.

v1.14 Slice 7 (DTO-based NestJS API request-validation foundation): added proper **Nest DTO validation** (`class-validator` + `class-transformer` + a validation pipe) for migrated `/api` routes and applied it to `GET /api/ports/advisory`. NO write endpoint migrated; CLI/UI/Go/replay untouched; Express untouched; `validate:contract` **234/234**. **(This SUPERSEDES the first cut of Slice 7, which used pure `Valid<T>` helpers in `api-validation.ts`; per a correction, validation is now DTO-based — do not reintroduce a custom validation mini-framework where Nest's DTO pattern fits. The old `api-validation.ts` helper file and its `optionalString`/`requiredString`/`integerInRange`/`clampPositiveInteger`/`enumValue`/`collectErrors` exports were removed.)** New deps (server package only): `class-validator` ^0.14, `class-transformer` ^0.5 — no new npm advisories. **DTO:** `server/sources/nest/api/ports/ports-advisory.query.dto.ts` (`PortsAdvisoryQueryDto`) declares Express-faithful coercion/validation with decorators — `port`: `@Transform(({value}) => Number(value))` + `@IsInt`/`@Min(1)`/`@Max(65535)` where **all three constraints carry the IDENTICAL message** `"port must be an integer from 1 to 65535."` so whichever fails the surfaced error is the same; `purpose`: `@IsIn(["management","forward"], {message:"purpose must be management or forward."})`; `listenHost`: `@IsOptional()` + `@Transform(({value}) => typeof value === "string" ? value : undefined)` so a non-string (e.g. a repeated query key parsed as an array) coerces to `undefined` and NEVER errors (matching Express's `typeof === "string" ? value : undefined`). **Pipe:** `server/sources/nest/common/api-validation.pipe.ts` — `ApiValidationPipe<T>` (a Nest `PipeTransform`) runs `plainToInstance(dtoClass, value ?? {})` then `validate(instance, {whitelist:true, stopAtFirstError:true})`, and on failure throws `ApiBadRequestException(flattenValidationErrors(errors))` → the Slice-3 `toApiError` → `400 { errors:[...] }`. `flattenValidationErrors` = `errors.flatMap(e => Object.values(e.constraints ?? {}))` (one message per field via `stopAtFirstError`, in property order, hiding class-validator internals). **CRITICAL — the pipe takes the DTO class EXPLICITLY** (`@Query(new ApiValidationPipe(PortsAdvisoryQueryDto))`), NOT via Nest's built-in `ValidationPipe` metatype inference, because the esbuild/Vitest transform does NOT emit `design:paramtypes` (so the built-in pipe would silently skip validation under test) — same reason DI uses explicit `@Inject` tokens; this keeps validation identical between `tsc` build and Vitest. class-validator/class-transformer work under esbuild because their decorators register their OWN metadata (not `design:type`). The pipe is applied **per-route** (no `app.useGlobalPipes`), so non-API routes are unaffected. `whitelist:true` strips/ignores extra query params (NOT `forbidNonWhitelisted`, which would reject — Express ignores extras); `stopAtFirstError:true` yields one message per field. **Controller/service:** `PortsController.getAdvisory(@Query(new ApiValidationPipe(PortsAdvisoryQueryDto)) query)` delegates to `PortsService.getAdvisories(query)` (a thin `getPortAdvisories({port,listenHost,purpose})`); validation moved entirely to the pipe — the controller no longer throws and the old `resolveAdvisories`/`AdvisoryResult` are gone. **`GET /api/activity` kept ENDPOINT-LOCAL (documented exception):** activity has **no validation errors** (its `limit`/filters are pure silent coercion-with-fallback — invalid `limit` → default 100, always `200`); a class-validator DTO would be transform-only (no validators) adding ceremony + a pipe with no validation value and risk obscuring the coercion, so per the correction activity stays as-is (`parseActivityLimit` + `typeof === "string"` in the service, UNCHANGED). `StatusService`/`ForwardsService` have no query validation. **Parity byte-for-byte:** `GET /api/ports/advisory` parity covers valid / invalid-port (`70000`) / non-integer (`1.5`) / non-numeric (`abc`) / invalid-purpose (`bogus`) / missing-port / **extra-param** — all match Express; `GET /api/activity` + `GET /api/status` + `GET /api/forwards` parity, `/health`, the `/api/*` 404 envelope, and non-API no-leak all re-confirmed. **One documented, non-required divergence:** an *entirely-empty / both-fields-invalid* query accumulates ALL field errors (idiomatic class-validator) whereas Express is first-error-wins — this is NOT one of the correction's required byte-for-byte cases (valid / invalid-port / invalid-purpose / missing-port, each single-field-invalid → identical), it is not contract-covered, the Nest route is shadow-only, and accumulate-all is what the upcoming write endpoints need (Express's `validateForwardRule` accumulates), so the pipe stays accumulate-all rather than imposing a per-request first-error policy. **Every new/changed nest file is 100% covered** (`ports-advisory.query.dto.ts` — its `@Transform` arrows exercised via the pipe; `api-validation.pipe.ts`; `ports.service.ts`; `ports.controller.ts`); 120 nest tests: `flattenValidationErrors` (constraints present + `?? {}` undefined branch), `ApiValidationPipe` over the ports DTO (valid with/without listenHost, non-string listenHost → undefined, each invalid case → single-message `ApiBadRequestException`, empty-query → both errors covering `value ?? {}`), `PortsService.getAdvisories`, `PortsController` delegation, and the 9-case integration parity. Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS — no gate lowered). lint/typecheck/build clean; full `npm run test` green (shared 105, server 510, client 449, all 12 Go packages). **JSON-body DTO validation is deferred to the write-migration slice** (its DTO shape follows the write endpoints). **Replay validation was NOT run** — no replay/shared-validation files were touched (per the v1.14 validation-scope correction). **Still scaffold-only:** served only under `npm run start:nest`; Express remains the default active runtime. Deferred: `/api/runtime` (volatile uptime/pid), `/api/connections`/`/api/config/export` (volatile timestamps), JSON-body validation, static serving, write/lifecycle endpoints (Milestone 3). Source: `docs/roadmap.md` v1.14.

**Durable rule (v1.14 Slice 7) — DTO-based API request validation:** Migrated Nest `/api` request validation uses **DTO classes with `class-validator`/`class-transformer` decorators**, applied via the shared `ApiValidationPipe` (`server/sources/nest/common/api-validation.pipe.ts`) — NOT a hand-rolled validation mini-framework, and NOT magic global pipes. **Custom coercion helpers/`@Transform` are allowed ONLY to preserve legacy Express coercion where a plain validator would drift** (e.g. `port` via `@Transform(Number)`, `listenHost` via `@Transform(typeof === "string" ? value : undefined)`). The `ApiValidationPipe` MUST take the **DTO class explicitly** (`@Query(new ApiValidationPipe(Dto))`), never rely on Nest's metatype/`design:paramtypes` inference (the esbuild/Vitest transform emits no decorator metadata, so the built-in `ValidationPipe` would silently skip validation under test — same reason DI uses explicit `@Inject` tokens); apply it **per-route**, never `app.useGlobalPipes` (non-API routes must stay unaffected). Validation **errors flow through the shared error layer**: the pipe throws `ApiBadRequestException(flattenValidationErrors(errors))` → `toApiError` → `400 {errors:[...]}`; controllers never hand-roll `{errors}`, services never touch Nest/HTTP objects and receive the validated DTO. DTO coercion/messages MUST mirror the existing Express route exactly so the endpoint stays **byte-for-byte parity-identical** for the contract-relevant cases (valid + each single-field-invalid) — give every constraint on a field the **same Express message**, use `stopAtFirstError:true` (one message per field) and `whitelist:true` (ignore extra query params; NEVER `forbidNonWhitelisted` unless Express rejects extras), and guard with the endpoint's parity suite. Pick a DTO **only when the endpoint actually validates** (rejects bad input): an endpoint that is pure silent coercion-with-fallback and always `200` (e.g. `/api/activity`'s `limit`/filters) keeps its coercion **endpoint-local in the service** rather than adding a transform-only DTO + pipe — document why. Idiomatic class-validator **accumulates all field errors**; that matches Express for single-field-invalid (the contract cases) and is what the write endpoints need (`validateForwardRule` accumulates) — do NOT impose a per-request first-error policy on the shared pipe; an entirely-both-invalid query returning the union is an accepted, documented, non-contract difference (Nest is shadow-only). DTOs have no runtime branches except `@Transform` callbacks — cover them (and the pipe's success/throw + `value ?? {}` branches, and `flattenValidationErrors`) to **100%** via direct pipe tests over the DTO; do NOT add broad coverage exclusions. JSON-body object validation lands in the write-migration slice (its DTO shape follows the endpoints). The CLI/Go/Express behaviour and the public contract are unchanged; `validate:contract` stays 234/234; migrated endpoints stay shadow-only under `start:nest`.

v1.14 Slice 6 (migrate `GET /api/forwards` into NestJS): migrated the next manager-backed read endpoint, **reusing the Slice-5 read-provider pattern** — `server/sources/nest/api/forwards/`. CLI/UI/Go/replay untouched; Express untouched; `validate:contract` **234/234**. **Endpoint choice:** `GET /api/forwards` (the spec's preferred candidate) is exact-parity-safe — Express returns `manager.listRules().map(toRuleResponse)` where `toRuleResponse(rule) = {...rule, advisories: getPortAdvisories({port: rule.listenPort, listenHost: rule.listenHost, purpose: "forward"})}`; rule fields are static and `getPortAdvisories` is deterministic, so there are **no volatile fields** (unlike `/api/connections` `generatedAt`, `/api/config/export` `exportedAt`, `/api/runtime` uptime/pid — all still deferred). `ForwardsController` (`@Controller("api/forwards")`, `@Get()`) returns the `ForwardRuleResponse[]` array verbatim via `ForwardsService.list()`, which **re-expresses `toRuleResponse`** (Express's `toRuleResponse` is private to `api.ts`, so it is NOT imported — the Nest service maps via the shared `getPortAdvisories`, the same one the ports endpoint uses, guaranteeing parity without duplicating advisory constants). Read-only, always `200`, NO error branch. **Only the list read is migrated** — every write/lifecycle route under `/api/forwards/...` (`POST`/`PATCH`/`DELETE`/`reorder`/`:id/start|stop`/`groups/:group/...`/`:id/diagnose`) stays with Express (they 404 in the shadow Nest app, which is fine; `@Get()` with no sub-path matches only the list, and `/api/forwards` has no GET sub-paths). **Provider seam (reuses Slice 5):** a narrow `ForwardsReader { listRules(): ForwardRule[] }` interface + `FORWARDS_READER` token in `forwards.reader.ts`; production code holds NO manager/store dependency — `ForwardsModule` defaults the token to a trivial `emptyForwardsReader` (`{ listRules: () => [] }`, exported + unit-tested); the domain `ForwardManager` satisfies `ForwardsReader` and is bound via `@nestjs/testing` `overrideProvider` in tests (and when Nest is active). **Parity:** a shared seeded `ForwardManager` instance injected into BOTH Express (`createApp(manager)`) and Nest → byte-for-byte equality (empty manager → `[]`; two stopped rules via `addRule` enabled:false — incl. a `0.0.0.0` listen host so the response carries a `LAN_EXPOSURE` advisory — `body` deep-equals the shared `toRuleResponse` mapping). No real sockets/forwarders. **The Slice-1 `app.integration.test.ts` "unknown /api route" probe was changed from `/api/forwards` (now migrated → `200 []`) to `/api/not-migrated`** — a reminder that migrating an endpoint flips its shadow 404 to a 200, so update any test that used that path as an "unmigrated" example. `GET /api/ports/advisory` + `GET /api/activity` + `GET /api/status` parity still pass; `/health`, the `/api/*` 404 envelope, and non-API no-leak re-confirmed. **Every new/changed nest file is 100% covered** (forwards reader/service/controller/module, `app.module.ts` — statements/branch/functions); 118 nest tests (9 added): `emptyForwardsReader` unit, `ForwardsService.list` (fake reader — advisory decoration + LAN_EXPOSURE assertion + empty), `ForwardsController` delegation, and the integration (default-app empty `[]` + scaffolding) + parity (empty + seeded-with-advisories) suite. Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS — no gate lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; full `npm run test` green (shared 105, server 508, client 449, all 12 Go packages). **No new durable rule — this slice confirms the v1.14 Slice 5 runtime/manager read-provider rule** (narrow reader interface + token, empty production default, shared-instance parity via `overrideProvider`, stopped-rules-only, list-read-only). **Still scaffold-only:** served only under `npm run start:nest`; the Express server remains the default active runtime and its `/api/forwards` is unchanged. Deferred: `/api/runtime` (volatile uptime/pid), `/api/connections`/`/api/config/export` (volatile timestamps), request-validation behaviour, static serving, and all write/lifecycle endpoints. Source: `docs/roadmap.md` v1.14.

v1.14 Slice 5 (migrate `GET /api/status` into NestJS — runtime/manager read-provider pattern): migrated the first **manager-dependent** read endpoint and established the narrow runtime-read provider seam — `server/sources/nest/api/status/`. CLI/UI/Go/replay untouched; Express untouched; `validate:contract` **234/234**. **Endpoint choice:** `GET /api/status` (the spec's preferred candidate) is the smallest manager-dependent read with **no volatile fields for stopped rules** — `ForwardStatus.startedAt` only appears when a rule is *running*, and the byte/packet/connection counters are `0`/absent when stopped, so seeding **stopped** rules (which endpoint tests must do anyway — the no-sockets rule) yields **exact byte-for-byte parity with no normalization**. (`/api/config/export` and `/api/connections` carry `exportedAt`/`generatedAt` timestamps → not exact-parity-safe → skipped; `/api/runtime`'s `uptimeSeconds`/`pid` likewise.) `StatusController` (`@Controller("api/status")`, `@Get()`) returns the `ForwardStatus[]` array verbatim (Express returns `manager.listStatus()` directly — an array, not an envelope) via `StatusService.list()`, which returns the injected reader's `listStatus()`. Read-only, always `200`, NO error branch. **Runtime/manager read-provider seam:** a narrow `StatusReader { listStatus(): ForwardStatus[] }` interface + a string token `STATUS_READER`, both in `status.reader.ts`; the production status code holds **NO manager/store dependency** — `StatusModule` defaults the token to a trivial `emptyStatusReader` (`{ listStatus: () => [] }`, exported + unit-tested), since the scaffold has no forwarding runtime wired. The real domain `ForwardManager` satisfies `StatusReader` and is bound to the token in tests (and when Nest becomes the active runtime). This seam is what the remaining manager-backed read endpoints (`/api/forwards`, `/api/connections`) will reuse. **Parity:** unlike the mutable `ActivityStore` (seedable via `app.get`), a `ForwardManager` requires a backing store and can't be cleanly seeded post-construction, so the integration test injects a **shared seeded `ForwardManager` instance** into BOTH Express (`createApp(manager)`) and Nest (`STATUS_READER` overridden via `@nestjs/testing` `Test.createTestingModule(...).overrideProvider(STATUS_READER).useValue(manager)`) — same instance → same `listStatus()` → byte-for-byte equality (empty manager → `[]`; two stopped tcp/udp rules added via `addRule` with `enabled:false` so no forwarder starts → 2 deterministic stopped statuses, `body` deep-equals `manager.listStatus()`). **Added `@nestjs/testing` (server devDependency, Nest 11)** for `overrideProvider` — the seeded manager + its test `MemoryStore` live in the test file (excluded from coverage), keeping production status code minimal and 100% covered; no new npm advisories (the pre-existing esbuild/shell-quote dev advisories are unrelated). `GET /api/ports/advisory` + `GET /api/activity` parity still pass; `/health`, the `/api/*` 404 envelope, and non-API no-leak re-confirmed; no real sockets/listeners/forwarders created. **Every new/changed nest file is 100% covered** (status reader/service/controller/module, `app.module.ts` — statements/branch/functions); 109 nest tests (8 added): `emptyStatusReader` unit, `StatusService.list` (fake reader, populated + empty), `StatusController` delegation, and the integration (default-app empty `[]` + scaffolding) + parity (empty + seeded) suite. Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS — no gate lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; TS workspaces green (shared 105, server 499, client 449); all 12 Go packages pass in isolation (the parallel-run flake of `TestTCPForwarderEmitsConnectionClosedEvent` is the documented pre-existing socket-timing issue — Go untouched). **Still scaffold-only:** served only under `npm run start:nest`; the Express server remains the default active runtime and its `/api/status` is unchanged. Deferred: `/api/runtime` (volatile uptime/pid), the manager-dependent `/api/forwards`/`/api/connections`/`/api/config/export`, request-validation behaviour, static serving, write/lifecycle endpoints. Source: `docs/roadmap.md` v1.14.

**Durable rule (v1.14 Slice 5) — runtime/manager read-provider seam & parity safety:** A migrated read endpoint backed by the forwarding runtime must depend on a **narrow Nest-side reader interface + injection token** (e.g. `StatusReader` + `STATUS_READER`), NOT the concrete `ForwardManager`/Express app. The production default for that token is a **trivial empty reader** (`emptyStatusReader` → `[]`) — the scaffold wires no real runtime; the real domain object (`ForwardManager`) satisfies the interface and is bound to the token in tests and when Nest becomes active. Endpoint tests must **never** start forwarders/listeners/sockets — seed **stopped** rules only (`addRule` with `enabled:false`, or a store of disabled rules) so no `startRule` runs. **Choose only endpoints whose response has no volatile fields under the test conditions** (status is exact-parity-safe because `startedAt` and counters are absent/0 for stopped rules); if an endpoint has inherently time/pid/timestamp-varying fields (`/api/runtime` uptime/pid, `/api/config/export` `exportedAt`, `/api/connections` `generatedAt`/`lastSeenAt`), either find an exact-parity approach or **defer it and document why** — do NOT introduce parity normalization to paper over volatility. Prove parity by injecting the **same domain instance** into both Express (`createApp(manager)`) and Nest, so identical data + identical domain logic give byte-for-byte equality: prefer seeding a mutable default via `app.get(TOKEN)` (as for `ActivityStore`); when the dependency can't be cleanly seeded post-construction (a store-backed `ForwardManager`), use `@nestjs/testing` `overrideProvider` and keep the seeded instance + its test store in TEST code (so production stays minimal and 100% covered). `@nestjs/testing` is an allowed server **devDependency** for exactly this. Controllers stay transport adapters returning the domain result verbatim (status is a bare `ForwardStatus[]` array, not an envelope); services hold no Nest/HTTP concerns. Migrate read endpoints only — no lifecycle/mutation. Express stays the default runtime; migrated endpoints stay shadow-only under `start:nest`; `validate:contract` stays 234/234; all new/changed Nest code is **100% covered** (no gate lowered, no broad exclusion). Do NOT import the Express app factory/internals into Nest controllers/services, couple to Go-service internals, or touch CLI/replay.

v1.14 Slice 4 (migrate `GET /api/activity` into NestJS): migrated the next safe read-only endpoint and established the **inject-dependency-behind-a-narrow-token/fake** DI pattern — `server/sources/nest/api/activity/`. CLI/UI/Go/replay untouched; Express untouched; `validate:contract` **234/234**. **Endpoint choice:** `GET /api/activity` was picked over `GET /api/runtime` because activity gives **true byte-for-byte parity** (its `{events}` response has no inherently time/process-varying fields, whereas runtime's `uptimeSeconds`/`pid` would force a parity normalization) AND it best establishes the DI-with-token seam the heavier manager-dependent endpoints (status/forwards/connections) will reuse. `ActivityController` (`@Controller("api/activity")`, `@Get()`) is a transport adapter that extracts the optional `@Query` `limit`/`ruleId`/`type`/`severity` and delegates to `ActivityService.list(...)`, which maps them to the store's `ActivityListParams` with the SAME coercion as the Express route — `parseActivityLimit` (`Number.isInteger(n)&&n>0 ? Math.min(n,500) : 100`), and `typeof raw === "string" ? raw : undefined` for the filters — then calls the injected store's `list`. The endpoint is **read-only and always `200 { events:[...] }` (NO error branch)**, so it raises no exception and uses no error envelope (the shared Slice-3 layer is still exercised by the unmatched-`/api/*` 404). Only the read is migrated; **`DELETE /api/activity` (a mutation) stays with Express, deferred**. DI seam: a narrow `ActivityReader { list(params): ActivityEvent[] }` interface + a string token `ACTIVITY_STORE` (both in `activity.service.ts`); `ActivityModule` defaults the token to a fresh `new ActivityStore()` (the DOMAIN store `server/sources/activity/activity-store.ts`, reused — NOT Express internals — like reusing `getPortAdvisories`). The integration/parity test gets the Nest app's own default store via `app.get(ACTIVITY_STORE)`, seeds it, and passes that **same instance** to the Express app (`createApp(manager, { activity: store })`) so identical data + identical `ActivityStore.list` logic give byte-for-byte parity — **no `@nestjs/testing` dependency needed** (avoided `overrideProvider` via `app.get`). Express↔Nest parity proven across **10 query variations** (no filter; `limit` valid/`0`/`abc`/`600`→clamp; each of ruleId/type/severity; unknown `ruleId`→`[]`; combined filters); `/health`, the `/api/*` 404 envelope, and non-API no-leak re-confirmed in this suite, and `GET /api/ports/advisory` parity still passes (its suite runs unchanged after the new module import). **Every new/changed nest file is 100% covered** (activity controller/service/module, `app.module.ts` — statements/branch/functions); 101 nest tests (26 added): `parseActivityLimit` table (default/clamp/invalid), `ActivityService.list` param-mapping via a recording fake `ActivityReader` (defaults + parsed-filter passthrough), `ActivityController` delegation (with and without params), and the integration+parity suite. Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS — no gate lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; full `npm run test` green (shared 105, server 491, client 449, all 12 Go packages). **Still scaffold-only:** served only under `npm run start:nest`; the Express server remains the default active runtime and its `/api/activity` is unchanged. Deferred: runtime/status/forwards/connections endpoints, request-validation behaviour, static serving, write/lifecycle endpoints. Source: `docs/roadmap.md` v1.14.

**Durable rule (v1.14 Slice 4) — read-only endpoint migration & DI seam:** When a migrated `/api` endpoint needs runtime/domain state, inject it through a **narrow Nest-side interface + injection token** (e.g. `ActivityReader` + `ACTIVITY_STORE`), default the token to the real DOMAIN object (`ActivityStore`, `ForwardManager`, … — domain modules are reusable; only the Express **app** factory `createApp`/`api.ts` internals are off-limits to Nest production code), and inject a **fake/seeded instance in tests** — NEVER stand up a real forwarding/socket/listener runtime in an endpoint test. Prefer seeding the Nest app's OWN default provider via `app.get(TOKEN)` and sharing that exact instance with the Express app for parity (this gives byte-for-byte equality with identical data and avoids a `@nestjs/testing` dependency); only reach for `@nestjs/testing` `overrideProvider` if a provider genuinely cannot be seeded post-construction. Keep migrating **read-only, low-risk endpoints first**; a read endpoint with **no error branch** (always `200`) is fine — do NOT fabricate error cases, but DO parity-test its graceful/edge query handling (invalid `limit` → default, unknown filter → empty, combined filters). The Nest endpoint MUST replicate the Express **query/param coercion exactly** (reuse shared/domain logic like `ActivityStore.list` for filtering; only the HTTP-param→domain-params mapping is re-expressed, and it is parity-tested). The controller stays a transport adapter (extract `@Query`/`@Param`, call the service, return its result); validation/mapping/behaviour live in the service. Migrate only the **read** verb of a multi-verb route — leave mutations (`DELETE /api/activity`, etc.) on Express until a deliberate write-migration slice. Express stays the default runtime; migrated endpoints stay shadow-only under `start:nest`; `validate:contract` stays 234/234; all new/changed Nest code is **100% covered** (no gate lowered, no broad exclusion). Do NOT import the Express app factory/internals into Nest controllers/services, couple to Go-service internals, or touch CLI/replay.

v1.14 Slice 3 (shared NestJS `/api` error-envelope layer): added a reusable error layer so migrated Nest controllers stop hand-rolling the Portier `{ errors: [...] }` envelope — `server/sources/nest/common/`. CLI/UI/Go/replay untouched; `validate:contract` **234/234** unchanged (Express untouched, no contract change). Three pieces: `api-error-envelope.ts` (pure `toApiError(exception): {status, errors[]}` + `isApiPath` — the single mapping), `api-error-envelope.filter.ts` (a global catch-all `@Catch()` `ApiErrorEnvelopeFilter implements ExceptionFilter`, registered via `APP_FILTER` in `app.module.ts`), and `api-errors.ts` (`ApiBadRequestException extends BadRequestException`, ctor takes `string[]` → `super({errors})`). `toApiError` is deterministic: an `HttpException` whose body is `{errors:string[]}` keeps those messages (the deliberate-envelope path, incl. `ApiBadRequestException` and a future `NotFoundException({errors})`); else an unmatched-route `NotFoundException` → `404 ["API route was not found."]` (preserves Slice-1 behavior); else any other `HttpException` → its message (`getResponse()` string body, or `.message`, or a generic fallback) at its own status; else (non-HTTP/unknown thrown value) → `500 ["Internal server error."]` with **no stack trace / raw exception leaked**. The filter applies `toApiError` only to `isApiPath` requests; **non-API routes keep NestJS's default error shape** (HttpException → status + `getResponse()` normalized; unknown → default `500 {statusCode,message}`), so non-API 404s never gain an `errors` key. This **replaces** the Slice-1 `api-not-found.filter.ts` (which only caught `NotFoundException`) — that file + its test were removed; `isApiPath` moved into `api-error-envelope.ts`. Controller cleanup: `PortsController` now `throw new ApiBadRequestException(result.errors)` instead of `new BadRequestException({errors})` — **no envelope literal in the controller** (the `{errors}` shape lives only in the shared layer). Behavior preserved: `/api/*` 404 envelope, non-API no-leak, and the Express↔Nest parity (valid/invalid-port/invalid-purpose/missing-port) all still pass; the success response is unchanged. **Every new/changed nest file is 100% covered** (`api-error-envelope.ts`, `api-error-envelope.filter.ts`, `api-errors.ts`, `ports.controller.ts`, `app.module.ts` — statements/branch/functions); 75 nest tests: `toApiError` table (explicit envelope, deliberate-404-envelope, unmatched-404 fixed message, string/default `BadRequestException` message, string-bodied `HttpException`, no-message + non-string-array `errors` fallbacks, non-HTTP → 500), the filter via a mock `ArgumentsHost` (the rare non-API string-body and unknown-error-500 branches that no real route triggers), and `ApiBadRequestException`. Server coverage 99.0%/95.0%/100% (gate 95/92/99 PASS — no gate lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; full `npm run test` green (shared 105, server 465, client 449, all 12 Go packages). Deferred: the remaining simple endpoints (runtime, activity read) and request-validation behaviour. Source: `docs/roadmap.md` v1.14.

**Durable rule (v1.14 Slice 3) — shared `/api` error-envelope layer:** Migrated Nest `/api` controllers must NOT hand-roll the Portier `{ errors: [...] }` envelope — the envelope shape lives in ONE place, the shared layer in `server/sources/nest/common/` (`toApiError` in `api-error-envelope.ts`, applied by the global `ApiErrorEnvelopeFilter`). A controller signals an error by **throwing a Nest exception carrying messages** (`ApiBadRequestException(string[])`, or any `HttpException` with a `{ errors: string[] }` body / a string message), never by composing the final `{errors}` response object itself and never by writing to the `res`. `toApiError` must stay **deterministic and non-leaking**: explicit `{errors}` body preserved; unmatched-route `NotFoundException` → `404 ["API route was not found."]`; other `HttpException` → its message at its own status; any non-HTTP/unknown value → `500 ["Internal server error."]` (NEVER a stack trace, raw `Error`, or exception object). The filter applies the envelope ONLY to `isApiPath` requests and must leave **non-API routes on NestJS's default error shape** (a non-API 404 must never gain an `errors` key) — guard this. Services must stay free of Nest/HTTP response concerns (no `res`, no `HttpException` in pure domain logic beyond raising a typed API exception at the boundary if unavoidable — prefer returning a discriminated result and letting the controller throw). There is exactly ONE global API exception filter (catch-all) — do NOT reintroduce a second per-concern filter (the Slice-1 `ApiNotFoundFilter` was folded in); add new API error *types* (e.g. an `ApiConflictException`) in `api-errors.ts` and a mapping branch in `toApiError`, both with tests, rather than new filters. Keep the error envelope **contract-compatible with Express** (`ValidationError→400`, `ConflictError→409`, `NotFoundError→404`, unknown→500, body `{errors:[...]}`) and parity-checked. All of this is new Nest migration code → **100% meaningful coverage** (every `toApiError` and filter branch, including branches no current route triggers, via a mock `ArgumentsHost` — that is legitimate, not gaming); do NOT lower a gate or broadly exclude. Do NOT import Express app internals into the filter/controllers, couple to Go-service internals, or touch CLI/replay. Express remains the default active runtime; migrated Nest endpoints stay shadow-only under `start:nest`; `validate:contract` stays 234/234.

v1.14 Slice 2 (NestJS API parity harness + first read-only endpoint migration): added a contract-safe **API parity harness** and migrated the smallest safe read-only `/api` route into Nest, with the Express server still the default runtime. CLI/UI/Go/replay untouched; `validate:contract` **234/234** unchanged (no contract change). Harness (`server/sources/nest/testing/api-parity.ts`, server-test-local, small — not a framework): `startHandlerServer(handler)` boots an Express app on an ephemeral 127.0.0.1 port, `startNestServer(app)` boots a Nest app likewise, `fetchApi(baseUrl,path)` captures `{status, body}` (empty body → `null`), `stableStringify` recursively sorts object keys, and `diffApiResponses(a,b)` returns the reasons two responses differ (`[]` = identical) so a test can fire the same request at the Express route and the Nest route and assert byte-equivalence. **Migrated endpoint: `GET /api/ports/advisory`** (`server/sources/nest/api/ports/`) — chosen as the smallest safe candidate: read-only, deterministic, **no runtime-manager dependency** (the Express route never touches the manager), wraps the pure shared `getPortAdvisories`, clear `400` branches. `PortsService.resolveAdvisories(rawPort, rawPurpose, rawListenHost)` owns validation (port integer 1–65535; purpose `management`/`forward`; `listenHost` optional string — the SAME coercion/rules as Express) and returns a discriminated `AdvisoryResult` (`{ok:true,advisories}` | `{ok:false,errors}`), then delegates to `getPortAdvisories`; `PortsController` (`@Controller("api/ports")`, `@Get("advisory")`) is a transport adapter that extracts `@Query` params and maps the result to `200` advisories or `throw new BadRequestException({ errors })` → `400 { errors:[...] }` (the object-body form gives the exact contract envelope; `BadRequestException` is NOT caught by the Slice-1 `ApiNotFoundFilter`, which only catches `NotFoundException`); `PortsModule` wires them and is imported by `app.module.ts`. The Nest route is **byte-for-byte identical to the existing Express route**, proven by an Express↔Nest parity test (`ports.integration.test.ts`) across valid / invalid-port / invalid-purpose / missing-port cases. **Still scaffold-only:** the migrated route is served by the Nest app ONLY under `npm run start:nest`; the **Express server (`index.ts`/`api.ts`) remains the default active runtime** and its `/api/ports/advisory` is unchanged — both serve it identically, so `validate:contract` (run against the active Express server + Go) stays 234/234. **Every new nest file is 100% covered** (api/ports controller/service/module, testing/api-parity.ts — statements/branch/functions); 62 nest tests total (31 added): harness units (stable-stringify key-order/array/primitive/null, diff identical/status/body/both, `startHandlerServer`+`fetchApi` JSON **and** empty-body 204 branch), `PortsService` branch table (valid with/without `listenHost`, non-string `listenHost`→absent, every invalid port `0`/`-1`/`70000`/`1.5`/`abc`/missing and invalid purpose), `PortsController` (200 path + 400-envelope path via a stub service, asserting `getResponse()` equals `{errors}`), and the real-Nest integration+parity test which also re-confirms `/health`, the `/api/*` 404 envelope, and no-envelope-leak on non-API 404s. Server coverage 98.9%/94.9%/100% (gate 95/92/99 PASS — no gate lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; full `npm run test` green (452+449 TS tests, all 12 Go packages). Deferred (later slices): static serving, `/api/health`/`/api/runtime` parity, a global error filter, and any write/lifecycle endpoint. Source: `docs/roadmap.md` v1.14.

**Durable rule (v1.14 Slice 2) — API parity harness & endpoint migration:** Every `/api` endpoint migrated into Nest must be checked **byte-for-byte against the existing Express route** using the parity harness (`server/sources/nest/testing/api-parity.ts`) — boot both, fire the same requests (success AND each error branch), and assert `diffApiResponses(...) === []` (status + deterministic-key-order body equality). Migrate **read-only, low-risk endpoints first** (no write/lifecycle/rule-mutation in early slices); pick the next-smallest safe one from the documented contract — do NOT invent endpoints or change the contract (`validate:contract` stays the source of truth, currently **234/234**; the Express route must stay unchanged and keep serving identically so the migrated Nest route is purely shadow until a deliberate runtime switch). **Controllers are transport adapters only** — they extract `@Query`/`@Param`/`@Body`, call a service, and map results/errors to HTTP; request **validation + behaviour live in the service** (return a discriminated result like `AdvisoryResult` so every branch is unit-testable without HTTP). Reuse the **shared contract logic** (`@portier/shared`, e.g. `getPortAdvisories`) rather than reimplementing it, so the runtimes cannot drift. The contract error envelope is produced by throwing a Nest `HttpException` with an **object body** (`new BadRequestException({ errors: [...] })` → `400 { errors:[...] }`), not Nest's default `{statusCode,message,error}` shape; remember the Slice-1 `ApiNotFoundFilter` only catches `NotFoundException`, so other exceptions fall through to Nest's default filter (a broader error-envelope filter is a later milestone). Do NOT import Express app internals into Nest controllers, couple Nest to Go-service internals, or touch CLI/replay. Endpoint tests must NOT open real forwarding sockets or instantiate the real runtime manager for socket work — inject a fake/minimal store where a manager is structurally required (the advisory route needs none). **All new Nest migration code (controllers, services, modules, and the parity harness if it holds logic) must reach 100% meaningful coverage** (per the v1.14 Slice 1 rule) — write the harness's I/O helpers so their branches are reachable (e.g. cover `fetchApi`'s empty-body path) rather than excluding them; the only permitted exclusion remains the logic-free `main.ts`. Keep the harness small and server-test-local — it is not a product framework.

v1.14 Slice 1 (NestJS server migration scaffold): started v1.14 TypeScript Server Migration to NestJS by adding an **incremental, reversible NestJS scaffold under `server/sources/nest/`** that does NOT replace the existing Express server — `server/sources/index.ts` + `server/sources/api.ts` (`createApp`) remain the **active runtime**, and their scripts are unchanged. CLI/UI/Go untouched; `validate:contract` **234/234** unchanged (no API/contract change — the active server is not modified). Scaffold layout: `app.module.ts` (root; registers the global filter via `APP_FILTER`), `app.factory.ts` (`createNestApp()` — builds the app WITHOUT a listener, shared by `bootstrap` and tests), `nest-options.ts` (`resolveNestListenOptions(env)` — pure env→`{host,port}` resolver), `bootstrap.ts` (`bootstrap(listen)` starts the app on explicit loopback options; `reportBootstrapFailure(error)` logs + sets `process.exitCode=1`), `main.ts` (a **logic-free process entry** — `void bootstrap(resolveNestListenOptions(process.env)).catch(reportBootstrapFailure)` — binds `127.0.0.1` only, never `0.0.0.0`; default port **47832** to avoid colliding with the management `47831`; it is the ONLY coverage-excluded nest file, like `index.ts`, because importing it would start a listener), `health/` (`health.controller.ts` = transport adapter only → `health.service.ts` = behaviour → `health.module.ts`), and `common/api-not-found.filter.ts`. Minimal, **contract-safe** surface: `GET /health` → `{ ok: true, server: "node", name: "Portier" }` (a liveness probe needing no runtime manager), deliberately served **OUTSIDE the frozen `/api` contract** — the documented `GET /api/health` is "TypeScript server: not implemented", so implementing `/api/health` (and `/api/runtime`) is **deferred to the endpoint-migration slices** (Milestone 2+) where it lands behind `validate:contract` rather than silently changing that documented status; plus a global `ApiNotFoundFilter` (`@Catch(NotFoundException)`) returning the contract envelope `{ "errors": ["API route was not found."] }` for any unmatched `/api/*` route (`isApiPath`), with non-API 404s keeping NestJS's default body (the filter only ever catches genuinely-unknown routes, so future migrated `/api` controllers respond normally). Dependencies added to the **server package only** (Nest 11 targets Express 5, aligned with the existing `express ^5.1.0`): `@nestjs/common`/`@nestjs/core`/`@nestjs/platform-express` `^11`, `reflect-metadata`, `rxjs` — introduced **no new npm advisories** (the pre-existing esbuild/shell-quote dev-tooling advisories are unrelated). DI is wired with explicit `@Inject(Token)` constructor params so it resolves identically under `tsc` (emits metadata) and the esbuild-based Vitest transform (does NOT emit `design:paramtypes`) — no reliance on emitted decorator metadata. The server `tsconfig.json` gained `experimentalDecorators`/`emitDecoratorMetadata` (harmless for the existing non-decorated code; picked up by vitest/esbuild for the nest files). Scripts added (additive — none renamed/removed): `build:nest`, `start:nest` (`tsx sources/nest/main.ts`), `test:nest` (`vitest run sources/nest`). Build output stays under `build/` (`server/build/nest/…`), NOT `dist/`. Tested meaningfully (31 tests, all under the existing server vitest so coverage counts): `health.service.test.ts` + `health.controller.test.ts` (units, direct construction), `common/api-not-found.filter.test.ts` (`isApiPath` table + the contract-envelope vs default-404-object vs string-response branches via a mock `ArgumentsHost`), `nest-options.test.ts` (default host/port, custom host, valid port, and the invalid `0`/negative/non-numeric/non-integer/empty → default branches), `bootstrap.test.ts` (boots via `bootstrap({host:"127.0.0.1",port:0})` on an ephemeral port → `/health`, asserts the log + 127.0.0.1; and `reportBootstrapFailure` sets `process.exitCode=1` + logs, with the prior exit code restored), and `app.integration.test.ts` which **boots the real Nest app** (`createNestApp` + `listen(0,"127.0.0.1")` + `fetch`) and asserts `/health`, the `/api/*` and bare-`/api` 404 envelopes, no envelope leak on a non-API 404, and 127.0.0.1 binding. **Every nest file is 100% covered** (statements/branch/functions) — the ONLY exclusion is the logic-free `main.ts` process entry (its constituents `resolveNestListenOptions`/`bootstrap`/`reportBootstrapFailure` are all unit-covered; importing `main.ts` would start a listener as a side effect — same precedent as `index.ts`); server coverage 98.6%/94.1%/100% (gate 95/92/99 PASS — no gate lowered). `validate:coverage:replay` still PASS (96.5%); lint/typecheck/build clean. A new `server/readme.md` documents the two coexisting implementations and the migration boundary. **The existing TypeScript server behavior and scripts are unchanged.** Source: `docs/roadmap.md` v1.14.

**Durable rule (v1.14 Slice 1) — NestJS migration scaffold:** The NestJS migration MUST be **incremental and reversible** — the existing Express server (`server/sources/index.ts` + `server/sources/api.ts`) stays the **active runtime** and its scripts must keep working until a NestJS replacement is **explicitly validated** (contract + runtime smoke + E2E); do NOT remove the Express server or rewrite runtime/forwarding behavior in scaffold/early slices. NestJS **controllers are transport adapters ONLY** — they translate HTTP to/from services and hold no domain/runtime logic; behaviour lives in **services**, features compose as **modules**, and the scaffold must NOT couple to Go-service internals, the CLI, or the replay tool. **API contract parity is mandatory during migration**: `npm run validate:contract` must stay green (TS↔Go is the source of truth, currently **234/234**), the error envelope stays `{ errors: [...] }` (`ValidationError→400`, `ConflictError→409`, `NotFoundError→404`, unknown→500), and **no public `/api` path or DTO is renamed for cosmetic reasons**. No endpoint is migrated without tests AND contract validation in the same slice. A scaffold/test endpoint that is not part of the public contract must stay **outside `/api`** (the scaffold's liveness probe is `GET /health`, not `/api/health`) OR be documented as a deliberate, contract-tested change — do NOT silently flip the documented "TypeScript server: not implemented" status of `/api/health`/`/api/runtime`; implement those behind `validate:contract` in the endpoint-migration milestones. Keep NestJS deps in the **server package only** and aligned with the existing Express major (Nest 11 ↔ Express 5). Wire DI with explicit `@Inject` tokens so it resolves identically under `tsc` and the esbuild-based Vitest transform (which does not emit decorator metadata) — do NOT rely on emitted `design:paramtypes`, and do NOT add a separate SWC/transform pipeline unless a real need is proven and scoped to `server`. Build output stays under `build/`, never `dist/`. The scaffold's bootstrap binds `127.0.0.1` only (never `0.0.0.0`) and must not default to the management port `47831`. **New NestJS migration code must reach 100% meaningful coverage** (controllers, services, modules, bootstrap/route/error-handling helpers, and any static adapter added) — do NOT treat 90–95% as acceptable for migration code, do NOT exclude new Nest code to pass, and do NOT game coverage with trivial tests; every controller route, service branch, and scaffold-introduced error path must be tested. Keep startup glue testable by **extracting logic out of the process entry** (`resolveNestListenOptions`, `bootstrap`, `reportBootstrapFailure` live in covered files; `main.ts` is a logic-free entry) — the ONLY permitted coverage exclusion is that no-logic `main.ts` wiring entry (mirroring `index.ts`, since importing it starts a listener); any other structurally-unreachable branch must be narrowly documented and reported, never broadly excluded. **Do not lower a coverage gate** to absorb scaffold code. No auth/remote/team model, no contract codegen, no DI overengineering, no API redesign, and no packaging change in the scaffold slices.

v1.13 Slice 1 (standalone replay tool scaffold): started v1.13 Local Replay & Offline Analysis with a **separate offline analysis tool beside the CLI** — `replay`, a NEW Go module under `tools/replay/` (module `portier/replay`, stdlib-only), NOT a `portier`/`portier workflow` subcommand. It analyzes EXISTING saved Portier workflow artifacts after the fact; it is fully self-contained — it does NOT import `tools/cli` (any package), the runtime client, or server/service/shared. `validate:contract` 234/234 unchanged (the replay tool is wholly independent of the runtime/API/contract). First command: `replay [--json] plan --from <file-or-dir> [--out <file>]`. Detection is by **JSON/manifest shape, not filename** (`tools/replay/sources/core/detect.go`): a single JSON file is a workflow **run report** (no `schemaVersion`, result passed/failed, has `steps`), **plan report** (`schemaVersion` + result valid/invalid + `steps`), or **history export** (`source:"workflow-history"` + `runs`/`runCount`); a **directory** is a support-report **bundle** detected by reading ONLY its `manifest.json` (`type:"workflow-report"` + `schemaVersion`) — no other bundle file and NO referenced file is read. The **replay plan** (`plan.go`, `Plan`/`PlanItem`/`PlanSummary`, `schemaVersion:1` — a LOCAL tool schema, not a REST/API contract) answers "what offline replay/analysis can this artifact support?" and does NOT replay by re-executing anything: per source kind it lists `available` analyses gated on the artifact's actual inputs (run: step-timeline, step-status-summary, explanation-lookup; plan: step-validation-summary, explanation-lookup; history export: result/workflow/step-status/step-type/code distributions + failed-run-shortlist; bundle: summary-reconstruction, normalized-report-inspection, explanation-lookup) plus FOUR always-`unavailable` safety items in EVERY plan — `replay.execution` (workflow re-execution), `replay.runtime` (runtime probing), `replay.input.reread` (referenced config/policy/baseline/report reread), `replay.mutation` (mutation/enforcement). Each `PlanItem.Inputs` is always a non-nil slice (serializes `[]`, never `null`). Rendering (`render.go`): `RenderHuman` prints `Available:`/`Unavailable:` (item messages, `- none` when a section is empty) + a `Summary:` count; `MarshalPlan` is the single JSON source so `--json` stdout and the `--out` file are byte-identical. `main.go` is the dispatcher (global `--json`; `plan` sub-flagset `--from`/`--out`). Exit codes: `0` plan produced; `1` an `--out` write failure or JSON-encode failure; `2` a usage error (missing/unknown subcommand, missing `--from`) or an unreadable/malformed/unsupported input artifact; there is **NO exit 3** (the tool never contacts a runtime). **Strictly offline & read-only by construction**: never executes workflows, contacts the runtime, reads the config/policy/baseline/report files an artifact refers to (proven by a test: a report referencing absent files still succeeds and the files are never touched), mutates inputs (proven by a before/after byte-compare test), applies/imports configs, enforces policy, schedules jobs, runs shell commands, uploads anything, or collects logs/environment/process data. Detection/plan/render are pure and unit-tested (`detect_test.go`/`plan_test.go`/`render_test.go`); `main_test.go` is black-box over `run(args, stdout, stderr)`. Scripts: `npm run build:replay` (`tools/replay/build/replay`), `test:replay`, `validate:replay`; `scripts/clean-build.js` now removes `tools/replay/build`. Coverage: `replay` pkg 97.8%, `main` 90.7% (the `main()` os.Exit wrapper is the only uncovered path) — no CLI coverage gate change (the replay module is separate and not yet gated). Source: `docs/roadmap.md` v1.13.

**Durable rule (v1.13 Slice 1) — standalone replay tool:** Replay is a **separate offline tool beside the CLI** (`tools/replay/`, `replay`, its own Go module) — NEVER a `portier`/`portier workflow replay` subcommand inside `tools/cli`, and it must stay independently buildable/testable. It must analyze **saved artifacts only** and be **strictly offline and read-only**: it must NEVER execute workflows, contact the Portier runtime, read the config/policy/baseline/report files an artifact refers to (for a support-report bundle directory, read ONLY the bundle's own `manifest.json`/`report.json`/`explanations.json` — never a referenced file), mutate runtime/config/history/report inputs, apply/import configs, enforce policy, schedule jobs, run shell commands, upload anything, or collect logs/environment/process data. **Input detection is by JSON/manifest shape, not filename.** The replay plan is a **capability description** ("what offline analysis can this artifact support?") — it must NOT replay by re-executing anything, and every plan must include the four always-unavailable safety items (execution, runtime probing, referenced-file reread, mutation/enforcement) so the boundary is explicit. Replay **schemas are LOCAL tool schemas** (`schemaVersion: 1` on the plan) — NOT a REST/API/contract surface — unless a schema is intentionally promoted later; they do not affect `validate:contract` (which must stay 234/234). The replay tool must NOT import `tools/cli` (any package), the runtime client, or server/service/shared — if reusing a CLI/workflow struct would couple the tool to the CLI module, define **minimal local DTOs** for detection instead (this slice does). Keep detection/plan/render as pure, unit-tested functions with a thin `main.go` dispatcher; `--json` stdout and the `--out` file must stay byte-identical (one marshaller). Exit codes: `0` success; `1` an output-write/JSON-encode failure; `2` a usage error or an unreadable/malformed/unsupported artifact; **no exit `3`** (the tool never contacts a runtime). The existing CLI stays unchanged unless a clearly-useful, low-risk shared-helper extraction is made; do NOT fold replay into `tools/cli`. Later v1.13 slices add the actual analyses (timeline reconstruction, distributions, correlation, incident reports) on top of this scaffold under the same safety boundary.

v1.13 Slice 2 (replay analyze command): added `replay [--json] analyze --from <file-or-dir> [--out <file>]` — the first real offline analysis command in the replay tool. It inspects a saved artifact and produces deterministic analysis derived ONLY from the artifact's own contents. Replay-tool-only (stdlib, no `tools/cli`/client imports); `validate:contract` 234/234 unchanged. Model/builders in `tools/replay/sources/core/analyze.go`; rendering in `render_analysis.go`; `main.go` dispatches `analyze` (mirrors `runPlan`, separate handler — Slice 1's `runPlan` untouched). Flow: `AnalyzeInput(path)` stats the path (dir → `analyzeBundleDir`, file → `AnalyzeJSON`); `AnalyzeJSON` **reuses Slice 1 `DetectJSON` to classify** then decodes RICHER analysis-specific raw structs (kept separate from detection's lightweight `Input`). Per kind: **run report** → step tallies (`tallyStep`), failed/skipped step IDs (sorted), per-step emitted codes (failed → `embeddedFindingCodes` from the step's EMBEDDED report = error-severity `findings[].code` + `new[].code`, deduped; skipped → `workflow.run.dependency_failed`) unioned with top-level `explanations` map keys, findings (failed-steps / skipped-steps / runtime-unreachable / input-failed) and insights (success / failed-steps / generic did-not-complete / skipped / runtime-unreachable / input-failed); **plan report** → valid/invalid tallies, invalid step IDs, `workflow.step.*` codes, valid-vs-"should not be run" insight; **history export** → run tallies, workflow distribution, **code distribution counting the RUNS containing each code** (`historyCodeCounts`, deduped per run — matching the v1.12 history-stats semantics), failed-run shortlist (finding `Related`), most-common-code + most-recent-run (runs[0], newest-first) insights, empty-export handling; **support-report bundle dir** → reads ONLY the bundle's own `manifest.json` (required, validated via Slice 1 `DetectBundleManifest`), `report.json`, `explanations.json` — NO other/referenced file; a missing/malformed `report.json` is tolerated → minimal analysis. Analysis model (`schemaVersion:1`, a LOCAL tool schema, SEPARATE from the replay-plan schema): `Analysis{source, workflow?, result?, summary{steps:StepSummary{total,passed,failed,skipped,valid,invalid}, runs:RunSummary{total,passed,failed}}, codes[]CodeCount, workflows?[]WorkflowCount, findings[]Finding{severity,message,related?}, insights[]string}`. Deterministic ordering: codes/workflows by count DESC then code/name ASC (`toSortedCodeCounts`/`workflowCounts`), IDs ascending, stable insight order; `codes`/`findings`/`insights` are always non-nil (`[]`, never `null`) via `newAnalysis`; `workflows` is `omitempty` (history only). Human renderer (`RenderAnalysisHuman`) shows a Runs+Workflows block for history else a Step summary (valid/invalid when `isPlanStyle` else passed/failed/skipped), then Codes/Findings/Insights (`- none` when empty); `MarshalAnalysis` is the single JSON source so `--json` stdout and `--out` are byte-identical. Exit codes: `0` analysis created; `1` an `--out` write or JSON-encode failure; `2` a usage error (missing `--from`, bad flag) or an unreadable/malformed/unsupported artifact; **NO exit 3** (never contacts a runtime). Strictly offline & read-only — proven by tests that a report referencing absent files still analyzes (files never touched) and the input bytes are unchanged after a run. Covered by white-box `analyze_test.go` + `render_analysis_test.go` (all four kinds incl. passed/failed/skipped/only-skipped runs, embedded-finding + dependency + explanation-union codes, ordering/tie-breaks, runtime-unreachable/input-failed, valid/invalid plan, empty/populated/all-passed history, runs-containing code counts, bundle dir + manifest-only + wrong-manifest-error + malformed-report-tolerated, `embeddedFindingCodes`/`dedupeStrings`/`firstNonEmpty`, determinism, `[]`-not-null) + black-box `main_analyze_test.go` (human/JSON for run/history/bundle, `--out` + `--json --out` byte parity + write-failure→1, missing-`--from`/unreadable/malformed/unsupported/bad-flag→2, no-input-mutation, does-not-read-referenced-files, **`plan` still works**). Replay package coverage 94.1% → 97.5% (above the 95% target; uncovered remainder = structurally-unreachable defensive branches: the `AnalyzeJSON` default case, post-`DetectJSON` `json.Unmarshal` re-decode errors, `MarshalPlan`/`MarshalAnalysis` encode-on-concrete-type errors, `os.ReadFile`-after-stat failure, the `main()` os.Exit wrapper). The replay module is not gated by `validate:coverage` (separate module); the CLI gate is untouched. Source: `docs/roadmap.md` v1.13.

**Durable rule (v1.13 Slice 2) — replay analyze:** `replay analyze` is **offline artifact analysis ONLY** — it must analyze the supplied artifact deterministically from its OWN contents and parse **only** the supplied `--from` JSON file (or, for a support-report bundle directory, ONLY the bundle's own `manifest.json`/`report.json`/`explanations.json`). It must NEVER execute workflows, contact the runtime, read the config/policy/baseline/report files an artifact refers to (reading a report's EMBEDDED report object inside the artifact JSON is fine; opening a referenced PATH is not), mutate inputs, apply/import configs, enforce policy, schedule jobs, run shell commands, upload anything, or collect logs/environment/process data. It must **reuse Slice 1 detection** (`DetectJSON`/`DetectBundleManifest`) to classify, then decode its own analysis-specific structs — do NOT fork a second classifier or couple to `tools/cli`/workflow internals (define minimal local DTOs). The **replay analysis schema is a LOCAL tool schema** (`schemaVersion: 1`, in package `core`) and is **separate from the replay-plan schema** — keep them distinct; neither is a REST/API/contract surface (don't let them touch `validate:contract`, which stays 234/234). Analysis must be **deterministic**: sort code/workflow distributions by count descending then key ascending, sort ID lists, keep insight order stable, and history code counts must count the **runs containing** each code (codes deduped per run, matching v1.12 history stats) — never silently nondeterministic map iteration. Output slices (`codes`/`findings`/`insights`) serialize as `[]` not `null`; `--json` stdout and the `--out` file stay byte-identical (one marshaller). Exit codes: `0` analysis created; `1` an output-write/JSON-encode failure; `2` a usage error or an unreadable/malformed/unsupported artifact; **no exit `3`** (never contacts a runtime). Add focused tests for every new analyzer/helper in the same slice and keep replay-package coverage high (≥95%). The CLI remains the operational artifact **producer**; replay remains the offline artifact **analyzer** — do NOT migrate or duplicate CLI commands, and do NOT add execution/rerun/scheduler/runtime-probing/history-import/upload under this banner. Existing `replay plan` and the whole CLI must stay unchanged.

v1.13 Slice 3 (replay tool naming & package-structure polish): behavior-preserving cleanup of the standalone replay tool before its surface grows — no replay behavior change. **Binary renamed `portier-replay` → `replay`** (`replay.exe` on Windows): `npm run build:replay` is now `go -C tools/replay build -o build/replay ./sources` (explicit `-o` name, matching `build:cli`'s `portier-cli` convention, so the file is `build/replay`); the help banner, usage lines, and all command examples now read `replay …`. **Domain package `tools/replay/sources/replay` → `tools/replay/sources/core`** (`git mv` of detect/plan/render/analyze/render_analysis + tests, `package replay` → `package core`); **CLI dispatch extracted into a new `tools/replay/sources/commands` package** (exported `Run(args []string, stdout, stderr io.Writer) int` plus `runPlan`/`runAnalyze`/`printHelp`, importing `core`); `main.go` is now a thin `os.Exit(commands.Run(os.Args[1:], os.Stdout, os.Stderr))`. Layering enforced: `commands` → `core` (one-way; `core` has no arg-parsing/command dependency), neither imports `tools/cli`/client/server/service/shared. The black-box command tests moved to package `commands` (`run(...)` → `Run(...)`); help-text tests assert the `replay` banner and the **absence** of `portier-replay`; a new package-`main` `TestBuildOutputName` runs `go build -o <tmp>/replay .` and asserts a `replay`/`replay.exe` artifact and no `portier-replay`. Output, exit codes (`0`/`1`/`2`, no `3`), the plan/analysis schemas, and the offline/read-only safety boundary are all unchanged; `validate:contract` 234/234. Coverage: `core` 97.5%, `commands` 90.5% (the dispatch handlers' only uncovered lines are the same structurally-unreachable `Marshal*` encode / `stdout.Write` error branches that existed pre-refactor), `main` is the bare `os.Exit` wrapper; the replay module remains separate and ungated by `validate:coverage` (CLI gate untouched). Docs/scripts updated together (`tools/replay/README.md` incl. a Structure section, root `README.md`, `docs/glossary.md`, `docs/changelog.md` Slice 3, `docs/roadmap.md`, this file + `AGENTS.md`); `scripts/clean-build.js` already removes `tools/replay/build`. The changelog's Slice 1/2 entries keep the historical `portier-replay` name; no other stale `portier-replay`/`sources/replay` references remain. Source: `docs/roadmap.md` v1.13.

**Durable rule (v1.13 Slice 3) — replay tool layout & binary name:** The standalone replay tool's binary is **`replay`** (`replay.exe` on Windows), built via `go -C tools/replay build -o build/replay ./sources` — do NOT reintroduce the `portier-replay` name (it only survives in historical changelog entries). The tool's Go layout is fixed: **`tools/replay/sources/core`** holds the domain logic (artifact detection, replay plan/analysis models, builders, renderers); **`tools/replay/sources/commands`** holds CLI dispatch, argument parsing, and the command runners (exported `Run`); **`tools/replay/sources/main.go`** is a thin entry point that only calls `commands.Run`. There is no `sources/replay` package. Layering is one-way — **`commands` may import `core`, but `core` must NEVER import `commands`** (domain logic stays free of argument/flag parsing), and neither package may import `tools/cli`, the runtime client, or server/service/shared. New replay commands add a runner in `commands` and put any new model/detection/analysis logic in `core` (or a new domain package) — do NOT push domain logic into `commands` or `main.go`, and do NOT fold the replay tool into `tools/cli`. This is a structural/naming rule only: it must not change replay behavior, output, exit codes, schemas, or the offline/read-only safety boundary, and must keep `validate:contract` at 234/234.

v1.13 Slice 4 (replay timeline command): added `replay [--json] timeline --from <file-or-dir> [--out <file>]` — reconstructs a deterministic, ordered timeline of what a saved artifact says happened. Replay-tool-only (stdlib, no `tools/cli`/client imports); `validate:contract` 234/234 unchanged. Model/builders in `tools/replay/sources/core/timeline.go`, rendering in `core/render_timeline.go`, runner in `commands/timeline.go` (`runTimeline`, dispatched from `commands.Run`); reuses Slice 1 detection (`DetectJSON`/`DetectBundleManifest`) + `embeddedFindingCodes`/`dedupeStrings`/`firstNonEmpty`. Flow mirrors analyze: `TimelineInput(path)` (dir → `timelineBundleDir`, file → `TimelineJSON`); `TimelineJSON` classifies via `DetectJSON` then decodes timeline-specific raw structs (richer than analyze's — captures per-step `exitCode`/`message`, and per-run `createdAt`/`summary`). Each timeline brackets the artifact's SAVED events with two clearly-marked SYNTHETIC lifecycle events (`*-start` with status `started`; `*-result`/`*-end` with the artifact result, or `reconstructed` when none); `TimelineEvent.Synthetic` is ALWAYS serialized (`json:"synthetic"`) and the summary carries a `synthetic` count, so reconstruction markers are unambiguous. NO timestamps are invented — order is the saved order, via a 1-based `Order`. Per kind: **run report** → step events (status/exitCode/message; failed → `embeddedFindingCodes`, skipped → `workflow.run.dependency_failed`); **plan report** → valid/invalid step events + `workflow.step.*` codes; **history export** → run events in **newest-first** stored order (id/createdAt/workflow/result(status)/per-run `runSummary`/codes), with no top-level `result`; **support bundle dir** → events from the bundle's own `manifest.json`/`report.json` ONLY (missing/malformed report → minimal start+result). Model (`schemaVersion:1`, a LOCAL tool schema SEPARATE from the plan and analysis schemas): `Timeline{source, workflow?, result?, events[]TimelineEvent, summary TimelineSummary{total,passed,failed,skipped,valid,invalid,synthetic}}`; `events` is always non-nil (`[]`). Summary: synthetic events count only toward `total`+`synthetic`; saved events toward their status bucket. Human renderer prints each event (synthetic → message; saved → type + workflow/created/exit-code/codes) + a summary (valid/invalid lines shown only when present); `MarshalTimeline` is the single JSON source so `--json` stdout and `--out` are byte-identical. Exit codes: `0` timeline created; `1` an `--out` write or JSON-encode failure; `2` a usage error (missing `--from`, bad flag) or an unreadable/malformed/unsupported artifact; **NO exit 3** (never contacts a runtime). Strictly offline & read-only — proven by tests (a report referencing absent files still builds a timeline; files never touched; input bytes unchanged). Covered by white-box `core/timeline_test.go`+`render_timeline_test.go` (all four kinds incl. passed/failed/skipped run, codes, valid/invalid plan, populated/empty history + newest-first + per-run summary, bundle dir + manifest-only + no-result + wrong-manifest, sequential order, deterministic, malformed/unsupported/missing-path, synthetic-marking, human/JSON shape, `[]`-not-null) + black-box `commands/commands_timeline_test.go` (run/history/bundle human+JSON, `--out`/`--json --out` byte parity + write-failure→1, missing-`--from`/unreadable/malformed/unsupported/bad-flag→2, no-input-mutation, does-not-read-referenced-files, **`plan` & `analyze` still work**). Replay **module-wide coverage 96.4%** (`go test -coverpkg=./...`): core 97.0% (the dip from 97.5% is the new structurally-unreachable defensive branches: `MarshalTimeline` encode-on-concrete-type, `TimelineInput` read-after-stat, `TimelineJSON` default case, the per-builder post-`DetectJSON` `json.Unmarshal` re-decode errors), commands 94.8% (raised by a `failWriter` test covering the real stdout-write-failure path of `plan`/`analyze`/`timeline` `--json`; the only remaining uncovered commands branches are the unreachable `Marshal*`-encode errors), and the `main()` `os.Exit` wrapper at 0%. The replay module is ungated by `validate:coverage` but is held to a **≥95% module bar** as an acceptance requirement; CLI gate untouched. Source: `docs/roadmap.md` v1.13.

**Durable rule (v1.13 Slice 4) — replay timeline:** `replay timeline` is **offline artifact analysis ONLY** — it reconstructs a deterministic ordered view from the supplied artifact's OWN contents and parses **only** the supplied `--from` JSON file (or, for a support-report bundle directory, ONLY the bundle's own `manifest.json`/`report.json`). It must NEVER execute workflows, contact the runtime, read the config/policy/baseline/report files an artifact refers to (reading a report's EMBEDDED report object is fine; opening a referenced PATH is not), mutate inputs, apply/import, enforce policy, schedule, run shells, upload, or collect logs/env/process data. It must **reuse the existing detection** (`DetectJSON`/`DetectBundleManifest`) to classify, then decode timeline-specific structs in `core` — do NOT fork a second classifier or couple to `tools/cli`/workflow internals. The timeline must **clearly distinguish SYNTHETIC reconstruction events from SAVED artifact events**: keep the always-serialized `synthetic` boolean on every event and the `synthetic` count in the summary, and bracket saved events with synthetic `*-start`/`*-result` (or `*-end`) lifecycle markers. It must **NEVER invent timestamps** the artifact does not contain — order is the saved order only (1-based `Order`); history events stay in the export's stored **newest-first** order (keep this tested). The **replay timeline schema is a LOCAL tool schema** (`schemaVersion: 1`, package `core`), **separate from the plan and analysis schemas** — keep them distinct; none is a REST/API/contract surface (`validate:contract` stays 234/234). Output is deterministic; `events` serializes as `[]` not `null`; `--json` stdout and the `--out` file are byte-identical (one marshaller). Exit codes: `0` timeline created; `1` an output-write/JSON-encode failure; `2` a usage error or an unreadable/malformed/unsupported artifact; **no exit `3`** (never contacts a runtime). Add focused tests for every new builder/helper. **The replay module must finish at ≥95% coverage (acceptance requirement), measured module-wide via `go -C tools/replay test -coverpkg=./... ./...`** — when a slice drops it below 95%, add MEANINGFUL tests for real branches/error cases (e.g. a `failWriter` for the stdout-write-failure path), never coverage-gaming tests; the only acceptable uncovered code is structurally-unreachable defensive branches (encode-on-concrete-type, post-detection `json` re-decode, read-after-stat, unused-classifier `default` arms) and the `main()` `os.Exit` wrapper, which must be documented. The CLI remains the operational artifact **producer**; replay remains the offline artifact **analyzer** — do NOT migrate/duplicate CLI commands or add execution/rerun/scheduler/runtime-probing/history-import/upload. `replay plan`/`analyze` and the whole CLI must stay unchanged.

v1.13 Slice 5 (replay compare command): added `replay [--json] compare --left <file-or-dir> --right <file-or-dir> [--out <file>]` — compares two saved artifacts offline and reports what changed. Replay-tool-only (stdlib, no `tools/cli`/client imports); `validate:contract` 234/234 unchanged. Model/builders in `tools/replay/sources/core/compare.go`, rendering in `core/render_compare.go`, runner in `commands/compare.go` (`runCompare`, dispatched from `commands.Run`). Built on a single-parse `compareFacts` extractor that reuses the existing detection (`DetectJSON`/`DetectBundleManifest`) + the timeline raw structs + the shared helpers (`tallyStep`/`embeddedFindingCodes`/`dedupeStrings`/`sortedMapKeys`/`firstNonEmpty`) — no third raw-struct family, no second classifier. `CompareInputs(left,right)` extracts facts for each side (`extractFacts` → dir=`bundleFacts` reading ONLY the bundle's own `manifest.json`/`report.json`, file=`factsFromJSON`→per-kind facts) then `BuildCompare`. Per same-kind builder: run-vs-run (workflow/result change, step count deltas, code/failed-step/skipped-step set diffs), plan-vs-plan (validity change + "Plan became valid/invalid.", valid/invalid count deltas, code/invalid-step set diffs), history-vs-history (run-count/passed/failed deltas, workflow-distribution deltas over a SORTED key union, code set, failed-run set, most-recent-run change, increased/decreased + no-comparable-runs insights), bundle-vs-bundle (workflow/result + six step-count deltas + code set). **Mixed-kind** (left.Source != right.Source) → a deliberately LIMITED result: one `kind.mismatch` change ("Artifacts are different kinds; detailed comparison is limited.") + a `replay analyze` pointer insight + NO `sets`; never a crash, never a false equivalence claim. Model (`schemaVersion:1`, a LOCAL tool schema SEPARATE from plan/analysis/timeline): `Compare{left CompareSide, right CompareSide, summary{sameKind,changed,changeCount}, changes[]CompareChange{id,type,message,left?,right?,added?,removed?}, sets map[string]CompareSet{added,removed,unchanged} (omitempty), insights[]}`. Determinism: changes appended in a fixed semantic order per builder (NO map iteration for change order — workflow deltas iterate `sortedUnionKeys`); `diffSets` returns sorted non-nil added/removed/unchanged; Go encodes the `sets` map with sorted keys. Each set diff emits up to two single-list changes (`<dim>.added` with `Added`, `<dim>.removed` with `Removed`) so human rendering is unambiguous (message + `  - <item>` sub-bullets). `changed`=`changeCount>0`; an equal same-kind pair yields 0 changes + the "No meaningful differences detected." insight (sets still present, showing `unchanged`). `MarshalCompare` is the single JSON source so `--json` stdout and `--out` are byte-identical. Exit codes: `0` comparison created; `1` an `--out` write or JSON-encode failure; `2` a usage error (missing `--left`/`--right`, bad flag) or an unreadable/malformed/unsupported artifact; **NO exit 3** (never contacts a runtime). Strictly offline & read-only — proven by tests (both inputs unchanged after a run; a report referencing absent files still compares; files never touched). Covered by white-box `core/compare_test.go`/`render_compare_test.go` (passed↔failed run, code add/remove/unchanged, skipped-step change, equal=no-changes, valid↔invalid plan, plan code add/remove, empty↔populated history, failed-count increase/decrease, both-empty, workflow-delta name-ascending ordering, bundle pair, mixed-kind, deterministic, `diffSets`, file/bundle/unreadable/malformed/unsupported, removed-items + no-changes + mixed-kind render, `[]`-not-null) + black-box `commands/commands_compare_test.go` (run/history human+JSON, mixed-kind, missing-`--left`/`--right`, unreadable-left/right, malformed/unsupported/bad-flag→2, `--out`/`--json --out` byte parity + out-write-failure→1 + stdout-write-failure→1 via `failWriter`, no-input-mutation on BOTH sides, does-not-read-referenced-files, **plan/analyze/timeline still work**). Replay **module-wide coverage 96.4%** (`go test -coverpkg=./...`): core 96.7%, commands 94.7%, `main()` 0% wrapper — meets the ≥95% bar; remaining gaps are the documented structurally-unreachable defensive branches. Source: `docs/roadmap.md` v1.13.

**Durable rule (v1.13 Slice 5) — replay compare:** `replay compare` is **offline artifact comparison ONLY** — it parses **only** the two supplied `--left`/`--right` artifacts (and, for a support-report bundle directory, ONLY the bundle's own `manifest.json`/`report.json`). It must NEVER execute workflows, contact the runtime, read the config/policy/baseline/report files an artifact refers to (an EMBEDDED report object is fine; a referenced PATH is not), mutate either input, apply/import, enforce policy, schedule, run shells, upload, or collect logs/env/process data. It must **reuse the existing detection + facts extraction** (`DetectJSON`/`DetectBundleManifest` + the single-parse `compareFacts`) — do NOT fork a second classifier or a third raw-struct family, and do NOT couple to `tools/cli`/workflow internals. **Mixed-kind comparisons MUST be clearly marked LIMITED** (a `kind.mismatch` change + a pointer to `replay analyze`, no `sets`) — never a crash, never a false claim of cross-kind equivalence; a kind difference is not an excuse to error (only undetectable/unparseable input is exit 2). Output MUST be **deterministic**: emit changes in a fixed semantic order (never raw map iteration — iterate sorted key unions for distribution deltas), sort set values ascending, and keep `diffSets` returning non-nil sorted added/removed/unchanged (serialize as `[]`, never `null`). The **replay compare schema is a LOCAL tool schema** (`schemaVersion: 1`, package `core`), **separate from the plan/analysis/timeline schemas** — none is a REST/API/contract surface (`validate:contract` stays 234/234). `--json` stdout and the `--out` file are byte-identical (one marshaller). Exit codes: `0` comparison created; `1` an output-write/JSON-encode failure; `2` a usage error (missing `--left`/`--right`) or an unreadable/malformed/unsupported artifact; **no exit `3`** (never contacts a runtime). Keep the replay module at **≥95% module-wide coverage** (per the acceptance requirement) with meaningful tests. The CLI remains the operational artifact **producer**; replay remains the offline artifact **analyzer** — do NOT migrate/duplicate CLI commands or add execution/rerun/scheduler/runtime-probing/history-import/upload. `replay plan`/`analyze`/`timeline` and the whole CLI must stay unchanged.

v1.13 Slice 6 (replay explain command): added `replay [--json] explain --from <file-or-dir> [--out <file>]` (+ `--code <code>` / `--list`, mutually exclusive) — explains the emitted codes a saved artifact records, mapping known codes to local offline explanations and PRESERVING unknown codes (marked `known:false`, never dropped). Replay-tool-only (stdlib, no `tools/cli`/client imports); `validate:contract` 234/234 unchanged. Model/registry/extraction in `tools/replay/sources/core/explain.go`, rendering in `core/render_explain.go`, runner in `commands/explain.go` (`runExplain`, dispatched from `commands.Run`). Inputs: the four workflow artifacts (codes via the existing `factsFromJSON`/`bundleFacts` — no new parsing) PLUS replay tool outputs (analysis/timeline/compare JSON) detected by shape in `replayOutputCodes` (compare→`sets["codes"]`, timeline→union of `events[].codes`, analysis→`codes[].code`). `explainCodesFromJSON` tries workflow detection first, then replay-output detection, else unsupported (exit 2). Codes are deduped + **sorted ascending**; a no-code artifact is **success (exit 0)** with a clear notice. Local explanation registry (24 codes: 15 `workflow.step.*` + 3 `workflow.run.*` + 6 `policy.*`), an `init()` stamping `Code`/`Known` from the map key; an integrity test locks the 24-entry set (Code==key, valid severity, non-empty fields). Doctor/runtime codes are intentionally OUT of scope (they never appear in workflow artifacts) and surface as `unknown`. Model (`schemaVersion:1`, a LOCAL tool schema SEPARATE from plan/analysis/timeline/compare): `Explain{source, codeCount, known, unknown, explanations[]Explanation{code,known,title,severity,meaning,suggestion}}`; `explanations` always non-nil (`[]`). Severity classification: `*.valid`→info, `workflow.*` failures→error, `policy.*` violations→warning, unknown→`unknown`. Human renderer shows a header (`Known`/`Unknown` only when codeCount>0) then one block per code, or a no-codes notice; `MarshalExplain` is the single JSON source so `--json` stdout and `--out` are byte-identical. `--code` reuses `ExplainCode` (source `code`), `--list` reuses `ExplainRegistry` (source `registry`, all 24). Command requires exactly one of `--from`/`--code`/`--list` (none→2, >1→2). Exit codes: `0` produced (incl. no codes); `1` an `--out` write or JSON-encode failure; `2` a usage error or an unreadable/malformed/unsupported artifact; **NO exit 3**. Strictly offline & read-only — proven by tests (input unchanged; a report referencing absent files still explains; files never touched). Covered by white-box `core/explain_test.go`/`render_explain_test.go` (registry integrity + sorted knownCodes, build known/unknown/no-codes, run/plan/history extraction + dedupe + sort order, replay analysis/timeline/compare detection + not-a-replay-output, file/bundle/missing-path, `--code`/`--list`, `sortedDedupeStrings`, deterministic, human/JSON shape, `[]`-not-null) + black-box `commands/commands_explain_test.go` (run human+JSON, history-no-codes→0, unknown-code-preserved, replay-analysis-output, `--code`/`--code`-unknown/`--list`=24, no-mode→2, multi-mode→2, unreadable/malformed/unsupported/bad-flag→2, `--out`/`--json --out` byte parity + out-write-failure→1 + stdout-write-failure→1 via `failWriter`, no-input-mutation, does-not-read-referenced-files, **plan/analyze/timeline/compare still work**). Replay **module-wide coverage 96.5%** (`go test -coverpkg=./...`): core 96.7%, commands 94.9%, `main()` 0% wrapper — meets the ≥95% bar; remaining gaps are documented structurally-unreachable defensive branches. Source: `docs/roadmap.md` v1.13.

**Durable rule (v1.13 Slice 6) — replay explain:** `replay explain` is **offline code explanation ONLY** — it parses **only** the supplied artifact (`--from`: a workflow run/plan report, history export, support-report bundle directory's own `manifest.json`/`report.json`, or a replay analysis/timeline/compare JSON) or operates purely on the local registry (`--code`/`--list`). It must NEVER execute workflows, contact the runtime, read the config/policy/baseline/report files an artifact refers to (an EMBEDDED report object is fine; a referenced PATH is not), mutate the input, apply/import, enforce policy, schedule, run shells, upload, or collect logs/env/process data. It MUST **reuse the existing detection + facts extraction** (`factsFromJSON`/`bundleFacts` for workflow artifacts; `replayOutputCodes` for replay outputs) — do NOT fork a new classifier or a new raw-struct family. **Unknown codes MUST be preserved and marked `known:false`** (never dropped or errored on); a **no-code artifact is success (exit 0)**, not an error. Codes MUST be deduped and **sorted ascending** (deterministic). The explanation registry is a **small LOCAL registry in `core`** (`workflow.step.*`/`workflow.run.*`/`policy.*`) — it must NOT import the CLI explain registry or any runtime/`tools/cli` package; doctor/runtime codes stay out of scope (reported unknown). Keep the registry integrity-tested (every entry `Code`==key, `Known` true, valid severity, non-empty fields). The **replay explain schema is a LOCAL tool schema** (`schemaVersion: 1`, package `core`), **separate from the plan/analysis/timeline/compare schemas** — none is a REST/API/contract surface (`validate:contract` stays 234/234). `explanations` serializes as `[]` not `null`; `--json` stdout and the `--out` file are byte-identical (one marshaller). The command requires exactly one of `--from`/`--code`/`--list`. Exit codes: `0` produced (incl. no codes); `1` an output-write/JSON-encode failure; `2` a usage error or an unreadable/malformed/unsupported artifact; **no exit `3`** (never contacts a runtime). Keep the replay module at **≥95% module-wide coverage** with meaningful tests. The CLI remains the operational artifact **producer**; replay remains the offline artifact **analyzer** — do NOT migrate/duplicate CLI commands or add execution/rerun/scheduler/runtime-probing/history-import/upload. `replay plan`/`analyze`/`timeline`/`compare` and the whole CLI must stay unchanged.

v1.13 Slice 7 (replay toolkit consistency audit): focused audit of the standalone replay tool (Slices 1–6) before v1.13 finalization — audit/cleanup only, no new features, no behavior change. **Result: PASS, v1.13 replay toolkit ready for finalization.** Verified: the five commands (`plan`/`analyze`/`timeline`/`compare`/`explain` + `explain --code`/`--list`) dispatch consistently with a uniform `--json`/`--out`/byte-parity convention and the `0`/`1`/`2` exit-code policy (no `3`); all five local schemas carry `schemaVersion:1`, serialize arrays as `[]` (never `null`), and render deterministically (every renderer iterates ordered SLICES — confirmed **no map iteration**; the `compare` `sets` map relies on Go's sorted-key JSON encoding); timeline always marks `synthetic`, explain preserves unknown codes `known:false`, mixed-kind compare is clearly limited; source-kind names are consistent (`workflow-run-report`/`workflow-plan-report`/`workflow-history-export`/`workflow-report-bundle` — NOTE the bundle name is "workflow-report-bundle", matching the manifest `type:"workflow-report"`; the audit-prompt's "workflow-support-bundle" is descriptive, NOT the canonical value — plus `replay-analysis`/`replay-timeline`/`replay-compare`); no replay schema leaks into the REST contract. Safety/architecture by inspection: no `tools/cli`/runtime-client/`net/http`/`os/exec`/`os.Getenv` imports in product code (the only `os/exec` is the `TestBuildOutputName` build test); `core` never imports `commands`; bundle directories read ONLY `manifest.json`/`report.json`/`explanations.json`; no referenced-file reads, no mutation beyond `--out`, no telemetry/upload/log/env/process collection; the full per-command regression matrix (human/JSON/`--out`/`--json --out`-parity/out-write-failure/stdout-write-failure/no-mutation/no-referenced-read) is present for all five. Fixes (docs + tests, no behavior change): stale `tools/replay/README.md` intro (Slice-1 "scaffold/plan only" language → the five-command tool) and incomplete bundle-files statement (now manifest/report/explanations + malformed-report tolerance); command-generic Exit-codes section + "no exit 3"; refreshed root `README.md` replay section (all five commands); added an **Exit codes** block to the tool help + a help regression test (every command + exit-code docs); added **malformed-bundle-report-tolerated** tests for `timeline` and `compare` (the invariant was previously asserted only for `analyze`). `validate:contract` 234/234; replay module-wide coverage 96.5% (core 96.7%, commands 94.9%; `go test -coverpkg=./...`) ≥95% bar; gofmt/vet/lint/typecheck clean. No release blockers; no new durable rule — the audit confirms the Slice 1–6 rules. **v1.13 finalization** also added the enforcing coverage gate, in `scripts/validate-coverage.js` alongside every other component gate: a new `replay` entry in its `GATES` (`{ statements: 95 }`), run via `npm run validate:coverage:replay` (= `validate-coverage.js --only replay`) and included in the aggregate `npm run validate:coverage`. Coverage is gathered module-wide by the new reporter `scripts/coverage-tools-replay.js` (`go -C tools/replay test -coverpkg=./... ./...`, run as `npm run coverage:replay`, modeled on `scripts/coverage-tools-cli.js`) which writes `coverage/replay/coverage-summary.json`; `validate-coverage.js` reads it and applies the 95% gate (verified: passes at 96.5%; `--only replay` exits non-zero when the gate is forced above the actual). The replay module is gated **separately** because it is its own Go module (`portier/replay`), but the gate **lives in the same place as the others** (`validate-coverage.js` GATES); the CLI/workspace gates are unchanged and replay does not depend on the CLI module. The replay ≥95% durable rule is machine-enforced here; run `npm run validate:coverage:replay` as part of replay validation. Source: `docs/roadmap.md` v1.13.

v1.12 Slice 5 (workflow history prune): added `portier workflow history prune` — destructive, `--yes`-gated removal of local history entries in one explicit mode: retention (`--keep <n>` keeps the newest N, removing older) or filter (`--result`/`--workflow`/`--code` REMOVE matching entries, AND-combined). CLI-only, `validate:contract` 234/234 unchanged (no shared/server/service/client/API/DTO/config change), CLI coverage 97.7% → 97.8% (gate 97 PASS, not lowered/raised — still short of a stable 98% buffer). Prune model/pure helper live in the `workflow` package (`workflow/history.go`): `HistoryPruneOptions{Keep *int; Filters HistoryFilters}` (keep-mode iff `Keep != nil`, else filter-mode; `Filters.Limit` ignored) + `HistoryPruneResult{schemaVersion, removed, kept, totalBefore, totalAfter}` + `PruneHistory(h, opts) (History, HistoryPruneResult)` (PURE — copies kept runs, never mutates input; keep-mode keeps newest N via the newest-first slice and clamps a negative N to 0; filter-mode keeps the runs NOT matching `runMatchesFilters`; an unmatched filter or `N >= len` removes nothing) + a new `HistoryStore.Save(h)` (stamps schemaVersion, MkdirAll + `output.WritePrettyJSON`). `runMatchesFilters(r, f)` (result/workflow/code, Limit ignored) was factored out of `ApplyHistoryFilters` and reused by prune. `commands` stays handlers-only (`runWorkflowHistoryPrune`): defines `--keep`/`--result`/`--workflow`/`--code`/`--yes` (NO `--limit`), parses via `parseHistoryFlags`, uses `fs.Visit` to detect keep-mode vs filter-mode, then validates — exactly one mode required (no mode → 2; `--keep` + filters → 2), `--keep` non-negative (negative → 2; non-numeric → flag-parse 2), filters validated like list/stats (unknown result/empty workflow/empty code → 2), and `--yes` required (missing → 2) — ALL usage errors return BEFORE touching the store (no mutation). Then loads via the new `loadHistoryStore(stderr)` (path-resolution → 1; `loadHistoryForRead` now delegates to it), `store.Load()` (read failure → 1), `PruneHistory`, and `store.Save(kept)` ONLY when `result.Removed > 0` (write failure → 1) — so a no-op prune (removed 0, incl. missing history) never rewrites/creates the file. `--json` emits `HistoryPruneResult`; human (`printHistoryPruneHuman`) prints `Removed`/`Kept`/`Total before`/`Total after`. Empty match AND missing/empty history are SUCCESS (exit 0, removed 0). Exit: 0 success (incl. removed 0), 1 read/write failure, 2 usage error; NO 3 (never contacts runtime). Prune mutates ONLY the compact local history store — never the runtime, workflow execution, referenced files, logs/env/process, or configs/policies/reports. NO date pruning, NO restore/import. Guarded by white-box `workflow/history_internal_test.go` (keep newest N, keep 0 removes all, keep ≥ count, negative-clamp, empty history, filter by result/workflow/code, AND composition, empty match, no-input-mutation, `Save` round-trip) + white-box `commands/workflow_history_internal_test.go` (keep + keep-0 human, filter prune + AND no-op, empty-match no-rewrite, missing-`--yes`→2-no-mutation, mode-validation table→2, empty/missing-history→0-no-file, read-failure→1, write-failure→1 via a read-only file (skips if root), usage-error no-mutation, path-resolution→1, JSON-encode→1, flag edges). New prune functions 100% covered. Source: `docs/roadmap.md` v1.12.

**Durable rule (v1.12 Slice 5) — workflow history prune:** `workflow history prune` is **destructive and MUST require `--yes`** (missing `--yes` → exit 2, NO mutation), mirroring `history clear`. It must operate **only on the compact local history store** and mutate ONLY that file — it must NEVER contact the runtime, execute a workflow, read the files a workflow step refers to, collect logs/env/process data, or touch configs/policies/reports. **Prune modes must be explicit and unambiguous**: exactly one of `--keep <n>` (retention; keep the newest N, `N >= 0`, `--keep 0` removes all) OR one-or-more filters (`--result`/`--workflow`/`--code`, AND-combined, which REMOVE matching entries) — no mode → 2, `--keep` + filters → 2, invalid `--keep`/filter value → 2; **`--limit` is NOT accepted** for prune. **Every usage error must return BEFORE the store is read/written** (validate mode + `--yes` first). **Empty match sets and missing/empty history are successful no-ops** (exit 0, removed 0) — and a no-op prune (removed 0) must NOT rewrite or create the store file. The prune model + pure logic (`HistoryPruneOptions`/`HistoryPruneResult`/`PruneHistory`) live in the `workflow` package and must NOT import `client`/`commands`; `PruneHistory` must stay pure (never mutate its input runs); persistence is via `HistoryStore.Save`; `commands` stays handlers-only (parse + validate the mode/`--yes`, load, prune, save only when something was removed). The `--json` shape is `{schemaVersion, removed, kept, totalBefore, totalAfter}` — keep it stable. Exit codes: 0 success (incl. removed 0), 1 a history read/write failure, 2 a usage error; there is NO exit 3 (prune never contacts the runtime). Do NOT add date pruning, history restore/import, sync, telemetry, or upload. CLI stays a pure API client; no API/DTO/contract change.

v1.12 Slice 4 (workflow history stats): added `portier workflow history stats` — a deterministic summary of the (optionally filtered) local workflow run history by result, workflow name, step status, step type, and emitted code. CLI-only, `validate:contract` 234/234 unchanged (no shared/server/service/client/API/DTO/config change), CLI coverage 97.6% → 97.7% (gate 97 PASS, not lowered/raised). Stats model/counting/sorting live in the `workflow` package (`workflow/history.go`): `HistoryStats{schemaVersion, filters *HistoryFilters (omitempty), totalStored, shown, results, steps, workflows[], stepTypes[], codes[]}` + fixed-key `HistoryResultCounts{passed,failed}`/`HistoryStepCounts{passed,failed,skipped}` + grouped `WorkflowCount{name,count}`/`StepTypeCount{type,count}`/`CodeCount{code,count}` + `BuildHistoryStats(h, f)` + the generic `sortCounts[T](items, key func(T)(int,string))` (count desc, then key asc). `BuildHistoryStats` reuses `ApplyHistoryFilters` (filters applied BEFORE counting), counts only compact metadata, counts each code by the NUMBER OF RUNS containing it (codes are deduped per run, so a per-run increment = runs-containing), sets `filters` only when `hasAny()`, keeps grouped slices non-nil (`[]`), and never mutates `h`. `commands` stays handlers-only (`runWorkflowHistoryStats`): the list/stats filter flag parse+validate was factored into a shared `parseHistoryFilters(fs, args, …) (HistoryFilters, code, done)` (defines `--result`/`--workflow`/`--code`/`--limit`, reuses `parseHistoryFlags`, validates via `fs.Visit` set-vs-default: unknown `--result`/empty `--workflow`/empty `--code`/non-positive `--limit` → 2, non-numeric `--limit` → flag-parse 2), the store load into `loadHistoryForRead(stderr) (History, code, done)` (path-resolution/read failure → 1), and the human Filters block into `printHistoryFilterBlock` — all shared by `list` and `stats`. `--json` emits `HistoryStats`; human (`printHistoryStatsHuman`) prints an optional Filters block, `Runs:`/`Steps:` buckets, then the grouped tables (empty tables OMITTED), with a short `Result: no history` form when `totalStored==0`. Empty history AND empty match sets are SUCCESS (exit 0, zero stats). Exit: 0 success, 1 history read failure, 2 invalid filter value; NO 3 (never contacts runtime). Stats never mutates the store, contacts the runtime, executes a workflow, reads workflow-referenced files, or scans raw reports. Guarded by white-box `workflow/history_internal_test.go` (empty, result/step/workflow/step-type/code counts, code-counts-runs-not-occurrences, count-desc/key-asc sorting, filtered stats, empty-match zero stats, no-input-mutation) + white-box `commands/workflow_history_internal_test.go` (empty human + JSON with `[]` grouped arrays, JSON counts+sorting, human shape, per-filter + AND + limit-before-stats, empty-match→0, invalid filters→2, read-failure→1, path-resolution→1, JSON-encode→1, store-not-mutated, flag edges). New stats functions 100% covered. Source: `docs/roadmap.md` v1.12.

**Durable rule (v1.12 Slice 4) — workflow history stats:** `workflow history stats` must operate **only on the compact local history metadata** — it must NEVER contact the runtime, execute a workflow, read the files a workflow step refers to, scan raw reports, collect logs/env/process data, or mutate the store. It must **reuse the same filters as `workflow history list`** (`--result`/`--workflow`/`--code`/`--limit`, via the shared `parseHistoryFilters`), AND-combined, **applied BEFORE computing stats** — do NOT add new filter types here. **Empty history and empty match sets are successes (exit 0) with zero stats**, not errors; an invalid filter value is exit 2; a history read failure is exit 1; there is NO exit 3 (it never contacts the runtime). **Code counts count the RUNS containing a code, not total occurrences** (history stores deduped codes per run) — keep this semantics tested. Grouped tables (workflows/step types/codes) must be **deterministically sorted (count descending, then key ascending)** and always serialize as arrays (`[]` when empty); the fixed result/step buckets are objects with stable keys; `filters` appears only when a filter is active (mirroring the list view). The stats model, counting, and sorting (`HistoryStats`/`BuildHistoryStats`/`sortCounts`) live in the `workflow` package and must NOT import `client`/`commands`; `commands` stays handlers-only (share the filter parsing/validation, store load, and Filters-block rendering with `list` — do not duplicate them). The `--json` shape is `{schemaVersion, filters?, totalStored, shown, results, steps, workflows[], stepTypes[], codes[]}` — keep it stable. Do NOT add date bucketing, trend charts, raw-report scanning, history import/sync, telemetry, or upload. CLI stays a pure API client; no API/DTO/contract change.

v1.12 Slice 3 (workflow history filtering): added AND-combined filters to `portier workflow history list` — `--result <passed|failed>` (exact run result), `--workflow <name>` (exact, case-sensitive), `--code <code>` (run whose `codes` list contains the exact code), `--limit <n>` (keep the newest N matches, applied AFTER filtering). CLI-only, `validate:contract` 234/234 unchanged (no shared/server/service/client/API/DTO/config change), CLI coverage 97.6% (gate 97 PASS, not lowered/raised). Filter model/matching/list-view live in the `workflow` package (`workflow/history.go`): `HistoryFilters{Result,Workflow,Code string; Limit int}` (json tags double as the `filters` object, all `omitempty`) + `hasAny()` + `ValidHistoryResult(s)` (allowlist = `runResultPassed`/`runResultFailed` only — an invalid plan is never recorded, so only passed/failed exist) + `ApplyHistoryFilters(runs, f)` (AND match, preserves newest-first input order, limit slices the first N, returns a NON-nil slice, never mutates input) + `HistoryListView{schemaVersion, filters *HistoryFilters (omitempty), shown, totalStored, runs}` + `BuildHistoryListView(h, f)` (filters pointer set ONLY when `hasAny()`; runs always non-nil `[]`). `commands` stays handlers-only (`runWorkflowHistoryList`): defines the four flags, parses via the shared `parseHistoryFlags` (non-numeric `--limit` → flag parse error → 2), then validates with `fs.Visit` to distinguish set-from-default so `--workflow ""`/`--code ""`/`--limit 0`/negative are rejected (→ 2 via `historyUsageError`) while an absent flag means "no filter"; an unknown `--result` value → 2. Empty match sets (incl. an unknown `--code`, which may name an older non-explainable code) → exit 0. Human output (`printHistoryListHuman`): an active `Filters:` block (only active fields), then matched runs, then a `Summary:` (`N shown`/`N total stored`); a no-match prints "No workflow history entries matched the filters."; the pristine first-run case (no filters, nothing stored) keeps the original "No workflow runs recorded." guidance with no summary. `--json` emits `HistoryListView` (the prior `list --json` shape `{schemaVersion,runs}` changed to add `filters?`/`shown`/`totalStored` — additive, CLI-only contract). Exit: 0 success (incl. empty match), 1 history read failure, 2 invalid filter value; NO 3 (never contacts runtime). Filtering never mutates the store, contacts the runtime, executes a workflow, reads workflow-referenced files, or collects logs/env/process data. Guarded by white-box `workflow/history_internal_test.go` (no-filter pass-through, per-filter + AND + limit-after-filter + unmatched-code, no-input-mutation, `ValidHistoryResult` allowlist, list-view filters-omitted/included + non-nil empty runs) + white-box `commands/workflow_history_internal_test.go` (result/workflow/code/limit, invalid result/empty workflow/empty code/invalid limits→2, AND composition, JSON shape incl. omitempty filter keys via raw-map inspection, human Filters+Summary + no-match, empty-history-with-filters→0, read-failure→1, store-not-mutated). New filter functions 100% covered. Source: `docs/roadmap.md` v1.12.

**Durable rule (v1.12 Slice 3) — workflow history filters:** `workflow history list` filters must operate **only on the compact local history metadata** — they must NEVER contact the runtime, execute a workflow, read the files a workflow step refers to, scan raw reports, or collect logs/env/process data. Filters **combine with AND**; `--limit` applies AFTER filtering and returns the **newest N** matches (the store is newest-first); an **empty match set is a success (exit 0)**, not an error (including an unknown `--code` — history may legitimately contain older codes that are no longer explainable, so do NOT require a filter code to be currently explainable). An **invalid filter value is a usage error (exit 2)**: an unknown `--result` (allowlist is `passed`/`failed` only via `workflow.ValidHistoryResult`), an explicitly-empty `--workflow`/`--code`, or a non-positive/non-numeric `--limit` — distinguish a set-but-empty flag from an absent one with `fs.Visit`. The filter model, matching, and list view (`HistoryFilters`/`ApplyHistoryFilters`/`HistoryListView`/`BuildHistoryListView`/`ValidHistoryResult`) live in the `workflow` package and must NOT import `client`/`commands`; `commands` stays handlers-only (parse + validate flags, call the helpers). Filtering must NOT mutate the store. The `--json` list view is `{schemaVersion, filters?, shown, totalStored, runs}` — `filters` present ONLY when a filter is active (and within it only active fields, `omitempty`), `runs` always an array; keep this shape stable. Do NOT add filtering over raw reports, history import/sync, telemetry, upload, or new result values beyond what the store can contain. CLI stays a pure API client; no API/DTO/contract change (the `list --json` additions are a CLI-only shape change).

v1.12 Slice 2 (workflow history export): added `portier workflow history export --out <file>` — a compact, deterministic, OFFLINE JSON snapshot of the local workflow run history for archiving/sharing. CLI-only, `validate:contract` 234/234 unchanged (no shared/server/service/client/API/DTO/config change), CLI coverage 97.6% (gate 97 PASS, not lowered/raised). Export model/projection live in the `workflow` package (`workflow/history.go`): `HistoryExport{schemaVersion,createdAt,source:"workflow-history",runCount,runs,safety}` + `HistorySafety{8 bool flags}` + `BuildHistoryExport(h History, createdAt time.Time)` (reuses the stored compact `HistoryRun` entries VERBATIM — newest-first order + codes left exactly as stored, NOT re-sorted/re-deduped; empty/nil runs → `[]` not null; createdAt injected for tests; does NOT mutate `h`). `HistorySafety` has eight ALWAYS-FALSE flags (`containsRawConfigs`/`containsRawPolicies`/`containsFullReports`/`containsLogs`/`containsEnvironment`/`containsProcessData`/`containsRuntimeUrls`/`containsTokens`) that explicitly STATE what the snapshot excludes (the projection is the guarantee; the flags let a reader confirm the safety boundary). The export carries the same compact metadata as the store — NEVER raw configs/policies, full reports, host/port payloads, file contents, logs, env vars, process data, secrets, runtime URLs, or tokens. `commands` stays handlers-only (`workflow_history.go` `runWorkflowHistoryExport`): parses `--out` (missing value → 2 via `parseHistoryFlags`; missing flag → 2), loads the store (read failure → 1, NO file written), builds the snapshot ONCE via `BuildHistoryExport(hist, time.Now())`, prints it to stdout under `--json` via `output.PrintJSON` then writes the file via `output.WritePrettyJSON` (write failure → 1) — so `--json --out` stdout/file are BYTE-IDENTICAL; human mode prints a confirmation (`File:`/`Runs:` + a fixed safety block) AFTER writing. Missing/empty history → a valid `runCount:0`/`runs:[]` snapshot, exit 0. `--out` parent dir must already exist (consistent with `policy check --out`/`doctor --out`/`workflow template --out` — `output.WritePrettyJSON` does no MkdirAll); existing file overwritten; the history store is NEVER mutated, the runtime is NEVER contacted, no workflow runs, no referenced files read. The `workflow` history code still does NOT import `client`/`commands`. NO filtering (no date/result/workflow/limit) this slice. Guarded by white-box `workflow/history_internal_test.go` (empty emits `runs:[]`, populated preserves newest-first + codes, safety all-false + all 8 keys present, deterministic createdAt, no-input-mutation) + white-box `commands/workflow_history_internal_test.go` (empty/populated export, runCount, newest-first, no-sensitive-content scan, `--json`/file BYTE PARITY, missing `--out`/missing-value→2, read-failure→1 no-file, write-failure→1, store-not-mutated, JSON-encode→1, path-resolution failure, flag edges) + the existing dispatch. `BuildHistoryExport`/`runWorkflowHistoryExport`/`printHistoryExportHuman` 100% covered. Source: `docs/roadmap.md` v1.12.

**Durable rule (v1.12 Slice 2) — workflow history export:** `workflow history export` must be **compact, deterministic, local, offline, and read-only** — it must NEVER mutate or truncate the history store, contact the runtime, execute a workflow, read the files a workflow step refers to, collect logs/env/process data, or upload anything. The snapshot must carry the **same compact metadata as the store and nothing more** — NEVER raw configs, raw policies, full embedded reports, host/port payloads, file contents, logs, environment variables, process data, secrets, runtime URLs, or auth tokens; it reuses the stored `HistoryRun` entries verbatim (newest-first, codes exactly as stored — export does NOT re-order/re-dedupe). It must include an explicit **`safety` object whose flags are all false** (the self-documenting exclusion statement; guard with a test asserting every flag false and all keys present). An empty/missing history must export a **valid `runCount: 0` snapshot** (exit 0), never an error. The export model + projection (`BuildHistoryExport`, with an injected clock) live in the `workflow` package; `commands` stays handlers-only (parse flags, load the store, call `BuildHistoryExport`, write via `output`); the `workflow` history code must NOT import `client`/`commands`. Build the payload ONCE so `--json` stdout and the `--out` file are **byte-identical** (test it). Exit codes: `0` written (incl. empty); `1` a history read failure, an output write failure, or a JSON-encode failure; `2` a missing `--out` value. The `--out` parent directory must already exist (match the other single-file `--out` exports — no MkdirAll); an existing file is overwritten. Do NOT add filtering (date/result/workflow/limit), history import, sync, telemetry, upload, or AI-handoff for history in this slice (AI handoff is the `workflow report` bundle). CLI stays a pure API client; no API/DTO/contract change.

v1.12 Slice 1 (local workflow run history): started v1.12 Local History & Observability with an OPT-IN, bounded, local record of `workflow run` outcomes — CLI-only, `validate:contract` 234/234 unchanged (no shared/server/service/client/API/DTO/config change), CLI coverage 97.5% (gate 97 PASS, not lowered/raised — ~0.5% buffer too thin for 98). `portier workflow run --file <workflow.json> --record-history` records ONE compact entry AFTER a completed run; without the flag nothing is recorded (no background/always-on collection, no telemetry, no upload). Model/projection/run-id/store live in the `workflow` package (`workflow/history.go`): `History{schemaVersion,runs}` + `HistoryRun{id,createdAt,workflow,result,summary,steps,codes?}` + `HistoryStep{id,type,status,exitCode}`; `ProjectRun(run,id,createdAt)` (compact projection — copies only identity/time/name/result/summary counts/compact step metadata + `sortedDedupe(ExplanationCodesForRun(run))`; DROPS each step's embedded report + message), `NewRunID(createdAt,workflow,suffix)` (`<YYYYMMDDThhmmssZ>-<sanitized-workflow>-<suffix>`, e.g. `20260614T101530Z-policy-baseline-check-a1b2c3d4`; suffix injected for tests), and `HistoryStore` (`Load` — missing/empty file → empty history, malformed → error; `Append` — newest-first prepend + bounded retention to the latest 100 + parent-dir creation; `Clear` — remove file, missing = success). Stored = compact metadata ONLY; NEVER raw configs/policies, full embedded policy reports, file contents, logs, env vars, process data, secrets, runtime URLs, or auth tokens. New CLI commands `workflow history list` (newest-first human or `--json {schemaVersion,runs}`; clear empty message), `workflow history show <run-id>` (human/`--json`; missing id → 2, unknown id → 1), `workflow history clear --yes` (requires `--yes`; already-empty/missing = success). `RunWorkflowRun` gained `--record-history`: after `EmitRun`, project + `store.Append`; a history WRITE FAILURE is a stderr WARNING that NEVER hides the run result — passed run → exit becomes 1, an already-failed run keeps its code (1/3, runtime-unreachable stays 3); an INVALID PLAN is never recorded (the invalid-plan path returns before the record block — completed runs only). Storage path `<user-config-dir>/portier/workflow-history.json` resolved via `os.UserConfigDir()`; `commands` stays handlers-only (`workflow_history.go`) with the path/suffix as unexported package-var seams (`historyStorePath`/`newHistorySuffix`) so tests inject a temp path WITHOUT an env override (keeping the workflow-handler "no `os.Getenv`" property). The `workflow` history code never imports `client`/`commands` and never contacts the runtime. Guarded by white-box `workflow/history_internal_test.go` (compact projection, no-sensitive-data-in-JSON, code dedupe/sort, run-id shape, load missing/empty/malformed, append ordering + parent-dir creation, retention keeps latest 100, schemaVersion default, clear incl. error) + white-box `commands/workflow_history_internal_test.go` (record compact + no banned content, no-record-without-flag, failed-run records `failed`, invalid-plan no-record, write-failure exit incl. runtime-unreachable→3 kept, list/show/clear human+JSON, unknown/missing id, read failures, flag-parse edges, path-resolution failures, defaults) + `main_test.go` offline dispatch. Only documented structurally-unreachable branches uncovered (crypto/rand suffix fallback; the post-`Load` filesystem-write-failure branch in the store). Source: `docs/roadmap.md` v1.12.

**Durable rule (v1.12 Slice 1) — local workflow run history:** Workflow run history is **OPT-IN and local-only** — `workflow run` must record NOTHING unless `--record-history` is passed; there is no always-on/background collection, no scheduler, no telemetry, and no upload/sync. History must store **compact metadata only** — run id, time, workflow name, result, summary counts, compact per-step metadata (`id`/`type`/`status`/`exitCode`), and the deduped+sorted emitted explanation/finding codes; it must **NEVER** store raw configs, raw policies, full embedded policy reports, file contents, logs, environment variables, process data, secrets, runtime URLs, or auth tokens (drop each step's embedded report and message). History must be **bounded** (retention keeps the latest 100 runs, newest first) and must **not alter workflow execution semantics** except for the explicit history-write-failure handling: a write failure is a WARNING that never hides the run result (a passed run becomes exit 1; an already-failed run keeps its 1/3, runtime-unreachable stays 3), and an **invalid plan is never recorded** (only completed runs are). The history model, compact projection (`ProjectRun`), run-id generation (`NewRunID`), and the bounded read/append/retention/clear store (`HistoryStore`) live in the `workflow` package and must **not import `client`/`commands` or contact the runtime**; `commands` stays handlers-only (flag parsing, path resolution via an injectable seam — NOT `os.Getenv` in a workflow handler — and rendering). `workflow history list`/`show`/`clear` are fully offline; exit codes: `0` success (incl. empty list / already-empty clear), `1` an unknown run id (`show`) or a history read/write failure, `2` a missing run id (`show`) or `clear` without `--yes`. Adding a stored field must keep the compact/no-sensitive-data guarantee (guard with a no-banned-content test) and stay parity-free (CLI-only — no API/DTO/contract change). Do NOT add background recording, a daemon, a UI history panel, remote storage, or AI handoff for history (history is local observability; AI handoff is the `workflow report` bundle).

v1.11 Slice 8 (workflow toolkit consistency audit): focused audit of the v1.11 workflow toolkit (Slices 1–7) — audit/cleanup only, no new features. **Result: PASS, v1.11 workflow toolkit ready for finalization.** Verified the command surface + all flag combinations (built-binary battery across `plan`/`run`/`runbook`/`report`/`template`/`explain`); JSON schema consistency (plan/runbook/run/template/report-bundle shapes; `--json --out` byte parity for plan/runbook/run incl. `--explain`; `--explain` additive/omitempty; passed/valid steps un-explained; `report.json` normalized — embedded per-step reports dropped so no config/host/port payload leaks; `explanations.json` `{}` when none; `explain --list` = 44 codes; `explain <workflow.step.*>`/`<workflow.run.*>` resolve); exit codes (plan 0/1/2, template 0/1/2, runbook 0/1/2, run 0/1/2/**3**, report 0/1/2 + non-empty-dir→1, no report exit-3; all `--out` write failures → 1); safety (no `os/exec`/shell/syscall/`net/http`/telemetry/`os.Getenv` in the `workflow` package or workflow handlers — the package's only direct OS write is the report bundle to the explicit `--out` dir; referenced-file reads are INJECTED via `RunDeps.ReadFile`, so `plan`/`runbook`/`report` never open referenced files; only `run` contacts the runtime, read-only; no apply/import/enforcement/scheduler/probing/`allowUdp`/mutation); and architecture (`commands` handlers-only; `workflow` imports only `config`/`policy`/`explain`/`output`, NOT `client`/`commands`; one canonical `explain` registry; no second schema; no import cycles; offline subcommands never resolve the runtime URL; `go vet` clean). Three small fixes: (1) the top-level `workflow` help intro verb list omitted "report" → now "Plan, validate, run, preview, report on, or template …"; (2) a stale `tools/cli/readme.md` runbook note ("Workflow execution is deferred to a later v1.11 slice") → now points to `workflow run`; (3) the exported `RunExplanations()` accessor had 0% coverage (no caller/test) → added a white-box test (now 100%; function coverage 99.2% → 99.6%). `validate:contract` 234/234; CLI coverage 97.5% (gate 97 PASS, none lowered, not raised — ~0.5% buffer too thin for 98); lint/typecheck/gofmt/vet clean. No release blockers; the only sub-100% workflow functions are the documented structurally-unreachable `json.Marshal`-on-concrete-type encode branches and the post-validation filesystem-write-failure branches in `WriteSupportReport`. No new durable rule — the audit confirms the Slice 1–7 rules. Source: `docs/roadmap.md` v1.11.

v1.11 Slice 7 (workflow support report): added `portier workflow report --from <report.json> --out <directory>` — packages an EXISTING workflow plan/run JSON report into a small, local, offline diagnostic bundle for manual review or AI handoff. CLI-only, `validate:contract` 234/234 unchanged, CLI coverage 97.5% (gate 97 PASS, not lowered/raised). Strictly a PACKAGING step over an already-produced report — it NEVER executes a workflow, runs a shell command, contacts the runtime, reads the config/policy/baseline/report files a step refers to, collects logs/env/process data, mutates the input, or uploads. Model/parser/extractor/summary/writer live in the `workflow` package (`workflow/report.go`): `SupportReport{Kind,Workflow,Result,Summary,Steps, inputCodes(unexported)}` + `SupportReportStep{ID,Type,Status,Code?,ExitCode?,Message,Codes}` + `SupportReportManifest{schemaVersion,type,createdAt,source,workflow,result,files}`; `ParseSupportReport(data)` (lenient decode + `detectKind`), `ExplanationCodes`/`Explanations` (via `reportExplainRegistry()` = `explain.Merge(explanations, runExplanations, policy.Explanations())`), `WriteSupportSummary`, `WriteSupportReport(dir, r, now)` (injected clock; writes summary.txt/report.json/explanations.json then manifest.json last). Detection: PLAN = `schemaVersion` present + result valid/invalid; RUN = no `schemaVersion` + result passed/failed; both require ≥1 step (a bare policy report → unsupported error). Per-step explainable codes: invalid plan step → its `workflow.step.*` `code`; skipped run step → `workflow.run.dependency_failed`; failed run step → error-severity `findings[].code` + `new[].code` from the step's EMBEDDED report (`embeddedFindingCodes`, parsed from the existing JSON, never re-evaluated); plus the input report's own `explanations` map keys (so a `--explain` run report carries `workflow.run.runtime_unreachable`/`input_failed`). Codes deduped; unknown codes dropped by `explain.ForReport`. report.json is a NORMALIZED view — it deliberately DROPS each run step's embedded policy/compare report so no rule host/port detail or config payload is carried; explanations.json is `{}` when no codes (stable shape); summary.txt ends in a fixed safety block. `commands` stays handlers-only — `RunWorkflowReport` parses `--from`/`--out`, reads the input (read error→2, parse/unsupported→2), reuses `prepareBundleDir` (create-if-missing, refuse non-empty → exit 1, matching `support-bundle`), then `WriteSupportReport`; `--json` emits the manifest. The `workflow` package does NOT import `client`/`commands`. Exit: 0 written; 1 dir create/write failure or `--json` encode failure; 2 usage (missing `--from`/`--out` value) or unreadable/malformed/unsupported report; NO 3 (never contacts runtime). Added docs-only AI prompt `prompts/workflow.md` (paste summary.txt/report.json/explanations.json; warns against secrets; forbids inventing blind config changes; no AI integration/upload/telemetry). Guarded by white-box `workflow/report_internal_test.go` (run/plan detection, empty/malformed/no-steps/bare-policy rejection, per-step code extraction incl. failed-compare `new` + malformed embedded report, dedupe + canonical-only, status tags incl. default, summary shape/placeholders/empty `{}`, bundle write w/ fixed clock) + black-box `commands/workflow_report_test.go` (failed/passed run, invalid plan, `--explain` run code carried, JSON manifest, **does-not-read-referenced-files + input-not-mutated**, missing-dir-created, empty-dir-OK, non-empty-dir refused→1 leaving unrelated files, malformed/unsupported→2, missing-args→2, unreadable→2, JSON-encode→1, dispatch) + `main_test.go` offline dispatch. Only the unreachable filesystem-write-failure branches in `WriteSupportReport` remain uncovered (documented, matching `support-bundle`). Source: `docs/roadmap.md` v1.11.

**Durable rule (v1.11 Slice 7) — workflow support report:** `workflow report` is a **packaging step over an EXISTING report only** — it must NEVER execute a workflow, run a shell command, contact the runtime, read the config/policy/baseline/report files a workflow step refers to, collect logs/env/process data, mutate the input report, or upload anything. It parses ONLY the provided report (`workflow plan`/`run` `--json` output) and re-derives explanation metadata from the **canonical `explain` registry** (merged workflow step + run + `policy` via `reportExplainRegistry`) — never a second explanation schema, never a fresh evaluation. The bundle must carry **no secrets, raw configs, env, process, or log data**: `report.json` is a NORMALIZED view (step id/type/status/message + per-step explainable codes) that DROPS each run step's embedded policy/compare report; `manifest.json`/`summary.txt`/`explanations.json` carry only metadata, the step list, and code explanations (`{}` when none, for a stable shape); `summary.txt` keeps its fixed safety block. Explanation codes are derived structurally and deduped: invalid plan step → its `workflow.step.*` code; skipped run step → `workflow.run.dependency_failed`; failed run step → the `policy.*` finding codes embedded in its report (`findings`/`new`); plus any codes in a `--explain` report's `explanations` map; unknown codes are dropped. Output goes ONLY inside the requested `--out` directory, following the `support-bundle` convention (create if missing, **refuse a non-empty directory**, never delete unrelated files). Exit codes: 0 written; 1 output create/write (or `--json` encode) failure; 2 usage error or an unreadable/malformed/unsupported input report — there is **NO connection-failure (3)** code (it never contacts the runtime). Report model/parser/detector/extraction/summary/writer live in the `workflow` package (`workflow/report.go`); `commands` stays handlers-only (parse flags, read input, reuse `prepareBundleDir`, call `ParseSupportReport`/`WriteSupportReport`); the `workflow` package must NOT import `client`/`commands`. Supporting plan AND run reports is intentional; detection is by `schemaVersion` + result label + presence of `steps`. The AI handoff prompt stays **docs-only** (`prompts/workflow.md`) — no `--prompt` helper, no AI integration/upload/telemetry; it must warn against pasting secrets and forbid asking the assistant to invent blind config changes. This mirrors the `support-bundle`/policy-export conventions; keep them aligned. CLI stays a pure API client; no API/DTO/contract change.

v1.11 Slice 6 (workflow run explanations): added `--explain` to `portier workflow run` — ADDITIVE explanations for failed/skipped steps, reusing the doctor/policy/workflow-plan `explain` pattern. CLI-only, `validate:contract` 234/234 unchanged, CLI coverage 97.5% (gate 97 PASS, NOT lowered/raised). Three stable workflow-RUN codes (separate from the 15 `workflow.step.*` plan codes), defined with their explanation registry + selection helpers in a new file `workflow/run_explain.go`: `workflow.run.dependency_failed` (a `reportFrom` dependency produced no report → dependent SKIPPED), `workflow.run.runtime_unreachable` (a `policy.check` runtime step couldn't reach the runtime), `workflow.run.input_failed` (an unreadable/malformed referenced config/policy/baseline/report file, OR the runtime config, OR a generic non-connection runtime read error). `RunExplanations()` returns the run registry; `Explanations()` now returns `explain.Merge(explanations, runExplanations)` (step+run) so the `explain` command (which already merges `doctor`+`policy`+`workflow`) lists/looks up both — `explain <workflow.run.*>` works and `explain --list` covers **44** codes (8+12+6+15+3); the `explain --list` count test moved 41→44. Each run step records its explain codes internally: `stepOutcome.explainCodes` + an UNEXPORTED `WorkflowRunStep.explainCodes` (NOT in the JSON contract) — a failed policy.check/review stores its error-finding codes (`errorFindingCodes`), a failed baseline.compare stores `cmp.New` codes (`newFindingCodes`), and the run-code helpers (`failInputOutcome`/`skipDependencyOutcome` + the runtime-unreachable branch) store the `workflow.run.*` code. `ExplanationCodesForRun(run)` flattens failed/skipped steps' codes (passed steps contribute nothing). Rendering reuses the canonical `explain` helpers via `runExplainRegistry()` = `explain.Merge(runExplanations, policy.Explanations())` (the only code families a run can carry — no doctor codes): human `PrintRunHuman(run, withExplain, w)` prints an indented `explain.PrintInline` block (deduped per step) under each failed/skipped step (passed steps never explained); JSON via a new `runJSON{WorkflowRun; Explanations omitempty}` wrapper set ONLY with `--explain` (and only `explain.ForReport(runExplainRegistry(), ExplanationCodesForRun(run))`), so without `--explain` — and when a run emits no explainable codes (e.g. all-passed) — output is byte-identical to before. `EmitRun(run, jsonOutput, explainOut, outPath, …)` builds the payload once → `--json --out --explain` byte-identical. The invalid-plan path now passes `Explain` to the plan emitter (reuses workflow-plan explanations). `--explain` NEVER changes which steps run, status, summary, result, or exit code (incl. runtime-unreachable still exit 3). Guarded by white-box `workflow/run_explain_internal_test.go` (locked 3-code run set + registry parity: every code explainable, no orphan/dup, Code==key, error severity, no dangling/self related; `Explanations()` merges step+run; `runExplainRegistry` resolves run+policy) + black-box `commands/workflow_run_explain_test.go` (policy-check/review/baseline finding codes in human+JSON, the 3 run codes incl. runtime-unreachable→3 still emits its code, passed-run emits NO map + no human blocks, `--explain` doesn't change the run via decode+`reflect.DeepEqual`, `--json --out --explain` byte parity, `explain <workflow.run.*>`/`--list` integration). Only the unreachable `json.Marshal`-on-concrete-type encode branches remain uncovered. The support-bundle `explanations.json` stays doctor-scoped (unchanged). Source: `docs/roadmap.md` v1.11.

**Durable rule (v1.11 Slice 6) — workflow run explanations:** `workflow run --explain` is **presentation only** — it must NEVER change which steps run, their status, the run summary, the run result, or any **exit code** (including the runtime-unreachable exit 3); guard this with a test that decodes a run with and without `--explain` and asserts the steps/summary/result are identical. **Passed steps are never explained** (no noise). Explanations are **additive**: human adds an inline block under each FAILED/SKIPPED step; JSON adds a top-level `explanations` map populated ONLY with `--explain` and ONLY for emitted codes (deduped), `omitempty` so without `--explain` — and when no explainable codes were emitted — the JSON is byte-identical to before, and `--json --out --explain` stdout/file stay byte-identical. **Reuse the canonical `explain` registry/renderer** (one `Explanation` type, `explain.PrintInline`/`explain.ForReport`) — do NOT add a second explanation schema or duplicate the policy explanations. A failed policy step is explained by its **policy finding codes** (from its report), NOT by a fabricated workflow-run code; the `workflow.run.*` codes exist ONLY for run-specific outcomes (dependency-skip, runtime-unreachable, input read/parse failure). The `workflow.run.*` codes are **stable operator-facing identifiers** (a CLI/tool contract) — exactly 3 today, locked by `run_explain_internal_test.go`; **every emitted run code MUST have an explanation** (adding a code without one fails that guard in the same slice), and there must be no unused entries. Run codes/registry/selection live in the `workflow` package (`workflow/run_explain.go`: `RunExplanations`, `ExplanationCodesForRun`, `runExplainRegistry`, the finding-code collectors); `Explanations()` merges the step+run registries so the `explain` command lists/looks up both. The render registry merges ONLY run + policy codes (a run cannot carry doctor codes). Per-step explain codes are stored UNEXPORTED on `WorkflowRunStep` (not part of the JSON contract — the additive top-level `explanations` map carries the data). `commands` stays handlers-only (`RunWorkflowRun` only wires the `--explain` flag into `EmitRun`/the plan emitter). The support-bundle `explanations.json` stays doctor-scoped — do NOT merge workflow/policy codes into it. This mirrors the v1.9/v1.10/v1.11-Slice-2 explain rules; the conventions must stay aligned. CLI stays a pure API client; no API/DTO/contract change.

v1.11 Slice 5 (workflow run — read-only execution): added `portier workflow run --file <workflow.json>` — the first workflow EXECUTION command, strictly READ-ONLY. CLI-only, `validate:contract` 234/234 unchanged (no new endpoint — runtime mode reuses `GET /api/config/export`). Model/orchestration/render live in the `workflow` package (`workflow/run.go`): `WorkflowRun{Workflow,Steps,Summary,Result, runtimeUnreachable(unexported)}` + `WorkflowRunStep{ID,Type,Status(passed|failed|skipped),ExitCode,Message,Report any}` + `WorkflowRunSummary{Total,Passed,Failed,Skipped}`; `RunDeps{ReadFile func, RuntimeRules func}` (INJECTED side effects); `Run(File, RunDeps)` (call only after a valid `BuildPlan`), `RunExitCode`, `PrintRunHuman`, `EmitRun(run, jsonOutput, outPath, …)`, and the sentinel `ErrRuntimeUnreachable`. Execution reuses the existing helpers directly: `policy.Evaluate` (check), `policy.BuildReview` (review), `policy.Compare`+`policy.ParseBaseline`/`ParseReportSnapshot` (baseline compare); config via `config.ParseLocal`. **`reportFrom` consumes the producing step's IN-MEMORY `policy.Report`**, round-tripped through JSON (`snapshotFromReport`) so finding fingerprints match file-derived reports/baselines; a producing step that yielded no report (errored / skipped / non-report type like baseline.compare) → dependent **skipped** (which fails the run). Step status: policy/review pass→passed, violation→failed; baseline no-new→passed, new→failed. Referenced files are read HERE during the run (unlike plan/runbook); an unreadable/malformed referenced file → step failed (exit 1), NOT a usage error. The `workflow` package does NOT import `client`/`commands` — the runtime fetch is injected: `commands.makeRuntimeRules(conn)` (handlers-only) resolves the URL + `client.ExportConfig()` (read-only) + `exportedRules`, memoizes (multiple runtime steps fetch once), and wraps a `*client.ConnectionError` with `workflow.ErrRuntimeUnreachable` so `RunExitCode` maps it to exit 3 without the workflow package importing the client. `RunWorkflow` now takes `ConnFlags` (threaded from `main.go`); only `run` uses it (plan/template/runbook stay offline). `RunWorkflowRun` parses `--file`/`--out`, reads+parses (errors→2), `BuildPlan`; if invalid → `workflow.Emit(plan,…)` (prints plan, exit 1, NO step run, NO `--out` write); if valid → `Run` + `EmitRun`. Human `[PASSED]`/`[FAILED]`/`[SKIPPED]` + `Result:`; `--json` full report; `--out` (`--json --out` byte-identical). `--explain` DEFERRED for run. Exit: 0 all passed (none skipped); 1 any failed/skipped, invalid plan, or `--out` write failure; 2 usage or unreadable/malformed workflow; 3 runtime unreachable. NEVER runs a shell command / the runbook display text, applies/imports, enforces, schedules, or mutates runtime/config/policy/baseline/report files. Guarded by white-box `workflow/run_internal_test.go` (per-type pass/fail via injected `fakeDeps`, runtime pass/unreachable→3/generic→1, reportFrom new-findings + **round-trip matches a `BuildBaseline`-derived baseline**, dependency-skip, all input-error branches, unknown-type defensive fail, human shape) + black-box `commands/workflow_run_test.go` (all types, reportFrom, skip, invalid→1-no-run-no-out, malformed→2, missing-referenced-file→step-fail, **runtime via `makeExportServer`** + unreachable→3 + API-500→1 + bad-`--url`→1 + memoized-single-fetch, JSON/human, `--out`/`--json --out` byte parity + write-failure, no-input-mutation, dispatch) + `main_test.go` offline dispatch (config-only run, bad `--url` ignored). CLI coverage 97.6% → 97.5% (gate 97 PASS, NOT lowered — a first pass landed at exactly 97.0%; added input-error/runtime-branch tests to restore buffer to 97.5%; only the unreachable `json.Marshal`-on-concrete-type encode branches in `EmitRun`/`snapshotFromReport` remain uncovered). Source: `docs/roadmap.md` v1.11.

**Durable rule (v1.11 Slice 5) — workflow run (read-only execution):** `workflow run` may execute ONLY the explicitly supported read-only step types (`policy.check`/`policy.review`/`policy.baseline.compare`) by **reusing the existing policy/config/baseline semantics** (`policy.Evaluate`/`BuildReview`/`Compare`, `config.ParseLocal`) — it must NEVER run a shell command, execute the runbook `display` text, apply/import configs, enforce a policy, schedule anything, probe targets, or mutate the runtime/config/policy/baseline/report files. It runs only from a VALID plan (an invalid workflow prints the plan and exits 1 with no step run and no `--out` file, mirroring `workflow plan`/`runbook`). A `reportFrom` step consumes the producing step's **in-memory** report (no intermediate file written/read); normalise that report (round-trip through JSON via `snapshotFromReport`) so its fingerprints match file-derived baselines, and **skip** the dependent step (failing the run) when the dependency produced no report. The `workflow` package must stay free of `client`/`commands`/runtime coupling — the runtime-config fetch is **injected** (`RunDeps.RuntimeRules`); the command layer owns the read-only `client.ExportConfig()` call, memoization, and wrapping a connection error with `workflow.ErrRuntimeUnreachable` so a runtime-unreachable run exits **3** (matching `policy check --runtime`). Exit codes: 0 all steps passed (none skipped); 1 any failed/skipped step, an invalid plan, or an `--out` write failure; 2 usage error or an unreadable/malformed workflow file; 3 runtime unreachable — a referenced file unreadable/malformed DURING the run is a **step failure (1)**, never a usage error (2). The run report carries only what the existing policy reports already include (no raw configs, no secrets). `commands` stays handlers-only; `--json`/`--out` behave like the other workflow emitters (`--json --out` byte-identical). Adding a runnable step type = a new executor in `run.go` reusing an existing safe helper + coverage; do NOT add shell/arbitrary-command steps, mutation, enforcement, or a scheduler under this banner. CLI stays a pure API client; no API/DTO/contract change.

v1.11 Slice 4 (workflow runbook preview): added `portier workflow runbook --file <workflow.json>` — turns a VALID workflow plan into the ordered list of Portier CLI commands a user would run manually (a PREVIEW, still NOT execution). CLI-only, fully offline, `validate:contract` 234/234 unchanged. Model/builder/renderer live in the `workflow` package (`workflow/runbook.go`): `Runbook{Workflow,Steps,Summary,Result}` + `RunbookStep{ID,Type,Command []string,Display,Notes []string}` + `RunbookSummary{Total}`; `BuildRunbook(File)` (called only after a valid `BuildPlan`), `PrintRunbookHuman`, `EmitRunbook(rb, jsonOutput, outPath, stdout, stderr)`. Step→command mapping: policy.check(config)→`portier policy check --config <f> --policy <f>`; policy.check(runtime)→`... --runtime --policy <f>`; policy.review→`portier policy review --current <f> --candidate <f> --policy <f>`; policy.baseline.compare(report)→`portier policy baseline compare --baseline <f> --report <f>`; (reportFrom)→`... --report <report-from:STEP-ID>` (explicit placeholder via `reportFromPlaceholder`, NOT a real path) + a note. Each step carries a canonical argv `Command` + a best-effort `Display` (`displayCommand`/`shellQuoteToken`: single-quote tokens with whitespace/quote/backslash, others as-is; Portier never shell-executes Display). `Result` is always `"ready"` (built only from a valid plan). `RunWorkflowRunbook` (`commands/workflow.go`, handlers-only) parses `--file`/`--out`, reads the file (read error→2, parse/schema error→2), runs `BuildPlan`; if invalid → `workflow.Emit(plan,...)` (prints the plan, exit 1, NO runbook, NO `--out` write); if valid → `BuildRunbook` + `EmitRunbook`. `--json` emits `{workflow,steps:[{id,type,command,display,notes}],summary:{total},result}`; `--out` writes that JSON (`--json --out` byte-identical; the `<report-from:...>` placeholder is HTML-escaped to `<`/`>` by Go's encoder identically in both PrintJSON and WritePrettyJSON, so parity holds). Exit: 0 runbook produced, 1 plan invalid OR `--out` write failure, 2 usage (missing `--file`/`--out` value) or unreadable/malformed workflow — NO `3` (never contacts runtime). NEVER executes a command, reads the files a step refers to, or inspects their contents. `runbook` added to `RunWorkflow` dispatch; root + workflow help updated. Guarded by white-box `workflow/runbook_internal_test.go` (per-type mapping, placeholder+note, ordering/summary, `shellQuoteToken`, human shape incl. unnamed/singular, unknown-type fallback returns nil/nil, all templates→runbooks) + black-box `commands/workflow_runbook_test.go` (all types, ordering, human/JSON, **invalid→plan+exit1+no `--out` file**, malformed/schema/missing-file/unreadable/missing-out→2, `--out`+`--json --out` byte parity+write-failure→1, **does-not-read-referenced-files**, no-mutation, templates→runbooks, dispatch) + `main_test.go` offline dispatch. CLI coverage 97.6% → 97.6% (gate 97 PASS, not lowered; builder/renderer/`RunWorkflowRunbook` 100%, only the unreachable `json.Marshal`-on-concrete-`Runbook` encode branch in `EmitRunbook` uncovered). Source: `docs/roadmap.md` v1.11.

**Durable rule (v1.11 Slice 4) — workflow runbook preview:** A workflow runbook (`workflow runbook --file`) is a **deterministic command PREVIEW, NOT execution** — it must NEVER execute a command, contact the runtime, read the files a step refers to (or inspect their contents), or mutate any file except the requested `--out` file. It is built **only from a VALID plan** (`BuildRunbook` is called only after `BuildPlan` reports valid; `Result` is always `"ready"`); an invalid workflow must print the plan and exit 1 with **no runbook and no `--out` file** (mirror `workflow plan`). The runbook must **not invent file paths** — a `reportFrom` step uses an explicit `<report-from:step-id>` placeholder (never a fabricated path) plus a note. Each step exposes a canonical argv `Command` (the source of truth) and a best-effort copy/paste `Display`; Portier must never shell-execute the `Display` string (quote only tokens needing it). Runbook model/builder/renderer live in the `workflow` package (`workflow/runbook.go`), reuse `Parse`+`BuildPlan`, and file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only (`RunWorkflowRunbook` validates then calls the helpers — no command-generation logic in `commands`). `--json`/`--out` behave like the other workflow emitters (additive nothing; `--json --out` byte-identical). Exit codes: 0 runbook, 1 invalid plan or `--out` write failure, 2 usage or unreadable/malformed workflow — there is **NO connection-failure (3)** code (never contacts the runtime). Adding a step type to the runbook = a new case in `runbookCommand` mapping to its real `policy`/CLI command, plus coverage; do NOT add execution, a scheduler, or background automation under this banner — workflow execution is a deliberate later v1.11 slice. CLI stays a pure API client; no API/DTO/contract change.

v1.11 Slice 3 (workflow templates): added built-in starter workflow files so operators can inspect/generate valid workflow JSON without hand-writing the schema — `portier workflow template <name>` / `--list`. CLI-only, fully offline, `validate:contract` 234/234 unchanged. Mirrors the v1.10 `policy template` convention. Registry/metadata/rendering live in the `workflow` package (`workflow/template.go`): `Template{Name,Title,Description, file templateWorkflowFile}` with `WorkflowValue()` (bare workflow file = exact `Parse` schema) / `DetailValue()` (`{name,title,description,workflow}` wrapper); `Templates()` (sorted copy), `FindTemplate(name)`, `TemplateNames()`, `PrintTemplateList(jsonOutput, w)`. Four templates, sorted: `policy-baseline-check` (check-config + compare-baseline via `reportFrom`), `policy-check-local` (config+policy), `policy-check-runtime` (`runtime:true`+policy), `policy-review` (current+candidate+policy). All `schemaVersion:1`, composing ONLY the three existing validated step types — **no execution/scheduler/mutation/apply/import fields**. The serializable `templateStep` uses `omitempty` so each template emits only the fields a step uses (field names match `stepRaw`, so a rendered template round-trips through `Parse`, which rejects unknown fields). Output: `template <name>` → bare workflow JSON; `--json` → wrapper; `--list` → human list / `--list --json` → `{templates:[{name,title,description}]}` (no embedded workflow body); `--out <file>` → writes the **bare** workflow JSON regardless of `--json` (human prints `Workflow written to <file>`, `--json` keeps stdout the pure wrapper) so the file is directly usable by `workflow plan --file`. Flags work before AND after the name via an interspersed flag-parse loop (`template policy-review --out f`). Exit: 0 success, 1 `--out` write failure, 2 usage (unknown template — lists available — / missing name / too many / missing `--out` value / `--list` with a name or `--out`). `commands/workflow.go` `RunWorkflowTemplate` parses flags + resolves the template + calls `output` for file writing; handlers-only. Guarded by white-box `workflow/template_internal_test.go` (each renders → `Parse` → `BuildPlan` valid; sorted/unique; field allowlist forbids exec/scheduler/mutation fields + non-validated step types; `FindTemplate`; `DetailValue` shape) + black-box `commands/workflow_template_test.go` (incl. `--out` file **end-to-end usable by `workflow plan`**) + `main_test.go` offline dispatch. CLI coverage 97.7% → 97.6% (gate 97 PASS, not lowered; `template.go` 100%, dip = the unreachable `json.Marshal`-on-concrete-DTO encode branches in `RunWorkflowTemplate`, as in `RunPolicyTemplate`). Source: `docs/roadmap.md` v1.11.

**Durable rule (v1.11 Slice 3) — workflow templates:** Built-in workflow templates must render to the **exact same workflow schema `workflow plan` accepts** (`schemaVersion: 1` + `name` + `steps`) — a rendered/`--out` template must be directly usable by `workflow plan --file` with no editing, and a white-box guard MUST render each template, `Parse` it, and assert `BuildPlan` reports `valid`. Templates must be **deterministic and sorted by name** (`Templates()` returns a sorted copy) and live in the `workflow` package (`workflow/template.go`: registry, metadata, `WorkflowValue`/`DetailValue`/`PrintTemplateList`); **template definitions must NOT live in `commands`** (it stays handlers-only — parse flags, resolve the template, call `output` for file writing). Templates must compose ONLY the existing validated step types (`policy.check`/`policy.review`/`policy.baseline.compare`) and must **never introduce execution, scheduler, or mutation/apply/import fields** (guarded by a field-allowlist test) — they are dry-run starter files over the existing schema. The `--out` file is the **bare** workflow JSON (no metadata wrapper) regardless of `--json`, written via `output.WritePrettyJSON` (no parent-dir creation, no partial write); a write failure → exit 1, a missing `--out` value / unknown or missing template / `--list`+name or `--list`+`--out` → exit 2. Fully offline — no runtime contact, no step execution, no reading of referenced workflow input files, modifies nothing but `--out`. Adding a template = a registry entry + its render-to-valid-plan coverage; do NOT grow this into a template-file loader, user-supplied template directory, or parameterized generator. This mirrors the v1.10 `policy template` rule; the two template conventions must stay aligned. CLI stays a pure API client; no API/DTO/contract change.

v1.11 Slice 2 (workflow plan explanations): added stable workflow step-validation codes + inline/JSON explanations, reusing the doctor/policy `explain`/`--explain` pattern. CLI-only, fully offline, `validate:contract` 234/234 unchanged. Each `StepPlan` now carries a stable `Code string json:"code"` (every step, valid or invalid; valid steps use `codeStepValid`). There are exactly **15** codes — one per Slice 1 validation outcome (`codeStepValid`/`codeStepDuplicateID`/`codeStepUnknownType`/`codeStepMissingID`/`codeStepMissingType`/`codeStepMissingConfigSource`/`codeStepConflictingConfigSources`/`codeStepMissingPolicy` (shared by policy.check + policy.review)/`codeStepMissingCurrent`/`codeStepMissingCandidate`/`codeStepMissingBaseline`/`codeStepMissingReportSource`/`codeStepConflictingReportSources`/`codeStepUnknownReportFrom`/`codeStepFutureReportFrom`) — locked by `workflow/explain_internal_test.go`. The workflow explanation registry lives in `workflow/explain.go` (`Explanations()`, keyed by the code constants, severity info for valid / error for the rest); the `explain` command merges it via `explain.Merge(doctor.Explanations(), policy.Explanations(), workflow.Explanations())` so `explain <workflow-code>` + `explain --list` cover **41** codes (was 26). `workflow plan --explain`: human inline `Code:`/`Meaning:`/`What to do:`/`Related:` block for INVALID steps only (via `explain.PrintInline`); JSON additive `explanations` map (`planJSON` embeds `Plan` + `omitempty` `Explanations`, set only with `--explain`, INVALID-step codes only via `invalidCodesOf` + `explain.ForReport`). A valid step is never explained (in human or JSON). `--explain` never changes steps/summary/result/exit code; without it JSON is byte-identical to before (and to the new `code` field aside, which is always present); `--json --out --explain` byte-identical. `PrintHuman` gained a `withExplain bool` param; `EmitOptions` gained `Explain`; `RunWorkflowPlan` parses `--explain`. Support-bundle `explanations.json` stays doctor-scoped (unchanged). Covered by white-box `workflow/explain_internal_test.go` (locked 15-set + registry parity: every code explainable, no orphan/dup, Code==key, fields/severity, no dangling/self related, severity-matches-outcome) + `workflow_internal_test.go` (per-outcome code mapping, `invalidCodesOf`, PrintHuman explain renders invalid-only) + black-box `commands/workflow_explain_test.go` (stable codes in JSON, human/JSON `--explain`, omit-without-explain, valid-plan-omits-map, `--explain`-doesn't-change-plan via decode+DeepEqual, `--out`/`--json --out` byte parity, `explain <code>`/`--list`/unknown→2). CLI coverage 97.7% → 97.7% (gate 97 PASS, NOT raised; `workflow` pkg 98.7%, only the structurally-unreachable `Emit` encode branch uncovered). Source: `docs/roadmap.md` v1.11.

**Durable rule (v1.11 Slice 2) — workflow validation codes & explanations:** Workflow step-validation codes (`workflow.step.*`) are **stable, operator-facing identifiers** (a CLI/tool contract) — there is exactly one per distinct validation outcome (15 today; `codeStepMissingPolicy` is shared by policy.check and policy.review), the set is **locked by a test**, and **every code MUST have an explanation** (guarded by `workflow/explain_internal_test.go` — adding a code without an explanation fails in the same slice). Do NOT invent codes for validations that do not exist. Workflow explanation DATA lives in the `workflow` package (`workflow/explain.go`, `Explanations()`, keyed by the code constants); the `explain` command composes domains via `explain.Merge(doctor, policy, workflow)` (dispatch-level composition in `commands`, not domain logic in `commands`). Reuse the canonical `explain` package — one `Explanation` type, one renderer (`explain.PrintInline`/`explain.ForReport`); do NOT add a second explanation schema or a registry framework. `workflow plan --explain` is **presentation only** — it must NOT change which steps are validated, the plan body (steps/summary/result), or the exit code; the JSON `explanations` map is additive (`omitempty`, set only with `--explain`, byte-identical without it; `--json --out --explain` byte-identical). `--explain` explains **invalid steps only** in BOTH human and JSON (a valid step needs no explanation; `workflow.step.valid` is in the registry for `explain <code>`/`--list` but is not emitted into a plan's explanations map). Workflow planning stays dry-run/offline and must not execute steps. The support-bundle `explanations.json` stays doctor-scoped (do not merge the workflow/policy codes into it). CLI stays a pure API client; no API/DTO/contract change.

v1.11 Slice 1 (workflow plan dry-run): started v1.11 Local Intelligence & Workflow Automation with `portier workflow plan --file <workflow.json>` — reads a local workflow file (an ordered sequence of EXISTING safe operations), validates its schema + step references, and prints a deterministic plan. CLI-only, pure API client, fully offline, `validate:contract` 234/234 unchanged (no shared/server/service/client/API/DTO/config change). New domain package `tools/cli/sources/workflow` (`workflow.go`): `File`/`Step` + `Parse` (DisallowUnknownFields; requires `schemaVersion==1` + non-empty `steps`); `Plan`/`StepPlan`/`PlanSummary` + `BuildPlan` (per-step validation → valid/invalid); `PlanExitCode`; `PrintHuman` (`[VALID]`/`[INVALID]` ASCII tags + summary + `Result:`); `Emit`/`EmitOptions` (`--json`/`--out`, byte-identical). `commands/workflow.go` (`RunWorkflow`/`RunWorkflowPlan`) parses flags + reads the file + calls workflow helpers; handlers-only; dispatched OFFLINE from `main.go` (like `explain`/`policy` — no URL resolution). Step types (validated, NOT executed): `policy.check` (exactly one of `config`/`runtime` + `policy`), `policy.review` (`current`/`candidate`/`policy`), `policy.baseline.compare` (`baseline` + exactly one of `report`/`reportFrom`). Validation: ids required + unique; type supported with required fields; `reportFrom` must reference an EARLIER step (so the only dependency edge is inherently acyclic — no cycle detector needed); unknown fields rejected. Planning is structural only — it does NOT open/parse the files a step refers to (a missing referenced file does NOT fail the plan). Plan model is separate from policy/doctor. Exit: 0 plan valid; 1 plan invalid (≥1 invalid step) OR `--out` write failure; 2 usage (missing `--file`/`--out` value) or unreadable/malformed workflow (missing/unsupported `schemaVersion`, no steps) — NO `3` (never contacts runtime). Covered by white-box `workflow_internal_test.go` + black-box `workflow_test.go` (no-mutation, does-not-open-referenced-files, `--out` byte parity/write-failure) + `main_test.go` offline dispatch. `coverpkg` (`scripts/coverage-tools-cli.js`) extended with the `workflow` package. CLI coverage 97.6% → 97.7% (gate 97 PASS, NOT raised — ~0.7% buffer too thin for 98; `workflow` pkg 98.6%, only the structurally-unreachable `json.Marshal`-on-concrete-`Plan` encode branch in `Emit` uncovered). Source: `docs/roadmap.md` v1.11.

**Durable rule (v1.11 Slice 1) — workflow plan dry-run:** A workflow is an **ordered sequence of EXISTING safe operations** described in a small JSON file (`schemaVersion: 1` + non-empty `steps`) — `workflow plan` must be **deterministic, fully offline, and dry-run**: it must NEVER execute a step, contact the runtime, read/parse the files a step refers to, apply/import configs, enforce a policy, schedule anything, or mutate any file except the requested `--out` file. It **composes existing operations by reference** — it validates that a step names a known operation (`policy.check`/`policy.review`/`policy.baseline.compare`) with coherent inputs; it must NOT duplicate policy/doctor semantics or become a scripting language/expression engine/plugin framework. **Step types** are stable operator-facing identifiers (a CLI/tool contract — test any new one). Keep the **plan model separate** from the policy/doctor report models (`Plan`/`StepPlan`/`PlanSummary` in package `workflow`). Validation is structural + reference-coherence only: `schemaVersion==1`, ≥1 step, unique non-empty ids, supported type with its required fields, exactly-one-of where specified (`config`/`runtime`; `report`/`reportFrom`), and `reportFrom` referencing an **earlier** step (keep the dependency graph acyclic by construction — do not add forward refs or a general cycle detector unless a real multi-edge dependency is introduced). **Unknown fields are rejected** (catch typos). A missing **referenced** file must NOT fail the plan (planning does not open them). Output is symbol-free ASCII (`[VALID]`/`[INVALID]` + `Result: valid/invalid`); `--json` emits the full plan; `--out` writes byte-identical JSON (build once, write both). Exit codes: 0 valid, 1 invalid plan OR `--out` write failure, 2 usage or unreadable/malformed workflow (missing/unsupported `schemaVersion`, no steps) — there is **NO connection-failure (3)** code because planning never contacts the runtime. Schema/parser/validator/model/render live in package `workflow`; `commands` stays handlers-only (parse flags, read file, call `workflow.Parse`/`BuildPlan`/`Emit`); dispatch is OFFLINE (no URL resolution). The CLI stays a pure API client (no shared/server/service imports). **No step execution, scheduler, background automation, policy enforcement, config apply/import, runtime mutation, target probing, UI workflow panel, or protocol allowlist/`allowUdp` in this slice** — execution and reporting are a deliberate later v1.11 slice.

v1.10 Slice 7 (policy baseline snapshots): added `portier policy baseline create --from-report <report.json> --out <baseline.json>` and `portier policy baseline compare --baseline <baseline.json> --report <report.json>` — a dry-run acceptance workflow over the policy report JSON files produced by `policy check`/`policy review` (`--json`/`--out`). CLI-only, fully offline, `validate:contract` 234/234, CLI coverage 97.6% (gate 97 PASS, not lowered). Model/fingerprint/build/compare live in the `policy` package (`policy/baseline.go`): `Baseline{schemaVersion,createdAt,source?,result,findings}` + `BaselineFinding{fingerprint,code,severity,title,message}` + `CompareReport{summary{new,resolved,unchanged},result,new[],resolved[],unchanged[]}`; `Fingerprint(Finding)`, `ParseReportSnapshot`/`ParseBaseline`, `BuildBaseline(snap, now)` (injected clock → RFC3339 UTC `createdAt`), `Compare`/`CompareExitCode`, `PrintCompareHuman`. `commands` (`RunPolicyBaseline`/`Create`/`Compare`) parses flags + reads files + calls the policy helpers + `output.WritePrettyJSON`; handlers-only. Fingerprints read the generic report-JSON detail map (so a finding fingerprints identically from offline `--config` or `--runtime`, never using volatile rule IDs): rule-scoped `code|name|protocol|listenHost|listenPort`, duplicate-binding `code|protocol|listenHost|listenPort|<sorted rule names>`, fallback `code|message`. The `policy.valid` marker is excluded from baselines/compare. Result `failed` iff NEW findings exist (resolved-only does not fail). Exit: create 0/1(write)/2(usage|malformed report); compare 0(no new)/1(new)/2(usage|malformed baseline-or-report incl. unsupported baseline schemaVersion). Covered by white-box `baseline_internal_test.go` + black-box `policy_baseline_test.go`. Source: `docs/roadmap.md` v1.10.

**Durable rule (v1.10 Slice 7) — policy baseline snapshots:** A policy baseline is an **accepted snapshot of policy findings, NOT a config copy** — it must store only the compact finding snapshot (`fingerprint`, `code`, `severity`, `title`, `message`) plus `schemaVersion`/`createdAt`/`source`/`result`; it must NEVER embed raw config, secrets, runtime host data, finding `details`, or explanations. Both `baseline create` and `baseline compare` are **fully offline and deterministic** — they operate ONLY on existing policy report JSON files (from `policy check`/`policy review`, `--json`/`--out`); they must never contact the runtime, probe targets, or mutate the report/baseline inputs. **New findings fail; resolved-only changes do not** (`compare` result is `failed` iff `summary.new > 0`; exit 1 iff new findings, else 0). Finding **fingerprints must be deterministic, stable, source-independent, and test-covered** — derived from the report's detail map (rule-scoped `code|name|protocol|listenHost|listenPort`; duplicate-binding `code|protocol|listenHost|listenPort|<sorted rule names>`; fallback `code|message`), never using volatile rule IDs, and identical whether the source report came from offline `--config` or `--runtime`. The `policy.valid` no-violations marker is excluded from baselines and from compare (it must never count as new/resolved/unchanged). Baseline model/fingerprinting/build/compare live in the `policy` package (`policy/baseline.go`), file writing in `output`, and `commands` stays handlers-only; `BuildBaseline` takes an injected clock so `createdAt` stays testable. Exit codes: create 0 written / 1 output-write failure / 2 usage or unreadable-malformed report; compare 0 no-new / 1 new / 2 usage or unreadable-malformed baseline-or-report (incl. unsupported baseline `schemaVersion`). CLI stays a pure API client; no API/DTO/contract change, no enforcement/remediation, no full config storage in the baseline.

v1.10 CLI package split (model/types out of `commands`): refactored the Go CLI so the general model/result types and their pure, offline logic live in focused per-domain packages instead of the catch-all `commands` package — behavior-preserving, `validate:contract` 234/234, CLI coverage 97.8% (gate 97). New packages: `tools/cli/sources/config` (the local config-file domain — `Rule`/`ParseLocal`/`Validate`/`Summary`/`FindDuplicateBindings`/`RuleDetail`/`BindingConflict` + `PartitionValidationErrors`/`RuleLabelName`/`HasControlChar`/`GroupMaxLength`/`DuplicateBindingErrPrefix`), `tools/cli/sources/doctor` (the doctor model `Report`/`CheckResult`/`Summary`/`Severity`, the live-runtime `RunLiveChecks` + builders, the offline `RunConfigChecks` + advisories, the `Explanation` registry + `ExplanationFor`/`SortedExplanations`, and report emission `Emit`/`PrintHuman`/`ExitCode`/`ResultLabel`/`ReportPayload`/`EmitOptions`), and `tools/cli/sources/policy` (the policy model `Report`/`Finding`/`Summary`/`Severity`, `Parse`/`Evaluate`/`PrintHuman`/`ExitCode`, the opaque `Policy` value). Three shared helpers moved into `output` (`PluralWord`/`PluralRule`/`WritePrettyJSON`). Types were **de-stuttered** for idiomatic Go (`DoctorReport`→`doctor.Report`, `PolicyReport`→`policy.Report`, `rawConfigRule`→`config.Rule`, etc.). The `commands` package now holds ONLY the `Run*` handlers + help text + dispatch; it imports `config`/`doctor`/`policy`/`client`/`output`. Package dependency DAG (no cycles): `output` ← everyone; `config` ← `doctor`,`policy`,`commands`; `doctor`/`policy` ← `commands`; `client`/`version` ← `doctor`,`commands`. Output is byte-identical (struct JSON tags unchanged; black-box tests assert verbatim). White-box internal tests moved to their packages (`config`/`doctor`/`policy`); black-box `commands_test` tests now reference `doctor.Report`/`policy.Report`/`doctor.Explanation`. The CLI coverage script (`scripts/coverage-tools-cli.js`) `coverpkg` list was extended to instrument the new packages (without it the gate under-counted cross-package-only functions and read 97.3%). CLI stays a pure API client. Source: user request (general types should not live in `commands`).

**Durable rule (v1.10 CLI package layout):** In the Go CLI, **general model/result types and their pure, offline logic do NOT live in the `commands` package** — `commands` holds only the `Run*` command handlers, help text, and dispatch. Domain types + logic live in focused per-domain packages: the local config-file domain in `config` (`Rule`, `ParseLocal`, `Validate`, `Summary`, `FindDuplicateBindings`, `RuleDetail`, …), the doctor model + check builders + explanation registry + emission in `doctor` (`Report`, `CheckResult`, `RunLiveChecks`, `RunConfigChecks`, `Explanation`/`ExplanationFor`, `Emit`, …), the policy model + evaluator in `policy` (`Report`, `Finding`, `Parse`, `Evaluate`, `PrintHuman`, …), the config plan/diff/apply **response rendering** in `planview` (`PrintPlan`, `PrintDiff`, `PrintApply`, `PlanExitCode`, `ApplyExitCode` — presentation over `client.ConfigPlanResponse`/`ConfigApplyResponse`, no plan logic), and generic output helpers in `output` (`PrintJSON`, `WritePrettyJSON`, `PrintTable`, `PluralWord`/`PluralRule`). A new CLI command adds its `Run*` handler in `commands` and puts any new model/logic in the appropriate domain package (or a new one) — do NOT add general types back into `commands`. Keep names **idiomatic (de-stuttered)**: a type in package `doctor` is `doctor.Report`, not `doctor.DoctorReport`. Preserve the **import DAG** (`output` depends on nothing internal; `config` is the base domain; `doctor`/`policy` build on `config`; `commands` composes them) — no import cycles, and `config`/`doctor`/`policy` must NOT import `commands`. These packages stay **offline/pure** where they were (no runtime contact except `doctor`'s live checks, which take a `*client.Client`); the doctor/policy/config code must not gain command-dispatch concerns. JSON output shape is a contract — struct field tags must stay byte-identical through any further move (black-box tests assert verbatim output; `validate:contract` 234/234). When you add or move a CLI package, update `scripts/coverage-tools-cli.js` `coverpkg` so it is instrumented, or the gate silently under-counts. Prior durable-rule references to `tools/cli/sources/commands/{doctor,policy,config}.go` and to `DoctorReport`/`PolicyReport`/`rawConfigRule` etc. are **superseded** by this layout (those notes were descriptive of the old location). The CLI stays a pure API client (no shared/server/service imports).

v1.10 Slice 1 (policy model + dry-run evaluator): started v1.10 Automation, Policies & Safe Operations with the first **offline policy dry-run** command — CLI-only, pure API client, `validate:contract` 234/234 unchanged (no shared/server/service/client/API/DTO/config change). New `portier policy check --config <file> --policy <file>` (`RunPolicy`/`RunPolicyCheck` in `tools/cli/sources/commands/policy.go`, dispatched **offline** from `main.go` like `explain` — no URL resolution): reads a local config + a small JSON policy file, evaluates the config against the policy, and prints a deterministic report. Fully offline — never contacts the runtime, never probes targets, never modifies the config/policy file. Policy format (`schemaVersion: 1` + a `rules` object): five boolean guardrails `requireGroup`/`allowLanExposure`/`allowPrivilegedPorts`/`allowAutostart`/`forbidDuplicateBindings`, parsed with `DisallowUnknownFields` (**unknown fields rejected**); omitted fields default to a **permissive baseline** (requireGroup/forbidDuplicateBindings off, allow* on — an empty policy permits everything; operators opt into restrictions). Result model (kept SEPARATE from `DoctorReport`): `PolicyFinding {code, severity(info|warning|error), title, message, details?}` → `PolicyReport {findings[], summary{info,warning,error}, result(passed|failed)}`. Stable finding codes: `policy.valid` (info, compliant), `policy.group_required`, `policy.lan_exposure_forbidden`, `policy.privileged_port_forbidden`, `policy.autostart_forbidden`, `policy.duplicate_binding_forbidden` — all violations are `error` this slice. Deterministic ordering: per-rule findings in config file order (within a rule: group → LAN exposure → privileged port → autostart), then duplicate-binding findings last (one per conflicting binding, sorted proto→host→port). Reuses the v1.9 config-doctor interpretations (`toRuleDetail`/`findDuplicateBindings`/`ruleLabelName`, LAN = `0.0.0.0`, privileged = `<1024`) so policy and doctor cannot drift. Human output `[INFO]`/`[ERROR]` tags + severity summary + `Result: passed/failed`; `--json` emits the full `PolicyReport`. Exit codes (v1.7 policy): 0 no violations, 1 ≥1 violation (or JSON-encode failure), 2 missing/invalid args or unreadable/malformed config/policy (incl. unsupported/missing `schemaVersion`). UDP stays first-class — **no `allowUdp`/protocol-restriction policy**, a UDP rule is evaluated by the same guardrails as TCP. Tests: black-box `policy_test.go` (every code, ordering, JSON/human shape, all exit-2 input errors, unknown-field rejection, no-mutation, offline, no-UDP-policy, dispatch) + white-box `policy_internal_test.go` (defaults/parse errors/report/exit/severity-tag) + `main_test.go` dispatch (offline — invalid `--url` ignored). CLI coverage 97.6% → 97.8% stmts, every policy function 100%. **Applying the opportunistic coverage-gate ratchet, the CLI gate was raised 95 → 97** (actual 97.8%; deterministic cross-package Go number; a tighter ~0.8% buffer accepted deliberately — the remaining uncovered lines are documented structurally-unreachable branches: os.Exit wrapper, `json.Marshal` on concrete DTOs, platform filesystem-failure paths). Source: `docs/roadmap.md` v1.10.

**Durable rule (v1.10 Slice 1) — policy model & offline dry-run:** Policy evaluation (`portier policy check`) must be **deterministic, fully offline, and dry-run** — it must NEVER contact the runtime, probe targets, scan host state, collect env/process/logs, or mutate the runtime/config/**policy** files, unless a later slice explicitly changes this. **Policy finding codes** (`policy.*`) are **stable operator-facing identifiers** (a CLI/tool contract) — do not rename casually; test any new code. Keep the result model small and **separate from `DoctorReport`**: `PolicyFinding {code, severity(info|warning|error), title, message, details?}` → `PolicyReport {findings[], summary, result}` — do not grow it into a rule-engine/scripting/expression/plugin framework. The policy file is a **small JSON schema** (`schemaVersion` + a `rules` object of booleans); **reject unknown fields** (a typo must not silently relax a guardrail) and apply a **permissive default** for omitted fields (an empty policy permits everything). Evaluation must **reuse the v1.9 config-doctor interpretations** (LAN exposure = `0.0.0.0`, privileged = `<1024`, duplicate binding via `findDuplicateBindings`, rule detail via `toRuleDetail`) — do not create divergent interpretations of the same concept; if sharing would force a large refactor, keep a small local helper but lock its behavior to the doctor's with tests. Ordering is **observable**: per-rule checks in config file order (group → LAN exposure → privileged port → autostart), duplicate bindings last, sorted proto→host→port — keep the ordering test green. Output is **symbol-free ASCII** (`[INFO]`/`[WARN]`/`[ERROR]`) with a `Result: passed/failed` line; `--json` emits the full report. Exit codes follow the v1.7 policy: `0` no violations, `1` ≥1 violation (or a JSON-encode failure), `2` usage / unreadable / malformed config or policy (incl. unsupported `schemaVersion`) — invalid input is a CLI error, **not** a policy finding. **UDP is first-class** — do NOT add an `allowUdp`/`udpForbidden` rule or any protocol allowlist/denylist unless a later feature deliberately introduces one. The CLI stays a **pure API client** (here a pure-local command; no shared/server/service imports). No live enforcement, no auto-remediation, no scheduler, no UI policy panel, no remote/auth/team behavior, no policy scripting in this slice.

v1.9 Slice 1 (doctor result model + offline config doctor): introduced the deterministic doctor result model and the first offline doctor command — CLI-only, pure API client, `validate:contract` 234/234 unchanged (no shared/server/service/client/API/DTO/config change). Model (`tools/cli/sources/commands/doctor.go`): `DoctorCheckResult {code, severity(info|warning|error), title, message, details?}`, `DoctorReport {checks[], summary{info,warning,error}}`, `newDoctorReport`/`exitCode`/`printDoctorHuman`/`emitDoctorReport`. Command `portier config doctor <file>` (`RunConfigDoctor` in `config.go`, dispatched from `RunConfig`): reads a local config file, runs deterministic **offline** checks (no running service, file never modified), human output (`[INFO]`/`[WARN]`/`[ERROR]` tags + severity summary) and `--json` (full `DoctorReport`). Stable check codes: `config.read_failed`, `config.parse_failed`, `config.empty`, `config.validation_failed`, `config.duplicate_binding`, `config.lan_exposure`, `config.privileged_port`, `config.valid`. Checks reuse `parseLocalConfig`/`validateLocalConfig`; duplicate-binding errors are partitioned out of field errors (shared `duplicateBindingErrPrefix` const so producer/consumer can't drift); LAN-exposure (`0.0.0.0`) and privileged-port (`< 1024`) advisories computed locally in file order. Full common-port advisory table deferred (would duplicate `@portier/shared` `COMMON_PORTS`). Exit codes (v1.7 policy): 0 no error-severity checks (warnings still 0), 1 ≥1 error-severity check, 2 missing/invalid arg. Tests: black-box `config_doctor_test.go` (all codes, JSON shape, human output, exit codes, dispatch, JSON-encode failure) + white-box `doctor_internal_test.go` (model/summary/exit/partition/tag). CLI coverage 97.2% → 97.4% stmts (98.9% → 99.0% funcs), gate 95% PASS — not lowered. Source: `docs/roadmap.md` v1.9.

v1.9 Slice 2 (live runtime doctor baseline): added the first live-runtime doctor command `portier doctor` (`RunDoctor` in `tools/cli/sources/commands/doctor.go`, dispatched from `main.go`), reusing the Slice 1 model (`DoctorReport`/`DoctorCheckResult`, `emitDoctorReport`/`printDoctorHuman`/`exitCode` — no second model). CLI-only, pure API client, `validate:contract` 234/234 unchanged (no new endpoints/DTOs — reuses `GET /api/runtime`, `GET /api/status`, `GET /api/config/export`). Read-only: never mutates runtime/config, never probes targets. Deterministic fixed-order checks: runtime reachability → version (info; **warning, never error**, on CLI/runtime mismatch) → status read → rule presence + aggregated health (error→warning→all-clear order) → read-only config-export. Health is read straight from the API `health` field — the CLI never re-derives it. Stable codes: `runtime.reachable`, `runtime.unreachable`, `runtime.version`, `runtime.status_read`, `runtime.status_failed`, `rules.none`, `rules.present`, `rules.health_ok`, `rules.health_warning`, `rules.health_error`, `config.export_read`, `config.export_failed`. Exit codes follow the doctor policy (0 no error-severity checks, warnings still 0; 1 ≥1 error-severity check; 2 usage) — **an unreachable runtime is reported as `runtime.unreachable` and exits 1, NOT the usual connection-failure 3** (the doctor always completes and emits a report); documented in `tools/cli/readme.md`. Tests: black-box `doctor_runtime_test.go` (all codes, reachable/unreachable, version match/mismatch, no-rules, health warning/error/mixed, status-failed, export-failed, JSON shape, human output, JSON-encode failure, arg/usage) + `main_test.go` dispatch. CLI coverage 97.4% → 97.6% stmts (99.0% → 99.1% funcs), gate 95% PASS — not lowered; every new doctor function 100% covered. Source: `docs/roadmap.md` v1.9.

v1.9 Slice 3 (doctor explain codes): added `portier explain <code>` (`RunExplain` in `tools/cli/sources/commands/explain.go`, dispatched from `main.go`) — a fully **offline** lookup that prints a deterministic explanation (meaning + what-to-do + related codes) for any stable doctor/check code. CLI-only, no client/API call (ignores `--url`), `validate:contract` 234/234 unchanged. Static `explanations` registry keyed by the existing doctor code constants (no string-literal duplication) covering all 20 codes (8 config-doctor + 12 live-doctor); `Explanation {code,title,meaning,action,severity,related}`. `--list` prints all codes sorted (human/JSON); unknown/missing/too-many-args → exit 2 with a clear error pointing to `--list` (never a generic explanation); single success → 0. Internal guard (`explain_internal_test.go`) asserts every code constant is explainable, registry has no extra/duplicate entries (count == 20), each `Code` matches its key, required fields present with a valid severity, and no `related` reference dangles or self-references. Tests: black-box `explain_test.go` (config/runtime code, JSON shape, `--list` human/JSON sorted, unknown/missing/too-many exit 2, JSON-encode failures, help/bad-flag) + `main_test.go` dispatch (offline — NOT in the invalid-URL table). CLI coverage 97.6% → 97.7% stmts (funcs 99.1%), gate 95% PASS — not lowered; every new explain function 100% covered. Source: `docs/roadmap.md` v1.9.

v1.9 Slice 4 (doctor strict mode + report polish): added `--strict` to both doctor commands (`portier doctor --strict`, `portier config doctor --strict <file>`) — strict treats **warnings as failures** (warning-only report → exit 1; normal unchanged: info/warning → 0, error → 1). CLI-only, `validate:contract` 234/234 unchanged (no checks/probing/mutation change). The old `DoctorReport.exitCode()` method was replaced by `doctorExitCode(report, strict)` + `doctorResultLabel(report, strict)` (`doctor.go`); `emitDoctorReport`/`printDoctorHuman` gained a `strict` param; both `RunDoctor` and `RunConfigDoctor` parse a `--strict` bool and route through them. Report polish: human output ends with a stable `Result: passed`/`Result: failed` line and adds a `Strict mode: warnings are treated as failures.` note when strict is the reason a warning-only report fails; JSON gains additive top-level `strict`+`result` via an embedded `doctorReportJSON{DoctorReport; Strict; Result}` (the pure `DoctorReport`/`checks`/`summary` shape is unchanged, so existing JSON consumers/tests keep working). For `config doctor`, `--strict` must precede the file (Go `flag` stops at the first non-flag arg — same flags-before-file convention as apply/plan/diff). Tests: black-box strict cases in `config_doctor_test.go`/`doctor_runtime_test.go` (warning-only normal 0 vs strict 1, error 1 both, valid/all-info strict 0, strict JSON `strict`/`result` fields), internal `doctor_internal_test.go` (`doctorExitCode` normal+strict, `doctorResultLabel`, strict human note), `main_test.go` dispatch (`doctor --strict`, `config doctor --strict <file>`). CLI coverage held at 97.7% stmts (99.1% funcs) — small fully-covered change, gate 95% PASS, not lowered; every new strict function/branch 100% covered. Source: `docs/roadmap.md` v1.9.

v1.9 Slice 5 (doctor report export): added `--out <file>` to both doctor commands (`portier doctor --out <file>`, `portier config doctor --out <file> <config>`) — writes the JSON doctor report to a file for CI artifacts/support bundles. CLI-only, `validate:contract` 234/234 unchanged (no schema/probing/mutation change). The exported file uses the **same** `doctorReportJSON` shape as `--json` (`checks`+`summary`+`strict`+`result`) — no second schema. `emitDoctorReport` gained an `outPath` param: it builds the payload once, prints to stdout (JSON if `--json`, else human), then if `outPath != ""` writes the same payload via the existing `writePrettyJSON` (pretty, create/truncate, no parent-dir creation, no partial write on marshal failure). `--out` writes the file regardless of `--json`; in human mode it adds a `Report written to <file>` confirmation on stdout, in JSON mode stdout stays pure JSON. Exit semantics unchanged except a **file-write failure → exit 1** (operation failure, reported on stderr, overrides the diagnostic exit code) and a missing `--out` value → exit 2; the report is still written when `--strict` fails the run on a warning (file shows `result:"failed"`). For `config doctor`, `--out`/`--strict` precede the file (flags-before-file). Tests: black-box `doctor_export_test.go` (both commands — write/strict/json-stdout+file parity/write-failure/missing-value) + `main_test.go` dispatch. CLI coverage held at 97.7% stmts (99.1% funcs) — small fully-covered change (emit/RunDoctor/RunConfigDoctor 100%), gate 95% PASS, not lowered. Source: `docs/roadmap.md` v1.9.

v1.9 Slice 6 (doctor support bundle baseline): added the top-level `portier support-bundle --out <dir>` (`RunSupportBundle` in `tools/cli/sources/commands/supportbundle.go`, dispatched from `main.go`) — collects deterministic doctor artifacts into a directory for CI artifacts/bug reports/future tooling. CLI-only, `validate:contract` 234/234 unchanged (no new endpoints/DTOs/schema). Read-only: contacts the live runtime but never mutates runtime/config. Directory output only (no zip this slice). Reuses the existing doctor builder (`runLiveDoctorChecks`), the `doctorReportJSON` payload, the human renderer (`printDoctorHuman` → `doctor.txt`), the explanation registry (extracted `sortedExplanations()` shared with `explain --list`), and `writePrettyJSON` — no duplicated doctor logic, no second doctor schema. Bundle files: `manifest.json` (schemaVersion 1, generatedAt RFC3339, cliVersion, source/generatedBy, runtimeUrl, strict, result, artifacts, warnings), `doctor.json` (same shape as `doctor --json`), `doctor.txt`, `explanations.json`, and — when available — `runtime.json`/`config-export.json` (omitted-with-manifest-warning when unreachable). Flags: `--out` (required), `--strict` (strict doctor interpretation), `--json` (bundle summary). Exit: 0 written+passed, 1 written+doctor-failed (errors/unreachable/strict-warning) OR filesystem/write failure, 2 missing `--out`/usage. `prepareBundleDir` creates a missing dir (MkdirAll), uses an existing **empty** dir, and **refuses** a non-empty dir or a non-directory path (exit 1 — never silently overwrite). Unreachable runtime still produces a useful bundle (manifest + doctor.json `runtime.unreachable` + doctor.txt + explanations.json) and exits 1. Safety: NO env vars / process lists / logs / tokens / filesystem scans (may include rule names/hosts/ports). The per-file writes were consolidated into one loop (`bundleArtifact{name, write}`) to minimize unreachable defensive branches. Tests: black-box `support_bundle_test.go` (12+ cases) + `main_test.go` dispatch (incl. invalid-URL → 2). CLI coverage 97.7% → 97.4% stmts (99.1% funcs), gate 95% PASS, **not lowered** — the dip is unavoidable defensive per-file filesystem-write-failure branches that can't be triggered portably once the dir is validated empty+writable (command-level filesystem-failure → exit 1 IS covered via mkdir-failure/non-empty/out-is-file). Source: `docs/roadmap.md` v1.9.

v1.9 Slice 7 (doctor inline explanations): added `--explain` to both doctor commands (`portier doctor --explain`, `portier config doctor --explain <file>`) — inline per-check explanations (code + meaning + next action + related) for the checks present in the report. CLI-only, `validate:contract` 234/234 unchanged. Reuses the Slice 3 explanation registry via `explanationsForReport(report)` (only emitted codes, deduped via the map, unknown codes safely omitted) — no duplicated explanation strings, no second schema. Human: `printDoctorHuman` gained an `explain` param; `printCheckExplanation` renders an indented `Code:`/`Meaning:`/`What to do:`/`Related:` block per check (unknown code → `(no explanation available)` fallback). JSON: `doctorReportJSON` gained `Explanations map[string]Explanation json:"explanations,omitempty"` — additive, populated only with `--explain`, only for emitted codes; without `--explain` the JSON is byte-identical to before (omitempty), so `--json`/`--out`/support-bundle `doctor.json` are unchanged. The emit flags were grouped into a small `doctorEmitOptions{strict, explain, json, outPath}` struct (internal refactor, no behavior change) threaded through `emitDoctorReport`; both `RunDoctor`/`RunConfigDoctor` parse `--explain` and pass it; `support-bundle` passes `explain:false`/builds `doctorReportJSON` without `Explanations` so its `doctor.txt`/`doctor.json` stay unchanged. `--explain` does NOT change which checks run, the summary, the result, or exit codes. Tests: black-box `doctor_explain_test.go` (both commands — human/json-map/out/strict/unreachable/no-report-change/no-map-without-explain) + internal `explanationsForReport` (only-emitted/dedupe/unknown-omitted/all-codes-explainable) + `printCheckExplanation` (known + fallback). CLI coverage 97.4% → 97.5% stmts (99.1% → 99.2% funcs), gate 95% PASS, not lowered — recovers part of the Slice 6 dip; every new explain helper 100% covered. Source: `docs/roadmap.md` v1.9.

v1.9 Slice 8 (config doctor advisory details): enriched `portier config doctor` findings with deterministic structured `details` (`config.go` `runConfigDoctorChecks`/`configDoctorAdvisories` + new `findDuplicateBindings`/`toRuleDetail`/`lanExposureTitle`/`privilegedPortTitle` + `ruleDetail`/`bindingConflict`/`bindingRule` types). CLI-only, `validate:contract` 234/234 unchanged, **no new check codes**. `config.duplicate_binding` gains a sorted (proto→host→port) `bindings` array (each `{protocol,listenHost,listenPort,rules:[{id?,name,enabled,group?}]}`, rules in file order) alongside the existing `errors`; `config.lan_exposure`/`config.privileged_port` now **aggregate all affected rules into one check** with a file-order `rules` array (`{id?,name,protocol,listenHost,listenPort,enabled,group?}`) — changing the per-advisory warning from N checks to 1 (exit behavior unchanged; single-rule titles preserved, multi-rule titles read "N rules …"); `config.empty`→`{ruleCount:0}`, `config.validation_failed`→`{errors,ruleCount}`, `config.valid`→`{ruleCount,tcpCount,udpCount}`. Details are derived ONLY from the offline config (no env/process/log/filesystem/runtime/probe data); ids included only when present (omitempty), groups trimmed. LAN exposure stays the `0.0.0.0` detection (consistent with the shared advisory — `::` intentionally NOT added so which rules are flagged is unchanged); privileged stays `<1024` (no `COMMON_PORTS` duplication). Details flow through `--json`/`--out` (part of `DoctorCheckResult`); `--explain` stays presentation-only; the live `doctor` and support-bundle are unaffected. Existing single-rule tests pass unchanged (aggregation is identity for one rule). Tests: black-box `config_doctor_details_test.go` + internal `configdetails_internal_test.go` (3-on-one, invalid-skipped, no-dup, sort tie-breakers, aggregation/titles, trim). CLI coverage held at 97.5% stmts (99.2% funcs) — new detail builders 100% covered, gate 95% PASS, not lowered. Source: `docs/roadmap.md` v1.9.

v1.9 Slice 10 (doctor toolkit release-readiness audit): focused audit of the v1.9 Doctor & Config Toolkit (Slices 1–9) — audit/cleanup only, no new features. **Result: PASS, v1.9 ready for finalization.** Verified the command surface + all flag combinations (binary battery), JSON schema consistency (`{checks,summary,strict,result}` + `explanations` only with `--explain` + `config` only for config doctor after parse; live doctor never carries `config`; `--json --out` byte-identical), check-code/explanation coverage (20 codes all explainable, no extra/dangling/ad-hoc-literal codes, `explain --list` sorted, inline only-emitted), exit codes (0/1/2 incl. unreachable→1 with useful report/bundle, write-failure→1, unknown-explain→2), and safety (doctor commands do read-only GETs only — `GetRuntime`/`GetStatus`/`ExportConfig`; config doctor is offline file-read; no env/process/log/exec collection; support bundle excludes unsafe data + refuses non-empty dirs; no target probing; `--strict`/`--explain`/`--out` never change which checks run). One tiny help-text fix: `doctor` help exit codes now mention `--out` write-failure→1 (parity with `config doctor`). Deferred non-blockers: offline `config doctor`/`config validate` still route through the URL-resolving `config` dispatch (pre-existing — a malformed `--url` fails at validation before the offline work; clean fix needs a dispatch refactor); two platform-dependent defensive filesystem-error branches in `prepareBundleDir`/`RunSupportBundle` stay uncovered (command-level filesystem-failure→1 fully tested). `validate:contract` 234/234; CLI coverage 97.6% (gate 95% PASS, none lowered); lint/typecheck clean. No new durable rule — the audit confirms the Slice 1–9 rules. Source: `docs/roadmap.md` v1.9.

v1.9 Slice 10 (doctor AI handoff prompt): added `prompts/doctor.md` — a reusable, copy-paste AI handoff prompt (full + short variants) for asking an AI assistant to interpret `portier doctor`/`config doctor` output or a support-bundle summary. **Docs-only** — no CLI/Go/TS code change (a `--prompt`/`prompt` CLI helper was considered and deferred to avoid a second copy of the prompt text that could drift from the doc); referenced from `tools/cli/readme.md` ("Using doctor output with an AI assistant") + a glossary term. Portier adds NO AI integration/upload/telemetry — the prompt is plain text the user pastes into an assistant of their choice. `validate:contract` 234/234, CLI coverage unchanged (no code), lint/typecheck clean. Source: `docs/roadmap.md` v1.9.

**Durable rule (v1.9 Slice 10) — AI handoff prompt:** The doctor AI handoff prompt (`prompts/doctor.md`) must stay **safe and offline**: it must NOT instruct users to paste secrets, tokens, private keys, full environment dumps, process lists, or unrelated logs (the doctor tools never collect those); it must tell the assistant to **treat the pasted doctor output as the source of truth** and not invent runtime state; and it must NOT imply Portier sends data to any AI service — Portier has **no AI integration, upload, or telemetry**, the user pastes locally-generated output into an assistant of their own choosing. The prompt must keep distinguishing info/warning/error, strict-mode failures, and operation failures, and ask for risk classification + prioritized next steps that **separate "safe now" from "requires admin/network/security approval."** If a CLI helper is ever added (`portier doctor --prompt` / a `prompt` command), it must be **fully offline** (no config read, no runtime contact, no external send) and only print the template — and the template must not drift from the canonical doc. Do not add AI API integration, telemetry, an upload feature, or automatic ticket creation under this banner.

v1.9 Slice 9 (config doctor summary + machine-readable counts): added a compact, deterministic top-level `config` summary to config doctor JSON (`config.go` `buildConfigSummary` + `configSummary`/`protocolCounts`/`groupSummary` types). CLI-only, `validate:contract` 234/234 unchanged, **no new check codes**. Lowest-risk placement: an optional `Config *configSummary json:"config,omitempty"` field on the SHARED `doctorReportJSON` wrapper (`doctor.go`) + a `config *configSummary` on `doctorEmitOptions`, set ONLY by config doctor — `runConfigDoctorChecks` now returns `(DoctorReport, *configSummary)` (nil on read/parse failure, non-nil incl. empty config) and `RunConfigDoctor` threads it into `emitDoctorReport`; the live `doctor` (RunDoctor) and the support bundle leave it nil, so their JSON is byte-identical (omitempty). Summary fields: `ruleCount`, `enabledRuleCount`, `disabledRuleCount`, `protocols{tcp,udp}`, `groupCount`, `groups[]{name,ruleCount,enabledRuleCount,disabledRuleCount}`, `ungroupedRuleCount`. Deterministic + offline: groups sorted by trimmed name, empty/whitespace group → ungrouped, stable `{tcp,udp}` shape (unknown protocols uncounted so `ruleCount` may exceed tcp+udp), derived only from the parsed config (no env/runtime/filesystem data), and does NOT repeat full rule data (Slice 8 details already identify rules). `listenHostCount`/`listenPortCount` intentionally omitted (compactness). Flows through `--json`/`--out` (byte-identical); `--explain` presentation-only (summary unchanged); `--strict` changes only exit/result. `checks` array shape unchanged; human output unchanged (JSON-only). Tests: black-box `config_doctor_summary_test.go` (mixed/empty/whitespace-group/validation-fail/parse-fail-no-summary/out/json-out-parity/explain-stable/strict-stable/live-doctor-has-none) + internal `buildConfigSummary` (counts/empty/unknown-protocol). CLI coverage 97.5% → 97.6% stmts (99.2% funcs), builder 100%, gate 95% PASS, not lowered. Source: `docs/roadmap.md` v1.9.

**Durable rule (v1.9 Slice 9) — config doctor summary:** The config doctor `config` summary must be **deterministic and derived ONLY from the offline config** — no environment, runtime, or filesystem data (beyond what already appears in read/parse error messages). It must NOT alter the `checks` array, exit semantics, `--strict` behavior, or `--explain` behavior (all of those must be byte-identical with vs without the summary present), and it must stay **compact** — counts and a per-group rollup only, never a re-dump of full rule data (Slice 8 finding details own that). Keep it deterministic: groups sorted by trimmed name, empty/whitespace group counted as ungrouped, a stable `protocols` shape. Place it as an **optional `omitempty` field on the shared `doctorReportJSON`** set ONLY by config doctor (via `doctorEmitOptions.config`) — the live `doctor` and the support bundle must leave it nil so their JSON stays byte-identical; do not fork a second wrapper. The summary is present whenever the file parses (including empty and validation-failure configs) and absent on read/parse failure. Prefer enriching report metadata over adding check codes (Slice 9 added none). CLI stays a pure client; no API/DTO/contract change, no support-bundle change.

**Durable rule (v1.9 Slice 8) — config doctor details:** Config doctor finding `details` must be **deterministic, JSON-serializable, and derived ONLY from the offline config** — never include OS environment variables, process info, logs, arbitrary filesystem data, runtime/live data, or target-probe results; only rule fields (name/host/port/protocol/enabled/group, and id when present in the config). Collections in details must be **sorted deterministically** (duplicate `bindings` by protocol→host→port; rules within a binding/advisory in file order) so JSON output is stable for tooling. **Enrich existing check codes before adding new ones** — Slice 8 added zero codes. Adding details must NOT change doctor semantics: keep exit behavior identical (normal warn→0, strict warn→1), keep LAN exposure as the `0.0.0.0` advisory (do not add `::` or otherwise change which rules are flagged), and keep the privileged-port advisory as the deterministic `<1024` check (never reproduce the shared `COMMON_PORTS` table in the CLI). The aggregated LAN/privileged advisories emit **one check per advisory kind** with a `rules` array — do not regress to one-check-per-rule, and preserve the single-rule title wording. Details belong on `DoctorCheckResult.Details` so they flow through `--json`/`--out`/`--explain` for free; `--explain` stays presentation-only; do not push config-doctor details into the live `doctor` or change support-bundle output. CLI stays a pure client; no API/DTO/contract change.

**Durable rule (v1.9 Slice 7) — doctor inline explanations:** Inline doctor explanations (`--explain`) MUST reuse the canonical Slice 3 explanation registry (via `explanationsForReport`) — never duplicate explanation strings or introduce a second explanation source. `--explain` must **not change which checks run**, the `checks` array, the `summary`, the `result`, or any **exit code** (it is presentation only) — guard this with a test comparing explain vs non-explain JSON (`checks`/`summary`/`strict`/`result` byte-identical). Explanations are emitted **only for codes present in the report**, deduplicated; a code with no registry entry must degrade safely (human `(no explanation available)`, JSON omitted) — and an internal test must keep proving every emittable doctor code IS explainable so that fallback never fires in practice. The JSON `explanations` map is **additive and backward-compatible**: it lives on `doctorReportJSON` as `omitempty`, set only with `--explain`, and must not alter the existing `checks`/`summary`/`strict`/`result` shape (output without `--explain` stays byte-identical). `--out --explain` writes the same payload as stdout (`--json --out --explain` byte-identical). Do NOT add inline explanations to the support bundle's `doctor.json`/`doctor.txt` (the bundle ships `explanations.json` separately) unless deliberately changing bundle behavior. Keep the doctor emit flags grouped in `doctorEmitOptions` rather than growing the parameter list. CLI stays a pure client; no API/DTO/contract change; existing `portier explain <code>` behavior is unchanged.

**Durable rule (v1.9 Slice 6) — doctor support bundle:** The support bundle must **reuse the doctor report JSON schema** (`doctorReportJSON`) for `doctor.json` and the existing doctor builder/renderer/explanation registry/`writePrettyJSON` helpers — never invent a second doctor/bundle schema or duplicate doctor logic. It must be **read-only**: contact the runtime but never mutate runtime/config, never start/stop rules, never probe targets. **Safety is non-negotiable** — a bundle must NOT include OS environment variables, process lists, logs, tokens/secrets, or arbitrary filesystem scans; it may include rule names/hosts/ports (Portier config/diagnostics data) and the configured runtime URL + requested output path only. A **runtime-unreachable bundle is still created** when possible (manifest + `doctor.json` with `runtime.unreachable` + `doctor.txt` + `explanations.json`; omitted artifacts → manifest warnings) and the command exits 1 (the doctor report has an error). Never silently overwrite: an existing **non-empty** output directory or a non-directory path is refused (exit 1); an existing empty dir is used; a missing dir is created. Filesystem/write failures are **operation failures → exit 1** (not diagnostic findings), distinct from usage errors (missing `--out` → 2). `manifest.json` is written last so its presence marks a complete bundle. Keep it a **directory** of plain JSON/text artifacts (zip is a deferred follow-up, not required); do not add a redaction framework, log/process/env collection, a server-side bundle endpoint, or a UI panel. The CLI stays a pure client; no API/DTO/contract change. Distinct from the existing single-file `diagnostics export` — do not merge or rename them without a deliberate decision.

**Durable rule (v1.9 Slice 5) — doctor report export:** Doctor report export (`--out`) must write the **same JSON schema as doctor `--json`** (the `doctorReportJSON` `{checks, summary, strict, result}` shape) — never invent a second export schema, and reuse the existing `writePrettyJSON` file helper (do not add a near-duplicate writer; do not create parent directories; no partial write on marshal failure). Exporting a report must **not mutate** the runtime or config and must not change which checks run or the diagnostic exit semantics. A **file-output failure is an operation failure → exit 1** (printed to stderr), NOT a diagnostic finding — it takes precedence over the report's own exit code; a missing `--out` value is a usage error → exit 2. `--out` writes the file regardless of `--json`; keep stdout valid JSON in `--json` mode (no confirmation line) and only print the `Report written to <file>` confirmation in human mode. The same payload that goes to stdout under `--json` must be byte-identical to the file contents (build it once, write it both places). The CLI stays a pure client; no API/DTO/contract change, no support-bundle/redaction/replay scope in this slice.

**Durable rule (v1.9 Slice 4) — doctor strict mode:** `--strict` changes **only the exit-code interpretation** of a doctor report (warnings become failing findings) — it must **never change which checks run**, never mutate the runtime/config, and never add probing. Both doctor commands share the same strict-aware helpers (`doctorExitCode(report, strict)` / `doctorResultLabel`) and the same `emitDoctorReport(..., strict, ...)` path — do not fork per-command exit logic. Normal-mode semantics are frozen: info-only → 0, warnings-only → 0, errors → 1; strict only adds warnings-only → 1 (errors still 1, info still 0). Keep the JSON report shape **additive and backward-compatible**: `checks`/`summary` stay on the pure `DoctorReport`, and `strict`/`result` are added via the embedded `doctorReportJSON` wrapper at emit time (do not pollute `DoctorReport` with presentation/outcome fields, and do not restructure the top-level shape). `result` mirrors the exit code (`passed`=0 / `failed`=1) and must stay consistent with `doctorExitCode`. Human output stays deterministic: the `Result:` line always prints; the `Strict mode: warnings are treated as failures.` note prints only when `strict && errors==0 && warnings>0`. For positional-arg doctor commands (`config doctor`), flags precede the file (consistent with apply/plan/diff) — document it, don't add bespoke interspersed-flag parsing. A future `--strict`-like modifier must reuse this exit/result plumbing, not a parallel one. The CLI stays a pure client; no API/DTO/contract change.

**Durable rule (v1.9 Slice 3) — doctor explain:** Every **stable doctor/check code must have an explanation** in the `explanations` registry — the `explain_internal_test.go` guard (driven by the explicit `allDoctorCodes` constant list) fails if a code constant has no entry, so **a new doctor code must add/update its explanation in the same slice**. Explanations are **static, deterministic, offline reference data** — no runtime contact, no external docs fetch, no AI generation, and they must **not claim active probing or automatic remediation** (the CLI does neither); keep `action` to operator guidance (e.g. "run `portier diagnose …`"), not a promise that Portier fixes it. Key the registry by the existing doctor code **constants** (reuse `checkConfig*`/`checkRuntime*`/`checkRules*` — no duplicated string literals), keep `related` entries pointing only at real known codes (guarded), and keep the `Explanation` shape small (`code/title/meaning/action/severity/related`) — do **not** grow this into a docs system, plugin/check-pack framework, or per-code file loader. Unknown/missing codes are a **usage error → exit 2** (never a generic fallback explanation); `--list` stays sorted/deterministic. `explain` is fully offline — it must not resolve a URL or build a client, and must stay out of the invalid-URL dispatch test. The CLI stays a pure API client (here, a pure-local command); no API/DTO/contract change. This command is the canonical place to explain replay/config/error codes later — extend the same registry, do not fork a second explainer.

**Durable rule (v1.9 Slice 2) — live runtime doctor:** `portier doctor` is a **read-only, deterministic** live diagnostic — it must **never mutate** the runtime or config, never start/stop rules, and **never probe forwarding targets** (no TCP/UDP dial, no reachability test) unless target probing is explicitly implemented in a later slice. It **reuses the Slice 1 doctor model** (`DoctorReport`/`DoctorCheckResult`, human/JSON/summary/exit helpers) — do **not** introduce a second doctor model or health model. Rule **health is read from the API `health` field** (v1.8) — the CLI must **not re-derive** health or invent its own classification; the runtime is the source of truth. Check codes (`runtime.*`, `rules.*`, `config.export_*`) are **stable operator-facing identifiers** (test any new one); checks run in a **fixed deterministic order** with a stable JSON shape. Version mismatch is a **warning, never an error** (do not fail `doctor` on version skew unless product policy changes). Exit codes follow the doctor policy (0 / 1 / 2); the **unreachable-runtime → exit 1** (not 3) deviation is intentional and must stay documented in `tools/cli/readme.md` — `portier doctor` always emits a report, so connection failure is a `runtime.unreachable` error check, not the usual `exitWithError` 3. The CLI stays a **pure API client** (reuse existing endpoints; no new API/DTO/contract surface for the live doctor baseline). Every new live doctor check needs **meaningful black-box tests** (httptest-driven). No background monitoring, no auto-fix/repair command, no server/service doctor endpoint, no UI doctor panel in this slice.

**Durable rule (v1.9 Slice 1) — doctor model & offline config doctor:** Doctor **check codes** (e.g. `config.valid`, `config.lan_exposure`) are **stable, operator-facing identifiers** — a CLI/tool contract; do not rename them casually, and add a test asserting any new code. A doctor result is the small fixed model `DoctorCheckResult {code, severity(info|warning|error), title, message, details?}` aggregated into `DoctorReport {checks[], summary}` — keep it small; do **not** grow it into a diagnostics/plugin/check-pack framework. Doctor output must be **deterministic** (stable check order — file order for per-rule advisories; stable JSON shape) and **symbol-free ASCII** to match the CLI style (`[INFO]`/`[WARN]`/`[ERROR]`). The **offline config doctor must not require or contact a live runtime** and must never modify the file; reuse the existing local config helpers (`parseLocalConfig`/`validateLocalConfig`) rather than forking validation, and keep duplicate-binding detection partitioned from field errors via the shared `duplicateBindingErrPrefix`. Warnings (LAN exposure, privileged port) are derived **only from the file contents** — they must **not imply runtime probing or target reachability**, and warnings alone exit `0` (no `--strict` yet). Exit codes follow the v1.7 policy: `0` no error-severity checks, `1` ≥1 error-severity check, `2` missing/invalid local argument. The CLI stays a **pure API client** (no shared/server/service imports; mirror shared thresholds like privileged `< 1024` locally, document any deferred duplication such as the full common-port table). Every new doctor check needs **meaningful tests** (prefer black-box command tests; add white-box only for model/helper branches). No server/service API doctor endpoint, no UI doctor panel, no live runtime doctor, and no config mutation in this slice.

Coverage policy: require 100% meaningful coverage for all newly added or materially changed files in v1.5 and v1.6. Existing baselines ratcheted incrementally. Do not block unrelated work on legacy uncovered areas. Do not lower gates without explicit rationale.

**Durable rule — coverage-gate tightening (opportunistic ratchet):** Coverage gates should be tightened **opportunistically**. When a slice raises **meaningful, stable** coverage and a component is **safely above** its current minimum gate, raise **that component's** gate **modestly** in the **same slice**. Concretely: **never lower a gate**; prefer **small increments** (e.g. 95% → 96%, not large jumps); raise **only the touched component's** gate, not every gate at once; only raise when the component remains **safely above the new threshold after validation** (re-run the relevant `validate:coverage:*` and confirm PASS); and do **not** raise based on a **brittle, platform-specific, or one-off** spike — if the remaining uncovered lines are legitimate **defensive/platform/error** paths whose coverage varies by OS or run, leaving the new gate with little headroom, keep the gate where it is and document why. When you change a gate, update **all** consistency surfaces in the same change: the gate in `scripts/validate-coverage.js` (with a dated comment), the coverage-baseline/gate prose in `CLAUDE.md`/`AGENTS.md`, the slice's checklist entry, and the changelog. **Report**: previous gate, new gate, actual measured coverage, and why the increase is safe — and **if a gate could have been raised but was not, explain why** (e.g. brittle defensive branches, cross-platform variance). The numeric gate is only the **floor**; the project goal stays **meaningful coverage** of important behavior, public contracts, error paths, rollback paths, and architectural guardrails — these must be **tested or explicitly documented as structurally unreachable**, regardless of the headline percentage.

E2E install (one-time): `npm run test:e2e:install`

Do not add `test:e2e` to `npm run test` or `npm run check`. E2E is a separate step.

E2E spec files:
- `tests/e2e/portier.spec.ts` — app load, CRUD, start/stop, merge import, form validation, diagnose
- `tests/e2e/settings.spec.ts` — replace-mode import (v1-mixed), invalid-JSON rejection, export shape, runtime info, plan preview, plan apply
- `tests/e2e/connections.spec.ts` — Live Connections view: title/tabs, empty states, tab switching, filters, auto-refresh, footer counts, rule filter
- `tests/e2e/activity.spec.ts` — activity log view opens and shows events
- `tests/e2e/apidocs.spec.ts` — endpoint list renders, GET /api/connections listed
- `tests/e2e/dashboard.spec.ts` — dashboard stat cards render
- `tests/e2e/mobile.spec.ts` — mobile hamburger / sidebar toggle
- `tests/e2e/tcp.spec.ts` — TCP real forwarding
- `tests/e2e/udp.spec.ts` — UDP one-way, last-client, multi-client, activity assertions

Settings E2E intentionally does not run the full fixture matrix. `validate:config` owns exhaustive TS/Go parity testing.

**Additional validation suites (run explicitly — slower/platform-sensitive, not part of `npm run check`):**

```powershell
npm run validate:config            # fixture-based rules.json compatibility validation
npm run validate:contract          # API contract parity: TypeScript server + Go service if available
npm run validate:binary            # runtime binary behavior: build:runtime then 5 behavioral tests
npm run validate:runtime:behavior  # alias for validate:binary (fits validate:runtime:* namespace)
npm run validate:scripts           # installer script static analysis + dry-run on current platform
```

- `validate:config`: loads fixtures from `tests/fixtures/config/`; checks load, import, export, rejection, duplicate bindings, UDP defaults; TypeScript always checked, Go checked when binary present; `--skip-go` to force skip. Uses temp dirs and free ports; never reads real `rules.json`.
- `validate:contract`: skips Go parity clearly if binary not present; `--skip-go` to force skip. Verifies status codes, response shapes, field names, error shapes, advisory/plan content parity (Arch-A: canonical message text per runtime plus a cross-runtime `compareParity` field-by-field diff), **and** (since Arch-D) CLI DTO parity — captures live runtime JSON and strictly decodes it into the CLI DTOs via `tools/cli`'s `TestCLIDTOContractParity` (skips if `go` toolchain absent). Uses the binary at `build/portier/service[.exe]` if present (else `service/build/portier-service[.exe]`); rebuild it after changing Go advisory/plan code or the guard tests stale output.
- `validate:binary`: runs `build:runtime` first; use `--no-build` to reuse existing `build/portier/`.
- `validate:scripts`: always runs static analysis; dynamic dry-run only on current platform.

Do not add these to `npm run test` or `npm run check`.

Go test binary path (Windows Firewall stability): both `test:service` (`node scripts/coverage-service.js --test-only`) and `coverage:service` (`node scripts/coverage-service.js`) are served by the single `scripts/coverage-service.js` — it **compiles** the Go test binaries to a stable, gitignored path (`build/tests/<pkg>.test.exe`) with `go test -c` and then **runs them from there** (`--test-only` skips coverage instrumentation/merge). Some service tests genuinely bind `0.0.0.0` (e.g. `TestDiagnoseLANExposureWarns`, which exercises the real diagnose listen-bind on a `0.0.0.0` rule), which triggers a Windows Firewall prompt; the firewall rule is keyed by executable path, so a fixed path lets one "allow" decision persist instead of re-prompting every run. **The old `go test -o build/tests/ ./...` form did NOT achieve this and was removed** — `-o` only *copies* the linked binary afterward; `go test` still ran the socket-binding binary from a fresh `…\Temp\go-build<random>\b001\<pkg>.test.exe` path every invocation, so every run re-prompted and accumulated another firewall rule. `go test -c` compiles without running, so executing the fixed-path binary ourselves is what makes the path stable. Do not regress `test:service`/`coverage:service` back to a plain `go test [-o] ./...` run, and do not change the diagnose test to loopback (the `0.0.0.0` bind is faithful product behavior, not a test artifact). Because `covdata` only sees packages linked into a test binary, `coverage:service` re-adds 0%-coverage blocks for test-less, un-imported packages (`sources/logger`, the `sources` main package, `sources/platform`) so the cross-package denominator — and the calibrated 90% gate — stays comparable to the old single-profile run.

Naming convention:
- `npm run test` = unit/integration test runner (Vitest + Go test)
- `npm run test:e2e` = Playwright browser E2E tests
- `npm run validate:config` = fixture-based rules.json compatibility validation
- `npm run validate:contract` = TS/Go API parity validation
- `npm run validate:binary` / `validate:runtime:behavior` = packaged service binary behavior validation
- `npm run validate:scripts` = installer/service script static + dry-run validation

**Protocol coverage (automated E2E — do not revert to manual QA):**
- TCP real forwarding: `tests/e2e/tcp.spec.ts`
- UDP one-way, bidirectional-last-client, bidirectional-multi-client: `tests/e2e/udp.spec.ts`

**Remaining manual QA:** Firewall and OS permission behavior only.

**OS service install validation is automated — run explicitly before release:**

```powershell
npm run validate:service:windows:user     # Windows scheduled task (no admin)
npm run validate:service:windows:machine  # Windows Service (admin)
npm run validate:service:current          # current platform
```

```bash
npm run validate:service:macos   # macOS LaunchAgent (no sudo)
npm run validate:service:linux   # Linux systemd (sudo)
```

These use test-specific names and temp dirs. Never touch production installs.
Do not add these to `npm run check`. Run them explicitly when releasing.

**Package build is automated — do not treat it as manual QA:**

```powershell
npm run validate:runtime           # validate existing build/portier/ layout
npm run validate:runtime:build     # build then validate
npm run validate:runtime:smoke     # build, validate, and run smoke test (preferred)
```

**macOS release archive — run explicitly when changing macOS scripts:**

```bash
npm run build:release:portable   # build:runtime then portable tar.gz (on macOS)
```

Output: `build/releases/macos/portier-portable-macos-<version>.tar.gz`. Requires `bash` and `tar`. Do not add to `npm run check` or any automated validation chain.

**Linux release archive — run explicitly when changing Linux scripts:**

```bash
npm run build:release:portable   # build:runtime then portable tar.gz (on Linux)
```

Output: `build/releases/linux/portier-<version>-linux.tar.gz`. Requires `bash` and `tar`. Do not add to `npm run check` or any automated validation chain.

**Windows release artifacts — run explicitly when changing release files:**

```powershell
npm run build:release:current    # portable zip + Inno Setup installer (installer non-fatal if absent)
npm run build:release:portable   # portable zip only
```

Output: `build/releases/windows/portier-<version>-windows-portable.zip` and `Portier-Setup-<version>.exe`. If Inno Setup is unavailable, the portable zip is still produced — report the missing installer clearly. Do not add to `npm run check` or any automated validation chain.

If a task touches packaging, run `npm run validate:runtime:smoke` when possible. The script:
- Builds `build/portier/` via `build:runtime` on the current platform
- Validates the layout (`service`/`service.exe`, `server.js`, `web/`, `readme.txt`)
- Smoke-tests the packaged binary on a free port without requiring admin/root

The platform-specific scripts still output to their own dirs when called directly:

```powershell
npm run build:runtime:windows     # Windows: produces build/windows/
npm run build:runtime:macos       # macOS/cross-compile: produces build/macos/
npm run build:runtime:linux       # Linux/cross-compile: produces build/linux/
```

If packaging cannot run because prerequisites are unavailable (e.g., Go is not installed), document that limitation clearly.

If a task touches formatting, run `npm run format` if available. Otherwise use `npm run lint` or the Prettier commands defined in `package.json`.

## UI Cleanup Guidance

- Keep the interface simple and practical.
- Prioritize clarity over visual flair.
- Make rule status obvious: running, stopped, error.
- Make protocol obvious: TCP or UDP.
- Make listen endpoint and target endpoint readable.
- Show LAN exposure warning for `0.0.0.0`.
- Show common port warnings inline.
- Keep add/edit rule flows compact.
- Do not hide dangerous states behind subtle styling.
- Avoid complex UI frameworks unless already present.
- Prefer accessible HTML controls and clear labels.

## Packaged Runtime Layout

Production/install layout for all platforms:

```text
<install-dir>/
  portier          (or portier.exe on Windows)   — CLI
  service          (or service.exe on Windows)   — background service
  server.js        (Node fallback — requires Node.js)
  web/
    index.html
    assets/
  readme.txt
```

- `portier` / `portier.exe` = CLI binary (talks to management API; does not start the service)
- `service` / `service.exe` = native Go runtime; default static dir is `web`
- `server.js` = bundled Node/TypeScript fallback
- `web/` = built React client UI (external; not baked into binary)
- `rules.json` = always external; never packaged

Dev build output (repo-internal, not distributed): `service/build/portier-service`, `server/build/`, `client/build/`, `tools/cli/build/portier-cli`.

Packaging scripts:
- `build:runtime` → `build/portier/` (cross-platform, primary generic output; builds CLI + service)
- `build:runtime:windows` → `build/windows/`, `build:runtime:macos` → `build/macos/`, `build:runtime:linux` → `build/linux/`
- `build:clean` removes the top-level outputs (`build/portier/`, all platform package output dirs, `build/releases/`, `build/tests/`) **and** the per-workspace dev build dirs (`client/build/`, `server/build/`, `shared/build/`, `service/build/`, `tools/cli/build/`)

Validation scripts:
- `validate:runtime` → validates `build/portier/` layout
- `validate:runtime:build` → builds then validates
- `validate:runtime:smoke` → builds, validates, and smoke-tests the packaged binary
- `validate:service:windows:user` → Windows scheduled task install/start/stop/uninstall (no admin)
- `validate:service:windows:machine` → Windows Service install/start/stop/uninstall (admin required)
- `validate:service:macos` → macOS LaunchAgent install/start/stop/uninstall (no sudo)
- `validate:service:linux` → Linux systemd install/start/stop/uninstall (root/sudo)
- `validate:service:current` → runs the appropriate platform script

## Remaining Work

Both runtimes are feature-complete. Package build correctness, OS service install/uninstall flows, and release artifact generation are automated. Remaining manual work is firewall and OS permission behavior. See `docs/checklist.md`.

v1.1 is complete: distribution, installers, release artifacts, service and package validation, platform polish. Tagged 1.1.0. See `docs/installer.md` for scope and slice history.

v1.2 is complete: runtime info endpoint, rule diagnostics API and UI, Activity Log polish, safer networking UX, settings/config polish, and diagnostics export. Tagged 1.2.0. See `docs/roadmap.md` for goals, slices, and non-goals.

v1.3 is complete: Go CLI under `tools/cli/`. All 8 slices done: `tools/cli/` module, HTTP API client (`ConnectionError`/`APIError`), `--url`/`--host`/`--port`/`PORTIER_URL` connection options, `--json` flag, `runtime`/`list`/`status`/`activity` commands (activity supports `--limit`/`--rule`/`--type`/`--severity`), `start`/`stop`/`diagnose` lifecycle and diagnostics commands (accept exact rule ID or unique name; duplicate names → exit 2 with ID disambiguation), `config validate`/`config export`/`config import` commands (local validation before API, replace requires `--yes`), `diagnostics export --out <file>` (builds JSON support bundle; `--run-diagnostics`; `--activity-limit` 1–500; partial-failure tolerant with `errors[]`), output helpers (`FormatBool`/`FormatBytes`/`FormatTimestamp`/`PrintTable`), safe rule resolver (`ResolveRule`), `ExportConfig`/`ImportConfig`/`BaseURL` API client additions, 153+ CLI tests, `build:cli`/`test:cli`/`validate:cli` npm scripts; CLI binary (`portier`/`portier.exe`) now built into `build/portier/` by all platform build scripts and included in release artifacts; Windows installer includes `portier.exe`; no PATH integration in v1.3; coverage gate enforces 92% threshold (92.7% actual after post-v1.3 ratchet). Tagged 1.3.0. The CLI talks to the management API; it does not replace the web UI or any runtime. See `tools/cli/readme.md` and `docs/roadmap.md`.

v1.4 is complete: Live Connection Inspector — read-only TCP connection and UDP session tracking in both runtimes, exposed via `GET /api/connections`, with a dedicated Live Connections view in the web UI. Coverage hardened across both runtimes before the feature was built. Shared live-connection types in `@portier/shared`, contract validation updated to 116/116, API Docs updated. Tagged 1.4.0. See `docs/roadmap.md` and `docs/changelog.md`.

v1.5 is complete: Declarative Config & Drift Control — plan/diff/apply workflows for comparing desired config files with the running configuration, previewing changes, and applying them safely from the CLI or UI. All 8 slices done: shared plan/diff/apply types in `@portier/shared` (`shared/sources/plan.ts`); pure plan engine in TypeScript server (`server/sources/config-plan.ts`) with id-first matching, identity key fallback, 8 material field diff, destructive flag, `REMOVE_EXISTING`/`LAN_EXPOSURE` warnings; `POST /api/config/plan` endpoint live on TypeScript server (Slice 2) and Go service (Slice 3, `service/sources/configplan/`); `POST /api/config/apply` endpoint live on TypeScript server and Go service (Slice 5) — plan errors → ok:false; dryRun → ok:true no mutation; destructive without yes → 400; drift → replace import with ID injection; `validate:contract` 156/156 (all plan + apply assertions against both runtimes); API Docs updated (no planned badges); 65 TS engine unit tests + 11 TS API integration tests + 49 Go engine unit tests + 10 Go plan API tests + 11 Go apply API tests; `portier config plan <file>`, `portier config diff <file>`, `portier config apply <file>` CLI commands (Slices 4–5) with structured output, field-level change detail, `--fail-on-drift` (exit 4), `--show-unchanged`, `--yes`, `--dry-run`, `--backup-out`, `--json`; 230+ CLI tests; CLI coverage 93.2% (gate 93%); Settings UI Plan & Apply section (Slice 6) — `planHelpers.ts` (4 helpers, 17 tests), `planConfig`/`applyConfig` API helpers with tests, Plan & Apply Config section in SettingsView with file picker, plan preview (summary counts/errors/warnings/operation list/destructive confirmation checkbox), apply with `yes:true`, `ok:false` error path, form-clear on success; 23 new SettingsView unit tests; E2E test in `settings.spec.ts`; client branch coverage 90.1% (gate 90%); all 5 coverage gates ratcheted to v1.5.0 values (Slice 7); full validation suite passed, version bumped to 1.5.0, changelog finalized (Slice 8). Tagged 1.5.0. See `docs/roadmap.md` and `docs/changelog.md`.

v1.6 is complete: Architecture, Quality & Maintainability Audit — a coverage-ratchet & quality-hardening pre-phase (all five coverage gates raised, meaningful edge-case tests) followed by a dedicated audit of architecture boundaries, runtime parity, forwarding correctness, API contract, CLI/UI/test quality, security/safety, packaging, and docs. Outcomes landed as behavior-preserving refactors, contract-parity guards, and durable rules — no product feature changes; `validate:contract` 185/185. Tagged 1.6.0. See `docs/roadmap.md` and `docs/changelog.md`.

v1.7 is complete: Cleanup & Maintainability — internal structure/refactor slices with no behavior change: validate-contract child-cleanup guard, Go `forwarders.Forwarder` interface + StartRule dedupe, TS UDP emit facade, diagnose phase-helper split (both runtimes), CLI config loader/mapper + exit-code policy, SettingsView decomposition, and a glossary-aligned UI wording pass. `validate:contract` 185/185; coverage gates unchanged. Tagged 1.7.0. See `docs/roadmap.md` and `docs/changelog.md`.

v1.8 is complete: Operator Power Tools — optional rule `group` metadata end to end (UI editing/filtering, group start/stop API, CLI `group` commands, UI group actions), derived rule `health` (status API + UI + CLI), a client-side duplicate-rule action, config-preview clarity, and a per-row action menu + Health column. Public-contract additions are additive only (the `group` field, `POST /api/forwards/groups/:group/{start,stop}` + `GroupActionResponse`, the required `ForwardStatus.health`); `validate:contract` → 234/234. Tagged 1.8.0. See `docs/roadmap.md` and `docs/changelog.md`.

v1.9 is complete: Doctor & Config Toolkit — operator diagnostic tooling built entirely in the Go CLI as a pure API client with no runtime/API/contract change: offline `portier config doctor` and live `portier doctor` (shared deterministic result model + 20 stable check codes), `portier explain`, `--strict`/`--out`/`--explain`, `portier support-bundle`, structured config-doctor `details` + a machine-readable config summary, and an offline AI handoff prompt; plus a docs cleanup and a release-readiness audit. `validate:contract` stays 234/234; all five coverage gates hold (CLI ≈ 97.6%). Tagged 1.9.0. See `docs/roadmap.md` and `docs/changelog.md`.

Release artifact commands:
- `npm run build:release:current` — portable archive + installer for current platform
- `npm run build:release:portable` — portable archive only
- `npm run validate:release:portable` — validate portable archive layout and contents
- `npm run validate:release:current` — validate portable + installer artifacts

Do not add release artifact commands to `npm run check`. They are explicit release steps.

## Review Checklist

- TypeScript types are strict and useful.
- TCP sockets clean up on close/error.
- UDP behavior is documented, especially bidirectional-last-client limitations.
- Duplicate `protocol + listenHost + listenPort` bindings are rejected.
- Management API remains localhost by default.
- Port advisory rules live in `shared/sources`.
- Tests cover validation and lifecycle behavior where practical.
- `README.md`, `docs/architecture.md`, and `docs/checklist.md` are updated for user-visible behavior changes.
- When an API endpoint is added, removed, or changed: update both `docs/api-contract.md` (durable external contract) AND the client in-app API Docs view (`client/sources/features/apidocs/ApiDocsView.tsx`) (user-facing in-app reference). Update `ApiDocsView.test.tsx` for new endpoints. Do not mark an API slice complete until both documentation surfaces and their tests are updated.

## Claude Response Style

- Summarize changed files.
- Mention validation commands run.
- Mention anything not run and why.
- Call out risks and follow-up tasks.
- Do not claim success unless validation passed or limitations are stated.

## Claude Code Hooks And Settings

Claude Code project settings live in `.claude/settings.json`. Hook event names and schemas can vary by Claude Code version, so treat the hook wiring as conservative project guidance. If a local Claude Code install rejects the hook section, keep the scripts and adjust only the event names/schema for that installed version.
