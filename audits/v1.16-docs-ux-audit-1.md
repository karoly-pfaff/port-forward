# v1.16 Documentation, Glossary & Operator-UX Audit (1)

**Audit:** v1.16 Audit Set #9 — Documentation, Glossary, and Operator UX
**Date:** 2026-06-17
**Baseline:** v1.15.0 shipped (NestJS single TypeScript runtime; Go preferred packaged runtime; chi router; `app.App` container).
**Previous audits:** #1 Contract Parity — PASS; #2 Architecture & Module Boundary — PASS; #3 Resilience & Durability — PASS WITH NOTES; #4 Security & Local-Safety — PASS WITH NOTES; #5 Observability & Replay — PASS WITH NOTES; #6 Automation/Policy/Workflow — PASS WITH NOTES; #7 Testing & Coverage — PASS WITH NOTES; #8 Complexity & Maintainability — PASS WITH NOTES.
**Type:** Audit + one tiny no-behavior docs addition (S-3 config-export topology note). No code/contract/schema/router change.
**Verdict:** ✅ **PASS WITH NOTES** (docs accurately describe the post-migration reality; minor docs-completeness/navigation follow-ups).

---

## 1. Executive Summary

Portier's documentation accurately describes the **post-v1.14/v1.15 product reality**, and the operator-facing terminology, safety warnings, and CLI/replay docs are consistent and current. The repo's docs were recently **restructured by the maintainer** (a `CLAUDE.md`/`AGENTS.md` trim to compact 140/196-line guides; the OpenAPI synced copy moved `docs/api/openapi.json` → `docs/openapi.json`; new `docs/readme.md` index + `docs/agentic.md`; the apidoc scripts renamed `generate:apidoc`→`apidoc:generate` / `copy-release.ts`→`release.ts`), and this audit confirms the **restructure is internally consistent**: the OpenAPI wiring matches the new paths, and there are **no stale `docs/api/openapi.json` or `generate:apidoc` references in any product doc**.

- **Product reality:** all current-guidance docs describe NestJS as the single TS runtime, Go as the preferred packaged runtime, chi as the Go router, `app.App` as the container, `server/build/api/openapi.json` as canonical (with `docs/openapi.json` the synced copy), `validate:contract` as TS↔Go parity, and the Go-only `/api/health` / Nest-only `/health` / 404-not-405 contracts. No stale Express/legacy current-guidance (the two `service/readme.md` mentions are accurate "serveLegacyAPI was retired" statements).
- **Terminology/glossary:** `docs/glossary.md` (137 lines) is comprehensive and current; no conflicting terms; no deferred feature (profile/temporary-start/recipe/saved-view/snapshot-restore) is named as implemented.
- **README/quick-start:** accurate; local-first posture + LAN Exposure Warning present; Go-preferred + NestJS-fallback clear; CLI usage accurate; versions = `v1.15` only.
- **API contract / CLI / replay docs:** special cases documented (422 `{errors,result}`, 204 no-body, apply `ok:false`/IMPORT_ERROR invariant, `/api/health`, unknown-route 404 envelope); CLI exit-code policy thoroughly documented; replay README is exemplary (offline/read-only/never-contacts-runtime/artifact-types/failure-modes).
- **Safety/privacy:** local-only, LAN-exposure, no-telemetry, diagnostics-sharing cautions, and AI-prompt review-before-sharing are all present.

**No blocking findings.** **Remediation performed:** one explicitly-permitted tiny addition — the **S-3 config-export topology-sensitivity note** in `docs/api-contract.md`. Non-blocking docs-only notes: **D-1** (api-contract.md could add the 404-not-405 / Nest-only-`/health` / generic-500-unmodelled contracts for completeness — currently only in `service/readme.md`/durable rules); **D-2** (the new `docs/readme.md` index omits `roadmap.md`, the CLI/replay readmes, and the service/server readmes); **A-3** (`policy.Warning` reserved/unused not noted in the CLI readme); **A-2** (deferred-automation note is optional — docs are correct by omission). No `coverage-baseline.md` exists (gates are canonical in `scripts/validate-coverage.js`). **The `docs/readme.md` "reviewed public reports stay in `audits/`" line is NOT a finding — per the maintainer, `audits/` stays gitignored during v1.16 and curated audit reports will be published at the end of v1.16; the wording is the intended workflow, not drift.**

---

## 2. Scope

In scope (read-only audit + tiny S-3 fix): documentation inventory; post-migration product-reality alignment; terminology/glossary; README/quick-start; API contract docs + OpenAPI artifacts; CLI/replay docs + prompts; safety/privacy messaging; deferred/future-feature wording; placement of the consolidated v1.16 follow-up backlog; contributor guidance (`CLAUDE.md`/`AGENTS.md`); docs maintainability.

Out of scope (explicitly): broad README/docs rewrite; rewriting historical changelog/checklist slice logs; mass-editing `CLAUDE.md`/`AGENTS.md`; code/contract/schema/router changes; lint-hardening; installer/build packaging (v1.18); migration/recovery (v1.17); release/tag. Contract/architecture/coverage substance is owned by audits #1/#2/#7.

---

## 3. Sources Inspected

`README.md`; `docs/{api-contract,architecture,glossary,checklist,changelog,roadmap,installer,readme,agentic}.md`; `docs/openapi.json` (synced copy) + `server/build/api/openapi.json` (canonical, via generator + Go inventory); `service/readme.md`; `server/readme.md`; `tools/cli/readme.md`; `tools/replay/README.md`; `CLAUDE.md`; `AGENTS.md`; `prompts/{doctor,policy,workflow}.md`; `server/sources/openapi/{generate,openapi}.ts` (artifact paths); `service/sources/api/route_inventory_test.go` (canonical path); root `package.json` scripts. Cross-checked with the prior v1.16 audits #1–#8.

---

## 4. Documentation Inventory

| Category | Docs | Notes |
|---|---|---|
| User/operator | `README.md`, `docs/glossary.md`, `docs/installer.md`, `docs/architecture.md` | quick-start, terms, install/layout, design. |
| Developer/contributor | `CLAUDE.md` (140 lines, trimmed), `AGENTS.md` (196 lines, trimmed), `docs/agentic.md` (new) | compact guides; agent-setup doc. |
| Release/checklist | `docs/changelog.md`, `docs/checklist.md`, `docs/roadmap.md` | version history; QA; Road-to-2.0 plan. |
| API docs | `docs/api-contract.md`; `docs/openapi.json` (tracked synced copy); `server/build/api/openapi.json` (canonical, gitignored build artifact) | contract prose + generated OpenAPI. |
| CLI/replay | `tools/cli/readme.md`, `tools/replay/README.md` | command reference + offline replay. |
| AI-handoff prompts | `prompts/{doctor,policy,workflow}.md` | local templates; review-before-sharing cautions. |
| Docs index | `docs/readme.md` (new) | navigation hub (see D-2). |
| Historical logs | `docs/changelog.md` + `docs/checklist.md` slice entries; `audits/` (gitignored) | preserved; period-accurate. |

---

## 5. Post-Migration Product-Reality Findings

**Consistent. PASS.**

- **Single TS runtime / no Express:** confirmed across README, `docs/architecture.md`, `docs/glossary.md`, `CLAUDE.md`/`AGENTS.md`, `service/readme.md`/`server/readme.md` — no current guidance says Express is a runtime (only `NestExpressApplication` platform-type usage in code, audit #2).
- **No legacy Go dispatch as current:** the only `serveLegacyAPI`/`legacy dispatch` mentions are `service/readme.md` lines 115/133 — both accurate "there is **no** legacy ordered dispatch (`serveLegacyAPI` was retired)" final-state statements.
- **Canonical OpenAPI ownership correct:** the generator writes the canonical `server/build/api/openapi.json` and syncs `docs/openapi.json` (`OPENAPI_DOCS_RELATIVE_PATH = "openapi.json"`); the Go inventory validates against `server/build/api/openapi.json` (`canonicalOpenAPIRelPath`). **No doc treats `docs/openapi.json` (or the old `docs/api/openapi.json`) as canonical.**
- **No stale renamed-script / moved-path references:** grep found **zero** `docs/api/openapi.json` or `generate:apidoc`/`copy:apidoc:release` references in product docs (the only hits are in my own prior gitignored v1.16 audit snapshots, which are period-accurate and left untouched). `CLAUDE.md`'s validation list uses the current names (`validate:openapi:go`, `validate:coverage`, `validate:contract`).
- **No installer/packaging-in-v1.16 claim** and no v1.14/v1.15 migration scaffolding described as current.

Findings: none.

---

## 6. Terminology / Glossary Findings

**Comprehensive and current. PASS.**

- `docs/glossary.md` covers Core / Runtime / Rule-State / Activity-Diagnostics / Policy-Workflow terms, all current: forward rule, group, exported/desired config, plan/diff/apply/import/export, drift, duplicate binding; runtime / **Go service = preferred packaged**, **TypeScript server = Node fallback**, CLI, web UI, **replay tool = offline**; enabled/running/status/health/lastError; activity type/severity, advisory vs warning, diagnose vs diagnostics export, doctor, support bundle, AI handoff prompt; policy/finding/template/review/**baseline = "compact snapshot of accepted policy findings"** (correctly distinct from a config snapshot), explanation, workflow.
- **No conflicting words** for the same concept; no internal-implementation term leaked into operator vocabulary; **no deferred-feature term** (profile/temporary-start/recipe/saved-view/snapshot-restore/watch) presented as implemented (grep: zero).

Findings: none.

---

## 7. README / Quick-Start Findings

**Accurate. PASS (S-3 addressed elsewhere).**

- Quick start, local-first posture, management bind default (`127.0.0.1:47831`), and the **LAN Exposure Warning** section are present and correct (audit #4).
- Go preferred packaged runtime + NestJS dev/fallback role are clear; CLI usage matches the implementation; the Settings/config export+import description is accurate (datetime-stamped JSON, merge/replace with backup prompt, local Download Diagnostics with no upload).
- No overpromise of installer/package work; version references are `v1.15` only (no stale older version in current guidance).
- **S-3 (config-export topology):** the README documents config export/import but had no explicit "exported config reveals local topology" note. **Addressed** by adding the sensitivity note to the canonical `docs/api-contract.md` config-export section (§15) — the most contained, canonical place — rather than editing the maintainer's freshly-reorganized README prose.

Findings: S-3 remediated.

---

## 8. API Contract Docs Findings

**Mostly complete; one optional completeness note. PASS.**

- **Canonical ownership** described correctly (NestJS reference implementation; Go implements the same contract; `validate:contract` guards parity — `docs/api-contract.md` intro).
- **Special cases documented:** per-endpoint 404s; `DELETE /api/activity` & `DELETE /api/forwards/:id` → `204` no body; config import `422 {errors, result}` (incl. duplicate-binding); config apply plan-errors → `200 ok:false` + the IMPORT_ERROR invariant ("apply never reports `ok:true` when import reports errors"); group-action `404` on no-match; unknown `/api/*` → `404 {errors:["API route was not found."]}`; the `GET /api/health` section.
- **D-1 (docs-only, optional):** three contracts documented in `service/readme.md` + the durable rules are **not** in `docs/api-contract.md`: (a) the **404-not-405** wrong-method behavior, (b) the explicit **Nest-only `/health` vs Go-only `/api/health`** distinction, (c) the **generic-500 intentionally-unmodelled** policy. Adding a short "error/method conventions" note to the canonical contract doc would improve completeness. Recorded as a follow-up (not done — keeps this slice's edit surface minimal).
- API examples versioning is coherent (`version:"1"` config; `v1.15` runtime example, audit #1).

Findings: D-1 (optional completeness follow-up).

---

## 9. CLI / Replay Docs Findings

**Accurate and thorough. PASS (one tiny note).**

- **CLI** (`tools/cli/readme.md`): commands match the implementation; the **exit-code policy is documented per command** (config plan/diff/apply/doctor/group/validate — `0/1/2/3/4` with the local-input-`2` vs output-`1` distinction); doctor warn-vs-block + `--strict`/`--explain`/`--out` documented; workflow/history compactness + local-only + bounded retention documented; policy/analyzer semantics documented.
- **A-3 (docs-only):** the CLI readme documents policy `info`/`warning`/`error` severities but does not note that **`policy.Warning` is currently reserved/unused** (all violations are `error`). A one-line note would aid contributors. Recorded as a follow-up.
- **Replay** (`tools/replay/README.md`): exemplary — explicitly **offline and read-only**; never runs a workflow, contacts the runtime, reads referenced config/policy/baseline/report files, mutates inputs, or uploads; lists the five commands, supported artifact types (run/plan reports, history exports, support-report bundles), and failure modes. The README also matches the README's replay section.
- Diagnostics/support-bundle include/exclude lists are accurate (audit #5).

Findings: A-3 (optional docs-only).

---

## 10. Safety / Privacy Messaging Findings

**Precise and present. PASS.**

- Local-only management API, the LAN Exposure Warning ("LAN-visible management UI/API is high risk"), unauthenticated-local-first posture, diagnostics/support-bundle sharing cautions, runtime-path-exposure-is-intentional-local-diagnostics (S-2), and the AI-prompt "review before sharing" guidance are all present (audit #4 confirmed; not over-alarming).
- **No telemetry / external calls** anywhere — confirmed by inspection and stated in `CLAUDE.md` safety rules ("Do not add telemetry, remote update, or download behavior").
- **S-3 (config-export topology):** now documented (§15).
- **S-4** (activity `details` redaction must be revisited if secrets are ever added) — a future-proofing principle; appropriately not over-stated in user docs.

Findings: S-3 remediated; S-2/S-4 are correct as-is.

---

## 11. Deferred / Future-Feature Wording Findings

**Correct by omission. PASS.**

- **No current doc claims** profiles, temporary/timed/TTL starts, recipes, persisted config snapshots/restore (beyond export/import), saved/persisted views, CLI watch mode, auth/remote management, or pre-v1.18 installers as implemented (grep: zero matches in README/glossary/architecture/CLI readme/service readme).
- The roadmap mentions these as **future** (v1.10/v1.11/v1.17/v1.18) — clearly framed as plans.
- **A-2 (optional):** since docs are correct by omission, an explicit "deferred automation" note is **optional**, not required — the glossary + roadmap already prevent confusion. Recorded as optional/low-value.

Findings: A-2 optional (no misleading wording found).

---

## 12. Known Follow-Up Documentation Findings (placement)

Using the audit #8 consolidated backlog, recommended documentation placement (avoid scattering):

| Follow-up | Where it belongs | Docs action |
|---|---|---|
| R-1 fatal autostart / corrupt-config | `docs/roadmap.md` (v1.17) + the fix slice | already roadmap-framed; document behavior **with** the fix |
| O-1 UDP `PruneExpired` registry growth | fix slice + `docs/changelog.md` when fixed | no docs needed now |
| S-1 Go generic-500 redaction | `docs/roadmap.md` (v1.19) | roadmap note when scheduling v1.19 |
| L-1 lint-hardening | `docs/roadmap.md` (v1.19) / dedicated slice | roadmap note |
| R-2/T-3 Go error-path tests | fix slice + changelog | no standalone doc |
| **S-3 config-export topology** | `docs/api-contract.md` | **done (§15)** |
| **A-3 reserved `policy.Warning`** | `tools/cli/readme.md` | tiny docs-only follow-up |
| **D-1 404-not-405 / `/health` / 500 policy** | `docs/api-contract.md` | optional completeness follow-up |
| A-2 deferred automation note | `docs/glossary.md` or roadmap | optional/low-value |
| M-2 ForwardRuleList decomposition | with L-1 / no docs | no docs |
| **Synthesis** of the whole backlog | `audits/v1.16-audit-synthesis-and-fix-plan.md` | the synthesis slice (#10 + plan) |

**Recommendation:** keep the backlog **centralized in the v1.16 synthesis-and-fix-plan**, with only the two tiny docs-only items (A-3, D-1) landing directly in their owning doc; do not scatter follow-up entries across many docs.

---

## 13. Contributor Guidance Findings

**Accurate post-trim. PASS (one observation).**

- The trimmed `CLAUDE.md` (140 lines) is accurate and current: Go = native production runtime, `server/` = TypeScript reference/fallback; correct naming/layout conventions; architecture guardrails (validate:contract, plan-engine helpers, validate-before-mutate + rollback, `domain.NewRuleID`, `forwarders.Forwarder`, `validate:openapi:go`); safety rules (no telemetry/remote/download; `0.0.0.0` = LAN exposure); **current** script names in the validation list; and "Coverage gates are canonical in `scripts/validate-coverage.js`; do not preserve old threshold history in this file" (explains the intentional removal of the coverage-baseline history).
- `AGENTS.md` (196 lines, the primary — `CLAUDE.md` says "Read `AGENTS.md` first") carries no stale Express/legacy wording (grep clean).
- **Observation (not a defect):** the old "record a durable architectural/quality lesson after each fix slice" convention is no longer an explicit `CLAUDE.md`/`AGENTS.md` rule — the trim **distributed slice history to `docs/roadmap.md`/`changelog.md`/`checklist.md`** (per the `CLAUDE.md` intro). This is the maintainer's deliberate choice; if the synthesis slice wants the "record the lesson" habit preserved, it should live in the roadmap/checklist process, not re-bloat `CLAUDE.md`.
- The two-file drift risk (audit #8) is **reduced** by the trim (both are now short and `CLAUDE.md` defers to `AGENTS.md`).
- No instruction pushes installer work into v1.16; file-naming conventions remain accurate.

Per the slice constraint, **`CLAUDE.md`/`AGENTS.md` were not edited** (the maintainer is actively curating them).

Findings: none blocking (one process observation).

---

## 14. Docs Maintainability Findings

- **D-2 (docs-only): `docs/readme.md` navigation gaps.** The new index lists architecture/api-contract/glossary/changelog/installer/platform-scripts/checklist/agentic but **omits** `docs/roadmap.md`, `tools/cli/readme.md`, `tools/replay/README.md`, and `service/readme.md`/`server/readme.md`. Adding those entries would complete "where to find what."
- **`audits/` publication wording — NOT a finding (clarified by the maintainer).** `docs/readme.md` says "reviewed public reports stay in `audits/` … raw/internal notes move under gitignored `audits/private/`." `audits/` is currently fully gitignored (`.gitignore:44`) **by design**: per the maintainer, raw audit notes stay local/gitignored throughout v1.16 and **curated audit reports will be published at the end of v1.16**. The wording describes the intended end-of-release workflow, not drift — no action needed.
- **No `docs/coverage-baseline.md`** exists — coverage gates + structural exceptions are canonical in `scripts/validate-coverage.js` (and per-slice in changelog). (My own audit #7 erroneously referenced a `coverage-baseline.md` as existing; it does not — a correction for the synthesis record, no product-doc impact.)
- **Renamed scripts:** the maintainer's `apidoc:generate`/`apidoc:release` rename and the `docs/api/openapi.json`→`docs/openapi.json` move are internally consistent; no broken references in product docs (§5).
- **Prompt text** (`prompts/*.md`) repeats a short "review before sharing" caution across three files — small, stable, low drift risk; no drift-control mechanism needed.
- Historical changelog/checklist slice logs are intact and clearly historical.

Findings: D-2 / D-2b (docs-only navigation/wording).

---

## 15. Remediation Performed

**One tiny, explicitly-permitted, no-behavior docs addition (S-3):** added a config-export **sensitivity note** to `docs/api-contract.md` (the canonical `GET /api/config/export` section):

> "Sensitivity: the exported config contains no secrets, but it reveals local network topology (listen/target hosts and ports, rule and group names). Treat an exported config as local diagnostic data and review it before sharing."

This closes the recurring S-3 follow-up (audits #4, #8) in the most contained, canonical location, without touching the maintainer's freshly-reorganized README prose. No code, contract, schema, router, script, or test was changed; the note is descriptive only.

**Not remediated here (recorded as follow-ups):** D-1 (api-contract 404-not-405/`/health`/500 completeness), A-3 (`policy.Warning` reserved note in CLI readme), D-2/D-2b (`docs/readme.md` navigation + `audits/` wording), A-2 (optional deferred-automation note) — each tiny but deferred to keep this slice's edit surface minimal and to let the maintainer's in-progress docs pass settle.

> **Note on the working tree:** `git status` also shows `docs/checklist.md`, `docs/readme.md`, and `docs/roadmap.md` as modified — those are the **maintainer's concurrent docs edits**, NOT this audit's. This audit changed **only** `docs/api-contract.md`.

---

## 16. Validation Results

| Command | Result |
|---|---|
| `npm run lint` | ✅ clean |
| `npm run typecheck` | ✅ clean |

- **Broader validation intentionally skipped** — this slice changed **only one Markdown file** (`docs/api-contract.md`, a descriptive note). No code, contract, schema, script, or OpenAPI artifact was touched, so `npm run test` / `test:cli` / `go test`/`vet`/`build` / `validate:contract` / `validate:openapi:go` would re-test unchanged behavior; all were green in audits #7/#8 the same day. `lint`/`typecheck` were run to confirm the repo is still clean (Markdown doesn't affect them, but they verify no accidental change).
- **`apidoc:generate` / `validate:openapi:go` not run** — no OpenAPI artifact or API doc *schema* was touched (the change is prose in `api-contract.md`, not the generated `openapi.json`); regeneration would be a no-op.
- **`validate:runtime:smoke`** not run — no runtime/package files touched.
- **Replay validation** not run — no replay/`@portier/shared` files touched.

---

## 17. Verdict

✅ **PASS WITH NOTES.**

Portier's documentation accurately reflects the post-v1.14/v1.15 product: NestJS single TS runtime, Go preferred packaged runtime, chi router, `app.App` container, canonical `server/build/api/openapi.json` with the `docs/openapi.json` synced copy, and the documented contract special cases. The maintainer's recent docs restructure (CLAUDE/AGENTS trim, OpenAPI-copy move, apidoc rename, new docs index) is internally consistent with no stale references in current guidance, the glossary is current, safety/privacy messaging is precise, and no deferred feature is falsely advertised. The recurring S-3 config-export topology note is now closed. No blockers.

**Blocking notes:** none.

**Non-blocking notes:**
1. **D-1** — add the 404-not-405 / Nest-only-`/health` / generic-500-unmodelled contracts to `docs/api-contract.md` (currently only in `service/readme.md`/durable rules).
2. **D-2** — `docs/readme.md` index omits roadmap + CLI/replay + service/server readmes. (The `audits/` publication wording is intentional — curated reports published at end of v1.16 — not a finding.)
3. **A-3** — note `policy.Warning` is reserved/unused in `tools/cli/readme.md`.
4. **A-2** — optional explicit "deferred automation" note (docs currently correct by omission).
5. **Process** — the "record a durable lesson after each fix slice" convention now lives in roadmap/changelog/checklist (post-trim), not `CLAUDE.md`; the synthesis slice should keep that habit in the process.

---

## 18. Recommended Next Audit

Proceed to **v1.16 Audit Set #10 — Release Readiness and Packaging Audit** (`audits/v1.16-release-readiness-audit-1.md`): build/release scripts, runtime smoke, release artifacts, version injection (incl. the hardcoded OpenAPI `version` example/`OPENAPI_DOC_VERSION` from audit #1), CLI/replay packaging, web-UI static serving, and platform scripts — then the **v1.16 synthesis-and-fix-plan** (`audits/v1.16-audit-synthesis-and-fix-plan.md`) to classify all #1–#10 follow-ups (MUST/SHOULD/NICE/DEFER/DO-NOT-DO) and schedule the MUST/SHOULD fixes. This docs audit's D-1/D-2/A-3/A-2 items feed that synthesis.
