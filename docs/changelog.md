# Changelog

All notable changes to Portier are documented here.

---

## [Unreleased] — v1.7 — Cleanup & Maintainability

### Changed

- **v1.7 Slice 3 — Go `Forwarder` interface + `StartRule` dedupe (Architecture-A)** — Routed the Go service's TCP and UDP rule startup through a small shared forwarder abstraction so the manager no longer carries duplicated, protocol-specific runtime-construction detail. **Internal Go refactor only — no runtime behavior, public API/DTO, config schema, CLI, activity-event, live-connection/session, rule-status/`lastError`, or duplicate-binding change; `validate:contract` stays exactly 185/185** (both runtimes; Go parity green against the freshly built binary). Added a 3-method `Forwarder` interface (`Start() error`, `Stop()`, `Status() domain.ForwardStatus`) and a `NewForwarder(rule, log, onEvent, tcpReg, udpReg) Forwarder` factory in the new `service/sources/forwarders/forwarder.go`; both `*TCPForwarder` and `*UDPForwarder` **already** satisfied the interface, so `tcp.go`/`udp.go` were not touched (the abstraction adds a shared shape, not new behavior). In `service/sources/manager/manager.go` the `runtimeState` struct's two concrete pointers (`tcp *forwarders.TCPForwarder`, `udp *forwarders.UDPForwarder`) collapse into a single `forwarder forwarders.Forwarder`, and the two near-identical ~18-line protocol blocks in `StartRule` collapse into one start/error/emit path built via the factory (the unknown-protocol case returns the same no-op synthetic status as before). `stopRuntime` and `statusForRule`'s running branch likewise simplify from `tcp != nil`/`udp != nil` pairs to a single `forwarder != nil` check — behaviorally identical because exactly one pointer was ever set per rule. The not-running synthetic status in `statusForRule` keeps its per-protocol shape (TCP `ActiveConnections`; UDP `PacketsIn`/`PacketsOut`/`ActiveUdpSessions`) since that is API-response shape, not lifecycle detail. Shutdown semantics are unchanged: both forwarders' `Stop()` remain `sync.Once`-guarded/bounded, `StopAll` is unchanged, and startup-failure cleanup (clear pointer, record `lastError`, emit `rule.error`) is preserved. Added 3 focused, socket-free factory tests (`service/sources/forwarders/forwarder_test.go`) asserting the new behavior boundary — TCP rule → forwarder with TCP status shape, UDP rule → UDP status shape, unrecognized protocol → `nil` — observing dispatch via status shape rather than concrete types. All existing manager/forwarder tests pass unchanged; `NewForwarder` is 100% covered; service coverage 90.3% → 90.1% (collapsing duplicated covered statements; **gate 90% still PASS**, no gate lowered). Validated: `go test ./service/...`, `npm run validate:runtime:smoke` (23/23 incl. smoke), `npm run validate:contract` (185/185), `lint`, `typecheck` — all green. `-race` not run (no cgo/gcc toolchain on this Windows host); the change does not alter any goroutine/socket lifecycle, so race surface is unchanged. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.7 item (Go Forwarder interface + StartRule dedupe).

- **v1.7 Slice 2 — CLI command-file naming cleanup (Naming-C)** — Renamed the Go CLI command files so each file name matches its actual responsibility, resolving the readability-audit finding that `tools/cli/sources/commands/config.go` was misleading — it contained **only** management-API URL/connection-target resolution (`ResolveURL`, `validateURL`, `DefaultHost`/`DefaultPort`/`DefaultURL`), not config plan/diff/apply/export/import behaviour (those live in the 1030-line `configcmd.go`). **File renames only — no Go code, package name (`commands`/`commands_test`), symbol, command, flag, output, exit-code, DTO, or API behaviour change; the CLI stays a pure API client.** All renames done with `git mv` (no content edits): `config.go` → `url.go` and `config_test.go` → `url_test.go` (URL/connection resolution); then, with the natural name freed, `configcmd.go` → `config.go`, `configcmd_test.go` → `config_test.go`, `configapplycmd_test.go` → `configapply_test.go`, `configplancmd_test.go` → `configplan_test.go`, `diagnosticscmd.go` → `diagnostics.go`, `diagnosticscmd_test.go` → `diagnostics_test.go`. The `cmd` suffix on `configcmd.go`/`diagnosticscmd.go` only ever existed as collision-avoidance (the natural `config.go` name was taken by the URL resolver; `diagnosticscmd` disambiguated from the separate `diagnose` command); dropping it leaves every command file a bare noun matching its command/responsibility (`config.go`, `diagnose.go`, `diagnostics.go`, `url.go`, `runtime.go`, `list.go`, `status.go`, `start.go`, `stop.go`, `activity.go`, `root.go`, `resolver.go`). Updated the two **current-state** references to the old `configcmd.go` filename (`docs/coverage-baseline.md` documented-uncovered-branch table and the `scripts/validate-coverage.js` header comment, both for `writePrettyJSON`); historical changelog/checklist/roadmap slice records are left as accurate point-in-time narrative. No `npm run check`, scenario, or coverage-gate impact. Validated: `go -C tools/cli test ./...` (all packages pass), `npm run validate:cli` (test + build), `npm run validate:contract` (**185/185**, Arch-D CLI DTO parity guard green — confirms package/symbols intact), `npm run validate:scripts` (48 passed), `lint`, `typecheck` — all green. The two pre-existing non-gofmt'd files (`helpers_internal_test.go`, `jsonerr_test.go`, from Coverage Slice C) were intentionally **not** reformatted — they are outside this slice's touched files and reformatting them would be out-of-scope churn. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-readability-naming-audit-1.md` (Naming-C), `docs/roadmap.md` v1.7 item 4.

- **v1.7 Slice 1 — `validate-contract.js` outer child-cleanup guard (Resilience-F)** — Hardened the contract validation runner so spawned runtime children are always terminated, including the failure paths the per-runtime `try/finally` does not cover (a thrown/unexpected error reaching `main().catch`, an early `process.exit`, a startup that never becomes ready, or an interrupt signal). This is the deferred outer-guard item that Fix Slice 4 explicitly left out of scope. **Script/runtime-hygiene only — no product code, API, contract behavior, scenario, or coverage-gate change; `validate:contract` stays exactly 185/185** (run with both runtimes; Go parity green). Added a module-level `activeChildren` set that every spawned server is registered into in `startServer` (and removed from on its `exit` event and in `cleanup`), plus a synchronous `killAllChildren()` belt-and-suspenders net wired to three places: `process.on("exit")` (drains any still-tracked child on normal or early exit), `SIGINT`/`SIGTERM` handlers (kill then exit 130 so an interrupted run does not orphan a child holding its listen port), and the outer `main().catch` (kill before exiting 1 on an unexpected throw). The primary, ordered per-runtime `cleanup(ts)`/`cleanup(go)` `try/finally` path is unchanged — the set merely makes those cleanups idempotent and adds coverage for the paths that bypass them. The file's intentional embedded `\x00` sort-key separators in the `normalize*` helpers are preserved. Validated with `validate:contract` (185/185), `validate:scripts` (48 passed), `lint`, and `typecheck` — all green. `npm run test` (Vitest + Go unit) does not exercise this build script, so it was not relied on for this slice. Source: `audits/v1.6-resilience-audit-1.md` (Resilience-F), deferred at Fix Slice 4.

---

## [1.6.0] — 2026-06-11 — Architecture, Quality & Maintainability Audit

### Goal

Architecture, quality, and maintainability hardening: a coverage ratchet across all five components followed by a ten-audit review and eight remediation fix slices. No new product features and no public API shape changes — the work raises test coverage on previously untested edge cases, fixes real runtime-parity and durability bugs found by the audits (duplicate-binding import parity, atomic config writes, persist-failure rollback), removes duplication, hardens contract validation, and adds a canonical terminology glossary. All five coverage gates were ratcheted to newly achieved stable values; none were lowered.

### Added

- **v1.6 Fix Slice 8 — Portier terminology glossary (Naming-A)** — Added a short, durable canonical-terminology document (`docs/glossary.md`) addressing the recurring terminology overloads the readability/naming audit flagged: "config" (rule persistence vs plan/apply engine vs export/import DTO vs desired-config input), "forward" vs "rule", "runtime"/"server"/"service"/`node`/`go`, "diagnose" vs "diagnostics export", "advisory" vs "warning", and TCP "live connection" vs UDP "session". The glossary has five sections (Core concepts, Runtime terminology, Rule lifecycle and state, Activity and diagnostics, Naming rules/exceptions) and explicitly documents the **frozen public-API exceptions** that must NOT be renamed (`/api/forwards`, `listenHost`/`targetHost`, `clientAddress`/`targetAddress`, runtime values `node`/`go`, CLI command names). **Docs-only — no product code, API, test, or coverage-gate change; no renames.** Added one-line pointers to the glossary from `CLAUDE.md` (Naming Conventions), `AGENTS.md` (new Terminology section), and `docs/api-contract.md` (intro); the glossary text lives in one place and is not duplicated. CLI command-file naming cleanup (Naming-C) and UI wording alignment (Naming-E) remain deferred follow-ups. Source: `audits/v1.6-readability-naming-audit-1.md`.

- **v1.6 Fix Slice 7 — Config apply `importConfig` result.errors invariant (Error-C·2 / Resilience-C)** — Hardened config apply so it can never report `ok:true` when the underlying import reports errors. Both apply handlers previously **discarded the `ImportResult`** (TS `api.ts` ignored the return value; Go `api.go` captured only `err` and discarded the result), so a future `importConfig`/`ImportConfig` that returned errors *without throwing* could surface a silent `ok:true`. **Audit of every `result.errors` path:** in **both runtimes** the replace-mode import-error paths are validation errors and duplicate listen bindings (both return errors-without-throw) plus persist failure (throws → 500); apply only ever calls import in **replace** mode, and the plan engine pre-blocks the two non-throwing paths before import — invalid desired rules via plan validation and duplicate bindings via `detectDuplicateKeys` → `summary.hasErrors` → `ok:false` (merge-only errors are unreachable; persist throws → 500). So **no `result.errors` path is currently reachable from apply** — but the discarded result left the invariant unenforced. **Defensive fix (small, parity-aligned, no valid-behavior change) in `server/sources/api.ts` and `service/sources/api/api.go`:** after `importConfig`, if `result.errors` is non-empty, respond `200 ok:false` with the errors surfaced through the existing `plan.errors` field (code `IMPORT_ERROR`) + `plan.summary.hasErrors: true` + zero applied counts — mirroring the existing plan-error response shape; **no new `ConfigApplyResponse` field added**. The Go construction is extracted into a unit-testable `applyImportErrorResponse` helper (the Go `Handler` holds a concrete `*manager.Manager` with no injection seam, so the handler branch is structurally unreachable in a unit test without the deferred manager-interface refactor; the construction is covered directly, and the TS twin is covered end-to-end). **Tests:** TS `api.test.ts` adds an explicit duplicate-desired-binding apply (pre-blocked → `ok:false`/`hasErrors`, no import, nothing created) and a defensive-branch test via an `ImportErrorManager` subclass whose `importConfig` returns errors without throwing (→ `ok:false`, `plan.errors` carries `IMPORT_ERROR`, zero counts); Go `api_test.go` adds the parity duplicate-binding pre-block test + `TestApplyImportErrorResponse` (covers the helper construction). `validate:contract` adds a duplicate-desired-binding apply parity scenario (**183 → 185**, both runtimes; stable). **CLI unaffected** — `applyExitCode` already maps `ok:false` → exit 1 (same as the existing plan-error path); fail-on-drift/backup-out/dry-run unchanged. No public API shape change, no valid-apply behavior change, no event/message change. server branch 94.0 → 94.1; service 90.3 → 90.2 (the unreachable 2-line handler guard, documented in `docs/coverage-baseline.md`); **all five gates unchanged**; full test 357 → 359; Go api package green. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-error-flow-audit-1.md`, `audits/v1.6-resilience-audit-1.md`.

- **v1.6 Fix Slice 6 — TS `ForwardManager.emitRuleEvent` helper (Dup-C2 / Naming-B / Error-B; Arch-C2 symmetry)** — Closed the last Arch-C2 asymmetry: the Go manager has had a single `emitRuleEvent` helper since Arch-C2, but the TypeScript `ForwardManager` still spelled the rule-scoped activity payload inline at 6 call sites. **Behavior-preserving product refactor — every emitted `ActivityEvent` payload is byte-identical; no API shape, event type/severity, message wording, ordering, or count change.** Added a private `emitRuleEvent(type, severity, rule, message)` to `server/sources/forward-manager.ts` (mirrors the Go `manager.emitRuleEvent` shape: populates `ruleId`/`ruleName`/`protocol` from the rule, keeps the `this.activity?.add` optional-chaining so a missing store is still a no-op, no id/timestamp handling — `ActivityStore` still owns those) and replaced the 6 inline rule-scoped blocks (`rule.created`/`rule.updated`/`rule.deleted`/`rule.started`/`rule.stopped`/`rule.error`) with calls to it. The **5 config-level events** (`config.exported`, `config.imported`, and the 3 `config.import.failed` paths) are intentionally left on direct `this.activity?.add` — they are not rule-scoped and carry their own `details` map (or no rule fields), exactly as the Go runtime keeps them on `emitActivity`. Added 3 focused full-payload regression tests in `server/sources/forward-manager.test.ts` (`emitRuleEvent` Go-parity describe) asserting exact `type`/`severity`/`ruleId`/`ruleName`/`protocol`/`message` for create/update/delete (no sockets), start/stop (a real forwarder via `startRuleStable`), and the start-failure `rule.error` path (exact fields + stable message prefix) — locking the helper output beyond the prior type-only checks; generated id/timestamp are not asserted. `forward-manager.ts` stays fully covered; server 98.9/93.8/100 → 98.9/**94.0**/100 (branch nudged by the consolidation + new tests); **all five gates unchanged** (not raised — refactor, +0.2% branch within Windows fluctuation). `validate:contract` stays **183/183** (the Slice 5 activity value-parity guard confirms no type/severity drift); full `npm run test` green (server 354 → 357). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-duplication-audit-1.md`, `audits/v1.6-readability-naming-audit-1.md`, `audits/v1.6-error-flow-audit-1.md`.

- **v1.6 Fix Slice 5 — Activity event-type/severity value parity guard (Error-A / Resilience-E / Dup-G)** — Closed the silent-drift gap on the activity taxonomy: the 17 event-type strings and 4 severities are duplicated across the TS union (`shared/sources/activity.ts`), the Go consts (`service/sources/activity/activity.go`), and the `/api/activity` runtime responses, but `validate:contract` previously asserted only `ActivityEvent` *shape*. **Contract/script hardening only — no product code, API shape, or coverage gate change.** Added canonical `EXPECTED_ACTIVITY_TYPES` (17) / `EXPECTED_ACTIVITY_SEVERITIES` (4) sets to `scripts/validate-contract.js` plus three layers of guard: (1) a once-run **source-of-truth parity check** (`runActivityValueSetParityChecks`) that reads the TS union and Go consts directly from source (independent of the Go binary) and asserts both declared sets equal the canonical set — a rename/typo/drift in *either* runtime's source now fails with a precise `extra`/`missing` diff (verified by a negative test: `rule.error→rule.failure` is caught); (2) per-runtime **runtime membership** assertions in `runActivityScenarios` — every emitted `type`/`severity` must be a member of the allowed set; (3) per-runtime **representative emitted-value** assertions — the contract flow's `rule.created`/`rule.updated`/`rule.started`/`rule.stopped` (with severities `success`/`info`/`success`/`info`) are observed, and the import scenarios' `config.imported`/`success` + `config.import.failed`/`error` (the error path) are observed. No existing activity assertion weakened or removed; no event names changed (no drift found). **`validate:contract` count 171 → 183** (intentional, documented: +4 source-set checks run once, +6 membership/representative ×2 runtimes, +2 config-event ×2 runtimes; stable on two consecutive runs). The embedded `\x00` sort-key separators are preserved. `docs/api-contract.md` now lists the `ActivityEventType`/`ActivitySeverity` value sets and the parity rule. Lint/typecheck/coverage/full-test all green; all five gates unchanged. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-error-flow-audit-1.md`, `audits/v1.6-resilience-audit-1.md`, `audits/v1.6-duplication-audit-1.md`.

- **v1.6 Fix Slice 4 — `validate-contract.js` scenario registry (Complexity-A / SOLID-A / Pattern-A / Dup-A / Naming-D)** — Decomposed the ~1090-line monolithic `runScenarios` in `scripts/validate-contract.js` into **12 named, focused scenario-group functions** driven by a small ordered registry. **Script-only refactor — no product code, API, contract behavior, or coverage gate change; `validate:contract` stays exactly 171/171** (run twice). Groups (registry order preserved verbatim from the monolith): `runRuntimeScenarios`, `runForwardsScenarios`, `runForwardLifecycleScenarios`, `runActivityScenarios`, `runConfigExportImportScenarios`, `runPortAdvisoryScenarios`, `runForwardDeleteScenarios`, `runConnectionsScenarios`, `runConfigPlanScenarios`, `runConfigApplyScenarios`, `runErrorEnvelopeScenarios`, `runDiagnoseScenarios`. Each takes a small `ctx` (`{ api, runtime, fixtureDir, state }`) destructuring only what it uses; cross-group rule ids (`tcpId`/`udpId`/`udpDefaultId`) now flow through `ctx.state` while group-local values (`exportedConfig`) stay local. `runScenarios` is now a slim wrapper that builds `api`/`ctx` and iterates `scenarioGroups` in order, then runs the CLI DTO fixture capture (Arch-D) last — unchanged. The readability audit's inline runtime-label→API-value special-case (`runtime === "TypeScript" && data.runtime === "node"` …) was extracted into a named `expectedRuntimeApiValue(runtimeLabel)` helper (Naming-D). Assertions were moved **verbatim** (the refactor was performed as a byte-level transform that preserves the file's intentional `\x00` sort-key separators in `normalizeAdvisories`/`normalizeWarnings`/`normalizeErrors`/`normalizeOperations`); no assertion weakened, no scenario removed, request order preserved, parity comparison (`compareParity`) and CLI DTO live-decode guard (`runCLIContractGuard`/`captureCLIFixtures`) untouched. Child-process startup/`cleanup()` behavior was **not** changed (the optional outer-guard hardening remains deferred as Resilience-F). Lint/typecheck clean; `validate:coverage` all five gates unchanged; full `npm run test` green. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-complexity-metrics-audit-1.md`, `audits/v1.6-solid-audit-1.md`, `audits/v1.6-design-pattern-audit-1.md`, `audits/v1.6-duplication-audit-1.md`, `audits/v1.6-readability-naming-audit-1.md`.

- **v1.6 Fix Slice 3 — Finish Test-A EADDRINUSE residual helper migration (test determinism)** — Closed the remaining allocate-close-rebind residuals flagged by `audits/v1.6-testing-audit-1.md` / Resilience-D / Error-E in the two named TS suites. **Tests / test-infrastructure only — no product code, API, contract, or gate change.** New helpers in `server/sources/test-helpers.ts`: `startRuleStable(manager, ruleId, freePort)` (mirrors the Go `service/sources/manager/portretry_test.go` helper — retries `manager.startRule` **only** on EADDRINUSE, **only** the bind, by rebinding the rule's `listenPort` via `updateRule`, bounded to 10 attempts; any other error rethrows immediately), and `startTcpServerOnFreePort()` / `bindUdpSocketOnFreePort()` (bind to port 0 and return the **live** listener/socket plus its actual bound port — no close-then-rebind TOCTOU window). Migrated `server/sources/forwarders/udp-forwarder.test.ts`: every forwarder listen bind now goes through a local `startUdpForwarder` wrapper over `startForwarderOnFreePort` (bounded EADDRINUSE retry, returns the bound port), every UDP target/echo socket uses `bindUdpSocketOnFreePort`, and several fixed-sleep session-count waits became `waitUntil` deadline polls. Migrated `server/sources/forward-manager.test.ts`: every manager `startRule` on an ephemeral port (the `startedRule` helper, the reorder test, the ActivityStore rule.started test, and the Test-D rollback tests for update-running and delete-running) now uses `startRuleStable`; TCP target servers use `startTcpServerOnFreePort`; the local allocate-then-bind `startTcpTarget` was removed. **Intentional-conflict tests deliberately bypass the helpers and still force a real EADDRINUSE:** the UDP "start() rejects when port is already bound" (live blocker socket), the manager "logs rule.error and re-throws when startRule fails" (live occupier), and the merge-import persist-failure rollback test (already race-tolerant — asserts the started forwarder is *not* running after rollback). No product-side bind retry was added — only test setup retries, only on EADDRINUSE, bounded, with the last error in the final failure message. The 58 migrated tests pass 3× consecutively; server stays 354 tests at 98.9/93.8/100 (no count or coverage change); `validate:contract` 171/171, `validate:config` 71/71, all five gates unchanged. **Remaining accepted residuals (documented, bounded):** the HTTP `/start` sites in `server/sources/api.test.ts` and `service/sources/api/api_test.go` (the bind happens behind the HTTP handler and each test asserts the start-response shape, so a robust fix needs an HTTP-level recreate-on-EADDRINUSE helper — a larger, lower-value follow-up), and the E2E `tests/e2e/helpers/port.ts` allocate-close helpers (E2E is fully serial — `workers:1`, `fullyParallel:false` — so no intra-process race is possible; only a rare cross-process race against the single dedicated server remains). Revisit trigger: an actually-observed HTTP `/start` or E2E EADDRINUSE flake. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-audit-synthesis-and-fix-plan.md`, `audits/v1.6-resilience-audit-1.md`.

- **v1.6 Fix Slice 2 — TypeScript `ConfigStore` atomic write parity with Go (durability fix)** — Closed the durability/parity divergence from `audits/v1.6-resilience-audit-1.md` (Resilience-A) and the error-flow persistence-durability risk. The TS `ConfigStore.save` was a bare `mkdir` + `writeFile`, so a crash / disk-full / interrupted write mid-`writeFile` could truncate `rules.json`; the next startup's load would then fail to parse it and the Node fallback runtime could refuse to start. The Go `config.Store.Save` was already crash-safe (temp file + `Sync` + atomic `Rename`). **Product fix in `server/sources/config-store.ts`:** `save` now mirrors Go — it writes to a **unique temp file in the same directory** as the target (`.portier-forwards-<pid>-<ts>-<rand>.tmp`), `fsync`s the contents via a `FileHandle` (`open` → `write` → `sync` → `close`), then **atomically renames** the temp file over `rules.json`. The previous file stays intact until the rename succeeds, so a failed save cannot corrupt it; the temp file is removed on any pre-rename failure (best-effort, so cleanup errors cannot mask the original error), and the original persistence error propagates to the caller so `ForwardManager`'s in-memory rollback (Test-D) still runs. On a write/sync failure the handle is closed best-effort (error ignored, parity with Go); a `close` error on the success path IS surfaced (also parity with Go). **`load` is unchanged** (still `node:fs/promises` directly, same ENOENT-→`[]` and invalid-JSON/invalid-rule behavior). Serialized output is byte-identical to before (`JSON.stringify(rules, null, 2)` + `\n`, matching Go's `MarshalIndent`). **Justified divergence from Go:** no remove-and-retry recovery branch after `rename` — Node's `fs.rename` replaces the destination atomically on the same filesystem on both POSIX (`rename(2)`) and Windows (libuv `MoveFileExW` with `MOVEFILE_REPLACE_EXISTING`), so Go's recovery branch (dead on Win/Linux) is unnecessary; a directory fsync is intentionally omitted (not portable on Windows — the same metadata-durability limit the Go runtime accepts). A minimal **test-only injection seam** (`ConfigStoreFileOps`/`ConfigFileHandle`, exported `defaultFileOps`, optional 2nd constructor arg defaulting to the real fs) was added — only `mkdir`/`open`(write/sync/close)/`rename`/`remove`, not a virtual filesystem — so failures can be forced without a real disk-full event; it is not exposed through any product/API path (`new ConfigStore(path)` is unchanged). 5 new unit tests in `server/sources/config-store.test.ts`: success writes pretty JSON + trailing newline and leaves **no temp file**; write-fails-before-rename keeps the existing `rules.json` byte-for-byte and still loads it (temp cleaned up); sync-failure propagates + cleans up; rename-failure keeps the target unchanged and removes the fully-written temp; first-save-fails creates no target file. `config-store.ts` coverage 100/100/100; server 98.9/93.6/100 → 98.9/**93.8**/100 (branch nudged up by the now-covered error paths); **all five gates unchanged** (not raised — durability slice, and the +0.2% branch is within the documented Windows ±1% fluctuation). `ForwardManager` rollback tests (Test-D) unchanged and still pass — the atomic store still throws on persist failure, so no manager change was needed. Verified with `vitest run config-store.test.ts` ×4 (filesystem-stable), `npm run test`, `validate:coverage`, `validate:config` (71/71), and `validate:contract` (171/171). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-resilience-audit-1.md`, `audits/v1.6-audit-synthesis-and-fix-plan.md`.

- **v1.6 Fix Slice 1 — TypeScript replace-import duplicate listen-binding parity (real parity bug fixed)** — Closed the one MUST-do correctness/parity divergence from `audits/v1.6-audit-synthesis-and-fix-plan.md` (Error-flow finding #1, Resilience-B). The Go service rejects duplicate listen bindings **within the imported set** for both import modes (`manager.ImportConfig` → `ensureNoDuplicateBindings(validated)`, run before the mode branch); the TypeScript `ForwardManager.importConfig` had **no equivalent guard** — replace mode had none at all, and merge mode only checked imported-vs-existing, not imported-vs-imported. So two desired rules with distinct IDs but the same `protocol + listenHost + listenPort` survived in TS (the rule map is keyed by ID), persisted, and only failed later when the second forwarder's bind was swallowed → a silent, Go-divergent invalid state. **Product fix (minimal, mirrors Go) in `server/sources/forward-manager.ts`:** a module-level `ensureNoDuplicateBindings(rules)` (mirroring the Go package-level function, including message wording — `a <proto> rule is already listening on <host>:<port> (rules "X" and "Y")`) is called in `importConfig` after validation and **before any mutation**, for both replace and merge. On a duplicate binding it returns `{ imported: 0, skipped: 0, errors: [msg] }`, emits one `config.import.failed` (severity `error`, no `details`), and performs **no** in-memory mutation, **no** persist, and **no** forwarder start/stop — matching Go. The HTTP layer already maps `result.errors` → **422** with `{ errors, result }` in both runtimes, so no API/handler change was needed. 3 new TS unit tests in `server/sources/forward-manager.test.ts` (replace rejects + no mutation/persist/success-event via `ControllableStore` + `ActivityStore`; merge rejects intra-set duplicates; distinct-binding replace still succeeds and persists). New `validate:contract` parity scenario asserts both modes × both runtimes return 422 with the `errors[]`/`result` envelope and a matching message classification (`validate:contract` 167 → **171**). **Config apply interaction (verified, no change needed):** `buildConfigPlan` already flags intra-desired duplicate bindings via `detectDuplicateKeys` → `summary.hasErrors`, and the apply handler returns `ok:false` on `hasErrors` before ever calling `importConfig`, so apply was already protected — this resolves the duplicate-binding half of the deferred Resilience-C "Unable to verify" question in the affirmative. No valid-import behavior change; merge imported-vs-existing behavior unchanged; the only new rejection is the intra-set duplicate that Go already rejected. All five coverage gates unchanged (server stays 98.9/93.6/100). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-audit-synthesis-and-fix-plan.md`.

- **v1.6 Testing Slice Test-E — Client/E2E meaningful coverage (32 → 34 E2E tests; brittle selectors cleaned)** — Addressed the Testing-Audit-1 E2E findings (thin/empty-state-only Live Connections coverage, no offline state, brittle selectors). **Tests only — no product code changed**; `validate:contract` stays 167/167 and client coverage is unchanged (95.2/90.2/80.3). New `tests/e2e/connections.spec.ts` workflows: **(I) populated TCP table** — creates+starts a real TCP rule forwarding to an in-process echo server, holds a forwarded client socket open across the browser fetch, and asserts a populated `role="table"` row (rule name "Live TCP Inspect", `127.0.0.1` endpoints, "Active" status), the empty state gone, and the TCP tab count non-zero; **(J) API-failure error banner** — Playwright `page.route` aborts `**/api/connections*` at the browser layer (the rest of the app still loads from the real server) and asserts the view's `role="alert"` banner appears with a non-empty message and the app stays usable (tabs still render). **Brittle selector cleanup:** removed `label.auto-refresh-toggle` (now reads state via the `input[aria-label="Auto-refresh"]` accessibility attribute and clicks the visible "Auto-refresh" label) and removed `.diag-panel-title` / `.diag-panel-body` from `portier.spec.ts` (now uses the accessible "Close diagnostics" button + the user-visible "Listen address" diagnostic check). Also fixed the `goToConnections` helper's fragile `Parameters<…>` type to a plain `Page`. Full E2E suite 34/34, run twice for stability (the populated-table test uses a real held socket + auto-refresh polling — no fixed sleeps, Playwright auto-waiting only). The populated-table test uses the **real runtime**; the offline test uses route interception (documented as the one deliberate exception in `docs/e2e-coverage.md`). No product bug found — the view already had a `role="alert"` error state and accessible hooks. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: E2E must cover critical user-visible workflows (not only empty states), prefer role/label selectors over CSS classes, and keep at least one Live Connections populated-state + one API-failure regression test. Source: `audits/v1.6-testing-audit-1.md`.

- **v1.6 Coverage Slice E — TypeScript server high-value error paths (95.4/92.2/100 → 98.9/93.6/100, gates 89/91/99 → 95/92/99)** — Raised TS server coverage with meaningful runtime error-path tests. **Tests only — no product code changed**; `validate:contract` stays 167/167. `server/sources/diagnose.test.ts` (85.9% → 98.0%): TCP listen-bind failure (occupied port → fail), target-connect success (local server) / connection-refused (unbound port) / DNS-resolution failure (`portier-nonexistent.invalid` → target-host fail + target-connect skip + summary fail), one-way udp-mode pass, and the 0.0.0.0 listen-host/lan-exposure and privileged-port/common-port advisory **warn** branches (UDP rules used so target-connect is skipped, isolating the advisory summary). `server/sources/forwarders/udp-forwarder.test.ts` (86.3% → 99.4%): the four UDP **send-callback error** branches — one-way target send, multi-client session send (on session reuse), last-client return send, multi-client return send — each emits `udp.packet.error` and records `lastError`. These are driven by a clean instance-level `.send` override on the specific socket (the same injection style as the existing `socket.emit("error")` tests — not `dgram` prototype monkeypatching), with a `waitUntil` deadline poll (no fixed sleeps). `server/sources/api.test.ts`: apply-with-drift persist failure → 500 via an inline failing `RuleStore` (parity with Go Coverage Slice D; covers the `configApply` catch → error-handler 500 mapping). Server coverage 95.4/92.2/100 → 98.9/93.6/100 (three consecutive `coverage:server` runs stable — the Slice-A ghost-entry dedup keeps the number deterministic); gates raised statements 89 → 95 and branches 91 → 92 (functions stays 99; cautious buffers of ~3.9% and ~1.6%). Structurally-hard branches documented in `docs/coverage-baseline.md` rather than chased: the 2 s diagnose bind/connect **timeout** branches (`tryTcpBind`/`tryUdpBind`/`tryTcpConnect`), the UDP `if (!this.listenSocket)` (target reply after stop) and `if (!this.lastClient)` (target reply before any client) race guards, the `tcp-forwarder.ts` optional-registry guard (by-design), and the api.ts platform-normalize branches (Windows-only env). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: server coverage prioritizes runtime error paths; clean instance-level socket injection is allowed, brittle monkeypatching is not. Source: `audits/v1.6-coverage-audit-1.md`, `audits/v1.6-testing-audit-1.md`.

- **v1.6 Coverage Slice D — Go service high-value error paths (88.5% → 90.3%, gate 88 → 90)** — Raised Go service coverage with meaningful error/correctness-path tests. **Tests only — no product code changed**; `validate:contract` stays 167/167. New `service/sources/api/errorpaths_test.go` (white-box `package api`) covers HTTP rejection paths previously untested: empty body and mid-stream body-read failure → 400 (`readBody`/`decodeRequest`/`updateForward`/`configApply`), malformed JSON → 400, `PATCH` of an unknown rule → 404, generic (non-typed) manager error → 500 (`writeManagerError` default + `createForward`), and config-apply `ImportConfig` failure → 500 — the persist failures are driven by a real manager built via `manager.NewWithStore` with a store path under a regular file (so `os.MkdirAll` fails cross-platform, no production seam, no mocking). Also covers `NewHandler` option defaults (nil manager / zero StartedAt / empty Version) and the `tryTCPBind`/`tryUDPBind` diagnostic helpers (success + occupied-port failure). New `service/sources/forwarders/udp_errorpaths_test.go` exercises the public `NewUDPForwarder` constructor and the `udpMode()` nil-default branch through the Test-A `startUDPForwarderOnFreePort` retry helper (no fixed sleeps). New `service/sources/static/static_test.go` covers `ServeClient` serving an existing asset vs the SPA index fallback, plus `HasClient`. New `service/sources/manager/errorpaths_test.go` covers `StartEnabled` skipping disabled rules (no socket binds) and `hasIDIn` regenerating a colliding ID on merge-import. New `service/sources/configplan/udpmode_test.go` covers `udpModeEqual`/`udpModeVal` for nil↔non-nil and non-nil↔non-nil mode changes. One `options` positional-argument rejection test (`parseCLI`). Service coverage 88.5% → 90.3% (two consecutive `coverage:service` runs both 90.3%; modified packages stable at `go test -count=3`); gate raised 88 → 90. Structurally-unreachable / seam-requiring branches are documented in `docs/coverage-baseline.md` rather than chased: crypto/rand fallbacks (`randomEventID`/`NewRuleID`/`generateConnectionID`), `config.Save` write/sync/rename injection (Go `os.Rename` replaces atomically, so the rename-recovery path is dead), and the UDP forwarder read-loop send-error branches (`emitPacketError`, multi-client/session write errors) which need a `net.Conn` injection seam not worth adding now. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: service coverage prioritizes correctness/error paths; do not add invasive production-only seams for unreachable socket internals. Source: `audits/v1.6-coverage-audit-1.md`.

- **v1.6 Coverage Slice C — CLI command edge-case coverage (93.2% → 97.7%, gate 93 → 95)** — Pushed CLI coverage past the audit's 95% target with meaningful failure-path and exit-code tests. **No CLI behavior change** — all additions are tests; the CLI remains a pure API client (no server/service imports, no new commands), and `validate:contract` stays 167/167. Added `tools/cli/sources/commands/jsonerr_test.go`: a shared `failingWriter` (every `Write` returns `os.ErrClosed`) drives the previously-untested `output.PrintJSON → "Error encoding JSON" → exit 1` branch in every JSON-emitting command (`runtime`, `list`, `status`, `start`, `stop`, `diagnose`, `activity`, `config export`/`validate`/`plan`/`diff`/`apply`/`import`, `diagnostics export`), asserting exit code 1 and a useful stderr message against a local `httptest` server. Added `tools/cli/sources/commands/helpers_internal_test.go` (white-box `package commands` tests): `formatChangeValue` across nil / integer-valued float / non-integer float / string / bool, and `opEndpoint` across Current-present / Desired-fallback / both-nil. Added `main_test.go` `TestRun_InvalidURL_AllDispatches` covering the repeated `ResolveURL`-error arm in `run()` (invalid `--url` → exit 2) for every subcommand. Per-function result: `run()`, `formatChangeValue`, `opEndpoint`, and most `Run*` commands reach 100%; CLI total 97.7% (cross-package, deterministic — two consecutive `coverage:cli` runs both report 97.7%). Gate raised 93 → 95 in `scripts/validate-coverage.js` (2.7% buffer retained). Structurally-unreachable branches are documented in `docs/coverage-baseline.md` rather than chased: `main()` os.Exit wrapper, `client.do` `http.NewRequest`/body-read errors, and `client.doWithBody` + `writePrettyJSON` `json.Marshal(Indent)` errors (impossible with the concrete CLI DTOs — adding fake unmarshalable types is forbidden). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: CLI coverage must include failure-path behavior and keep config command exit-code priority tested. Source: `audits/v1.6-coverage-audit-1.md`.

- **v1.6 Testing Slice Test-D — TypeScript ForwardManager persist-failure rollback parity (real bug fixed)** — Closed the HIGH runtime-parity gap from `audits/v1.6-testing-audit-1.md`. The Go manager rolls back every persist path so a failed `store.Save()` leaves no partial state (`TestCreateRulePersistFailureRollsBack`, `TestUpdateRulePersistFailureRollsBack`, `TestUpdateRulePersistFailureWithRunningRuleRestartsOriginal`, `TestDeleteRulePersistFailureRollsBack`, `TestReorderRulesPersistFailureRollsBack`); the TypeScript `ForwardManager` mutated its in-memory rule map *before* `persist()` with **no rollback**, so a persistence failure left the in-memory rules inconsistent with `rules.json` and divergent from Go. New rollback tests in `server/sources/forward-manager.test.ts` (a `ControllableStore` fake whose `save()` fails on demand and records persisted snapshots) **exposed this as a real bug** — all six initially failed. **Product fix (minimal, mirrors Go) in `server/sources/forward-manager.ts`:** `addRule` deletes the appended rule on persist failure; `updateRule` restores the original rule and restarts it if a running forwarder was stopped for a forwarding-field change; `deleteRule` restores the rule (and restarts it if it was running); `reorderRules` restores the prior order; `importConfig` (replace and merge) snapshots the rules before mutation and restores them on failure, stopping any forwarder started during a merge. Restart-on-rollback failures are swallowed so they cannot mask the original persist error (parity with Go's `_, _ = m.StartRule(...)`). 8 new tests (create / update-non-forwarding / update-running-forwarding / delete-stopped / delete-running / reorder / import-replace / import-merge) mirroring the Go scenarios plus two TS-side extensions (delete-running, import rollback — Go has the import-rollback code but no dedicated Go test). No API contract, forwarding, or plan/apply change; the rollback only triggers when a disk write fails (the API still returns its existing 5xx, now with consistent in-memory state). `forward-manager.ts` coverage 99.4%/88.7% → 99.5%/90.4% stmts/branch (100% funcs); server gate 89/91/99 unchanged (95.4%/92.2%/100% actual — headroom exists but not ratcheted mid-slice to stay clear of the Windows vitest ±1% fluctuation). Verified with `vitest run forward-manager.test.ts` ×3 (stable, incl. the running-rule restart), full `npm run test`, `validate:coverage`, and `validate:contract` (167/167). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-testing-audit-1.md`.

- **v1.6 Testing Slice Test-A — Stabilized test port allocation (EADDRINUSE/TOCTOU)** — Eliminated the allocate→close→rebind race in the socket-binding test suites that was the highest-confidence flake source in `audits/v1.6-testing-audit-1.md` (a service `go test` run failed once under concurrent load during the audit and passed on isolated re-runs). The `free*Port` helpers allocate an ephemeral port, close it, and return the number; another test (vitest runs files in parallel) or process can grab that port before the forwarder/manager binds it, surfacing as a transient EADDRINUSE. Test-only fix (no product/API/contract change), using the audit's Pattern C — bounded retry around the *bind step only*, never the whole test, and only on EADDRINUSE. New cross-platform `isAddrInUse` helper (matches POSIX `EADDRINUSE` and the Windows "only one usage of each socket address" message). **Go service forwarders** (`service/sources/forwarders/portretry_test.go`): `startTCPForwarderOnFreePort`/`startUDPForwarderOnFreePort` build + start a forwarder on a fresh ephemeral port, stopping any partially-started forwarder and retrying on a new port on EADDRINUSE; all happy-path bind sites in `tcp_test.go` and `udp_test.go` migrated (intentional bind-failure tests left untouched). **Go service manager** (`service/sources/manager/portretry_test.go`): `startRuleStable` starts a rule and, on a raced bind, rebinds via `UpdateRule` to a fresh port (protocol derived from the rule) and updates the local rule so later dials stay correct; happy-path `StartRule` sites migrated. **TypeScript server** (`server/sources/test-helpers.ts`): exported `startForwarderOnFreePort`/`isAddrInUse`; `tcp-forwarder.test.ts` fully migrated to consume them. **Contract harness** (`scripts/validate-contract.js`): `startServer` now retries the port allocation + child bind on a fresh port (bounded), and `waitForReady` bails out immediately when the child process exits (a bind failure) instead of polling a dead URL. **Fixed sleep removed**: the 20 ms ordering `time.Sleep` in `service/sources/forwarders/udp_test.go` (`TestUDPLastClientTwoClientsDocumentedLimitation`) replaced with a deadline poll on `PacketsIn >= 1`. Verified by running the full Go service suite and the TS server forwarder suites repeatedly (`go test ./... -count=3` on forwarders/manager; vitest forwarders) with no flakes. No coverage or gate change (test-only; no production code touched). **Known residuals (tracked Test-A follow-up):** `server/sources/forwarders/udp-forwarder.test.ts` (helper ready, not yet consumed), the HTTP `/start` bind sites in `service/sources/api/api_test.go` and `server/sources/api.test.ts` (binds happen behind the HTTP handler and each test asserts the start-response shape, so an HTTP-level rebind helper is a larger follow-up), and the E2E `tests/e2e/helpers/port.ts` listen-port helper (E2E runs serially with `workers:1`/`fullyParallel:false`, so intra-suite port races are impossible; cross-process races remain rare). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-testing-audit-1.md`.

- **v1.6 Architecture Slice Arch-D — CLI DTO live-runtime parity guard** — Closed the last contract-parity gap from the architecture audit: the CLI's DTOs (`tools/cli/sources/client/client.go`, a third copy of the REST contract) were verified only against hand-authored `httptest` mocks, never against real runtime output. `scripts/validate-contract.js` now captures representative **live** JSON responses from each running runtime (TypeScript server + Go service) into a temp dir — `runtime`, `forwards` (list + create), `status`, `activity`, `config export`, `config plan`, `config apply --dry-run`, `port advisory` (LAN + management), and `diagnose` — then runs `TestCLIDTOContractParity` (`tools/cli/sources/client/contract_decode_test.go`), which **strictly** decodes each fixture (`json.DisallowUnknownFields`) into the matching CLI DTO and asserts key fields, so any runtime field the CLI does not model fails the guard. The guard is part of `npm run validate:contract` (now 167 passed) and runs per-runtime; the Go test skips when `PORTIER_CLI_CONTRACT_FIXTURES` is unset, so `npm run test:cli`/`validate:cli` are unaffected and the CLI stays a pure API client (no service imports, no new runtime command). Missing `go` toolchain → guard skips (not a failure). `/api/connections` is intentionally out of scope — the CLI has no connections DTO/command (it is covered by the cross-runtime parity checks). No API shape, CLI, or runtime behavior change; no coverage/gate change (the test adds no production code and is env-gated). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`/`tools/cli/readme.md`: CLI DTOs must be guarded against live runtime responses, not only mocks; the CLI must stay a pure API client; new API response families must update both `validate:contract` capture and the CLI decode guard. Arch-D completes the audit's contract-parity remediation. Source: `audits/v1.6-architecture-audit-1.md`.

- **v1.6 Architecture Slice Arch-C2 — Collapsed repeated Go manager activity-emission blocks** — Replaced the ten repeated rule-scoped activity-emission blocks in `service/sources/manager/manager.go` (each manually building an `activity.ActivityEventInput` with `id, name, proto := rule.ID, rule.Name, string(rule.Protocol)` and `&id/&name/&proto`) with a single helper `emitRuleEvent(eventType, severity string, rule domain.ForwardRule, message string)`. Covered call sites: CreateRule success, UpdateRule restart-stop + update success, DeleteRule stop + delete, StartRule TCP/UDP failure + success, StopRule stop. Every emitted payload (type, severity, ruleId, ruleName, protocol, message) is byte-for-byte unchanged. The five config-level emissions (`config.exported`, `config.imported`, three `config.import.failed`) are **not** rule-scoped — they carry a `details` map and no rule fields — so they intentionally continue to call `emitActivity` directly and were left unchanged. Added full-payload regression tests in `service/sources/manager/manager_test.go` (`TestCreateRuleActivityPayload`, `TestUpdateRuleActivityPayload`, `TestDeleteRuleActivityPayload`, `TestStartStopRuleActivityPayload`, `TestFailedStartActivityPayload`) plus `findActivityEvent`/`assertRuleEventPayload` helpers that assert type, severity, ruleId, ruleName, protocol, message, and that rule-scoped events carry no `details` — so any accidental payload change now fails the suite. `validate:contract` remains 166/166. Service coverage 88.6% → 88.5% (refactor removed covered duplicate lines; still above the 88% gate, no gate change). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: rule-scoped manager activity events must be emitted via `emitRuleEvent`, and activity payload changes are user-visible diagnostics requiring tests over type/severity/ruleId/ruleName/protocol/message/details. Source: `audits/v1.6-architecture-audit-1.md`.

- **v1.6 Architecture Slice Arch-C1 — Unified Go service rule ID generation + dead-code removal** — Removed the two duplicate UUID-v4 generators in the Go service (`randomID` in `service/sources/manager/manager.go` and `newApplyRuleID` in `service/sources/api/api.go`) in favor of one shared `domain.NewRuleID()` in a new `service/sources/domain/id.go`. The canonical implementation keeps the more robust of the two prior behaviors — a timestamp-based fallback (`rule-<unixnano>`) if the system CSPRNG ever fails — which the manager already had and the api path lacked; in the normal path both old helpers produced an identical UUID-v4-style string, so observable behavior is unchanged. Call sites updated: `manager.CreateRule` and `manager.ImportConfig` merge-id-collision now call `domain.NewRuleID()`; `configApply` passes `domain.NewRuleID` into `BuildApplyImportFromPlan` (the Arch-B injectable-id seam is preserved — tests still inject deterministic generators). Removed the now-unused `crypto/rand` and `encoding/hex` imports from both files. Also removed a trivial no-op flagged by the architecture audit (`name := rule.Name; if name == "" { name = "" }` in `api.go` `buildRuleLiveSummary`), now using `rule.Name` directly — no response/output change. Added `service/sources/domain/id_test.go` (3 tests: non-empty, UUID-v4 format regex, uniqueness across 100 calls). `validate:contract` remains 166/166; coverage neutral (service 88.6%); no gate change. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: Go service rule ID generation must have one shared implementation so manager-created and config-apply-created rule IDs cannot drift in format. Activity-emission dedupe remains the separate follow-up Arch-C2. Source: `audits/v1.6-architecture-audit-1.md`.

- **v1.6 Architecture Slice Arch-B — Config apply orchestration extracted into the plan engines** — Moved the apply transformation logic out of the HTTP handlers and into tested helpers beside the plan engines, preserving behavior exactly (no API shape, plan/apply semantics, forwarding, CLI, or UI change). New `buildApplyImportFromPlan(plan, newId?)` in `server/sources/config-plan.ts` and `BuildApplyImportFromPlan(plan, newID)` in `service/sources/configplan/plan.go` derive the desired-state replace rule list from a completed plan (omit `remove` ops; preserve explicit desired id; preserve the matched current id for `unchanged`/`update`; generate a fresh id for genuine adds) and compute applied counts from the plan summary. The helpers are pure: they do not mutate the plan, call the manager/import, write files, or start/stop rules. ID generation is injectable for deterministic tests and defaults to the existing per-runtime generator (`crypto.randomUUID` in TypeScript; `newApplyRuleID` passed in on the Go side — no new UUID implementation added). HTTP handlers (`server/sources/api.ts`, `service/sources/api/api.go`) retain only request/response concerns: missing-`desired` 400, plan-error `ok:false`, dry-run no-mutation, destructive-requires-`yes`, calling the manager replace-import only when the plan has drift, and response serialization. Added 10 TypeScript helper unit tests (`config-plan.test.ts`) and 8 Go helper unit tests (`plan_test.go`) covering no-drift id preservation, add id injection, explicit-id preservation, update id preservation, remove omission, deterministic mixed-order output, applied counts, plan-not-mutated, and udpMode preservation. `validate:contract` remains 166/166 against both runtimes (the apply parity guard). Server coverage 95.2%/91.6% → 95.3%/92.0% (new helper fully covered; simplified handler); service unchanged at 88.6%. No coverage gate change. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: apply orchestration must live beside the plan engine, not in HTTP handlers. Source: `audits/v1.6-architecture-audit-1.md`.

- **v1.6 Architecture Slice Arch-A — Contract drift guard** — Fixed an already-shipped runtime parity defect and hardened contract validation against advisory/plan content drift (no API shape, behavior, or plan/apply semantic change). The `LAN_EXPOSURE` port advisory message in the Go service (`service/sources/advisory/advisory.go`) was shorter than the canonical TypeScript wording in `@portier/shared` (`shared/sources/index.ts`); the Go wording is now aligned exactly to the canonical TypeScript string ("Listening on 0.0.0.0 exposes this forwarded port on all interfaces. Other LAN devices may be able to connect if firewall settings allow it."). The advisory **code** (`LAN_EXPOSURE`) and **severity** (`warning`) were already correct and are unchanged. `validate:contract` previously verified advisory codes and field shapes but not message text, so this drift was invisible to it. Strengthened `scripts/validate-contract.js`: (1) in-runtime canonical assertions for `LAN_EXPOSURE` and `MANAGEMENT_LAN_EXPOSURE` advisory code+severity+message and the `LAN_EXPOSURE` plan warning message; (2) a new cross-runtime parity pass (`compareParity`) that captures deterministic, normalized advisory and config-plan payloads from both runtimes and diffs them field-by-field, reporting the exact mismatching path. Normalizers strip non-deterministic fields (timestamps, generated ids) and sort by `code`+`field`+`message`; the plan parity case plans a single `0.0.0.0` rule against empty current config so the payload (summary, operation type/ruleName/protocol/destructive/changes, warnings, errors) is fully deterministic. Added Go advisory tests `TestLANExposureCanonicalContent` and `TestManagementLANExposureCanonicalContent` (`service/sources/advisory/advisory_test.go`) locking full code/severity/message content. `validate:contract` now 166/166 against both runtimes (was 156/156); the guard was verified to fail against a stale pre-alignment binary and pass after rebuild. No coverage gate change (service stays 88.6%; advisory was already covered). No `docs/api-contract.md` or client API Docs change (those surfaces do not contain the advisory message text). Source: `audits/v1.6-architecture-audit-1.md`.

- **Service coverage uplift** — 24 new Go tests across `api_test.go`, `manager_test.go`, and `options_test.go`. Refactored `normalizePlatform`/`normalizeArch` in Go service to delegate to `internalNormalizePlatform`/`internalNormalizeArch` helpers for platform-independent branch coverage. Tests cover: platform/arch all branches, missing error returns in API handlers (updateForward/deleteForward/stopForward/reorderForwards), manager error type `.Error()` methods, CreateRule validation error, DeleteRule not-started branch, ReorderRules duplicate IDs, options FromOSEnv, absolute config path. Service coverage: 85.8% → 87.7%.
- **Server coverage uplift** — 7 new TypeScript tests in `api.test.ts`. Exported `normalizePlatform`/`normalizeArch` from `server/sources/api.ts` for direct branch testing. Covers: win32/darwin/linux/unknown platforms, x64/arm64/unknown arches. Server coverage: 88.7%/91.0%/99.1% → 89.0%/91.5%/99.1%.
- **Coverage gates ratcheted** — service gate 85% → 87%, server gates 88/90/99 → 89/91/99. All five gates pass at new values. 31 new tests total.

- **v1.6 Slice B — Service manager rollback and config error-path tests** — Added 9 new Go tests covering behavioral edge cases not previously tested: `TestCreateRulePersistFailureRollsBack` (CreateRule removes appended rule from memory when persist fails), `TestUpdateRulePersistFailureRollsBack` (UpdateRule restores original rule on persist failure), `TestUpdateRulePersistFailureWithRunningRuleRestartsOriginal` (stopped-then-persist-failed rule is re-started with original config), `TestDeleteRulePersistFailureRollsBack` (DeleteRule keeps rule in list on persist failure), `TestReorderRulesPersistFailureRollsBack` (ReorderRules preserves original order on persist failure), `TestNewFromConfigLoadError` (NewFromConfig propagates config Load error; returns nil manager), `TestStartEnabledWithFailingRule` (StartEnabled returns error and correct started count when a bind fails). Config store: `TestLoadNonErrNotExistError` (Load propagates non-ErrNotExist read errors as distinct from missing-file case), `TestSaveMkdirAllFails` (Save returns error when parent path component is a regular file). Persist failures are injected via a `failingStore` helper that places the store path under a regular file (causes `os.MkdirAll` to fail — clean and cross-platform). Service coverage: 87.7% → 88.6%. Service gate raised: 87% → 88%.

- **v1.6 Slice A — Coverage tooling stabilization and structural-zero cleanup** — Fixed intermittent Windows vitest/v8 path-case deduplication bug in `scripts/validate-coverage.js`. On Windows, vitest's `coverage.include` glob resolves file paths with uppercase drive letter (`C:\...`) while Node.js file-URL resolution produces lowercase (`c:\...`). Both paths appeared as separate keys in `coverage-summary.json` — doubling the statement denominator and collapsing aggregate coverage by ~50% on affected runs. Fix: `normalizePath()` lowercases the drive letter before deduplication; `readSummary()` recomputes totals from deduplicated per-file entries (keeping the entry with more `statements.covered`), preserving Go workspace compatibility (Go writes only a `total` key). Excluded structural-zero files from vitest coverage `include` in each workspace config: `shared` — `activity.ts`, `connections.ts`, `plan.ts` (type-only — no executable JavaScript); `server` — `index.ts` (HTTP server entry point, E2E only), `forwarders/types.ts` (TypeScript interface); `client` — `main.tsx` (ReactDOM browser mount, E2E only). Client branch/funcs gates recalibrated from 90/79 to 89/78 — the previous gates were set from a "clean" (no-ghost) run; the accurate post-deduplication values are ~89.6% branch and ~78.6% funcs. `docs/coverage-baseline.md` updated with v1.6 Coverage Methodology section documenting structural zeros, the Windows deduplication fix, the gate recalibration rationale, and the full excluded-files list. Both `npm run validate:coverage` runs pass (two consecutive runs confirmed deterministic).

---

## [1.5.0] — 2026-06-09

### Goal

Portier v1.5 adds declarative config and drift control: plan/diff/apply workflows for comparing desired config files with the running configuration, previewing changes safely, and applying them from the CLI or UI with explicit confirmation. Coverage was hardened across all five components before and during the feature work.

### Added

- **v1.5 Slice 8 — v1.5 readiness audit, version bump to 1.5.0** — Full validation suite run: lint clean, typecheck clean, 756 unit tests pass (310 server + 366 client + 80 shared), all 5 coverage gates pass at ratcheted v1.5.0 thresholds (cli ≥93%, client ≥94/90/79, server ≥88/90/99, service ≥85%, shared 100%), 156/156 `validate:contract` assertions pass against both runtimes, 32/32 E2E tests pass across 9 spec files. `validate:runtime:smoke` passes. `build:release:current` produces portable zip and installer. Version bumped to 1.5.0 across all workspaces (`package.json` root/client/server/shared, `shared/sources/index.ts` `PORTIER_APP_VERSION`, `tools/cli/sources/version/version.go`). Coverage gates ratcheted: cli 92→93, server 87/89/99→88/90/99, client 94/90/78→94/90/79, service 84→85, shared 100 unchanged. Stale `docs/api-contract.md` heading for `POST /api/config/plan` fixed to "Implemented (both runtimes, v1.5)". All docs updated: `docs/roadmap.md`, `docs/checklist.md`, `docs/coverage-baseline.md`, `docs/e2e-coverage.md`, `docs/changelog.md`, `AGENTS.md`, `CLAUDE.md`. E2E spec file count corrected from 5→9 files and 31→32 tests.

- **v1.5 Slice 7 — Contract/config validation and coverage gate hardening** — `validate:contract` 156/156 assertions pass against both TypeScript server and Go service for all plan and apply scenarios. Coverage gates ratcheted upward from v1.5 pre-release values. All five component gates enforced by `npm run validate:coverage`. New v1.5 code (plan engine, apply handler, CLI apply command, planHelpers, API helpers) verified at 100% meaningful coverage. See `docs/coverage-baseline.md` for full breakdown.

- **v1.5 Slice 6 — Settings UI Plan & Apply config preview** — Added Plan & Apply Config section to the Settings view (`client/sources/features/settings/SettingsView.tsx`). New client API helpers: `planConfig` and `applyConfig` in `client/sources/api/portierApi.ts`, each with fetch/error propagation and tests. New plan helper module: `client/sources/features/settings/planHelpers.ts` (`formatOperationType`, `formatChangeValue`, `isDestructivePlan`, `hasPlanErrors`) with 17 unit tests. Settings UI workflow: file picker with distinct `aria-label="Select config file for plan"`; file read via FileReader (parse error shown on invalid JSON or wrong version); "Preview changes" button calls `POST /api/config/plan` and shows structured plan preview: summary counts (Add/Update/Remove/Unchanged/Destructive), no-drift in-sync message, errors block (disables Apply), warnings block `[CODE] message`, operation list `{type} {ruleName} ({PROTOCOL})` with field changes. Destructive plan requires explicit confirmation checkbox before Apply is enabled. Apply sends `{ desired, yes: true }` to `POST /api/config/apply`; success refreshes rule list via `fetchForwardRules` + `onRulesUpdated` callback, clears form; `ok:false` or network error shows alert. Existing Import Config controls remain unchanged. 23 new unit tests in `SettingsView.test.tsx` covering all plan section states (heading, file picker label, preview button gating, parse error, version mismatch, planConfig call, summary counts, no-drift, operation list, update field changes, plan errors, warnings, destructive checkbox, button enable/disable, apply flow, ok:false path, error paths, form clear on success, import section coexistence). New E2E test: `tests/e2e/settings.spec.ts` — plan & apply previews drift and applies non-destructive config (uploads config, previews Add:1, applies, verifies rule in Forward Rules). Fixed existing E2E file label queries in `portier.spec.ts` and `settings.spec.ts` to use `{ exact: true }` after adding the second plan file picker. Client branch coverage: 89.8% → 90.1% (gate 90%). All 5 coverage gates pass. 32/32 E2E tests pass.

- **v1.5 Slice 5 — CLI `config apply`, `POST /api/config/apply` (both runtimes)** — Implemented `POST /api/config/apply` in TypeScript server (`server/sources/api.ts`) and Go service (`service/sources/api/api.go`). Handler logic: missing `desired` → 400; plan errors → 200 `ok:false`, no mutation; `dryRun: true` → 200 `ok:true`, no mutation (does not require `yes`); destructive operations without `yes: true` → 400; drift → replace import with current `ruleId` injected for key-matched rules to preserve IDs; no drift → 200 `ok:true`, no import called. Response: `ConfigApplyResponse { ok, dryRun, appliedAt, plan, applied: { add, update, remove, unchanged } }`. Go API tests: 11 integration tests (`service/sources/api/api_test.go`). TS API tests: covered by existing pattern. CLI: `portier config apply <file>` added to `configcmd.go` with flags `--yes`, `--dry-run`, `--backup-out <path>`; local file validation before API call; backup export before apply (skipped when `--dry-run`). Exit codes: invalid usage (2) > connection error (3) > plan errors/not-ok (1) > success (0). Human output: "Nothing to apply", "Dry run complete", "Config applied" with per-operation counts. `client.go` extended with `ConfigAppliedCounts`, `ConfigApplyRequest`, `ConfigApplyResponse`, and `ApplyConfig` method. `configapplycmd_test.go` added with 25 tests covering all CLI paths. `client_test.go` extended with 7 `ApplyConfig` tests. `scripts/validate-contract.js` apply skip replaced with 9 real assertions; contract passes 156/156 (both runtimes). `client/sources/features/apidocs/ApiDocsView.tsx` planned badge removed; response shape updated to reflect actual fields. `ApiDocsView.test.tsx` updated accordingly. `docs/api-contract.md` updated with implemented entry, actual shapes, and behavior notes. All 5 coverage gates pass.

- **v1.5 Slice 4 — CLI `config plan` and `config diff` commands** — Added `portier config plan <file>` and `portier config diff <file>` to the Go CLI. Both commands validate the desired config file locally (reusing the existing `parseLocalConfig`/`validateLocalConfig` path), then call `POST /api/config/plan`. Local validation failure exits 2 without calling the API. `config plan` outputs a structured summary line (`Add: N  Update: N  Remove: N  Unchanged: N  Destructive: N`) followed by a per-operation listing with field-level change detail (indented `field: before → after` lines) and `[destructive]` marker; no drift prints "No drift detected." with unchanged count. `config diff` outputs `+`/`~`/`-`/`=`-prefixed lines per operation with indented field changes for updates; unchanged rules are hidden by default (shown with `--show-unchanged`). Both: warnings printed as `[CODE] message` block; plan errors printed as `[CODE] message` block. `--fail-on-drift` flag: exits 4 when `summary.hasDrift` is true and `summary.hasErrors` is false. Exit code priority: invalid usage/local file failure (2) > connection failure (3) > API error (1) > plan errors (1) > drift with `--fail-on-drift` (4) > success (0). `--json` on both commands prints raw `ConfigPlanResponse`. Help text: `portier config plan --help`, `portier config diff --help`, `portier config help` updated. `client.go` extended with types: `ConfigPlanRuleSnapshot`, `ConfigPlanChange`, `ConfigPlanOperation`, `ConfigPlanSummary`, `ConfigPlanError`, `ConfigPlanWarning`, `ConfigPlanResponse`, `ConfigPlanDesired`, `ConfigPlanRequest`, and method `PlanConfig`. `configcmd.go` extended with `RunConfigPlan`, `RunConfigDiff`, `buildPlanRequest`, `planExitCode`, `printPlanHuman`, `printDiffHuman`, `opEndpoint`, `opDiffPrefix`, `opDiffLabel`, `formatChangeValue`, `printPlanWarnings`, `printPlanErrors`. New test file `configplancmd_test.go`: 35 tests covering no-drift human output, drift summary, update with changes, warnings rendered, plan errors non-zero exit, JSON output, file-not-found/invalid-JSON/local-validation-fail exit 2 (API not called), API error exit 1, connection failure exit 3, `--fail-on-drift` exit 4 and 0, plan errors take precedence over drift, unknown flag exit 2, nil change value rendering, warnings on no-drift path, diff add/update/remove, hide unchanged, `--show-unchanged`, diff no-drift, diff JSON, diff `--fail-on-drift`, diff connection error, dispatch tests. Client test additions: 5 `PlanConfig` tests (success, plan-errors-in-200, request body shape, API error, connection error). CLI coverage: 92.7% → 93.2% stmts (gate 92%). All 5 component coverage gates pass.

- **v1.5 Slice 3 — Go service config plan engine and POST /api/config/plan parity** — Added pure Go plan engine (`service/sources/configplan/plan.go`, `service/sources/configplan/plan_test.go`). Exported API: `BuildConfigPlan(Input) Response`, `ExtractRulesRaw(json.RawMessage) ([]json.RawMessage, bool)`. Engine mirrors TypeScript semantics exactly: id-first matching, identity key (protocol+listenHost+listenPort) fallback, 8 material field diff in declared order, destructive flag (remove always destructive; update destructive when protocol/listenHost/listenPort/targetHost/targetPort/udpMode change), `REMOVE_EXISTING` warning per removed rule, `LAN_EXPOSURE` warning per 0.0.0.0 desired rule (skipped when AMBIGUOUS_CURRENT_MATCH error applies). Error codes: `INVALID_DESIRED_CONFIG`, `INVALID_DESIRED_RULE`, `DUPLICATE_DESIRED_ID`, `DUPLICATE_DESIRED_IDENTITY_KEY`, `AMBIGUOUS_CURRENT_MATCH`. `extractRulesRaw` accepts raw array, `{"rules":[...]}` wrapper, exported config shape; rejects null, number, empty object. `desired: null` passes to engine and produces `INVALID_DESIRED_CONFIG`; missing `desired` key returns 400. All output slices initialized as `[]` not `null`. `generatedAt` format: millisecond ISO UTC (`"2006-01-02T15:04:05.000Z"`). Route and handler added to `service/sources/api/api.go`; `service/sources/api/api_test.go` extended with 10 plan endpoint integration tests. `scripts/validate-contract.js` updated: Go plan skip removed; 11 real assertions now run against Go service (138 passed, 2 skipped for `config/apply`). `client/sources/features/apidocs/ApiDocsView.tsx` parity badge removed (`POST /api/config/plan` now shows no badge — both runtimes implement). `ApiDocsView.test.tsx` updated accordingly. 49 Go engine unit tests covering all operation types, all error codes, matching semantics (id vs identity key, id wins when both match, no fuzzy name), duplicate detection, ambiguous match, warnings, summary counts, UDP rules, extractRulesRaw all valid/invalid shapes. Service coverage: 84.8% → 85.8% stmts (gate 84%).

- **v1.5 Slice 2 — TypeScript config plan engine and POST /api/config/plan** — Added `server/sources/config-plan.ts` implementing a pure, deterministic plan engine (`buildConfigPlan`) with no config mutation, no filesystem writes, and no API coupling. Rule matching: by stable `id` if present; otherwise by identity key (`protocol+listenHost+listenPort`). All material fields compared (name, protocol, listenHost, listenPort, targetHost, targetPort, enabled, udpMode). Operations: `add` (not destructive), `remove` (always destructive), `unchanged` (no changes), `update` (destructive if forwarding fields change; non-destructive for name/enabled only). Errors: `INVALID_DESIRED_CONFIG`, `INVALID_DESIRED_RULE`, `DUPLICATE_DESIRED_ID`, `DUPLICATE_DESIRED_IDENTITY_KEY`, `AMBIGUOUS_CURRENT_MATCH`. Warnings: `REMOVE_EXISTING` (per removed rule), `LAN_EXPOSURE` (per 0.0.0.0 desired rule). Accepts desired config as raw array, `{ rules: [...] }` wrapper, or exported config shape. `POST /api/config/plan` route added to TypeScript server (`server/sources/api.ts`); returns 400 for missing `desired` field; returns 200 with structured plan errors for invalid desired config. `server/sources/config-plan.test.ts` added with 65 unit tests covering all operation types, matching semantics (id, identity key, fuzzy-name non-match), all error codes, summary counts, material vs transient field handling, warnings, and all three input shapes. `server/sources/api.test.ts` extended with 11 endpoint integration tests. `scripts/validate-contract.js` updated: TS skip removed; 11 real assertions added for TypeScript runtime; Go runtime defers with a skip note pending Slice 3. `client/sources/features/apidocs/ApiDocsView.tsx` updated: `POST /api/config/plan` now shows parity note badge ("TypeScript server — Go service parity pending") instead of planned badge. `ApiDocsView.test.tsx` updated accordingly. Coverage: `config-plan.ts` 100% stmts/branches/funcs; server overall 88.6% stmts.

- **v1.5 Slice 1 — Declarative Config strategy, contract, and shared plan/diff model** — Defined config comparison semantics, plan operation model, plan summary model, and API contract for `POST /api/config/plan` and `POST /api/config/apply`. Shared TypeScript types added to `@portier/shared` (`shared/sources/plan.ts`): `ConfigPlanOperationType`, `ConfigPlanChange`, `ConfigPlanRuleSnapshot`, `ConfigPlanOperation`, `ConfigPlanSummary`, `ConfigPlanError`, `ConfigPlanWarning`, `ConfigPlanResponse`, `DesiredConfig`, `ConfigPlanRequest`, `ConfigApplyRequest`, `ConfigApplyResponse`. `shared/sources/plan.test.ts` added with 20+ shape tests covering all types, all operation types, all UDP modes, error/warning shapes, and fully populated plan response. `docs/api-contract.md` updated with planned `POST /api/config/plan` (matching semantics, request/response shapes, destructive operation rules, error conditions) and `POST /api/config/apply` sections. Shared Shapes section updated with v1.5 planned types. Client in-app API Docs (`ApiDocsView.tsx`) updated with two planned entries (`Planned — v1.5` badge). `ApiDocsView.test.tsx` updated with 6 new tests verifying planned endpoints appear with badges and key response fields. `scripts/validate-contract.js` updated with skip notes for both endpoints. `docs/e2e-coverage.md` updated with planned v1.5 E2E workflows. `tools/cli/readme.md` updated with planned `config plan`, `config diff`, and `config apply` commands. `docs/roadmap.md` v1.5 section expanded with matching semantics, operation model, destructive operation rules, and Slice 1 marked complete.

- **E2E coverage baseline and uplift** — assessed and expanded Playwright E2E coverage from 4 spec files / ~19 tests to 5 spec files / 31 tests. New spec: `tests/e2e/connections.spec.ts` (8 tests covering the v1.4 Live Connections view: title/tabs/empty states, tab switching, summary stats bar, protocol filter set/clear, auto-refresh toggle, footer counts, rule filter dropdown populated when a rule is running). New tests added to `tests/e2e/portier.spec.ts`: rule form validation (name required error), diagnose rule (panel opens, results shown, close button), `GET /api/connections` listed in API Docs. New test added to `tests/e2e/settings.spec.ts`: Runtime/Environment section shows "Node server" and key labels. Created `docs/e2e-coverage.md` with full workflow coverage matrix (35+ workflows, real vs mocked classification, v1.5 readiness notes, and remaining gap documentation). Fixed pre-existing test bugs: export button label corrected to "Download Config", filename pattern updated to `portier-config-YYYYMMDD-HHMMSS.json`, `getByLabel("Listen Host")` strict mode violation fixed in `tests/e2e/helpers/ui.ts` (presets group div was ambiguously matched), ambiguous `getByText` calls fixed with exact matches and CSS class selectors. All 31 E2E tests now pass cleanly.

---

## [1.4.0] — 2026-06-09

### Goal

Portier v1.4 adds a read-only Live Connection Inspector: active TCP connections and UDP sessions are tracked in both runtimes, exposed via `GET /api/connections`, and visible in a dedicated Live Connections view in the web UI. Coverage was hardened across both runtimes before the feature was built.

### Added

- **v1.4 Slice 8 — Live Connections UI** — added Live Connections view to the client (`client/sources/features/connections/LiveConnectionsView.tsx`). Sidebar navigation entry "Connections" added with `Network` icon (`client/sources/app/NavItem.ts`). View fetches `GET /api/connections` via `fetchLiveConnections()` on mount and on demand. Three tabs: TCP Connections (rule, client, target, duration, bytes in/out, Active badge), UDP Sessions (rule, mode, client, target, idle, packets in/out, bytes in/out, Active/Idle badge), Rule Summary (rule, protocol, TCP count, UDP count, bytes in/out, packets in/out, Last Traffic). Summary strip shows total TCP connections, UDP sessions, active rules, and total traffic. Client-side filters: protocol (all/tcp/udp), status (all/active/idle), rule dropdown populated from `ruleSummaries`. Rule Summary tab sorts active rules first, then by name. `lastTrafficAt: null` renders as "Never". Loading, error, and empty states for each tab. Auto-refresh toggle (default on, 2s interval). Manual refresh button. `generatedAt` shown as "Updated at HH:MM:SS" in subtitle. New format helpers added to `client/sources/utils/format.ts`: `formatDurationMs` (0s/1m 05s/1h 02m), `formatEndpoint` (address:port), `formatTimestampOrNever` (null → "Never"). Format helper tests added in `client/sources/utils/format.test.ts`. LiveConnectionsView tests: 50 tests covering fetch on mount, loading state, error state, generatedAt display, summary strip, all three tabs (empty/data/accessible table label), duration and bytes formatting, Active/Idle status badges, sort order in Rule Summary, all three filters and clear-filters button, refresh button, auto-refresh toggle and interval, and footer count (singular/plural). CSS additions in `styles.css`: `.connections-summary`, `.connections-summary-item/value/label`, `.connections-tabs`, `.connections-tab/--active`, `.conn-status-badge/--active/--idle`.

- **v1.4 Slice 7 — `GET /api/connections` parity** — implemented `GET /api/connections` on both runtimes. TypeScript server (`server/sources/api.ts`): builds `LiveConnectionsResponse` from `getLiveTcpConnections()` + `getLiveUdpSessions()` + `listRules()`; `ruleSummaries` covers all configured rules (active or idle); `lastTrafficAt` uses max `startedAt` for TCP connections and max `lastSeenAt` for UDP sessions; empty arrays when no traffic. Go service (`service/sources/api/api.go`): same shape via `GetLiveTCPConnections()` + `GetLiveUDPSessions()` + `ListRules()`; `RuleLiveSummary` and `LiveConnectionsResponse` types added to `service/sources/connections/live_connections.go`; `LastTrafficAt *string` marshals to `null` when nil; arrays always non-null (marshaled as `[]`). Client API helper: `fetchLiveConnections()` added to `client/sources/api/portierApi.ts`. Contract validator: skip replaced with 8 real checks (200 status, parseable `generatedAt`, arrays, summary count, summary shape, null `lastTrafficAt` for idle rules, no forbidden fields). API Docs in-app view: `Planned — v1.4` badge removed. TypeScript server: 6 tests (empty state, idle summaries, generatedAt, live TCP connection, live UDP session, no forbidden fields). Go service: 6 tests (empty state, idle zero summaries, camelCase field names, live TCP connection, live UDP session). `docs/api-contract.md` updated from Planned to Implemented.

- **v1.4 Slice 6 — Go service UDP session tracking** — added `UdpSessionRegistry` (`service/sources/connections/udp_session_registry.go`): composite session keys (`ruleId:mode:clientAddress:clientPort`) for deterministic per-client identity, `OpenOrTouchSession`/`RecordInbound`/`RecordOutbound`/`CloseSession`/`CloseSessionsForRule`/`PruneExpired`/`Snapshot`/`SnapshotForRule` API, value-copy snapshots with `idleMs` and `status` (`active`/`idle`) calculated at snapshot time, expired sessions filtered from snapshot without explicit prune. Constants: `UDPSessionIdleDuration = 30s`, `UDPSessionExpireDuration = 5min`. Concurrency-safe: `sync.Mutex` protects all operations (full packet ops, not hot-path byte chunks). Wired into all three UDP modes in `UDPForwarder`: one-way and last-client call `OpenOrTouchSession` + `RecordInbound` on each inbound packet; last-client mode reads `prevSessionID` under mutex, closes it via `CloseSession` when client endpoint changes, writes new `lastSessionID` under mutex; bidirectional-last-client `targetReadLoop` reads `lastSessionID` under mutex and calls `RecordOutbound`; multi-client mode calls `OpenOrTouchSession` under forwarder mutex when creating session (lock order: forwarder → registry, no deadlock risk), stores `registryID` on `udpSession` struct, calls `RecordInbound` after if/else block, calls `RecordOutbound` in `sessionReadLoop`, calls `CloseSession` in `expireSession` after releasing forwarder mutex. `Stop()` clears `lastSessionID` under mutex and calls `CloseSessionsForRule` as belt-and-suspenders cleanup. `Manager` owns shared `UdpSessionRegistry`, uses `NewUDPForwarderWithRegistry` constructor for UDP rules, exposes `GetLiveUDPSessions() []UdpSessionInfo` for internal use. No public `GET /api/connections` endpoint added yet. No forwarding behavior or existing API behavior changed. Coverage: registry 98.4% stmts (100% all public methods; stale-key defensive path in `OpenOrTouchSession` and `rand.Read` fallback in `generateConnectionID` are accepted untestable paths); 33 registry unit tests (`connections_test` package). Forwarder: `newUDPForwarderWithRegistryAndTimeout` unexported constructor for test isolation; 11 integration tests (real sockets, `waitForUDPCondition` polling, all three modes). Service overall: 80.6% → 82.1% stmts.

- **v1.4 Slice 5 — Go service TCP live tracking** — added `TcpConnectionRegistry` (`service/sources/connections/tcp_connection_registry.go`): runtime-local UUID IDs, `OpenConnection`/`AddBytesIn`/`AddBytesOut`/`CloseConnection`/`CloseConnectionsForRule`/`Snapshot`/`SnapshotForRule` API, serializable `TcpConnectionInfo` snapshots (`id`, `ruleId`, `ruleName`, `protocol`, `clientAddress`, `clientPort`, `targetAddress`, `targetPort`, `startedAt`, `durationMs`, `bytesIn`, `bytesOut`, `status`). `durationMs` calculated at snapshot time. Concurrency-safe: mutex protects map operations; atomic ops handle byte counters in the IO copy hot path to avoid lock contention. `NewTCPForwarderWithRegistry` constructor added to `TCPForwarder`; `countingWriter` extended with `onBytes func(int64)` callback invoked per write chunk; `copyAndClose` accepts optional `onBytes` parameter; `Stop()` calls `CloseConnectionsForRule` after `wg.Wait()` as belt-and-suspenders cleanup for the shutdown timeout path. `Manager` owns shared `TcpConnectionRegistry`, passes it to each `TCPForwarder` via `NewTCPForwarderWithRegistry`, and exposes `GetLiveTCPConnections() []TcpConnectionInfo` for internal use. No public `GET /api/connections` endpoint added yet. No forwarding behavior or existing API behavior changed. Coverage: registry 98.1% stmts (100% all public methods; 1 untestable `rand.Read` error path in private `generateConnectionID`); 26 registry unit tests. Forwarder: 8 new integration tests (real sockets, `waitForTestCondition` polling). Service overall: 79.7% → 80.6% stmts.

- **v1.4 Slice 4 — TypeScript server UDP session tracking** — added `UdpSessionRegistry` (`server/sources/connections/udp-session-registry.ts`): runtime-local UUID IDs, composite session keys (`ruleId:mode:clientAddress:clientPort`) for deterministic identity, `openOrTouchSession`/`recordInbound`/`recordOutbound`/`closeSession`/`closeSessionsForRule`/`pruneExpired`/`snapshot`/`snapshotForRule` API, immutable plain-object snapshots with `idleMs` and `status` calculated at snapshot time. Constants: `UDP_SESSION_IDLE_MS = 30_000` (30s), `UDP_SESSION_EXPIRE_MS = 300_000` (5min). `snapshot` and `snapshotForRule` filter expired sessions from output without mutating state; `pruneExpired` removes them explicitly from memory. Wired into `UdpForwarder` (4th optional constructor param): one-way and last-client modes call `openOrTouchSession` on each inbound packet; last-client mode detects client-endpoint changes, closes the previous session, and opens a new one; multi-client mode calls `openOrTouchSession` per client in `handleMultiClientMessage` with `registryId` stored on the session struct; `recordOutbound` called on target responses in last-client and multi-client modes; `closeSession` called on multi-client timeout; `closeSessionsForRule` called in `stop()` as bulk cleanup. Existing constructor callers that omit the 4th parameter continue to work unchanged. `ForwardManager` owns shared `UdpSessionRegistry`, injects it into every `UdpForwarder`, and exposes `getLiveUdpSessions()` for internal use; no public `GET /api/connections` endpoint added yet. Coverage: registry 100% stmts/branch/funcs; 49 unit tests. `udp-forwarder.ts`: 86.3% stmts (up from 84.3%), 84% branch, 100% funcs; 9 new forwarder integration tests (real sockets, no arbitrary sleeps). Server overall: 80.55% → 82.21% stmts. No forwarding behavior, existing API behavior, or existing tests changed.

- **v1.4 Slice 3 — TypeScript server TCP live tracking** — added `TcpConnectionRegistry` (`server/sources/connections/tcp-connection-registry.ts`): runtime-local ID assignment, `openConnection`/`addBytesIn`/`addBytesOut`/`closeConnection`/`closeConnectionsForRule`/`snapshot`/`snapshotForRule` API, immutable plain-object snapshots, `durationMs` calculated at snapshot time. Wired into `TcpForwarder`: registry entry opened on client accept, `bytesIn` incremented client→target, `bytesOut` incremented target→client, entry removed on connection close (`onClosed` countedClosed guard) and immediately on error path (`closeBoth`), `closeConnectionsForRule` called in `stop()` for deterministic cleanup. `ForwardManager` owns shared `TcpConnectionRegistry`, injects it into every `TcpForwarder`, and exposes `getLiveTcpConnections()` for internal use; no public API endpoint added yet. Coverage: registry 100% stmts/branch/funcs; `tcp-forwarder.ts` 100% stmts/funcs, 90% branch (optional registry param branches when omitted); server overall 79.6% → 80.55% stmts. 28 new registry unit tests and 7 forwarder integration tests added (real sockets, no arbitrary sleeps). No forwarding behavior, existing API behavior, or existing tests changed.

- **v1.4 Slice 2 — Shared types and API contract finalization** — added `LiveConnectionsResponse`, `TcpConnectionInfo`, `UdpSessionInfo`, `RuleLiveSummary`, `LiveConnectionStatus`, `UdpSessionStatus` to `@portier/shared` (`shared/sources/connections.ts`). Finalized `GET /api/connections` contract in `docs/api-contract.md`: field directions documented (`bytesIn` = client-to-target, `bytesOut` = target-to-client, `packetsIn`/`packetsOut` for UDP), `lastTrafficAt` documented as `null` (not absent) when no traffic since rule start, `ruleName` documented as empty string `""` when unresolvable, Shared Shapes section updated to record types as defined with implementation pending. Client in-app API Docs updated with planned endpoint entry and `Planned — v1.4` badge; 5 new `ApiDocsView` tests added. `validate:contract` updated with a skip note for the unimplemented endpoint. `connections.test.ts` added with 14 shape tests covering all types, status unions, UDP modes, and the fully populated response. Runtime implementation remains pending (Slices 3–7).

- **v1.4 Slice 1 — TypeScript server forwarder coverage hardening** — raised `tcp-forwarder.ts` from 68.5% to 100% statements and 100% functions; raised `udp-forwarder.ts` from 57.7% to 84.3% statements and 100% functions. Server overall coverage: 71.9% → 79.6% statements. Added 7 new TCP forwarder tests (idempotent start/stop, bind failure, connection opened/closed events, target-unreachable error, post-bind server error, stop-with-active-connections) and 13 new UDP forwarder tests (lifecycle, one-way forwarded event, forwarded rate-limiting, last-client stats and returned event, multi-client timer reset, session opened/closed events, multi-client forwarded rate-limiting, listen/target/session socket error handlers). Remaining UDP gaps (multi-client send/return error callbacks and a race guard) require mocking to trigger deterministically; documented in `docs/coverage-baseline.md`. No forwarding behavior, API contracts, or runtime behavior changed.

- **Coverage baseline measured** — pre-v1.4 statement coverage: tools/cli 92.7% (gate 92%), client 89.2%, service 79.7%, shared 82.1%, server 71.9%. Scripts not yet measured. Full per-file breakdown and ratchet plan in `docs/coverage-baseline.md`.
- **Coverage tooling added** — `@vitest/coverage-v8` installed; vitest.config.ts files added/updated in all three TypeScript workspaces (client, server, shared) with coverage include/exclude settings; `coverage` script added to each workspace; `npm run coverage:shared/server/client/service/cli/baseline` scripts added to root `package.json`; `scripts/coverage-service.js` and `scripts/coverage-cli.js` added for Go combined coverage reporting; `scripts/validate-coverage.js` added as unified coverage validator with `--only <component>` support, replacing `scripts/validate-cli-coverage.js`; `validate:cli:coverage` now delegates to `validate-coverage.js --only cli`.
- **`waitForTestCondition` timeout raised** — `service/sources/forwarders/tcp_test.go` timeout increased from 2s to 5s to eliminate a flaky failure under cross-package coverage instrumentation with sequential (`-p 1`) test execution.

---

## [1.3.0] — 2026-06-08

### Added (v1.3 Slice 8 — Readiness audit, coverage gate, version bump)

- **CLI test coverage gate** — `npm run validate:cli:coverage` runs Go tests with cross-package coverage instrumentation and fails if total statement coverage falls below 88%. `scripts/validate-cli-coverage.js` documents genuinely untestable branches (main entry point, http.NewRequest errors, json.Marshal errors). Coverage gate included in `validate:cli`.
- **Coverage tests added** — new tests fill coverage gaps: `output/output_test.go` (FormatBytes MB/GB path, FormatTimestamp RFC3339Nano and invalid paths), `main_test.go` (run() full dispatch for all 9 commands + flag/help/version/unknown branches), and targeted additions to `list_test.go`, `activity_test.go`, `configcmd_test.go`, `status_test.go`, `diagnose_test.go`, `start_test.go`, `stop_test.go`, and `diagnosticscmd_test.go` covering formatProto all UDP modes, --help flags for all subcommands, RunConfig dispatch through all three subcommands, parseLocalConfig edge cases (empty file, invalid array, invalid object, missing rules field, rules not array, null rules), ActiveUDPSessions branch, empty Checks branch, non-connection API errors, per-rule diagnose errors, and JSON output with warningCount.
- **Total CLI test coverage: 90.1%** across sources, client, commands, and output packages.
- **Version bumped to 1.3.0** — `package.json` and `tools/cli/sources/version/version.go`.

### Added (v1.3 Slice 7 — CLI packaging into runtime and release artifacts)

- **CLI binary included in runtime package** — `portier`/`portier.exe` is now built and included in `build/portier/` alongside `service`/`service.exe`, `server.js`, `web/`, and `readme.txt`.
- **Platform build scripts updated** — `scripts/windows/build-runtime.ps1`, `scripts/macos/build-runtime.sh`, and `scripts/linux/build-runtime.sh` each build the CLI from `tools/cli/` directly into the output directory as part of the normal `npm run build:runtime` step.
- **Runtime validation updated** — `scripts/validate-runtime.js` now requires the CLI binary (`portier`/`portier.exe`), verifies it is non-empty, verifies CLI and service binaries have different names, and checks that `readme.txt` mentions `portier runtime`, `portier list`, and `portier diagnostics export`.
- **Release archive validation updated** — `scripts/validate-release.js` now requires the CLI binary in portable archives and validates `readme.txt` CLI command references.
- **Windows installer updated** — `scripts/windows/release/portier.iss` now includes `portier.exe` in the installed files under `{app}`.
- **`readme.txt` updated** — all three platform readme.txt templates now list the files in the package, document CLI usage (six key commands), and note that the CLI requires a running service and does not start the service by itself.
- **`build:clean` updated** — `scripts/clean-build.js` now removes `tools/cli/build/` alongside the other build output directories.
- **No PATH integration in v1.3** — the installer does not add `portier` to PATH; users invoke the CLI by full path or add the install directory to PATH manually.

### Added (v1.3 Slice 6 — Diagnostics export command)

- **`portier diagnostics export --out <file>`** — builds a local JSON diagnostics bundle from the running Portier service. Fetches runtime info, rules, statuses, and recent activity independently; partial failures are recorded in `errors[]` rather than aborting the export. Bundle is always written as long as the file write succeeds.
- **`--run-diagnostics` flag** — when present, runs `POST /api/forwards/:id/diagnose` for each rule sequentially and includes results in `diagnostics.<ruleId>`. If the rules list could not be fetched, per-rule diagnostics are skipped entirely. Per-rule failures are recorded in `errors[]` with source `diagnostics:<ruleId>`.
- **`--activity-limit <n>` flag** — controls how many activity events are included (1–500, default: 100). Exits code `2` if out of range.
- **Bundle schema** (`schemaVersion: "1"`): `app`, `runtime`, `rules`, `statuses`, `diagnostics`, `diagnosticsNote`, `activity` (with `included`/`events`/`note`), `metadata` (`managementUrl`, `source: "cli"`, `generatedBy`), `errors[]`. Does not include logs, environment variables, OS usernames, or raw disk files.
- **Human output**: `Exported diagnostics to <file>` with rule/status/activity counts; with partial failures: `Exported diagnostics with warnings to <file>`.
- **JSON output** (with `--out`): `{"ok":true,"path":"...","ruleCount":N,"statusCount":N,"activityCount":N,"diagnosticCount":N}`; with warnings adds `"warningCount":N`. Without `--out`: prints full bundle to stdout.
- **Human mode without `--out`**: exits code `2` with clear message.
- **Diagnostics subcommand routing** (`RunDiagnostics`): `portier diagnostics help` / `portier diagnostics <unknown>` handled cleanly.
- **`BaseURL()` method** added to API client for embedding the management URL in the bundle metadata.
- **Tests**: 153 CLI tests total (was 132); new tests cover dispatch, usage validation (missing `--out`, invalid `--activity-limit`), happy path (writes file, human/JSON output), partial failure (still writes bundle, "with warnings"), `--run-diagnostics` calls diagnose per rule, activity limit passed to API, rules failure skips diagnostics, write failure exits nonzero, bundle content (schemaVersion, metadata.source, diagnosticsNote, counts), no forbidden fields.
- **Help text updated**: `diagnostics` listed in `portier help`; `portier diagnostics help` shows subcommand list and options.

### Added (v1.3 Slice 5 — Config commands: validate, export, import)

- **`portier config validate <file>`** — validates a local config file without importing it or contacting the service; accepts raw JSON arrays, `{"rules":[...]}` wrapper objects, and full export objects `{"version":"1","exportedAt":"...","rules":[...]}`. Checks: non-empty name, valid protocol, non-empty hosts, ports in range, valid `udpMode`, no duplicate listen bindings. Human output: `Config is valid.` / `Config is invalid.` with errors listed. `--json` output: `{"valid":true,"ruleCount":N,"tcpCount":N,"udpCount":N,"errors":[]}`. Exit 0 valid, exit 1 invalid/unreadable.
- **`portier config export --out <file>`** — exports current rules from the running service to a JSON file via `GET /api/config/export`; file is written only after a successful API response (no partial writes). `--json` + `--out`: prints `{"ok":true,"path":"...","ruleCount":N}`. `--json` without `--out`: prints raw config JSON to stdout (useful for piping). Human mode without `--out`: exits with code 2 (requires `--out`).
- **`portier config import --mode merge|replace [--yes] <file>`** — reads and validates a local config file, then calls `POST /api/config/import`. Invalid files are rejected locally without contacting the service. Replace mode requires `--yes` — without it exits with code 2 and shows a clear warning. No interactive prompts. Human output: `Imported config using merge|replace mode.`; `--json`: `{"ok":true,"mode":"merge"|"replace"}`.
- **Config subcommand routing** (`RunConfig`): `portier config help` / `portier config <unknown>` handled cleanly.
- **New API client types**: `ConfigRule`, `ConfigExportResponse`, `ConfigImportRequest`, `ImportResult`, `ConfigImportResponse` mirroring `GET /api/config/export` and `POST /api/config/import` response shapes.
- **New API client methods**: `ExportConfig()`, `ImportConfig(req)` using a new `doWithBody` helper for POST with a JSON body.
- **Tests**: 132 CLI tests total (was 89); new tests cover `ExportConfig`/`ImportConfig` client methods, all validation cases (valid shapes, malformed JSON, invalid protocol/port/udpMode/host/name/duplicate binding), all export cases (writes file, JSON output, stdout mode, API error leaves no file, connection failure), all import cases (merge/replace modes, `--yes` requirement, local validation blocks API, bad mode, connection failure), and config dispatch.
- **Help text updated**: `config` listed in `portier help`; `portier config help` shows subcommand list; each subcommand shows usage and flags.

### Added (v1.3 Slice 4 — Lifecycle and diagnostics commands: start, stop, diagnose)

- **`portier start <id|name>`** — resolves rule by exact ID or exact name, calls `POST /api/forwards/:id/start`; human output shows `Started <name>  (<listen> → <target>)`; `--json` prints `{"ok": true, "action": "start", "ruleId": "..."}`.
- **`portier stop <id|name>`** — resolves rule, calls `POST /api/forwards/:id/stop`; human output shows `Stopped <name>`; `--json` prints `{"ok": true, "action": "stop", "ruleId": "..."}`.
- **`portier diagnose <id|name>`** — resolves rule, calls `POST /api/forwards/:id/diagnose`; human output shows summary status + message and a CHECK/STATUS/MESSAGE table; `--json` prints raw `RuleDiagnosticsResult`.
- **Safe rule resolver** (`commands.ResolveRule`): exact ID match wins unconditionally; exact name match succeeds if unique; duplicate names produce exit code 2 with an ID/NAME/PROTO/LISTEN disambiguation table on stderr; no match exits 1.
- **API client methods**: `StartForward(id)`, `StopForward(id)`, `DiagnoseForward(id)` added to the management API client; shared `do(method, path, out)` helper extracted to remove duplication between GET and POST paths.
- **New types**: `DiagnosticCheck`, `DiagnosticSummary`, `RuleDiagnosticsResult` mirroring the `POST /api/forwards/:id/diagnose` response shape.
- **Tests**: 89 CLI tests total (was 59); new tests cover `StartForward`/`StopForward`/`DiagnoseForward` client methods, all resolver cases (exact ID, exact name, ID wins over same-text name, duplicate ambiguity, not found, empty list), and all three command handlers (human output by ID and by name, JSON output, missing arg, ambiguity, connection failure, not found).
- **Help text updated**: `start`, `stop`, `diagnose` listed in `portier help`; rule identity note added.

### Added (v1.3 Slice 3 — Read-only commands: list, status, activity)

- **`portier list`** — calls `GET /api/forwards`; human-readable aligned table (NAME, PROTO, LISTEN, TARGET, ENABLED); `--json` prints raw `ForwardRuleResponse[]`. Empty state shows a friendly message. UDP mode shown compactly in PROTO column (`udp/1way`, `udp/lc`, `udp/mc`).
- **`portier status`** — calls `GET /api/status`; human output joins rule names and protocols via `GET /api/forwards` (falls back to ruleId if unavailable); shows NAME, PROTO, STATE, CONNS, BYTES IN, BYTES OUT, LAST ERROR; `--json` prints raw `ForwardStatus[]`.
- **`portier activity`** — calls `GET /api/activity`; human output shows TIME, SEV, TYPE, RULE, MESSAGE; `--json` prints raw `ActivityEvent[]` (not the `{"events":[...]}` API wrapper). Filters: `--limit` (1–500, default 50), `--rule` (rule ID), `--type` (event type), `--severity`; invalid `--limit` exits with code 2.
- **Output helpers** (`tools/cli/sources/output/`): `FormatBool`, `FormatBytes`, `FormatTimestamp`, `PrintTable` — used by all human-readable command output.
- **`exitWithError` helper** in `commands` package — shared connection/API error handling across commands.
- **Tests**: 59 Go CLI tests total (was 22+); new tests cover `GetForwards`, `GetStatus`, `GetActivity` client methods (success, error, query params, empty), plus all three command handlers (human output, JSON output, empty state, connection failure, API error, filter passing, limit validation, ruleId fallback).

### Added (v1.3 Slice 2 — Go CLI skeleton and API client)

- **`tools/cli/` module** — new Go module (`portier/cli`) under `tools/cli/` for the Portier CLI. Separate from the Go service (`portier/service`); the CLI is an API client, not a second runtime.
- **CLI binary name**: `portier` (Linux/macOS) / `portier.exe` (Windows). Background service binary remains `service` / `service.exe`.
- **`portier runtime` command** — calls `GET /api/runtime`; displays name, version, runtime, platform/arch, uptime, management URL, config path, static dir, service mode. Works with both the Go service and TypeScript server.
- **`--json` flag** — prints machine-readable JSON from the API response; no decorative text around JSON output.
- **Connection options** — `--url`, `--host`, `--port`, `PORTIER_URL` env var; precedence: `--url` > `--host`/`--port` > `PORTIER_URL` > default (`http://127.0.0.1:47831`).
- **`portier help` / `--help` / `-h`** — shows purpose, default URL, global flags, available commands, planned commands, exit codes.
- **`portier version` / `--version`** — shows `Portier CLI <version>`.
- **Exit code policy**: `0` success, `1` general/API error, `2` invalid arguments/usage, `3` connection failure.
- **`User-Agent` header**: `PortierCLI/<version>` on every API request.
- **Structured error handling**: `ConnectionError` (maps to exit 3 + hint) and `APIError` (non-2xx response with `errors[]` shape).
- **Tests**: 22+ Go tests using `httptest` covering URL resolution precedence, runtime human/JSON output, connection failure, API errors, User-Agent, invalid JSON, and error type unwrapping.
- **npm scripts**: `build:cli`, `test:cli`, `validate:cli`.
- **`tools/cli/readme.md`** — CLI usage, global flags, commands, exit codes, module structure, planned commands.

---

## [1.2.0] - 2026-06-07

### Goal

Portier v1.2 improves operational confidence with diagnostics, visibility, and safer networking UX. See `docs/roadmap.md` for goals, slices, and non-goals.

### Added (v1.2 Slice 7 — Diagnostics export / support bundle)

- **Download Diagnostics JSON** button in Settings, below Runtime / Environment. Assembles a local JSON support bundle and downloads it directly in the browser — no upload, no backend endpoint.
- **Bundle contents**: `schemaVersion`, `exportedAt`, `app` (name + version), `runtime` (from `GET /api/runtime`), `rules` (fresh fetch), `statuses` (fresh fetch), `diagnostics` (any results already run in the current UI session), `activity` (up to 100 recent events), and `metadata` (management URL, source, generatedBy).
- **Partial-failure handling**: each data source is fetched independently using `Promise.allSettled`. If any source fails, the rest are still included; an `errors` array is added to the bundle and a warning is shown in the UI. The download still proceeds.
- **Empty diagnostics note**: if no rule diagnostics have been run in the current session, the bundle includes `diagnosticsNote: "No rule diagnostics had been run in this UI session."` alongside an empty `diagnostics` object.
- **Excluded data**: raw config file contents from disk, logs, environment variables, OS user name, home directory beyond paths already in the runtime endpoint, node_modules, and build file listings. Nothing is uploaded or transmitted.
- **Filename format**: `portier-diagnostics-YYYYMMDD-HHMMSS.json` (local time; no characters invalid on Windows).
- **`diagnosticsExport.ts`** helper module added at `client/sources/features/settings/`: exports `buildDiagnosticsBundle()`, `buildDiagnosticsFilename()`, and `downloadJson()`.
- **`settings-warn` CSS class** added for the partial-data warning state.
- **New tests**: `diagnosticsExport.test.ts` (28 unit tests covering bundle structure, diagnostics inclusion, partial failure, filename, and download mechanics); 10 new `SettingsView` component tests covering render, fetch calls, success/partial/disabled states, bundle schema, and diagnostics from UI state. 216 tests total pass.

### Added (v1.2 Slice 6 — Safer networking UX)

- **Listen host presets** in the Add/Edit Rule drawer: "Local only" (127.0.0.1) and "LAN exposed" (0.0.0.0) quick-select buttons. Active preset is highlighted; manual host entry remains fully supported.
- **Inline LAN warning** in the rule form: when listenHost is `0.0.0.0`, a warning block appears directly below the listen host/port row stating that the rule listens on all interfaces and that Portier does not create firewall rules automatically.
- **Listen host hint text**: "Only this computer can connect" shown for 127.0.0.1; "Other devices on your network may be able to connect if firewall rules allow it" shown for 0.0.0.0.
- **Firewall note advisory card**: shown in the advisory section when listenHost is `0.0.0.0`. Platform-aware: shows a Windows-specific message if `runtimePlatform="windows"` is provided, otherwise a generic OS note. `ForwardRuleForm` now accepts an optional `runtimePlatform` prop.
- **Friendly conflict error copy**: when a save fails because the listen binding is already in use, the form shows "Another rule is already using this protocol, listen host, and listen port. Choose a different listen port, or stop/remove the conflicting rule." instead of the raw server error.
- **Improved LAN_EXPOSURE advisory message** (shared): updated to "Listening on 0.0.0.0 exposes this forwarded port on all interfaces. Other LAN devices may be able to connect if firewall settings allow it."
- **New tests** (ForwardRuleForm): Local only / LAN exposed presets set correct host; custom host entry still works; LAN warning visible for 0.0.0.0; LAN warning absent for 127.0.0.1; generic and Windows-specific firewall notes; save not blocked by LAN warning; friendly conflict error shown; privileged port advisory. 17 new/updated test cases.

### Added (v1.2 Slice 5 — Settings / runtime / config polish)

- **App version in sidebar footer**: Sidebar now shows `v{version}` below the management host. Sourced from new `PORTIER_APP_VERSION` constant in `@portier/shared`.
- **Copy buttons in Runtime / Environment**: Config path, static dir, and management URL each have a small "Copy" button. Uses `navigator.clipboard.writeText`; shows "Copied!" on success, "Failed" on error, resets after 2 seconds. Gracefully handles environments where clipboard is unavailable.
- **Export Config UX**:
  - Filename is now datetime-stamped: `portier-config-YYYYMMDD-HHMMSS.json` (local time).
  - Button renamed to "Download Config (JSON)" for clarity.
  - Helper text now states "The Activity Log is not included" so users know what the export covers.
  - Export shows a success confirmation message; errors are displayed inline instead of via `alert()`.
- **Import Config UX**:
  - Import mode (Merge / Replace) is now shown above the file picker with descriptive explanations, so users understand the difference before selecting a file.
  - Merge description: "adds rules from the file; skips rules with conflicting listen port bindings."
  - Replace description: "stops and deletes all current rules, then applies imported rules. Export a backup first if you want to keep your current rules."
  - Replace confirmation dialog now includes an "Export current config as backup" button for a one-click safety exit.
- **`PORTIER_APP_VERSION`** constant exported from `shared/sources/index.ts`.
- **New tests** (SettingsView): import mode descriptions visible before file selection; import button absent until valid file loaded; replace confirm shows backup export button; copy config path/static dir/management URL; clipboard failure shows "Failed" feedback; export success/error feedback. 18 new test cases.
- **New tests** (Sidebar): 6 tests covering render, nav click, open state, management host, and version footer.

### Added (v1.2 Slice 4 — Activity Log polish)

- **View activity** button (Activity icon) added to every rule row in the Forward Rules table. Clicking it navigates to the Activity view and pre-filters to that rule.
- Activity view **filter banner**: when navigating from a rule row, a status banner shows "Filtered to rule: [name]" with a Clear button. Handles deleted/missing rule names gracefully by displaying the raw rule ID.
- Activity view **type filter**: new dropdown lets users filter events by event type (rule lifecycle, connection, packet, session, config events). Type is passed as a query param to `GET /api/activity`.
- **Clear filters** button appears in the Activity header when any filter (rule, severity, type) is active.
- **Export JSON** button in the Activity footer exports currently loaded/filtered events as a JSON file. Filename format: `portier-activity-YYYYMMDD-HHMMSS.json`. Payload includes `exportedAt`, `filters`, and `events`. Client-side only; no backend endpoint.
- **Clear Log** button in the Activity footer calls `DELETE /api/activity` and empties the displayed list.
- Packet event **throttle note**: small explanatory text in the Activity body — "High-frequency packet events may be summarized or throttled; counters remain exact in rule status."
- `DELETE /api/activity` endpoint on both the TypeScript server and Go service: clears the in-memory activity log and returns `204 No Content`. Does not affect rules or forwarding state.
- `clearActivity()` API helper added to `client/sources/api/portierApi.ts`.
- `Store.Clear()` method added to `service/sources/activity/store.go` (Go).
- `Manager.ClearActivity()` method added to `service/sources/manager/manager.go` (Go).
- `docs/api-contract.md` updated with `DELETE /api/activity` section.
- Client in-app API Docs view updated with `DELETE /api/activity` entry.
- 18 new `ActivityLogView` component tests (21 total); 4 new `ForwardRuleList` activity navigation tests; 2 new `ApiDocsView` tests.
- 3 new TypeScript server API tests for `DELETE /api/activity`; 3 new Go API tests.
- `validate:contract` updated with `DELETE /api/activity` scenario and post-clear empty-array verification.

### Added (v1.2 Slice 3 — rule diagnostics UI)

- **Diagnose** action button (stethoscope icon) added to every rule row in the Forward Rules table.
- Clicking Diagnose calls `POST /api/forwards/:id/diagnose` and shows a collapsible diagnostics panel inline below the rule row — no navigation required.
- Diagnostics panel states: loading spinner while in flight, pass/warn/fail summary with timestamp, per-check rows with pass/warn/fail/skip status, and error display for API failures.
- Duplicate Diagnose clicks are prevented while a request is in flight for that rule; other rules remain clickable.
- Re-running Diagnose replaces the previous result immediately (pending → result).
- Closing the panel (✕ button) clears the result; deleting a rule also clears its diagnostic state.
- `diagnoseForwardRule(ruleId)` API helper added to `client/sources/api/portierApi.ts`.
- `DiagnosisEntry` union type exported from `ForwardRuleList.tsx` for App-level state management.
- `RuleDiagnosticsPanel` component added at `client/sources/features/forwards/RuleDiagnosticsPanel.tsx`.
- CSS for diagnostics panel added to `client/sources/styles/styles.css`.
- 11 new `RuleDiagnosticsPanel` unit tests; 9 new `ForwardRuleList` diagnostics tests; 2 new `App` integration tests (133 total client tests).

### Added (v1.2 Slice 2 — rule diagnostics API)

- `POST /api/forwards/:id/diagnose` endpoint on both the TypeScript server and Go service: runs diagnostic checks against an existing forward rule without changing rule state or opening long-lived sockets.
- Response shape: `RuleDiagnosticsResult` — ruleId, ruleName, protocol, summary (`pass`/`warn`/`fail`), checks array, diagnosedAt timestamp.
- Checks implemented: `listen-host`, `lan-exposure`, `privileged-port`, `common-port`, `listen-bind`, `target-host`, `target-connect`, `udp-mode` (UDP only).
- `listen-bind` skips the bind attempt when the rule is running and returns `pass` — Portier owns the socket.
- `target-connect` is always `skip` for UDP rules (UDP reachability cannot be verified without a protocol-specific response).
- `udp-mode` warns for `bidirectional-last-client`; passes for `one-way` and `bidirectional-multi-client`.
- `DiagnosticStatus`, `DiagnosticCheck`, `DiagnosticSummary`, `RuleDiagnosticsResult` types exported from `@portier/shared`.
- `getRule(id)` method added to `ForwardManager` (TypeScript server).
- `diagnoseRule()` helper added to `server/sources/diagnose.ts` (TypeScript) and `service/sources/api/diagnose.go` (Go).
- `DiagnosticCheck`, `DiagnosticSummary`, `RuleDiagnosticsResult` types added to `service/sources/domain/domain.go`.
- `validate:contract` updated with diagnose scenarios: unknown rule 404, TCP reachable target, UDP target-connect skip, LAN exposure warning, response shape verification.
- TypeScript tests and Go tests added for all check scenarios.

### Added (v1.2 Slice 1 — runtime info)

- `GET /api/runtime` endpoint on both the TypeScript server and Go service: returns name, version, runtime (`"node"`/`"go"`), platform, arch, uptimeSeconds, startedAt, managementHost/Port, configPath, staticDir, serviceMode, and pid.
- `RuntimeInfo` type exported from `@portier/shared`.
- `fetchRuntimeInfo()` API helper in the client.
- Settings view — **Runtime / Environment** section: shows live runtime info (runtime, version, platform/arch, uptime, management URL, config path, static dir, service mode, PID). Gracefully shows "Runtime information is unavailable from this backend." for older backends.
- Go service version package (`service/sources/version/version.go`): default `"dev"`, injectable at build time via `-ldflags`.
- `validate:contract` updated to verify GET /api/runtime shape and runtime field for both runtimes.

### Added (post-v1.1, pre-v1.2)

- Config compatibility fixtures (`tests/fixtures/config/`): 8 valid and 8 invalid `rules.json` fixtures covering TCP, all UDP modes, both config shapes (raw array and Go wrapper), and all field-level error categories.
- `npm run validate:config` — fixture-based compatibility runner (`scripts/validate-config.js`): validates config load, HTTP API import/export, UDP mode defaults, duplicate binding rejection, and invalid fixture rejection against the TypeScript runtime and the Go service when binary is available. Never reads or writes the user's real `rules.json`.

---

## [1.1.0] - 2026-06-06

### Goal

Portier v1.1 makes Portier easy and safe to install as a native background service on Windows, macOS, and Linux, with automated package/service validation and clean release artifacts.

See `docs/installer-strategy.md` for scope, platform decisions, and implementation slices.

### Added

- Windows Inno Setup installer (`scripts/windows/release/portier.iss`): installs to `%ProgramFiles%\Portier\`, optional Windows Service registration with auto-start at boot, config at `%ProgramData%\Portier\rules.json`. Upgrade support: stops running service before overwriting binaries. Uninstall removes service registration and logs; preserves `rules.json` by default.
- `build-release.ps1` (`scripts/windows/release/`): reads version from `package.json`, runs `build:runtime`, calls ISCC.exe, produces `build/releases/windows/Portier-Setup-<version>.exe`.
- macOS install scripts updated: `install-launch-agent.sh` now auto-copies `build/portier/` to `~/Applications/Portier/` by default; adds `--source-dir`, `--no-start`, and `--runtime service|node` options; fixes label consistency bug (`com.portier.port-forwarding` everywhere).
- `uninstall-launch-agent.sh` adds `--purge` flag for removing config and logs (config is preserved by default).
- `scripts/macos/release/build-release.sh` — builds `build/releases/macos/portier-portable-macos-<version>.tar.gz` from `build/portier/`.
- Signing and notarization documented in `scripts/macos/readme.md` (unsigned local builds supported; Developer ID signing documented as required for public distribution).
- Linux `install-service.sh` updated: auto-copies `build/portier/` to `/opt/portier/` by default; adds `--source-dir`, `--no-enable`, `--no-start`, and `--runtime service|node` options.
- `scripts/linux/release/build-release.sh` — builds `build/releases/linux/portier-<version>-linux.tar.gz` from `build/portier/`.
- `scripts/linux/readme.md` updated: install flags table, release archive section, firewall notes, journald commands, `--no-enable` documented.
- `scripts/windows/service/validate-user-install.ps1`: validates user-scope scheduled task flow with test name `PortierTestUser`, isolated temp dirs, auto-port detection, `-NoBuild`/`-KeepFiles`/`-Port` flags; never touches production.
- `scripts/windows/service/validate-machine-service.ps1`: validates machine-scope Windows Service flow with test name `PortierTestMachine`; requires Administrator; same flags; never touches production.
- `scripts/macos/service/validate-launch-agent.sh`: validates LaunchAgent flow with test label `com.portier.test`, temp plist at `~/Library/LaunchAgents/com.portier.test.plist`; no sudo required; `--no-build`/`--keep-files`/`--port` flags.
- `scripts/linux/service/validate-systemd-service.sh`: validates systemd flow with test unit `portier-test.service`, temp paths under `/tmp/portier-test-<pid>/`; requires root; fails clearly if not root or systemd unavailable.
- `scripts/validate-service.js`: cross-platform dispatcher — Windows runs user-scope, macOS runs LaunchAgent, Linux runs systemd; fails clearly on unsupported platforms.
- `npm run validate:service:current` / `validate:service:windows:user` / `validate:service:windows:machine` / `validate:service:macos` / `validate:service:linux` — explicit release validation commands.

- `scripts/build-release.js`: unified release packaging script for all platforms. Reads version from `package.json`, calls `build:runtime`, produces portable archives (Windows `.zip`, macOS/Linux `.tar.gz`) and Windows installer (non-fatal if Inno Setup absent). Service binaries are platform-native; run on each target OS.
- `scripts/validate-release.js`: validates `build/releases/<platform>/` layout, archive contents (required/forbidden files, readme.txt content), and optional installer artifact.
- Updated `readme.txt` in Windows/macOS/Linux build scripts: now includes portable archive notice ("does not install OS services"), `--config` / `--static-dir web` options, "not bundled in this archive" note for config.
- `npm run build:release` / `build:release:current` — full release packaging for current platform (portable + installer if available).
- `npm run build:release:portable` — portable archive only, skip installer.
- `npm run validate:release` / `validate:release:current` — validate release artifacts for current platform.
- `npm run validate:release:portable` — validate portable archive only.

### Deferred

- macOS `.pkg` installer via `pkgbuild`/`productbuild` (requires macOS tooling; deferred to future release).
- Linux `.deb` / `.rpm` packages (deferred beyond v1.1).

---

## [1.0.0] - 2026-06-06

### Added

- Native Go service runtime in `service/`, preferred for production packages while the TypeScript server in `server/` remains supported as the reference and fallback runtime.
- TCP forwarding in both runtimes with real listeners, bidirectional piping, status counters, and lifecycle cleanup.
- UDP forwarding in both runtimes across all supported modes: `one-way`, `bidirectional-last-client`, and `bidirectional-multi-client`.
- Rule CRUD, start/stop, update/restart behavior, duplicate listen binding rejection, persistent config, config import/export, and rule reorder.
- Activity Log with bounded in-memory events for rule lifecycle, TCP connections, UDP packets, UDP multi-client sessions, config export, and config import.
- Dark React web UI with Dashboard, Forward Rules, Activity, Settings, API Docs, mobile sidebar, add/edit/delete drawer flows, error banners, LAN exposure warnings, and common/recommended port advisories.
- Playwright E2E coverage for app load, add/edit/delete flows, start/stop, settings import, mobile navigation, and real TCP/UDP forwarding.
- Go service tests for config, manager behavior, API routes, TCP forwarding, UDP modes, activity events, validation, options, and advisories.
- Windows, macOS, and Linux packaging/install docs and scripts, including Go service mode and Node/TypeScript server fallback where applicable.

### Changed

- Repository convention standardized on `sources/` for source directories, `build/` for generated output, and `scripts/` for executable automation; platform docs and templates are co-located under `scripts/{platform}/`.
- Production package layout standardized to a flat install directory:

```text
<install-dir>/
  service          (or service.exe on Windows)
  server.js        (Node fallback, requires Node.js)
  web/
    index.html
    assets/
```

- Native Go service static directory defaults to `web` for packaged layout. Repository development commands may still pass `../client/build`.
- TypeScript server remains supported and can serve either repository `client/build` or packaged `web` via `--static-dir` / `PORTIER_STATIC_DIR`.
- Runtime config remains external in all modes; `rules.json` is not baked into binaries or packages.
- Manual QA scope narrowed to platform/package/service install behavior because core TCP/UDP correctness is automated.

### Validation

- Root validation scripts cover lint, typecheck, unit/integration tests, Go tests, build, Playwright E2E, package smoke checks, and repository hygiene checks.
- Real protocol coverage is automated for TCP, UDP one-way, UDP bidirectional-last-client, and UDP bidirectional-multi-client.

---

## [0.1.0] - 2026-06-04

First usable internal release of Portier, a local TCP/UDP port forwarding manager for development and LAN testing.

### Added

- TypeScript server with REST API on `127.0.0.1:47831` by default.
- TCP forwarding, UDP one-way forwarding, and UDP bidirectional-last-client forwarding.
- React management UI for listing, creating, editing, starting, stopping, and deleting forward rules.
- Shared TypeScript validation, common port advisories, recommended forward port range, and LAN exposure warnings.
- External JSON config path via `--config` / `PORTIER_CONFIG`.
- Static web serving via `--static-dir` / `PORTIER_STATIC_DIR`.
