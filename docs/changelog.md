# Changelog

All notable changes to Portier are documented here.

---

## [Unreleased] — v1.14 — TypeScript Server Migration to NestJS (in progress)

v1.14 is an **architecture-migration release, not a feature release**: it moves the TypeScript Node fallback server from a single Express app to a NestJS modules/controllers/services structure while preserving the existing REST API contract, error taxonomy, static client serving, and Go-service parity. The Go service remains the preferred runtime; the CLI and web UI work unchanged; `validate:contract` stays the parity source of truth (still **234/234**). The migration is incremental and reversible — the existing Express server stays the active runtime until a NestJS replacement is explicitly validated.

### Added — Slice 8: first NestJS write endpoint (`DELETE /api/activity`) + explicit request/response DTOs

The first migrated mutation endpoint, plus a corrected DTO rule: **every migrated endpoint now has explicit request DTOs (where input exists) and explicit response DTOs (always)**.

- **Migrated `DELETE /api/activity`** (`server/sources/nest/api/activity/`): clears the in-memory activity log and returns `204` with an empty body, matching Express exactly. Selected as the lowest-risk write — the paired mutation for the already-migrated `GET /api/activity`, reuses the `ACTIVITY_STORE` token, touches **no** forwarder lifecycle, sockets, rule persistence, import/export, or manager restart.
- **Narrow write capability:** added an `ActivityClearer { clear(): void }` interface alongside `ActivityReader`; the `ACTIVITY_STORE` value satisfies both (domain `ActivityStore` has `list` + `clear`), and `ActivityService` narrows to `ActivityReader & ActivityClearer`. `ActivityController.clear()` is `@Delete() @HttpCode(204)` returning `void` (Nest defaults `DELETE` to `200`, so `@HttpCode(204)` matches the Express `res.status(204).end()`).
- **DTO rule (request + response) applied to all migrated endpoints:** added explicit **response DTOs + pure mappers** (the HTTP boundary between domain/runtime data and JSON): `PortsAdvisoryResponseDto`/`toPortsAdvisoryResponseDto`, `ActivityListResponseDto`/`toActivityListResponseDto`, `StatusListResponseDto`/`toStatusListResponseDto`, `ForwardsListResponseDto`/`toForwardsListResponseDto`. Controllers now map the service result through the mapper (a fresh copy) instead of returning raw domain objects; services return domain data. The mappers preserve the Express JSON shape byte-for-byte (array-vs-object, property names, empty arrays/objects) and the `@portier/shared` types are documented as the REST contract shape (no internal fields leak). **Request DTOs:** `GET /api/ports/advisory` keeps `PortsAdvisoryQueryDto` (+ `ApiValidationPipe`, Slice 7); `GET /api/activity`'s query stays endpoint-local coercion (documented no-validation parity exception); `GET /api/status`/`GET /api/forwards`/`DELETE /api/activity` have **no request input → no request DTO** (documented). `DELETE /api/activity` is `204` with **no body → no response DTO** (the absent body is the response, matching Express — the response-side analogue of the no-input rule).
- **Parity byte-for-byte:** `DELETE` on a populated and an empty store both return `{ status: 204, body: null }` identically to Express (shared `ActivityStore` instance used by both apps); a subsequent `GET /api/activity` returns `{ events: [] }` in both runtimes (proves the clear). All read parity (`GET /api/activity` ×10, `/api/ports/advisory`, `/api/status`, `/api/forwards`) still byte-for-byte after the mapper change; `/health`, the `/api/*` 404 envelope, and non-API no-leak re-confirmed. `validate:contract` stays **234/234** (Express untouched).
- **Still scaffold-only:** served only under `npm run start:nest`; the Express server remains the default active runtime.
- **100% coverage of all new/changed Nest code** (4 response DTO mappers, 5 controllers, activity service `list`/`clear`); 133 nest tests (13 added: 4 service/controller `clear` + 8 mapper + adjustments). Each mapper is tested byte-for-byte, for empty + populated input, that it does **not mutate the source**, and that it returns a fresh object. Server coverage 99.0%/95.5%/100% (gate 95/92/99 PASS, none lowered). Replay validation was **not** run — no replay/shared-validation files were touched.

### Added — Slice 7: DTO-based NestJS API request-validation foundation

Proper Nest DTO validation (`class-validator` + `class-transformer` + a validation pipe) for migrated `/api` routes — no write endpoint migrated.

- **DTO + custom pipe (`class-validator` / `class-transformer`):** a request DTO (`server/sources/nest/api/ports/ports-advisory.query.dto.ts`) declares validation/coercion with decorators; a shared `ApiValidationPipe` (`server/sources/nest/common/api-validation.pipe.ts`) runs `plainToInstance` + `validate` and converts failures into the contract envelope via `ApiBadRequestException(messages)` → `toApiError` → `400 { errors: [...] }`. The pipe takes the **DTO class explicitly** (`@Query(new ApiValidationPipe(PortsAdvisoryQueryDto))`) rather than inferring it from `design:paramtypes`, because the esbuild/Vitest transform doesn't emit decorator metadata — the same reason DI uses explicit `@Inject` tokens, and it keeps behaviour identical between `tsc` builds and tests. It is applied per-route (no global pipe), so non-API routes are unaffected.
- **Express-faithful coercion (byte-for-byte parity):** `PortsAdvisoryQueryDto` coerces `port` via `@Transform(Number)` and validates `@IsInt`/`@Min(1)`/`@Max(65535)` (all three share the exact Express message so any failure surfaces the same string), `purpose` via `@IsIn(["management","forward"])`, and `listenHost` via `@Transform(typeof === "string" ? value : undefined)` so a non-string never errors. The pipe uses `whitelist: true` (extra query params are ignored, not rejected — matching Express; never `forbidNonWhitelisted`) and `stopAtFirstError: true` (one message per field). `PortsController` now delegates to `PortsService.getAdvisories(dto)` (validation lives in the pipe; the service just computes via the shared `getPortAdvisories`).
- **`GET /api/activity` kept endpoint-local (documented exception):** activity has **no validation errors** — its `limit`/filters are pure silent coercion-with-fallback (always `200`). A class-validator DTO would add a transform-only DTO + pipe with no validation value and risk obscuring the coercion, so per the correction's guidance the activity coercion stays endpoint-local (`parseActivityLimit` + `typeof === "string"`, unchanged). `StatusService`/`ForwardsService` have no query validation.
- **Parity byte-for-byte:** `GET /api/ports/advisory` parity covers valid / invalid-port (`70000`) / non-integer (`1.5`) / non-numeric (`abc`) / invalid-purpose / missing-port / **extra-param** — all match Express. `GET /api/activity`, `GET /api/status`, `GET /api/forwards` parity, `/health`, the `/api/*` 404 envelope, and non-API no-leak all re-confirmed. `validate:contract` stays **234/234** (Express untouched). (Note: an *entirely-empty* both-fields-invalid query accumulates all field errors — idiomatic — vs Express's first-error; this is not a contract/required parity case and the Nest route is shadow-only.)
- **Dependencies:** added `class-validator` and `class-transformer` (server package only); no new npm advisories.
- **100% coverage of all new/changed Nest code** (`ports-advisory.query.dto.ts`, `api-validation.pipe.ts`, `ports.service.ts`, `ports.controller.ts`); 120 nest tests. Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS, none lowered). JSON-body DTO validation is deferred to the write-migration slice (its shape follows the write endpoints). Replay validation was **not** run — no replay/shared-validation files were touched.

### Added — Slice 6: migrate `GET /api/forwards` into NestJS

The next manager-backed read endpoint, reusing the Slice-5 read-provider pattern.

- **Migrated `GET /api/forwards`** (`server/sources/nest/api/forwards/`): `ForwardsController` (transport adapter) → `ForwardsService` (maps each rule to a `ForwardRuleResponse` — `{ ...rule, advisories }` — using the shared `getPortAdvisories`, exactly as the Express route's private `toRuleResponse`) → a narrow **`ForwardsReader { listRules(): ForwardRule[] }`** behind the **`FORWARDS_READER`** token (default = trivial `emptyForwardsReader`). Returns the `ForwardRuleResponse[]` array verbatim; read-only and always `200` (no error branch). Only the **list read** is migrated — every write/lifecycle route under `/api/forwards/...` (`POST`/`PATCH`/`DELETE`/`reorder`/`:id/start|stop`/`groups`/`diagnose`) stays with Express.
- **Why this endpoint:** exact-parity-safe — rule fields are static and `getPortAdvisories` is deterministic, so there are no volatile fields. It reuses the Slice-5 manager-read provider/fake pattern and is the natural step before `/api/connections`/`/api/config/export` (both deferred — they carry `generatedAt`/`exportedAt` timestamps).
- **Provider pattern:** mirrors `STATUS_READER` — production code holds no manager/store dependency; the domain `ForwardManager` satisfies `ForwardsReader` and is bound to the token in tests (and when Nest becomes active) via `@nestjs/testing` `overrideProvider`. No advisory/contract constants duplicated (reuses `@portier/shared` `getPortAdvisories`, like the ports endpoint).
- **Parity** proven byte-for-byte for an empty manager and a seeded manager (two stopped rules, including a `0.0.0.0` rule so the response carries a `LAN_EXPOSURE` advisory) by sharing the **same `ForwardManager` instance** between Express (`createApp(manager)`) and Nest. No real sockets/forwarders started (rules stopped).
- **Existing parity preserved:** `GET /api/ports/advisory`, `GET /api/activity`, and `GET /api/status` parity still pass; `/health`, the `/api/*` 404 envelope, and non-API no-leak re-confirmed. The Slice-1 `app.integration.test.ts` "unknown /api route" probe was changed from `/api/forwards` (now migrated) to `/api/not-migrated`.
- **Still scaffold-only:** served only under `npm run start:nest`; the Express server remains the default active runtime and its `/api/forwards` is unchanged. `validate:contract` stays **234/234**.
- **100% coverage of all new/changed Nest code** (forwards reader/service/controller/module, `app.module.ts`); 118 nest tests (9 added). Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS, none lowered).

### Added — Slice 5: migrate `GET /api/status` into NestJS (runtime/manager read-provider pattern)

The first **manager-dependent** read endpoint, establishing the narrow runtime-read provider seam.

- **Migrated `GET /api/status`** (`server/sources/nest/api/status/`): `StatusController` (transport adapter) → `StatusService` (returns the injected reader's `listStatus()`) → a narrow **`StatusReader { listStatus(): ForwardStatus[] }`** behind the **`STATUS_READER`** token (`status.reader.ts`). The response is the `ForwardStatus[]` array verbatim, matching Express. Read-only and always `200` (no error branch).
- **Why this endpoint:** chosen as the smallest manager-dependent read with **no volatile fields** — `ForwardStatus.startedAt` only appears for *running* rules, and counters are `0`/absent when stopped, so seeding **stopped** rules (which endpoint tests must do anyway — no sockets) gives **exact byte-for-byte parity** with no normalization. (`/api/config/export` and `/api/connections` carry `exportedAt`/`generatedAt` timestamps and were skipped for that reason.)
- **Runtime/manager read-provider pattern:** the production status code holds **no manager/store dependency** — the `STATUS_READER` token defaults to a trivial `emptyStatusReader` (`{ listStatus: () => [] }`, the scaffold has no runtime wired). The real domain `ForwardManager` satisfies `StatusReader` and is bound to the token in tests (and when Nest becomes the active runtime). This seam is what the remaining manager-backed read endpoints (`/api/forwards`, `/api/connections`) will reuse.
- **Parity** proven byte-for-byte for an empty manager and for a seeded manager (two stopped tcp/udp rules): the **same `ForwardManager` instance** is shared between Express (`createApp(manager)`) and Nest (`STATUS_READER` overridden via `@nestjs/testing`'s `overrideProvider`), so identical data + identical `listStatus()` give identical output. Added `@nestjs/testing` (server devDependency, Nest 11) — justified because a `ForwardManager` requires a backing store and cannot be cleanly seeded post-construction via `app.get` the way the mutable `ActivityStore` was; the seeded manager stays in test code. No new npm advisories.
- **Existing parity preserved:** `GET /api/ports/advisory` and `GET /api/activity` parity still pass; `/health`, the `/api/*` 404 envelope, and non-API no-leak re-confirmed. No real sockets/listeners/forwarders are created (rules stay stopped).
- **Still scaffold-only:** served only under `npm run start:nest`; the Express server remains the default active runtime and its `/api/status` is unchanged. `validate:contract` stays **234/234**.
- **100% coverage of all new/changed Nest code** (status reader/service/controller/module, `app.module.ts`); 109 nest tests (8 added). Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS, none lowered).

### Added — Slice 4: migrate `GET /api/activity` into NestJS (injected-store DI pattern)

The next safe read-only endpoint, establishing the inject-a-dependency-behind-a-token/fake pattern.

- **Migrated `GET /api/activity`** (`server/sources/nest/api/activity/`): `ActivityController` (transport adapter — extracts the optional `limit`/`ruleId`/`type`/`severity` query params) → `ActivityService` (maps them to the store's `ActivityListParams` with the SAME coercion as Express — `limit` defaults to 100 / clamps to 500, filters pass through — then delegates to the injected store). Read-only and **always `200 { events: [...] }`** (no error branches). Only the read is migrated; `DELETE /api/activity` (a mutation) stays with Express and is deferred.
- **Why this endpoint:** chosen over `GET /api/runtime` because activity gives **true byte-for-byte parity** — its response carries no inherently time/process-varying fields (runtime's `uptimeSeconds`/`pid` would force a parity normalization) — and it best establishes the **dependency-injection-behind-a-narrow-token** pattern the heavier manager-dependent endpoints (status/forwards/connections) will reuse.
- **DI seam:** a narrow `ActivityReader { list(params): ActivityEvent[] }` interface + `ACTIVITY_STORE` token (`activity.service.ts`); the module defaults it to a fresh in-memory `ActivityStore` (the domain store, not Express internals). Tests use `app.get(ACTIVITY_STORE)` to seed the Nest app's own store and share that exact instance with the Express app, so identical data + identical `list()` logic give byte-for-byte parity — **no `@nestjs/testing` dependency needed**.
- **Express↔Nest parity** proven across 10 query variations (no filter, `limit` valid/zero/non-numeric/over-max, each filter, an unknown `ruleId` → `[]`, and a combined filter); `/health`, the `/api/*` 404 envelope, and non-API no-leak all re-confirmed; `GET /api/ports/advisory` parity still passes after the new module import.
- **Still scaffold-only:** served by the Nest app only under `npm run start:nest`; the Express server remains the default active runtime and its `/api/activity` is unchanged. `validate:contract` stays **234/234**.
- **100% coverage of all new/changed Nest code** (activity controller/service/module, `app.module.ts`); 101 nest tests (26 added): `parseActivityLimit` table, service param-mapping via a recording fake reader, controller delegation, and the integration + parity suite. Server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS, none lowered).

### Added — Slice 3: shared NestJS `/api` error-envelope layer

A reusable error layer so migrated Nest controllers never hand-roll the Portier `{ errors: [...] }` envelope.

- **Shared error-envelope layer** (`server/sources/nest/common/`): `api-error-envelope.ts` holds the pure `toApiError(exception)` mapping (+ `isApiPath`); `api-error-envelope.filter.ts` is a global catch-all `ApiErrorEnvelopeFilter` (registered via `APP_FILTER`) that applies the envelope to `/api/*` routes and leaves non-API routes on NestJS's default error shape; `api-errors.ts` adds `ApiBadRequestException(errors: string[])` so controllers raise a `400` with a plain `string[]` and the envelope shape lives in one place.
- **Deterministic mapping** (`toApiError`): an `HttpException` whose body is `{ errors: string[] }` keeps those messages; an unmatched-route `NotFoundException` → `404 { errors: ["API route was not found."] }` (preserves prior behavior); any other `HttpException` → its message wrapped as one error; any non-HTTP/unknown value → `500 { errors: ["Internal server error."] }` with **no stack trace or raw exception leaked**.
- **Controller cleanup:** `GET /api/ports/advisory` now throws `ApiBadRequestException(result.errors)` instead of `BadRequestException({ errors })` — no envelope literal in the controller. The success response and behaviour are unchanged.
- **Replaced** the Slice-1 `api-not-found.filter.ts` (which only caught `NotFoundException`) with the broader catch-all filter; the `/api/*` 404 envelope and non-API no-leak behaviour are preserved (re-confirmed by the existing integration + parity tests).
- **Parity unchanged:** the Express↔Nest parity suite still passes byte-for-byte (valid / invalid-port / invalid-purpose / missing-port), `/api/*` 404 still returns the envelope, non-API 404 still does not leak it, `/health` still works. `validate:contract` stays **234/234** (Express untouched).
- **100% coverage of all new/changed Nest code** (envelope mapping, filter, `ApiBadRequestException`, ports controller); 75 nest tests (every `toApiError` and filter branch, including the non-API string-body and unknown-error 500 branches via a mock `ArgumentsHost`). Server coverage 99.0%/95.0%/100% (gate 95/92/99 PASS, none lowered).

### Added — Slice 2: API parity harness + first read-only endpoint migration

A contract-safe parity harness and the first `/api` route migrated into Nest, with the Express server still the default runtime.

- **API parity harness** (`server/sources/nest/testing/api-parity.ts`) — a small, server-test-local helper that boots an Express handler or a NestJS app on an ephemeral loopback port (`startHandlerServer` / `startNestServer`), captures `{ status, body }` (`fetchApi`), and deterministically compares two responses (`stableStringify` recursively sorts object keys; `diffApiResponses` returns the reasons two responses differ). It lets a test fire the same request at the existing Express route and the new Nest route and assert byte-equivalent behaviour. Not a framework.
- **First migrated endpoint — `GET /api/ports/advisory`** (read-only, deterministic, **no runtime-manager dependency**): `PortsModule` → `PortsController` (transport adapter; extracts query params, maps the result to `200` advisories or a `400 { errors: [...] }` envelope via `BadRequestException`) → `PortsService` (`resolveAdvisories` validates `port` is an integer 1–65535 and `purpose` is `management`/`forward` with the SAME rules as Express, then delegates to the shared `getPortAdvisories`). The Nest route is **byte-for-byte identical** to the Express route (proven by the parity harness across valid, invalid-port, invalid-purpose, and missing-port cases).
- **Still scaffold-only / not the active runtime:** the migrated route is served by the NestJS app only when launched via `npm run start:nest`. The **Express server remains the default active runtime** and its `/api/ports/advisory` route is unchanged — both serve the endpoint identically. No contract change: `validate:contract` stays **234/234**.
- **100% coverage of all new Nest code** — every new `sources/nest/` file (parity harness, ports controller/service/module) is at 100% (statements/branch/functions); 62 nest tests total (31 added): harness units (stable-stringify/diff/boot/fetch incl. the empty-body branch), `PortsService` branch table (valid with/without `listenHost`, non-string `listenHost`, every invalid port/purpose), `PortsController` units (200 + 400-envelope branches), and a real-Nest integration test that also runs the Express↔Nest parity comparison and re-confirms `/health`, the `/api/*` 404 envelope, and no-envelope-leak on non-API 404s.
- **Deferred:** static serving, `/api/health`/`/api/runtime` parity, and any write/lifecycle endpoint — all later slices. No packaging change.

### Added — Slice 1: NestJS server migration scaffold

A safe, contract-preserving NestJS foundation that does **not** replace the existing server.

- **NestJS scaffold under `server/sources/nest/`** — `app.module.ts`, `app.factory.ts` (`createNestApp()`), `main.ts` (bootstrap), a `health/` feature (controller → service → module), and `common/api-not-found.filter.ts`. The existing Express server (`server/sources/index.ts` + `server/sources/api.ts`) is untouched and remains the active/fallback runtime.
- **Minimal, contract-safe surface:** `GET /health` returns `{ ok: true, server: "node", name: "Portier" }` (a liveness probe requiring no runtime manager). It is deliberately served **outside the frozen `/api` contract** — the documented `GET /api/health` is "TypeScript server: not implemented", so aligning to `/api/health` (and `/api/runtime`) is deferred to the endpoint-migration milestones, where it will be done behind `validate:contract`. A global `ApiNotFoundFilter` returns the contract envelope `{ "errors": ["API route was not found."] }` for any unmatched `/api/*` route; non-API 404s keep NestJS's default body.
- **Layered by design:** controllers are transport adapters only; behaviour lives in services; features compose as modules. DI is wired with explicit `@Inject` tokens so it behaves identically under `tsc` and the esbuild-based Vitest transform.
- **Dependencies (server package only):** `@nestjs/common`, `@nestjs/core`, `@nestjs/platform-express` (`^11`, aligned with the existing Express 5), `reflect-metadata`, `rxjs`. Nest 11 targets Express 5, matching the server's existing `express ^5.1.0`.
- **Scripts (additive — none renamed or removed):** `build:nest`, `start:nest`, `test:nest` in `@portier/server`. `start:nest` binds `127.0.0.1` only (never `0.0.0.0`) and defaults to port `47832` so it cannot collide with the management server's default `47831`.
- **Build output stays under `build/`** (`server/build/nest/…`), not `dist/`.
- **Tested meaningfully (31 tests):** health service/controller units, `ApiNotFoundFilter` units (path classification table + contract-envelope vs default-404 branches), `resolveNestListenOptions` env-resolution branches (defaults, custom host, valid port, invalid `0`/negative/non-numeric/non-integer/empty → default), `bootstrap`/`reportBootstrapFailure` (boots on an ephemeral port → `/health`; sets a failing exit code on failure), and an integration test that boots the real Nest app and asserts `/health`, the `/api/*` 404 envelope, no envelope leak on non-API 404s, and 127.0.0.1 binding.
- **100% meaningful coverage of all new NestJS code.** Startup logic is extracted into covered files (`nest-options.ts`, `bootstrap.ts`) so the only coverage-excluded file is the **logic-free `main.ts` process entry** (importing it would start a listener — same precedent as `index.ts`); no broad exclusions, no gate lowered.
- **Validation:** `validate:contract` **234/234** (no API/contract change), every `sources/nest/` file 100% covered, server coverage 98.6%/94.1%/100% (gate 95/92/99 PASS — no gate lowered), `validate:coverage:replay` PASS (96.5%), and lint/typecheck/build clean. **No release or tag.**

---

## [1.13.0] — 2026-06-14 — Local Replay & Offline Analysis

v1.13 adds a **standalone offline replay/analysis tool beside the CLI** — the `replay` binary (`replay.exe` on Windows), a separate Go module under `tools/replay/` (`portier/replay`), **not** a `portier workflow replay` subcommand. It analyzes saved Portier workflow artifacts after the fact and is strictly offline and read-only — it never executes workflows, contacts the runtime, reads the files an artifact references, mutates inputs, imports `tools/cli`/runtime clients, uploads, collects logs/env/process data, or enforces policy. The Portier CLI remains the operational artifact **producer**; `replay` is the offline **analyzer**. All replay schemas are local tool schemas; **`validate:contract` stays 234/234** (no shared/server/service/client/API/DTO/contract change — the replay tool is wholly independent of the runtime).

**Highlights**

- **Standalone replay tool** under `tools/replay/` — its own stdlib-only Go module, separate binary `replay`/`replay.exe`, built by `npm run build:replay`.
- **Five offline commands:** `replay plan` (what analysis an artifact supports), `replay analyze` (deterministic findings/insights), `replay timeline` (ordered reconstruction with explicit synthetic vs saved events), `replay compare` (diff two artifacts; mixed-kind limited), and `replay explain` (explain emitted codes; preserves unknown codes; `--code`/`--list` modes).
- **Package structure:** command handling and core domain logic are separated — `tools/replay/sources/commands` (dispatch / arg parsing / runners) and `tools/replay/sources/core` (detection, local DTOs, models, builders, renderers, explanation registry). `core` never imports `commands`; neither imports `tools/cli`.
- **Consistent surface:** every command supports `--json`/`--out` with byte-identical parity, a `schemaVersion: 1` local schema, deterministic output, and the shared exit-code policy — `0` success (incl. no-codes / no-diff), `1` output-write/JSON-encode failure, `2` usage / unreadable / malformed / unsupported input; **no `3`** (replay never contacts a runtime). A `replay version` / `replay --version` surface (`tools/replay/sources/version/version.go`, tracking the overall Portier release) mirrors the CLI.
- **Safety boundaries** documented and test-covered (no workflow execution, no runtime contact, no referenced-file reads, no input mutation, no uploads/telemetry, no policy enforcement).
- **Hard replay coverage gate:** `tools/replay` has a machine-enforced **≥95% module-wide coverage gate** — the `replay` component in `scripts/validate-coverage.js` (run via `npm run validate:coverage:replay`), fed by the new reporter `scripts/coverage-tools-replay.js` (`npm run coverage:replay`). Verified at **96.5%** (core 96.7%, commands 94.9%). Existing CLI/workspace coverage gates are unchanged.

### Audited — Slice 7: Replay toolkit consistency audit — PASS

A focused audit of the standalone replay tool (Slices 1–6) before v1.13 finalization — audit/cleanup only, no new features, no behavior change. **Result: PASS — the v1.13 replay toolkit is ready for finalization.**

- **Command surface / help:** verified all five commands (`plan`/`analyze`/`timeline`/`compare`/`explain` + `explain --code`/`--list`) dispatch consistently with a uniform `--json`/`--out` convention, byte-parity, and exit codes (`0` success incl. no-codes/no-diff, `1` output-write/encode failure, `2` usage/unreadable/malformed/unsupported, **no `3`**). Added an explicit **Exit codes** block to the tool help and a help regression test asserting every command + the exit-code documentation.
- **JSON shapes:** confirmed all five local schemas carry `schemaVersion: 1`, arrays serialize as `[]` (never `null`), output is deterministic (every renderer iterates ordered slices — no map iteration), timeline always marks `synthetic`, explain preserves unknown codes as `known:false`, mixed-kind compare is clearly limited, and source-kind names are consistent (`workflow-run-report`/`workflow-plan-report`/`workflow-history-export`/`workflow-report-bundle`, plus `replay-analysis`/`replay-timeline`/`replay-compare`). No replay schema leaks into the REST contract.
- **Safety / architecture (code inspection):** no `tools/cli`/runtime-client/`net/http`/`os/exec`/`os.Getenv` imports in product code; `core` never imports `commands`; bundle directories read only `manifest.json`/`report.json`/`explanations.json`; no referenced-file reads, no mutation beyond `--out`, no telemetry/upload/log/env/process collection.
- **Fixes (docs + tests, no behavior change):** corrected stale `tools/replay/README.md` intro (Slice 1 "scaffold/plan only" language → the five-command tool), completed the bundle-files statement (manifest/report/explanations + malformed-report tolerance), made the Exit-codes section command-generic and noted "no exit 3", refreshed the root `README.md` replay section to list all five commands; added **malformed-bundle-report-tolerated** tests for `timeline` and `compare` (the invariant was previously tested only for `analyze`).
- **Validation:** `validate:contract` **234/234**; replay module-wide coverage **96.5%** (core 96.7%, commands 94.9%; `go test -coverpkg=./...`), above the ≥95% bar; `test:replay`/`build:replay`/gofmt/vet/lint/typecheck clean. No release blockers; no new durable rule (the audit confirms the Slice 1–6 rules).
- **Replay coverage gate (v1.13 finalization):** added a hard **95% module-wide coverage gate** for `tools/replay`, enforced by `scripts/validate-coverage.js` (new `replay` component in its `GATES`, run via `npm run validate:coverage:replay` and included in the aggregate `npm run validate:coverage`). Coverage is gathered module-wide by the new reporter `scripts/coverage-tools-replay.js` (`go -C tools/replay test -coverpkg=./... ./...`, run as `npm run coverage:replay`, modeled on `scripts/coverage-tools-cli.js`), which writes `coverage/replay/coverage-summary.json`; `validate-coverage.js` reads it and applies the gate alongside the other components. Verified: passes at 96.5%; `--only replay` exits non-zero when the gate is forced above the actual. The replay module is a separate Go module (`portier/replay`) gated independently — existing CLI/workspace coverage gates are unchanged.

### Added — Slice 6: Replay explain command

- **`replay [--json] explain --from <file-or-dir> [--out <file>]`** — collects the emitted codes a saved artifact records and maps the known ones to local offline explanations. Also `replay explain --code <code>` (explain one code) and `replay explain --list` (list every known code); the three modes are mutually exclusive. Analysis only: no workflow execution, no runtime contact, no referenced-file reads, no mutation.
- **Inputs:** the four workflow artifacts (run/plan reports, history export, support-report bundle directory) **plus** replay tool outputs (analysis/timeline/compare JSON), detected by shape. Codes are deduped and **sorted ascending**; **unknown codes are preserved and marked `known: false`**, never dropped. An artifact with no codes is a success (exit 0) with a clear notice.
- **Local explanation registry** in `tools/replay/sources/core` (24 codes: `workflow.step.*`, `workflow.run.*`, `policy.*`) — it does NOT import the CLI explain registry or any runtime package. Doctor/runtime codes are out of scope (they never appear in workflow artifacts) and are reported as unknown if encountered.
- **Explain model (`schemaVersion: 1`, a local tool schema, separate from the plan/analysis/timeline/compare schemas):** `{source, codeCount, known, unknown, explanations:[{code, known, title, severity, meaning, suggestion}]}`. Human and JSON output; `--json --out` byte-identical.
- **Exit codes:** `0` explanations produced (including when no codes are found); `1` an output-write or JSON-encode failure; `2` a usage error (no/duplicate mode, bad flag) or an unreadable/malformed/unsupported artifact. No exit `3` — explain never contacts a runtime.
- Built by reusing the existing detection + the `compareFacts` extractor for workflow-artifact codes (no new duplicate parsing). Model/registry/extraction in `tools/replay/sources/core/explain.go`, rendering in `core/render_explain.go`, runner in `commands/explain.go`. `replay plan`/`analyze`/`timeline`/`compare` are unchanged. **Replay module coverage 96.5%** (core 96.7%, commands 94.9%; `go test -coverpkg=./...`) — meets the ≥95% module bar.

### Added — Slice 5: Replay compare command

- **`replay [--json] compare --left <file-or-dir> --right <file-or-dir> [--out <file>]`** — compares two saved artifacts offline and reports what changed. Analysis only: no workflow execution, no runtime contact, no referenced-file reads, no mutation.
- **Same-kind comparisons** produce a full deterministic diff — changed result/validity, step/run count deltas, added/removed/unchanged emitted-code sets, changed failed/skipped step sets (run), changed invalid step set (plan), history run-count deltas + workflow-distribution deltas + changed failed-run sets, and a changed most-recent run. **Mixed-kind comparisons** produce a deterministic *limited* result (the two source kinds + a clear "detailed comparison is limited" marker + a pointer to `replay analyze`), never a crash.
- **Compare model (`schemaVersion: 1`, a local tool schema, separate from the plan/analysis/timeline schemas):** `{left, right, summary{sameKind,changed,changeCount}, changes[], sets{<dim>:{added,removed,unchanged}}, insights[]}`. Changes are emitted in a stable semantic order (not map iteration); set values and workflow-distribution deltas are sorted ascending; `sets` slices are always `[]` not null. Human and JSON output; `--json --out` byte-identical.
- **Exit codes:** `0` comparison created; `1` an output-write or JSON-encode failure; `2` a usage error (missing `--left`/`--right`, bad flag) or an unreadable/malformed/unsupported artifact. No exit `3` — compare never contacts a runtime.
- Built on a single-parse `compareFacts` extractor that reuses the existing detection and raw structs. Model/builders in `tools/replay/sources/core/compare.go`, rendering in `core/render_compare.go`, runner in `commands/compare.go`. `replay plan`/`analyze`/`timeline` are unchanged. **Replay module coverage 96.4%** (core 96.7%, commands 94.7%; `go test -coverpkg=./...`) — meets the ≥95% module bar.

### Added — Slice 4: Replay timeline command

- **`replay [--json] timeline --from <file-or-dir> [--out <file>]`** — reconstructs a deterministic, ordered timeline of what a saved artifact says happened. Analysis only: no workflow execution, no runtime contact, no referenced-file reads, no mutation.
- **Per-artifact timelines:** run reports → a step timeline (status/exit code/codes per step); plan reports → a validation timeline (valid/invalid + validation codes); history exports → a run timeline in **newest-first** stored order (each event carries id, createdAt, workflow, result, per-run summary, codes); support-report bundle directories → a timeline from the bundle's own `manifest.json`/`report.json`.
- **Synthetic vs saved events:** each timeline brackets the artifact's saved events with clearly-marked synthetic lifecycle events (`*-start` / `*-result`/`*-end`); every event carries a `synthetic` boolean and the summary reports a `synthetic` count. No timestamps are invented — ordering is by saved order only.
- **Timeline model (`schemaVersion: 1`, a local tool schema, separate from the plan and analysis schemas):** `{source, workflow?, result?, events[], summary{total,passed,failed,skipped,valid,invalid,synthetic}}`. Human and JSON output; `--json --out` byte-identical (single marshaller).
- **Exit codes:** `0` timeline created; `1` an output-write or JSON-encode failure; `2` a usage error or an unreadable/malformed/unsupported artifact. No exit `3` — timeline never contacts a runtime.
- Model/builders in `tools/replay/sources/core/timeline.go`, rendering in `core/render_timeline.go`, command runner in `commands/timeline.go`; reuses the existing detection helpers. `replay plan` and `replay analyze` are unchanged. **Replay module coverage 96.4%** (core 97.0%, commands 94.8%) — meets the ≥95% module bar; the remaining uncovered code is structurally-unreachable defensive branches (encode-on-concrete-type, post-detection re-decode, `os.ReadFile`-after-stat, the unused-classifier `default` arm) plus the `main()` `os.Exit` wrapper. A `failWriter`-driven test covers the real stdout-write-failure path of `plan`/`analyze`/`timeline` under `--json`.

### Changed — Slice 3: Replay tool naming & package-structure polish

- **Binary renamed `portier-replay` → `replay`** (`replay.exe` on Windows). `npm run build:replay` now outputs `tools/replay/build/replay`; the previous Slice 1/2 entries above describe the same tool under its earlier `portier-replay` name.
- **Domain package `sources/replay` → `sources/core`** and **CLI dispatch extracted to `sources/commands`**. `main.go` is now a thin entry point (`os.Exit(commands.Run(...))`); `commands` owns argument parsing and the command runners and depends on `core`; `core` (artifact detection + replay plan/analysis models, builders, renderers) never depends on `commands`. Neither package imports `tools/cli`.
- Behavior-preserving: command output, exit codes, schemas, and the offline/read-only safety boundary are unchanged. Help text and command examples now read `replay …`. Docs and scripts updated. `validate:contract` stays **234/234**; replay package coverage held (core 97.5%, commands 90.5% — the dispatch handlers' only uncovered lines are the same unreachable marshal/write-error branches).

### Added — Slice 2: Replay analyze command

- **`portier-replay [--json] analyze --from <file-or-dir> [--out <file>]`** — the first real offline analysis command. It inspects a saved artifact and produces deterministic analysis **from the artifact contents only**.
- **Per-artifact analysis:** workflow **run** reports (step tallies, first/failed/skipped steps, emitted-code distribution, result-based findings/insights, runtime-unreachable/input-failed detection); workflow **plan** reports (valid/invalid tallies, invalid step IDs, `workflow.step.*` codes, plan readiness); workflow **history exports** (run tallies, workflow distribution, code distribution counting the **runs** containing each code, failed-run shortlist, most-common code, most-recent run, empty-export handling); workflow **support-report bundle** directories (step summary + codes from the bundle's own `manifest.json`/`report.json`/`explanations.json` only — no other file read).
- **Analysis model (`schemaVersion: 1`, a local tool schema, separate from the replay-plan schema):** `{source, workflow?, result?, summary{steps,runs}, codes[], workflows?, findings[], insights[]}`. Deterministic sorting (codes/workflows by count desc then name/code asc; IDs asc; stable insight order). Human and JSON output; `--json --out` byte-identical.
- **Exit codes:** `0` analysis created; `1` an output-write or JSON-encode failure; `2` a usage error or an unreadable/malformed/unsupported artifact. No exit `3` — analysis never contacts a runtime.
- **Safety boundary (held by construction, test-proven):** parses only the supplied artifact (or explicit support-bundle files); never executes workflows, contacts the runtime, reads the config/policy/baseline/report files an artifact refers to, mutates inputs, applies/imports, enforces policy, runs shell commands, uploads, or collects logs/environment/process data.
- The existing `portier-replay plan` command and the entire Portier CLI are unchanged. Replay package coverage: **94.1% → 97.5%** after adding focused tests (≈97.8% Slice-1 baseline maintained); remaining gaps are unreachable defensive branches.

### Added — Slice 1: Standalone replay tool scaffold

- **New tool `portier-replay`** (`tools/replay/`, module `portier/replay`, stdlib-only). It is a separate binary beside `portier` — not a CLI subcommand — and is independently testable. It does not import `tools/cli` or any runtime client package.
- **`portier-replay [--json] plan --from <file-or-dir> [--out <file>]`.** Detects an existing workflow artifact and prints a deterministic **replay plan** answering "What offline replay/analysis can this saved artifact support?" — it does **not** replay by re-executing anything.
- **Input detection by JSON/manifest shape (not filename):** workflow **run** reports (`workflow run --json --out`), workflow **plan** reports (`workflow plan --json --out`), workflow **history exports** (`workflow history export --out`), and workflow **support-report bundle** directories (`workflow report --out`, whose `manifest.json` is the only file read).
- **Replay plan model (`schemaVersion: 1`, a local tool schema — no REST/API contract).** Each plan lists `available` analyses gated on the inputs the artifact contains (step timeline, step-status summary, validation summary, history distributions, failed-run shortlist, explanation lookup, summary/report inspection) and `unavailable` analyses — always including the four the tool deliberately never performs: workflow re-execution, runtime probing, referenced-file reread, and mutation/enforcement. Human and JSON output; `--json --out` is byte-identical.
- **Exit codes:** `0` plan produced; `1` an output-write or JSON-encode failure; `2` a usage error or an unreadable/malformed/unsupported input artifact. No exit `3` — the tool never contacts a runtime.
- **Safety boundary (held by construction):** offline and read-only — never executes workflows, contacts the runtime, reads the config/policy/baseline/report files an artifact refers to, mutates inputs, applies/imports configs, enforces policy, schedules jobs, runs shell commands, uploads anything, or collects logs/environment/process data.
- **Scripts:** `npm run build:replay` / `test:replay` / `validate:replay`; `build:clean` now also removes `tools/replay/build`.

---

## [1.12.0] — 2026-06-14 — Local History & Observability

v1.12 stays **local-first** and adds local observability around safe operations — no telemetry, no background services, no schedulers, no mutation, no enforcement, no uploads. All new tooling lives in the Go CLI as a **pure API client**; `validate:contract` stays **234/234** (no runtime/API/server/service/client contract change). The **CLI coverage gate remains 97%** (actual ~97.8%); no gates were lowered.

The release adds an **opt-in compact local workflow run history** — `portier workflow run --file <workflow.json> --record-history` records one compact entry per completed run (run id, time, workflow name, result, summary counts, compact per-step `id`/`type`/`status`/`exitCode`, and the deduped+sorted emitted codes) in `<user-config-dir>/portier/workflow-history.json`, **bounded to the latest 100 runs**. It is inspected and managed with `workflow history list` / `show <run-id>` / `clear --yes`; archived with `workflow history export --out <file>` (a compact snapshot with an explicit all-false `safety` object and `--json --out` byte parity); navigated with `list`/`stats` **filters** (`--result`, `--workflow`, `--code`, `--limit`, AND-combined); summarized with `workflow history stats` (result/workflow/step-status/step-type/code counts); and trimmed with `workflow history prune` (retention `--keep <n>` or filtered `--result`/`--workflow`/`--code`, requiring `--yes`). A **Slice 6 consistency audit** verified the command surface, JSON schemas, exit codes, safety, and architecture (**PASS**). **Safety boundaries** (held throughout): history is opt-in and recorded only after a completed run; it stores **compact local metadata only** — never raw configs/policies, full reports, file contents, logs, environment variables, process data, secrets, runtime URLs, or tokens; history commands are fully **offline** (no runtime contact, no workflow execution, no referenced-file reads, no raw-report scanning); prune/clear require `--yes`; and there is no telemetry, upload, sync, scheduler, background automation, runtime mutation, policy enforcement, config apply/import, or shell execution.

### Slice 6: Workflow history toolkit consistency audit — PASS

A focused audit of the v1.12 workflow history/observability toolkit (Slices 1–5) — audit/cleanup only, no new features, no behavior change beyond small help/docs fixes. **Result: PASS — the v1.12 history toolkit is ready for finalization.**

- **Command surface** (verified via a built-binary battery against a redirected config dir, plus the white-box test suite): `workflow run --record-history` (recorded passed/failed runs; invalid plan records nothing) and `workflow history list/stats/show/export/prune/clear` dispatch consistently; all history commands are **offline** (a malformed `--url` is ignored — `RunWorkflowHistory` takes no connection flags). Exit codes confirmed: list/stats `0` (incl. empty match), show `0`/unknown-id `1`/missing-id `2`, export `0`/usage `2`, prune `0` (incl. removed 0)/no-mode·`--keep`+filter·missing-`--yes` `2`, clear `0`/missing-`--yes` `2`; no history command uses exit `3`.
- **JSON schemas** confirmed: list `{schemaVersion, filters?, shown, totalStored, runs}`; stats `{schemaVersion, filters?, totalStored, shown, results, steps, workflows[], stepTypes[], codes[]}`; export `{schemaVersion, createdAt, source, runCount, runs, safety}`; prune `{schemaVersion, removed, kept, totalBefore, totalAfter}`. Arrays are non-nil (`[]` when empty), `filters` appears only when active, `--json --out` export byte parity holds, and the eight `safety` flags are present and all false. Stored/exported entries carry compact metadata only (no full reports/messages/raw configs/runtime URLs/tokens).
- **Safety/architecture** (code-inspection): `workflow/history.go` imports only `output` — no `client`/`commands`/`os/exec`/`net/http`/`os.Getenv`; history commands never contact the runtime, execute workflows, read referenced files, scan raw reports, or collect logs/env/process data. Only `workflow run --record-history` writes history (after a completed run); prune/clear require `--yes`; mutations touch only the compact store (or the explicit export `--out`). `commands` stays handlers-only; the model/projection/filter/stats/prune/store logic lives in the `workflow` package; no import cycles, `go vet` clean.
- **Fixes made (4, all small, no behavior change):** (1) the `workflow history` help **usage line** was stale (`<list|show|clear>`) → now `<list|stats|show|export|prune|clear>`; (2) the `--json` options note now lists `list/show/stats/prune`; (3) the `--yes` options note now reads "the destructive operation (prune, clear)"; (4) the `workflow run` `--record-history` help and the glossary "Workflow run history" row now list the full `list|stats|show|export|prune|clear` set.
- **Validation:** `validate:contract` **234/234**; CLI coverage **97.8%** (gate 97 **PASS**, none lowered, not raised — the buffer over 97 is meaningful but still short of a stable 98%); `validate:cli`, lint, typecheck, `gofmt`, `go vet` all clean. No release blockers; no deferred non-blockers. No new durable rule — the audit confirms the Slice 1–5 rules.

### Added — Slice 5: Workflow history prune

- **`portier workflow history prune`.** Removes entries from the local history in one of two explicit modes — **retention** (`--keep <n>` keeps the newest `n`, removing older ones) or **filter** (`--result`/`--workflow`/`--code` remove matching entries, AND-combined). It is **destructive and requires `--yes`**. It mutates **only the compact local history store** — it never contacts the runtime, executes a workflow, reads workflow-referenced files, collects logs/env/process data, or touches configs/policies/reports.
- **Exactly one mode.** No mode, or `--keep` combined with filters, is a usage error (exit `2`). `--keep` must be a non-negative integer (`--keep 0` removes all); `--limit` is **not** accepted for prune. Filter values are validated like `list`/`stats` (unknown `--result`, empty `--workflow`/`--code` → `2`).
- **Empty match is a successful no-op** — a filter (or `--keep n >=` the entry count) matching nothing removes `0` and exits `0`. Missing history is treated as empty: prune succeeds with `removed 0, kept 0` and **creates no file**. A no-op prune does not rewrite the store.
- **Output.** Human prints `Removed`/`Kept`/`Total before`/`Total after`; `--json` emits `{ schemaVersion, removed, kept, totalBefore, totalAfter }`.
- **Exit codes.** `0` success (including `removed 0`); `1` a history read/write failure; `2` a usage error (no/ambiguous mode, invalid `--keep`/filter value, or missing `--yes`). No exit `3` — prune never contacts the runtime. Any usage error leaves the store untouched.

### Notes (Slice 5)

- CLI-only, `validate:contract` stays **234/234**, no API/DTO/contract change. The prune model + pure helper live in the `workflow` package (`workflow/history.go`: `HistoryPruneOptions`, `HistoryPruneResult`, `PruneHistory`, plus a `HistoryStore.Save` and a shared `runMatchesFilters` predicate factored out of `ApplyHistoryFilters`); `commands` stays **handlers-only** (`runWorkflowHistoryPrune` parses/validates the mode + `--yes`, loads via the new `loadHistoryStore`, and saves only when `removed > 0`). The `workflow` history code still never imports `client`/`commands` and never contacts the runtime; prune mutates only the history store.
- Covered by white-box `workflow/history_internal_test.go` (keep newest N, keep 0 removes all, keep ≥ count / negative-clamp / empty history, filter by result/workflow/code, AND composition, empty match, no-input-mutation, `Save` round-trip) and `commands/workflow_history_internal_test.go` (keep + keep-0 human, filter prune + AND no-op, empty match no-rewrite, missing `--yes` → 2 no-mutation, mode validation table → 2, empty/missing history → 0 no-file, read failure → 1, write failure → 1 (read-only file), usage-error no-mutation, path-resolution → 1, JSON-encode → 1, flag edges). CLI coverage **97.8%** (gate **97 PASS**, not lowered/raised — still short of a stable 98% buffer); the new prune helpers and handler are 100% covered.

### Added — Slice 4: Workflow history stats

- **`portier workflow history stats`.** Summarizes the (optionally filtered) local workflow run history by result, workflow name, step status, step type, and emitted code. Computed **only from compact local history metadata** — it never contacts the runtime, executes a workflow, reads workflow-referenced files, or scans raw reports.
- **Filters.** Reuses the Slice 3 filters exactly — `--result`, `--workflow`, `--code`, `--limit` — applied **before** computing stats, AND-combined. No new filter types.
- **Counts.** `totalStored`/`shown`, result counts (passed/failed), step status counts (passed/failed/skipped), and grouped counts for workflows, step types, and codes. **Code counts count the runs containing each code** (history stores deduped codes per run), not total occurrences. Grouped tables are sorted **count descending, then key ascending** (deterministic).
- **`--json`** emits `{ schemaVersion, filters?, totalStored, shown, results, steps, workflows[], stepTypes[], codes[] }`. The `filters` object appears **only when a filter is active** (and contains only the active fields); grouped arrays are always present (`[]` when empty).
- **Human output** shows an optional Filters block, `Runs`/`Steps` buckets, and the grouped tables (empty grouped tables are omitted). Empty/missing history prints a short `Result: no history` form.
- **Empty history and empty match sets are success** (exit `0` with zero stats). Exit codes: `0` success; `1` a history read failure; `2` an invalid filter value. No exit `3` — stats never contacts the runtime.

### Notes (Slice 4)

- CLI-only, `validate:contract` stays **234/234**, no API/DTO/contract change. The stats model + counting + deterministic sorting live in the `workflow` package (`workflow/history.go`: `HistoryStats`, `HistoryResultCounts`, `HistoryStepCounts`, `WorkflowCount`/`StepTypeCount`/`CodeCount`, `BuildHistoryStats`, the generic `sortCounts`); `commands` stays **handlers-only** (`runWorkflowHistoryStats`). The list/stats flag parsing+validation was factored into a shared `parseHistoryFilters`, and the store load into `loadHistoryForRead`, used by both `list` and `stats`; the human Filters block is a shared `printHistoryFilterBlock`. The `workflow` history code still never imports `client`/`commands` and never contacts the runtime; stats does not mutate the store.
- Covered by white-box `workflow/history_internal_test.go` (empty stats, result/step/workflow/step-type/code counts, **code counts = runs containing code**, deterministic count-desc/key-asc sorting, filtered stats, empty-match zero stats, no-input-mutation) and `commands/workflow_history_internal_test.go` (empty human/JSON with `[]` grouped arrays, JSON counts + sorting, human shape, per-filter + AND + limit-before-stats, empty-match → 0 zero stats, invalid filters → 2, read failure → 1, path-resolution failure → 1, JSON-encode failure → 1, store-not-mutated, flag edges). CLI coverage **97.7%** (gate **97 PASS**, not lowered/raised); the new stats helpers and handler are 100% covered.

### Added — Slice 3: Workflow history filtering

- **Filters on `portier workflow history list`.** `--result <passed|failed>` (exact run result), `--workflow <name>` (exact, case-sensitive), `--code <code>` (run whose `codes` list contains the code, exact match), and `--limit <n>` (keep at most the newest `n` matches). Filtering operates **only on the compact local history** — it never contacts the runtime, executes a workflow, reads workflow-referenced files, or collects logs/env/process data.
- **AND composition.** All active filters must match. `--limit` is applied **after** filtering, so it returns the newest `n` matches (the history is newest-first).
- **Empty match sets are success.** A filter that matches nothing (including an unknown `--code`, which may name an older code no longer explainable) returns an empty list and exits `0`.
- **Human output** shows an active **Filters** block and a `Summary` with `N shown` / `N total stored`; a no-match shows "No workflow history entries matched the filters." The pristine first-run case (nothing recorded, no filters) keeps its original guidance message.
- **`--json`** emits `{ schemaVersion, filters?, shown, totalStored, runs }`. The `filters` object is present **only when at least one filter is active**, and within it only the active fields appear (`omitempty`); `runs` is always an array (`[]` when empty).
- **Exit codes.** `0` success (including an empty match set); `1` a history read failure; `2` an invalid filter value (an unknown `--result`, an empty `--workflow`/`--code`, or a `--limit` that is non-positive or non-numeric). No exit `3` — listing never contacts the runtime.

### Notes (Slice 3)

- CLI-only, `validate:contract` stays **234/234**, no API/DTO/contract change. The filter model + matching + list-view live in the `workflow` package (`workflow/history.go`: `HistoryFilters`, `ApplyHistoryFilters`, `HistoryListView`, `BuildHistoryListView`, `ValidHistoryResult`); `commands` stays **handlers-only** (`runWorkflowHistoryList` parses/validates the flags via `fs.Visit` for set-vs-default and calls the workflow helpers). The `workflow` history code still never imports `client`/`commands` and never contacts the runtime; filtering does not mutate the store.
- Covered by white-box `workflow/history_internal_test.go` (no-filter pass-through, per-filter + AND composition + limit-after-filter + unmatched-code, no-input-mutation, `ValidHistoryResult` allowlist, list-view filters-omitted/included + non-nil empty runs) and `commands/workflow_history_internal_test.go` (result/workflow/code/limit, invalid result/empty workflow/empty code/invalid limits → 2, AND composition, JSON shape incl. `omitempty` filter keys, human Filters+Summary + no-match, empty-history-with-filters, read failure → 1, store-not-mutated). CLI coverage **97.6%** (gate **97 PASS**, not lowered/raised); the new filter helpers and handler paths are 100% covered.

### Added — Slice 2: Workflow history export

- **`portier workflow history export --out <file>`.** Writes a compact, deterministic JSON snapshot of the local workflow history to a file for manual archiving or sharing. Fully **offline and read-only** — it loads the history store, builds the snapshot, and writes it; it **never** mutates the store, contacts the runtime, executes a workflow, or reads the files a workflow step refers to.
- **Snapshot schema** (`schemaVersion: 1`): `createdAt` (RFC3339 UTC), `source: "workflow-history"`, `runCount`, `runs` (the compact history entries, newest first, exactly as stored — codes are not re-ordered or re-deduped), and an explicit **`safety`** object whose eight flags (`containsRawConfigs`, `containsRawPolicies`, `containsFullReports`, `containsLogs`, `containsEnvironment`, `containsProcessData`, `containsRuntimeUrls`, `containsTokens`) are **always false**, stating what the snapshot excludes. The snapshot carries the same compact metadata as the store — never raw configs, policies, full reports, host/port payloads, file contents, logs, environment variables, process data, secrets, runtime URLs, or tokens.
- **Empty/missing history** exports a valid snapshot with `runCount: 0` and `runs: []` and exits `0`.
- **Human output** confirms the file, run count, and a fixed safety block; **`--json`** (global) prints the same snapshot to stdout **byte-identical** to the file.
- **Exit codes.** `0` written (including an empty snapshot); `1` a history read failure, an output write failure, or a JSON-encode failure; `2` a missing `--out` value. The `--out` parent directory must already exist (consistent with the other single-file `--out` exports — `policy check --out`, `doctor --out`, `workflow template --out`); an existing file is overwritten.

### Notes (Slice 2)

- CLI-only, `validate:contract` stays **234/234**, no API/DTO/contract change. The export model + projection live in the `workflow` package (`workflow/history.go`: `HistoryExport`/`HistorySafety`/`BuildHistoryExport`, with `createdAt` injected for tests); `commands` stays **handlers-only** (`workflow_history.go`: `runWorkflowHistoryExport` loads the store, calls `BuildHistoryExport`, and writes via `output.WritePrettyJSON`/`output.PrintJSON`). The `workflow` history code still never imports `client`/`commands` and never contacts the runtime. The export reuses the stored `HistoryRun` entries verbatim (it does **not** mutate the store).
- Covered by white-box `workflow/history_internal_test.go` (empty snapshot emits `runs:[]`, populated preserves newest-first order + codes, safety all-false with all eight keys present, deterministic `createdAt`, does-not-mutate-input) and `commands/workflow_history_internal_test.go` (empty/populated export, run count, newest-first, no sensitive content, `--json`/file **byte parity**, missing `--out`/missing-value → 2, read failure → 1 with no file written, write failure → 1, store-not-mutated, JSON-encode failure → 1, path-resolution failure, flag edges). CLI coverage **97.6%** (gate **97 PASS**, not lowered/raised); the new `BuildHistoryExport`/`runWorkflowHistoryExport`/`printHistoryExportHuman` are 100% covered.

### Added — Slice 1: Local workflow run history

- **Opt-in local workflow run history.** `portier workflow run --file <workflow.json> --record-history` records a **compact** entry about a completed run in a local history file. Without `--record-history`, nothing is recorded — there is no always-on or background collection.
- **What is stored (compact metadata only):** run id, `createdAt` (RFC3339 UTC), workflow name, result, summary counts, compact per-step metadata (`id`/`type`/`status`/`exitCode`), and the deduped+sorted explanation/finding codes the run emitted. It **never** stores raw configs, raw policies, full embedded policy reports, file contents, logs, environment variables, process data, secrets, runtime URLs, or auth tokens. A step's embedded report and message are intentionally dropped.
- **Bounded retention.** The store keeps the most recent **100** runs (newest first), dropping the oldest when appending beyond the limit.
- **Run id.** `<YYYYMMDDThhmmssZ>-<sanitized-workflow>-<suffix>`, e.g. `20260614T101530Z-policy-baseline-check-a1b2c3d4` (the short suffix is random in production, injected in tests).
- **`portier workflow history list`** lists recorded runs (newest first) in human form, or a clear empty message when none; `--json` emits `{schemaVersion, runs:[…]}`.
- **`portier workflow history show <run-id>`** prints one recorded run (human or `--json`).
- **`portier workflow history clear --yes`** deletes the local history file (requires `--yes`; already-empty/missing is a success).
- **Exit codes.** `workflow run --record-history`: a history **write failure is reported as a warning** and never hides the workflow result — if the run passed, the exit code becomes `1`; if the run already failed (`1`/`3`), that code is kept; runtime unreachable stays `3`. An **invalid plan is never recorded** (only completed runs are). `workflow history`: `0` success (including `list` with no runs and `clear` when already empty), `1` an unknown run id (`show`) or a history read/write failure, `2` missing/invalid arguments (a missing run id for `show`, or `clear` without `--yes`).
- **Storage location.** `<user-config-dir>/portier/workflow-history.json` (resolved via `os.UserConfigDir()`); the path is injectable for tests.

### Notes (Slice 1)

- CLI-only, `validate:contract` stays **234/234**, no API/DTO/contract change. The history model, compact projection, run-id generation, and the bounded read/append/retention/clear store live in the `workflow` package (`workflow/history.go`: `History`/`HistoryRun`/`HistoryStep`, `ProjectRun`, `NewRunID`, `HistoryStore`). `commands` stays **handlers-only** (`workflow_history.go`: flag parsing, path resolution, rendering); the `workflow` history code never imports `client`/`commands` and never contacts the runtime.
- Covered by white-box `workflow/history_internal_test.go` (compact projection, no-sensitive-data-in-JSON, code dedupe/sort, run-id shape, load missing/empty/malformed, append ordering + parent-dir creation, retention keeps latest 100, schemaVersion default, clear incl. error) and `commands/workflow_history_internal_test.go` (record-history records a compact entry with no sensitive content, no-record-without-flag, failed run records `failed`, invalid plan does not record, history-write-failure exit behavior incl. runtime-unreachable keeping exit 3, list/show/clear human+JSON, unknown/missing id, read failures, flag-parse edges, path-resolution failures, defaults) + a `main_test.go` offline dispatch case.
- CLI coverage **97.5%** (gate **97 PASS**, not lowered, not raised — the ~0.5% statement buffer is too thin for 98). The only sub-100% lines are documented structurally-unreachable branches (the crypto/rand fallback in the id suffix and the post-`Load` filesystem-write-failure branch in the store, consistent with the rest of the CLI).

---

## [1.11.0] — 2026-06-13 — Local Intelligence & Workflow Automation

v1.11 stays **local-first** and makes Portier better at understanding, explaining, and automating local forwarding workflows. All new tooling lives in the Go CLI as a **pure API client** — no runtime/API/server/service/client contract change (`validate:contract` stays **234/234**), no enforcement, no scheduler, no background automation, no telemetry.

The workflow toolkit (Slices 1–8) lets an operator describe an ordered sequence of existing safe Portier operations in a small JSON file and then plan, preview, run, explain, and package it: a fully offline **workflow planner** (`workflow plan`) that validates the schema and step references; **stable workflow validation codes** (`workflow.step.*`) with inline/JSON explanations (`workflow plan --explain`) wired into the canonical `portier explain` registry; built-in **workflow templates** (`workflow template --list`/`<name>`/`--out` — `policy-check-local`, `policy-check-runtime`, `policy-review`, `policy-baseline-check`); a **runbook preview** (`workflow runbook`) that maps a valid workflow to the manual CLI commands; **read-only workflow execution** (`workflow run`) for the supported safe step types (`policy.check`, `policy.review`, `policy.baseline.compare`) with in-memory `reportFrom` handoff; **workflow run explanations** (`workflow run --explain`) with the `workflow.run.*` codes (`dependency_failed`, `runtime_unreachable`, `input_failed`); a **workflow support report** bundle (`workflow report --from <report.json> --out <dir>`) that packages an existing plan/run report for review or AI handoff; and a docs-only **AI handoff prompt** ([`prompts/workflow.md`](../prompts/workflow.md)). **Safety boundaries** (held throughout): no shell execution, no execution of the runbook display strings, no scheduler or background automation, no policy enforcement, no config apply/import, no runtime mutation, no target probing, no `allowUdp`/protocol allowlist, no telemetry/upload, and no env/process/log collection — `plan`/`template`/`runbook`/`report` are fully offline, and `workflow run` contacts the runtime only for a read-only runtime-config read in a `policy.check` runtime step. A **Slice 8 consistency audit** verified the command surface, JSON schemas, exit codes, safety, and architecture (**PASS**). The CLI also gained readable underscore-separated multi-word command test filenames, and the AI handoff prompts moved from `docs/prompts/` to `prompts/` at the repo root. The **CLI coverage gate remains 97%** (actual ~97.5%); no gates were lowered.

### Slice 8: Workflow toolkit consistency audit — PASS

A focused audit of the v1.11 workflow toolkit (Slices 1–7) — audit/cleanup only, no new features, no behavior change beyond small help/docs/test fixes. **Result: PASS — the v1.11 workflow toolkit is ready for finalization.**

- **Command surface** (verified via a built-binary battery across `plan`/`run`/`runbook`/`report`/`template` and `explain`): dispatch is consistent; offline subcommands (`plan`/`runbook`/`template`/`report`) never resolve or validate the runtime URL (a malformed `--url` is ignored), and only `workflow run` lazily contacts the runtime (read-only, for a `policy.check` runtime step). `explain --list` covers **44** codes; `explain <workflow.step.*>`/`<workflow.run.*>` resolve. No stale `cmd`-suffix or run-together test filenames remain.
- **JSON schemas:** plan `{schemaVersion,name,steps,summary,result(+explanations only with --explain)}`; runbook `{workflow,steps:[{id,type,command,display,notes}],summary,result}`; run `{workflow,steps:[{id,type,status,exitCode,message,report?}],summary,result(+explanations only with --explain)}`; template list `{templates:[…]}` / single `{name,title,description,workflow}` / bare workflow for no-`--json`+`--out`; report bundle `manifest.json`/`summary.txt`/`report.json` (normalized — embedded per-step reports dropped, no config/host/port payload)/`explanations.json` (`{}` when none). `--json --out` byte parity holds for plan/runbook/run (incl. `--explain`); `--explain` is additive/omitempty; passed/valid steps stay un-explained.
- **Exit codes** (battery-verified): plan 0/1/2 (+`--out` write 1); template 0/1/2; runbook 0/1/2 (+`--out` write 1); run 0/1/2/**3** (runtime unreachable) + `--out` write 1; report 0/1/2 (non-empty `--out` refused → 1; **no** exit 3).
- **Safety** (code-inspection): no `os/exec`/shell/syscall anywhere in the `workflow` package or workflow handlers; no `net/http`/telemetry/upload/`os.Getenv`; the `workflow` package's only direct OS write is the report bundle to the explicit `--out` directory; all referenced-file reads are **injected** from the command layer (`RunDeps.ReadFile`), so `plan`/`runbook`/`report` never open the files a step refers to. No apply/import/enforcement/scheduler/probing, no `allowUdp`/protocol allowlist, no input mutation.
- **Architecture:** `commands` stays handlers-only; the `workflow` package owns model/validation/templates/runbook/run-orchestration/report-packaging and imports only `config`/`policy`/`explain`/`output` — **not** `client`/`commands` (runtime access stays injected). One canonical `explain` registry/renderer; no second explanation schema; no import cycles; `go vet` clean.
- **Fixes made (3, all small):** (1) the top-level `workflow` help intro verb list omitted "report" → now "Plan, validate, run, preview, report on, or template …"; (2) a stale `tools/cli/readme.md` runbook note said workflow execution was "deferred to a later v1.11 slice" → now points to `workflow run`; (3) the exported `RunExplanations()` accessor had no test (0% coverage) → added a white-box test (now 100%; function coverage 99.2% → 99.6%).
- **Validation:** `validate:contract` **234/234**; CLI coverage **97.5%** (gate 97 **PASS**, none lowered, not raised — the ~0.5% statement buffer is too thin for 98); lint/typecheck/`gofmt`/`go vet` clean. No release blockers. The only sub-100% workflow functions are the **documented structurally-unreachable** branches (the `json.Marshal`-on-concrete-type encode paths in the emitters and the post-validation filesystem-write-failure paths in `WriteSupportReport`), consistent with the rest of the CLI. No new durable rule — the audit confirms the Slice 1–7 rules.

### Added — Slice 7: Workflow support report

- **`portier workflow report --from <report.json> --out <directory>`.** Packages an **existing** workflow plan/run JSON report (from `workflow plan --json --out` or `workflow run --json --out`) into a small, local diagnostic bundle for manual review or AI handoff. It is **fully offline and read-only** — a packaging step only. It **never** executes a workflow, runs a shell command, contacts the runtime, reads the config/policy/baseline/report files a step refers to, collects logs/environment/process data, mutates the input report, or uploads anything; it only parses the provided report and re-derives explanation metadata from the canonical registry.
- **Bundle contents.** `manifest.json` (schema version, RFC3339 `createdAt`, source `workflow-run`/`workflow-plan`, workflow, result, files), `summary.txt` (human summary: source/workflow/result, the step list with `[PASSED]`/`[FAILED]`/`[SKIPPED]`/`[VALID]`/`[INVALID]` tags, the explained codes, and a fixed **safety block** documenting what was and was not collected), `report.json` (a **normalized** report — each step's id/type/status/message + the explainable codes; it deliberately **drops** each run step's embedded policy/compare report so no rule host/port detail or config payload is carried), and `explanations.json` (canonical explanations for the emitted codes — `{}` when none, for a stable bundle shape).
- **Code extraction (canonical registry, deduped).** Invalid plan steps contribute their `workflow.step.*` code; failed/skipped run steps contribute `workflow.run.dependency_failed` (skip), or the **policy finding codes** embedded in a failed step (`policy.*`, from `findings`/`new`); and any codes already in a `--explain` report's `explanations` map (e.g. `workflow.run.runtime_unreachable`/`workflow.run.input_failed`) are carried through. Unknown codes are dropped by the canonical registry.
- **Exit codes.** `0` bundle written; `1` output directory create/write failure (a non-empty `--out` directory is refused, matching `portier support-bundle`); `2` missing/invalid arguments (including a missing `--from`/`--out` value) or an unreadable/malformed/unsupported input report. No exit `3` — the command never contacts the runtime.
- **AI handoff prompt.** Added [`prompts/workflow.md`](../prompts/workflow.md) — a docs-only, copy-paste prompt for interpreting a workflow report bundle (paste `summary.txt`/`report.json`/`explanations.json`). It warns against pasting secrets and explicitly forbids asking the assistant to invent config changes to apply blindly. Portier adds no AI integration/upload/telemetry.

### Notes (Slice 7)

- CLI-only, `validate:contract` stays **234/234**, no API/DTO/contract change. The report parser/detector, normalized model, explanation-code extraction, summary rendering, and bundle writer live in the `workflow` package (`workflow/report.go`: `SupportReport`/`SupportReportStep`/`SupportReportManifest`, `ParseSupportReport`, `ExplanationCodes`/`Explanations`, `WriteSupportSummary`, `WriteSupportReport` with an injected clock); explanations reuse the canonical `explain` registry merged with `policy` (`reportExplainRegistry`). `commands` stays handlers-only — `RunWorkflowReport` parses flags, reads the input, calls `workflow.ParseSupportReport`/`WriteSupportReport`, and reuses the existing `prepareBundleDir` directory convention. The `workflow` package does **not** import `client`/`commands`.
- Covered by white-box `workflow/report_internal_test.go` (run/plan detection, unsupported/malformed/empty/no-steps/bare-policy-report rejection, per-step code extraction incl. failed-compare `new` findings + malformed embedded report, dedupe + canonical-only explanations, status tags, summary shape + placeholders + empty `{}`, bundle write with fixed clock) and black-box `commands/workflow_report_test.go` (failed/passed run, invalid plan, `--explain` input carrying a run code, JSON manifest, **does-not-read-referenced-files + input-not-mutated**, missing-dir created, empty-dir OK, non-empty-dir refused→1 without touching unrelated files, malformed/unsupported→2, missing-args→2, unreadable→2, JSON-encode-failure→1, dispatch) + a `main_test.go` offline dispatch case.
- CLI coverage **97.5%** (gate 97 PASS, **not lowered**, not raised — the ~0.5% buffer is too thin for 98). The new helpers and handler are covered including error branches; the only uncovered lines are the documented structurally-unreachable filesystem-write-failure branches in `WriteSupportReport` (after the directory is validated writable, matching the `support-bundle` convention).

### Added — Slice 6: Workflow run explanations

- **`portier workflow run --file <workflow.json> --explain`.** Adds additive explanations so a failed or skipped step is easier to understand. For a failed `policy.check`/`policy.review` it explains the **policy finding codes** the step produced; for a failed `policy.baseline.compare` it explains the **underlying finding codes** of the new findings; for a step skipped because its `reportFrom` dependency produced no report it explains a new **`workflow.run.dependency_failed`** code; for a `policy.check` runtime step that couldn't reach the runtime it explains **`workflow.run.runtime_unreachable`**; and for any unreadable/malformed referenced input (config/policy/baseline/report file, or the runtime config) it explains **`workflow.run.input_failed`**. **Passed steps are never explained** (no noise).
- **Additive only.** `--explain` does **not** change which steps run, their status, the run summary, the run result, or the **exit code**. Human output adds an indented `Code:`/`Meaning:`/`What to do:`/`Related:` block under each failed/skipped step (reusing the canonical inline renderer); JSON adds a top-level **`explanations`** map (deduplicated by code) populated **only** with `--explain` — without `--explain` the JSON is byte-identical to before, and a `--explain` run that emits no explainable codes (e.g. an all-passed run) omits the map entirely. `--json --out --explain` keeps stdout and the file byte-identical.
- **Three stable workflow-run codes** (`workflow.run.dependency_failed`, `workflow.run.runtime_unreachable`, `workflow.run.input_failed`) join the canonical explain registry: `portier explain <workflow.run.*>` works and `portier explain --list` now covers **44** codes (8 config-doctor + 12 live-doctor + 6 policy + 15 workflow-step + 3 workflow-run). Doctor/policy/workflow-plan explain behavior is unchanged; the support-bundle `explanations.json` stays doctor-scoped.

### Notes (Slice 6)

- CLI-only, `validate:contract` stays **234/234**, no API/DTO/contract change. The workflow-run codes, their explanation registry, and the run-code selection helpers live in the `workflow` package (`workflow/run_explain.go`: `RunExplanations`, `ExplanationCodesForRun`, the `runExplainRegistry` that merges run + policy codes, and the per-step finding-code collectors); `Explanations()` now returns the workflow-step **and** workflow-run registries merged so the `explain` command lists/looks up both. The `explain` package stays the single Explanation type + renderer (`explain.PrintInline`/`explain.ForReport`); no second explanation schema. `commands` stays handlers-only — `RunWorkflowRun` only wires the `--explain` flag into `EmitRun`.
- Each run step records its explain codes internally (unexported `explainCodes` on `WorkflowRunStep`, not part of the JSON contract); the additive top-level `explanations` map carries the data under `--explain`.
- Covered by white-box `workflow/run_explain_internal_test.go` (locked 3-code run set + registry parity: every code explainable, no orphan/dup, `Code`==key, error severity, no dangling/self `related`; `Explanations()` merges step+run; `runExplainRegistry` resolves run + policy codes) and black-box `commands/workflow_run_explain_test.go` (policy-check/review/baseline finding codes, the three workflow-run codes incl. runtime-unreachable→3 still emits its code, passed-run emits no map, `--explain` does not change the run (decode + `DeepEqual`), `--json --out --explain` byte parity, `explain <workflow.run.*>`/`--list` integration); the `explain --list` count test moves 41 → 44.
- CLI coverage **97.5%** (gate 97 PASS, **not lowered** and **not raised** — the ~0.5% buffer is too thin to reach 98 safely). The new selection helpers and `--explain` paths are covered; the only uncovered lines remain the documented structurally-unreachable `json.Marshal`-on-concrete-type encode branches in the emitters.

### Added — Slice 5: Workflow run (read-only execution)

- **`portier workflow run --file <workflow.json>`.** The first workflow **execution** command — and it is strictly **read-only**. It validates the workflow, then runs each step in order by calling the existing safe domain helpers directly (`policy.Evaluate` / `policy.BuildReview` / `policy.Compare`), collecting a deterministic run report. It **never** runs a shell command (or the runbook display text), applies/imports configs, enforces a policy, schedules anything, or mutates the runtime/config/policy/baseline/report files. The **only** runtime contact is a read-only runtime-config read (via the existing config-export path) for a `policy.check` runtime step.
- **Step execution & status.** `policy.check` (config offline / runtime read-only) and `policy.review` → passed when the policy result passes, failed on a violation; `policy.baseline.compare` → passed with no new findings vs the baseline, failed when there are new findings. A `policy.baseline.compare` using **`reportFrom`** consumes the **in-memory** report produced by the referenced earlier step (no intermediate file is read or written); the in-memory report is round-tripped through JSON so its finding fingerprints match file-derived reports/baselines. If the referenced step produced no report (it errored, was skipped, or isn't a report-producing type), the dependent step is **skipped** — which fails the run.
- **Report model & output.** `{workflow, steps:[{id, type, status(passed|failed|skipped), exitCode, message, report?}], summary:{total, passed, failed, skipped}, result(passed|failed)}`, kept separate from the plan/runbook/policy models; the per-step `report` carries only what the existing policy reports already include (no raw configs, no secrets). Human output is a tagged (`[PASSED]`/`[FAILED]`/`[SKIPPED]`) list ending in `Result:`; `--json` emits the full report; `--out <file>` writes it (with `--json`, stdout and the file are byte-identical). (`--explain` for `workflow run` arrives in Slice 6.)
- **Invalid workflows & exit codes.** An invalid workflow prints the plan and exits **1** with **no step run and no `--out` file** (like `workflow plan`/`runbook`). Exit codes: `0` all steps passed (none skipped); `1` a step failed/was skipped, the plan was invalid, or an `--out` write failure; `2` usage error (missing `--file`/`--out` value) or an unreadable/malformed workflow file; `3` a `policy.check` runtime step could not reach the runtime (matching `policy check --runtime`). A referenced config/policy/baseline/report file that is unreadable/malformed **during the run** is a step failure (exit 1), not a usage error — referenced files are read **here** (during run), unlike plan/runbook which never open them.

### Notes (Slice 5)

- CLI-only, `validate:contract` stays **234/234** (no new endpoint — runtime mode reuses `GET /api/config/export`). The run model, orchestration, and rendering live in the `workflow` package (`workflow/run.go`: `WorkflowRun`/`WorkflowRunStep`/`WorkflowRunSummary`, `RunDeps`, `Run`, `RunExitCode`, `PrintRunHuman`, `EmitRun`, `ErrRuntimeUnreachable`); it reuses `Parse`+`BuildPlan` and the `policy`/`config` helpers, and **does not import `client`/`commands`**. The runtime fetch is **injected** as a function (`RunDeps.RuntimeRules`) — the command layer (`commands/workflow.go` `makeRuntimeRules`) wires the read-only config-export client, memoizes it (multiple runtime steps fetch once), and wraps a connection error with `workflow.ErrRuntimeUnreachable` so execution maps it to exit 3 without importing the client. `commands` stays handlers-only.
- Covered by white-box `workflow/run_internal_test.go` (each step type pass/fail, runtime pass/unreachable→3/generic-error→1, reportFrom new-findings + **fingerprint round-trip matches a file-derived baseline**, dependency-skip, every input-error branch, unknown-type defensive fail, human shape) and black-box `commands/workflow_run_test.go` (all step types, reportFrom, dependency-skip, invalid-plan→1-no-run-no-out, malformed→2, missing-referenced-file→step-fail, **runtime via httptest** + unreachable→3 + API-error→1 + bad-URL→1 + memoized single fetch, JSON/human shape, `--out`/`--json --out` byte parity/write-failure, **no input mutation**, dispatch) + a `main_test.go` offline dispatch case.
- CLI coverage **97.6% → 97.5%** (gate 97 PASS, **not lowered**). The run orchestrator/executors/renderer are covered including the error branches; the only uncovered lines are the documented structurally-unreachable `json.Marshal`-on-concrete-type encode branches (`EmitRun`, `snapshotFromReport`) — same pattern as the other emitters.

### Added — Slice 4: Workflow runbook preview

- **`portier workflow runbook --file <workflow.json>`.** Turns a **valid** workflow plan into the ordered list of Portier CLI commands you would run manually to carry the workflow out — a **preview only**. Still **not execution**: it parses + validates the workflow, then maps each step to a command. It never executes a command, contacts the runtime, reads the files a step refers to (or inspects their contents), or mutates any file except the requested `--out` file.
- **Step → command mapping.** `policy.check` (config) → `portier policy check --config <f> --policy <f>`; `policy.check` (runtime) → `... --runtime --policy <f>`; `policy.review` → `portier policy review --current <f> --candidate <f> --policy <f>`; `policy.baseline.compare` (report file) → `portier policy baseline compare --baseline <f> --report <f>`. For a `reportFrom` step the report is an explicit **`<report-from:step-id>` placeholder** (not a real path) plus a note telling you to replace it with the report that step produces — the runbook never invents a file path.
- **Model & output.** Each step carries a canonical argv `command` array and a best-effort copy/paste `display` string (Portier never shell-executes the display; tokens with whitespace/quotes are single-quoted, others left as-is). Human output is a numbered list ending in `Result: ready`; `--json` emits `{workflow, steps:[{id, type, command, display, notes}], summary:{total}, result}`; `--out <file>` writes that JSON (with `--json`, stdout and the file are byte-identical).
- **Invalid workflows.** If the plan is invalid, `workflow runbook` prints the plan (the validation errors) and exits **1** — exactly like `workflow plan` — and produces **no runbook and no `--out` file**. Exit codes: `0` runbook produced; `1` plan invalid or an `--out` write failure; `2` usage error (missing `--file`/`--out` value) or an unreadable/malformed workflow file. No connection-failure (`3`) code — it never contacts the runtime.

### Notes (Slice 4)

- CLI-only, fully offline, `validate:contract` stays **234/234**. The runbook model, builder, and renderer live in the `workflow` package (`workflow/runbook.go`: `Runbook`/`RunbookStep`/`RunbookSummary`, `BuildRunbook`, `PrintRunbookHuman`, `EmitRunbook`); it reuses `Parse` + `BuildPlan` (a runbook is built only from a valid plan); file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only (`RunWorkflowRunbook` parses flags, validates, and calls the workflow helpers).
- Covered by white-box `workflow/runbook_internal_test.go` (per-step-type mapping, reportFrom placeholder + note, ordering/summary, `shellQuoteToken`, human shape incl. unnamed/singular, the defensive unknown-type fallback, all templates produce runbooks) and black-box `commands/workflow_runbook_test.go` (all step types, multi-step ordering, human/JSON shape, invalid → plan + exit 1 + **no `--out` file**, malformed/unsupported-schema → 2, missing `--file`/`--out`/unreadable → 2, `--out` write + `--json --out` byte parity + write-failure → 1, **does-not-read-referenced-files**, no-mutation, templates → runbooks, dispatch) + a `main_test.go` offline dispatch case.
- CLI coverage **97.6% → 97.6%** (gate 97 PASS, **not lowered**). The runbook builder/renderer and `RunWorkflowRunbook` are 100% covered; the only uncovered line is the documented structurally-unreachable `json.Marshal`-on-concrete-`Runbook` encode branch in `EmitRunbook` (same as the plan/policy/doctor emitters).

### Added — Slice 3: Workflow templates

- **`portier workflow template <name>` / `--list`.** Built-in starter workflow files so operators don't have to hand-write the workflow JSON schema. Fully **offline**: never contacts the runtime, never executes a step, never reads referenced workflow input files, and never modifies any file except the requested `--out` file. A rendered template is a complete `schemaVersion: 1` workflow that can be passed straight to `portier workflow plan --file <file>`.
- **Four templates (sorted by name):** `policy-baseline-check` (check a config, then compare the report to a baseline via `reportFrom`), `policy-check-local` (check a local config against a policy), `policy-check-runtime` (check the live runtime config against a policy), `policy-review` (compare current vs candidate and evaluate the candidate). Each composes only the existing validated step types — **no execution, scheduler, or mutation/apply/import fields**.
- **Output.** `workflow template <name>` prints the bare workflow JSON; `--json` prints a `{name, title, description, workflow}` metadata wrapper; `--list` prints a compact human list and `--list --json` prints `{"templates":[{name,title,description}]}` (no embedded workflow bodies). `--out <file>` always writes the **bare** workflow JSON (directly usable by `workflow plan`), regardless of `--json`; in human mode it confirms with `Workflow written to <file>`. Flags may appear before or after the name.
- **Exit codes.** `0` success; `1` an `--out` write failure; `2` a usage error — an unknown template, a missing/extra template name, a missing `--out` value, or `--list` combined with a name or `--out`.

### Notes (Slice 3)

- CLI-only, fully offline, `validate:contract` stays **234/234**. The template registry, metadata, and rendering live in the `workflow` package (`workflow/template.go`: `Template`, `Templates()`, `FindTemplate`, `TemplateNames`, `WorkflowValue`/`DetailValue`, `PrintTemplateList`); file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only (`RunWorkflowTemplate` parses flags and calls the workflow helpers). Mirrors the v1.10 `policy template` convention (bare value vs `--json` wrapper; bare `--out` file; interspersed flag parsing).
- **Every built-in template is validated by the planner** — a white-box guard renders each template, parses it, and asserts `BuildPlan` reports `valid`, and a field allowlist guard ensures no template introduces execution/scheduler/mutation fields or a non-validated step type. A black-box test confirms a `--out` file is accepted end-to-end by `workflow plan --file`.
- Covered by white-box `workflow/template_internal_test.go` (sorted/unique registry, render-to-valid-plan, no-forbidden-fields, `FindTemplate`, `DetailValue` shape) and black-box `commands/workflow_template_test.go` (list human/JSON/sorted, render each, single JSON wrapper, `--out` bare + usable-by-plan, `--json --out` writes bare, flag-after-name, unknown/missing/too-many/missing-out-value/list+name/list+out/bad-flag → 2, write-failure → 1, no-file-mutation, dispatch) + a `main_test.go` offline dispatch case.
- CLI coverage **97.7% → 97.6%** (gate 97 PASS, **not lowered** — the small dip is the documented structurally-unreachable `json.Marshal`-on-concrete-DTO encode branches in `RunWorkflowTemplate`, exactly as in `RunPolicyTemplate`). `template.go` is 100% covered.

### Added — Slice 2: Workflow plan explanations

- **Stable workflow step-validation codes.** Each planned step now carries a stable, operator-facing `code` (e.g. `workflow.step.unknown_report_from`). There are exactly **15** codes — one per Slice 1 validation outcome — locked by a code-set test: `workflow.step.valid`, `.duplicate_id`, `.unknown_type`, `.missing_id`, `.missing_type`, `.missing_config_source`, `.conflicting_config_sources`, `.missing_policy`, `.missing_current`, `.missing_candidate`, `.missing_baseline`, `.missing_report_source`, `.conflicting_report_sources`, `.unknown_report_from`, `.future_report_from`. Every step (valid or invalid) carries a `code` in the plan; valid steps use `workflow.step.valid`.
- **`portier workflow plan --file <workflow.json> --explain`.** Adds inline explanations (code + meaning + next action + related) for the plan's **invalid** steps in human output, and an additive `explanations` map (deduplicated, invalid-step codes only) under `--json`. A valid step needs no explanation, so it is not explained (in either form). `--explain` never changes the steps, summary, result, or exit code; without it, the JSON is byte-identical to before (the `explanations` map is `omitempty`). `--json --out --explain` keeps stdout/file byte-identical.
- **`portier explain <workflow-code>` / `explain --list`.** The `explain` command now merges the workflow registry with doctor and policy, so all 15 workflow codes are explainable and listed (sorted); `explain --list` covers **41** codes total (8 config doctor + 12 live doctor + 6 policy + 15 workflow). Existing doctor/policy explanations are unchanged.

### Notes (Slice 2)

- CLI-only, fully offline, `validate:contract` stays **234/234**. Workflow validation codes + the explanation registry live in the `workflow` package (`workflow/explain.go`, `Explanations()`, keyed by the workflow code constants); the `commands` explain handler composes domains via `explain.Merge(doctor.Explanations(), policy.Explanations(), workflow.Explanations())`; `commands` stays handlers-only. Inline rendering reuses the canonical `explain.PrintInline`/`explain.ForReport` — no second explanation schema. The plan now carries `StepPlan.Code` and `Emit` wraps the plan in a `planJSON` that adds the `omitempty` `explanations` map under `--explain`.
- **`--explain` is presentation only** — it does not change which steps are validated, the plan body, the result, or the exit code (guarded by a decode-and-deep-compare test). The support bundle's `explanations.json` stays **doctor-scoped** (unchanged).
- Covered by white-box `workflow/explain_internal_test.go` (locked 15-code set; every code explainable; no orphan/dup; Code==key; required fields/severity; no dangling/self `related`; severity matches outcome) + `workflow_internal_test.go` (per-outcome code mapping, `invalidCodesOf`, human explain renders invalid-only) and black-box `commands/workflow_explain_test.go` (stable codes in JSON, human/JSON `--explain`, omit-without-explain, valid-plan omits map, `--explain` doesn't change the plan, `--out`/`--json --out` byte parity, `explain <workflow-code>`/`--list`/unknown-code).
- CLI coverage **97.7% → 97.7%** (gate 97 PASS, **not raised** — the buffer above 97 is too thin to move to 98 safely). The `workflow` package is at 98.7%; the only uncovered line is the documented structurally-unreachable `json.Marshal`-on-concrete-`Plan` encode branch in `Emit` (same as policy/doctor).

### Added — Slice 1: Workflow plan dry-run

- **`portier workflow plan --file <workflow.json>`.** Reads a local **workflow** file (an ordered sequence of existing safe Portier operations), validates its schema and step references, and prints a deterministic plan. Fully **offline and dry-run**: it does **not execute** any step, never contacts the runtime, never reads the files a step refers to, never applies/imports configs, never enforces a policy, and never mutates any file except the requested `--out` file. `--json` emits the full plan; `--out <file>` also writes that JSON (with `--json`, stdout and the file are byte-identical).
- **Workflow file format (`schemaVersion: 1`).** A `name` (optional) and a non-empty `steps` array. Supported step types (validated, not executed): `policy.check` (exactly one of `config`/`runtime`, plus `policy`), `policy.review` (`current`/`candidate`/`policy`), and `policy.baseline.compare` (`baseline`, plus exactly one of `report`/`reportFrom`). A `reportFrom` must reference a step that appears **earlier** in the file (so the dependency graph is inherently acyclic). Unknown fields anywhere are **rejected** (to catch typos); step ids must be unique and non-empty.
- **Plan model.** `{schemaVersion, name, steps:[{id, type, status(valid|invalid), message, inputs, dependsOn}], summary:{total, valid, invalid}, result(valid|invalid)}` — kept separate from the policy/doctor report models. Human output tags each step `[VALID]`/`[INVALID]` and ends with a summary and `Result:` line.
- **Exit codes.** `0` the plan is valid; `1` the workflow parsed but the plan is invalid (one or more invalid steps) **or** an `--out` write failure; `2` missing/invalid arguments (including a missing `--file`/`--out` value) or an unreadable/malformed workflow file (missing/unsupported `schemaVersion`, or no steps). No connection-failure (`3`) code — planning never contacts the runtime.

### Notes (Slice 1)

- CLI-only, fully offline, `validate:contract` stays **234/234**. The workflow schema, parser, validator, plan model, and rendering live in a new domain package `tools/cli/sources/workflow` (`File`/`Step`/`Parse`, `Plan`/`StepPlan`/`PlanSummary`/`BuildPlan`, `PlanExitCode`, `PrintHuman`, `Emit`/`EmitOptions`); `commands` stays handlers-only (`RunWorkflow`/`RunWorkflowPlan`). The command dispatches **offline** from `main.go` (like `explain`/`policy`) — no URL resolution.
- Workflow planning **composes existing safe operations by reference only** — it validates that a step names a known operation with coherent inputs; it does not duplicate policy/doctor semantics and does not run them. Step **execution** and reporting are deferred to a later v1.11 slice.
- Covered by white-box `workflow/workflow_internal_test.go` (parse/schema/unknown-field/empty-steps; every step type valid + each invalid path; duplicate id; reportFrom unknown/later/self; mixed counts; exit code; human rendering incl. placeholders) and black-box `commands/workflow_test.go` (valid/invalid/usage exits, JSON shape, `--out` write + `--json --out` byte parity + write-failure, **no file mutation**, **does not open referenced files**, dispatch/help) + a `main_test.go` offline dispatch case.
- CLI coverage **97.6% → 97.7%** (gate 97 PASS, **not raised** — the ~0.7% buffer above 97 is too thin to move to 98 safely). The new `workflow` package is at 98.6%; the only uncovered line is the documented structurally-unreachable `json.Marshal`-on-concrete-`Plan` encode branch in `Emit` (same as policy/doctor).

---

## [1.10.0] — 2026-06-13 — Automation, Policies & Safe Operations

v1.10 gives operators a **local-first, dry-run policy toolkit** for defining and evaluating safe operating rules — entirely in the Go CLI, with **no runtime/API/server/service/client contract change** (`validate:contract` stays **234/234**) and **no enforcement, automation, mutation, scheduler, or telemetry**. The CLI stays a pure API client; UDP stays first-class (there is **no `allowUdp`/protocol-restriction policy**).

The toolkit (Slices 1–8): an **offline policy dry-run evaluator** — `policy check --config <file> --policy <file>` against a small JSON policy of boolean guardrails (`requireGroup`/`allowLanExposure`/`allowPrivilegedPorts`/`allowAutostart`/`forbidDuplicateBindings`) with stable finding codes; **runtime policy check** — `policy check --runtime` evaluates the live runtime config read-only via the existing config-export path (offline `--config` stays fully offline); **policy finding explanations** — `portier explain <code>`/`--list` and inline `policy check --explain` (plus a neutral leaf `explain` package); **policy report export** — `policy check --out <file>` with `--json --out` byte parity; **built-in policy templates** — `policy template --list`/`<name>`/`--out` (`permissive`, `local-safe`, `managed`); **policy-aware config review** — `policy review --current --candidate --policy` (compact change summary + candidate evaluation); **policy baseline snapshots** — `policy baseline create`/`compare` (accepted findings, deterministic fingerprints, new/resolved/unchanged); and an offline **AI handoff prompt** (`prompts/policy.md`). A **CLI architecture cleanup** moved general model/result types out of the catch-all `commands` package into focused domain packages (`config`, `doctor`, `policy`, `planview`, `output`, `explain`) — `commands` is handlers-only. A **Slice 8 consistency audit** verified the command surface, JSON schemas, exit codes, safety, and architecture boundaries (PASS WITH NOTES). The **CLI coverage gate was ratcheted 95% → 97%** (actual ~97.6%); no other gates changed, none lowered.

### Slice 8: Policy toolkit consistency audit — PASS WITH NOTES

Focused audit of the v1.10 policy toolkit (Slices 1–7) — audit/cleanup only, no new features. **Result: PASS, v1.10 policy toolkit ready for finalization.**

- **Command surface (verified, live battery).** `policy check` (`--config`/`--runtime`, `--json`/`--out`/`--explain` and all combinations incl. `--runtime --json --out --explain`), `policy review`, `policy baseline create`/`compare`, `policy template <name>`/`--list`, and `explain --list`. Source selection is correct: exactly one of `--config`/`--runtime`; both → exit 2; neither → exit 2. Offline `--config` mode ignores even a malformed `--url` (proven: bad `--url` + offline still exits 1 for violations, never resolves the runtime URL).
- **JSON schemas (verified).** `policy check` → `{source?, explanations?, findings, summary, result}` (`source`/`explanations` additive, omitted when unused); `policy review` → `{review, findings, summary, result, explanations?}`; baseline file → `{schemaVersion, createdAt, source?, result, findings:[{fingerprint, code, severity, title, message}]}` (no raw config/details/explanations); `baseline compare` → `{summary:{new,resolved,unchanged}, result, new, resolved, unchanged}`; `template` → list `{templates:[{name,title,description}]}`, single `{name,title,description,policy}`, `--out` bare policy JSON. `--json --out` byte parity holds across check (incl. `--explain`, both `--config` and `--runtime`), review, and report export.
- **Exit codes (verified).** check 0/1/2/3 (runtime-unreachable → 3, export/API failure → 1); review 0/1/2; template 0/1/2; baseline create 0/1/2; baseline compare 0/1/2 — all as documented.
- **Safety (verified).** Offline commands never resolve/use the runtime URL; runtime check is read-only (the export test server fails on any non-GET request); no command mutates config/policy/report/baseline inputs (only explicit `--out` files written); no probing, enforcement, scheduler, telemetry, or env/process/log collection; no `allowUdp`/protocol restriction; baselines store only fingerprinted finding snapshots.
- **Architecture (verified).** `commands` stays handlers-only; the `policy` package imports only `config`/`explain`/`output` (no `client`/`commands`, no import cycle) — the runtime fetch + client-DTO→`config.Rule` mapping live in the command layer (`loadCheckRules`/`exportedRules`). `go vet` clean.
- **Fix made:** the top-level `portier policy` help intro was stale (claimed the whole toolkit was "offline" and listed only check + templates). Updated it to describe check/review/baseline/template, note all policy commands are read-only/dry-run, and show `check (--config | --runtime)`. One small test added (`--runtime --json --explain --out` byte parity). No behavior change beyond the help text.

`validate:contract` **234/234**; CLI coverage **97.6%** (gate 97 PASS, none lowered); lint/typecheck/vet clean. No new durable rule — the audit confirms the Slice 1–7 rules. No release blockers; no deferred non-blockers.

### Added — Slice 7: Policy baseline snapshots

- **`portier policy baseline create --from-report <report.json> --out <baseline.json>`.** Saves an accepted set of policy findings as a compact baseline file (a dry-run "these findings were accepted at this point" snapshot). Works on any policy report JSON produced by `policy check` or `policy review` (`--json`/`--out`). Fully offline — never contacts the runtime, never mutates the report.
- **`portier policy baseline compare --baseline <baseline.json> --report <report.json>`.** Compares a fresh policy report against a baseline and reports which findings are **new**, **resolved**, or **unchanged** (keyed by a deterministic fingerprint). Human output + `--json`. Result is `failed` only when there are **new** findings — **resolved-only changes do not fail**.
- **Baseline schema (deterministic).** `{schemaVersion: 1, createdAt (RFC3339 UTC), source?, result, findings: [{fingerprint, code, severity, title, message}]}`. It is an **accepted snapshot of findings, not a config copy** — no raw config, no secrets, no runtime host data, no details/explanations. The `policy.valid` "no violations" marker is excluded (it must never count as new/resolved/unchanged).
- **Deterministic fingerprints.** Rule-scoped findings (`group_required`/`lan_exposure_forbidden`/`privileged_port_forbidden`/`autostart_forbidden`) fingerprint as `code|name|protocol|listenHost|listenPort`; duplicate bindings as `code|protocol|listenHost|listenPort|<sorted rule names>`; anything else falls back to `code|message`. Computed from the report's detail map, so a finding fingerprints identically whether the report came from offline `--config` or `--runtime`, and never depends on volatile rule IDs.
- **Exit codes.** create: `0` written, `1` output-file write failure, `2` usage error or unreadable/malformed report. compare: `0` no new findings, `1` one or more new findings, `2` usage error or unreadable/malformed baseline or report (including an unsupported baseline `schemaVersion`).

### Notes (Slice 7)

- CLI-only, fully offline, `validate:contract` stays **234/234**. The baseline model, fingerprinting, build, and compare live in the `policy` package (`policy/baseline.go`: `Baseline`/`BaselineFinding`/`CompareReport`, `Fingerprint`, `ParseReportSnapshot`/`ParseBaseline`, `BuildBaseline`, `Compare`/`CompareExitCode`, `PrintCompareHuman`); file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only (`RunPolicyBaseline`/`Create`/`Compare`). `BuildBaseline` takes an injected clock so `createdAt` is testable.
- Covered by white-box `policy/baseline_internal_test.go` (fingerprint stability incl. duplicate-binding name-order independence and the message fallback; tracked-finding exclusion; injected-clock UTC `createdAt`; new/resolved/unchanged + sorting; parse rejection) and black-box `commands/policy_baseline_test.go` (create from check & review reports, schema/createdAt/no-raw-config assertions, JSON summary, compare no-change/new/resolved-only/mixed, JSON & human shape, all error exits, **no input mutation**, dispatch/help/bad-flag).
- CLI coverage **97.6% → 97.6%** (gate 97 PASS, not lowered). Baseline model/helpers are fully covered; residual sub-100% lines are the documented structurally-unreachable `json.Marshal`-on-concrete-DTO encode branches in the handlers.

### Added — Slice 6: Runtime policy check

- **`portier policy check --runtime --policy <file>`.** Evaluates the **live runtime config** against a policy without manually exporting it first — read via the existing read-only `GET /api/config/export` path (no new endpoint), then evaluated with the **same evaluator and report shape** as offline `policy check`. The offline form `policy check --config <file> --policy <file>` is unchanged and stays fully offline. Read-only and dry-run: no enforcement, no remediation, no target probing, no runtime/config/policy mutation, no telemetry.
- **Exactly one config source.** `--config <file>` (offline) or `--runtime` (live). Supplying **both** or **neither** exits `2`. `--policy` remains required and is validated **before** any runtime I/O (so an unsupported schema exits `2`, not a runtime error). Offline `--config` mode never resolves or contacts the runtime URL; runtime mode uses the existing global `--url`/`--host`/`--port` like other live commands.
- **Additive `source` field.** Both the human report (`Source: runtime` / `Source: config file` under the title) and the JSON report gain an additive `source` field (`omitempty`) recording where the evaluated config came from. The flag set (`--json`, `--out`, `--explain`) behaves exactly as in offline `policy check` — `--json --out` byte-identical, `--explain` adds the additive explanations map, no change to findings/summary/result/exit.
- **Exit codes (runtime mode).** `0` runtime config passes; `1` runtime config violates the policy, an `--out` write failure, or a config-export/API failure; `2` invalid input (bad source selection, missing/invalid policy); `3` the runtime is unreachable. A runtime-unreachable or export failure is reported as a runtime/operation error (the standard live-CLI convention) — **never fabricated as a policy finding**, and no report is invented when the exported config could not be read.

### Notes (Slice 6)

- CLI-only, **no new API endpoint** and no contract change (`validate:contract` stays **234/234**). The runtime config is read via the existing `client.ExportConfig()` (`GET /api/config/export`) and mapped 1:1 into the config domain rules, so a runtime config evaluates identically to the same config in a local file. The policy package stays free of live-client behavior (the durable offline/runtime split): `commands` selects the source (`loadCheckRules`/`exportedRules`) and lazily builds the client only in runtime mode; `policy` evaluates and emits. `commands` stays handlers-only.
- `policy check` and `policy review` continue to share `printFindings`/`printSummaryAndResult`; the new `source` line/field lives only on the policy-check report wrapper, so offline `policy check --json` without a source is byte-identical to before (and `policy review` is unaffected).
- Covered by black-box `policy_runtime_test.go` (httptest config-export server): runtime pass/violation exits, JSON `source:"runtime"` shape, `--out` byte parity, `--explain` explanations, **runtime-unreachable → 3**, **export/API failure → 1**, `--config`+`--runtime` → 2, neither → 2, missing/invalid policy → 2, invalid `--url` → 2, **policy validated before any runtime I/O**, **offline mode never contacts/resolves the runtime** (a server that fails on any hit + a deliberately invalid `--url` that offline mode ignores), and **read-only** (the export server fails the test on any non-GET-export request). No new policy codes.
- CLI coverage **97.6% → 97.6%** (gate 97 PASS, not lowered). `exportedRules`/`RunPolicyCheck` are 100% covered; residual sub-100% lines are the documented structurally-unreachable `json.Marshal`-on-concrete-DTO encode branches.

### Added — Slice 5: Policy-aware config review

- **`portier policy review --current <file> --candidate <file> --policy <file>`.** A fully offline, dry-run command that answers "if I moved from the current config to the candidate config, would the candidate pass this policy, and what changed?". It reads the three files, compares the two configs at a compact summary level, evaluates **only the candidate** against the policy, and reports both — **never mutating anything, never contacting the runtime, never probing targets, and never using import/apply**.
- **Compact, deterministic change summary.** A `review` block with `current` and `candidate` config summaries (reusing the v1.9/v1.10 `config.BuildSummary`) plus a scalar `delta` (`ruleCount`, `enabledRuleCount`, `disabledRuleCount`, `groupCount`, `ungroupedRuleCount`). This is intentionally **not** a full config diff engine.
- **Reuses policy check semantics.** The candidate's findings/summary/result come from the same `policy.Evaluate` and finding rendering as `policy check` — there is no second finding/report schema. Human output is `Portier Policy Review` + a `Config changes:` block (`Rules: 2 → 3 (+1)` …) + the candidate's policy findings/summary/`Result:` line. `--json` emits `{review, findings, summary, result}`; `--explain` adds the same additive `explanations` map as `policy check --json --explain` (and inline blocks in human output) without changing the review, findings, summary, result, or exit code; `--out <file>` writes the same JSON (byte-identical to `--json` stdout).
- **Exit codes.** `0` candidate passes; `1` candidate violates the policy (or a JSON-encode / `--out` write failure); `2` missing/invalid arguments or an unreadable/malformed config or policy file (including an unsupported `schemaVersion`).

### Notes (Slice 5)

- CLI-only. The review model + evaluation + rendering live in the `policy` package (`policy/review.go`: `Review`/`ReviewSummary`/`ReviewDelta`, `BuildReview`, `PrintReviewHuman`, `EmitReview`); config summaries reuse `config.BuildSummary`; file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only. The policy-check and review human renderers now share `printFindings`/`printSummaryAndResult` so finding output cannot drift (policy check output stays byte-identical). `validate:contract` stays **234/234**; the CLI stays a pure API client.
- Covered by black-box `policy_review_test.go` (pass/fail exit codes, exact current/candidate/delta counts incl. negative deltas, JSON + human shape, all input-error exit-2 paths, `--out` byte parity + write failure, `--explain` adds explanations without changing the report, no input mutation, dispatch, help, bad-flag).
- CLI coverage **97.6% → 97.6%** (gate 97 PASS, not lowered). `BuildReview`/`PrintReviewHuman` are 100% covered; the remaining sub-100% lines are the structurally-unreachable `json.Marshal`-on-concrete-DTO encode-failure branches (the documented CLI pattern). Gate not raised — buffer intentionally tight.

### Added — Slice 4: Policy templates

- **`portier policy template <name>` / `--list`.** Built-in, deterministic policy templates so operators don't have to invent the JSON schema by hand. Three templates (sorted by name): **`local-safe`** (forbids LAN exposure + privileged ports, keeps autostart, no group requirement), **`managed`** (requires groups; forbids LAN exposure, privileged ports, and autostart), and **`permissive`** (mirrors Portier's default — only duplicate bindings forbidden). Every template is `schemaVersion: 1` with only the five standard boolean guardrails — **no protocol allowlist/denylist and no `allowUdp`** (UDP stays first-class).
- **Output shapes.** `policy template <name>` prints the **bare policy JSON** (directly usable by `policy check`); `--json` prints a metadata wrapper `{name, title, description, policy}`; `--list` prints a compact human list (name column + title + indented description); `--list --json` prints `{"templates":[{name, title, description}]}` (no embedded policy). `--out <file>` writes the **bare policy JSON** regardless of `--json`, so the file can be passed straight to `policy check --policy <file>` (human mode confirms with `Policy written to <file>`; `--json` mode keeps stdout the pure wrapper).
- **Offline + safe.** Fully offline — never contacts the runtime, never probes targets, and modifies nothing except the requested `--out` file. Exit codes: `0` success; `1` `--out` write failure (operation failure, `Error writing` on stderr); `2` usage error — unknown template, missing template name, too many names, a missing `--out` value, or `--list` combined with a name/`--out`. Flags may appear before or after the template name (`template managed --out f`).

### Notes (Slice 4)

- CLI-only. Template registry, metadata, and rendering live in the `policy` package (`Templates`/`FindTemplate`/`Template.PolicyValue`/`DetailValue`/`PrintTemplateList`/`TemplateNames`, in `policy/template.go`); file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only. `validate:contract` stays **234/234**; the CLI stays a pure API client.
- Templates are guarded by a white-box registry test (each renders to a schema-1 policy that `Parse` accepts and `Evaluate` runs; sorted/unique; no `udp`/`protocol` token; guardrail booleans locked to each template's documented purpose) plus black-box command tests (list/render/`--out`-usable-by-`check`/error paths).
- CLI coverage **97.8% → 97.6%** (gate 97 PASS, not lowered). `policy/template.go` is 100% covered; the ~0.4% dip is the three structurally-unreachable `json.Marshal`-on-concrete-DTO encode-failure branches in `RunPolicyTemplate` (the same documented CLI pattern). Gate not raised — the buffer is intentionally tight and the remaining gap is defensive/unreachable.

### Added — Slice 3: Policy report export

- **`portier policy check --out <file>`.** Writes the policy report to a file as deterministic pretty JSON, for CI artifacts, reviews, and support handoff. The exported file uses the **same shape as `--json`** (`findings` + `summary` + `result`, plus the additive `explanations` map under `--explain`) — there is no second report schema.
- **Output behavior mirrors the v1.9 doctor `--out` convention.** `--out` writes the file regardless of `--json`; in human mode stdout stays human (with a `Report written to <file>` confirmation), in `--json` mode stdout stays pure JSON and is **byte-identical to the file**. `--explain --out` writes the explanations map to the file (and prints inline explanations in human stdout); `--json --explain --out` keeps stdout/file byte-identical.
- **Errors are operation failures, not findings.** A file-write failure exits `1` (with a clear `Error writing <file>` on stderr) — it overrides the report's own exit code; a missing `--out` value exits `2`. A malformed/unreadable config or policy still exits `2` and **writes no file** (no success-looking report). The file is only written after a successful marshal (no partial writes); parent directories are not created.

### Notes (Slice 3)

- CLI-only, fully offline: no config/policy mutation, no runtime access, no target probing, no telemetry/upload. `validate:contract` stays **234/234**; the CLI stays a pure API client. Report construction lives in the `policy` package (`policy.Emit`/`EmitOptions.OutPath`), file writing reuses `output.WritePrettyJSON`, and `commands` remains handlers-only.
- Reuses the existing `--json` payload (`reportJSON`) — the same bytes go to stdout and the file, so `--json --out` parity is guaranteed by construction.
- CLI coverage held at **97.8%** (gate 97, not lowered — the ~0.8% buffer is too tight to raise to 98 safely; the new export branches are covered by black-box command tests). No gates lowered.

### Added — Slice 2: Policy finding explanations

- **`portier explain` now covers policy finding codes.** `portier explain policy.lan_exposure_forbidden` (and every policy code) prints a deterministic explanation (meaning + what to do + related codes); `portier explain --list` now lists all **26** codes (8 config-doctor + 12 live-doctor + 6 policy), sorted. Doctor code explanations are unchanged.
- **`portier policy check --explain`.** Adds an inline explanation block (`Code:`/`Meaning:`/`What to do:`/`Related:`) after each emitted finding in human output, and an additive top-level `explanations` map (deduplicated by code) under `--json`. `--explain` does **not** change the findings, summary, result, or exit code — without it the JSON is byte-identical to before (the `explanations` map is omitted via `omitempty`).
- **Neutral `explain` package (architecture).** The shared `Explanation` type and its rendering helpers (`For`/`Sorted`/`SortedCodes`/`ForReport`/`PrintInline`/`Merge`) moved out of `doctor` into a new leaf package `tools/cli/sources/explain` (imports nothing internal). Each domain (`doctor`, `policy`) owns its own explanation **data** keyed by its own code constants; the `explain` command merges them. This keeps `doctor` from owning policy codes, avoids import cycles, and keeps `commands` handlers-only.
- **Explanations are policy-framed and safe.** Each policy explanation frames the finding as a **policy choice** (the operator's `allow*`/`require*` guardrail), offers both resolutions (change the rule **or** adjust the policy), and never claims Portier enforces a policy or fixes a violation automatically.
- **AI handoff prompt.** Added [`prompts/policy.md`](../prompts/policy.md) — a focused, offline, copy-paste prompt for interpreting policy reports (distinct from the doctor prompt because policy findings need the "change the rule vs. adjust the policy" framing). Docs-only; no CLI prompt helper, no AI integration/upload/telemetry.

### Notes (Slice 2)

- CLI-only, behavior-preserving for doctor: `validate:contract` stays **234/234**, doctor `--explain` output is byte-identical (same `Explanation` JSON shape, same inline rendering), and the support bundle's `explanations.json` stays doctor-scoped (20 codes). The CLI stays a pure API client.
- Every policy finding code is explainable, guarded by a registry parity test in the `policy` package (mirroring the doctor guard). Inline explanations reuse the canonical registry — no second explanation schema.
- CLI coverage held at **97.8%** (gate 97, not lowered); the new `explain` package and policy explanation code are fully covered.

### Added — Slice 1: Policy model + dry-run evaluator

- **`portier policy check --config <file> --policy <file>` command.** Evaluates a local Portier config file against a small JSON policy file and prints a deterministic report. **Fully offline** — it never contacts the runtime, never probes targets, and never modifies the config or policy file. Human output by default; `--json` emits the full `PolicyReport`.
- **Small JSON policy format (`schemaVersion: 1`).** A `rules` object with five boolean guardrails: `requireGroup`, `allowLanExposure`, `allowPrivilegedPorts`, `allowAutostart`, `forbidDuplicateBindings`. Omitted fields fall back to a **permissive baseline** (an empty policy permits everything — operators opt into each restriction). **Unknown fields are rejected** so a typo cannot silently relax a guardrail.
- **Policy result model.** `PolicyFinding` (`code`, `severity` `info`/`warning`/`error`, `title`, `message`, optional `details`) aggregated into a `PolicyReport` (`findings[]` + `summary` counts + `result` `passed`/`failed`). Kept separate from the doctor `DoctorReport`.
- **Stable policy finding codes.** `policy.valid`, `policy.group_required`, `policy.lan_exposure_forbidden`, `policy.privileged_port_forbidden`, `policy.autostart_forbidden`, `policy.duplicate_binding_forbidden`. Operator-facing identifiers — not renamed casually. In this slice every violation is an `error`; a compliant config emits one `policy.valid` info finding.
- **Deterministic evaluation.** Per-rule checks run in config file order (within a rule: group → LAN exposure → privileged port → autostart); duplicate-binding findings are emitted last, one per conflicting binding, sorted by protocol → listen host → port. LAN exposure (`0.0.0.0`), privileged port (`< 1024`), and duplicate-binding detection reuse the v1.9 config-doctor interpretations so the two cannot drift.
- **Exit codes.** `0` no violations, `1` one or more violations (or a JSON-encode failure), `2` missing/invalid arguments or an unreadable/malformed config or policy file (including an unsupported `schemaVersion`).

### Notes (Slice 1)

- CLI-only change. The CLI stays a pure API client; no shared/server/service/client code, no API/DTO/config shape, and no plan/apply or forwarding behavior changed. `validate:contract` unchanged at **234/234**.
- **No enforcement, no mutation, no automation.** This slice is dry-run evaluation only — there is no live policy enforcement, no automatic remediation, no scheduler, no UI panel, and no remote/team/auth behavior.
- **UDP is first-class.** There is intentionally **no `allowUdp`/`udpForbidden` policy** and no protocol allowlist/denylist; a UDP rule is evaluated by the same general guardrails as a TCP rule.
- CLI coverage **97.6% → 97.8%** statements; every new policy function is 100% covered. Applying the new **opportunistic coverage-gate ratchet**, the **CLI gate was raised 95% → 97%** (`scripts/validate-coverage.js`) — actual 97.8%, a deliberately tighter ~0.8% buffer (deterministic cross-package Go measurement). The remaining uncovered lines are documented structurally-unreachable CLI branches (os.Exit wrapper, `json.Marshal` on concrete DTOs, platform filesystem-failure paths). No gate lowered.

### Changed — CLI package layout (model/types out of `commands`)

- **Behavior-preserving refactor of the Go CLI.** The general model/result types and their pure, offline logic moved out of the catch-all `commands` package into focused per-domain packages, so `commands` now holds only the `Run*` command handlers, help text, and dispatch. New packages: **`tools/cli/sources/config`** (local config-file domain — `Rule`, `ParseLocal`, `Validate`, `Summary`, `FindDuplicateBindings`, `RuleDetail`, …), **`tools/cli/sources/doctor`** (doctor model `Report`/`CheckResult`/`Severity`, live `RunLiveChecks` + offline `RunConfigChecks` builders, the `Explanation` registry, and report emission `Emit`/`PrintHuman`/`ReportPayload`), **`tools/cli/sources/policy`** (policy model `Report`/`Finding`, `Parse`/`Evaluate`/`PrintHuman`), and **`tools/cli/sources/planview`** (config plan/diff/apply response rendering — `PrintPlan`/`PrintDiff`/`PrintApply`/`PlanExitCode`/`ApplyExitCode`). Three shared helpers (`PluralWord`, `PluralRule`, `WritePrettyJSON`) moved into `output`. The request-DTO mapping, per-command printers, single-command output structs, and plumbing intentionally stay with the handlers in `commands`.
- **Idiomatic, de-stuttered names** (`DoctorReport` → `doctor.Report`, `PolicyReport` → `policy.Report`, `rawConfigRule` → `config.Rule`, …). No import cycles: `output` ← everyone; `config` is the base domain; `doctor`/`policy` build on `config`; `commands` composes them.
- **No behavior change.** Struct JSON tags are unchanged, so all command output (human + JSON) is byte-identical; `validate:contract` stays **234/234** (CLI DTO parity guard green), and the full CLI test suite passes across the new packages. White-box tests moved alongside their code; black-box `commands_test` tests reference the new package types.
- `scripts/coverage-tools-cli.js` `coverpkg` extended to instrument the new packages (else the gate under-counts cross-package-only functions); CLI coverage holds at **97.8%** (gate 97). No gate lowered, no API/DTO/contract change.

---

## [1.9.0] — 2026-06-13 — Doctor & Config Toolkit

### Goal

Operator-facing diagnostic tooling, built entirely in the Go CLI as a pure API client — **no runtime/API/server/service/client contract changes** (`validate:contract` stays **234/234** throughout). v1.9 adds: an **offline `portier config doctor <file>`** and a **live `portier doctor`** sharing one deterministic doctor result model (`DoctorReport`/`DoctorCheckResult`) and **20 stable check codes**; **`portier explain <code>`** backed by a test-guarded explanation registry covering every code; **`--strict`** (treat warnings as failures — exit-code interpretation only, never changing which checks run), **`--out <file>`** (export the JSON report, same schema as `--json`), and **`--explain`** (inline per-check explanations) across both doctor commands; **`portier support-bundle --out <dir>`** collecting deterministic doctor artifacts (manifest + `doctor.json`/`doctor.txt` + explanations) **safely** — no environment/process/log/token/filesystem collection, and a runtime-unreachable bundle is still produced; **structured config-doctor `details`** (duplicate-binding / LAN-exposure / privileged-port / validation findings, derived only from the offline config) plus a compact, machine-readable **`config` summary**; and an offline, safety-first **AI handoff prompt** ([`prompts/doctor.md`](../prompts/doctor.md)) with no AI integration/upload/telemetry. Rounded out by a **docs cleanup** (removed unmaintained docs, renamed/tidied references) and a **release-readiness audit** (Slices 1–9 PASS). All five coverage gates hold (none lowered); CLI coverage ≈ 97.6% (gate 95%).

### Added — Slice 1: Doctor result model + offline config doctor

- **Doctor result model (CLI).** A small, deterministic doctor result model in the Go CLI: `DoctorCheckResult` (`code`, `severity`, `title`, `message`, optional `details`) with severities `info`/`warning`/`error`, plus a `DoctorReport` (`checks[]` + `summary` counts). This is the data foundation for the v1.9 Doctor & Config Toolkit; a live runtime doctor comes later.
- **`portier config doctor <file>` command.** Runs deterministic, **offline** diagnostic checks on a local config file — it does **not** require or contact a running Portier service, and never modifies the file. Human output by default; `--json` emits the full `DoctorReport`. Exit codes follow the v1.7 CLI policy: `0` when no error-severity checks (warnings alone still exit `0`), `1` when one or more error-severity checks are found, `2` for a missing/invalid argument.
- **Stable check codes.** `config.read_failed`, `config.parse_failed`, `config.empty`, `config.validation_failed`, `config.duplicate_binding`, `config.lan_exposure`, `config.privileged_port`, `config.valid`. These are operator-facing identifiers and will not be renamed casually.
- **Offline checks.** File readability, JSON parse, local config validation (field validity), duplicate listen-binding detection (reported separately from field errors), empty-config detection, and the LAN-exposure (`0.0.0.0`) and privileged-port (`< 1024`) advisories — all computed locally from the parsed file, in file order.

### Notes

- CLI-only change. The CLI stays a pure API client; no shared/server/service/client code, no API/DTO/config shape, and no plan/apply or forwarding behavior changed. `validate:contract` unchanged at **234/234**.
- Deferred (documented): a full common-port advisory table is **not** reproduced in the CLI (it would duplicate the 25-entry `@portier/shared` `COMMON_PORTS` list); only the deterministic privileged-port (`< 1024`) advisory is implemented in this slice.
- CLI coverage **97.2% → 97.4%** statements (functions 98.9% → 99.0%); gate 95% holds. No coverage gate lowered.

### Added — Slice 2: Live runtime doctor baseline

- **`portier doctor` command.** Runs deterministic diagnostic checks against the **live** Portier runtime, reusing the Slice 1 doctor model (`DoctorReport`/`DoctorCheckResult`, human + `--json` output, summary/exit-code helpers — no second model). Read-only: it never mutates the runtime or config and never probes forwarding targets.
- **Stable live check codes.** `runtime.reachable`, `runtime.unreachable`, `runtime.version`, `runtime.status_read`, `runtime.status_failed`, `rules.none`, `rules.present`, `rules.health_ok`, `rules.health_warning`, `rules.health_error`, `config.export_read`, `config.export_failed`. Operator-facing identifiers — not renamed casually.
- **Checks (deterministic, fixed order).** Runtime reachability (`GET /api/runtime`); runtime version (info, **warning** — never failure — on CLI/runtime mismatch); rule status read (`GET /api/status`); rule presence + an aggregated health summary read straight from the API's `health` field (error before warning before all-clear, so a mixed fleet reports both); and a read-only config-export check (`GET /api/config/export`, nothing written). An unreachable runtime short-circuits the later checks.
- **Exit codes.** `0` no error-severity checks (warnings alone still `0`), `1` one or more error-severity checks, `2` usage error. **Intentional deviation:** an unreachable runtime is reported as a doctor check and exits `1` (the doctor ran and found a problem), *not* the usual connection exit code `3` — because `portier doctor` always completes and emits a report. Documented in `tools/cli/readme.md`.

### Notes (Slice 2)

- CLI-only change. The CLI stays a pure API client (reuses existing `GET /api/runtime`/`/api/status`/`/api/config/export`); no new endpoints/DTOs, no shared/server/service/client change. `validate:contract` unchanged at **234/234**. Rule health is **read** from the API's `health` field — the CLI never re-derives it.
- CLI coverage **97.4% → 97.6%** statements (functions 99.0% → 99.1%); gate 95% holds. No coverage gate lowered. Every new doctor function is 100% covered.
- No target reachability probing, no background monitoring, no auto-fix, no server-side doctor endpoint, no UI doctor panel.

### Added — Slice 3: Doctor explain codes

- **`portier explain <code>` command.** Prints a deterministic, **offline** explanation of any stable doctor/check code — its meaning, what to do next, and related codes. Static reference data: no runtime contact, no external docs fetch, no AI, no remediation. `--json` emits the explanation; `portier explain --list` lists all known codes (human or JSON).
- **Explanations for all 20 stable codes** from Slices 1–2 (8 config-doctor + 12 live-doctor), keyed by the existing code constants so the strings can't drift. An internal guard test asserts every doctor code constant is explainable, the registry has no extra/duplicate entries, each entry's `Code` matches its key, required fields are present with a valid severity, and no `related` reference dangles.
- **Unknown/missing code → exit 2** with a clear error and a pointer to `--list` (never a generic explanation). Single-code success exits `0`.

### Notes (Slice 3)

- CLI-only change; pure offline reference command (no client, no API call — `explain` ignores `--url`). No shared/server/service/client change, no new endpoints/DTOs. `validate:contract` unchanged at **234/234**.
- CLI coverage **97.6% → 97.7%** statements (functions 99.1%); gate 95% holds. No coverage gate lowered. Every new explain function is 100% covered.

### Added — Slice 4: Doctor strict mode + report polish

- **`--strict` for both doctor commands** (`portier doctor --strict`, `portier config doctor --strict <file>`). Strict mode treats **warnings as failures**: a warning-only report exits `1` instead of `0`. Normal mode is unchanged (info-only → `0`, warnings-only → `0`, errors → `1`). Strict changes **only the exit-code interpretation** — it does not change which checks run, and never mutates the runtime or config.
- **Report polish.** Human output now ends with a stable `Result: passed` / `Result: failed` line, and — when strict mode is the reason a warning-only report fails — a `Strict mode: warnings are treated as failures.` note. The JSON report gains two additive top-level fields: `"strict"` (bool) and `"result"` (`"passed"`/`"failed"`). The existing `checks`/`summary` shape is unchanged (the new fields are emitted alongside via an embedded struct, so existing JSON consumers keep working).
- Shared `doctorExitCode(report, strict)` / `doctorResultLabel(report, strict)` helpers replace the old `DoctorReport.exitCode()` method; both doctor commands route through them.

### Notes (Slice 4)

- CLI-only change. No new checks, no probing, no runtime/config mutation; pure exit-code interpretation + output polish. No shared/server/service/client change, no new endpoints/DTOs. `validate:contract` unchanged at **234/234**.
- `--strict` precedes the file for `config doctor` (`config doctor --strict <file>`), consistent with the other config subcommands' flags-before-file parsing.
- CLI coverage **held at 97.7%** statements (functions 99.1%); gate 95% holds, none lowered. The change is small and fully covered, so the ratio is unchanged (added covered statements are proportional to existing coverage); every new strict function/branch is 100% covered.

### Added — Slice 5: Doctor report export

- **`--out <file>` for both doctor commands** (`portier doctor --out <file>`, `portier config doctor --out <file> <config>`). Writes the JSON doctor report — the **same** `{ checks, summary, strict, result }` shape as `--json` (Slice 4) — to a file for CI artifacts and support bundles. No second schema.
- **Output convention:** `--out` always writes the JSON report file regardless of `--json`. stdout stays normal human output unless `--json` is set (in which case the same JSON also prints to stdout). In human mode a `Report written to <file>` line confirms the export; in JSON mode stdout stays pure JSON. Reuses the existing `writePrettyJSON` helper (pretty JSON, create/truncate, no partial write on marshal failure, does not create parent directories).
- **Exit codes:** export does not change diagnostic semantics — info/warning/error and `--strict` behavior are unchanged. A **file-write failure is an operation failure → exit 1** (and is reported on stderr), taking precedence over the diagnostic exit code; a missing `--out` value is a usage error → exit 2. The report is still written even when the run "fails" on a warning under `--strict` (the file reflects `result: "failed"`).

### Notes (Slice 5)

- CLI-only change. No new schema, no probing, no runtime/config mutation; export is a read-only file write. No shared/server/service/client change, no new endpoints/DTOs. `validate:contract` unchanged at **234/234**.
- `--out` (and `--strict`) precede the config file for `config doctor` (flags-before-file), consistent with the other config subcommands.
- CLI coverage **held at 97.7%** statements (functions 99.1%); gate 95% holds, none lowered. The export additions are small and fully covered (`emitDoctorReport`/`RunDoctor`/`RunConfigDoctor` 100%), so the ratio is unchanged.

### Added — Slice 6: Doctor support bundle baseline

- **`portier support-bundle --out <directory>`** — collects deterministic doctor artifacts into a local directory for CI artifacts, bug reports, and future tooling. Read-only: it contacts the live runtime but never mutates the runtime or its config. Directory output only (no zip yet).
- **Bundle contents:** `manifest.json` (schemaVersion, generatedAt, cliVersion, source/generatedBy, runtimeUrl, strict, result, artifacts list, collection warnings), `doctor.json` (the **same schema as `portier doctor --json`** — no second doctor schema), `doctor.txt` (the human doctor report), `explanations.json` (all known doctor/check code explanations, same as `explain --list --json`), and — when the runtime provides them — `runtime.json` and `config-export.json` (read-only). If the runtime is unreachable the bundle is still created with the manifest, `doctor.json` (containing `runtime.unreachable`), `doctor.txt`, and `explanations.json`, and the omitted artifacts are recorded as manifest warnings.
- **Flags:** `--out <dir>` (required), `--strict` (apply strict interpretation to the bundled doctor report), `--json` (print a machine-readable bundle summary to stdout). Reuses the existing doctor report builder/renderer, the explanation registry, and `writePrettyJSON` — no duplicated doctor logic.
- **Exit codes:** `0` bundle written and the doctor report passed; `1` bundle written but the doctor report failed (errors, unreachable runtime, or a warning under `--strict`), **or** a filesystem/write failure; `2` missing `--out` or usage error. An existing **non-empty** output directory (or a non-directory path) is **refused** (exit 1) — a bundle is never silently overwritten; an existing empty directory is used.
- **Safety:** the bundle deliberately excludes OS environment variables, process lists, logs, tokens, and arbitrary filesystem scans. It **may** include rule names, hosts, and ports, because those are part of Portier configuration/diagnostics. (Distinct from the existing single-file `portier diagnostics export` bundle; `support-bundle` is the doctor-centric directory bundle.)

### Notes (Slice 6)

- CLI-only change. No second doctor/bundle schema, no probing, no runtime/config mutation. No shared/server/service/client change, no new endpoints/DTOs. `validate:contract` unchanged at **234/234**.
- CLI coverage **97.7% → 97.4%** statements (functions 99.1%); gate 95% holds, **none lowered**. The small dip is from unavoidable defensive per-file filesystem-write-failure branches that cannot be triggered portably once the output directory is validated as empty and writable — the command-level filesystem-failure → exit 1 behavior **is** covered (mkdir failure, non-empty directory, `--out` is a file). The bundle writes were consolidated into one loop to minimize the unreachable surface.

### Added — Slice 7: Doctor inline explanations

- **`--explain` for both doctor commands** (`portier doctor --explain`, `portier config doctor --explain <file>`). Adds an inline explanation — code, meaning, and next action (plus related codes) — for the checks that **actually appear** in the report, reusing the Slice 3 explanation registry (no duplicated strings).
- **Human output:** each check is followed by an indented `Code: / Meaning: / What to do: / Related:` block. Only emitted codes are explained.
- **JSON output:** an **additive** top-level `explanations` map (`code → { code, title, meaning, action, severity, related }`) is included only with `--explain`, and only for codes present in `checks` (deduplicated). The existing `checks`/`summary`/`strict`/`result` shape is unchanged; without `--explain` the JSON is byte-identical to before (the field is `omitempty`).
- **Combines with `--strict`, `--json`, and `--out`:** `--out --explain` writes JSON with explanations; `--json --out --explain` keeps stdout and file byte-identical. Works for `config doctor` with flags before the file.
- **`--explain` changes nothing else:** it does not change which checks run, the summary, the result, or exit codes (info/warnings → 0, errors → 1; strict warnings → 1). Codes with no registry entry degrade safely (`(no explanation available)`), and an internal test guarantees every emittable doctor code is explainable so that fallback never triggers in practice.

### Notes (Slice 7)

- CLI-only change. Reuses the doctor report model + explanation registry; support-bundle output is unchanged (`doctor.json`/`doctor.txt` carry no inline explanations — the bundle already ships `explanations.json`). No second schema, no probing, no runtime/config mutation. No shared/server/service/client change, no new endpoints/DTOs. `validate:contract` unchanged at **234/234**.
- The doctor emit flags were grouped into a small `doctorEmitOptions` struct to avoid parameter sprawl (internal refactor, no behavior change).
- CLI coverage **97.4% → 97.5%** statements (functions 99.1% → 99.2%); gate 95% holds, none lowered — recovering part of the Slice 6 dip. Every new explain helper is 100% covered.

### Added — Slice 8: Config doctor advisory details

- **Structured `details` on config doctor findings.** `portier config doctor --json` now attaches deterministic, JSON-serializable `details` to its checks, making JSON reports, exports, and inline explanations more useful — **no new check codes** (existing codes enriched).
  - `config.duplicate_binding`: a structured `bindings` array (each `{ protocol, listenHost, listenPort, rules: [{ id?, name, enabled, group? }] }`) identifying every conflicting binding and the rules on it, alongside the existing `errors` strings. Bindings sorted by protocol→host→port; rules in file order.
  - `config.lan_exposure` / `config.privileged_port`: each now **aggregates all affected rules into one check** with a `rules` array (`{ id?, name, protocol, listenHost, listenPort, enabled, group? }`, file order). (Previously one check per affected rule — the warning is now one-per-advisory-kind. Exit behavior is unchanged.)
  - `config.empty`: `{ ruleCount: 0 }`. `config.validation_failed`: `{ errors, ruleCount }`. `config.valid`: `{ ruleCount, tcpCount, udpCount }`.
- **Derived only from the offline config** — details contain no environment variables, process info, logs, filesystem data, runtime data, or probe results; only rule names/hosts/ports/groups/ids from the file. IDs are included only when present in the config (`omitempty`); groups are trimmed.
- **LAN exposure stays the `0.0.0.0` check** (consistent with the shared advisory — `::` is intentionally not added so which rules are flagged is unchanged); the privileged-port check stays the deterministic `< 1024` advisory (no `COMMON_PORTS` duplication).

### Notes (Slice 8)

- CLI-only change. Details flow naturally through `--json` and `--out` (they are part of `DoctorCheckResult`); `--explain` stays presentation-only and compatible; **support-bundle output is unchanged** (it runs the *live* doctor, not config doctor). No shared/server/service/client change, no new endpoints/DTOs/codes. `validate:contract` unchanged at **234/234**.
- Human output is essentially unchanged (details are a JSON concern); the aggregated advisory titles read `Rule "X" listens on 0.0.0.0` for one rule and `N rules listen on 0.0.0.0` for several.
- CLI coverage **held at 97.5%** statements (functions 99.2%); gate 95% holds, none lowered. Every new detail builder (`findDuplicateBindings`, `toRuleDetail`, the aggregated advisories, titles) is 100% covered; the ratio is unchanged because the fully-covered additions are proportional to existing coverage.

### Added — Slice 9: Config doctor summary + machine-readable counts

- **Top-level `config` summary on config doctor JSON.** `portier config doctor --json` now emits a compact, deterministic, machine-readable config summary so CI/tooling can understand a config's shape without parsing individual checks: `{ ruleCount, enabledRuleCount, disabledRuleCount, protocols: { tcp, udp }, groupCount, groups: [ { name, ruleCount, enabledRuleCount, disabledRuleCount } ], ungroupedRuleCount }`.
- **Lowest-risk placement:** an optional `config` field on the shared doctor JSON wrapper (`doctorReportJSON`), populated **only by config doctor** and `omitempty`. The live `portier doctor` and the support bundle never set it, so their JSON is byte-identical to before. The `checks` array shape is unchanged.
- **Deterministic & offline:** groups are sorted by (trimmed) name; empty/whitespace groups count as ungrouped; protocol counts use a stable `{ tcp, udp }` shape (unknown protocols simply aren't counted); the summary is derived **only from the offline config** (no environment/runtime/filesystem data) and does **not** repeat full rule data (Slice 8 details already identify affected rules). Present whenever the file parses — including an empty config (`ruleCount: 0`) and a validation-failure config; absent on read/parse failure.
- **Composes cleanly:** flows through `--out` (and `--json --out` stdout/file stays byte-identical); `--explain` stays presentation-only and does not alter the summary; `--strict` only changes the result/exit code, not the summary. **No new check codes.**

### Notes (Slice 9)

- CLI-only change. No shared/server/service/client change, no new endpoints/DTOs/codes, no support-bundle change (it runs the live doctor). `validate:contract` unchanged at **234/234**. Human output is unchanged (the summary is a JSON concern); `listenHostCount`/`listenPortCount` were intentionally omitted to keep the summary compact.
- CLI coverage **97.5% → 97.6%** statements (functions 99.2%); gate 95% holds, none lowered. The new `buildConfigSummary` builder is 100% covered.

### Slice 10: Doctor toolkit release-readiness audit — PASS

Focused release-readiness audit of the v1.9 Doctor & Config Toolkit (Slices 1–9). Audit/cleanup only; **no new features**. Result: **ready for finalization** with one tiny consistency fix.

- **Command surface** (audited against the binary): all `doctor` / `config doctor` / `explain` / `support-bundle` flag combinations (`--json`/`--strict`/`--out`/`--explain` and their combinations) dispatch, help, and exit correctly; `explain` stays fully offline (no URL resolution); `doctor`/`support-bundle` correctly resolve the runtime URL.
- **JSON schema** (verified): doctor JSON is `{ checks, summary, strict, result }` + `explanations` only with `--explain` + `config` only for config doctor after a successful parse; the live `doctor` JSON never carries `config`; non-`--explain` output carries no `explanations`; `--json --out` stays byte-identical (both config and live doctor).
- **Check codes & explanations** (verified): all 20 codes are explainable, the registry has no extra/duplicate/dangling entries (guarded by tests), `explain --list` is sorted, inline explanations include only emitted codes, and **no check-code string literals exist outside the constants** (only a doc-comment example and the `runtime.json` bundle filename matched).
- **Exit codes** (verified end-to-end): normal info/warn → 0, error → 1; strict warn/error → 1, info → 0; usage → 2; unreachable `doctor`/`support-bundle` → 1 with a useful report/bundle still produced; filesystem/write failure → 1; unknown `explain` code → 2.
- **Safety** (verified): no doctor command mutates runtime/config (only read-only `GET /api/runtime`/`/api/status`/`/api/config/export`; config doctor is offline file-read only); no env/process/log/exec collection anywhere; the support bundle excludes env vars/process lists/logs/tokens/filesystem scans and refuses a non-empty output directory (no silent overwrite); the live doctor does not probe targets; `--strict`/`--explain`/`--out` never change which checks run.
- **Fix made:** aligned the `portier doctor` help exit-code text to mention the `--out` write-failure → exit 1 case (parity with `config doctor` help). Help-text only — no behavior/coverage/contract change.
- **Deferred non-blockers:** (1) `config doctor` (and `config validate`) still flow through the URL-resolving `config` dispatch, so a malformed `--url` passed to these offline subcommands fails at URL validation (exit 2) before the offline work — pre-existing, consistent with `config validate`, and only reachable by passing connection flags to an offline command; a clean fix needs a dispatch refactor (out of audit scope). (2) Two platform-dependent defensive filesystem-error branches in `prepareBundleDir`/`RunSupportBundle` remain uncovered (can't be triggered portably once the output dir is validated empty+writable); the command-level filesystem-failure → exit 1 paths are fully tested.
- **Validation:** `go test ./...` green; `validate:cli` green; `validate:coverage:cli` **97.6%** (gate 95% PASS, none lowered); `validate:contract` **234/234**; `lint` + `typecheck` clean. **v1.9 is ready for version bump/finalization.**

### Added — Slice 10: Doctor AI handoff prompt (docs-only)

- **Reusable AI handoff prompt** in [`prompts/doctor.md`](../prompts/doctor.md) — a polished, copy-paste prompt (a **full** version and a **short** version) for asking an AI assistant to help interpret `portier doctor` / `portier config doctor` output or a support-bundle summary. Referenced from `tools/cli/readme.md` ("Using doctor output with an AI assistant") and the glossary.
- **Safety-first and fully offline.** Portier adds **no** AI integration, upload, or telemetry — the prompt is plain text the user pastes into an assistant of their choice, together with output they generated locally. The prompt instructs the assistant to: treat the pasted report as the **only source of truth** and not invent runtime state; prefer JSON over human output and flag conflicts; distinguish `info` / `warning` / `error`, strict-mode failures, and operation failures; explain stable check `code`s via the `explanations` map; respect Portier semantics (runtime-reported health, `0.0.0.0` LAN exposure, `< 1024` privileged ports, protocol+host+port duplicate bindings); and **never ask for secrets, tokens, keys, environment dumps, process lists, or logs**. It asks for a verdict, prioritized findings (errors first), likely cause, a low/medium/high risk level, and **separated** "safe to try now" vs "requires admin/network/security review" next steps.
- **Docs-only.** No CLI/Go/TS code changed (a `--prompt`/`prompt` CLI helper was considered and intentionally deferred to avoid a second copy of the prompt that could drift from the doc). No shared/server/service/client change, no new endpoints/DTOs/codes, no doctor checks. `validate:contract` unchanged at **234/234**; CLI coverage unchanged at 97.6% (no code added).

---

## [1.8.0] — 2026-06-11 — Operator Power Tools

### Goal

Operator power tools built around **rule groups** and day-to-day conveniences — all backward-compatible (existing configs and API clients keep working unchanged). Ten slices: optional, behavior-neutral `group` metadata end to end (Slice 1) with UI set/edit/clear/display (Slice 2), rule-list group filtering (Slice 3), a group start/stop API in both runtimes (Slice 4), CLI `group list/start/stop` commands (Slice 5), and UI group action buttons (Slice 6); plus derived rule **health** (`healthy`/`warning`/`error`) on the status API, web UI, and CLI (Slice 7); a client-side **duplicate rule** action over the existing create flow (Slice 8); **config preview** clarity in Settings — friendly field labels, group/metadata-vs-forwarding change tags, and a clear metadata-only signal for group-only changes (Slice 9); and a per-row **action menu (kebab)** plus a dedicated **Health column** that declutter the Forward Rules table (Slice 10). The only public-contract additions are the optional `group` field, the `POST /api/forwards/groups/:group/{start,stop}` endpoints + `GroupActionResponse`, and the required `ForwardStatus.health` field — all additive. `group` is optional and non-forwarding (a group-only change is a non-destructive plan update, never a socket restart); health is derived deterministically with no probing or background monitor. `validate:contract` ends at **234/234** (185 → 204 → 225 → 234 across Slices 1/4/7); all five coverage gates hold (none lowered).

### Changed

- **Forward Rules — per-row action menu (kebab)** — The Forward Rules table row was getting crowded, so the lower-priority actions moved into a compact **overflow menu** (`MoreVertical` "⋮" trigger) on the right of each row (`client/sources/features/forwards/ForwardRuleList.tsx` + `styles.css`). **Only Start/Stop stays inline, as a button** — **Edit**, **Duplicate**, **Diagnose**, **Activity** (renamed from "View activity"), and **Delete** are now in the menu, in that order (Edit first). Each item keeps its icon (`Pencil`/`Copy`/`Stethoscope`/`Activity`/`Trash2`); **Delete is styled red** (`row-menu-item--danger`). The "⋮" trigger is **seamless — styled like the reorder drag handle** (no button border/background, a muted glyph that brightens on hover / while open), not a boxed icon button. The menu is a `role="menu"` (labelled `Actions for <rule name>`) of `role="menuitem"` buttons opened by a `More actions for <rule name>` trigger (`aria-haspopup="menu"` + `aria-expanded`); it **closes on outside click or Escape**, only one row's menu is open at a time, and the trigger is disabled while the row is busy or mid delete-confirm. The existing delete **confirmation flow is unchanged** — choosing Delete still shows the inline "Delete "X"? Confirm/Cancel" prompt in the Actions cell. This replaces the old standalone trash column (`delete-cell*` styles removed); column count/`colSpan` are unchanged (the trash column became the menu column). The **"Actions" column header is now blank**, the **Traffic column is given a modest `min-width`** (not greedy — the text columns keep the slack), and the action column shrinks to its content (`actions-col`) so the Start/Stop button sits toward the right edge near the kebab with comfortable spacing. **No API/behaviour change.** Tests: `ForwardRuleList`/`App` interactions for the moved actions (incl. Edit) now open the menu first and target `role="menuitem"`; new menu tests (Start stays outside the menu; menu lists Edit/Duplicate/Diagnose/Activity/Delete in order with Delete as the danger item; Edit/Delete fire their handlers; closes on Escape and on outside click); the edit/duplicate/diagnose/delete E2E flows in `tests/e2e/portier.spec.ts` open the menu via the `More actions for <name>` trigger. 449 client unit tests + 39 E2E pass; client gates 94/89/78 hold (none lowered).

- **Rule form — Listen Host presets redesign** — Reworked the **Local only** / **LAN exposed** quick-select in the add/edit rule drawer (`client/sources/features/forwards/ForwardRuleForm.tsx` + `styles.css`) from two loose buttons above the input into a polished, full-width segmented control sitting **below** the Listen Host / Listen Port row. Each segment carries an icon (`Lock` for Local only, `Globe` for LAN exposed, both `aria-hidden`) and is risk-coloured when active — **green** for Local only (safe) and **amber** for LAN exposed (reachable from the network) — and now exposes `aria-pressed` toggle state. The free-text Listen Host **input is unchanged** (a specific interface/NIC IP can still be typed); the presets just set the two common values. No behavior, validation, payload, or accessible-name change (the buttons keep their "Local only" / "LAN exposed" names); all form unit tests and the rule-CRUD E2E pass unchanged.

### Added

- **v1.8 Slice 9 — Config preview UX polish** — Polished the Settings **Plan & Apply Config** preview so operators can reason about a config change before applying it, with special clarity around **group** (a material but non-forwarding field). **Client-only change — no shared/server/service/CLI code touched, no API/DTO/config or plan/apply semantics change; `validate:contract` stays 234/234.** **What's clearer:** the plan summary now also surfaces a labelled **`Destructive: N`** count when any operation is destructive (a named number, not colour-only). Each operation row is rebuilt as a structured card with an **action badge** (Add / Update / Remove / Unchanged), the rule name + protocol, an explicit **impact note** in plain language (`Creates a new rule` / `Removes this existing rule` / `Metadata only — the forwarder is not restarted` / `Changes forwarding — the forwarder will restart` / `No changes`), and **text tags** for `Destructive` and `Metadata only` (so destructive/metadata state never relies on colour alone). Each changed field row now shows a **friendly field label** (`Listen port`, `Target host`, `Autostart`, `UDP mode`, `Group`, …) plus a small **`forwarding` / `metadata` impact tag** and the `before → after` values. **Group changes are first-class:** a group add/remove/change is shown with its values (`(none) → backend`) and is explicitly labelled metadata-only — the preview never implies a socket restart for a group-only edit, because the impact is derived from the *actual changed fields* (a client-side mirror of the server's `FORWARDING_FIELDS`: `protocol`/`listenHost`/`listenPort`/`targetHost`/`targetPort`/`udpMode`), not guessed. **No behaviour change:** the existing no-drift/in-sync message, plan errors (apply disabled), warnings, the destructive confirmation checkbox + gated Apply button, and the apply success/failure/`ok:false` states are all unchanged; the server remains authoritative for the `destructive` flag the Apply gating uses. **Implementation:** new pure helpers in `planHelpers.ts` — `formatFieldLabel`, `changeImpact` (forwarding vs metadata), `isMetadataOnlyUpdate`, `describeOperationImpact` — consumed by `PlanApplySection.tsx`; new scoped CSS for the operation cards/badges/tags in `styles.css`. **Accessibility:** the preview keeps its semantic structure (lists, headings, `role="alert"`/`role="status"` for errors/success), destructive and metadata states are conveyed by **text** (badges/tags + impact sentence), and field changes read as label + impact + values. **Also in this slice — Forward Rules Health column:** the Slice 7 rule health (previously a bare dot tucked next to the Status badge) now has its **own `Health` column** on the Forward Rules table, showing the colour dot **plus a one-word text label** (`Healthy`/`Warning`/`Error`, so health is not conveyed by colour alone), with a `—` placeholder when a rule has no status yet. Status and Health stay distinct columns (lifecycle vs operator reading); the dot keeps its accessible `role="img"` label/tooltip, and the new text label is `aria-hidden` to avoid a duplicate screen-reader announcement. `RuleHealthBadge` gained a `healthShortLabel` helper; the table `colSpan` placeholders moved 10 → 11. **Tests:** new `planHelpers` unit tests (field labels incl. fallback; forwarding/metadata classification; `isMetadataOnlyUpdate` for group-only / mixed / non-update / empty; impact strings for add/remove/unchanged/metadata-update/forwarding-update); new `SettingsView` tests (group-only update shows the Group change + `Metadata only` + no-restart + non-destructive apply enabled; destructive remove shows the `Destructive` text tag + `Removes this existing rule` + `Destructive: 1` summary); the existing "update shows field changes" test updated to the friendly `Target port` label + restart note; new Health-column tests (`Health` column header; dot + one-word `Warning` label; `—` placeholder when a rule has no status) + `healthShortLabel` unit tests; plus an E2E in `tests/e2e/settings.spec.ts` (seed an ungrouped rule → plan a desired config that only adds a group → preview shows the Group change marked metadata-only with no restart and no destructive confirmation → apply → the rule shows the `backend` group chip). All 444 client unit tests and 39 E2E pass; client coverage 96.1/91.7/83.5, gates 94/89/78 hold (none lowered). Validated: `npm run test -w client`, `npm run validate:coverage:client`, `npm run validate:contract` (234/234), `npm run lint`, `npm run typecheck`, `npm run test:e2e`. Docs: changelog, `docs/checklist.md` (manual QA for preview clarity + group changes), durable config-preview UI rule in `CLAUDE.md`/`AGENTS.md`; `docs/api-contract.md` unchanged (no API change). Source: `docs/roadmap.md` v1.8.

- **v1.8 Slice 8 — Duplicate rule action** — Added a safe **duplicate-rule** convenience so operators can create a new rule based on an existing one. **Client-only change — no shared/server/service/CLI code touched, no new backend endpoint, no API/DTO/config schema change; `validate:contract` stays 234/234.** **UX:** each rule row gained a **Duplicate** action (a `Copy` icon button between Edit and Diagnose) with the rule-scoped accessible name `Duplicate rule <name>`. Clicking it opens the existing rule drawer in **create mode**, pre-filled from the source rule, with a clear **"Duplicate Rule"** title and a "New rule copied from …" subtitle (the drawer's accessible name is "Duplicate Forward Rule"). **The source rule is never modified** — the duplicate is not created until the user submits, and saving goes through the normal create path (`POST /api/forwards`, `id` undefined), not update. **Pre-fill rules** (`ruleToDuplicateForm` in `RuleForm.ts`): copies the editable definition fields (protocol, listen host/port, target host/port, UDP mode, **group**) and adjusts the name to `<name> copy`; **drops the id** (so it saves as a new rule) and **forces `enabled` (autostart) off** so a duplicate can never auto-start on save (a freshly created enabled rule starts immediately — see `addRule`). Runtime-only state (status, `lastError`, health, active connections/sessions) is inherently excluded — it was never part of the form. **Validation is unchanged:** the duplicate passes the same client + server validation as any new rule, including duplicate listen-binding protection — if the user saves without changing the conflicting listen host/port/protocol, the existing create error is shown (no special-casing in the duplicate flow). **Group metadata is preserved**, so a duplicate appears under the same group filter (Slice 3) and shows the same group chip after refresh. **Implementation:** `ForwardRuleForm` gained an optional `duplicateSource` prop (create-mode prefill; ignored when `editingRule` is also set — edit wins); `ForwardRuleList` gained an optional `onDuplicate` prop (the button only renders when provided, matching the existing optional-action pattern); `App` tracks a `duplicateSourceId`, derives the source rule, opens/clears it alongside the other form-open paths, and keys the drawer on `dup-<id>` so re-duplicating a different rule re-initializes the form. **Tests:** new `RuleForm.test.ts` (10 tests for `duplicateName`/`ruleToDuplicateForm`: copies fields + group, drops id, copy-name, forces autostart off, does not mutate the source, normalizes absent group); `ForwardRuleForm` duplicate-mode tests (title/subtitle, create-mode submit button, pre-fill incl. group, saves via create with `id` undefined + `enabled:false`, edit-wins); `ForwardRuleList` tests (button shown only with `onDuplicate`, rule-scoped name, calls handler, disabled while busy); `App` integration tests (Duplicate opens create form pre-filled; saving calls create not update and the source is unchanged; Cancel makes no save call); and an E2E in `tests/e2e/portier.spec.ts` (create a grouped rule → Duplicate → change name + listen port → save → both rules exist, group chip preserved, source's port unchanged, duplicate `enabled:false`). The new Duplicate label collided with four pre-existing E2E selectors that matched action buttons by **loose** substring name (e.g. `{ name: "Edit" }` also matched "Duplicate rule E2E **Edit** Original"); those four selectors (Edit/Start/Stop/Delete/Diagnose) were tightened with `exact: true`. All 425 client unit tests and 38 E2E tests pass; client coverage 96.0/91.6/83.1, gates 94/89/78 hold (none lowered). Validated: `npm run test -w client`, `npm run validate:coverage:client`, `npm run validate:contract` (234/234), `npm run lint`, `npm run typecheck`, `npm run test:e2e`. Docs: changelog, `docs/checklist.md` (manual QA), `docs/glossary.md` (canonical "duplicate rule"), durable UI rule in `CLAUDE.md`/`AGENTS.md`; `docs/api-contract.md` unchanged (no API change). Source: `docs/roadmap.md` v1.8.

- **v1.8 Slice 7 — Rule health basics** — Added a lightweight, operator-facing **health** classification (`healthy` / `warning` / `error`) so operators can see at a glance which rules are fine, drifting, or failing. **Derived deterministically from existing runtime state — NO target probing, NO background check, NO new network I/O, no rule mutation.** Exposed on the runtime status API so the web UI and CLI share one definition. **Model (priority order, identical in both runtimes):** `error` when a rule has a current non-empty `lastError` (failed start / socket error); else `warning` when the rule is `enabled` (autostart) but **not** running (expected to run but isn't); else `healthy` (running cleanly, or intentionally stopped). Health is **distinct from the lifecycle `running` state** — it is the operator's reading. **Shared:** `RuleHealth` type + a required `health` field on `ForwardStatus` + the pure `deriveRuleHealth({enabled, running, lastError})` helper in `@portier/shared`. **TS server + Go service:** the **manager** is the single place that derives health (it owns the rule's `enabled` flag) — `ForwardManager.getStatus` (TS, with the forwarders now returning a health-less `ForwarderStatus`) and `Manager.statusForRule` (Go, via `domain.DeriveRuleHealth`); forwarders do not classify health. **Client:** a compact colour-coded **health dot** (`RuleHealthBadge`, green/amber/red) with an accessible label/tooltip is shown next to the existing status badge in the rule list — the lifecycle status badge is unchanged (status vs health kept distinct). **CLI:** `portier status` gained a `HEALTH` column (human output) and the `ForwardStatus` DTO/`--json` now include `health`. **Contract:** `validate:contract` **225 → 234** — `GET /api/status` now asserts the `health` field + valid value, a new self-contained `rule-health` scenario drives running / enabled-stopped / disabled-stopped and asserts `healthy`/`warning`/`healthy`, and a cross-runtime `status:health` parity capture confirms TS/Go agree. New unit tests: shared `deriveRuleHealth` (all branches incl. whitespace-lastError), TS `ForwardManager` (healthy/running/warning), Go `domain.DeriveRuleHealth` (table) + `manager` status health (healthy/warning/running/**error-on-failed-start**), CLI `status` (HEALTH column + JSON), client `RuleHealthBadge` + a `ForwardRuleList` badge assertion; ~30 existing `ForwardStatus` test fixtures gained `health` (required field). All coverage gates hold (none lowered). **Existing status/lifecycle/forwarding behaviour is unchanged; the `lastError` lifecycle is unchanged** (whether a `lastError` survives a failed start follows the existing per-runtime behaviour — documented, not modified). Validated: `npm run test`, `go -C service test ./...`, `go -C tools/cli test ./...`, `npm run validate:coverage`, `npm run validate:contract` (234/234), `npm run validate:cli`, `npm run validate:runtime:smoke`, `npm run lint`, `npm run typecheck`, `npm run test:e2e`. Docs: changelog, `docs/api-contract.md`, `docs/glossary.md` (canonical "health"), `docs/checklist.md`, `tools/cli/readme.md`, durable rule in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.8.

- **v1.8 Slice 6 — UI group action buttons** — Exposed the v1.8 Slice 4 group start/stop API in the web UI so operators can start or stop every rule in a selected group. **Client-only change — no shared/server/service/CLI code touched; no API/DTO shape change (uses the Slice 4 `setGroupRunning` groundwork from Slice 5's sibling client method); `validate:contract` stays 225/225.** **UX:** when the **Filter by group** control (Slice 3) is set to one **concrete** group, a compact group-action toolbar appears under the rule-list header with `Start group "<name>"` and `Stop group "<name>"` buttons (`ForwardRuleList.tsx`). The toolbar is **hidden for All Groups and for Ungrouped** (no ungrouped bulk action exists) and only renders when an `onGroupAction` handler is provided — so the rule list is otherwise unchanged. Clicking a button calls `POST /api/forwards/groups/:group/{start,stop}` (via `App.handleGroupAction` → `setGroupRunning`), shows a per-action loading state (`Starting…`/`Stopping…`, both buttons disabled to prevent double-submit), **refreshes rule/status data** through the existing `refreshAll` pattern on success, and shows a concise inline summary near the buttons: `Started group "web": 2 succeeded, 1 skipped (3 total)` (`role="status"`); when `failed > 0` the summary is a warning (`role="alert"`, amber); an API error (e.g. the `404` "No rules found in group" or any rejection) shows the server message inline (`role="alert"`, red) using existing error conventions — no new global notification system. The previous result is cleared when the group selection changes (it is scoped to that group). **Accessibility:** buttons carry the group-scoped accessible name (`Start group "web"`) so they are unambiguous even though only one group's actions are shown at a time; the toolbar is a labelled `role="group"`; status/error use `status`/`alert` live regions; keyboard interaction is standard. **No rule-definition mutation, no rule-list redesign, no profiles/tags/colors/rename/management screen.** **Tests:** 11 new `ForwardRuleList` unit tests (buttons shown only for a concrete group; hidden for All Groups / Ungrouped / no-handler; start/stop call the handler with the right args; success summary; failed→warning; rejection→error; in-flight disables both + blocks double-submit; result cleared on filter change) + 2 `App` integration tests (start/stop call `setGroupRunning` and refresh) + an E2E in `tests/e2e/portier.spec.ts` (create two grouped rules → filter to the group → Start group → both Running + summary → Stop group → both Stopped). All existing client/E2E tests pass; client coverage gates 94/89/78 hold (none lowered). Validated: `npm run test -w client`, `npm run validate:coverage:client`, `npm run validate:contract` (225/225), `npm run lint`, `npm run typecheck`, `npm run test:e2e`. Docs: changelog, checklist (manual QA), durable UI rule in `CLAUDE.md`/`AGENTS.md`; `docs/api-contract.md` unchanged (no API change). Source: `docs/roadmap.md` v1.8.

- **v1.8 Slice 5 — CLI group commands** — Exposed the v1.8 Slice 4 group operations API through the Go CLI: `portier group list`, `portier group start <group>`, `portier group stop <group>` (singular `group` noun, matching the existing `config` subcommand style). **CLI-only change — no shared/server/service/UI code touched; the CLI stays a pure API client; `validate:contract` stays 225/225.** **`group list`** derives the distinct non-empty groups from the live rules (via `GET /api/forwards` + `GET /api/status`), sorted alphabetically, with per-group rule and running counts; prints `No rule groups configured.` (exit `0`) when no rule has a group; human output is a `GROUP / RULES / RUNNING` table and `--json` emits `{ groups: [{ group, total, running }] }`. **`group start`/`stop`** call `POST /api/forwards/groups/:group/{start,stop}` and print a summary line (`N succeeded, N skipped, N failed (N total)`) + a `RULE / STATUS / REASON` table; `--json` emits the full `GroupActionResponse`. **Exit codes follow the v1.7 Slice 7 policy:** `0` success — *including* rules skipped for `already_running`/`not_running` (a skip is not a failure); `1` API/runtime error, no-matching-group (the API's `404`), or `failed > 0`; `2` missing/invalid group argument; `3` connection failure. The group argument is **trimmed and validated locally** (non-empty `group is required.`, ≤ 64 chars, no control characters — reusing the Slice 1 `groupMaxLength`/`hasControlChar` helpers) before the request, with the server remaining authoritative. **Client:** new `client.StartGroup`/`StopGroup` methods + `GroupActionResponse`/`GroupActionResult` DTOs (`url.PathEscape` keeps an encoded `/` in a group name a single path segment); `group` wired into `main.go` dispatch and the root help. **Arch-D guard extended:** `validate:contract`'s CLI-fixture capture now starts a dedicated grouped rule and captures a live `group-start.json`, which `TestCLIDTOContractParity` strictly decodes into `GroupActionResponse` — proving the new CLI DTO matches real runtime output (the main capture rule stays ungrouped so plan/apply fixtures remain drift-free). **No mutation of rule definitions; no UI changes; no profiles/tags/colors/rename/ungrouped-bulk.** New black-box `commands_test` coverage: list (human/JSON/empty/connection-error), start (success/skipped/JSON/failed→exit 1/404→exit 1/missing-arg→exit 2/invalid-group→exit 2/connection→exit 3), stop (success/encoded-group-path), and dispatch (no-args/help/unknown-subcommand); `main_test.go` dispatch table extended with `group`. CLI coverage gate (95%) holds; no gates lowered. Validated: `go -C tools/cli test ./...`, `npm run validate:cli`, `npm run validate:coverage:cli`, `npm run validate:contract` (225/225), `npm run lint`, `npm run typecheck`. Docs: changelog, `tools/cli/readme.md` (command/exit-code/JSON docs), checklist, durable CLI rule in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.8.

- **v1.8 Slice 4 — Group operations API** — Added backend/API support to **start or stop all rules in a group**, in both runtimes, with **no UI buttons or CLI commands** (deferred). New endpoints `POST /api/forwards/groups/:group/start` and `POST /api/forwards/groups/:group/stop` (chosen to nest under the existing `/api/forwards` namespace; the 4-segment path cannot collide with the 3-segment `/api/forwards/:id/start`). **Behaviour over existing rule metadata — never mutates rule definitions, order, `enabled`, `group`, or duplicate-binding behaviour; per-rule lifecycle and activity events (`rule.started`/`rule.stopped`/`rule.error`) are identical to the single-rule endpoints (no new event type).** **Group identity:** matching uses the normalized (trimmed, Slice 1) group value, exact/case-sensitive; the `:group` path is validated with the same rules **plus** a non-empty requirement (`group is required.` for empty/whitespace) since a group operation needs a target — added `validateGroupName` to `@portier/shared` and `validation.ValidateGroupName` to the Go service. **Semantics (documented, deterministic, parity-matched):** rules are processed in **rule order**; **start** starts every matched rule that is not already running (autostart/`enabled` is *not* a precondition, matching single-rule start), skips already-running rules (`reason: already_running`), and a start error yields `failed` without aborting the rest (partial success); **stop** stops running matched rules and skips non-running ones (`reason: not_running`). **No matching group → `404`** with the standard `{ errors: [...] }` envelope (group ops do not silently no-op on an absent target). **Response shape** `GroupActionResponse { group, action, total, succeeded, skipped, failed, results: [{ ruleId, ruleName, status, reason? }] }` — shared DTOs in `@portier/shared` (`shared/sources/groups.ts`) + Go `domain.GroupActionResponse`/`GroupActionResult`; the summary counts are built by the shared `summarizeGroupAction` (TS) mirrored by Go `buildGroupActionResponse`. **Implementation:** `ForwardManager.startGroup`/`stopGroup` (TS) and `Manager.StartGroup`/`StopGroup` (Go) orchestrate per-rule start/stop over the matched, in-order rule set; the HTTP handlers validate the group, return `404` on an empty match, and serialize the summary. **Client:** a typed `setGroupRunning(group, running)` was added to the API client (`portierApi.ts`) as groundwork for a later UI slice — **no UI wiring**; the in-app API Docs view (`ApiDocsView.tsx`) documents both endpoints (project rule). **CLI:** unchanged (no group commands; CLI stays a pure API client, group endpoints out of CLI DTO scope). **Validation:** `validate:contract` **204 → 225** (new self-contained `group-action` scenario group: start/stop/skip/no-match/invalid-group/metadata-preserved/other-group-untouched + a cross-runtime `group:start` parity capture; all created rules cleaned up). New unit tests: TS `ForwardManager` (start/stop/skip/no-match/**partial-failure**/no-mutation) + `api.test.ts` HTTP (start/stop/skip/404/400-too-long/400-whitespace/encoded-space); Go `manager` (same set incl. partial-failure) + `api` HTTP + `validation.ValidateGroupName`; client `portierApi`/`ApiDocsView`. All coverage gates hold (none lowered). Validated: `npm run test`, `go -C service test ./...`, `go -C tools/cli test ./...`, `npm run validate:coverage`, `npm run validate:contract` (225/225), `npm run validate:config`, `npm run validate:runtime:smoke`, `npm run lint`, `npm run typecheck`, plus `npm run test -w client` (client surface touched). Docs: changelog, `docs/api-contract.md` (endpoints/shape/errors/validation), checklist, durable rule in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.8.

- **v1.8 Slice 3 — Group filtering and grouping groundwork** — Added a **Filter by group** control to the Forward Rules list so operators can scan/browse rules by group — **read-only navigation only** (no group start/stop, bulk actions, profiles, tags, colours, management screen, or persistence). **Client-only change — no shared/server/service/CLI/API/DTO/config touched; `validate:contract` stays 204/204.** In `client/sources/features/forwards/ForwardRuleList.tsx`: a third `<select className="filter-select">` (reusing the existing status-filter styling) labelled **Filter by group**, rendered **only when at least one rule has a group** (so ungrouped-only setups see no extra control). Its options are derived from the current rules — distinct, trimmed group names sorted **locale-insensitive alphabetical** (`localeCompare`, `sensitivity: "base"`) — plus **All Groups** (no filter) and, when any rule is ungrouped, **Ungrouped**. The filter is applied in the same `filteredRules` pass as the existing search and status filters, so all three **combine cleanly** (AND). A stale selection (e.g. the last rule in a group is deleted) is **clamped** back to All Groups via a derived `activeGroupFilter`, so the control never shows a value with no matching option and never silently hides every rule. Drag-reorder is disabled while a group filter is active (consistent with the existing search/status behaviour — reordering a filtered subset is ambiguous). The existing empty states are preserved: "No forwarding rules yet…" when there are no rules, and "No rules match the current filter." when a filter (now including group) excludes everything. The Slice 2 group chip and group-aware search are unchanged. **Accessibility:** the control is a native labelled `<select>` (`aria-label="Filter by group"`), keyboard-accessible by default; no new roles or screen-reader noise (no visual grouping headers were added). **Tests:** 8 new `ForwardRuleList` unit tests (control hidden when no groups; lists distinct sorted groups + All Groups + Ungrouped; filters to a group; filters to ungrouped; clears back to all; combines with search; no-match empty state; Ungrouped omitted when all rules are grouped) plus a `tests/e2e/portier.spec.ts` E2E (create web/api/ungrouped rules → filter to web → to Ungrouped → back to All Groups). One Slice 2 unit assertion was tightened to scope the group **chip** lookup to the table (the group name now also appears as a filter `<option>`). All existing client/E2E tests pass; client coverage gates 94/89/78 hold (none lowered). Validated: `npm run test -w client`, `npm run validate:coverage:client`, `npm run validate:contract` (204/204), `npm run lint`, `npm run typecheck`, `npm run test:e2e`. Docs: changelog + checklist (manual QA note); no glossary change (no new terminology); durable UI rule in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.8.

- **v1.8 Slice 2 — Rule group UI editing and display** — Surfaced the optional `group` metadata from Slice 1 in the web UI so operators can set, edit, clear, and see a rule's group — **no new group operations** (no filtering UI beyond reusing the existing search box, no grouping/start-stop, no profiles/tags/colors). **Client-only change — no shared/server/service/CLI code touched; no API shape change (uses the Slice 1 `group` field); `validate:contract` stays 204/204.** **Rule form** (`client/sources/features/forwards/`): added an optional **Group** input to the add/edit drawer (`RuleForm.ts` form state + `ForwardRuleForm.tsx`), placed under Name, with a proper `<label>` + explicit `aria-label="Group"` (so the always-present hint span does not pollute the accessible name, matching the existing Listen Host pattern), a `maxLength` of `PORTIER_GROUP_MAX_LENGTH` (64), and a hint describing it as optional. `formToPayload` always sends `group` (raw string) so the Slice 1 contract handles every case: a non-empty value sets the group, and an empty string **clears** it on edit (and normalizes to absent on create) — clearing the field on an edit therefore removes the group. The existing client-side `validateForwardRule` already rejects an over-long or control-character group on submit (shown in the form's error banner); the server stays authoritative for trimming/normalization. **Rule list** (`ForwardRuleList.tsx`): a grouped rule now shows a subtle `.rule-group-label` chip beneath its name (kept quiet so it does not compete with status/protocol/endpoints; ungrouped rules render no placeholder), and the existing rule search box now also matches on `group`. **Existing behavior unchanged:** rules created/edited without a group work and render exactly as before; runtime forwarding, duplicate-binding, and status behavior are untouched. **Tests:** added client unit tests for the form (renders the Group field with the 64-char `maxLength`; submits the group on create; pre-fills on edit; sends `group: ""` to clear on edit; rejects a control-character group on submit) and the list (shows the group chip when present, renders none when absent, matches by group in search), plus an E2E test in `tests/e2e/portier.spec.ts` (create a rule with a group → chip visible + API shows the group → edit to clear → chip gone + API shows no group). All existing client/E2E tests pass unchanged; client coverage gates 94/89/78 hold (none lowered). Validated: `npm run test -w client`, `npm run validate:coverage:client`, `npm run validate:contract` (204/204), `npm run lint`, `npm run typecheck`, and `npm run test:e2e`. Docs: changelog + checklist (manual QA note); no glossary change (Slice 1 already canonicalized "group"); durable UI rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.8.

- **v1.8 Slice 1 — Rule group metadata contract** — Added an optional, **behavior-neutral** `group` label to forward rules as the data foundation for v1.8's operator tools (rule groups/profiles). **No forwarding, lifecycle, duplicate-binding, or status behavior changed — `group` is metadata only until group operations land in a later slice.** Backward compatible end to end: existing rules/configs without `group` stay valid, existing API clients keep working, and an ungrouped rule omits the field entirely (no drift vs legacy). **Contract & types:** `group?: string` on the shared `ForwardRule` and `ConfigPlanRuleSnapshot` (`@portier/shared`), `Group *string` (`json:"group,omitempty"`) on the Go `domain.ForwardRule`/`ForwardRuleResponse`/`configplan.RuleSnapshot`, and `group` on the CLI `ConfigRule`/`ConfigPlanRuleSnapshot`/`ForwardRuleResponse` DTOs — all optional, all `omitempty`. **Validation (identical in both runtimes, parity-tested):** optional; whitespace is trimmed; an empty/whitespace value normalizes to *absent* (no group); a present value must be ≤ 64 characters with no control characters (else rejected — `group must be 64 characters or fewer.` / `group must not contain control characters.`); a non-string is rejected (`group must be a string.`). On `PATCH`, sending `group: ""` clears the group, a non-empty string sets it, and omitting the field (or `null`) leaves it unchanged. **Roundtrip:** `group` is preserved through config export/import and plan/apply; in a plan it is a **material** field (a group-only change shows as an `update`) but **not** a forwarding field (so the change is **non-destructive** — no socket restart). **CLI:** the config commands forward `group` to the API and locally pre-validate length/control-chars; no new group commands or filtering yet (deferred). **UI:** no group editing in this slice (API/config contract only; deferred to a later slice). **Validation:** `validate:contract` **185 → 204** (new self-contained `rule-group` scenario group — create/get/patch/clear/invalid/legacy, all rules cleaned up — plus a `config import → export` group-preservation roundtrip and a cross-runtime `plan:GROUP_CHANGE` parity capture; CLI DTO parity guard green). New unit tests across all layers: shared validation (group accept/trim/normalize/reject + `validateGroup`), TS `config-plan` (group drift/non-destructive) and `ForwardManager` (create/patch/clear/export-import), Go `validation` (accept/normalize/reject + patch set/clear/unchanged) and `configplan` (group diff + apply preserve), and CLI (validate accept/reject + plan request forwards `group`). No coverage gates changed or lowered. Docs updated: `docs/api-contract.md` (optional group field + validation rules), `docs/glossary.md` (canonical "group" term), `docs/checklist.md` (group roundtrip release check), and the durable rule in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.8.

---

## [1.7.0] — 2026-06-11 — Cleanup & Maintainability

### Goal

A focused cleanup and maintainability release following the v1.6 architecture/quality audit. Nine slices reduce duplication and clarify structure across every component — the contract runner, the Go service, the TypeScript server, the CLI, and the web client — without adding features or changing public behavior. Highlights: an outer child-cleanup guard for the contract validation runner; responsibility-based CLI command-file names; a small Go `forwarders.Forwarder` interface + `NewForwarder` factory that removes per-protocol branching from the manager; a co-located UDP activity-event emit facade in the TypeScript server; the diagnose flow split into mirrored, ordered, named check-phase helpers in both runtimes (with new check-ordering regression tests); shared CLI config load/validate/mapping helpers; a reviewed and documented CLI exit-code policy; the 736-line `SettingsView` decomposed into focused panels and hooks (DOM byte-identical); and a glossary-aligned UI wording fix ("Connections" → "Live Connections" in the nav). The single intentional behavior change is in the CLI: `config import` now exits `2` (not `1`) for local input/read/parse/validation errors, matching `config apply`/`plan`/`diff` and the documented policy. `validate:contract` stays **185/185**, all five coverage gates hold (none lowered), and the public API/DTO/config contract is unchanged.

### Changed

- **v1.7 Slice 9 — UI wording consistency pass (Naming-E)** — Reviewed the client UI copy (nav labels, view/section titles, field labels, buttons, help text, status badges, and connection/diagnose/config terminology) against `docs/glossary.md` and fixed the **one clear, glossary-backed inconsistency**: the sidebar navigation label for the Live Connections view was **"Connections"** while the view's own title, the glossary canonical term, and this slice's own example list all say **"Live Connections"** (glossary §"Live connection" / Naming-rules: *use "live connection" for TCP*). Changed `client/sources/app/NavItem.ts` nav label `"Connections"` → `"Live Connections"` (one line) so the nav entry point and the destination title match and the term is unambiguous (vs generic "connections"). **No behavior, layout, route, API, DTO, or component-structure change** — only the visible nav text (and its natural width). **E2E follow-up (required by the label change):** the `goToConnections` helper in `tests/e2e/connections.spec.ts` navigated by clicking the `{ name: "Connections" }` nav button and then waited on `getByText("Live Connections")`; with the nav now also reading "Live Connections" that text matched two nodes (nav button + view title), so the helper was updated to click `{ name: "Live Connections" }` and confirm arrival via the unique `getByRole("tablist", { name: "Connection views" })` instead — same navigation, no brittle title-text dependency. **The audit found the rest of the UI already consistent and glossary-aligned — no other changes made (conservative pass):** field labels use "Listen Host/Port" and "Target Host/Port"; status badges and dashboard cards use "Running"/"Stopped"/"Error" (observed state) distinct from "Running" counts and the "Autostart" label for the `enabled` toggle (used consistently in the rule form and import preview); the Live Connections view uses "TCP Connections"/"UDP Sessions" and "Active"/"Idle" (TCP=connection, UDP=session per glossary); the per-rule **"Diagnose"** action (verb) is kept distinct from the **"Diagnostics Export"** support bundle; config copy uses "Export Config"/"Import Config"/"Plan & Apply Config"; no "Configuration" vs "Config" or standalone-"forward"-as-noun drift was found. The nav-label/page-heading difference for **"API Docs" (nav + header) vs "API Reference" (page title)** was reviewed and intentionally left — it has no glossary anchor, both terms are clear, and a short nav label differing from a descriptive page heading is an accepted pattern; the glossary-mandated "Live Connections" case is the one that warranted alignment. **No glossary change needed** (it already canonicalizes "Live Connections"). **No API/DTO/config/server/service/CLI behavior change; `validate:contract` 185/185; client coverage 95.3/90.3/81.3, gates 94/89/78 unchanged.** Validated: `npm run test -w client` (366), `npm run validate:coverage:client` (PASS), `npm run validate:contract` (185/185), `npm run lint`, `npm run typecheck`, and the **full E2E suite** (34/34, run because the nav lives in shared chrome on every screen) — all green. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.7 (UI wording consistency pass), `docs/glossary.md`.

- **v1.7 Slice 8 — SettingsView decomposition (Readability-B)** — Decomposed the 736-line monolithic `client/sources/features/settings/SettingsView.tsx` (8 sections, ~25 `useState`/`useRef` declarations, and 9 handlers in one component) into focused per-panel components and hooks, leaving `SettingsView` a ~90-line thin orchestrator. **Internal client refactor only — no UI redesign, no visible label/wording/default/validation/save-apply-reset/loading-error-state change, no accessibility (label/role/aria/form-association/keyboard) change, no new settings, no new API calls, no CSS change; the rendered DOM is byte-identical.** New files (all under `client/sources/features/settings/`): a presentational `SettingsSection` wrapper (the shared `.settings-section` / `.settings-section-title` markup every panel repeated inline); three hooks — `useRuntimeInfo` (the mount fetch + loading/unavailable state), `useClipboardCopy` (`copyToClipboard`/`copyLabel` with transient per-key feedback), and `useConfigExport` (the config-download flow); and five panel components — `ExportConfigSection`, `PlanApplySection`, `ImportConfigSection`, `RuntimeEnvironmentSection`, `DiagnosticsExportSection`. Each stateful panel **owns its own state** (no prop-drilling explosion): `PlanApplySection` and `ImportConfigSection` take `onRulesUpdated`, `DiagnosticsExportSection` takes `diagnosisMap`, `RuntimeEnvironmentSection` takes nothing (uses the two hooks). The one genuine cross-panel concern — the config-export flow is shared by the Export panel **and** the Import panel's "Export current config as backup" button (they share the in-progress `exporting` flag) — is created once via `useConfigExport()` in `SettingsView` and passed to both, preserving the previous single-state behaviour exactly. The three trivial static panels (Management Endpoint, Recommended Forward Port Range, About Portier) stay inline in `SettingsView`, now wrapped in `SettingsSection`. Panel render order is unchanged (Management → Port range → Export → Plan & Apply → Import → Runtime → Diagnostics → About). Every JSX block, class name, `aria-label`, `role`, `id`/`htmlFor` association, and `onClick` expression was moved **verbatim**. **No test changes were required** — the existing `SettingsView.test.tsx` (63 tests) renders the composed top-level `SettingsView` and asserts via text/role/label, so it covers the new components transitively and all 63 pass unmodified; the new components/hooks are exercised through it (client coverage 95.3/90.3/81.3, **gates 94/89/78 unchanged, none lowered**). Validated: `npm run test -w client` (366), `npm run validate:coverage:client` (PASS), `npm run validate:contract` (185/185), `npm run lint`, `npm run typecheck` (all workspaces), and the **settings E2E spec** (`tests/e2e/settings.spec.ts`, 6/6 — run because SettingsView is E2E-covered) — all green. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.7 (SettingsView decomposition).

- **v1.7 Slice 7 — CLI exit-code normalization review (Consistency-A)** — Audited every Go CLI command's exit-code behavior against the documented policy (`0` success, `1` API/runtime/operation error, `2` invalid arguments / invalid local *input* config, `3` connection failure, `4` drift with `--fail-on-drift`) and fixed the **one clear inconsistency**: `config import` returned **`1`** for local file read/parse/validation failures, while its sibling config-consuming commands (`config apply`/`plan`/`diff`) returned **`2`** for the identical category — and the policy maps a bad local *input* file to `2`. Import's `1` was undocumented (absent from `importHelp` and the readme) and its tests only asserted *non-zero*, confirming it was an incidental pre-policy artifact rather than a deliberate contract. **One-line behavior fix** in `tools/cli/sources/commands/config.go`: `RunConfigImport` now calls `loadConfigForCommand(…, "import", 2, …)` (was `1`), so an unreadable/missing/malformed/invalid local config aborts import with `2` (no API call, same stderr wording). **No other command changed** — the audit confirmed the rest are consistent or intentionally different (now documented, not "fixed"): **`config validate`** keeps `1` for an invalid/unreadable file because its exit code *is* its result (validator semantics: `0` valid / `1` invalid / `2` missing path arg); **local output-write** failures (`config export`, `config apply --backup-out`, `diagnostics export`) stay `1` (an I/O/operation failure, distinct from bad input); **rule selectors** keep no-match → `1` (target does not exist, like an API 404) vs ambiguous-name → `2` (fixable selector, IDs listed); **API errors** → `1` and **unreachable service** → `3` everywhere (the central `exitWithError`, mirrored inline in `runtime.go`). **Tests:** tightened the three import local-error tests (`InvalidLocalFile`/`FileNotFound`/`MalformedJSONFile`) from `!= 0` to **`== 2`** to lock the normalized behavior (no new files; black-box command tests); the usage-error tests already pinned `2`. Added an `Exit codes` section to `importHelp` (the `--help` test checks only the usage substring, so unaffected). **Docs:** documented the full exit-code policy and the three intentional non-inconsistencies (local input vs output; validator semantics; rule-selector codes) in `tools/cli/readme.md` (global table note + per-command import note). **No API/DTO/config/server/client change; CLI stays a pure API client; `validate:contract` 185/185; CLI coverage 97.9% (gate 95 unchanged, none lowered).** Validated: `go -C tools/cli test ./...`, `npm run validate:cli`, `npm run validate:coverage:cli` (PASS), `npm run validate:contract` (185/185), `lint`, `typecheck` — all green. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.7 (CLI exit-code normalization review).

- **v1.7 Slice 6 — CLI config loader / mapper cleanup (Duplication-B)** — Extracted the duplicated config-command prelude and DTO-mapping logic in `tools/cli/sources/commands/config.go` into small focused package-private helpers, so the `import`/`plan`/`diff`/`apply` command functions read as intent rather than repeating the same read→parse→validate and rule-mapping boilerplate. **Internal CLI refactor only — no command, flag, default, stdout/stderr wording, JSON output shape, exit code, file read/decode behavior, API request, or DTO mapping change; the CLI stays a pure API client; `validate:contract` stays 185/185 (Arch-D CLI DTO guard green).** Added `loadConfigForCommand(filePath, verb, errExit, stderr) ([]rawConfigRule, int, bool)` which performs the read + `parseLocalConfig` + `validateLocalConfig` sequence and writes the exact same stderr output (`Error reading …`, `Error: <parse>`, `Config is invalid — <verb> aborted.` + indented errors), returning the caller's exit code on failure — the four commands previously inlined this identical block (only the verb and exit code differed: import → 1, plan/diff/apply → 2). Added `toConfigRules([]rawConfigRule) []client.ConfigRule` (the field-for-field mapping loop that was copy-pasted in three places — import, apply, and `buildPlanRequest`), and mirror request builders `buildApplyRequest(rules, yes, dryRun)` and `buildImportRequest(rules, mode)` alongside the existing `buildPlanRequest` (now also using `toConfigRules`). `RunConfigValidate` deliberately keeps its own flow — its parse-error path is JSON-aware (emits a `configValidationResult`) and does not match the `loadConfigForCommand` shape, so forcing it through the helper would change behavior. Output formatting (`printPlanHuman`/`printDiffHuman`/`printApplyHuman` and the `opEndpoint`/`formatChangeValue`/`printPlan*` helpers) and `writePrettyJSON` were already isolated and are unchanged. Net effect: `RunConfigImport`/`RunConfigPlan`/`RunConfigDiff`/`RunConfigApply` each drop ~20–40 lines of boilerplate to a couple of helper calls. **No new tests needed** — the extraction revealed no coverage gap: all five new/changed helpers are 100% covered by the existing black-box (`commands_test`) behavior tests that exercise the public commands (verified via `go tool cover -func`), and no test references the internals. CLI coverage 97.7% → 97.9% (the dedup removed duplicated covered statements; **gate 95% unchanged, not lowered**); functions 98.7%. Validated: `go -C tools/cli test ./...` (all pass), `npm run validate:cli` (test + build), `npm run validate:coverage:cli` (PASS 97.9%), `npm run validate:contract` (185/185), `lint`, `typecheck` — all green. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.7 (CLI config loader / mapper cleanup).

- **v1.7 Slice 5 — Diagnose check-phase helper split (Readability-A)** — Split the long sequential `diagnoseRule` flow in **both runtimes** into small, named, intention-revealing per-check phase helpers, so the diagnostic sequence reads as an explicit ordered list of phases instead of a ~190-line append-after-append block. **Internal refactor only — no diagnostic check IDs, labels, severities, ordering, messages, details, summary behavior, HTTP status, CLI output, or response-shape change; output is byte-identical; `validate:contract` stays 185/185 (both runtimes, diagnose parity preserved against freshly built binaries).** Both `server/sources/diagnose.ts` and `service/sources/api/diagnose.go` now have `diagnoseRule` reduced to the phase list (`checkListenHost` → `checkLanExposure` → `checkPrivilegedPort` → `checkCommonPort` → `checkListenBind` → `checkTargetHost` → `checkTargetConnect` → `checkUdpMode` for UDP), each helper returning exactly one `DiagnosticCheck` built verbatim from the previous inline literal. The `target-connect` gate (`targetResolved`) is now derived from the `target-host` check's pass status rather than a separate mutable flag — behaviorally identical (target-host is "pass" iff the host resolved). The two runtimes stay structurally mirrored (split in lockstep so they cannot drift); the I/O helpers (`tryBind`/`tryTcpBind`/`tryUdpBind`/`tryTcpConnect` and Go equivalents) and `buildSummary` are unchanged. No new checks, no removed checks, no changed check semantics, no changed advisory wording, no new "rule engine" abstraction. **Tests:** the prior diagnose tests assert checks by id and the contract asserts ID *membership* — neither guarded the exact **ordering**, a stated requirement, so added one focused ordering regression test per runtime (TS `diagnose.test.ts` "diagnoseRule check ordering" — TCP 7-check order and UDP 8-check order incl. `udp-mode` last; Go white-box `service/sources/api/diagnose_phases_test.go` — `TestDiagnoseCheckOrderingTCP`/`UDP`, `isRunning=true` to skip the bind socket). These are behavior/output tests on the public `diagnoseRule`, not private-helper tests. All existing diagnose tests pass unchanged (TS diagnose 11 → 13; Go api package green). Server coverage 98.9/94.1/100 → 98.9/94.2/100 and service functions 95.9% → 96.1% (new phase helpers fully covered; the early-return phase structure trims branches); **all gates unchanged (server 95/92/99, service 90), none lowered.** Validated: `npm run test -w server` (363), `go test ./service/...` (all pass), `npm run validate:contract` (185/185), `npm run validate:runtime:smoke` (23/23), `npm run validate:coverage:server`/`:service` (both PASS), `lint`, `typecheck` (all workspaces) — all green. `docs/api-contract.md` not touched (the diagnose contract — check IDs, order, statuses, shape — is unchanged). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.7 (diagnose check-phase helper split).

- **v1.7 Slice 4 — TypeScript UDP emit facade (Duplication-A)** — Centralized the TypeScript UDP forwarder's repeated activity-event construction behind a small co-located `UdpEventEmitter` facade so the nine emission sites no longer each re-spell the `ruleId`/`ruleName`/`protocol: "udp"` envelope (and the identical `udp.packet.error` shape that was copied four times with only the message differing). **Internal refactor only — no UDP forwarding/mode behavior, activity event type/severity/payload/timestamp, `lastError`, live-session/session-history, lifecycle, or public API/DTO/config/CLI/client change; emitted payloads are byte-identical (same fields, same insertion order, `details` omitted exactly when not provided); `validate:contract` stays 185/185.** In `server/sources/forwarders/udp-forwarder.ts` the forwarder now holds `private readonly events: UdpEventEmitter` (built once in the constructor from `rule` + `onEvent`); the facade exposes five intention-named methods — `packetError(message)` (no details), `packetForwarded`/`packetReturned`/`sessionOpened`/`sessionClosed`(message, details) — each delegating to a single private `emit(type, severity, message, details?)` that stamps the rule envelope and dispatches through the optional `onEvent`. The forwarder's stateful concerns stay where they belong: throttle timing (`lastForwardLogAt`/`lastReturnLogAt` + the 1s `UDP_LOG_INTERVAL_MS` guard), `this.status.lastError` mutation, session bookkeeping, and the registry calls are all unchanged in the forwarder body — the facade only builds and dispatches the event object. The `private readonly onEvent` field became a plain constructor parameter (it is now owned by the emitter; nothing else referenced `this.onEvent`). The facade is module-private (not exported) — it is UDP-specific (TCP has its own distinct event set) and not reused cross-file, and tests exercise it through the public forwarder rather than by importing it. Added 2 focused facade-regression tests in `server/sources/forwarders/udp-forwarder.test.ts` (33 UDP tests total, up from 31) asserting the **full** `ActivityEventInput` envelope — a `udp.packet.error` event (`severity: "error"`, `ruleId`/`ruleName`/`protocol`, message, and `details` undefined) and a `udp.packet.forwarded` event (`severity: "info"`, full `details` with `fromAddress`/`fromPort`/`targetHost`/`targetPort`/`bytes`) — which the prior type+message-only assertions would not have caught a dropped/renamed field on. All 361 server tests pass; server coverage 98.9/94.0/100 → 98.9/94.1/100 (branch nudged up as the four identical error envelopes collapse to one; **gates 95/92/99 unchanged, none lowered**). Validated: `npm run test -w server` (361), `npm run validate:contract` (185/185, rebuilt TS server), `npm run validate:runtime:smoke` (23/23), `npm run validate:coverage:server` (PASS), `lint`, `typecheck` (all workspaces) — all green. Go service untouched (no cross-runtime parity obligation for this internal TS structure). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.7 (TypeScript UDP emit facade).

- **v1.7 Slice 3 — Go `Forwarder` interface + `StartRule` dedupe (Architecture-A)** — Routed the Go service's TCP and UDP rule startup through a small shared forwarder abstraction so the manager no longer carries duplicated, protocol-specific runtime-construction detail. **Internal Go refactor only — no runtime behavior, public API/DTO, config schema, CLI, activity-event, live-connection/session, rule-status/`lastError`, or duplicate-binding change; `validate:contract` stays exactly 185/185** (both runtimes; Go parity green against the freshly built binary). Added a 3-method `Forwarder` interface (`Start() error`, `Stop()`, `Status() domain.ForwardStatus`) and a `NewForwarder(rule, log, onEvent, tcpReg, udpReg) Forwarder` factory in the new `service/sources/forwarders/forwarder.go`; both `*TCPForwarder` and `*UDPForwarder` **already** satisfied the interface, so `tcp.go`/`udp.go` were not touched (the abstraction adds a shared shape, not new behavior). In `service/sources/manager/manager.go` the `runtimeState` struct's two concrete pointers (`tcp *forwarders.TCPForwarder`, `udp *forwarders.UDPForwarder`) collapse into a single `forwarder forwarders.Forwarder`, and the two near-identical ~18-line protocol blocks in `StartRule` collapse into one start/error/emit path built via the factory (the unknown-protocol case returns the same no-op synthetic status as before). `stopRuntime` and `statusForRule`'s running branch likewise simplify from `tcp != nil`/`udp != nil` pairs to a single `forwarder != nil` check — behaviorally identical because exactly one pointer was ever set per rule. The not-running synthetic status in `statusForRule` keeps its per-protocol shape (TCP `ActiveConnections`; UDP `PacketsIn`/`PacketsOut`/`ActiveUdpSessions`) since that is API-response shape, not lifecycle detail. Shutdown semantics are unchanged: both forwarders' `Stop()` remain `sync.Once`-guarded/bounded, `StopAll` is unchanged, and startup-failure cleanup (clear pointer, record `lastError`, emit `rule.error`) is preserved. Added 3 focused, socket-free factory tests (`service/sources/forwarders/forwarder_test.go`) asserting the new behavior boundary — TCP rule → forwarder with TCP status shape, UDP rule → UDP status shape, unrecognized protocol → `nil` — observing dispatch via status shape rather than concrete types. All existing manager/forwarder tests pass unchanged; `NewForwarder` is 100% covered; service coverage 90.3% → 90.1% (collapsing duplicated covered statements; **gate 90% still PASS**, no gate lowered). Validated: `go test ./service/...`, `npm run validate:runtime:smoke` (23/23 incl. smoke), `npm run validate:contract` (185/185), `lint`, `typecheck` — all green. `-race` not run (no cgo/gcc toolchain on this Windows host); the change does not alter any goroutine/socket lifecycle, so race surface is unchanged. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `docs/roadmap.md` v1.7 item (Go Forwarder interface + StartRule dedupe).

- **v1.7 Slice 2 — CLI command-file naming cleanup (Naming-C)** — Renamed the Go CLI command files so each file name matches its actual responsibility, resolving the readability-audit finding that `tools/cli/sources/commands/config.go` was misleading — it contained **only** management-API URL/connection-target resolution (`ResolveURL`, `validateURL`, `DefaultHost`/`DefaultPort`/`DefaultURL`), not config plan/diff/apply/export/import behaviour (those live in the 1030-line `configcmd.go`). **File renames only — no Go code, package name (`commands`/`commands_test`), symbol, command, flag, output, exit-code, DTO, or API behaviour change; the CLI stays a pure API client.** All renames done with `git mv` (no content edits): `config.go` → `url.go` and `config_test.go` → `url_test.go` (URL/connection resolution); then, with the natural name freed, `configcmd.go` → `config.go`, `configcmd_test.go` → `config_test.go`, `configapplycmd_test.go` → `config_apply_test.go`, `configplancmd_test.go` → `config_plan_test.go`, `diagnosticscmd.go` → `diagnostics.go`, `diagnosticscmd_test.go` → `diagnostics_test.go`. The `cmd` suffix on `configcmd.go`/`diagnosticscmd.go` only ever existed as collision-avoidance (the natural `config.go` name was taken by the URL resolver; `diagnosticscmd` disambiguated from the separate `diagnose` command); dropping it leaves every command file a bare noun matching its command/responsibility (`config.go`, `diagnose.go`, `diagnostics.go`, `url.go`, `runtime.go`, `list.go`, `status.go`, `start.go`, `stop.go`, `activity.go`, `root.go`, `resolver.go`). Updated the two **current-state** references to the old `configcmd.go` filename (`docs/coverage-baseline.md` documented-uncovered-branch table and the `scripts/validate-coverage.js` header comment, both for `writePrettyJSON`); historical changelog/checklist/roadmap slice records are left as accurate point-in-time narrative. No `npm run check`, scenario, or coverage-gate impact. Validated: `go -C tools/cli test ./...` (all packages pass), `npm run validate:cli` (test + build), `npm run validate:contract` (**185/185**, Arch-D CLI DTO parity guard green — confirms package/symbols intact), `npm run validate:scripts` (48 passed), `lint`, `typecheck` — all green. The two pre-existing non-gofmt'd files (`helpers_internal_test.go`, `json_err_test.go`, from Coverage Slice C) were intentionally **not** reformatted — they are outside this slice's touched files and reformatting them would be out-of-scope churn. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-readability-naming-audit-1.md` (Naming-C), `docs/roadmap.md` v1.7 item 4.

- **v1.7 Slice 1 — `validate-contract.js` outer child-cleanup guard (Resilience-F)** — Hardened the contract validation runner so spawned runtime children are always terminated, including the failure paths the per-runtime `try/finally` does not cover (a thrown/unexpected error reaching `main().catch`, an early `process.exit`, a startup that never becomes ready, or an interrupt signal). This is the deferred outer-guard item that Fix Slice 4 explicitly left out of scope. **Script/runtime-hygiene only — no product code, API, contract behavior, scenario, or coverage-gate change; `validate:contract` stays exactly 185/185** (run with both runtimes; Go parity green). Added a module-level `activeChildren` set that every spawned server is registered into in `startServer` (and removed from on its `exit` event and in `cleanup`), plus a synchronous `killAllChildren()` belt-and-suspenders net wired to three places: `process.on("exit")` (drains any still-tracked child on normal or early exit), `SIGINT`/`SIGTERM` handlers (kill then exit 130 so an interrupted run does not orphan a child holding its listen port), and the outer `main().catch` (kill before exiting 1 on an unexpected throw). The primary, ordered per-runtime `cleanup(ts)`/`cleanup(go)` `try/finally` path is unchanged — the set merely makes those cleanups idempotent and adds coverage for the paths that bypass them. The file's intentional embedded `\x00` sort-key separators in the `normalize*` helpers are preserved. Validated with `validate:contract` (185/185), `validate:scripts` (48 passed), `lint`, and `typecheck` — all green. `npm run test` (Vitest + Go unit) does not exercise this build script, so it was not relied on for this slice. Source: `audits/v1.6-resilience-audit-1.md` (Resilience-F), deferred at Fix Slice 4.

---

## [1.6.0] — 2026-06-11 — Architecture, Quality & Maintainability Audit

### Goal

Architecture, quality, and maintainability hardening: a coverage ratchet across all five components followed by a ten-audit review and eight remediation fix slices. No new product features and no public API shape changes — the work raises test coverage on previously untested edge cases, fixes real runtime-parity and durability bugs found by the audits (duplicate-binding import parity, atomic config writes, persist-failure rollback), removes duplication, hardens contract validation, and adds a canonical terminology glossary. All five coverage gates were ratcheted to newly achieved stable values; none were lowered.

### Added

- **v1.6 Fix Slice 8 — Portier terminology glossary (Naming-A)** — Added a short, durable canonical-terminology document (`docs/glossary.md`) addressing the recurring terminology overloads the readability/naming audit flagged: "config" (rule persistence vs plan/apply engine vs export/import DTO vs desired-config input), "forward" vs "rule", "runtime"/"server"/"service"/`node`/`go`, "diagnose" vs "diagnostics export", "advisory" vs "warning", and TCP "live connection" vs UDP "session". The glossary has five sections (Core concepts, Runtime terminology, Rule lifecycle and state, Activity and diagnostics, Naming rules/exceptions) and explicitly documents the **frozen public-API exceptions** that must NOT be renamed (`/api/forwards`, `listenHost`/`targetHost`, `clientAddress`/`targetAddress`, runtime values `node`/`go`, CLI command names). **Docs-only — no product code, API, test, or coverage-gate change; no renames.** Added one-line pointers to the glossary from `CLAUDE.md` (Naming Conventions), `AGENTS.md` (new Terminology section), and `docs/api-contract.md` (intro); the glossary text lives in one place and is not duplicated. CLI command-file naming cleanup (Naming-C) and UI wording alignment (Naming-E) remain deferred follow-ups. Source: `audits/v1.6-readability-naming-audit-1.md`.

- **v1.6 Fix Slice 7 — Config apply `importConfig` result.errors invariant (Error-C·2 / Resilience-C)** — Hardened config apply so it can never report `ok:true` when the underlying import reports errors. Both apply handlers previously **discarded the `ImportResult`** (TS `api.ts` ignored the return value; Go `api.go` captured only `err` and discarded the result), so a future `importConfig`/`ImportConfig` that returned errors *without throwing* could surface a silent `ok:true`. **Audit of every `result.errors` path:** in **both runtimes** the replace-mode import-error paths are validation errors and duplicate listen bindings (both return errors-without-throw) plus persist failure (throws → 500); apply only ever calls import in **replace** mode, and the plan engine pre-blocks the two non-throwing paths before import — invalid desired rules via plan validation and duplicate bindings via `detectDuplicateKeys` → `summary.hasErrors` → `ok:false` (merge-only errors are unreachable; persist throws → 500). So **no `result.errors` path is currently reachable from apply** — but the discarded result left the invariant unenforced. **Defensive fix (small, parity-aligned, no valid-behavior change) in `server/sources/api.ts` and `service/sources/api/api.go`:** after `importConfig`, if `result.errors` is non-empty, respond `200 ok:false` with the errors surfaced through the existing `plan.errors` field (code `IMPORT_ERROR`) + `plan.summary.hasErrors: true` + zero applied counts — mirroring the existing plan-error response shape; **no new `ConfigApplyResponse` field added**. The Go construction is extracted into a unit-testable `applyImportErrorResponse` helper (the Go `Handler` holds a concrete `*manager.Manager` with no injection seam, so the handler branch is structurally unreachable in a unit test without the deferred manager-interface refactor; the construction is covered directly, and the TS twin is covered end-to-end). **Tests:** TS `api.test.ts` adds an explicit duplicate-desired-binding apply (pre-blocked → `ok:false`/`hasErrors`, no import, nothing created) and a defensive-branch test via an `ImportErrorManager` subclass whose `importConfig` returns errors without throwing (→ `ok:false`, `plan.errors` carries `IMPORT_ERROR`, zero counts); Go `api_test.go` adds the parity duplicate-binding pre-block test + `TestApplyImportErrorResponse` (covers the helper construction). `validate:contract` adds a duplicate-desired-binding apply parity scenario (**183 → 185**, both runtimes; stable). **CLI unaffected** — `applyExitCode` already maps `ok:false` → exit 1 (same as the existing plan-error path); fail-on-drift/backup-out/dry-run unchanged. No public API shape change, no valid-apply behavior change, no event/message change. server branch 94.0 → 94.1; service 90.3 → 90.2 (the unreachable 2-line handler guard, documented in `docs/coverage-baseline.md`); **all five gates unchanged**; full test 357 → 359; Go api package green. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-error-flow-audit-1.md`, `audits/v1.6-resilience-audit-1.md`.

- **v1.6 Fix Slice 6 — TS `ForwardManager.emitRuleEvent` helper (Dup-C2 / Naming-B / Error-B; Arch-C2 symmetry)** — Closed the last Arch-C2 asymmetry: the Go manager has had a single `emitRuleEvent` helper since Arch-C2, but the TypeScript `ForwardManager` still spelled the rule-scoped activity payload inline at 6 call sites. **Behavior-preserving product refactor — every emitted `ActivityEvent` payload is byte-identical; no API shape, event type/severity, message wording, ordering, or count change.** Added a private `emitRuleEvent(type, severity, rule, message)` to `server/sources/forward-manager.ts` (mirrors the Go `manager.emitRuleEvent` shape: populates `ruleId`/`ruleName`/`protocol` from the rule, keeps the `this.activity?.add` optional-chaining so a missing store is still a no-op, no id/timestamp handling — `ActivityStore` still owns those) and replaced the 6 inline rule-scoped blocks (`rule.created`/`rule.updated`/`rule.deleted`/`rule.started`/`rule.stopped`/`rule.error`) with calls to it. The **5 config-level events** (`config.exported`, `config.imported`, and the 3 `config.import.failed` paths) are intentionally left on direct `this.activity?.add` — they are not rule-scoped and carry their own `details` map (or no rule fields), exactly as the Go runtime keeps them on `emitActivity`. Added 3 focused full-payload regression tests in `server/sources/forward-manager.test.ts` (`emitRuleEvent` Go-parity describe) asserting exact `type`/`severity`/`ruleId`/`ruleName`/`protocol`/`message` for create/update/delete (no sockets), start/stop (a real forwarder via `startRuleStable`), and the start-failure `rule.error` path (exact fields + stable message prefix) — locking the helper output beyond the prior type-only checks; generated id/timestamp are not asserted. `forward-manager.ts` stays fully covered; server 98.9/93.8/100 → 98.9/**94.0**/100 (branch nudged by the consolidation + new tests); **all five gates unchanged** (not raised — refactor, +0.2% branch within Windows fluctuation). `validate:contract` stays **183/183** (the Slice 5 activity value-parity guard confirms no type/severity drift); full `npm run test` green (server 354 → 357). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-duplication-audit-1.md`, `audits/v1.6-readability-naming-audit-1.md`, `audits/v1.6-error-flow-audit-1.md`.

- **v1.6 Fix Slice 5 — Activity event-type/severity value parity guard (Error-A / Resilience-E / Dup-G)** — Closed the silent-drift gap on the activity taxonomy: the 17 event-type strings and 4 severities are duplicated across the TS union (`shared/sources/activity.ts`), the Go consts (`service/sources/activity/activity.go`), and the `/api/activity` runtime responses, but `validate:contract` previously asserted only `ActivityEvent` *shape*. **Contract/script hardening only — no product code, API shape, or coverage gate change.** Added canonical `EXPECTED_ACTIVITY_TYPES` (17) / `EXPECTED_ACTIVITY_SEVERITIES` (4) sets to `scripts/validate-contract.js` plus three layers of guard: (1) a once-run **source-of-truth parity check** (`runActivityValueSetParityChecks`) that reads the TS union and Go consts directly from source (independent of the Go binary) and asserts both declared sets equal the canonical set — a rename/typo/drift in *either* runtime's source now fails with a precise `extra`/`missing` diff (verified by a negative test: `rule.error→rule.failure` is caught); (2) per-runtime **runtime membership** assertions in `runActivityScenarios` — every emitted `type`/`severity` must be a member of the allowed set; (3) per-runtime **representative emitted-value** assertions — the contract flow's `rule.created`/`rule.updated`/`rule.started`/`rule.stopped` (with severities `success`/`info`/`success`/`info`) are observed, and the import scenarios' `config.imported`/`success` + `config.import.failed`/`error` (the error path) are observed. No existing activity assertion weakened or removed; no event names changed (no drift found). **`validate:contract` count 171 → 183** (intentional, documented: +4 source-set checks run once, +6 membership/representative ×2 runtimes, +2 config-event ×2 runtimes; stable on two consecutive runs). The embedded `\x00` sort-key separators are preserved. `docs/api-contract.md` now lists the `ActivityEventType`/`ActivitySeverity` value sets and the parity rule. Lint/typecheck/coverage/full-test all green; all five gates unchanged. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-error-flow-audit-1.md`, `audits/v1.6-resilience-audit-1.md`, `audits/v1.6-duplication-audit-1.md`.

- **v1.6 Fix Slice 4 — `validate-contract.js` scenario registry (Complexity-A / SOLID-A / Pattern-A / Dup-A / Naming-D)** — Decomposed the ~1090-line monolithic `runScenarios` in `scripts/validate-contract.js` into **12 named, focused scenario-group functions** driven by a small ordered registry. **Script-only refactor — no product code, API, contract behavior, or coverage gate change; `validate:contract` stays exactly 171/171** (run twice). Groups (registry order preserved verbatim from the monolith): `runRuntimeScenarios`, `runForwardsScenarios`, `runForwardLifecycleScenarios`, `runActivityScenarios`, `runConfigExportImportScenarios`, `runPortAdvisoryScenarios`, `runForwardDeleteScenarios`, `runConnectionsScenarios`, `runConfigPlanScenarios`, `runConfigApplyScenarios`, `runErrorEnvelopeScenarios`, `runDiagnoseScenarios`. Each takes a small `ctx` (`{ api, runtime, fixtureDir, state }`) destructuring only what it uses; cross-group rule ids (`tcpId`/`udpId`/`udpDefaultId`) now flow through `ctx.state` while group-local values (`exportedConfig`) stay local. `runScenarios` is now a slim wrapper that builds `api`/`ctx` and iterates `scenarioGroups` in order, then runs the CLI DTO fixture capture (Arch-D) last — unchanged. The readability audit's inline runtime-label→API-value special-case (`runtime === "TypeScript" && data.runtime === "node"` …) was extracted into a named `expectedRuntimeApiValue(runtimeLabel)` helper (Naming-D). Assertions were moved **verbatim** (the refactor was performed as a byte-level transform that preserves the file's intentional `\x00` sort-key separators in `normalizeAdvisories`/`normalizeWarnings`/`normalizeErrors`/`normalizeOperations`); no assertion weakened, no scenario removed, request order preserved, parity comparison (`compareParity`) and CLI DTO live-decode guard (`runCLIContractGuard`/`captureCLIFixtures`) untouched. Child-process startup/`cleanup()` behavior was **not** changed (the optional outer-guard hardening remains deferred as Resilience-F). Lint/typecheck clean; `validate:coverage` all five gates unchanged; full `npm run test` green. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-complexity-metrics-audit-1.md`, `audits/v1.6-solid-audit-1.md`, `audits/v1.6-design-pattern-audit-1.md`, `audits/v1.6-duplication-audit-1.md`, `audits/v1.6-readability-naming-audit-1.md`.

- **v1.6 Fix Slice 3 — Finish Test-A EADDRINUSE residual helper migration (test determinism)** — Closed the remaining allocate-close-rebind residuals flagged by `audits/v1.6-testing-audit-1.md` / Resilience-D / Error-E in the two named TS suites. **Tests / test-infrastructure only — no product code, API, contract, or gate change.** New helpers in `server/sources/test-helpers.ts`: `startRuleStable(manager, ruleId, freePort)` (mirrors the Go `service/sources/manager/portretry_test.go` helper — retries `manager.startRule` **only** on EADDRINUSE, **only** the bind, by rebinding the rule's `listenPort` via `updateRule`, bounded to 10 attempts; any other error rethrows immediately), and `startTcpServerOnFreePort()` / `bindUdpSocketOnFreePort()` (bind to port 0 and return the **live** listener/socket plus its actual bound port — no close-then-rebind TOCTOU window). Migrated `server/sources/forwarders/udp-forwarder.test.ts`: every forwarder listen bind now goes through a local `startUdpForwarder` wrapper over `startForwarderOnFreePort` (bounded EADDRINUSE retry, returns the bound port), every UDP target/echo socket uses `bindUdpSocketOnFreePort`, and several fixed-sleep session-count waits became `waitUntil` deadline polls. Migrated `server/sources/forward-manager.test.ts`: every manager `startRule` on an ephemeral port (the `startedRule` helper, the reorder test, the ActivityStore rule.started test, and the Test-D rollback tests for update-running and delete-running) now uses `startRuleStable`; TCP target servers use `startTcpServerOnFreePort`; the local allocate-then-bind `startTcpTarget` was removed. **Intentional-conflict tests deliberately bypass the helpers and still force a real EADDRINUSE:** the UDP "start() rejects when port is already bound" (live blocker socket), the manager "logs rule.error and re-throws when startRule fails" (live occupier), and the merge-import persist-failure rollback test (already race-tolerant — asserts the started forwarder is *not* running after rollback). No product-side bind retry was added — only test setup retries, only on EADDRINUSE, bounded, with the last error in the final failure message. The 58 migrated tests pass 3× consecutively; server stays 354 tests at 98.9/93.8/100 (no count or coverage change); `validate:contract` 171/171, `validate:config` 71/71, all five gates unchanged. **Remaining accepted residuals (documented, bounded):** the HTTP `/start` sites in `server/sources/api.test.ts` and `service/sources/api/api_test.go` (the bind happens behind the HTTP handler and each test asserts the start-response shape, so a robust fix needs an HTTP-level recreate-on-EADDRINUSE helper — a larger, lower-value follow-up), and the E2E `tests/e2e/helpers/port.ts` allocate-close helpers (E2E is fully serial — `workers:1`, `fullyParallel:false` — so no intra-process race is possible; only a rare cross-process race against the single dedicated server remains). Revisit trigger: an actually-observed HTTP `/start` or E2E EADDRINUSE flake. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-audit-synthesis-and-fix-plan.md`, `audits/v1.6-resilience-audit-1.md`.

- **v1.6 Fix Slice 2 — TypeScript `ConfigStore` atomic write parity with Go (durability fix)** — Closed the durability/parity divergence from `audits/v1.6-resilience-audit-1.md` (Resilience-A) and the error-flow persistence-durability risk. The TS `ConfigStore.save` was a bare `mkdir` + `writeFile`, so a crash / disk-full / interrupted write mid-`writeFile` could truncate `rules.json`; the next startup's load would then fail to parse it and the Node fallback runtime could refuse to start. The Go `config.Store.Save` was already crash-safe (temp file + `Sync` + atomic `Rename`). **Product fix in `server/sources/config-store.ts`:** `save` now mirrors Go — it writes to a **unique temp file in the same directory** as the target (`.portier-forwards-<pid>-<ts>-<rand>.tmp`), `fsync`s the contents via a `FileHandle` (`open` → `write` → `sync` → `close`), then **atomically renames** the temp file over `rules.json`. The previous file stays intact until the rename succeeds, so a failed save cannot corrupt it; the temp file is removed on any pre-rename failure (best-effort, so cleanup errors cannot mask the original error), and the original persistence error propagates to the caller so `ForwardManager`'s in-memory rollback (Test-D) still runs. On a write/sync failure the handle is closed best-effort (error ignored, parity with Go); a `close` error on the success path IS surfaced (also parity with Go). **`load` is unchanged** (still `node:fs/promises` directly, same ENOENT-→`[]` and invalid-JSON/invalid-rule behavior). Serialized output is byte-identical to before (`JSON.stringify(rules, null, 2)` + `\n`, matching Go's `MarshalIndent`). **Justified divergence from Go:** no remove-and-retry recovery branch after `rename` — Node's `fs.rename` replaces the destination atomically on the same filesystem on both POSIX (`rename(2)`) and Windows (libuv `MoveFileExW` with `MOVEFILE_REPLACE_EXISTING`), so Go's recovery branch (dead on Win/Linux) is unnecessary; a directory fsync is intentionally omitted (not portable on Windows — the same metadata-durability limit the Go runtime accepts). A minimal **test-only injection seam** (`ConfigStoreFileOps`/`ConfigFileHandle`, exported `defaultFileOps`, optional 2nd constructor arg defaulting to the real fs) was added — only `mkdir`/`open`(write/sync/close)/`rename`/`remove`, not a virtual filesystem — so failures can be forced without a real disk-full event; it is not exposed through any product/API path (`new ConfigStore(path)` is unchanged). 5 new unit tests in `server/sources/config-store.test.ts`: success writes pretty JSON + trailing newline and leaves **no temp file**; write-fails-before-rename keeps the existing `rules.json` byte-for-byte and still loads it (temp cleaned up); sync-failure propagates + cleans up; rename-failure keeps the target unchanged and removes the fully-written temp; first-save-fails creates no target file. `config-store.ts` coverage 100/100/100; server 98.9/93.6/100 → 98.9/**93.8**/100 (branch nudged up by the now-covered error paths); **all five gates unchanged** (not raised — durability slice, and the +0.2% branch is within the documented Windows ±1% fluctuation). `ForwardManager` rollback tests (Test-D) unchanged and still pass — the atomic store still throws on persist failure, so no manager change was needed. Verified with `vitest run config-store.test.ts` ×4 (filesystem-stable), `npm run test`, `validate:coverage`, `validate:config` (71/71), and `validate:contract` (171/171). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-resilience-audit-1.md`, `audits/v1.6-audit-synthesis-and-fix-plan.md`.

- **v1.6 Fix Slice 1 — TypeScript replace-import duplicate listen-binding parity (real parity bug fixed)** — Closed the one MUST-do correctness/parity divergence from `audits/v1.6-audit-synthesis-and-fix-plan.md` (Error-flow finding #1, Resilience-B). The Go service rejects duplicate listen bindings **within the imported set** for both import modes (`manager.ImportConfig` → `ensureNoDuplicateBindings(validated)`, run before the mode branch); the TypeScript `ForwardManager.importConfig` had **no equivalent guard** — replace mode had none at all, and merge mode only checked imported-vs-existing, not imported-vs-imported. So two desired rules with distinct IDs but the same `protocol + listenHost + listenPort` survived in TS (the rule map is keyed by ID), persisted, and only failed later when the second forwarder's bind was swallowed → a silent, Go-divergent invalid state. **Product fix (minimal, mirrors Go) in `server/sources/forward-manager.ts`:** a module-level `ensureNoDuplicateBindings(rules)` (mirroring the Go package-level function, including message wording — `a <proto> rule is already listening on <host>:<port> (rules "X" and "Y")`) is called in `importConfig` after validation and **before any mutation**, for both replace and merge. On a duplicate binding it returns `{ imported: 0, skipped: 0, errors: [msg] }`, emits one `config.import.failed` (severity `error`, no `details`), and performs **no** in-memory mutation, **no** persist, and **no** forwarder start/stop — matching Go. The HTTP layer already maps `result.errors` → **422** with `{ errors, result }` in both runtimes, so no API/handler change was needed. 3 new TS unit tests in `server/sources/forward-manager.test.ts` (replace rejects + no mutation/persist/success-event via `ControllableStore` + `ActivityStore`; merge rejects intra-set duplicates; distinct-binding replace still succeeds and persists). New `validate:contract` parity scenario asserts both modes × both runtimes return 422 with the `errors[]`/`result` envelope and a matching message classification (`validate:contract` 167 → **171**). **Config apply interaction (verified, no change needed):** `buildConfigPlan` already flags intra-desired duplicate bindings via `detectDuplicateKeys` → `summary.hasErrors`, and the apply handler returns `ok:false` on `hasErrors` before ever calling `importConfig`, so apply was already protected — this resolves the duplicate-binding half of the deferred Resilience-C "Unable to verify" question in the affirmative. No valid-import behavior change; merge imported-vs-existing behavior unchanged; the only new rejection is the intra-set duplicate that Go already rejected. All five coverage gates unchanged (server stays 98.9/93.6/100). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-audit-synthesis-and-fix-plan.md`.

- **v1.6 Testing Slice Test-E — Client/E2E meaningful coverage (32 → 34 E2E tests; brittle selectors cleaned)** — Addressed the Testing-Audit-1 E2E findings (thin/empty-state-only Live Connections coverage, no offline state, brittle selectors). **Tests only — no product code changed**; `validate:contract` stays 167/167 and client coverage is unchanged (95.2/90.2/80.3). New `tests/e2e/connections.spec.ts` workflows: **(I) populated TCP table** — creates+starts a real TCP rule forwarding to an in-process echo server, holds a forwarded client socket open across the browser fetch, and asserts a populated `role="table"` row (rule name "Live TCP Inspect", `127.0.0.1` endpoints, "Active" status), the empty state gone, and the TCP tab count non-zero; **(J) API-failure error banner** — Playwright `page.route` aborts `**/api/connections*` at the browser layer (the rest of the app still loads from the real server) and asserts the view's `role="alert"` banner appears with a non-empty message and the app stays usable (tabs still render). **Brittle selector cleanup:** removed `label.auto-refresh-toggle` (now reads state via the `input[aria-label="Auto-refresh"]` accessibility attribute and clicks the visible "Auto-refresh" label) and removed `.diag-panel-title` / `.diag-panel-body` from `portier.spec.ts` (now uses the accessible "Close diagnostics" button + the user-visible "Listen address" diagnostic check). Also fixed the `goToConnections` helper's fragile `Parameters<…>` type to a plain `Page`. Full E2E suite 34/34, run twice for stability (the populated-table test uses a real held socket + auto-refresh polling — no fixed sleeps, Playwright auto-waiting only). The populated-table test uses the **real runtime**; the offline test uses route interception (documented as the one deliberate exception in `docs/e2e-coverage.md`). No product bug found — the view already had a `role="alert"` error state and accessible hooks. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: E2E must cover critical user-visible workflows (not only empty states), prefer role/label selectors over CSS classes, and keep at least one Live Connections populated-state + one API-failure regression test. Source: `audits/v1.6-testing-audit-1.md`.

- **v1.6 Coverage Slice E — TypeScript server high-value error paths (95.4/92.2/100 → 98.9/93.6/100, gates 89/91/99 → 95/92/99)** — Raised TS server coverage with meaningful runtime error-path tests. **Tests only — no product code changed**; `validate:contract` stays 167/167. `server/sources/diagnose.test.ts` (85.9% → 98.0%): TCP listen-bind failure (occupied port → fail), target-connect success (local server) / connection-refused (unbound port) / DNS-resolution failure (`portier-nonexistent.invalid` → target-host fail + target-connect skip + summary fail), one-way udp-mode pass, and the 0.0.0.0 listen-host/lan-exposure and privileged-port/common-port advisory **warn** branches (UDP rules used so target-connect is skipped, isolating the advisory summary). `server/sources/forwarders/udp-forwarder.test.ts` (86.3% → 99.4%): the four UDP **send-callback error** branches — one-way target send, multi-client session send (on session reuse), last-client return send, multi-client return send — each emits `udp.packet.error` and records `lastError`. These are driven by a clean instance-level `.send` override on the specific socket (the same injection style as the existing `socket.emit("error")` tests — not `dgram` prototype monkeypatching), with a `waitUntil` deadline poll (no fixed sleeps). `server/sources/api.test.ts`: apply-with-drift persist failure → 500 via an inline failing `RuleStore` (parity with Go Coverage Slice D; covers the `configApply` catch → error-handler 500 mapping). Server coverage 95.4/92.2/100 → 98.9/93.6/100 (three consecutive `coverage:server` runs stable — the Slice-A ghost-entry dedup keeps the number deterministic); gates raised statements 89 → 95 and branches 91 → 92 (functions stays 99; cautious buffers of ~3.9% and ~1.6%). Structurally-hard branches documented in `docs/coverage-baseline.md` rather than chased: the 2 s diagnose bind/connect **timeout** branches (`tryTcpBind`/`tryUdpBind`/`tryTcpConnect`), the UDP `if (!this.listenSocket)` (target reply after stop) and `if (!this.lastClient)` (target reply before any client) race guards, the `tcp-forwarder.ts` optional-registry guard (by-design), and the api.ts platform-normalize branches (Windows-only env). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: server coverage prioritizes runtime error paths; clean instance-level socket injection is allowed, brittle monkeypatching is not. Source: `audits/v1.6-coverage-audit-1.md`, `audits/v1.6-testing-audit-1.md`.

- **v1.6 Coverage Slice D — Go service high-value error paths (88.5% → 90.3%, gate 88 → 90)** — Raised Go service coverage with meaningful error/correctness-path tests. **Tests only — no product code changed**; `validate:contract` stays 167/167. New `service/sources/api/errorpaths_test.go` (white-box `package api`) covers HTTP rejection paths previously untested: empty body and mid-stream body-read failure → 400 (`readBody`/`decodeRequest`/`updateForward`/`configApply`), malformed JSON → 400, `PATCH` of an unknown rule → 404, generic (non-typed) manager error → 500 (`writeManagerError` default + `createForward`), and config-apply `ImportConfig` failure → 500 — the persist failures are driven by a real manager built via `manager.NewWithStore` with a store path under a regular file (so `os.MkdirAll` fails cross-platform, no production seam, no mocking). Also covers `NewHandler` option defaults (nil manager / zero StartedAt / empty Version) and the `tryTCPBind`/`tryUDPBind` diagnostic helpers (success + occupied-port failure). New `service/sources/forwarders/udp_errorpaths_test.go` exercises the public `NewUDPForwarder` constructor and the `udpMode()` nil-default branch through the Test-A `startUDPForwarderOnFreePort` retry helper (no fixed sleeps). New `service/sources/static/static_test.go` covers `ServeClient` serving an existing asset vs the SPA index fallback, plus `HasClient`. New `service/sources/manager/errorpaths_test.go` covers `StartEnabled` skipping disabled rules (no socket binds) and `hasIDIn` regenerating a colliding ID on merge-import. New `service/sources/configplan/udpmode_test.go` covers `udpModeEqual`/`udpModeVal` for nil↔non-nil and non-nil↔non-nil mode changes. One `options` positional-argument rejection test (`parseCLI`). Service coverage 88.5% → 90.3% (two consecutive `coverage:service` runs both 90.3%; modified packages stable at `go test -count=3`); gate raised 88 → 90. Structurally-unreachable / seam-requiring branches are documented in `docs/coverage-baseline.md` rather than chased: crypto/rand fallbacks (`randomEventID`/`NewRuleID`/`generateConnectionID`), `config.Save` write/sync/rename injection (Go `os.Rename` replaces atomically, so the rename-recovery path is dead), and the UDP forwarder read-loop send-error branches (`emitPacketError`, multi-client/session write errors) which need a `net.Conn` injection seam not worth adding now. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: service coverage prioritizes correctness/error paths; do not add invasive production-only seams for unreachable socket internals. Source: `audits/v1.6-coverage-audit-1.md`.

- **v1.6 Coverage Slice C — CLI command edge-case coverage (93.2% → 97.7%, gate 93 → 95)** — Pushed CLI coverage past the audit's 95% target with meaningful failure-path and exit-code tests. **No CLI behavior change** — all additions are tests; the CLI remains a pure API client (no server/service imports, no new commands), and `validate:contract` stays 167/167. Added `tools/cli/sources/commands/json_err_test.go`: a shared `failingWriter` (every `Write` returns `os.ErrClosed`) drives the previously-untested `output.PrintJSON → "Error encoding JSON" → exit 1` branch in every JSON-emitting command (`runtime`, `list`, `status`, `start`, `stop`, `diagnose`, `activity`, `config export`/`validate`/`plan`/`diff`/`apply`/`import`, `diagnostics export`), asserting exit code 1 and a useful stderr message against a local `httptest` server. Added `tools/cli/sources/commands/helpers_internal_test.go` (white-box `package commands` tests): `formatChangeValue` across nil / integer-valued float / non-integer float / string / bool, and `opEndpoint` across Current-present / Desired-fallback / both-nil. Added `main_test.go` `TestRun_InvalidURL_AllDispatches` covering the repeated `ResolveURL`-error arm in `run()` (invalid `--url` → exit 2) for every subcommand. Per-function result: `run()`, `formatChangeValue`, `opEndpoint`, and most `Run*` commands reach 100%; CLI total 97.7% (cross-package, deterministic — two consecutive `coverage:cli` runs both report 97.7%). Gate raised 93 → 95 in `scripts/validate-coverage.js` (2.7% buffer retained). Structurally-unreachable branches are documented in `docs/coverage-baseline.md` rather than chased: `main()` os.Exit wrapper, `client.do` `http.NewRequest`/body-read errors, and `client.doWithBody` + `writePrettyJSON` `json.Marshal(Indent)` errors (impossible with the concrete CLI DTOs — adding fake unmarshalable types is forbidden). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: CLI coverage must include failure-path behavior and keep config command exit-code priority tested. Source: `audits/v1.6-coverage-audit-1.md`.

- **v1.6 Testing Slice Test-D — TypeScript ForwardManager persist-failure rollback parity (real bug fixed)** — Closed the HIGH runtime-parity gap from `audits/v1.6-testing-audit-1.md`. The Go manager rolls back every persist path so a failed `store.Save()` leaves no partial state (`TestCreateRulePersistFailureRollsBack`, `TestUpdateRulePersistFailureRollsBack`, `TestUpdateRulePersistFailureWithRunningRuleRestartsOriginal`, `TestDeleteRulePersistFailureRollsBack`, `TestReorderRulesPersistFailureRollsBack`); the TypeScript `ForwardManager` mutated its in-memory rule map *before* `persist()` with **no rollback**, so a persistence failure left the in-memory rules inconsistent with `rules.json` and divergent from Go. New rollback tests in `server/sources/forward-manager.test.ts` (a `ControllableStore` fake whose `save()` fails on demand and records persisted snapshots) **exposed this as a real bug** — all six initially failed. **Product fix (minimal, mirrors Go) in `server/sources/forward-manager.ts`:** `addRule` deletes the appended rule on persist failure; `updateRule` restores the original rule and restarts it if a running forwarder was stopped for a forwarding-field change; `deleteRule` restores the rule (and restarts it if it was running); `reorderRules` restores the prior order; `importConfig` (replace and merge) snapshots the rules before mutation and restores them on failure, stopping any forwarder started during a merge. Restart-on-rollback failures are swallowed so they cannot mask the original persist error (parity with Go's `_, _ = m.StartRule(...)`). 8 new tests (create / update-non-forwarding / update-running-forwarding / delete-stopped / delete-running / reorder / import-replace / import-merge) mirroring the Go scenarios plus two TS-side extensions (delete-running, import rollback — Go has the import-rollback code but no dedicated Go test). No API contract, forwarding, or plan/apply change; the rollback only triggers when a disk write fails (the API still returns its existing 5xx, now with consistent in-memory state). `forward-manager.ts` coverage 99.4%/88.7% → 99.5%/90.4% stmts/branch (100% funcs); server gate 89/91/99 unchanged (95.4%/92.2%/100% actual — headroom exists but not ratcheted mid-slice to stay clear of the Windows vitest ±1% fluctuation). Verified with `vitest run forward-manager.test.ts` ×3 (stable, incl. the running-rule restart), full `npm run test`, `validate:coverage`, and `validate:contract` (167/167). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-testing-audit-1.md`.

- **v1.6 Testing Slice Test-A — Stabilized test port allocation (EADDRINUSE/TOCTOU)** — Eliminated the allocate→close→rebind race in the socket-binding test suites that was the highest-confidence flake source in `audits/v1.6-testing-audit-1.md` (a service `go test` run failed once under concurrent load during the audit and passed on isolated re-runs). The `free*Port` helpers allocate an ephemeral port, close it, and return the number; another test (vitest runs files in parallel) or process can grab that port before the forwarder/manager binds it, surfacing as a transient EADDRINUSE. Test-only fix (no product/API/contract change), using the audit's Pattern C — bounded retry around the *bind step only*, never the whole test, and only on EADDRINUSE. New cross-platform `isAddrInUse` helper (matches POSIX `EADDRINUSE` and the Windows "only one usage of each socket address" message). **Go service forwarders** (`service/sources/forwarders/portretry_test.go`): `startTCPForwarderOnFreePort`/`startUDPForwarderOnFreePort` build + start a forwarder on a fresh ephemeral port, stopping any partially-started forwarder and retrying on a new port on EADDRINUSE; all happy-path bind sites in `tcp_test.go` and `udp_test.go` migrated (intentional bind-failure tests left untouched). **Go service manager** (`service/sources/manager/portretry_test.go`): `startRuleStable` starts a rule and, on a raced bind, rebinds via `UpdateRule` to a fresh port (protocol derived from the rule) and updates the local rule so later dials stay correct; happy-path `StartRule` sites migrated. **TypeScript server** (`server/sources/test-helpers.ts`): exported `startForwarderOnFreePort`/`isAddrInUse`; `tcp-forwarder.test.ts` fully migrated to consume them. **Contract harness** (`scripts/validate-contract.js`): `startServer` now retries the port allocation + child bind on a fresh port (bounded), and `waitForReady` bails out immediately when the child process exits (a bind failure) instead of polling a dead URL. **Fixed sleep removed**: the 20 ms ordering `time.Sleep` in `service/sources/forwarders/udp_test.go` (`TestUDPLastClientTwoClientsDocumentedLimitation`) replaced with a deadline poll on `PacketsIn >= 1`. Verified by running the full Go service suite and the TS server forwarder suites repeatedly (`go test ./... -count=3` on forwarders/manager; vitest forwarders) with no flakes. No coverage or gate change (test-only; no production code touched). **Known residuals (tracked Test-A follow-up):** `server/sources/forwarders/udp-forwarder.test.ts` (helper ready, not yet consumed), the HTTP `/start` bind sites in `service/sources/api/api_test.go` and `server/sources/api.test.ts` (binds happen behind the HTTP handler and each test asserts the start-response shape, so an HTTP-level rebind helper is a larger follow-up), and the E2E `tests/e2e/helpers/port.ts` listen-port helper (E2E runs serially with `workers:1`/`fullyParallel:false`, so intra-suite port races are impossible; cross-process races remain rare). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-testing-audit-1.md`.

- **v1.6 Architecture Slice Arch-D — CLI DTO live-runtime parity guard** — Closed the last contract-parity gap from the architecture audit: the CLI's DTOs (`tools/cli/sources/client/client.go`, a third copy of the REST contract) were verified only against hand-authored `httptest` mocks, never against real runtime output. `scripts/validate-contract.js` now captures representative **live** JSON responses from each running runtime (TypeScript server + Go service) into a temp dir — `runtime`, `forwards` (list + create), `status`, `activity`, `config export`, `config plan`, `config apply --dry-run`, `port advisory` (LAN + management), and `diagnose` — then runs `TestCLIDTOContractParity` (`tools/cli/sources/client/contract_decode_test.go`), which **strictly** decodes each fixture (`json.DisallowUnknownFields`) into the matching CLI DTO and asserts key fields, so any runtime field the CLI does not model fails the guard. The guard is part of `npm run validate:contract` (now 167 passed) and runs per-runtime; the Go test skips when `PORTIER_CLI_CONTRACT_FIXTURES` is unset, so `npm run test:cli`/`validate:cli` are unaffected and the CLI stays a pure API client (no service imports, no new runtime command). Missing `go` toolchain → guard skips (not a failure). `/api/connections` is intentionally out of scope — the CLI has no connections DTO/command (it is covered by the cross-runtime parity checks). No API shape, CLI, or runtime behavior change; no coverage/gate change (the test adds no production code and is env-gated). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`/`tools/cli/readme.md`: CLI DTOs must be guarded against live runtime responses, not only mocks; the CLI must stay a pure API client; new API response families must update both `validate:contract` capture and the CLI decode guard. Arch-D completes the audit's contract-parity remediation. Source: `audits/v1.6-architecture-audit-1.md`.

- **v1.6 Architecture Slice Arch-C2 — Collapsed repeated Go manager activity-emission blocks** — Replaced the ten repeated rule-scoped activity-emission blocks in `service/sources/manager/manager.go` (each manually building an `activity.ActivityEventInput` with `id, name, proto := rule.ID, rule.Name, string(rule.Protocol)` and `&id/&name/&proto`) with a single helper `emitRuleEvent(eventType, severity string, rule domain.ForwardRule, message string)`. Covered call sites: CreateRule success, UpdateRule restart-stop + update success, DeleteRule stop + delete, StartRule TCP/UDP failure + success, StopRule stop. Every emitted payload (type, severity, ruleId, ruleName, protocol, message) is byte-for-byte unchanged. The five config-level emissions (`config.exported`, `config.imported`, three `config.import.failed`) are **not** rule-scoped — they carry a `details` map and no rule fields — so they intentionally continue to call `emitActivity` directly and were left unchanged. Added full-payload regression tests in `service/sources/manager/manager_test.go` (`TestCreateRuleActivityPayload`, `TestUpdateRuleActivityPayload`, `TestDeleteRuleActivityPayload`, `TestStartStopRuleActivityPayload`, `TestFailedStartActivityPayload`) plus `findActivityEvent`/`assertRuleEventPayload` helpers that assert type, severity, ruleId, ruleName, protocol, message, and that rule-scoped events carry no `details` — so any accidental payload change now fails the suite. `validate:contract` remains 166/166. Service coverage 88.6% → 88.5% (refactor removed covered duplicate lines; still above the 88% gate, no gate change). Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: rule-scoped manager activity events must be emitted via `emitRuleEvent`, and activity payload changes are user-visible diagnostics requiring tests over type/severity/ruleId/ruleName/protocol/message/details. Source: `audits/v1.6-architecture-audit-1.md`.

- **v1.6 Architecture Slice Arch-C1 — Unified Go service rule ID generation + dead-code removal** — Removed the two duplicate UUID-v4 generators in the Go service (`randomID` in `service/sources/manager/manager.go` and `newApplyRuleID` in `service/sources/api/api.go`) in favor of one shared `domain.NewRuleID()` in a new `service/sources/domain/id.go`. The canonical implementation keeps the more robust of the two prior behaviors — a timestamp-based fallback (`rule-<unixnano>`) if the system CSPRNG ever fails — which the manager already had and the api path lacked; in the normal path both old helpers produced an identical UUID-v4-style string, so observable behavior is unchanged. Call sites updated: `manager.CreateRule` and `manager.ImportConfig` merge-id-collision now call `domain.NewRuleID()`; `configApply` passes `domain.NewRuleID` into `BuildApplyImportFromPlan` (the Arch-B injectable-id seam is preserved — tests still inject deterministic generators). Removed the now-unused `crypto/rand` and `encoding/hex` imports from both files. Also removed a trivial no-op flagged by the architecture audit (`name := rule.Name; if name == "" { name = "" }` in `api.go` `buildRuleLiveSummary`), now using `rule.Name` directly — no response/output change. Added `service/sources/domain/id_test.go` (3 tests: non-empty, UUID-v4 format regex, uniqueness across 100 calls). `validate:contract` remains 166/166; coverage neutral (service 88.6%); no gate change. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: Go service rule ID generation must have one shared implementation so manager-created and config-apply-created rule IDs cannot drift in format. Activity-emission dedupe remains the separate follow-up Arch-C2. Source: `audits/v1.6-architecture-audit-1.md`.

- **v1.6 Architecture Slice Arch-B — Config apply orchestration extracted into the plan engines** — Moved the apply transformation logic out of the HTTP handlers and into tested helpers beside the plan engines, preserving behavior exactly (no API shape, plan/apply semantics, forwarding, CLI, or UI change). New `buildApplyImportFromPlan(plan, newId?)` in `server/sources/config-plan.ts` and `BuildApplyImportFromPlan(plan, newID)` in `service/sources/configplan/plan.go` derive the desired-state replace rule list from a completed plan (omit `remove` ops; preserve explicit desired id; preserve the matched current id for `unchanged`/`update`; generate a fresh id for genuine adds) and compute applied counts from the plan summary. The helpers are pure: they do not mutate the plan, call the manager/import, write files, or start/stop rules. ID generation is injectable for deterministic tests and defaults to the existing per-runtime generator (`crypto.randomUUID` in TypeScript; `newApplyRuleID` passed in on the Go side — no new UUID implementation added). HTTP handlers (`server/sources/api.ts`, `service/sources/api/api.go`) retain only request/response concerns: missing-`desired` 400, plan-error `ok:false`, dry-run no-mutation, destructive-requires-`yes`, calling the manager replace-import only when the plan has drift, and response serialization. Added 10 TypeScript helper unit tests (`config-plan.test.ts`) and 8 Go helper unit tests (`plan_test.go`) covering no-drift id preservation, add id injection, explicit-id preservation, update id preservation, remove omission, deterministic mixed-order output, applied counts, plan-not-mutated, and udpMode preservation. `validate:contract` remains 166/166 against both runtimes (the apply parity guard). Server coverage 95.2%/91.6% → 95.3%/92.0% (new helper fully covered; simplified handler); service unchanged at 88.6%. No coverage gate change. Durable rule recorded in `CLAUDE.md`/`AGENTS.md`: apply orchestration must live beside the plan engine, not in HTTP handlers. Source: `audits/v1.6-architecture-audit-1.md`.

- **v1.6 Architecture Slice Arch-A — Contract drift guard** — Fixed an already-shipped runtime parity defect and hardened contract validation against advisory/plan content drift (no API shape, behavior, or plan/apply semantic change). The `LAN_EXPOSURE` port advisory message in the Go service (`service/sources/advisory/advisory.go`) was shorter than the canonical TypeScript wording in `@portier/shared` (`shared/sources/index.ts`); the Go wording is now aligned exactly to the canonical TypeScript string ("Listening on 0.0.0.0 exposes this forwarded port on all interfaces. Other LAN devices may be able to connect if firewall settings allow it."). The advisory **code** (`LAN_EXPOSURE`) and **severity** (`warning`) were already correct and are unchanged. `validate:contract` previously verified advisory codes and field shapes but not message text, so this drift was invisible to it. Strengthened `scripts/validate-contract.js`: (1) in-runtime canonical assertions for `LAN_EXPOSURE` and `MANAGEMENT_LAN_EXPOSURE` advisory code+severity+message and the `LAN_EXPOSURE` plan warning message; (2) a new cross-runtime parity pass (`compareParity`) that captures deterministic, normalized advisory and config-plan payloads from both runtimes and diffs them field-by-field, reporting the exact mismatching path. Normalizers strip non-deterministic fields (timestamps, generated ids) and sort by `code`+`field`+`message`; the plan parity case plans a single `0.0.0.0` rule against empty current config so the payload (summary, operation type/ruleName/protocol/destructive/changes, warnings, errors) is fully deterministic. Added Go advisory tests `TestLANExposureCanonicalContent` and `TestManagementLANExposureCanonicalContent` (`service/sources/advisory/advisory_test.go`) locking full code/severity/message content. `validate:contract` now 166/166 against both runtimes (was 156/156); the guard was verified to fail against a stale pre-alignment binary and pass after rebuild. No coverage gate change (service stays 88.6%; advisory was already covered). No `docs/api-contract.md` or client API Docs change (those surfaces do not contain the advisory message text). Source: `audits/v1.6-architecture-audit-1.md`.

- **Service coverage uplift** — 24 new Go tests across `api_test.go`, `manager_test.go`, and `options_test.go`. Refactored `normalizePlatform`/`normalizeArch` in Go service to delegate to `internalNormalizePlatform`/`internalNormalizeArch` helpers for platform-independent branch coverage. Tests cover: platform/arch all branches, missing error returns in API handlers (updateForward/deleteForward/stopForward/reorderForwards), manager error type `.Error()` methods, CreateRule validation error, DeleteRule not-started branch, ReorderRules duplicate IDs, options FromOSEnv, absolute config path. Service coverage: 85.8% → 87.7%.
- **Server coverage uplift** — 7 new TypeScript tests in `api.test.ts`. Exported `normalizePlatform`/`normalizeArch` from `server/sources/api.ts` for direct branch testing. Covers: win32/darwin/linux/unknown platforms, x64/arm64/unknown arches. Server coverage: 88.7%/91.0%/99.1% → 89.0%/91.5%/99.1%.
- **Coverage gates ratcheted** — service gate 85% → 87%, server gates 88/90/99 → 89/91/99. All five gates pass at new values. 31 new tests total.

- **v1.6 Slice B — Service manager rollback and config error-path tests** — Added 9 new Go tests covering behavioral edge cases not previously tested: `TestCreateRulePersistFailureRollsBack` (CreateRule removes appended rule from memory when persist fails), `TestUpdateRulePersistFailureRollsBack` (UpdateRule restores original rule on persist failure), `TestUpdateRulePersistFailureWithRunningRuleRestartsOriginal` (stopped-then-persist-failed rule is re-started with original config), `TestDeleteRulePersistFailureRollsBack` (DeleteRule keeps rule in list on persist failure), `TestReorderRulesPersistFailureRollsBack` (ReorderRules preserves original order on persist failure), `TestNewFromConfigLoadError` (NewFromConfig propagates config Load error; returns nil manager), `TestStartEnabledWithFailingRule` (StartEnabled returns error and correct started count when a bind fails). Config store: `TestLoadNonErrNotExistError` (Load propagates non-ErrNotExist read errors as distinct from missing-file case), `TestSaveMkdirAllFails` (Save returns error when parent path component is a regular file). Persist failures are injected via a `failingStore` helper that places the store path under a regular file (causes `os.MkdirAll` to fail — clean and cross-platform). Service coverage: 87.7% → 88.6%. Service gate raised: 87% → 88%.

- **v1.6 Slice A — Coverage tooling stabilization and structural-zero cleanup** — Fixed intermittent Windows vitest/v8 path-case deduplication bug in `scripts/validate-coverage.js`. On Windows, vitest's `coverage.include` glob resolves file paths with uppercase drive letter (`C:\...`) while Node.js file-URL resolution produces lowercase (`c:\...`). Both paths appeared as separate keys in `coverage-summary.json` — doubling the statement denominator and collapsing aggregate coverage by ~50% on affected runs. Fix: `normalizePath()` lowercases the drive letter before deduplication; `readSummary()` recomputes totals from deduplicated per-file entries (keeping the entry with more `statements.covered`), preserving Go workspace compatibility (Go writes only a `total` key). Excluded structural-zero files from vitest coverage `include` in each workspace config: `shared` — `activity.ts`, `connections.ts`, `plan.ts` (type-only — no executable JavaScript); `server` — `index.ts` (HTTP server entry point, E2E only), `forwarders/types.ts` (TypeScript interface); `client` — `main.tsx` (ReactDOM browser mount, E2E only). Client branch/funcs gates recalibrated from 90/79 to 89/78 — the previous gates were set from a "clean" (no-ghost) run; the accurate post-deduplication values are ~89.6% branch and ~78.6% funcs. `docs/coverage-baseline.md` updated with v1.6 Coverage Methodology section documenting structural zeros, the Windows deduplication fix, the gate recalibration rationale, and the full excluded-files list. Both `npm run validate:coverage` runs pass (two consecutive runs confirmed deterministic).

---

## [1.5.0] — 2026-06-09

### Goal

Portier v1.5 adds declarative config and drift control: plan/diff/apply workflows for comparing desired config files with the running configuration, previewing changes safely, and applying them from the CLI or UI with explicit confirmation. Coverage was hardened across all five components before and during the feature work.

### Added

- **v1.5 Slice 8 — v1.5 readiness audit, version bump to 1.5.0** — Full validation suite run: lint clean, typecheck clean, 756 unit tests pass (310 server + 366 client + 80 shared), all 5 coverage gates pass at ratcheted v1.5.0 thresholds (cli ≥93%, client ≥94/90/79, server ≥88/90/99, service ≥85%, shared 100%), 156/156 `validate:contract` assertions pass against both runtimes, 32/32 E2E tests pass across 9 spec files. `validate:runtime:smoke` passes. `build:release:current` produces portable zip and installer. Version bumped to 1.5.0 across all workspaces (`package.json` root/client/server/shared, `shared/sources/index.ts` `PORTIER_APP_VERSION`, `tools/cli/sources/version/version.go`). Coverage gates ratcheted: cli 92→93, server 87/89/99→88/90/99, client 94/90/78→94/90/79, service 84→85, shared 100 unchanged. Stale `docs/api-contract.md` heading for `POST /api/config/plan` fixed to "Implemented (both runtimes, v1.5)". All docs updated: `docs/roadmap.md`, `docs/checklist.md`, `docs/coverage-baseline.md`, `docs/e2e-coverage.md`, `docs/changelog.md`, `AGENTS.md`, `CLAUDE.md`. E2E spec file count corrected from 5→9 files and 31→32 tests.

- **v1.5 Slice 7 — Contract/config validation and coverage gate hardening** — `validate:contract` 156/156 assertions pass against both TypeScript server and Go service for all plan and apply scenarios. Coverage gates ratcheted upward from v1.5 pre-release values. All five component gates enforced by `npm run validate:coverage`. New v1.5 code (plan engine, apply handler, CLI apply command, planHelpers, API helpers) verified at 100% meaningful coverage. See `docs/coverage-baseline.md` for full breakdown.

- **v1.5 Slice 6 — Settings UI Plan & Apply config preview** — Added Plan & Apply Config section to the Settings view (`client/sources/features/settings/SettingsView.tsx`). New client API helpers: `planConfig` and `applyConfig` in `client/sources/api/portierApi.ts`, each with fetch/error propagation and tests. New plan helper module: `client/sources/features/settings/planHelpers.ts` (`formatOperationType`, `formatChangeValue`, `isDestructivePlan`, `hasPlanErrors`) with 17 unit tests. Settings UI workflow: file picker with distinct `aria-label="Select config file for plan"`; file read via FileReader (parse error shown on invalid JSON or wrong version); "Preview changes" button calls `POST /api/config/plan` and shows structured plan preview: summary counts (Add/Update/Remove/Unchanged/Destructive), no-drift in-sync message, errors block (disables Apply), warnings block `[CODE] message`, operation list `{type} {ruleName} ({PROTOCOL})` with field changes. Destructive plan requires explicit confirmation checkbox before Apply is enabled. Apply sends `{ desired, yes: true }` to `POST /api/config/apply`; success refreshes rule list via `fetchForwardRules` + `onRulesUpdated` callback, clears form; `ok:false` or network error shows alert. Existing Import Config controls remain unchanged. 23 new unit tests in `SettingsView.test.tsx` covering all plan section states (heading, file picker label, preview button gating, parse error, version mismatch, planConfig call, summary counts, no-drift, operation list, update field changes, plan errors, warnings, destructive checkbox, button enable/disable, apply flow, ok:false path, error paths, form clear on success, import section coexistence). New E2E test: `tests/e2e/settings.spec.ts` — plan & apply previews drift and applies non-destructive config (uploads config, previews Add:1, applies, verifies rule in Forward Rules). Fixed existing E2E file label queries in `portier.spec.ts` and `settings.spec.ts` to use `{ exact: true }` after adding the second plan file picker. Client branch coverage: 89.8% → 90.1% (gate 90%). All 5 coverage gates pass. 32/32 E2E tests pass.

- **v1.5 Slice 5 — CLI `config apply`, `POST /api/config/apply` (both runtimes)** — Implemented `POST /api/config/apply` in TypeScript server (`server/sources/api.ts`) and Go service (`service/sources/api/api.go`). Handler logic: missing `desired` → 400; plan errors → 200 `ok:false`, no mutation; `dryRun: true` → 200 `ok:true`, no mutation (does not require `yes`); destructive operations without `yes: true` → 400; drift → replace import with current `ruleId` injected for key-matched rules to preserve IDs; no drift → 200 `ok:true`, no import called. Response: `ConfigApplyResponse { ok, dryRun, appliedAt, plan, applied: { add, update, remove, unchanged } }`. Go API tests: 11 integration tests (`service/sources/api/api_test.go`). TS API tests: covered by existing pattern. CLI: `portier config apply <file>` added to `configcmd.go` with flags `--yes`, `--dry-run`, `--backup-out <path>`; local file validation before API call; backup export before apply (skipped when `--dry-run`). Exit codes: invalid usage (2) > connection error (3) > plan errors/not-ok (1) > success (0). Human output: "Nothing to apply", "Dry run complete", "Config applied" with per-operation counts. `client.go` extended with `ConfigAppliedCounts`, `ConfigApplyRequest`, `ConfigApplyResponse`, and `ApplyConfig` method. `configapplycmd_test.go` added with 25 tests covering all CLI paths. `client_test.go` extended with 7 `ApplyConfig` tests. `scripts/validate-contract.js` apply skip replaced with 9 real assertions; contract passes 156/156 (both runtimes). `client/sources/features/apidocs/ApiDocsView.tsx` planned badge removed; response shape updated to reflect actual fields. `ApiDocsView.test.tsx` updated accordingly. `docs/api-contract.md` updated with implemented entry, actual shapes, and behavior notes. All 5 coverage gates pass.

- **v1.5 Slice 4 — CLI `config plan` and `config diff` commands** — Added `portier config plan <file>` and `portier config diff <file>` to the Go CLI. Both commands validate the desired config file locally (reusing the existing `parseLocalConfig`/`validateLocalConfig` path), then call `POST /api/config/plan`. Local validation failure exits 2 without calling the API. `config plan` outputs a structured summary line (`Add: N  Update: N  Remove: N  Unchanged: N  Destructive: N`) followed by a per-operation listing with field-level change detail (indented `field: before → after` lines) and `[destructive]` marker; no drift prints "No drift detected." with unchanged count. `config diff` outputs `+`/`~`/`-`/`=`-prefixed lines per operation with indented field changes for updates; unchanged rules are hidden by default (shown with `--show-unchanged`). Both: warnings printed as `[CODE] message` block; plan errors printed as `[CODE] message` block. `--fail-on-drift` flag: exits 4 when `summary.hasDrift` is true and `summary.hasErrors` is false. Exit code priority: invalid usage/local file failure (2) > connection failure (3) > API error (1) > plan errors (1) > drift with `--fail-on-drift` (4) > success (0). `--json` on both commands prints raw `ConfigPlanResponse`. Help text: `portier config plan --help`, `portier config diff --help`, `portier config help` updated. `client.go` extended with types: `ConfigPlanRuleSnapshot`, `ConfigPlanChange`, `ConfigPlanOperation`, `ConfigPlanSummary`, `ConfigPlanError`, `ConfigPlanWarning`, `ConfigPlanResponse`, `ConfigPlanDesired`, `ConfigPlanRequest`, and method `PlanConfig`. `configcmd.go` extended with `RunConfigPlan`, `RunConfigDiff`, `buildPlanRequest`, `planExitCode`, `printPlanHuman`, `printDiffHuman`, `opEndpoint`, `opDiffPrefix`, `opDiffLabel`, `formatChangeValue`, `printPlanWarnings`, `printPlanErrors`. New test file `configplancmd_test.go`: 35 tests covering no-drift human output, drift summary, update with changes, warnings rendered, plan errors non-zero exit, JSON output, file-not-found/invalid-JSON/local-validation-fail exit 2 (API not called), API error exit 1, connection failure exit 3, `--fail-on-drift` exit 4 and 0, plan errors take precedence over drift, unknown flag exit 2, nil change value rendering, warnings on no-drift path, diff add/update/remove, hide unchanged, `--show-unchanged`, diff no-drift, diff JSON, diff `--fail-on-drift`, diff connection error, dispatch tests. Client test additions: 5 `PlanConfig` tests (success, plan-errors-in-200, request body shape, API error, connection error). CLI coverage: 92.7% → 93.2% stmts (gate 92%). All 5 component coverage gates pass.

- **v1.5 Slice 3 — Go service config plan engine and POST /api/config/plan parity** — Added pure Go plan engine (`service/sources/configplan/plan.go`, `service/sources/configplan/plan_test.go`). Exported API: `BuildConfigPlan(Input) Response`, `ExtractRulesRaw(json.RawMessage) ([]json.RawMessage, bool)`. Engine mirrors TypeScript semantics exactly: id-first matching, identity key (protocol+listenHost+listenPort) fallback, 8 material field diff in declared order, destructive flag (remove always destructive; update destructive when protocol/listenHost/listenPort/targetHost/targetPort/udpMode change), `REMOVE_EXISTING` warning per removed rule, `LAN_EXPOSURE` warning per 0.0.0.0 desired rule (skipped when AMBIGUOUS_CURRENT_MATCH error applies). Error codes: `INVALID_DESIRED_CONFIG`, `INVALID_DESIRED_RULE`, `DUPLICATE_DESIRED_ID`, `DUPLICATE_DESIRED_IDENTITY_KEY`, `AMBIGUOUS_CURRENT_MATCH`. `extractRulesRaw` accepts raw array, `{"rules":[...]}` wrapper, exported config shape; rejects null, number, empty object. `desired: null` passes to engine and produces `INVALID_DESIRED_CONFIG`; missing `desired` key returns 400. All output slices initialized as `[]` not `null`. `generatedAt` format: millisecond ISO UTC (`"2006-01-02T15:04:05.000Z"`). Route and handler added to `service/sources/api/api.go`; `service/sources/api/api_test.go` extended with 10 plan endpoint integration tests. `scripts/validate-contract.js` updated: Go plan skip removed; 11 real assertions now run against Go service (138 passed, 2 skipped for `config/apply`). `client/sources/features/apidocs/ApiDocsView.tsx` parity badge removed (`POST /api/config/plan` now shows no badge — both runtimes implement). `ApiDocsView.test.tsx` updated accordingly. 49 Go engine unit tests covering all operation types, all error codes, matching semantics (id vs identity key, id wins when both match, no fuzzy name), duplicate detection, ambiguous match, warnings, summary counts, UDP rules, extractRulesRaw all valid/invalid shapes. Service coverage: 84.8% → 85.8% stmts (gate 84%).

- **v1.5 Slice 2 — TypeScript config plan engine and POST /api/config/plan** — Added `server/sources/config-plan.ts` implementing a pure, deterministic plan engine (`buildConfigPlan`) with no config mutation, no filesystem writes, and no API coupling. Rule matching: by stable `id` if present; otherwise by identity key (`protocol+listenHost+listenPort`). All material fields compared (name, protocol, listenHost, listenPort, targetHost, targetPort, enabled, udpMode). Operations: `add` (not destructive), `remove` (always destructive), `unchanged` (no changes), `update` (destructive if forwarding fields change; non-destructive for name/enabled only). Errors: `INVALID_DESIRED_CONFIG`, `INVALID_DESIRED_RULE`, `DUPLICATE_DESIRED_ID`, `DUPLICATE_DESIRED_IDENTITY_KEY`, `AMBIGUOUS_CURRENT_MATCH`. Warnings: `REMOVE_EXISTING` (per removed rule), `LAN_EXPOSURE` (per 0.0.0.0 desired rule). Accepts desired config as raw array, `{ rules: [...] }` wrapper, or exported config shape. `POST /api/config/plan` route added to TypeScript server (`server/sources/api.ts`); returns 400 for missing `desired` field; returns 200 with structured plan errors for invalid desired config. `server/sources/config-plan.test.ts` added with 65 unit tests covering all operation types, matching semantics (id, identity key, fuzzy-name non-match), all error codes, summary counts, material vs transient field handling, warnings, and all three input shapes. `server/sources/api.test.ts` extended with 11 endpoint integration tests. `scripts/validate-contract.js` updated: TS skip removed; 11 real assertions added for TypeScript runtime; Go runtime defers with a skip note pending Slice 3. `client/sources/features/apidocs/ApiDocsView.tsx` updated: `POST /api/config/plan` now shows parity note badge ("TypeScript server — Go service parity pending") instead of planned badge. `ApiDocsView.test.tsx` updated accordingly. Coverage: `config-plan.ts` 100% stmts/branches/funcs; server overall 88.6% stmts.

- **v1.5 Slice 1 — Declarative Config strategy, contract, and shared plan/diff model** — Defined config comparison semantics, plan operation model, plan summary model, and API contract for `POST /api/config/plan` and `POST /api/config/apply`. Shared TypeScript types added to `@portier/shared` (`shared/sources/plan.ts`): `ConfigPlanOperationType`, `ConfigPlanChange`, `ConfigPlanRuleSnapshot`, `ConfigPlanOperation`, `ConfigPlanSummary`, `ConfigPlanError`, `ConfigPlanWarning`, `ConfigPlanResponse`, `DesiredConfig`, `ConfigPlanRequest`, `ConfigApplyRequest`, `ConfigApplyResponse`. `shared/sources/plan.test.ts` added with 20+ shape tests covering all types, all operation types, all UDP modes, error/warning shapes, and fully populated plan response. `docs/api-contract.md` updated with planned `POST /api/config/plan` (matching semantics, request/response shapes, destructive operation rules, error conditions) and `POST /api/config/apply` sections. Shared Shapes section updated with v1.5 planned types. Client in-app API Docs (`ApiDocsView.tsx`) updated with two planned entries (`Planned — v1.5` badge). `ApiDocsView.test.tsx` updated with 6 new tests verifying planned endpoints appear with badges and key response fields. `scripts/validate-contract.js` updated with skip notes for both endpoints. `docs/e2e-coverage.md` updated with planned v1.5 E2E workflows. `tools/cli/readme.md` updated with planned `config plan`, `config diff`, and `config apply` commands. `docs/roadmap.md` v1.5 section expanded with matching semantics, operation model, destructive operation rules, and Slice 1 marked complete.

- **E2E coverage baseline and uplift** — assessed and expanded Playwright E2E coverage from 4 spec files / ~19 tests to 5 spec files / 31 tests. New spec: `tests/e2e/connections.spec.ts` (8 tests covering the v1.4 Live Connections view: title/tabs/empty states, tab switching, summary stats bar, protocol filter set/clear, auto-refresh toggle, footer counts, rule filter dropdown populated when a rule is running). New tests added to `tests/e2e/portier.spec.ts`: rule form validation (name required error), diagnose rule (panel opens, results shown, close button), `GET /api/connections` listed in API Docs. New test added to `tests/e2e/settings.spec.ts`: Runtime/Environment section shows "Node server" and key labels. Created `docs/e2e-coverage.md` with full workflow coverage matrix (35+ workflows, real vs mocked classification, v1.5 readiness notes, and remaining gap documentation). Fixed pre-existing test bugs: export button label corrected to "Download Config", filename pattern updated to `portier-config-YYYYMMDD-HHMMSS.json`, `getByLabel("Listen Host")` strict mode violation fixed in `tests/e2e/helpers/ui.ts` (presets group div was ambiguously matched), ambiguous `getByText` calls fixed with exact matches and CSS class selectors. All 31 E2E tests now pass cleanly.

---

## [1.4.0] — 2026-06-09

### Goal

Portier v1.4 adds a read-only Live Connection Inspector: active TCP connections and UDP sessions are tracked in both runtimes, exposed via `GET /api/connections`, and visible in a dedicated Live Connections view in the web UI. Coverage was hardened across both runtimes before the feature was built.

### Added

- **v1.4 Slice 8 — Live Connections UI** — added Live Connections view to the client (`client/sources/features/connections/LiveConnectionsView.tsx`). Sidebar navigation entry "Connections" added with `Network` icon (`client/sources/app/NavItem.ts`). View fetches `GET /api/connections` via `fetchLiveConnections()` on mount and on demand. Three tabs: TCP Connections (rule, client, target, duration, bytes in/out, Active badge), UDP Sessions (rule, mode, client, target, idle, packets in/out, bytes in/out, Active/Idle badge), Rule Summary (rule, protocol, TCP count, UDP count, bytes in/out, packets in/out, Last Traffic). Summary strip shows total TCP connections, UDP sessions, active rules, and total traffic. Client-side filters: protocol (all/tcp/udp), status (all/active/idle), rule dropdown populated from `ruleSummaries`. Rule Summary tab sorts active rules first, then by name. `lastTrafficAt: null` renders as "Never". Loading, error, and empty states for each tab. Auto-refresh toggle (default on, 2s interval). Manual refresh button. `generatedAt` shown as "Updated at HH:MM:SS" in subtitle. New format helpers added to `client/sources/utils/format.ts`: `formatDurationMs` (0s/1m 05s/1h 02m), `formatEndpoint` (address:port), `formatTimestampOrNever` (null → "Never"). Format helper tests added in `client/sources/utils/format.test.ts`. LiveConnectionsView tests: 50 tests covering fetch on mount, loading state, error state, generatedAt display, summary strip, all three tabs (empty/data/accessible table label), duration and bytes formatting, Active/Idle status badges, sort order in Rule Summary, all three filters and clear-filters button, refresh button, auto-refresh toggle and interval, and footer count (singular/plural). CSS additions in `styles.css`: `.connections-summary`, `.connections-summary-item/value/label`, `.connections-tabs`, `.connections-tab/--active`, `.conn-status-badge/--active/--idle`.

- **v1.4 Slice 7 — `GET /api/connections` parity** — implemented `GET /api/connections` on both runtimes. TypeScript server (`server/sources/api.ts`): builds `LiveConnectionsResponse` from `getLiveTcpConnections()` + `getLiveUdpSessions()` + `listRules()`; `ruleSummaries` covers all configured rules (active or idle); `lastTrafficAt` uses max `startedAt` for TCP connections and max `lastSeenAt` for UDP sessions; empty arrays when no traffic. Go service (`service/sources/api/api.go`): same shape via `GetLiveTCPConnections()` + `GetLiveUDPSessions()` + `ListRules()`; `RuleLiveSummary` and `LiveConnectionsResponse` types added to `service/sources/connections/live_connections.go`; `LastTrafficAt *string` marshals to `null` when nil; arrays always non-null (marshaled as `[]`). Client API helper: `fetchLiveConnections()` added to `client/sources/api/portierApi.ts`. Contract validator: skip replaced with 8 real checks (200 status, parseable `generatedAt`, arrays, summary count, summary shape, null `lastTrafficAt` for idle rules, no forbidden fields). API Docs in-app view: `Planned — v1.4` badge removed. TypeScript server: 6 tests (empty state, idle summaries, generatedAt, live TCP connection, live UDP session, no forbidden fields). Go service: 6 tests (empty state, idle zero summaries, camelCase field names, live TCP connection, live UDP session). `docs/api-contract.md` updated from Planned to Implemented.

- **v1.4 Slice 6 — Go service UDP session tracking** — added `UdpSessionRegistry` (`service/sources/connections/udp_session_registry.go`): composite session keys (`ruleId:mode:clientAddress:clientPort`) for deterministic per-client identity, `OpenOrTouchSession`/`RecordInbound`/`RecordOutbound`/`CloseSession`/`CloseSessionsForRule`/`PruneExpired`/`Snapshot`/`SnapshotForRule` API, value-copy snapshots with `idleMs` and `status` (`active`/`idle`) calculated at snapshot time, expired sessions filtered from snapshot without explicit prune. Constants: `UDPSessionIdleDuration = 30s`, `UDPSessionExpireDuration = 5min`. Concurrency-safe: `sync.Mutex` protects all operations (full packet ops, not hot-path byte chunks). Wired into all three UDP modes in `UDPForwarder`: one-way and last-client call `OpenOrTouchSession` + `RecordInbound` on each inbound packet; last-client mode reads `prevSessionID` under mutex, closes it via `CloseSession` when client endpoint changes, writes new `lastSessionID` under mutex; bidirectional-last-client `targetReadLoop` reads `lastSessionID` under mutex and calls `RecordOutbound`; multi-client mode calls `OpenOrTouchSession` under forwarder mutex when creating session (lock order: forwarder → registry, no deadlock risk), stores `registryID` on `udpSession` struct, calls `RecordInbound` after if/else block, calls `RecordOutbound` in `sessionReadLoop`, calls `CloseSession` in `expireSession` after releasing forwarder mutex. `Stop()` clears `lastSessionID` under mutex and calls `CloseSessionsForRule` as belt-and-suspenders cleanup. `Manager` owns shared `UdpSessionRegistry`, uses `NewUDPForwarderWithRegistry` constructor for UDP rules, exposes `GetLiveUDPSessions() []UdpSessionInfo` for internal use. No public `GET /api/connections` endpoint added yet. No forwarding behavior or existing API behavior changed. Coverage: registry 98.4% stmts (100% all public methods; stale-key defensive path in `OpenOrTouchSession` and `rand.Read` fallback in `generateConnectionID` are accepted untestable paths); 33 registry unit tests (`connections_test` package). Forwarder: `newUDPForwarderWithRegistryAndTimeout` unexported constructor for test isolation; 11 integration tests (real sockets, `waitForUDPCondition` polling, all three modes). Service overall: 80.6% → 82.1% stmts.

- **v1.4 Slice 5 — Go service TCP live tracking** — added `TcpConnectionRegistry` (`service/sources/connections/tcp_connection_registry.go`): runtime-local UUID IDs, `OpenConnection`/`AddBytesIn`/`AddBytesOut`/`CloseConnection`/`CloseConnectionsForRule`/`Snapshot`/`SnapshotForRule` API, serializable `TcpConnectionInfo` snapshots (`id`, `ruleId`, `ruleName`, `protocol`, `clientAddress`, `clientPort`, `targetAddress`, `targetPort`, `startedAt`, `durationMs`, `bytesIn`, `bytesOut`, `status`). `durationMs` calculated at snapshot time. Concurrency-safe: mutex protects map operations; atomic ops handle byte counters in the IO copy hot path to avoid lock contention. `NewTCPForwarderWithRegistry` constructor added to `TCPForwarder`; `countingWriter` extended with `onBytes func(int64)` callback invoked per write chunk; `copyAndClose` accepts optional `onBytes` parameter; `Stop()` calls `CloseConnectionsForRule` after `wg.Wait()` as belt-and-suspenders cleanup for the shutdown timeout path. `Manager` owns shared `TcpConnectionRegistry`, passes it to each `TCPForwarder` via `NewTCPForwarderWithRegistry`, and exposes `GetLiveTCPConnections() []TcpConnectionInfo` for internal use. No public `GET /api/connections` endpoint added yet. No forwarding behavior or existing API behavior changed. Coverage: registry 98.1% stmts (100% all public methods; 1 untestable `rand.Read` error path in private `generateConnectionID`); 26 registry unit tests. Forwarder: 8 new integration tests (real sockets, `waitForTestCondition` polling). Service overall: 79.7% → 80.6% stmts.

- **v1.4 Slice 4 — TypeScript server UDP session tracking** — added `UdpSessionRegistry` (`server/sources/connections/udp-session-registry.ts`): runtime-local UUID IDs, composite session keys (`ruleId:mode:clientAddress:clientPort`) for deterministic identity, `openOrTouchSession`/`recordInbound`/`recordOutbound`/`closeSession`/`closeSessionsForRule`/`pruneExpired`/`snapshot`/`snapshotForRule` API, immutable plain-object snapshots with `idleMs` and `status` calculated at snapshot time. Constants: `UDP_SESSION_IDLE_MS = 30_000` (30s), `UDP_SESSION_EXPIRE_MS = 300_000` (5min). `snapshot` and `snapshotForRule` filter expired sessions from output without mutating state; `pruneExpired` removes them explicitly from memory. Wired into `UdpForwarder` (4th optional constructor param): one-way and last-client modes call `openOrTouchSession` on each inbound packet; last-client mode detects client-endpoint changes, closes the previous session, and opens a new one; multi-client mode calls `openOrTouchSession` per client in `handleMultiClientMessage` with `registryId` stored on the session struct; `recordOutbound` called on target responses in last-client and multi-client modes; `closeSession` called on multi-client timeout; `closeSessionsForRule` called in `stop()` as bulk cleanup. Existing constructor callers that omit the 4th parameter continue to work unchanged. `ForwardManager` owns shared `UdpSessionRegistry`, injects it into every `UdpForwarder`, and exposes `getLiveUdpSessions()` for internal use; no public `GET /api/connections` endpoint added yet. Coverage: registry 100% stmts/branch/funcs; 49 unit tests. `udp-forwarder.ts`: 86.3% stmts (up from 84.3%), 84% branch, 100% funcs; 9 new forwarder integration tests (real sockets, no arbitrary sleeps). Server overall: 80.55% → 82.21% stmts. No forwarding behavior, existing API behavior, or existing tests changed.

- **v1.4 Slice 3 — TypeScript server TCP live tracking** — added `TcpConnectionRegistry` (`server/sources/connections/tcp-connection-registry.ts`): runtime-local ID assignment, `openConnection`/`addBytesIn`/`addBytesOut`/`closeConnection`/`closeConnectionsForRule`/`snapshot`/`snapshotForRule` API, immutable plain-object snapshots, `durationMs` calculated at snapshot time. Wired into `TcpForwarder`: registry entry opened on client accept, `bytesIn` incremented client→target, `bytesOut` incremented target→client, entry removed on connection close (`onClosed` countedClosed guard) and immediately on error path (`closeBoth`), `closeConnectionsForRule` called in `stop()` for deterministic cleanup. `ForwardManager` owns shared `TcpConnectionRegistry`, injects it into every `TcpForwarder`, and exposes `getLiveTcpConnections()` for internal use; no public API endpoint added yet. Coverage: registry 100% stmts/branch/funcs; `tcp-forwarder.ts` 100% stmts/funcs, 90% branch (optional registry param branches when omitted); server overall 79.6% → 80.55% stmts. 28 new registry unit tests and 7 forwarder integration tests added (real sockets, no arbitrary sleeps). No forwarding behavior, existing API behavior, or existing tests changed.

- **v1.4 Slice 2 — Shared types and API contract finalization** — added `LiveConnectionsResponse`, `TcpConnectionInfo`, `UdpSessionInfo`, `RuleLiveSummary`, `LiveConnectionStatus`, `UdpSessionStatus` to `@portier/shared` (`shared/sources/connections.ts`). Finalized `GET /api/connections` contract in `docs/api-contract.md`: field directions documented (`bytesIn` = client-to-target, `bytesOut` = target-to-client, `packetsIn`/`packetsOut` for UDP), `lastTrafficAt` documented as `null` (not absent) when no traffic since rule start, `ruleName` documented as empty string `""` when unresolvable, Shared Shapes section updated to record types as defined with implementation pending. Client in-app API Docs updated with planned endpoint entry and `Planned — v1.4` badge; 5 new `ApiDocsView` tests added. `validate:contract` updated with a skip note for the unimplemented endpoint. `connections.test.ts` added with 14 shape tests covering all types, status unions, UDP modes, and the fully populated response. Runtime implementation remains pending (Slices 3–7).

- **v1.4 Slice 1 — TypeScript server forwarder coverage hardening** — raised `tcp-forwarder.ts` from 68.5% to 100% statements and 100% functions; raised `udp-forwarder.ts` from 57.7% to 84.3% statements and 100% functions. Server overall coverage: 71.9% → 79.6% statements. Added 7 new TCP forwarder tests (idempotent start/stop, bind failure, connection opened/closed events, target-unreachable error, post-bind server error, stop-with-active-connections) and 13 new UDP forwarder tests (lifecycle, one-way forwarded event, forwarded rate-limiting, last-client stats and returned event, multi-client timer reset, session opened/closed events, multi-client forwarded rate-limiting, listen/target/session socket error handlers). Remaining UDP gaps (multi-client send/return error callbacks and a race guard) require mocking to trigger deterministically; documented in `docs/coverage-baseline.md`. No forwarding behavior, API contracts, or runtime behavior changed.

- **Coverage baseline measured** — pre-v1.4 statement coverage: tools/cli 92.7% (gate 92%), client 89.2%, service 79.7%, shared 82.1%, server 71.9%. Scripts not yet measured. Full per-file breakdown and ratchet plan in `docs/coverage-baseline.md`.
- **Coverage tooling added** — `@vitest/coverage-v8` installed; vitest.config.ts files added/updated in all three TypeScript workspaces (client, server, shared) with coverage include/exclude settings; `coverage` script added to each workspace; `npm run coverage:shared/server/client/service/cli/baseline` scripts added to root `package.json`; `scripts/coverage-service.js` and `scripts/coverage-cli.js` added for Go combined coverage reporting; `scripts/validate-coverage.js` added as unified coverage validator with `--only <component>` support, replacing `scripts/validate-cli-coverage.js`; `validate:cli:coverage` now delegates to `validate-coverage.js --only cli`.
- **`waitForTestCondition` timeout raised** — `service/sources/forwarders/tcp_test.go` timeout increased from 2s to 5s to eliminate a flaky failure under cross-package coverage instrumentation with sequential (`-p 1`) test execution.

---

## [1.3.0] — 2026-06-08

### Added (v1.3 Slice 8 — Readiness audit, coverage gate, version bump)

- **CLI test coverage gate** — `npm run validate:cli:coverage` runs Go tests with cross-package coverage instrumentation and fails if total statement coverage falls below 88%. `scripts/validate-cli-coverage.js` documents genuinely untestable branches (main entry point, http.NewRequest errors, json.Marshal errors). Coverage gate included in `validate:cli`.
- **Coverage tests added** — new tests fill coverage gaps: `output/output_test.go` (FormatBytes MB/GB path, FormatTimestamp RFC3339Nano and invalid paths), `main_test.go` (run() full dispatch for all 9 commands + flag/help/version/unknown branches), and targeted additions to `list_test.go`, `activity_test.go`, `configcmd_test.go`, `status_test.go`, `diagnose_test.go`, `start_test.go`, `stop_test.go`, and `diagnosticscmd_test.go` covering formatProto all UDP modes, --help flags for all subcommands, RunConfig dispatch through all three subcommands, parseLocalConfig edge cases (empty file, invalid array, invalid object, missing rules field, rules not array, null rules), ActiveUDPSessions branch, empty Checks branch, non-connection API errors, per-rule diagnose errors, and JSON output with warningCount.
- **Total CLI test coverage: 90.1%** across sources, client, commands, and output packages.
- **Version bumped to 1.3.0** — `package.json` and `tools/cli/sources/version/version.go`.

### Added (v1.3 Slice 7 — CLI packaging into runtime and release artifacts)

- **CLI binary included in runtime package** — `portier`/`portier.exe` is now built and included in `build/portier/` alongside `service`/`service.exe`, `server.js`, `web/`, and `readme.txt`.
- **Platform build scripts updated** — `scripts/windows/build-runtime.ps1`, `scripts/macos/build-runtime.sh`, and `scripts/linux/build-runtime.sh` each build the CLI from `tools/cli/` directly into the output directory as part of the normal `npm run build:runtime` step.
- **Runtime validation updated** — `scripts/validate-runtime.js` now requires the CLI binary (`portier`/`portier.exe`), verifies it is non-empty, verifies CLI and service binaries have different names, and checks that `readme.txt` mentions `portier runtime`, `portier list`, and `portier diagnostics export`.
- **Release archive validation updated** — `scripts/validate-release.js` now requires the CLI binary in portable archives and validates `readme.txt` CLI command references.
- **Windows installer updated** — `scripts/windows/release/portier.iss` now includes `portier.exe` in the installed files under `{app}`.
- **`readme.txt` updated** — all three platform readme.txt templates now list the files in the package, document CLI usage (six key commands), and note that the CLI requires a running service and does not start the service by itself.
- **`build:clean` updated** — `scripts/clean-build.js` now removes `tools/cli/build/` alongside the other build output directories.
- **No PATH integration in v1.3** — the installer does not add `portier` to PATH; users invoke the CLI by full path or add the install directory to PATH manually.

### Added (v1.3 Slice 6 — Diagnostics export command)

- **`portier diagnostics export --out <file>`** — builds a local JSON diagnostics bundle from the running Portier service. Fetches runtime info, rules, statuses, and recent activity independently; partial failures are recorded in `errors[]` rather than aborting the export. Bundle is always written as long as the file write succeeds.
- **`--run-diagnostics` flag** — when present, runs `POST /api/forwards/:id/diagnose` for each rule sequentially and includes results in `diagnostics.<ruleId>`. If the rules list could not be fetched, per-rule diagnostics are skipped entirely. Per-rule failures are recorded in `errors[]` with source `diagnostics:<ruleId>`.
- **`--activity-limit <n>` flag** — controls how many activity events are included (1–500, default: 100). Exits code `2` if out of range.
- **Bundle schema** (`schemaVersion: "1"`): `app`, `runtime`, `rules`, `statuses`, `diagnostics`, `diagnosticsNote`, `activity` (with `included`/`events`/`note`), `metadata` (`managementUrl`, `source: "cli"`, `generatedBy`), `errors[]`. Does not include logs, environment variables, OS usernames, or raw disk files.
- **Human output**: `Exported diagnostics to <file>` with rule/status/activity counts; with partial failures: `Exported diagnostics with warnings to <file>`.
- **JSON output** (with `--out`): `{"ok":true,"path":"...","ruleCount":N,"statusCount":N,"activityCount":N,"diagnosticCount":N}`; with warnings adds `"warningCount":N`. Without `--out`: prints full bundle to stdout.
- **Human mode without `--out`**: exits code `2` with clear message.
- **Diagnostics subcommand routing** (`RunDiagnostics`): `portier diagnostics help` / `portier diagnostics <unknown>` handled cleanly.
- **`BaseURL()` method** added to API client for embedding the management URL in the bundle metadata.
- **Tests**: 153 CLI tests total (was 132); new tests cover dispatch, usage validation (missing `--out`, invalid `--activity-limit`), happy path (writes file, human/JSON output), partial failure (still writes bundle, "with warnings"), `--run-diagnostics` calls diagnose per rule, activity limit passed to API, rules failure skips diagnostics, write failure exits nonzero, bundle content (schemaVersion, metadata.source, diagnosticsNote, counts), no forbidden fields.
- **Help text updated**: `diagnostics` listed in `portier help`; `portier diagnostics help` shows subcommand list and options.

### Added (v1.3 Slice 5 — Config commands: validate, export, import)

- **`portier config validate <file>`** — validates a local config file without importing it or contacting the service; accepts raw JSON arrays, `{"rules":[...]}` wrapper objects, and full export objects `{"version":"1","exportedAt":"...","rules":[...]}`. Checks: non-empty name, valid protocol, non-empty hosts, ports in range, valid `udpMode`, no duplicate listen bindings. Human output: `Config is valid.` / `Config is invalid.` with errors listed. `--json` output: `{"valid":true,"ruleCount":N,"tcpCount":N,"udpCount":N,"errors":[]}`. Exit 0 valid, exit 1 invalid/unreadable.
- **`portier config export --out <file>`** — exports current rules from the running service to a JSON file via `GET /api/config/export`; file is written only after a successful API response (no partial writes). `--json` + `--out`: prints `{"ok":true,"path":"...","ruleCount":N}`. `--json` without `--out`: prints raw config JSON to stdout (useful for piping). Human mode without `--out`: exits with code 2 (requires `--out`).
- **`portier config import --mode merge|replace [--yes] <file>`** — reads and validates a local config file, then calls `POST /api/config/import`. Invalid files are rejected locally without contacting the service. Replace mode requires `--yes` — without it exits with code 2 and shows a clear warning. No interactive prompts. Human output: `Imported config using merge|replace mode.`; `--json`: `{"ok":true,"mode":"merge"|"replace"}`.
- **Config subcommand routing** (`RunConfig`): `portier config help` / `portier config <unknown>` handled cleanly.
- **New API client types**: `ConfigRule`, `ConfigExportResponse`, `ConfigImportRequest`, `ImportResult`, `ConfigImportResponse` mirroring `GET /api/config/export` and `POST /api/config/import` response shapes.
- **New API client methods**: `ExportConfig()`, `ImportConfig(req)` using a new `doWithBody` helper for POST with a JSON body.
- **Tests**: 132 CLI tests total (was 89); new tests cover `ExportConfig`/`ImportConfig` client methods, all validation cases (valid shapes, malformed JSON, invalid protocol/port/udpMode/host/name/duplicate binding), all export cases (writes file, JSON output, stdout mode, API error leaves no file, connection failure), all import cases (merge/replace modes, `--yes` requirement, local validation blocks API, bad mode, connection failure), and config dispatch.
- **Help text updated**: `config` listed in `portier help`; `portier config help` shows subcommand list; each subcommand shows usage and flags.

### Added (v1.3 Slice 4 — Lifecycle and diagnostics commands: start, stop, diagnose)

- **`portier start <id|name>`** — resolves rule by exact ID or exact name, calls `POST /api/forwards/:id/start`; human output shows `Started <name>  (<listen> → <target>)`; `--json` prints `{"ok": true, "action": "start", "ruleId": "..."}`.
- **`portier stop <id|name>`** — resolves rule, calls `POST /api/forwards/:id/stop`; human output shows `Stopped <name>`; `--json` prints `{"ok": true, "action": "stop", "ruleId": "..."}`.
- **`portier diagnose <id|name>`** — resolves rule, calls `POST /api/forwards/:id/diagnose`; human output shows summary status + message and a CHECK/STATUS/MESSAGE table; `--json` prints raw `RuleDiagnosticsResult`.
- **Safe rule resolver** (`commands.ResolveRule`): exact ID match wins unconditionally; exact name match succeeds if unique; duplicate names produce exit code 2 with an ID/NAME/PROTO/LISTEN disambiguation table on stderr; no match exits 1.
- **API client methods**: `StartForward(id)`, `StopForward(id)`, `DiagnoseForward(id)` added to the management API client; shared `do(method, path, out)` helper extracted to remove duplication between GET and POST paths.
- **New types**: `DiagnosticCheck`, `DiagnosticSummary`, `RuleDiagnosticsResult` mirroring the `POST /api/forwards/:id/diagnose` response shape.
- **Tests**: 89 CLI tests total (was 59); new tests cover `StartForward`/`StopForward`/`DiagnoseForward` client methods, all resolver cases (exact ID, exact name, ID wins over same-text name, duplicate ambiguity, not found, empty list), and all three command handlers (human output by ID and by name, JSON output, missing arg, ambiguity, connection failure, not found).
- **Help text updated**: `start`, `stop`, `diagnose` listed in `portier help`; rule identity note added.

### Added (v1.3 Slice 3 — Read-only commands: list, status, activity)

- **`portier list`** — calls `GET /api/forwards`; human-readable aligned table (NAME, PROTO, LISTEN, TARGET, ENABLED); `--json` prints raw `ForwardRuleResponse[]`. Empty state shows a friendly message. UDP mode shown compactly in PROTO column (`udp/1way`, `udp/lc`, `udp/mc`).
- **`portier status`** — calls `GET /api/status`; human output joins rule names and protocols via `GET /api/forwards` (falls back to ruleId if unavailable); shows NAME, PROTO, STATE, CONNS, BYTES IN, BYTES OUT, LAST ERROR; `--json` prints raw `ForwardStatus[]`.
- **`portier activity`** — calls `GET /api/activity`; human output shows TIME, SEV, TYPE, RULE, MESSAGE; `--json` prints raw `ActivityEvent[]` (not the `{"events":[...]}` API wrapper). Filters: `--limit` (1–500, default 50), `--rule` (rule ID), `--type` (event type), `--severity`; invalid `--limit` exits with code 2.
- **Output helpers** (`tools/cli/sources/output/`): `FormatBool`, `FormatBytes`, `FormatTimestamp`, `PrintTable` — used by all human-readable command output.
- **`exitWithError` helper** in `commands` package — shared connection/API error handling across commands.
- **Tests**: 59 Go CLI tests total (was 22+); new tests cover `GetForwards`, `GetStatus`, `GetActivity` client methods (success, error, query params, empty), plus all three command handlers (human output, JSON output, empty state, connection failure, API error, filter passing, limit validation, ruleId fallback).

### Added (v1.3 Slice 2 — Go CLI skeleton and API client)

- **`tools/cli/` module** — new Go module (`portier/cli`) under `tools/cli/` for the Portier CLI. Separate from the Go service (`portier/service`); the CLI is an API client, not a second runtime.
- **CLI binary name**: `portier` (Linux/macOS) / `portier.exe` (Windows). Background service binary remains `service` / `service.exe`.
- **`portier runtime` command** — calls `GET /api/runtime`; displays name, version, runtime, platform/arch, uptime, management URL, config path, static dir, service mode. Works with both the Go service and TypeScript server.
- **`--json` flag** — prints machine-readable JSON from the API response; no decorative text around JSON output.
- **Connection options** — `--url`, `--host`, `--port`, `PORTIER_URL` env var; precedence: `--url` > `--host`/`--port` > `PORTIER_URL` > default (`http://127.0.0.1:47831`).
- **`portier help` / `--help` / `-h`** — shows purpose, default URL, global flags, available commands, planned commands, exit codes.
- **`portier version` / `--version`** — shows `Portier CLI <version>`.
- **Exit code policy**: `0` success, `1` general/API error, `2` invalid arguments/usage, `3` connection failure.
- **`User-Agent` header**: `PortierCLI/<version>` on every API request.
- **Structured error handling**: `ConnectionError` (maps to exit 3 + hint) and `APIError` (non-2xx response with `errors[]` shape).
- **Tests**: 22+ Go tests using `httptest` covering URL resolution precedence, runtime human/JSON output, connection failure, API errors, User-Agent, invalid JSON, and error type unwrapping.
- **npm scripts**: `build:cli`, `test:cli`, `validate:cli`.
- **`tools/cli/readme.md`** — CLI usage, global flags, commands, exit codes, module structure, planned commands.

---

## [1.2.0] - 2026-06-07

### Goal

Portier v1.2 improves operational confidence with diagnostics, visibility, and safer networking UX. See `docs/roadmap.md` for goals, slices, and non-goals.

### Added (v1.2 Slice 7 — Diagnostics export / support bundle)

- **Download Diagnostics JSON** button in Settings, below Runtime / Environment. Assembles a local JSON support bundle and downloads it directly in the browser — no upload, no backend endpoint.
- **Bundle contents**: `schemaVersion`, `exportedAt`, `app` (name + version), `runtime` (from `GET /api/runtime`), `rules` (fresh fetch), `statuses` (fresh fetch), `diagnostics` (any results already run in the current UI session), `activity` (up to 100 recent events), and `metadata` (management URL, source, generatedBy).
- **Partial-failure handling**: each data source is fetched independently using `Promise.allSettled`. If any source fails, the rest are still included; an `errors` array is added to the bundle and a warning is shown in the UI. The download still proceeds.
- **Empty diagnostics note**: if no rule diagnostics have been run in the current session, the bundle includes `diagnosticsNote: "No rule diagnostics had been run in this UI session."` alongside an empty `diagnostics` object.
- **Excluded data**: raw config file contents from disk, logs, environment variables, OS user name, home directory beyond paths already in the runtime endpoint, node_modules, and build file listings. Nothing is uploaded or transmitted.
- **Filename format**: `portier-diagnostics-YYYYMMDD-HHMMSS.json` (local time; no characters invalid on Windows).
- **`diagnosticsExport.ts`** helper module added at `client/sources/features/settings/`: exports `buildDiagnosticsBundle()`, `buildDiagnosticsFilename()`, and `downloadJson()`.
- **`settings-warn` CSS class** added for the partial-data warning state.
- **New tests**: `diagnosticsExport.test.ts` (28 unit tests covering bundle structure, diagnostics inclusion, partial failure, filename, and download mechanics); 10 new `SettingsView` component tests covering render, fetch calls, success/partial/disabled states, bundle schema, and diagnostics from UI state. 216 tests total pass.

### Added (v1.2 Slice 6 — Safer networking UX)

- **Listen host presets** in the Add/Edit Rule drawer: "Local only" (127.0.0.1) and "LAN exposed" (0.0.0.0) quick-select buttons. Active preset is highlighted; manual host entry remains fully supported.
- **Inline LAN warning** in the rule form: when listenHost is `0.0.0.0`, a warning block appears directly below the listen host/port row stating that the rule listens on all interfaces and that Portier does not create firewall rules automatically.
- **Listen host hint text**: "Only this computer can connect" shown for 127.0.0.1; "Other devices on your network may be able to connect if firewall rules allow it" shown for 0.0.0.0.
- **Firewall note advisory card**: shown in the advisory section when listenHost is `0.0.0.0`. Platform-aware: shows a Windows-specific message if `runtimePlatform="windows"` is provided, otherwise a generic OS note. `ForwardRuleForm` now accepts an optional `runtimePlatform` prop.
- **Friendly conflict error copy**: when a save fails because the listen binding is already in use, the form shows "Another rule is already using this protocol, listen host, and listen port. Choose a different listen port, or stop/remove the conflicting rule." instead of the raw server error.
- **Improved LAN_EXPOSURE advisory message** (shared): updated to "Listening on 0.0.0.0 exposes this forwarded port on all interfaces. Other LAN devices may be able to connect if firewall settings allow it."
- **New tests** (ForwardRuleForm): Local only / LAN exposed presets set correct host; custom host entry still works; LAN warning visible for 0.0.0.0; LAN warning absent for 127.0.0.1; generic and Windows-specific firewall notes; save not blocked by LAN warning; friendly conflict error shown; privileged port advisory. 17 new/updated test cases.

### Added (v1.2 Slice 5 — Settings / runtime / config polish)

- **App version in sidebar footer**: Sidebar now shows `v{version}` below the management host. Sourced from new `PORTIER_APP_VERSION` constant in `@portier/shared`.
- **Copy buttons in Runtime / Environment**: Config path, static dir, and management URL each have a small "Copy" button. Uses `navigator.clipboard.writeText`; shows "Copied!" on success, "Failed" on error, resets after 2 seconds. Gracefully handles environments where clipboard is unavailable.
- **Export Config UX**:
  - Filename is now datetime-stamped: `portier-config-YYYYMMDD-HHMMSS.json` (local time).
  - Button renamed to "Download Config (JSON)" for clarity.
  - Helper text now states "The Activity Log is not included" so users know what the export covers.
  - Export shows a success confirmation message; errors are displayed inline instead of via `alert()`.
- **Import Config UX**:
  - Import mode (Merge / Replace) is now shown above the file picker with descriptive explanations, so users understand the difference before selecting a file.
  - Merge description: "adds rules from the file; skips rules with conflicting listen port bindings."
  - Replace description: "stops and deletes all current rules, then applies imported rules. Export a backup first if you want to keep your current rules."
  - Replace confirmation dialog now includes an "Export current config as backup" button for a one-click safety exit.
- **`PORTIER_APP_VERSION`** constant exported from `shared/sources/index.ts`.
- **New tests** (SettingsView): import mode descriptions visible before file selection; import button absent until valid file loaded; replace confirm shows backup export button; copy config path/static dir/management URL; clipboard failure shows "Failed" feedback; export success/error feedback. 18 new test cases.
- **New tests** (Sidebar): 6 tests covering render, nav click, open state, management host, and version footer.

### Added (v1.2 Slice 4 — Activity Log polish)

- **View activity** button (Activity icon) added to every rule row in the Forward Rules table. Clicking it navigates to the Activity view and pre-filters to that rule.
- Activity view **filter banner**: when navigating from a rule row, a status banner shows "Filtered to rule: [name]" with a Clear button. Handles deleted/missing rule names gracefully by displaying the raw rule ID.
- Activity view **type filter**: new dropdown lets users filter events by event type (rule lifecycle, connection, packet, session, config events). Type is passed as a query param to `GET /api/activity`.
- **Clear filters** button appears in the Activity header when any filter (rule, severity, type) is active.
- **Export JSON** button in the Activity footer exports currently loaded/filtered events as a JSON file. Filename format: `portier-activity-YYYYMMDD-HHMMSS.json`. Payload includes `exportedAt`, `filters`, and `events`. Client-side only; no backend endpoint.
- **Clear Log** button in the Activity footer calls `DELETE /api/activity` and empties the displayed list.
- Packet event **throttle note**: small explanatory text in the Activity body — "High-frequency packet events may be summarized or throttled; counters remain exact in rule status."
- `DELETE /api/activity` endpoint on both the TypeScript server and Go service: clears the in-memory activity log and returns `204 No Content`. Does not affect rules or forwarding state.
- `clearActivity()` API helper added to `client/sources/api/portierApi.ts`.
- `Store.Clear()` method added to `service/sources/activity/store.go` (Go).
- `Manager.ClearActivity()` method added to `service/sources/manager/manager.go` (Go).
- `docs/api-contract.md` updated with `DELETE /api/activity` section.
- Client in-app API Docs view updated with `DELETE /api/activity` entry.
- 18 new `ActivityLogView` component tests (21 total); 4 new `ForwardRuleList` activity navigation tests; 2 new `ApiDocsView` tests.
- 3 new TypeScript server API tests for `DELETE /api/activity`; 3 new Go API tests.
- `validate:contract` updated with `DELETE /api/activity` scenario and post-clear empty-array verification.

### Added (v1.2 Slice 3 — rule diagnostics UI)

- **Diagnose** action button (stethoscope icon) added to every rule row in the Forward Rules table.
- Clicking Diagnose calls `POST /api/forwards/:id/diagnose` and shows a collapsible diagnostics panel inline below the rule row — no navigation required.
- Diagnostics panel states: loading spinner while in flight, pass/warn/fail summary with timestamp, per-check rows with pass/warn/fail/skip status, and error display for API failures.
- Duplicate Diagnose clicks are prevented while a request is in flight for that rule; other rules remain clickable.
- Re-running Diagnose replaces the previous result immediately (pending → result).
- Closing the panel (✕ button) clears the result; deleting a rule also clears its diagnostic state.
- `diagnoseForwardRule(ruleId)` API helper added to `client/sources/api/portierApi.ts`.
- `DiagnosisEntry` union type exported from `ForwardRuleList.tsx` for App-level state management.
- `RuleDiagnosticsPanel` component added at `client/sources/features/forwards/RuleDiagnosticsPanel.tsx`.
- CSS for diagnostics panel added to `client/sources/styles/styles.css`.
- 11 new `RuleDiagnosticsPanel` unit tests; 9 new `ForwardRuleList` diagnostics tests; 2 new `App` integration tests (133 total client tests).

### Added (v1.2 Slice 2 — rule diagnostics API)

- `POST /api/forwards/:id/diagnose` endpoint on both the TypeScript server and Go service: runs diagnostic checks against an existing forward rule without changing rule state or opening long-lived sockets.
- Response shape: `RuleDiagnosticsResult` — ruleId, ruleName, protocol, summary (`pass`/`warn`/`fail`), checks array, diagnosedAt timestamp.
- Checks implemented: `listen-host`, `lan-exposure`, `privileged-port`, `common-port`, `listen-bind`, `target-host`, `target-connect`, `udp-mode` (UDP only).
- `listen-bind` skips the bind attempt when the rule is running and returns `pass` — Portier owns the socket.
- `target-connect` is always `skip` for UDP rules (UDP reachability cannot be verified without a protocol-specific response).
- `udp-mode` warns for `bidirectional-last-client`; passes for `one-way` and `bidirectional-multi-client`.
- `DiagnosticStatus`, `DiagnosticCheck`, `DiagnosticSummary`, `RuleDiagnosticsResult` types exported from `@portier/shared`.
- `getRule(id)` method added to `ForwardManager` (TypeScript server).
- `diagnoseRule()` helper added to `server/sources/diagnose.ts` (TypeScript) and `service/sources/api/diagnose.go` (Go).
- `DiagnosticCheck`, `DiagnosticSummary`, `RuleDiagnosticsResult` types added to `service/sources/domain/domain.go`.
- `validate:contract` updated with diagnose scenarios: unknown rule 404, TCP reachable target, UDP target-connect skip, LAN exposure warning, response shape verification.
- TypeScript tests and Go tests added for all check scenarios.

### Added (v1.2 Slice 1 — runtime info)

- `GET /api/runtime` endpoint on both the TypeScript server and Go service: returns name, version, runtime (`"node"`/`"go"`), platform, arch, uptimeSeconds, startedAt, managementHost/Port, configPath, staticDir, serviceMode, and pid.
- `RuntimeInfo` type exported from `@portier/shared`.
- `fetchRuntimeInfo()` API helper in the client.
- Settings view — **Runtime / Environment** section: shows live runtime info (runtime, version, platform/arch, uptime, management URL, config path, static dir, service mode, PID). Gracefully shows "Runtime information is unavailable from this backend." for older backends.
- Go service version package (`service/sources/version/version.go`): default `"dev"`, injectable at build time via `-ldflags`.
- `validate:contract` updated to verify GET /api/runtime shape and runtime field for both runtimes.

### Added (post-v1.1, pre-v1.2)

- Config compatibility fixtures (`tests/fixtures/config/`): 8 valid and 8 invalid `rules.json` fixtures covering TCP, all UDP modes, both config shapes (raw array and Go wrapper), and all field-level error categories.
- `npm run validate:config` — fixture-based compatibility runner (`scripts/validate-config.js`): validates config load, HTTP API import/export, UDP mode defaults, duplicate binding rejection, and invalid fixture rejection against the TypeScript runtime and the Go service when binary is available. Never reads or writes the user's real `rules.json`.

---

## [1.1.0] - 2026-06-06

### Goal

Portier v1.1 makes Portier easy and safe to install as a native background service on Windows, macOS, and Linux, with automated package/service validation and clean release artifacts.

See `docs/installer.md` for scope, platform decisions, and implementation slices.

### Added

- Windows Inno Setup installer (`scripts/windows/release/portier.iss`): installs to `%ProgramFiles%\Portier\`, optional Windows Service registration with auto-start at boot, config at `%ProgramData%\Portier\rules.json`. Upgrade support: stops running service before overwriting binaries. Uninstall removes service registration and logs; preserves `rules.json` by default.
- `build-release.ps1` (`scripts/windows/release/`): reads version from `package.json`, runs `build:runtime`, calls ISCC.exe, produces `build/releases/windows/Portier-Setup-<version>.exe`.
- macOS install scripts updated: `install-launch-agent.sh` now auto-copies `build/portier/` to `~/Applications/Portier/` by default; adds `--source-dir`, `--no-start`, and `--runtime service|node` options; fixes label consistency bug (`com.portier.port-forwarding` everywhere).
- `uninstall-launch-agent.sh` adds `--purge` flag for removing config and logs (config is preserved by default).
- `scripts/macos/release/build-release.sh` — builds `build/releases/macos/portier-portable-macos-<version>.tar.gz` from `build/portier/`.
- Signing and notarization documented in `scripts/macos/readme.md` (unsigned local builds supported; Developer ID signing documented as required for public distribution).
- Linux `install-service.sh` updated: auto-copies `build/portier/` to `/opt/portier/` by default; adds `--source-dir`, `--no-enable`, `--no-start`, and `--runtime service|node` options.
- `scripts/linux/release/build-release.sh` — builds `build/releases/linux/portier-<version>-linux.tar.gz` from `build/portier/`.
- `scripts/linux/readme.md` updated: install flags table, release archive section, firewall notes, journald commands, `--no-enable` documented.
- `scripts/windows/service/validate-user-install.ps1`: validates user-scope scheduled task flow with test name `PortierTestUser`, isolated temp dirs, auto-port detection, `-NoBuild`/`-KeepFiles`/`-Port` flags; never touches production.
- `scripts/windows/service/validate-machine-service.ps1`: validates machine-scope Windows Service flow with test name `PortierTestMachine`; requires Administrator; same flags; never touches production.
- `scripts/macos/service/validate-launch-agent.sh`: validates LaunchAgent flow with test label `com.portier.test`, temp plist at `~/Library/LaunchAgents/com.portier.test.plist`; no sudo required; `--no-build`/`--keep-files`/`--port` flags.
- `scripts/linux/service/validate-systemd-service.sh`: validates systemd flow with test unit `portier-test.service`, temp paths under `/tmp/portier-test-<pid>/`; requires root; fails clearly if not root or systemd unavailable.
- `scripts/validate-service.js`: cross-platform dispatcher — Windows runs user-scope, macOS runs LaunchAgent, Linux runs systemd; fails clearly on unsupported platforms.
- `npm run validate:service:current` / `validate:service:windows:user` / `validate:service:windows:machine` / `validate:service:macos` / `validate:service:linux` — explicit release validation commands.

- `scripts/build-release.js`: unified release packaging script for all platforms. Reads version from `package.json`, calls `build:runtime`, produces portable archives (Windows `.zip`, macOS/Linux `.tar.gz`) and Windows installer (non-fatal if Inno Setup absent). Service binaries are platform-native; run on each target OS.
- `scripts/validate-release.js`: validates `build/releases/<platform>/` layout, archive contents (required/forbidden files, readme.txt content), and optional installer artifact.
- Updated `readme.txt` in Windows/macOS/Linux build scripts: now includes portable archive notice ("does not install OS services"), `--config` / `--static-dir web` options, "not bundled in this archive" note for config.
- `npm run build:release` / `build:release:current` — full release packaging for current platform (portable + installer if available).
- `npm run build:release:portable` — portable archive only, skip installer.
- `npm run validate:release` / `validate:release:current` — validate release artifacts for current platform.
- `npm run validate:release:portable` — validate portable archive only.

### Deferred

- macOS `.pkg` installer via `pkgbuild`/`productbuild` (requires macOS tooling; deferred to future release).
- Linux `.deb` / `.rpm` packages (deferred beyond v1.1).

---

## [1.0.0] - 2026-06-06

### Added

- Native Go service runtime in `service/`, preferred for production packages while the TypeScript server in `server/` remains supported as the reference and fallback runtime.
- TCP forwarding in both runtimes with real listeners, bidirectional piping, status counters, and lifecycle cleanup.
- UDP forwarding in both runtimes across all supported modes: `one-way`, `bidirectional-last-client`, and `bidirectional-multi-client`.
- Rule CRUD, start/stop, update/restart behavior, duplicate listen binding rejection, persistent config, config import/export, and rule reorder.
- Activity Log with bounded in-memory events for rule lifecycle, TCP connections, UDP packets, UDP multi-client sessions, config export, and config import.
- Dark React web UI with Dashboard, Forward Rules, Activity, Settings, API Docs, mobile sidebar, add/edit/delete drawer flows, error banners, LAN exposure warnings, and common/recommended port advisories.
- Playwright E2E coverage for app load, add/edit/delete flows, start/stop, settings import, mobile navigation, and real TCP/UDP forwarding.
- Go service tests for config, manager behavior, API routes, TCP forwarding, UDP modes, activity events, validation, options, and advisories.
- Windows, macOS, and Linux packaging/install docs and scripts, including Go service mode and Node/TypeScript server fallback where applicable.

### Changed

- Repository convention standardized on `sources/` for source directories, `build/` for generated output, and `scripts/` for executable automation; platform docs and templates are co-located under `scripts/{platform}/`.
- Production package layout standardized to a flat install directory:

```text
<install-dir>/
  service          (or service.exe on Windows)
  server.js        (Node fallback, requires Node.js)
  web/
    index.html
    assets/
```

- Native Go service static directory defaults to `web` for packaged layout. Repository development commands may still pass `../client/build`.
- TypeScript server remains supported and can serve either repository `client/build` or packaged `web` via `--static-dir` / `PORTIER_STATIC_DIR`.
- Runtime config remains external in all modes; `rules.json` is not baked into binaries or packages.
- Manual QA scope narrowed to platform/package/service install behavior because core TCP/UDP correctness is automated.

### Validation

- Root validation scripts cover lint, typecheck, unit/integration tests, Go tests, build, Playwright E2E, package smoke checks, and repository hygiene checks.
- Real protocol coverage is automated for TCP, UDP one-way, UDP bidirectional-last-client, and UDP bidirectional-multi-client.

---

## [0.1.0] - 2026-06-04

First usable internal release of Portier, a local TCP/UDP port forwarding manager for development and LAN testing.

### Added

- TypeScript server with REST API on `127.0.0.1:47831` by default.
- TCP forwarding, UDP one-way forwarding, and UDP bidirectional-last-client forwarding.
- React management UI for listing, creating, editing, starting, stopping, and deleting forward rules.
- Shared TypeScript validation, common port advisories, recommended forward port range, and LAN exposure warnings.
- External JSON config path via `--config` / `PORTIER_CONFIG`.
- Static web serving via `--static-dir` / `PORTIER_STATIC_DIR`.
