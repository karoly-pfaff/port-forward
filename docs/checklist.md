# QA Checklist

## Automated Release Validation

Run these before tagging:

- [ ] `npm run lint`
- [ ] `npm run typecheck`
- [ ] `npm run test`
- [ ] `npm run build`
- [ ] `npm run build:client`
- [ ] `npm run test:e2e`
- [ ] `npm run check`
- [ ] `go test ./...` from `service/`
- [ ] `go build ./...` from `service/`
- [ ] `npm run build:service`
- [ ] `npm run validate:runtime:smoke` — builds `build/portier/`, validates layout, runs smoke test

## Additional Validation Suites

Run explicitly — not part of `npm run check`. Slower or platform-sensitive.

- [ ] `npm run validate:config` — Config compatibility: loads every fixture from `tests/fixtures/config/`, verifies valid fixtures load and import correctly, invalid fixtures are rejected with appropriate errors, duplicate bindings are caught, UDP mode defaults are applied, and the export shape is stable. TypeScript runtime always checked; Go runtime checked when binary is available. (`scripts/validate-config.js`)
- [ ] `npm run validate:contract` — API contract parity: runs all API scenarios against TypeScript server; if Go binary available, runs same scenarios against Go service and compares response shapes, status codes, field names, and error shapes. Includes the `rule-group` scenario group (v1.8): optional rule `group` metadata round-trips through create/PATCH/clear/export-import/plan-apply, invalid groups (too long / control chars) are rejected, ungrouped/legacy rules omit the field, and a group-only plan change is a non-destructive `update` (cross-runtime `plan:GROUP_CHANGE` parity). Also the `group-action` group (v1.8 Slice 4): `POST /api/forwards/groups/:group/{start,stop}` summary shape, rule-order results, already_running/not_running skips, 404 no-match, 400 invalid group, metadata preserved, other-group/ungrouped untouched (cross-runtime `group:start` parity). (`scripts/validate-contract.js`)
- [ ] `npm run validate:binary` (or `validate:runtime:behavior`) — Runtime binary behavior: starts `build/portier/service[.exe]`, verifies health, static serving, missing-static-dir handling, invalid-config failure, and clean shutdown. Runs `build:runtime` first unless `--no-build` is passed. (`scripts/validate-binary.js`)
- [ ] `npm run validate:scripts` — Installer script analysis: static analysis of all platform install and validation scripts (no firewall commands, test-specific names in validate scripts, production path defaults in install scripts, quoting correctness); plus dry-run execution on the current platform. (`scripts/validate-scripts.js`)

## Explicit OS Service Install Validation

These commands must be run explicitly on the target platform before distribution. They are not run by `npm run check`.

Each script uses test-specific service names, ports, and temp directories. Production Portier installs and config are never touched.

- [ ] `npm run validate:service:windows:user` — Windows scheduled task (no Administrator required)
- [ ] `npm run validate:service:windows:machine` — Windows Service (Administrator required)
- [ ] `npm run validate:service:macos` — macOS LaunchAgent (no sudo required)
- [ ] `npm run validate:service:linux` — Linux systemd unit (root/sudo required)

Each script validates:
1. Package copy to isolated temp install dir
2. Service/task/agent registration with test-specific name
3. Service start
4. `/api/health` responds 200
5. Web UI HTML served at `/`
6. Service stop
7. Service/task/agent unregistered and verified removed
8. Temp files cleaned up

Flags supported by all scripts:
- `--no-build` / `-NoBuild` — skip `npm run build:runtime`, use existing `build/portier/`
- `--keep-files` / `-KeepFiles` — preserve temp directories on failure for debugging
- `--port` / `-Port` — override the management port (default: auto-detect free port)

## Coverage Tooling Notes

- [x] **v1.6 Fix Slice 8 — Portier terminology glossary (Naming-A)** — Docs-only; no code/API/test/gate change, no renames. Added `docs/glossary.md` (canonical terms: forward rule, config plan/apply/import/export, desired/exported config, drift, runtime/server/service/`node`/`go`, enabled vs running, activity event/type/severity, advisory vs warning, diagnose vs diagnostics export, live connection vs UDP session) with the frozen public-API exceptions documented (`/api/forwards`, `listenHost`/`targetHost`, `clientAddress`/`targetAddress`, CLI commands). Pointers added from `CLAUDE.md`, `AGENTS.md`, and `docs/api-contract.md`; glossary text kept in one place. **New docs/API/CLI/UI wording should follow `docs/glossary.md`, or update the glossary when introducing a genuinely new public term.** CLI file naming (Naming-C) and UI wording (Naming-E) deferred. Source: `audits/v1.6-readability-naming-audit-1.md`. Next: v1.6 release-readiness checkpoint.
- [x] **v1.6 Fix Slice 7 — Config apply `importConfig` result.errors invariant (Error-C·2/Resilience-C)** — Apply can never report `ok:true` when the underlying import reports errors. Both handlers discarded the `ImportResult`; audit confirmed no `result.errors` path is currently reachable from apply (validation pre-validated, duplicate bindings pre-blocked by `detectDuplicateKeys`→`hasErrors`, persist throws→500, merge-only N/A in replace), but the discarded result left the invariant unenforced. Defensive fix in `server/sources/api.ts` + `service/sources/api/api.go`: after import, if `result.errors` non-empty → `200 ok:false` with errors surfaced via existing `plan.errors` (`IMPORT_ERROR`) + `hasErrors:true` + zero counts (no new response field). Go construction extracted to unit-testable `applyImportErrorResponse` (handler branch structurally unreachable without the deferred manager-interface refactor; documented). Tests: TS duplicate-binding pre-block + defensive branch via `ImportErrorManager` subclass; Go duplicate-binding pre-block + `TestApplyImportErrorResponse`. `validate:contract` 183 → 185 (duplicate-binding apply parity, both runtimes). CLI unaffected (`applyExitCode` already maps `ok:false`→exit 1). No API-shape/valid-behavior change; server branch 94.0→94.1, service 90.3→90.2, gates unchanged; full test 357→359. Durable rule in `CLAUDE.md`/`AGENTS.md`. Source: error-flow/resilience audits. Next: Fix Slice 8 — Naming-A glossary.
- [x] **v1.6 Fix Slice 6 — TS `ForwardManager.emitRuleEvent` helper (Dup-C2/Naming-B/Error-B; Arch-C2 symmetry)** — Behavior-preserving refactor; payloads byte-identical, no API/type/severity/message/order/count change. Added private `emitRuleEvent(type, severity, rule, message)` to `server/sources/forward-manager.ts` (mirrors Go `manager.emitRuleEvent`; populates ruleId/ruleName/protocol; keeps `activity?.add` optional-chaining; ActivityStore still owns id/timestamp) and replaced the 6 inline rule-scoped blocks (created/updated/deleted/started/stopped/error). The 5 config-level events (`config.exported`/`config.imported`/3× `config.import.failed`) stay on direct `activity?.add` (not rule-scoped, carry `details`), mirroring Go. Added 3 full-payload regression tests (create/update/delete; start/stop via `startRuleStable`; start-failure rule.error) asserting exact type/severity/ruleId/ruleName/protocol/message. server 98.9/93.8/100 → 98.9/94.0/100, **gates unchanged**; `validate:contract` 183/183; full test 354 → 357. Durable rule in `CLAUDE.md`/`AGENTS.md`. Source: duplication/readability/error-flow audits. Next: Fix Slice 7 — config apply result.errors general case.
- [x] **v1.6 Fix Slice 5 — Activity event-type/severity value parity guard (Error-A/Resilience-E/Dup-G)** — Contract/script hardening only; no product/API-shape/gate change. Added canonical `EXPECTED_ACTIVITY_TYPES` (17) / `EXPECTED_ACTIVITY_SEVERITIES` (4) to `scripts/validate-contract.js` + three guard layers: (1) once-run source-of-truth parity (`runActivityValueSetParityChecks`) reads the TS union + Go consts from source (binary-independent) and asserts both declared sets equal the canonical set — drift fails with `extra`/`missing` diff (negative-tested `rule.error→rule.failure`); (2) per-runtime membership (every emitted type/severity ∈ allowed set) in `runActivityScenarios`; (3) per-runtime representative emitted values — `rule.created/updated/started/stopped` (success/info/success/info) and the import error path `config.imported/success` + `config.import.failed/error`. No assertions weakened, no event names changed. **`validate:contract` 171 → 183** (intentional: +4 once, +6 membership/representative ×2, +2 config ×2; stable ×2 runs); `\x00` separators preserved. `docs/api-contract.md` lists the value sets + parity rule. Lint/typecheck/coverage/test green. Durable rule in `CLAUDE.md`/`AGENTS.md`. Source: error-flow/resilience/duplication audits. Next: Fix Slice 6 — TS ForwardManager emitRuleEvent helper.
- [x] **v1.6 Fix Slice 4 — `validate-contract.js` scenario registry (Complexity-A/SOLID-A/Pattern-A/Dup-A/Naming-D)** — Script-only refactor; no product/API/contract/gate change, `validate:contract` 171/171 (run twice). Split the ~1090-line `runScenarios` into 12 named group functions (`runRuntimeScenarios`, `runForwardsScenarios`, `runForwardLifecycleScenarios`, `runActivityScenarios`, `runConfigExportImportScenarios`, `runPortAdvisoryScenarios`, `runForwardDeleteScenarios`, `runConnectionsScenarios`, `runConfigPlanScenarios`, `runConfigApplyScenarios`, `runErrorEnvelopeScenarios`, `runDiagnoseScenarios`) driven by an ordered `scenarioGroups` registry; each takes a small `ctx` (`{api, runtime, fixtureDir, state}`); cross-group rule ids flow via `ctx.state` (local `exportedConfig` stays local). Added `expectedRuntimeApiValue(label)` helper for the runtime-label→API-value mapping (Naming-D). Assertions moved verbatim (byte-level transform preserving the intentional `\x00` sort-key separators); parity comparison + CLI DTO guard + child cleanup untouched (optional outer-guard hardening deferred as Resilience-F). Lint/typecheck/coverage/full-test all green. Durable rule in `CLAUDE.md`/`AGENTS.md`. Source: complexity/SOLID/pattern/duplication/readability audits. Next: Fix Slice 5 — activity event-type/severity value parity guard.
- [x] **v1.6 Fix Slice 3 — Finish Test-A EADDRINUSE residual helper migration (test determinism)** — Tests/test-infra only; no product/API/contract/gate change. New `server/sources/test-helpers.ts` helpers: `startRuleStable` (mirrors Go `portretry_test.go` — retries `manager.startRule` only on EADDRINUSE by rebinding `listenPort` via `updateRule`), `startTcpServerOnFreePort`/`bindUdpSocketOnFreePort` (bind port 0, return the live listener/socket + actual port). Migrated `udp-forwarder.test.ts` (listen binds → `startForwarderOnFreePort` via local `startUdpForwarder`; UDP targets → `bindUdpSocketOnFreePort`; some fixed sleeps → `waitUntil`) and `forward-manager.test.ts` (manager `startRule` incl. Test-D rollback tests → `startRuleStable`; TCP targets → `startTcpServerOnFreePort`; removed local `startTcpTarget`). Intentional-conflict tests kept outside the helpers (UDP blocker, manager occupier, race-tolerant merge rollback). 58 tests pass ×3; server 354 tests, 98.9/93.8/100 unchanged; `validate:contract` 171/171, `validate:config` 71/71, all gates unchanged. Accepted bounded residuals (documented): HTTP `/start` sites in `api.test.ts`/`api_test.go` (bind behind handler → needs an HTTP-level recreate-on-EADDRINUSE helper, larger follow-up) and E2E `port.ts` (serial `workers:1` → no intra-process race). Durable rule in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-audit-synthesis-and-fix-plan.md`. Next: validate-contract.js scenario registry (Complexity-A/SOLID-A/Pattern-A/Dup-A/Naming-D).
- [x] **v1.6 Fix Slice 2 — TS `ConfigStore` atomic write parity with Go (durability)** — Closed Resilience-A. `server/sources/config-store.ts` `save` now writes crash-safely: unique same-directory temp file → `fsync` via FileHandle → atomic `rename` over `rules.json` (mirrors Go `config.Store.Save`). Previous file intact until rename; temp removed on any pre-rename failure (best-effort, never masks the original error); persistence error propagates so `ForwardManager` rollback (Test-D) runs. `load` unchanged; serialized output byte-identical. No remove-and-retry recovery branch (Node `fs.rename` replaces atomically on POSIX + Windows `MoveFileExW REPLACE_EXISTING`); directory fsync omitted (Windows portability — same limit Go accepts). Minimal test-only `ConfigStoreFileOps` seam (optional 2nd constructor arg, defaults to real fs; not in any product/API path) forces write/sync/rename failures. 5 new tests (success leaves no temp; write-fail keeps existing file byte-for-byte + temp cleaned; sync-fail cleanup; rename-fail keeps target + removes temp; first-save-fail creates no target). `config-store.ts` 100/100/100; server 98.9/93.6/100 → 98.9/93.8/100; **all gates unchanged**. `config-store.test.ts` ×4 stable; `validate:config` 71/71; `validate:contract` 171/171. Durable rule in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-resilience-audit-1.md`. Next: finish Test-A EADDRINUSE residual helper migration.
- [x] **v1.6 Fix Slice 1 — TS replace-import duplicate listen-binding parity (real parity bug)** — Closed the one MUST-do correctness/parity divergence (Error-flow #1 / Resilience-B). Go rejected duplicate listen bindings within the imported set for both modes; TS replace mode had no guard and merge only checked imported-vs-existing. Product fix in `server/sources/forward-manager.ts`: module-level `ensureNoDuplicateBindings(rules)` (mirrors Go wording) runs in `importConfig` after validation, before any mutation, for both modes → `{imported:0, errors:[msg]}` + one `config.import.failed` (error), no mutation/persist/start/stop. HTTP maps `result.errors` → 422 `{errors, result}` (both runtimes, unchanged). 3 TS unit tests + `validate:contract` parity scenario (167 → 171). Config **apply** verified already-protected (plan engine `detectDuplicateKeys` → `hasErrors` → `ok:false` before `importConfig`) — resolves the duplicate-binding half of Resilience-C. No valid-import / merge-vs-existing / gate change. Durable rule in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-audit-synthesis-and-fix-plan.md`. Next: Resilience-A (TS ConfigStore atomic write parity).
- [x] **v1.6 Testing Slice Test-E — Client/E2E meaningful coverage (32 → 34 E2E, brittle selectors cleaned)** — Tests-only; no product code changed, client coverage unchanged (95.2/90.2/80.3), `validate:contract` 167/167. Added `connections.spec.ts` (I) populated TCP table (real held connection → populated row, empty state gone) and (J) API-failure `role="alert"` banner (Playwright `page.route` abort). Removed brittle `label.auto-refresh-toggle` (→ aria-label attribute + visible-label click) and `.diag-panel-title`/`.diag-panel-body` (→ accessible "Close diagnostics" button + "Listen address" check). Full E2E 34/34, run twice (real socket + auto-refresh polling, no fixed sleeps). Remaining E2E gaps (UDP populated table deferred, duplicate-binding error, a11y/keyboard smoke). Source: `audits/v1.6-testing-audit-1.md`. Next: short v1.6 readiness audit/checkpoint (summarize hardening slices, verify gates, list accepted gaps, decide if more coverage slices add value before release).
- [x] **v1.6 Coverage Slice E — TypeScript server error paths (95.4/92.2/100 → 98.9/93.6/100, gates 89/91/99 → 95/92/99)** — Tests-only; no product code changed, `validate:contract` 167/167. `diagnose.test.ts` (→98.0%): TCP bind-fail, target-connect success/refused, DNS-fail→skip, one-way mode, 0.0.0.0/privileged/common-port advisory warns. `udp-forwarder.test.ts` (→99.4%): four send-callback error branches (one-way/multi-client send, last-client/multi-client return) via clean instance-level `.send` injection + `waitUntil` deadline polling. `api.test.ts`: apply-drift persist-failure → 500 (Go-parity, inline failing `RuleStore`). Three consecutive `coverage:server` runs stable; gates raised statements 89→95, branches 91→92 (functions 99). Remaining blockers documented (2 s diagnose timeouts, UDP post-stop/pre-client race guards, tcp optional-registry guard, api.ts platform-normalize). Source: `audits/v1.6-coverage-audit-1.md`, `audits/v1.6-testing-audit-1.md`. Next: client/E2E thinness slice (Live Connections populated-table E2E, offline/error-banner E2E, brittle selector cleanup).
- [x] **v1.6 Coverage Slice D — Go service error paths (88.5% → 90.3%, gate 88 → 90)** — Tests-only error/correctness-path coverage; no product code changed, `validate:contract` 167/167. New `api/errorpaths_test.go` (body-read/malformed → 400, unknown rule → 404, generic manager error → 500, config-apply import failure → 500, `NewHandler` defaults, `tryTCPBind`/`tryUDPBind`), `forwarders/udp_errorpaths_test.go` (`NewUDPForwarder` + `udpMode` nil-default), `static/static_test.go` (`ServeClient`/`HasClient`), `manager/errorpaths_test.go` (`StartEnabled` skip-disabled, `hasIDIn` merge collision), `configplan/udpmode_test.go` (`udpModeEqual`/`udpModeVal`), and an `options` positional-arg test. Persist-failure 500s use a `manager.NewWithStore` bad-path store (no mocking, cross-platform). Two consecutive `coverage:service` runs both 90.3%; modified packages stable at `-count=3`; gate raised 88 → 90. Remaining blockers documented (crypto/rand fallbacks, `config.Save` injection, UDP read-loop send errors — all need seams not worth adding). Source: `audits/v1.6-coverage-audit-1.md`. Next: server high-value gaps (UDP forwarder send-error callbacks, diagnose timeout/error paths).
- [x] **v1.6 Coverage Slice C — CLI command edge cases (93.2% → 97.7%, gate 93 → 95)** — Raised CLI coverage past the audit's 95% target with failure-path/exit-code tests only; no CLI behavior change, CLI stays a pure API client. Added: `commands/json_err_test.go` (a shared `failingWriter` covering the `output.PrintJSON → "Error encoding JSON" → exit 1` branch in all 14 JSON-emitting commands), `commands/helpers_internal_test.go` (white-box `formatChangeValue` + `opEndpoint` branch tests), and `main_test.go` `TestRun_InvalidURL_AllDispatches` (invalid `--url` → exit 2 for every subcommand). Gate raised 93 → 95 in `scripts/validate-coverage.js` (Go cross-package number is deterministic; repeated `coverage:cli` runs both report 97.7%, 2.7% buffer). Structurally-unreachable branches documented (not chased): `main()` os.Exit, `client.do`/`doWithBody` request/marshal errors, `writePrettyJSON` marshal error. `validate:contract` 167/167 unchanged (no production code touched). Source: `audits/v1.6-coverage-audit-1.md`. Next: service/server high-value gaps (service manager/forwarder/API error paths; server UDP send-error + diagnose timeout paths).
- [x] **v1.6 Testing Slice Test-D — TypeScript ForwardManager persist-failure rollback parity** — Closed the HIGH parity gap from `audits/v1.6-testing-audit-1.md`. New `ControllableStore`-driven rollback tests in `server/sources/forward-manager.test.ts` exposed a **real bug**: the TS `ForwardManager` mutated its in-memory rule map before `persist()` with no rollback (Go rolls back), so a failed `store.save()` left in-memory rules inconsistent with `rules.json`. Minimal product fix in `server/sources/forward-manager.ts` mirroring Go: `addRule`/`updateRule`/`deleteRule`/`reorderRules`/`importConfig` restore prior state on persist failure (and best-effort restart a forwarder stopped for a forwarding-field update). 8 new tests mirror the Go `Test*PersistFailureRollsBack` scenarios plus two TS-side extensions (delete-running, import rollback). `forward-manager.ts` 99.4%/88.7% → 99.5%/90.4% stmts/branch, 100% funcs; server gate 89/91/99 unchanged (95.4%/92.2%/100% actual). No API/forwarding/plan-apply change; rollback only triggers on disk-write failure. `vitest forward-manager.test.ts` ×3 stable; `validate:coverage` + `validate:contract` (167/167) pass. Durable rule in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-testing-audit-1.md`. Next: resume coverage push (CLI command edge cases → 95%+, then service/server high-value gaps from `audits/v1.6-coverage-audit-1.md`).
- [x] **v1.6 Testing Slice Test-A — Stabilized test port allocation (EADDRINUSE/TOCTOU)** — Removed the allocate→close→rebind flake flagged in `audits/v1.6-testing-audit-1.md`. Bounded bind-retry helpers (Pattern C — retry the bind step only, only on EADDRINUSE, never whole tests/suites): `isAddrInUse` + `startTCPForwarderOnFreePort`/`startUDPForwarderOnFreePort` (`service/sources/forwarders/portretry_test.go`), `startRuleStable` (`service/sources/manager/portretry_test.go`, rebinds via `UpdateRule`), `startForwarderOnFreePort` (`server/sources/test-helpers.ts`). Migrated all happy-path binds in Go `tcp_test.go`/`udp_test.go`/`manager_test.go` and TS `tcp-forwarder.test.ts` (intentional bind-failure tests left untouched). `scripts/validate-contract.js` `startServer` retries the child bind on a fresh port; `waitForReady` bails on child exit. Removed the 20 ms ordering `time.Sleep` in `udp_test.go` → deadline poll. Verified with `go test ./... -count=3` (forwarders/manager) and vitest forwarder suites; full `npm run validate:coverage` run twice consecutively. Test-only; no coverage numbers or gates change. Residual follow-ups (tracked): TS `udp-forwarder.test.ts`, the api HTTP `/start` sites (`api_test.go`/`api.test.ts`), E2E `port.ts` (serial `workers:1` → no intra-suite race). Durable testing rule in `CLAUDE.md`/`AGENTS.md`. Source: `audits/v1.6-testing-audit-1.md`. Next: Test-D (TS persist-failure rollback parity tests matching the Go manager).
- [x] **v1.6 Architecture Slice Arch-D — CLI DTO live-runtime parity guard** — `validate:contract` now captures live JSON from both runtimes (runtime/forwards/status/activity/export/plan/apply-dry-run/advisory/diagnose) and strictly decodes them (`DisallowUnknownFields`) into the CLI DTOs via `TestCLIDTOContractParity` (`tools/cli/sources/client/contract_decode_test.go`). Proves the CLI's third contract copy stays in sync with real runtime output, not just `httptest` mocks. Env-gated (`PORTIER_CLI_CONTRACT_FIXTURES`), so `test:cli`/`validate:cli` skip it; CLI stays a pure API client (no service imports). `validate:contract` 167 passed; no coverage/gate change. `/api/connections` out of scope (no CLI DTO). Durable rule: CLI DTOs guarded against live responses; new API response families must update both the capture and the decode guard. Arch-D completes the audit's contract-parity remediation (Arch-A/B/C1/C2/D done). Source: `audits/v1.6-architecture-audit-1.md`. Next: resume coverage push (CLI command edge cases → 95%+, then service/server high-value gaps from `audits/v1.6-coverage-audit-1.md`).
- [x] **v1.6 Architecture Slice Arch-C2 — Collapsed Go manager activity-emission blocks** — Replaced the 10 repeated rule-scoped emission blocks in `manager.go` with one `emitRuleEvent(eventType, severity, rule, message)` helper (create/update/delete/start/stop/error). Payloads preserved byte-for-byte. The 5 config-level emissions (export/import/import-failed) carry a `details` map and no rule fields, so they stay on `emitActivity` directly (documented). Added full-payload regression tests (`TestCreateRuleActivityPayload`, `TestUpdateRuleActivityPayload`, `TestDeleteRuleActivityPayload`, `TestStartStopRuleActivityPayload`, `TestFailedStartActivityPayload`) + `assertRuleEventPayload` asserting type/severity/ruleId/ruleName/protocol/message and no-details. `validate:contract` 166/166; service 88.6% → 88.5% (covered duplicate lines removed; still above 88% gate, no gate change). Durable rule: rule-scoped manager activity events go through `emitRuleEvent`; activity payload changes are user-visible diagnostics needing payload-level tests. Source: `audits/v1.6-architecture-audit-1.md`. Arch-C complete (C1 + C2). Next: Arch-D (CLI DTO parity guard) or resume coverage push (CLI command edge cases).
- [x] **v1.6 Architecture Slice Arch-C1 — Unified Go rule ID generation + dead-code removal** — Replaced duplicate UUID generators `manager.randomID` and `api.newApplyRuleID` with one shared `domain.NewRuleID()` (`service/sources/domain/id.go`); canonical impl keeps the timestamp fallback the manager already had (api path previously ignored RNG errors). Call sites: `CreateRule`, `ImportConfig` merge, and `configApply` (via `BuildApplyImportFromPlan`'s injectable id seam from Arch-B). Removed unused `crypto/rand`/`encoding/hex` imports and the no-op `name` assignment in `buildRuleLiveSummary`. Added `domain/id_test.go` (non-empty, UUID-v4 format, uniqueness). Behavior preserved exactly; `validate:contract` 166/166; coverage neutral (service 88.6%); no gate change. Durable rule: Go service rule ID generation must have one shared implementation so manager-created and apply-created IDs cannot drift in format. Source: `audits/v1.6-architecture-audit-1.md`. Next: Arch-C2 (collapse repeated Go manager activity-emission blocks into one helper, preserving event payloads exactly).
- [x] **v1.6 Architecture Slice Arch-B — Config apply orchestration extracted** — Apply transformation moved from the HTTP handlers into tested plan-engine helpers: `buildApplyImportFromPlan` (`server/sources/config-plan.ts`) and `BuildApplyImportFromPlan` (`service/sources/configplan/plan.go`). Helpers are pure (derive replace rule list + applied counts; inject/preserve ids; no manager/import, no mutation, no file/socket I/O); id generator injectable for tests, defaults to existing per-runtime generator (no new UUID impl). Handlers keep only request/response/gating concerns. 10 TS + 8 Go helper unit tests added. Behavior preserved exactly; `validate:contract` 166/166; server coverage 95.2/91.6 → 95.3/92.0; service unchanged 88.6%; no gate change. Durable rule: apply orchestration lives beside the plan engine, not in HTTP handlers; TS and Go helpers stay semantically mirrored; `validate:contract` is the parity guard. Source: `audits/v1.6-architecture-audit-1.md`. Next: Arch-C (Go service safe dedupe — unify UUID generation, collapse repeated manager activity-emission blocks).
- [x] **v1.6 Architecture Slice Arch-A — Contract drift guard** — Aligned the Go service `LAN_EXPOSURE` advisory message (`service/sources/advisory/advisory.go`) to the canonical TypeScript wording in `@portier/shared`; code/severity unchanged. Strengthened `scripts/validate-contract.js` with in-runtime canonical advisory/plan-warning assertions plus a cross-runtime `compareParity` pass that diffs normalized advisory and config-plan payloads field-by-field (timestamps/ids excluded). Added `TestLANExposureCanonicalContent` / `TestManagementLANExposureCanonicalContent` Go tests. `validate:contract` 166/166 against both runtimes; guard verified to fail against the stale pre-alignment binary. No API shape/behavior change, no plan/apply semantic change, no coverage gate change. Source: `audits/v1.6-architecture-audit-1.md`. Next: Arch-B (extract config apply orchestration into tested plan/apply engine functions in both runtimes).
- [x] **v1.6 Slice B — Service manager rollback and config error-path tests** — 9 new Go tests: 7 in `manager_test.go` (CreateRule/UpdateRule/DeleteRule/ReorderRules persist-failure rollback, UpdateRule restart-after-rollback, NewFromConfig load error, StartEnabled bind failure), 2 in `config_test.go` (Load non-ErrNotExist error, Save MkdirAll failure). Service coverage 87.7% → 88.6%, gate raised 87% → 88%. All tests pass without sockets or permissions tricks.
- [x] **v1.6 Slice A — Coverage tooling stabilized** — Windows vitest/v8 path-case deduplication bug fixed in `scripts/validate-coverage.js`. Structural-zero files excluded from vitest `coverage.include` in shared/server/client workspaces. Client branch/funcs gates recalibrated to accurate post-dedup values (89/78). Two consecutive `npm run validate:coverage` runs both pass.

## Automated Coverage Confirmed

Shared and TypeScript coverage:

- [ ] Shared validation and port advisories
- [ ] TypeScript server CRUD HTTP layer
- [ ] TypeScript server import/export HTTP layer
- [ ] TypeScript server status, start/stop, reorder, duplicate binding, and static serving behavior
- [ ] Client App integration flows
- [ ] Settings import/FileReader flow
- [ ] Dashboard, Activity, Settings, API Docs, Forward Rules, and drawer flows
- [ ] Rule diagnostics UI: Diagnose button, loading state, pass/warn/fail/skip panel, error display, duplicate prevention, clear on close/delete
- [ ] Diagnostics export: Download Diagnostics JSON button in Settings, bundle structure (schemaVersion, runtime, rules, statuses, activity, diagnostics, metadata), partial-failure errors array, empty-diagnostics note, filename pattern, disabled-while-generating
- [ ] **API documentation rule:** when an API endpoint is added, removed, or changed — both `docs/api-contract.md` and the client in-app API Docs view (`client/sources/features/apidocs/ApiDocsView.tsx`) must be updated, along with `ApiDocsView.test.tsx`.
- [x] **TypeScript server forwarder coverage hardening (v1.4 Slice 1)** — tcp-forwarder.ts raised to 100% statements/functions; udp-forwarder.ts raised to 84.3% statements/100% functions. 7 new TCP tests + 13 new UDP tests. Remaining UDP gaps (multi-client send/return error callbacks, race guard) documented. Server overall: 71.9% → 79.6% stmts.
- [x] **TypeScript server UDP session tracking (v1.4 Slice 4)** — added `UdpSessionRegistry` (`server/sources/connections/udp-session-registry.ts`): runtime-local UUID IDs, composite session keys (`ruleId:mode:clientAddress:clientPort`), `openOrTouchSession`/`recordInbound`/`recordOutbound`/`closeSession`/`closeSessionsForRule`/`pruneExpired`/`snapshot`/`snapshotForRule` API. Constants: `UDP_SESSION_IDLE_MS = 30_000`, `UDP_SESSION_EXPIRE_MS = 300_000`. `snapshot` filters expired sessions without pruning; `pruneExpired` removes them explicitly. Wired into `UdpForwarder`: one-way and last-client tracked via inbound packet handler (last-client change detects new client, closes old session, opens new); multi-client tracked per client endpoint in `handleMultiClientMessage`; `recordOutbound` called on target responses in last-client and multi-client; `closeSession` called on multi-client timeout; `closeSessionsForRule` called in `stop()`. `ForwardManager` owns shared `UdpSessionRegistry`, injects via 4th constructor parameter, exposes `getLiveUdpSessions()` for internal use. No public `GET /api/connections` endpoint added yet. Registry: 100% stmts/branch/funcs; 49 unit tests. `udp-forwarder.ts`: 86.3% stmts (up from 84.3%), 84% branch, 100% funcs; 9 new integration tests. Server overall: 80.55% → 82.21% stmts.
- [x] **Go service TCP live tracking (v1.4 Slice 5)** — added `TcpConnectionRegistry` (`service/sources/connections/tcp_connection_registry.go`): runtime-local UUID IDs, `OpenConnection`/`AddBytesIn`/`AddBytesOut`/`CloseConnection`/`CloseConnectionsForRule`/`Snapshot`/`SnapshotForRule` API, concurrency-safe (mutex for map ops, atomic ops for byte counters), serializable `TcpConnectionInfo` snapshots with `durationMs` at snapshot time. `NewTCPForwarderWithRegistry` constructor added; `countingWriter` extended with `onBytes` callback; `CloseConnectionsForRule` called in `Stop()` after `wg.Wait()` as belt-and-suspenders cleanup. `Manager` owns shared `TcpConnectionRegistry`, passes it to each `TCPForwarder` via `NewTCPForwarderWithRegistry`, exposes `GetLiveTCPConnections()` for internal use. No public `GET /api/connections` endpoint added yet. Registry: 98.1% stmts (100% all public methods; 1 untestable `rand.Read` error path in private `generateConnectionID`); 26 unit tests. Forwarder: 8 new integration tests. Service overall: 79.7% → 80.6% stmts.

Playwright E2E coverage (32 tests across 9 spec files):

- [x] App load, sidebar navigation, mobile hamburger/sidebar, Dashboard stat cards
- [x] Add/edit/delete rule flows
- [x] Start/stop rule flow
- [x] Rule form validation — name required error
- [x] Rule group metadata (v1.8 Slice 2) — set group on create (chip shown in list + API persists it), edit to clear group (chip gone + API reports no group)
- [x] Rule group filtering (v1.8 Slice 3) — Filter by group control appears when groups exist; filtering to a group / Ungrouped narrows the list; combines with search/status; All Groups restores everything
- [x] CLI group commands (v1.8 Slice 5) — `portier group list` (counts + empty state), `portier group start/stop <group>` (summary, skipped vs failed, `--json` GroupActionResponse), exit codes: 0 success/skip, 1 failed/no-match, 2 invalid arg, 3 unreachable
- [x] UI group actions (v1.8 Slice 6) — filter to a concrete group → Start/Stop group buttons appear (hidden for All Groups / Ungrouped); start/stop refreshes status and shows an inline summary; failed/error shows a warning/error message; buttons disable while in flight
- [x] Rule health (v1.8 Slice 7, column added v1.8 Slice 9) — rule list has a dedicated **Health** column (colour dot + one-word `Healthy`/`Warning`/`Error` label, `—` when no status), distinct from the Status column; a stopped autostart rule reads `warning`, a running rule `healthy`, a rule with a lastError `error`; `portier status` shows a HEALTH column and `--json` includes `health`; health is derived only (no probing)
- [x] Duplicate rule (v1.8 Slice 8) — the Duplicate action on a rule row opens the drawer in "Duplicate Rule" (create) mode pre-filled from the source; name becomes `<name> copy`, group is carried over, autostart is off, no id; saving without changing the listen host/port/protocol shows the duplicate-binding error; changing the listen port and saving creates a second rule while the source is unchanged; Cancel/Close creates nothing
- [x] Config preview clarity (v1.8 Slice 9) — Settings → Plan & Apply preview shows per-operation cards with Add/Update/Remove/Unchanged badges, a plain-language impact note, and `Destructive`/`Metadata only` text tags (not colour-only); changed fields show friendly labels (`Listen port`, `Target host`, `Autostart`, `Group`, …) with a `forwarding`/`metadata` tag and `before → after`; the summary shows `Destructive: N` when applicable; no-drift/in-sync, plan-error (apply disabled), warning, destructive-confirmation, and apply success/failure states all still work
- [x] Config preview group change (v1.8 Slice 9) — planning a config whose only change is a rule's group shows the Group change (`(none) → backend`), marks it `Metadata only`, states the forwarder is not restarted, requires no destructive confirmation, and applies to show the group chip on the rule
- [x] Restore `config.exported` live activity emission (v1.14.1 candidate) — restored the one documented deferred item from the v1.14 NestJS migration: a live `GET /api/config/export` emits the `config.exported` activity event again (the migration routed the live read through the pure `ConfigExportService` → `buildExportedConfig` path instead of the old `ForwardManager.exportConfig`, dropping the emission). **Ownership:** a new narrow `ConfigExportRecorder`/`CONFIG_EXPORT_RECORDER` (`api/config/config-export.recorder.ts`) owns the single side-effect — the live runtime binds `createConfigExportRecorder(rt.activity)` (emits into the live `ActivityStore`), the static `AppModule` (OpenAPI generation + tests) binds `noopConfigExportRecorder` (records nothing); `ConfigExportService` injects it and calls `recordExport(config.rules.length)` after building, so **exactly one** event per live export and **none** during OpenAPI generation / through no-op providers. The reader (`listRules`) + `buildExportedConfig` stay pure, the controller stays transport-only. **No drift:** the canonical payload is a single shared `configExportedActivityEvent(ruleCount)` (`config/config-export.ts`) used by both the recorder and `ForwardManager.exportConfig`. **Unchanged:** response body, `exportedAt`/clock semantics, OpenAPI schema (recorder is a provider, not a DTO — `docs/api/openapi.json` regenerated to identical bytes), `validate:contract` **234/234**. No API/contract change, no new endpoint, no Go/CLI/replay change, no version bump/tag. Validation: lint/typecheck/build clean; server suite green (618, +4: 2 recorder + 1 service + 1 live-runtime integration); `validate:coverage:server` **100/100/100** (new recorder + factory + service path fully covered, no gate lowered/excluded); `validate:contract` **234/234**; packaged runtime layout + smoke **24/24** (the only `build:runtime` hiccup was the unrelated pre-existing `TestTCPForwarderEmitsConnectionClosedEvent` Go socket-timing flake — no Go files touched, passes in isolation). **Replay validation skipped** (no replay/`@portier/shared` files touched). **Last documented migration-deferred item — none remain.**
- [x] Final v1.14 release audit & readiness review (v1.14 Slice 32) — **Result: READY WITH NOTES.** Audit + small remediation only; no feature/API/behavior change; `validate:contract` **234/234**. **Verified:** one TypeScript server runtime (NestJS); packaged `server.js` bundles NestJS and boots; Go is the preferred packaged runtime; no Express runtime/scripts; no production import of removed Express/parity files; no `sources/nest|legacy|common`; source layout final (root `sources/` = `index.ts` only; API + `api/common/` under `api/`; domain/runtime/persistence homed); runtime logging is NestJS `Logger` (`console.*` only in build-tooling entries); coverage **100/100/100**; OpenAPI primary/docs/release flow verified; static + `/api/*` envelope intact; smoke green. **Remediation:** reworded ~80 stale "Express"/"the Express route|manager"/"cannot drift"/"are migrated/stay with Express" references in production comments + ~13 in test descriptions → current `/api`-contract wording (no behavior change); the generated `docs/api/openapi.json` now has **0 "Express"** in operation descriptions (was 9 user-facing); fixed `docs/architecture.md` server-flow (`api.ts`+Express → NestJS), `docs/api-contract.md` intro (`server/sources/api.ts` → NestJS under `server/sources/api/`), `docs/glossary.md` ("Node/Express" → "Node/NestJS"); historical slice-logs keep period-accurate wording. **Version NOTE:** versions are still **1.13.0** and the changelog header is `[Unreleased] — v1.14 (in progress)` — the bump to **1.14.0** + changelog finalization + tag are the deliberate release step (not this audit). Validation: lint/typecheck/build clean; full `npm run test` green (shared 105, server 614, client 449, all 12 Go); `validate:contract` 234/234; `validate:coverage:server` 100/100/100; `validate:runtime:smoke` 24/24 (incl. OpenAPI 0-Express + Go smoke + static UI); packaged `server.js` boot-verified. **Replay validation skipped** (no replay/shared files touched). **v1.14 can be tagged next.**
- [x] Server source layout cleanup and NestJS logging consolidation (v1.14 Slice 31) — structural cleanup before the final v1.14 release audit. No API behavior/contract change; CLI/UI/Go/replay untouched; `validate:contract` **234/234**; `docs/api/openapi.json` unchanged. **Root `sources/` now holds only the entry (`index.ts`):** moved the domain/runtime/persistence files that sat beside it into homed concern folders — `forwarders/forward-manager.ts`, `config/{config-export,config-plan}.ts`, `persistence/config-store.ts`, `connections/connections-snapshot.ts`, `runtime/runtime-info.ts`, `diagnostics/diagnose.ts`, `app/server-options.ts`, `testing/test-helpers.ts` (a path-aware codemod rewrote 58 import specifiers across 43 files; `git mv` preserved history). **Root `common/` dissolved** — its files are used only by `api/` (plus the app filter registration), so the API-layer shared infra (error envelope/filter/exceptions, validation pipe, shared `CLOCK_READER`, the one cross-feature `ApiErrorResponseDto` schema) moved to **`api/common/`**; `runtime-context.ts` → `app/` (DI/bootstrap wiring) and `manager-error.ts` → `api/forwards/` (forwards-owned) left separately. No more `sources/common/`. **Logging consolidated onto NestJS `Logger`:** removed the bespoke structured-console logger (`logger.ts`); the entry uses `new Logger("Server")`, and `createNestApp` enables the NestJS logger for a live runtime / silences it for OpenAPI generation + tests (`resolveLoggerOption`/`CreateNestAppOptions.logger`, unit-tested). The only remaining `console.*` are the logic-free OpenAPI build-tooling entries (`generate.ts`/`copy-release.ts` — normal script output, outside runtime logging). DTO/schema ownership stays feature-local; no `sources/nest/` or `sources/legacy/` returns. Coverage gate stays **100/100/100** (added `app/app.factory.test.ts` for `resolveLoggerOption`; `vitest.config.ts` test-helpers exclude path updated; `testing/test-helpers.ts` excluded). `validate:contract` **234/234**; `validate:coverage:server` **100/100/100**; `validate:runtime:smoke` **24/24** (packaged NestJS `server.js` boots with NestJS logging); lint/typecheck/build clean; full `npm run test` green (shared 105, server 614, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **v1.14 server tree is final-audit-ready.**
- [x] Server structure cleanup, 100% coverage gate, release OpenAPI artifact (v1.14 Slice 30) — the post-switch hardening slice; NestJS becomes the **normal server**, not a migration scaffold. No API behavior/contract change; CLI/UI/Go/replay untouched; `validate:contract` **234/234** (boots NestJS); `docs/api/openapi.json` unchanged. **Legacy Express removed:** deleted `sources/legacy/` (Express `createApp`/entry/tests), the parity harness (`api-parity.ts`), and the 11 Express↔Nest `*.integration.test.ts` parity tests; reusable domain modules (forward manager, config store/plan/export, connections snapshot, runtime info, diagnose, activity store, forwarders) stay; `express` kept (NestJS platform/static need it), `cors`/`@types/cors` dropped. **`sources/nest/` flattened** into `sources/{app, api/<feature>, common, health, openapi, static}/` + framework-free domain modules — one runtime, no `nest/` layer. **Coverage gate → 100/100/100** (server 95/92/99 → 100/100/100) reached honestly: re-covered the manager behaviors the deleted forwards parity test drove through HTTP (it had dropped `forward-manager.ts` to 87% branch) with direct unit tests; structurally-unreachable defensive branches (2 s diagnose socket-bind/connect timeouts, post-stop socket-response races, nullish-on-initialized-counter fallbacks, DNS-error fallback, merged-rule re-validation, validated-id reorder guard, stopGroup catch) narrowly `/* v8 ignore … -- reason */`-annotated — no broad exclusions, no lowered gate; only logic-free entries (`index.ts`/`openapi/generate.ts`/`openapi/copy-release.ts`) + `**/*.schema.ts` excluded; 616 server tests. **OpenAPI release artifact wired:** new logic-free `openapi/copy-release.ts` + `copy:apidoc:release` → `copyOpenApiToRelease(releaseDir, resolveOpenApiPaths().primary)` (copies the primary `server/build/api/openapi.json`, no regen); the 3 platform build scripts run `generate:apidoc` then the copy, so packaged output includes `api/openapi.json` (`validate-runtime` asserts it). **Packaged `server.js` now bundles NestJS** (esbuild entry repointed `sources/legacy/index.ts` → `sources/index.ts` + 4 optional transports external); fixed a pre-existing latent bug — the CJS bundle crashed as ESM under the `"type":"module"` root, so the scripts write `{ "type": "commonjs" }` into the package dir; packaged `server.js` verified to boot (`/health`, `/api/forwards`, `/api/runtime` `runtime:node`, `/api` envelope, UI 200). Go remains the preferred packaged runtime. **Scripts/docs cleaned:** removed `dev:legacy`/`start:legacy`/`start:nest`/`test:nest` + root `start:server:legacy`; `server/readme.md` + comments rewritten for the single NestJS server (no Express/legacy/shadow framing). `validate:contract` **234/234**; `validate:coverage:server` **100/100/100**; `validate:runtime:smoke` **24/24** (package + OpenAPI artifact + Go smoke); lint/typecheck/build clean; full `npm run test` green (shared 105, server 616, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **v1.14 is complete — NestJS is the single TypeScript server.**
- [x] Switch default TypeScript runtime from Express to NestJS (v1.14 Slice 29) — the runtime switch. **NestJS is now the default TypeScript server runtime**; Express is retained under `sources/legacy/` for rollback/reference. No API contract change; `validate:contract` **234/234** (now run against the NestJS server). **Default runtime:** `sources/index.ts` builds a NestJS app via `createNestApp(runtime)` and keeps the same lifecycle (load config + start enabled forwarders, bind HTTP server with socket tracking, graceful shutdown); `node build/index.js`/`dev`/the contract harness boot NestJS. **Live wiring:** a `@Global()` `RuntimeContextModule` provides `APP_RUNTIME`; each feature provider sources its token from it — the live `ForwardManager` backs every reader/writer/diagnostic/group/config token, the live `ActivityStore` backs `ACTIVITY_STORE`, runtime-info/static back the rest; `AppModule` stays static (shadow, `APP_RUNTIME=null`) for OpenAPI/tests (`overrideProvider`), `createLiveAppModule(runtime)` is the live root. Proven by a new `live-runtime.integration.test.ts` (10 tests: reads reflect the live manager/store, a write mutates it, static+SPA+asset+`/api` envelope+`/health`). **Legacy:** Express preserved under `sources/legacy/` (`api.ts`/`index.ts`/`api.test.ts`, not deleted) — the parity baseline (`*.integration.test.ts` import `legacy/api.ts`) + rollback path; shared domain modules stay outside `legacy/`; the obsolete shadow scaffold (`nest/main.ts`/`bootstrap.ts`/`nest-options.ts`) removed. **Scripts:** default `dev`/`start`/`start:nest` + root `start:server`; legacy `dev:legacy`/`start:legacy` + root `start:server:legacy`. **Static** runs through the default runtime (entry wires `configureStaticAssets` + `STATIC_FALLBACK`). **Packaging:** the single-file `server.js` Node fallback bundles the **legacy Express** entry (Go is the preferred packaged runtime; bundling NestJS into the single-file fallback is a documented follow-up — verified the legacy bundle compiles); 3 platform scripts repointed. **OpenAPI** unchanged (no drift). `validate:contract` **234/234 against the rebuilt NestJS server**; server coverage 99.4%/97.0%/100% (gate PASS, none lowered), every measured Nest file + `runtime-context.ts`/`app.module.ts`/`app.factory.ts` at 100%; 843 server tests; lint/typecheck/build clean; full `npm run test` green (shared 105, server 843, client 449, all 12 Go). **Accepted boundaries** documented (non-API 404 body shape; generic-500 no-leak; header parity out of scope). **Replay validation skipped** (no replay/shared files touched). **Rollback** = one-line entry switch to the preserved legacy Express. Full `validate:runtime:smoke` not re-run (Go binary unchanged; legacy `server.js` bundle verified to compile).
- [x] Final NestJS migration audit & switch-readiness review (v1.14 Slice 28) — **Result: PASS**. The final v1.14 audit; an audit/remediation slice, NOT a runtime switch. CLI/UI/Go/replay untouched; no production Nest source changed; `validate:contract` **234/234**. **Endpoint inventory:** all 20 Express `/api/*` endpoints migrated into NestJS + the `/api` 404 envelope + static/SPA serving; `/health` is the documented probe outside the frozen `/api` contract; the OpenAPI doc's 18 paths match exactly (new inventory regression guard asserts the exact path set + methods). **Parity:** every endpoint has a byte-for-byte Express↔Nest integration test; volatile fields pinned by clock/process seams (not stripped); socket tests use free ports + `stopAll()` in `finally`; unknown-route probe uses `/api/not-migrated`; static boundaries documented. **DTO/schema/OpenAPI:** every input endpoint has a request DTO or documented exception (activity coercion); every body response a response DTO; the `204` + `@Res` non-envelope (`config-import` `422 {errors,result}`) cases documented + in OpenAPI; the only `unknown` schema fields use the generator-safe explicit object schema (no Swagger circular); schema classes feature-local, only `ApiErrorResponseDto` in `common/`. **Error/provider/static:** no controller hand-rolls `{errors}` (filter/`mapManagerError` own it); generic-500 no-leak intact; providers narrow, no Nest production import of Express `createApp`/Go/CLI/replay (`createApp` only in parity harnesses); static gating/fallback/`/api`-envelope-precedence hold, no OpenAPI routes added. **OpenAPI workflow:** primary `server/build/api/openapi.json` (gitignored) + synced tracked `docs/api/openapi.json`, deterministic (sorted schemas), no drift, release-copy helper covered. **Comment hygiene:** removed remaining "scaffold"/migration wording from Nest test descriptions. **Switch-readiness checklist** added to `server/readme.md` (wire real runtime tokens/static/config-export activity, scripts/packaging, smoke tests, rollback plan, `legacy/` Express preservation, accepted boundaries) — Express stays default; the switch + `legacy/` move is a separate slice. Fixes: 1 new route-inventory regression test, ~23 test-comment rewords, the checklist. 843 server tests; coverage 99.4%/96.8%/100% (gate PASS, none lowered), every measured Nest file 100%; lint/typecheck/build clean; full `npm run test` green (shared 105, server 843, client 449, all 12 Go). **Replay validation skipped** (no replay/shared files touched). **v1.14 endpoint/serving migration complete + audited; the runtime switch is a deliberate later step.**
- [x] OpenAPI artifact placement & DTO/schema hygiene cleanup (v1.14 Slice 27) — cleanup/refactor before the final migration audit; no endpoint behavior change, no runtime switch. **OpenAPI placement:** `generate:apidoc` writes a primary server-owned `server/build/api/openapi.json` (gitignored) and syncs the tracked `docs/api/openapi.json` copy from it; new pure helpers `resolveOpenApiPaths`/`writeOpenApiArtifacts`/`copyOpenApiToRelease` (packaging copies the primary artifact into `<releaseDir>/api/openapi.json` without regenerating; generator never needs a release dir); output is deterministic across transforms via sorted `components.schemas` (`canonicalizeOpenApiDocument`). **Schema ownership:** dissolved the centralized `common/api-schemas.ts` (37 classes) into feature-local `*.schema.ts` files (forwards/config/ports/activity/runtime/connections/status); `common/` keeps only `ApiErrorResponseDto` (`common/api-error.schema.ts`); class names unchanged → generated OpenAPI semantically identical (only sorted ordering + reworded description differ); coverage exclusion moved to the `sources/nest/**/*.schema.ts` glob (mappers stay 100% covered). **Comment cleanup:** removed slice/migration-history language from NestJS code comments (current-architecture wording; bootstrap log says "NestJS server"/"shadow mode"). **Legacy guideline (docs only):** documented the future `legacy/` preservation of Express for a rollback path — no Express code moved, Express remains active. `docs/api/openapi.json` keeps all 18 routes + 37 schemas; static still adds no routes. `validate:contract` **234/234**; 434 nest tests (+5); server coverage 99.3%/96.7%/100% (gate PASS, none lowered), every measured Nest file 100%; lint/typecheck/build clean; full `npm run test` green (shared 105, server 843, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **v1.14 ready for the final migration audit / switch-readiness review.**
- [x] Migrate static client serving — SPA fallback (v1.14 Slice 26) — migrated static client serving into the Nest shadow runtime (`server/sources/nest/static/`), completing the v1.14 endpoint/serving migration; only the final audit/switch-readiness review remains. **Inspected Express:** static is gated on `staticClientDir && hasStaticClient(dir)` (usable only if it has `index.html`); when enabled, `express.static(dir)` serves assets + a catch-all serves `index.html` for non-`/api` paths (SPA); a **missing dir is allowed** (API + `/api/*` JSON-404 stay usable). **Nest design:** `static-serving.ts` provides `resolveStaticOptions`/`hasStaticClient` (mirror the enable rule), `configureStaticAssets(app, dir)` (`app.useStaticAssets` — the `express.static` equivalent), and a `StaticFallback`/`STATIC_FALLBACK` strategy that serves `index.html` for an unmatched non-API GET/HEAD route. Since Nest surfaces unmatched routes as `NotFoundException` → the global `ApiErrorEnvelopeFilter`, that filter is the single unmatched-route handler, so it now also owns the **non-API SPA fallback** (delegating the static decision to the injected `StaticFallback`); the `/api/*` envelope branch runs first and is untouched. **Shadow-only & API-safe:** `STATIC_FALLBACK` defaults to `disabledStaticFallback` (scaffold wires no dir), so live behavior is unchanged and the API works with no client build; static is proven via tests + the reusable `configureStaticAssets` helper, with live `start:nest` wiring deferred to the switch slice (no `main.ts`/`bootstrap.ts` change). `/health` and all controllers still match before any fallback. **Express↔Nest parity** (raw status+body): root `/`, SPA routes, real asset, missing asset → index.html, unmatched `/api/*` → JSON envelope — all identical; plus a disabled-static case (API usable, no SPA served; the non-API 404 *body shape* differs — Express HTML vs NestJS JSON — the documented pre-existing boundary). **OpenAPI:** static adds **no** routes (`docs/api/openapi.json` unchanged; a generator test asserts only `/health` + `/api/*` paths, no `/`/catch-all leakage). Express remains the default runtime; Nest static is shadow-only. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code**; 429 nest tests (+30); server coverage 99.4%/96.8%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 838, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **v1.14 ready for the final migration audit / switch-readiness review.**
- [x] Migrate `POST /api/config/apply` — mutating config apply (v1.14 Slice 25) — migrated the **final config endpoint** into `server/sources/nest/api/config/`, completing the config export/plan/import/apply migration. Applies a desired config (`{desired, yes?, dryRun?}`) via a narrow `ConfigApplier`/`CONFIG_APPLIER` (`listRules` + `importConfig`; the domain `ForwardManager` satisfies it — a separate token from `CONFIG_IMPORTER` so each config endpoint is independently overridable), reusing the **shared `buildConfigPlan` + `buildApplyImportFromPlan`** and, on the non-dry-run drift path, the **shared `ForwardManager.importConfig` (`replace`)** (inheriting persist rollback, enabled-rule start, activity). **Exact Express ordering preserved:** missing `desired` → `400`; `hasErrors` → `200 ok:false`; `dryRun` → `200 ok:true` (BEFORE the destructive gate); `destructive && !yes` → `400`; `hasDrift` → replace import (+ belt-and-suspenders import-error guard → `200 ok:false` via `plan.errors`); else → `200 ok:true`. **All success/`ok:false` outcomes are `200`** (only the two gating errors are `400`), so `@HttpCode(200)` + the service **throws** `ApiBadRequestException` for the `400`s and **returns** the `ConfigApplyResponse` otherwise — no `@Res` needed. **Request validation delegated** (documented parity exception): the `desired`-key-presence check (`desired: null` allowed → plan error, not `400`) and destructive-gate are inline service checks; `ConfigApplyBodyDto` is doc-only. **Volatile parity:** the response has TWO volatile timestamps (`appliedAt` + embedded `plan.generatedAt`) — both now stamped from ONE clock instant (Express threads `options.now` into both `appliedAt` and `buildConfigPlan`'s `now` — a production-invisible refinement; Nest injects `CLOCK_READER`); parity pins the same clock in both runtimes, **no field stripped/normalized**. **Response DTO:** new `config-apply.response.dto.ts` (`toConfigApplyResponseDto`, `structuredClone` deep copy) + decorated `ConfigApplyResponseDto`/`ConfigAppliedCountsDto`/`ConfigApplyBodyDto` in `api-schemas.ts`. **Byte-for-byte parity** with **separate seeded managers** (apply mutates) + fixed-id `enabled:false` rules + pinned clock: `400` missing desired, dry-run (pinned `appliedAt`+`generatedAt`, no mutation), apply-add (+ `GET /api/forwards`/`export` state-after parity), destructive blocked without `yes` (state unchanged), destructive applied with `yes:true` (rule removed), invalid rule → `ok:false` (no mutation), no-drift → `ok:true`. The import-error guard + persist re-throw (`500`) are service unit-covered (fake applier). **OpenAPI** documents the apply (body `ConfigApplyBodyDto`/`200` `ConfigApplyResponseDto`/`400`); export/plan/import docs intact; `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `POST /api/config/apply` is behavior-unchanged (only the production-invisible clock seam). `validate:contract` **234/234**. **100% coverage of all new/changed Nest code**; 399 nest tests (32 added); server coverage 99.3%/96.7%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 808, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **Config milestone complete; remaining v1.14: static client serving.**
- [x] Migrate `POST /api/config/import` — mutating config import (v1.14 Slice 24) — migrated the **first mutating config** endpoint into `server/sources/nest/api/config/` via a narrow `ConfigImporter`/`CONFIG_IMPORTER` (`importConfig` + `listRules`; the domain `ForwardManager` satisfies it), reusing the **shared `ForwardManager.importConfig`** (replace/merge mutation, duplicate-binding/merge-conflict rejection, **persist rollback**, enabled-rule start, `config.imported`/`config.import.failed` activity — nothing reimplemented). **Request validation delegated** (documented parity exception): Express validates `mode` then `config` with short-circuit ordering (class-validator would accumulate both → diverge), so `ConfigImportBodyDto` is doc-only and the service does the inline checks in Express's exact order (`400 ["mode must be replace or merge."]` first, then `400 ["config must be a valid Portier config object with version 1 and a rules array."]`). **`200 {result, rules}` and `422 {errors, result}` RETURNED via `@Res({ passthrough: true })`** (matching Express's `response.status().json()`) — the `422` carries `result` alongside `errors`, so it is NOT the plain `{errors}` envelope and must not flow through the error filter (`toApiError` strips extra fields); only the two `400`s are thrown as `ApiBadRequestException`. No volatile field → no clock seam. **Response DTOs:** new `config-import.response.dto.ts` (`toConfigImportResponseDto`/`toConfigImportErrorResponseDto`, `structuredClone` deep copies) + decorated `ConfigImportResponseDto`/`ConfigImportErrorResponseDto`/`ImportResultDto`/`ConfigImportBodyDto` (config reuses `ConfigExportResponseDto`) in `api-schemas.ts`. **Mutation/rollback safety:** all `422` paths reject before mutating; persist failure rolls back (manager Test-D) + propagates → `500` (covered via throwing fake). **Byte-for-byte parity** with **separate seeded managers** (import mutates) + fixed-id `enabled:false` rules (deterministic, socket-free): `400` mode/config, `200` replace into empty + over seeded (+ `GET /api/forwards`/`export` state-after parity), `422` invalid rule (+ no-mutation assertion), `422` duplicate binding. **OpenAPI** documents the import (body/`200`/`422`/`400`); export+plan docs intact; `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `POST /api/config/import` is unchanged (no Express source modified). `validate:contract` **234/234**. **100% coverage of all new/changed Nest code**; 367 nest tests (27 added); server coverage 99.3%/96.7%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 776, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **Remaining Milestone 4: the mutating `apply` (volatile `appliedAt` + destructive-gating/rollback).**
- [x] Migrate `POST /api/config/plan` — non-mutating config dry-run (v1.14 Slice 23) — began the config milestone with the **safest** config endpoint, migrated into `server/sources/nest/api/config/`. **Inventory:** Express has `GET /api/config/export` (done), `POST /api/config/import` + `POST /api/config/apply` (both mutating), and `POST /api/config/plan` (**non-mutating** dry-run) — `plan` was selected. Diffs the desired config against the current rules via a narrow `ConfigPlanReader`/`CONFIG_PLAN_READER` (`listRules()`; domain `ForwardManager`) + the shared `CLOCK_READER`, using the **shared `buildConfigPlan`** (same pure engine Express calls). Returns **`200`** + a `ConfigPlanResponse` (`@HttpCode(200)`); **never mutates rules / opens sockets / emits activity**. **Request validation delegated** (documented parity exception): the check is `"desired" in body` key-presence (`desired: null` is allowed → plan error, not `400`; class-validator `@IsDefined` would diverge), so the service does the inline check (`400 ["desired is required."]`) and `ConfigPlanBodyDto` is doc-only. **Volatile `generatedAt` pinned** via the existing `now` seam — `buildConfigPlan` already had a `now` arg; the Express route now threads `options.now` (behavior-preserving), Nest passes `CLOCK_READER.now()`. **Response DTO:** new `config-plan.response.dto.ts` (`toConfigPlanResponseDto`, a `structuredClone` deep copy of the deeply-nested plan) + decorated `ConfigPlanResponseDto`/`ConfigPlanSummaryDto`/`ConfigPlanOperationDto`/`ConfigPlanRuleSnapshotDto`/`ConfigPlanChangeDto`/`ConfigPlanErrorDto`/`ConfigPlanWarningDto`/`DesiredConfigDto`/`ConfigPlanBodyDto` in `api-schemas.ts`. **Non-mutating byte-for-byte parity** (shared manager + pinned clock): `400`-missing-desired, empty plan, add/update/unchanged drift (with a `manager.listRules()` before/after no-mutation assertion), invalid-desired-rule → plan error (`200`), `desired: null` → plan error. **OpenAPI** documents the plan (body `ConfigPlanBodyDto`/`200` `ConfigPlanResponseDto`/`400`); config-export docs intact; `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `POST /api/config/plan` is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code**; `config-plan.ts` stays at its pre-existing 99.3% (unchanged — only `api.ts` threaded the seam). 340 nest tests (20 added); server coverage 99.3%/96.6%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 749, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **Remaining Milestone 4: the mutating `import`/`apply` (with rollback parity).**
- [x] Migrate `POST /api/forwards/groups/:group/start` — group start (v1.14 Slice 22) — migrated the **group start** endpoint into `server/sources/nest/api/forwards/`, **completing the group-action pair** (and the entire `/api/forwards` surface), via a narrow `ForwardGroupStarter`/`FORWARD_GROUP_STARTER` (the domain `ForwardManager.startGroup` satisfies it). The service mirrors `handleGroupAction("start")` exactly (the Slice-21 stop counterpart) — normalizes the `:group` (`decodeURIComponent(...).trim()`), validates with the shared `validateGroupName` (→ `400`, delegated), starts the group, maps empty → `404`, summarizes with the shared `summarizeGroupAction`. Returns **`200`** + a `GroupActionResponse` (`@HttpCode(200)`); **behaviour over rule metadata** (no rule mutation; autostart not a precondition); `:group` via `@ApiParam`; throws the shared `ApiBadRequestException`/`ApiNotFoundException` directly (matching Express's inline `400`/`404`). **Response DTO REUSED** — the shape matches group stop, so it reuses the Slice-21 `GroupActionResponseDto`/`GroupActionResultDto` + `toGroupActionResponseDto` (no new DTO/schema). **Socket-safe byte-for-byte parity** (`GroupActionResponse` has no volatile field): `400`/`404`/encoded-group-normalization are socket-free (separate managers); the success case uses the **idempotent already-running** path on a **shared** manager (open the listener once via Test-A `startRuleStable`, both runtimes → `already_running` skips, `stopAll()` cleanup), and a single-runtime cold-start test starts a real listener through the Nest endpoint (`started` + GET-status-running) with `stopAll()` cleanup — no leaked sockets. **OpenAPI** documents the group start (path param/`200` `GroupActionResponseDto`/`400`/`404`); group stop docs intact; `docs/api/openapi.json` regenerated; generator test asserts both group routes. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its group start is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code**; 320 nest tests (13 added); server coverage 99.3%/96.6%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 729, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **Milestone 3 (write/lifecycle) complete — entire `/api/forwards` surface migrated.**
- [x] Migrate `POST /api/forwards/groups/:group/stop` — group stop (v1.14 Slice 21) — migrated the **first group action** into `server/sources/nest/api/forwards/` via a narrow `ForwardGroupStopper`/`FORWARD_GROUP_STOPPER` (the domain `ForwardManager.stopGroup` satisfies it). **Inventory:** Express has two group actions (`POST /api/forwards/groups/:group/{start,stop}` via `handleGroupAction`); **stop** was chosen as the safest first migration because stopping a group of stopped rules is a deterministic, socket-free no-op (all `not_running` skips), whereas **start** opens sockets (deferred). The service mirrors `handleGroupAction("stop")` exactly — normalizes the `:group` (`decodeURIComponent(...).trim()`), validates with the shared `validateGroupName` (→ `400`, delegated — not re-expressed in class-validator, a documented parity exception), stops the group, maps empty → `404`, summarizes with the shared `summarizeGroupAction`. Returns **`200`** + a `GroupActionResponse` (`@HttpCode(200)`); **behaviour over rule metadata** (no rule mutation); `:group` via `@ApiParam`. The service throws the shared `ApiBadRequestException`/`ApiNotFoundException` directly (matching Express's inline `400`/`404`). **Response DTO:** new `group-action.response.dto.ts` (`toGroupActionResponseDto`, deep copy with fresh `results`) + decorated `GroupActionResponseDto`/`GroupActionResultDto` in `api-schemas.ts`; `GroupActionResponse` has no volatile field (no clock seam). **Socket-free byte-for-byte parity** with separate seeded `enabled:false` managers: success (all `not_running` skips + counts + GET-unchanged), `404` (unknown group), `400` (empty/whitespace group), encoded-group normalization (`team%20space` → `team space`). **OpenAPI** documents the group stop (path param/`200` `GroupActionResponseDto`/`400`/`404`; group start NOT in the Nest doc); `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its group stop is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code**; 307 nest tests (16 added); server coverage 99.3%/96.5%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 716, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched). **Group start deferred** (it opens sockets — the lifecycle pair, a later slice).
- [x] Migrate `POST /api/forwards/:id/diagnose` — rule diagnose (v1.14 Slice 20) — migrated the **rule diagnose** endpoint into `server/sources/nest/api/forwards/` via a narrow read-only `DiagnosticReader`/`DIAGNOSTIC_READER` (`getRule` + `getStatus`; the domain `ForwardManager` satisfies it) + the shared `CLOCK_READER`. The service looks up the rule (unknown id → `404` via a direct `ApiNotFoundException`, matching Express's INLINE check — `getRule` returns `undefined`, it does not throw), reads its running state, and runs the **shared `diagnoseRule`** (the SAME function Express calls — identical checks/order/messages/summary: listen-host/LAN/privileged/common advisories, listen-bind probe, target-host DNS, TCP target-connect, UDP mode). Returns **`200`** + a `RuleDiagnosticsResult` (`@HttpCode(200)`); **read-only** (no mutation); **no request body**; `:id` via `@ApiParam`. **Volatile `diagnosedAt` pinned** via a behavior-preserving `diagnoseRule(rule, isRunning, now = new Date())` seam — Express threads its existing `AppOptions.now`, Nest passes `CLOCK_READER.now()` (mirrors the `exportConfig(now)` seam; production output unchanged). **Response DTO:** new `rule-diagnostics.response.dto.ts` (`toRuleDiagnosticsResponseDto`, deep structural copy incl. fresh `summary`/`checks`/`details`) + decorated `RuleDiagnosticsResultDto`/`DiagnosticCheckDto`/`DiagnosticSummaryDto` in `api-schemas.ts`. **Network-aware byte-for-byte parity:** because diagnose probes the network, the success test uses a **UDP** rule (target-connect always `skip` — no TCP probe; `127.0.0.1` instant DNS; only a transient UDP listen-bind), a **shared** manager, **sequential** calls (no listen-bind overlap), and a **pinned clock** in both runtimes — entire body incl. `diagnosedAt` matches, no normalization; the `404` path (no probes) uses separate managers. **OpenAPI** documents the diagnose (path param/`200` `RuleDiagnosticsResultDto`/`404`); `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `POST /api/forwards/:id/diagnose` is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code**; `diagnose.ts` stays at its pre-existing 98.2% (documented socket-timeout branches; the new `now` default is covered by existing 2-arg callers). 291 nest tests (12 added); server coverage 99.3%/96.5%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 700, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] Migrate `POST /api/forwards/reorder` — rule reorder (v1.14 Slice 19) — migrated the **rule reorder** endpoint into `server/sources/nest/api/forwards/` via a narrow `ForwardRulesReorderer`/`FORWARD_RULES_REORDERER` (the domain `ForwardManager` satisfies it via `reorderRules` + `listRules`) — the **FIRST migrated endpoint with a real validated request body DTO** (`ApiValidationPipe`, not doc-only). Returns **`200`** + the full reordered `ForwardRuleResponse[]` (reusing `ForwardsListResponseDto` + `toForwardRuleResponse` — same shape as `GET /api/forwards`); `@HttpCode(200)` (Nest defaults `POST`→`201`). The whole reorder is delegated to `manager.reorderRules` — id-existence check (unknown → `404`), listed-ids-first / unlisted-appended ordering (partial allowed, duplicate tolerated, empty = no-op), persistence rollback, and **no socket churn** (metadata-only). **Request DTO:** Express's inline `{ ids: string[] }` check is simple enough to re-express exactly, so `ReorderForwardRulesBodyDto` (`@IsArray` + `@IsString({each:true})`, both with the exact Express message `"ids must be an array of strings."`; `@ApiProperty({type:[String]})`) runs through `ApiValidationPipe` — non-array/missing/non-string-element → byte-identical `400`; whitelist strips extras; empty array valid. **Errors:** unknown id via the existing shared `mapManagerError` (`NotFoundError`→`404`); unexpected persist failure → generic `500` (documented Slice-3 divergence; unit-tested) — no new error type. **Socket-free byte-for-byte parity** (separate seeded `enabled:false` managers): full reorder (+GET-after-reorder), partial, duplicate, empty no-op, unknown id (`404`, order unchanged), and invalid body (`400`). **OpenAPI** documents the reorder (body/`200` array of `ForwardRuleResponseDto`/`400`/`404`); `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `POST /api/forwards/reorder` is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed code**; 279 nest tests (22 added); server coverage 99.3%/96.4%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 688, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] Migrate `POST /api/forwards/:id/stop` — rule lifecycle stop (v1.14 Slice 18) — migrated the **natural pair to start** into `server/sources/nest/api/forwards/` via a narrow `ForwardRuleStopper`/`FORWARD_RULE_STOPPER` (the domain `ForwardManager.stopRule` satisfies it), completing the single-rule start/stop lifecycle. Returns **`200`** + the rule's `ForwardStatus` (`@HttpCode(200)` — Nest defaults `POST`→`201`), **reusing the Slice-17 `forward-status.response.dto.ts` (`ForwardStatusDto` + `toForwardStatusResponseDto`) unchanged**. The whole stop is delegated to `manager.stopRule` — not-found handling, **idempotent already-stopped** behaviour (a not-running rule returns its status without touching a socket), listener close, `rule.stopped` emission — identical to Express. **No request body**; the `:id` path param is a plain string (Express does no validation; `requireRule`→`404`), documented via `@ApiParam`. **Errors:** unknown id flows through the existing shared `mapManagerError` (`NotFoundError`→`404`) — no new error type; an unexpected stop error → generic `500` (documented Slice-3 divergence; unit-tested). **Simpler-than-start parity:** a *stopped* status is fully deterministic (`running:false`, no `startedAt`), so byte-for-byte parity needs no shared manager — the already-stopped path is parity-tested with **zero sockets** (separate managers), and the running-rule stop is parity-tested by starting each manager on its own free port (Test-A `startRuleStable`), stopping via the endpoint (`200`, `running:false`), and asserting `GET /api/status`-after-stop parity, with `stopAll()` cleanup; a single-runtime UDP start→stop test proves UDP first-class. **OpenAPI** documents the stop (path param/`200` `ForwardStatusDto`/`404`); `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `POST /api/forwards/:id/stop` is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed code**; 257 nest tests (11 added); server coverage 99.2%/96.2%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 666, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] Migrate `POST /api/forwards/:id/start` — rule lifecycle start (v1.14 Slice 17) — migrated the **first lifecycle endpoint** into `server/sources/nest/api/forwards/` via a narrow `ForwardRuleStarter`/`FORWARD_RULE_STARTER` (the domain `ForwardManager.startRule` satisfies it). Returns **`200`** + the rule's `ForwardStatus` (`@HttpCode(200)` required — Nest defaults `POST`→`201`). The whole start is delegated to `manager.startRule` — not-found handling, **idempotent already-running** behaviour (returns status without restarting), listener open, `rule.started` emission — identical to Express; autostart/`enabled` is not a precondition. **Response DTO:** the body is a `ForwardStatus`, so a new `forward-status.response.dto.ts` reuses the `ForwardStatusDto` schema (shared with `GET /api/status`) via a pure `toForwardStatusResponseDto` mapper. **No request body**; the `:id` path param is a plain string (Express does no validation; `requireRule`→`404`), documented via `@ApiParam`. **Errors:** unknown id flows through the existing shared `mapManagerError` (`NotFoundError`→`404`) — no new error type; a start failure (port in use) → generic `500` (documented Slice-3 divergence; unit-tested, not cross-runtime asserted). **Socket-safe byte-for-byte parity:** a started status carries a volatile `startedAt`, so parity uses the **idempotent already-running** path on a **shared** manager — the listener is opened once via the Test-A `startRuleStable` bind-retry helper (no fixed ports / EADDRINUSE flake), then both runtimes return the identical pinned status (`200`, `running:true`); `GET /api/status` after start also matches byte-for-byte; unknown-id `404` parity uses separate managers. Two single-runtime **real cold-start** integration tests open a real TCP and UDP listener through the Nest endpoint (`200`, `running:true`, ISO `startedAt`) and **`stopAll()` in `finally`** — no leaked sockets. UDP first-class. **OpenAPI** documents the start (path param/`200` `ForwardStatusDto`/`404`); `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `POST /api/forwards/:id/start` is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed code**; 246 nest tests (12 added); server coverage 99.2%/96.1%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 655, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] Migrate `DELETE /api/forwards/:id` — rule delete (v1.14 Slice 16) — migrated the **rule delete** endpoint into `server/sources/nest/api/forwards/` via a narrow `ForwardRuleDeleter`/`FORWARD_RULE_DELETER` (the domain `ForwardManager.deleteRule` satisfies it), completing the rule CRUD trio (create/update/delete). Returns **`204` with no body** (matching Express; `@HttpCode(204)` + `void` return). The whole delete is delegated to `manager.deleteRule` — not-found handling, **runtime cleanup** (a running forwarder stopped first), **persistence rollback**, and the `rule.deleted` emission — identical to Express. **No request body** (no request DTO); the `:id` path param is a plain string (Express does no validation; `requireRule`→`404`), documented via `@ApiParam`; **no response DTO** (the `204` empty body is the response, the same exception as `DELETE /api/activity`). **Errors:** unknown id flows through the existing shared `mapManagerError` (`NotFoundError`→`404 { errors }`) — no new error type. **Byte-for-byte parity:** delete a stopped TCP rule (`204`, removed from a subsequent `GET`), delete a stopped UDP rule (UDP first-class), unknown id (`404`), and a repeated delete (first `204` then `404`). **OpenAPI** documents the DELETE (path param/`204`/`404`); `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `DELETE /api/forwards/:id` is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed code**; 234 nest tests (13 added); server coverage 99.2%/96.1%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 643, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] Migrate `PATCH /api/forwards/:id` — rule update (v1.14 Slice 15) — migrated the **rule update** endpoint into `server/sources/nest/api/forwards/` via a narrow `ForwardRuleUpdater`/`FORWARD_RULE_UPDATER` (the domain `ForwardManager.updateRule` satisfies it); returns `200` + the updated `ForwardRuleResponse`. The whole update is delegated to `manager.updateRule` — partial validation, **merge that never overwrites unspecified fields with `undefined`**, duplicate-binding rejection, not-found handling, persistence rollback, and **restart only when a running rule's forwarding field changes** (metadata-only updates don't restart; a stopped rule isn't started) — identical to Express. **Request DTOs:** `UpdateForwardRuleBodyDto` (partial) is **documentation/typing only** — validation delegated to the shared `validateForwardRulePatch` (no validation pipe on the body, documented parity exception); the `:id` path param is a plain string (Express does no validation; `requireRule`→`404`), documented via `@ApiParam`. **Response** reuses `ForwardRuleResponseDto` + `toForwardRuleResponse`. **Errors:** added `ApiNotFoundException` + a `NotFoundError`→404 branch in `mapManagerError` (Validation→400, NotFound→404, Conflict→409). **Byte-for-byte parity:** metadata-only patch (no restart, fields preserved), forwarding-affecting patch on a stopped rule, unknown id (404), invalid port (400), invalid protocol (400), duplicate binding (409), extra-field-ignored, and GET-after-PATCH. **OpenAPI** documents the PATCH (path/body/200/400/404/409); `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `PATCH /api/forwards/:id` is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed code**; 221 nest tests (18 added); server coverage 99.2%/96.0%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 630, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] Begin rule write migration with `POST /api/forwards` (v1.14 Slice 14) — migrated the **first rule write/mutation endpoint** into `server/sources/nest/api/forwards/` (the lowest-risk write — no start/stop lifecycle). Creates a rule via a narrow `ForwardRuleCreator`/`FORWARD_RULE_CREATOR` (the domain `ForwardManager.addRule` satisfies it); returns `201` + the created `ForwardRuleResponse` (rule + advisories), matching Express. **Request body DTO** `CreateForwardRuleBodyDto` is **documentation/typing only** — validation is delegated to the shared `validateForwardRule` inside `addRule` (the single contract validator), so **no validation pipe runs on the body** (documented parity exception; re-expressing it as class-validator would risk message/coercion/`id` drift). **Response DTO** `ForwardRuleResponseDto`; advisory decoration via the new shared `toForwardRuleResponse` used by both list + create. **Errors** via the shared layer: manager `ValidationError`→`400`, `ConflictError`→`409` (duplicate binding) translated by `mapManagerError` (+ new `ApiConflictException`); persist failure → generic `500` (Slice-3 behavior, documented). **Byte-for-byte parity** for valid TCP/UDP create (fixed `id` → deterministic), missing field/invalid port/invalid protocol (`400`), duplicate binding (`409`), and GET-after-create; UDP first-class (no `allowUdp`). `addRule` starts the forwarder only for `enabled:true` (matching Express); parity tests use `enabled:false` (no sockets). **OpenAPI** documents the POST (body/`201`/`400`/`409`); `docs/api/openapi.json` regenerated; generator test asserts it. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and its `POST /api/forwards` is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed code**; 203 nest tests (25 added); server coverage 99.2%/96.0%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 612, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] Generated NestJS OpenAPI documentation pipeline — confirmed (v1.14 Slice 13) — the generated OpenAPI pipeline (delivered as the additional Slice-12 requirement) **confirmed to meet the full Slice-13 spec**, with the generator tests **hardened** to the slice's explicit checklist: added an assertion that `GET /api/activity` documents its query params (`limit`/`ruleId`/`type`/`severity`, all optional) **despite endpoint-local coercion** (no DTO), an explicit volatile-endpoint presence check (`/api/runtime`/`/api/config/export`/`/api/connections` → their response schemas), and a **clean-shutdown** test proving `generateOpenApiDocument` always closes the Nest app (via a minimal injectable app-factory seam `generateOpenApiDocument(createApp = createNestApp)`; production calls with no arg). 3 tests added (14 in `openapi.test.ts`); `openapi.ts` stays 100% covered. No new endpoints, no runtime change, committed `docs/api/openapi.json` unchanged. `validate:contract` **234/234**; server coverage 99.1%/95.9%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 587, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] Generated API documentation from NestJS — OpenAPI (v1.14 Slice 12) — added `@nestjs/swagger` + an **offline** generator (`npm run generate:apidoc`, root→`-w server`) that builds an OpenAPI 3 doc from Nest controller/DTO metadata into the **tracked** `docs/api/openapi.json` (deterministic + idempotent; an `openapi.test.ts` drift guard fails CI if stale). Swagger metadata on every migrated endpoint (`@ApiTags`/`@ApiOperation`/`@Api*Response`/`@ApiQuery`); decorated `@ApiProperty` schema classes in `nest/common/api-schemas.ts` (each `implements` its `@portier/shared` type so drift is a compile error) for the error envelope `ApiErrorResponseDto` (`{errors:string[]}`), all 4 response bodies, and the reusable item shapes — generated doc has 8 endpoints + `/health` and 13 schemas, `DELETE /api/activity` documents `204`/no-body, `GET /api/ports/advisory` documents its query params + the 400 envelope. Schema classes are metadata-only (`api-schemas.ts` coverage-excluded as logic-free decorator metadata — a single named file); the response **mappers** stay in `*.response.dto.ts` and remain **100% covered**; the generator helpers (`openapi.ts`) are 100% covered, only the logic-free `generate.ts` entry excluded. No Swagger UI/`/docs` route, no runtime change, Express stays default + Nest shadow-only. **DTO durable rule updated:** future endpoint migrations must include OpenAPI metadata + decorated DTO in the same slice and regenerate `docs/api/openapi.json`. `validate:contract` **234/234**; server coverage 99.1%/95.9%/100% (gate PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 584, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared files touched).
- [x] NestJS read-migration consistency audit (v1.14 Slice 12) — **PASS WITH NOTES**; audit + small remediation only, no new endpoints, no behavior change. Verified module structure (module → controller → service → narrow reader/token), provider/token consistency, request/response DTO rule (response DTO + pure mapper on every body-returning `/api` endpoint; documented input exceptions: `PortsAdvisoryQueryDto`+pipe the only validated input, `GET /api/activity` endpoint-local coercion, `/health`/`DELETE` no-input), the three shared builders each called by **both** Express and Nest, the volatile parity pattern (one production-invisible `AppOptions.now` seam; fixed-clock/seeded-record parity with **no normalization/stripping**), the global `ApiErrorEnvelopeFilter` (only `/api/*`; non-API 404 keeps NestJS default), byte-for-byte parity tests for all 8 migrated endpoints (unknown-route probe uses `/api/not-migrated`), and **no coupling** (no Nest production import of the Express app factory/routes, Go internals, CLI, or replay; `createApp` only in parity tests). **One fix:** extracted `ActivityReader`/`ActivityClearer`/`ACTIVITY_STORE` from `activity.service.ts` into a new `activity.reader.ts` so all six runtime-backed features keep their reader/token in a `*.reader.ts` file (behavior-identical; 164 nest tests unchanged; new/changed activity files 100% covered). **Notes (intentional):** `/health` is a scaffold probe outside `/api` (typed constant, no mapper); `ACTIVITY_STORE` is a read+write store token (not `*_READER`). Added an endpoint inventory table to `server/readme.md`. `validate:contract` **234/234**; server coverage 99.1%/95.9%/100% (gate 95/92/99 PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 573, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared-validation files touched). Express remains the default runtime; Nest stays shadow-only. **Read-side migration complete — next milestone is write/lifecycle/static, not more read endpoints.**
- [x] Migrate `GET /api/connections` — volatile `generatedAt` parity (v1.14 Slice 11) — migrated the **last** deferred volatile read endpoint into `server/sources/nest/api/connections/` (`ConnectionsController` → `ConnectionsService` → narrow `ConnectionsReader`/`CONNECTIONS_READER` + shared clock); read-only, always `200`, returns `LiveConnectionsResponse` (`{ generatedAt, tcpConnections, udpSessions, ruleSummaries }`). No write/lifecycle migrated; **no real sockets/listeners/forwarders in any test.** **Shared pure builder:** extracted the route's inline per-rule aggregation into `buildLiveConnections({ rules, tcpConnections, udpSessions, now })` in `server/sources/connections-snapshot.ts`; both the Express route and the Nest service call it (no drift; field order + `lastTrafficAt` logic preserved; `now` injected; arrays passed through). **Clock reuse:** injects the shared `ClockReader`/`CLOCK_READER`; the Express route passes the existing `AppOptions.now` seam — one seam now serves all three volatile endpoints (`uptimeSeconds`/`exportedAt`/`generatedAt`). **Exact parity (no normalization, no sockets):** parity test boots Express (`createApp(manager, { now: () => FIXED_NOW })`, via a small `SeededManager` whose connection-read methods return fixed records) and Nest (override `CLOCK_READER` → fixed clock, `CONNECTIONS_READER` → a seeded fake with fixed `TcpConnectionInfo`/`UdpSessionInfo`) → `diffApiResponses === []` byte-for-byte for empty + a seeded snapshot (1 TCP + 1 UDP → 2 rule summaries); every field (incl. `durationMs`/`idleMs`) deterministic, nothing stripped/normalized. Response DTO `ConnectionsResponseDto` + mapper (fresh object + fresh arrays of fresh records); **no request DTO** (no input — documented). `GET /api/ports/advisory`/`GET /api/activity`/`DELETE /api/activity`/`GET /api/status`/`GET /api/forwards`/`GET /api/runtime`/`GET /api/config/export` parity, `/health`, `/api/*` 404 envelope, non-API no-leak all still pass. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and serves identical output. `validate:contract` **234/234**. **100% coverage of all new/changed code** (`connections-snapshot.ts` incl. both sort-comparator arms + the connections reader/service/controller/module/response DTO); 164 nest tests (10 added) + 9 builder unit tests; server coverage 99.1%/95.9%/100% (gate 95/92/99 PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 573, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared-validation files touched). **All deferred volatile read endpoints are now migrated.**
- [x] Migrate `GET /api/config/export` — volatile `exportedAt` parity (v1.14 Slice 10) — migrated the second volatile read endpoint into `server/sources/nest/api/config/` (`ConfigExportController` → `ConfigExportService` → narrow `ConfigExportReader`/`CONFIG_EXPORT_READER` + shared clock); read-only, always `200`, returns `ExportedConfig` (`{ version, exportedAt, rules }`). **Read-only by design** — the `config.exported` activity emission (a write side-effect) stays with the Express manager, deferred with config-write; **no config import/write migrated.** **Shared pure builder:** extracted `buildExportedConfig({ rules, now })` into `server/sources/config-export.ts`; the Express `ForwardManager.exportConfig()` builds via it (then emits activity, unchanged) and the Nest service builds via the same builder (no drift; field order preserved; `now` injected; rules passed through). **Clock reuse:** promoted `ClockReader`/`CLOCK_READER`/`defaultClockReader` to `server/sources/nest/common/clock.reader.ts` (runtime reader re-exports it, Slice-9 imports unchanged); `exportConfig(now = new Date())` gained an optional clock arg, and the Express route passes the existing Slice-9 `AppOptions.now` seam — one seam serves both volatile endpoints. **Exact parity (no normalization):** parity test boots Express (`createApp(manager, { now: () => FIXED_NOW })`) and Nest (override `CLOCK_READER` → fixed clock, `CONFIG_EXPORT_READER` → the same seeded manager) → `diffApiResponses === []` byte-for-byte for empty + two seeded stopped rules; no field stripped/normalized. Response DTO `ConfigExportResponseDto` + mapper (fresh object + fresh rule array); **no request DTO** (no input — documented). `GET /api/ports/advisory`/`GET /api/activity`/`DELETE /api/activity`/`GET /api/status`/`GET /api/forwards`/`GET /api/runtime` parity, `/health`, `/api/*` 404 envelope, non-API no-leak all still pass. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and serves identical output. `validate:contract` **234/234**. **100% coverage of all new/changed code** (`config-export.ts` + `common/clock.reader.ts` + config reader/service/controller/module/response DTO); 154 nest tests (10 added) + 4 builder unit tests; server coverage 99.1%/95.6%/100% (gate 95/92/99 PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 554, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared-validation files touched).
- [x] Volatile read-endpoint parity pattern + migrate `GET /api/runtime` (v1.14 Slice 9) — migrated the first endpoint with **volatile fields** (`uptimeSeconds`, process `pid`/`platform`/`arch`) into `server/sources/nest/api/runtime/`, establishing the time/process provider strategy before `/api/connections`/`/api/config/export`. **Shared pure builder:** extracted `buildRuntimeInfo` + `normalizePlatform`/`normalizeArch` + `RuntimeInfoOptions` into `server/sources/runtime-info.ts` (framework-free); **both** the Express route and the Nest `RuntimeService` call it (no drift), preserving the historical Express defaults and contract field order — `api.ts` re-exports the moved symbols (behavior-preserving). **Volatile providers:** three narrow readers in `runtime.reader.ts` — `ClockReader`/`CLOCK_READER` (generic `now()`, reusable for `generatedAt`/`exportedAt`), `ProcessReader`/`PROCESS_READER` (`pid`/`platform`/`arch`), `RuntimeInfoReader`/`RUNTIME_INFO_READER` (static launch metadata + a once-at-construction start time, mirroring Express's `runtimeStartedAt`); production reads the real clock/process, the scaffold default has no runtime info wired. `RuntimeController` maps via `toRuntimeInfoResponseDto` (response DTO); **no request DTO** — endpoint has no input (documented exception). **Exact parity (no normalization):** added a minimal optional production-invisible clock seam to Express `AppOptions` (`now?: () => Date`, defaults to the real clock — like the existing `runtimeInfo.startedAt` test seam); the parity test boots the **real Express app and the Nest app with the same fixed clock + runtime info** (PROCESS_READER left as the real default so `pid`/`platform`/`arch` match in-process), so `uptimeSeconds` is deterministic (`30`) and `diffApiResponses(...) === []` byte-for-byte — **no field stripped/normalized**. `GET /api/ports/advisory`/`GET /api/activity`/`DELETE /api/activity`/`GET /api/status`/`GET /api/forwards` parity, `/health`, `/api/*` 404 envelope, non-API no-leak all still pass. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and serves identical output. `validate:contract` **234/234**. **100% coverage of all new/changed code** (`runtime-info.ts` + runtime reader/service/controller/module/response DTO); 144 nest tests (11 added) + 6 `runtime-info` unit tests; server coverage 99.0%/95.6%/100% (gate 95/92/99 PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 540, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared-validation files touched).
- [x] First NestJS write endpoint (`DELETE /api/activity`) + explicit request/response DTOs (v1.14 Slice 8) — migrated the first mutation endpoint (`server/sources/nest/api/activity/`): clears the in-memory activity log, returns `204` empty matching Express. **Selected** as the lowest-risk write (paired mutation for the migrated `GET /api/activity`, reuses `ACTIVITY_STORE`, no forwarder lifecycle/sockets/rule-persistence/import-export/manager). Added a narrow `ActivityClearer { clear(): void }` interface beside `ActivityReader`; `ActivityService` narrows to `ActivityReader & ActivityClearer`; `ActivityController.clear()` is `@Delete() @HttpCode(204)` returning `void` (`@HttpCode(204)` required — Nest defaults `DELETE` to `200`). **Corrected DTO rule (request + response):** every migrated endpoint now has explicit **response DTOs + pure mappers** (the HTTP boundary): `PortsAdvisoryResponseDto`/`ActivityListResponseDto`/`StatusListResponseDto`/`ForwardsListResponseDto` + `to*ResponseDto` mappers — controllers map the service result through the mapper (a fresh copy), not raw domain objects; mappers preserve Express JSON byte-for-byte (array-vs-object, names, empties) and the `@portier/shared` types are the documented REST-contract shape (no internal leaks). Request DTOs: `GET /api/ports/advisory` keeps `PortsAdvisoryQueryDto` + `ApiValidationPipe`; `GET /api/activity` query stays endpoint-local coercion (documented no-validation parity exception); `/api/status`/`/api/forwards`/`DELETE /api/activity` have no request input → no request DTO (documented); `DELETE` is `204` no-body → no response DTO (documented). Parity byte-for-byte: DELETE populated + empty → `{204, null}` identically to Express (shared `ActivityStore` instance), subsequent `GET` → `{ events: [] }` in both runtimes (proves clear); all read parity (`GET /api/activity` ×10, `/api/ports/advisory`, `/api/status`, `/api/forwards`) still byte-for-byte after the mapper change; `/health`, `/api/*` 404 envelope, non-API no-leak all still pass. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code** (4 mappers + 5 controllers + activity service); each mapper tested byte-for-byte / empty + populated / no-source-mutation / fresh-object; 133 nest tests (13 added); server coverage 99.0%/95.5%/100% (gate 95/92/99 PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 523, client 449, all 12 Go packages). **Replay validation skipped** (no replay/shared-validation files touched).
- [x] DTO-based NestJS API request-validation foundation (v1.14 Slice 7) — added proper Nest DTO validation with `class-validator` + `class-transformer` + a validation pipe. **`PortsAdvisoryQueryDto`** (`api/ports/ports-advisory.query.dto.ts`) declares Express-faithful coercion/validation: `port` via `@Transform(Number)` + `@IsInt`/`@Min(1)`/`@Max(65535)` (all three sharing the exact Express message), `purpose` via `@IsIn(["management","forward"])`, `listenHost` via `@Transform(typeof === "string" ? value : undefined)` (non-string → undefined, never errors). **`ApiValidationPipe`** (`common/api-validation.pipe.ts`) runs `plainToInstance` + `validate` (`whitelist: true` — extra params ignored not rejected, matching Express; `stopAtFirstError: true` — one message per field) and converts failures to `ApiBadRequestException(messages)` → `toApiError` → `400 { errors }`. The pipe takes the **DTO class explicitly** (`@Query(new ApiValidationPipe(Dto))`), not via `design:paramtypes` reflection (esbuild/Vitest doesn't emit decorator metadata — same reason as explicit `@Inject` tokens), applied **per-route** (no global pipe → non-API routes unaffected). `PortsController` delegates to `PortsService.getAdvisories(dto)` (validation in the pipe; service computes via shared `getPortAdvisories`). **`GET /api/activity` kept endpoint-local (documented exception):** it has no validation errors (pure silent coercion-with-fallback, always `200`), so a class-validator DTO would add ceremony without value and risk drift — `parseActivityLimit`/`typeof` coercion stays in the service, unchanged. Parity byte-for-byte for `/api/ports/advisory` (valid / `70000` / `1.5` / `abc` / invalid-purpose / missing-port / extra-param); `/api/activity` + `/api/status` + `/api/forwards` parity, `/health`, `/api/*` 404 envelope, non-API no-leak all still pass. (An entirely-empty both-fields-invalid query accumulates all field errors vs Express's first-error — not a contract/required case, Nest shadow-only.) Added `class-validator`/`class-transformer` (server only; no new advisories). `validate:contract` **234/234** (Express untouched). **100% coverage of all new/changed Nest code** (DTO, pipe, ports service/controller); 120 nest tests; server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS, none lowered); lint/typecheck/build clean; full `npm run test` green (shared 105, server 510, client 449, all 12 Go packages). JSON-body DTO validation deferred to the write slice. **Replay validation skipped** (no replay/shared-validation files touched — per the v1.14 scope correction).
- [x] Migrate `GET /api/forwards` into NestJS (v1.14 Slice 6) — migrated the next manager-backed read endpoint, **`GET /api/forwards`** (`server/sources/nest/api/forwards/`): `ForwardsController` → `ForwardsService` (maps each rule to `ForwardRuleResponse` = `{ ...rule, advisories }` via the shared `getPortAdvisories`, exactly as Express's private `toRuleResponse`) over a narrow **`ForwardsReader { listRules(): ForwardRule[] }`** behind the **`FORWARDS_READER`** token (default = trivial `emptyForwardsReader`). Returns `ForwardRuleResponse[]` verbatim; read-only, always `200`. Only the **list read** migrated — write/lifecycle routes under `/api/forwards/...` stay with Express. **Exact-parity-safe** (static rule fields + deterministic advisories, no volatile fields); reuses the Slice-5 manager-read provider/fake pattern; no contract constants duplicated (reuses `@portier/shared` `getPortAdvisories`). Parity proven byte-for-byte for an empty manager and a seeded manager (two stopped rules, incl. a `0.0.0.0` rule → `LAN_EXPOSURE` advisory) by sharing the same `ForwardManager` instance between Express (`createApp(manager)`) and Nest (`FORWARDS_READER` overridden via `@nestjs/testing` `overrideProvider`). No sockets/forwarders started. `GET /api/ports/advisory` + `GET /api/activity` + `GET /api/status` parity, `/health`, `/api/*` 404 envelope, non-API no-leak all still pass (the Slice-1 "unknown /api route" probe moved from `/api/forwards` → `/api/not-migrated`). Still scaffold-only under `npm run start:nest`; Express remains the default runtime and is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code** (forwards reader/service/controller/module + `app.module.ts`); 118 nest tests (9 added); server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS, none lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; full `npm run test` green (shared 105, server 508, client 449, all 12 Go packages).
- [x] Migrate `GET /api/status` into NestJS — runtime/manager read-provider pattern (v1.14 Slice 5) — migrated the first **manager-dependent** read endpoint, **`GET /api/status`** (`server/sources/nest/api/status/`): `StatusController` → `StatusService` (returns the injected reader's `listStatus()`) over a narrow **`StatusReader { listStatus(): ForwardStatus[] }`** behind the **`STATUS_READER`** token. Response is the `ForwardStatus[]` array verbatim; read-only, always `200`. **Selected** as the smallest manager-dependent read with **no volatile fields** — `startedAt` only appears for *running* rules and counters are `0`/absent when stopped, so seeding **stopped** rules (which endpoint tests must do anyway — no sockets) gives **exact byte-for-byte parity** without normalization (`/api/config/export`/`/api/connections` carry timestamps → skipped). **Read-provider pattern:** production status code holds no manager/store dependency — `STATUS_READER` defaults to a trivial `emptyStatusReader` (`{ listStatus: () => [] }`); the domain `ForwardManager` satisfies `StatusReader` and is bound in tests / when Nest becomes active. Parity proven byte-for-byte for an empty manager and seeded (2 stopped tcp/udp rules) by sharing the **same `ForwardManager` instance** between Express (`createApp(manager)`) and Nest (`STATUS_READER` overridden via `@nestjs/testing` `overrideProvider`). Added `@nestjs/testing` (server devDependency, Nest 11) — a `ForwardManager` needs a backing store and can't be cleanly seeded via `app.get` like the mutable `ActivityStore`; seeded manager stays in test code; no new npm advisories. No real sockets/forwarders created (rules stopped). `GET /api/ports/advisory` + `GET /api/activity` parity, `/health`, `/api/*` 404 envelope, non-API no-leak all still pass. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code** (status reader/service/controller/module + `app.module.ts`); 109 nest tests (8 added); server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS, none lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; TS workspaces green (shared 105, server 499, client 449); all 12 Go packages pass in isolation (the parallel-run flake of `TestTCPForwarderEmitsConnectionClosedEvent` is a pre-existing socket-timing issue, unrelated — Go untouched).
- [x] Migrate `GET /api/activity` into NestJS (v1.14 Slice 4) — migrated the next safe read-only endpoint, **`GET /api/activity`** (`server/sources/nest/api/activity/`): `ActivityController` (extracts optional `limit`/`ruleId`/`type`/`severity`) → `ActivityService` (same `limit` default-100/clamp-500 + pass-through filter coercion as Express; delegates to the injected store). Read-only, **always `200 { events }`** (no error branches); only the read migrated — `DELETE /api/activity` (mutation) deferred. **Selected over `GET /api/runtime`** because activity yields **true byte-for-byte parity** (no inherently time/process-varying fields like runtime's `uptimeSeconds`/`pid` that would force a parity normalization) and best establishes the **inject-dependency-behind-a-narrow-token/fake** DI pattern (`ActivityReader` interface + `ACTIVITY_STORE` token; module defaults to a fresh domain `ActivityStore`). Parity test seeds the Nest app's own store via `app.get(ACTIVITY_STORE)` and shares that instance with Express → identical data + `list()` logic = byte-for-byte parity (**no `@nestjs/testing` dependency**). Express↔Nest parity proven across 10 query variations (no filter / `limit` valid/zero/non-numeric/over-max / each filter / unknown-ruleId→`[]` / combined); `/health`, `/api/*` 404 envelope, non-API no-leak, and `GET /api/ports/advisory` parity all still pass. Still scaffold-only under `npm run start:nest`; Express remains the default runtime and is unchanged. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code** (activity controller/service/module + `app.module.ts`); 101 nest tests (26 added); server coverage 99.0%/95.4%/100% (gate 95/92/99 PASS, none lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; full `npm run test` green (shared 105, server 491, client 449, all 12 Go packages).
- [x] Shared NestJS `/api` error-envelope layer (v1.14 Slice 3) — added a reusable error layer under `server/sources/nest/common/` so migrated Nest controllers stop hand-rolling the Portier `{ errors: [...] }` envelope: `api-error-envelope.ts` (pure `toApiError(exception)` + `isApiPath`), a global catch-all `ApiErrorEnvelopeFilter` (`api-error-envelope.filter.ts`, registered via `APP_FILTER`) that envelopes `/api/*` and leaves non-API routes on NestJS defaults, and `ApiBadRequestException(errors: string[])` (`api-errors.ts`) so controllers raise a `400` from a plain `string[]`. `toApiError` is deterministic: explicit `{ errors }` body preserved; unmatched-route `NotFoundException` → `404 ["API route was not found."]`; other `HttpException` → its message; non-HTTP/unknown → `500 ["Internal server error."]` (no stack/raw-exception leak). `GET /api/ports/advisory` now throws `ApiBadRequestException(result.errors)` (no envelope literal in the controller); the Slice-1 `api-not-found.filter.ts` (NotFound-only) was **replaced** by the broader filter, preserving the `/api/*` 404 envelope and non-API no-leak (re-confirmed by the integration + parity tests). Express↔Nest parity still byte-for-byte; Express server unchanged and still the default runtime; the Nest endpoint stays shadow-only under `npm run start:nest`. `validate:contract` **234/234**. **100% coverage of all new/changed Nest code** (mapping, filter, `ApiBadRequestException`, controller) — 75 nest tests covering every `toApiError`/filter branch (incl. the non-API string-body and unknown-error 500 paths via a mock `ArgumentsHost`); server coverage 99.0%/95.0%/100% (gate 95/92/99 PASS, none lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; full `npm run test` green (shared 105, server 465, client 449, all 12 Go packages).
- [x] NestJS API parity harness + first read-only endpoint migration (v1.14 Slice 2) — added a small, server-test-local **API parity harness** (`server/sources/nest/testing/api-parity.ts`: `startHandlerServer`/`startNestServer` boot an Express handler or Nest app on an ephemeral loopback port, `fetchApi` captures `{status,body}`, `stableStringify` recursively sorts keys, `diffApiResponses` reports why two responses differ) and migrated the **smallest safe read-only endpoint — `GET /api/ports/advisory`** into Nest. Selected because it is read-only, deterministic, has **no runtime-manager dependency** (the Express route never touches the manager), wraps the pure shared `getPortAdvisories`, and has clear `400` branches. `PortsController` (transport adapter — parses query params, returns `200` advisories or throws `BadRequestException({errors})` → `400 {errors:[...]}`) → `PortsService.resolveAdvisories` (validates `port` 1–65535 + `purpose` management/forward with the SAME rules as Express, then delegates to `getPortAdvisories`); wired via `PortsModule` into `app.module.ts`. The Nest route is **byte-for-byte identical to the existing Express route** — proven by an Express↔Nest parity test across valid / invalid-port / invalid-purpose / missing-port cases. **Still scaffold-only:** the migrated route is served only under `npm run start:nest`; the **Express server remains the default active runtime** and its `/api/ports/advisory` is unchanged. `validate:contract` **234/234** (no contract change). **100% coverage of all new Nest code** (parity harness, ports controller/service/module — 100% statements/branch/functions); 62 nest tests (31 added): harness units (incl. the `fetchApi` empty-body branch), `PortsService` branch table, `PortsController` 200/400 branches, and a real-Nest integration + parity test that re-confirms `/health`, the `/api/*` 404 envelope, and no-envelope-leak on non-API 404s. Server coverage 98.9%/94.9%/100% (gate 95/92/99 PASS, none lowered); `validate:coverage:replay` PASS (96.5%); lint/typecheck/build clean; full `npm run test` green (452 + 449 TS tests, all 12 Go packages). **Existing Express runtime and scripts unchanged.**
- [x] NestJS server migration scaffold (v1.14 Slice 1) — added an incremental, reversible **NestJS scaffold under `server/sources/nest/`** that does **not** replace the existing Express server (`sources/index.ts` + `sources/api.ts` stay the active runtime; their scripts are unchanged). Scaffold surface: `GET /health` → `{ ok: true, server: "node", name: "Portier" }` (a liveness probe that needs no runtime manager, deliberately **outside the frozen `/api` contract** — `/api/health` parity is deferred to the endpoint-migration milestones), plus a global `ApiNotFoundFilter` that returns the contract envelope `{ "errors": ["API route was not found."] }` for any unmatched `/api/*` route (non-API 404s keep NestJS's default body). Layered structure: `health/` (controller = transport only, service = behaviour, module), `common/api-not-found.filter.ts`, `app.module.ts` (registers the filter via `APP_FILTER`), `app.factory.ts` (`createNestApp()` — no listener, shared by `main.ts` and tests), `main.ts` (bootstrap: binds `127.0.0.1` only, default port `47832` to avoid colliding with the management `47831`). Dependencies (server package only, Nest 11 aligns with the existing express 5): `@nestjs/common`/`@nestjs/core`/`@nestjs/platform-express` `^11`, `reflect-metadata`, `rxjs`. Scripts (additive, none renamed/removed): `build:nest`, `start:nest`, `test:nest`. Build output stays under `build/` (`server/build/nest/…`), not `dist/`. DI uses explicit `@Inject` tokens so it works identically under `tsc` and the esbuild-based Vitest transform (no reliance on emitted metadata). Startup logic is extracted into covered files — `nest-options.ts` (`resolveNestListenOptions`) and `bootstrap.ts` (`bootstrap`/`reportBootstrapFailure`) — so `main.ts` is a logic-free process entry. Tested meaningfully — 31 tests: service/controller units, filter units (`isApiPath` table + contract-envelope vs default-404 branches), `resolveNestListenOptions` branch table (defaults/custom host/valid port/invalid→default), `bootstrap`/`reportBootstrapFailure` (ephemeral-port boot → `/health`; failing exit code on error), and an **integration test that boots the real Nest app** on an ephemeral loopback port and asserts `/health`, `/api/*` 404 envelope, no-envelope-leak on non-API 404, and 127.0.0.1 binding. **Every `sources/nest/` file is 100% covered** (statements/branch/functions); the ONLY exclusion is the logic-free `main.ts` entry (importing it starts a listener — same precedent as `index.ts`), no broad exclusions; server coverage 98.6%/94.1%/100% (gate 95/92/99 PASS, none lowered). `validate:contract` **234/234** (no API/contract change — the existing server is untouched); `validate:coverage:replay` still PASS (96.5%); lint/typecheck/build clean. **Existing TypeScript server behavior and scripts are unchanged.**
- [x] Replay toolkit consistency audit (v1.13 Slice 7) — **PASS, v1.13 replay toolkit ready for finalization**. Focused audit of the standalone replay tool (Slices 1–6) — audit/cleanup only, no new features, no behavior change. **Command surface/help:** all five commands (`plan`/`analyze`/`timeline`/`compare`/`explain` + `explain --code`/`--list`) dispatch consistently; uniform `--json`/`--out` + byte-parity; exit codes `0`/`1`/`2` (no `3`) — added an **Exit codes** block to the tool help + a help regression test (every command + exit-code docs). **JSON shapes:** all five local schemas `schemaVersion:1`, arrays `[]`-not-null, deterministic (every renderer iterates ordered slices — verified **no map iteration**), timeline always marks `synthetic`, explain preserves unknown codes `known:false`, mixed-kind compare clearly limited, consistent source-kind names (`workflow-run-report`/`workflow-plan-report`/`workflow-history-export`/`workflow-report-bundle` + `replay-analysis`/`replay-timeline`/`replay-compare`); no replay schema leaks into REST contract. **Safety/architecture (inspection):** no `tools/cli`/runtime-client/`net/http`/`os/exec`/`os.Getenv` in product code; `core` never imports `commands`; bundle dirs read only `manifest.json`/`report.json`/`explanations.json`; no referenced-file reads, no mutation beyond `--out`, no telemetry/upload/log/env/process collection; full per-command regression matrix confirmed (human/JSON/`--out`/`--json --out`-parity/out-write-failure/stdout-write-failure/no-mutation/no-referenced-read for all five). **Fixes (docs + tests, no behavior change):** stale `tools/replay/README.md` intro (Slice-1 scaffold language → five-command tool), completed bundle-files statement (manifest/report/explanations + malformed-report tolerance), command-generic Exit-codes section + "no exit 3", refreshed root `README.md` replay section (all five commands); added **malformed-bundle-report-tolerated** tests for `timeline` + `compare` (was tested only for `analyze`). **Validation:** `validate:contract` 234/234; replay module-wide coverage **96.5%** (core 96.7%, commands 94.9%; `go test -coverpkg=./...`) ≥95% bar; `test:replay`/`build:replay`/gofmt/vet/lint/typecheck clean. No release blockers; no new durable rule (confirms Slice 1–6 rules). **Finalization:** added a hard **95% module-wide coverage gate** for `tools/replay`, enforced in `scripts/validate-coverage.js` (new `replay` component in `GATES`, run via `npm run validate:coverage:replay` and the aggregate `npm run validate:coverage`); coverage is gathered module-wide by the new reporter `scripts/coverage-tools-replay.js` (`go test -coverpkg=./... ./...`, `npm run coverage:replay`, modeled on `coverage-tools-cli.js`) which writes `coverage/replay/coverage-summary.json` for `validate-coverage.js` to gate; passes at 96.5%, exits non-zero if forced above actual (verified); replay is a separate Go module gated independently — existing CLI/workspace gates unchanged
- [x] Replay explain command (v1.13 Slice 6) — added `replay [--json] explain --from <file-or-dir> [--out <file>]` (+ `--code <code>` / `--list`, mutually exclusive), explaining the emitted codes a saved artifact records (offline). Inputs: the four workflow artifacts **plus** replay analysis/timeline/compare JSON (shape-detected via `replayOutputCodes`); workflow-artifact codes reuse the existing detection + `factsFromJSON`/`bundleFacts` (no new parsing). Codes deduped + **sorted ascending**; **unknown codes preserved + marked `known:false`**, never dropped; no-codes artifact → exit 0 with a clear notice. Local explanation registry (`tools/replay/sources/core/explain.go`, **24 codes**: 15 `workflow.step.*` + 3 `workflow.run.*` + 6 `policy.*`) — does NOT import the CLI explain registry or any runtime package; doctor/runtime codes out of scope (never appear in workflow artifacts → reported unknown). Model (`schemaVersion:1`, local tool schema separate from plan/analysis/timeline/compare): `{source, codeCount, known, unknown, explanations:[{code,known,title,severity,meaning,suggestion}]}`; `explanations` always `[]`. Renderer + single JSON marshaller (`core/render_explain.go`) so `--json --out` is byte-identical; runner `commands/explain.go` (one of `--from`/`--code`/`--list` required, >1 → exit 2); dispatch + help updated. Exit: `0` produced (incl. no codes); `1` output-write/JSON-encode failure; `2` usage error or unreadable/malformed/unsupported artifact; **no `3`**. **Strictly offline & read-only** — never executes workflows, contacts the runtime, reads referenced config/policy/baseline/report files (test-proven), mutates inputs (before/after byte-compare), applies/imports, enforces policy, runs shells, uploads, or collects logs/env/process data. Covered by white-box `core/explain_test.go`/`render_explain_test.go` (registry integrity: 24 entries, Code==key, valid severity, non-empty fields; knownCodes sorted; build known/unknown/no-codes; run/plan/history extraction + dedupe + sort; replay analysis/timeline/compare detection; not-a-replay-output; file/bundle/missing-path; `--code` known/unknown; `--list`=24; `sortedDedupeStrings`; deterministic; human/JSON shape; `[]`-not-null) + black-box `commands/commands_explain_test.go` (run human+JSON, history no-codes→0, unknown-code preserved, replay-analysis-output, `--code`/`--code`-unknown/`--list`=24, no-mode→2, multi-mode→2, unreadable/malformed/unsupported/bad-flag→2, `--out`/`--json --out` byte parity + out-write-failure→1 + stdout-write-failure→1, no-input-mutation, does-not-read-referenced-files, **plan/analyze/timeline/compare still work**). **Replay module-wide coverage 96.5%** (core 96.7%, commands 94.9%; `go test -coverpkg=./...`) — meets the ≥95% bar; remaining gaps are structurally-unreachable defensive branches + the `main()` `os.Exit` wrapper. `validate:contract` 234/234; gofmt/vet/lint/typecheck clean
- [x] Replay compare command (v1.13 Slice 5) — added `replay [--json] compare --left <file-or-dir> --right <file-or-dir> [--out <file>]`, comparing two saved artifacts offline (analysis only). Built on a single-parse `compareFacts` extractor (`tools/replay/sources/core/compare.go`) that reuses existing detection (`DetectJSON`/`DetectBundleManifest`) + the timeline raw structs + shared helpers (`tallyStep`/`embeddedFindingCodes`/`dedupeStrings`/`sortedMapKeys`/`firstNonEmpty`). **Same-kind** diffs: run-vs-run (workflow/result change, step count deltas, added/removed/unchanged code + failed-step + skipped-step sets), plan-vs-plan (validity change incl. "Plan became valid/invalid.", step valid/invalid deltas, code + invalid-step sets), history-vs-history (run-count + passed/failed deltas, workflow-distribution deltas sorted by name asc, code set, failed-run set, most-recent-run change, increased/decreased insights), bundle-vs-bundle (manifest/report workflow/result + step deltas + code set). **Mixed-kind** → deterministic limited result (source kinds + "different kinds; detailed comparison is limited." + `replay analyze` pointer), never a crash. Model (`schemaVersion:1`, local tool schema separate from plan/analysis/timeline): `{left, right, summary{sameKind,changed,changeCount}, changes[], sets{<dim>:{added,removed,unchanged}}, insights[]}`; changes in stable semantic order (no map iteration; workflow deltas over a sorted key union), set slices always `[]` not null. Renderer + single JSON marshaller (`core/render_compare.go`) so `--json --out` is byte-identical; runner `commands/compare.go`; dispatch + help updated. Exit: `0` created; `1` output-write/JSON-encode failure; `2` usage error (missing `--left`/`--right`, bad flag) or unreadable/malformed/unsupported artifact; **no `3`**. **Strictly offline & read-only** — never executes workflows, contacts the runtime, reads referenced config/policy/baseline/report files (test-proven), mutates inputs (before/after byte-compare on both sides), applies/imports, enforces policy, runs shells, uploads, or collects logs/env/process data. Covered by white-box `core/compare_test.go`/`render_compare_test.go` (passed↔failed run, code added/removed/unchanged, skipped-step change, equal=no-changes, valid↔invalid plan, plan code add/remove, empty↔populated history, failed-count increase/decrease, both-empty, workflow-delta ordering, bundle pair, mixed-kind, deterministic, `diffSets`, file/bundle/unreadable/malformed/unsupported, removed-items + no-changes + mixed-kind render, `[]`-not-null) + black-box `commands/commands_compare_test.go` (run/history human+JSON, mixed-kind, missing-`--left`/`--right`/unreadable-left/unreadable-right/malformed/unsupported/bad-flag→2, `--out`/`--json --out` byte parity + out-write-failure→1 + stdout-write-failure→1, no-input-mutation, does-not-read-referenced-files, **plan/analyze/timeline still work**). **Replay module-wide coverage 96.4%** (core 96.7%, commands 94.7%; `go test -coverpkg=./...`) — meets the ≥95% bar; remaining gaps are structurally-unreachable defensive branches + the `main()` `os.Exit` wrapper. `validate:contract` 234/234; gofmt/vet/lint/typecheck clean
- [x] Replay timeline command (v1.13 Slice 4) — added `replay [--json] timeline --from <file-or-dir> [--out <file>]`, reconstructing a deterministic ordered timeline from a saved artifact (analysis only). Per kind: **run report** → step timeline (status/exit code/codes per saved step; failed → embedded finding codes, skipped → `workflow.run.dependency_failed`); **plan report** → validation timeline (valid/invalid + `workflow.step.*` codes); **history export** → run timeline in **newest-first** stored order (each event: id, createdAt, workflow, result(status), per-run summary, codes); **support bundle dir** → timeline from the bundle's own `manifest.json`/`report.json` only (missing/malformed report → minimal start/result). Every timeline brackets the saved events with clearly-marked **synthetic** lifecycle events (`*-start`/`*-result`/`*-end`); each event carries a `synthetic` boolean and the summary a `synthetic` count; **no timestamps invented** (order by saved order only). Model (`tools/replay/sources/core/timeline.go`, `schemaVersion:1`, a local tool schema separate from plan/analysis): `{source, workflow?, result?, events[], summary{total,passed,failed,skipped,valid,invalid,synthetic}}`; `events` always `[]`. Renderer + single JSON marshaller (`core/render_timeline.go`) so `--json --out` is byte-identical; command runner `commands/timeline.go`; dispatch + help updated; reuses existing detection (`DetectJSON`/`DetectBundleManifest`) and `embeddedFindingCodes`/`dedupeStrings`/`firstNonEmpty`. Exit: `0` created; `1` output-write/JSON-encode failure; `2` usage error or unreadable/malformed/unsupported artifact; **no `3`**. **Strictly offline & read-only** — never executes workflows, contacts the runtime, reads referenced config/policy/baseline/report files (test-proven), mutates inputs (before/after byte-compare test), applies/imports, enforces policy, runs shells, uploads, or collects logs/env/process data. Covered by white-box `core/timeline_test.go`/`render_timeline_test.go` (passed/failed/skipped run, codes, valid/invalid plan, populated/empty history + newest-first order + per-run summary, bundle dir + manifest-only + no-result + wrong-manifest, sequential order, deterministic, malformed/unsupported/missing-path, synthetic-marking, human/JSON shape, `[]`-not-null) + black-box `commands/commands_timeline_test.go` (run/history/bundle human+JSON, `--out`/`--json --out` byte parity, missing-`--from`/unreadable/malformed/unsupported/bad-flag→2, out-write-failure→1, no-input-mutation, does-not-read-referenced-files, **plan & analyze still work**). `replay plan`/`analyze` and the whole CLI unchanged. **Replay module coverage 96.4%** (core 97.0%, commands 94.8%; `go test -coverpkg=./...`) — meets the ≥95% module bar; remaining gaps are structurally-unreachable defensive branches (encode-on-concrete-type, post-detection `json` re-decode, `os.ReadFile`-after-stat, the unused-classifier `default` arm) + the `main()` `os.Exit` wrapper. Added a `failWriter` stdout-write-failure test for `plan`/`analyze`/`timeline` `--json` (a real error path). `validate:contract` 234/234; gofmt/vet/lint/typecheck clean
- [x] Replay tool naming & package-structure polish (v1.13 Slice 3) — behavior-preserving cleanup before the replay surface grows. **Binary renamed `portier-replay` → `replay`** (`replay.exe` on Windows): `npm run build:replay` now outputs `tools/replay/build/replay` (`-o build/replay`, matching `build:cli`'s explicit-name convention); help text, usage, and all command examples now read `replay plan …` / `replay analyze …`. **Domain package `sources/replay` → `sources/core`** (artifact detection + replay plan/analysis models, builders, renderers — files `git mv`'d, `package replay` → `package core`); **CLI dispatch extracted to `sources/commands`** (exported `Run(args, stdout, stderr) int` + `runPlan`/`runAnalyze`/`printHelp`, importing `core`); `main.go` reduced to a thin `os.Exit(commands.Run(...))` entry point. Separation enforced: `commands` depends on `core`, `core` never depends on `commands`, neither imports `tools/cli`. Black-box command tests moved to package `commands` (`run(...)` → `Run(...)`); a new package-`main` `TestBuildOutputName` builds via `go build -o <tmp>/replay .` and asserts a `replay`/`replay.exe` output and **no** `portier-replay`; help tests assert the `replay` banner and absence of `portier-replay`. Behavior-preserving: output, exit codes (`0`/`1`/`2`, no `3`), schemas, and the offline/read-only safety boundary unchanged. Docs/scripts updated (replay README incl. a Structure section, root README, glossary, changelog, roadmap, CLAUDE/AGENTS durable rules). Coverage: `core` 97.5%, `commands` 90.5% (uncovered = the same unreachable marshal/write-error branches), `main` is just the `os.Exit` wrapper; replay module not gated by `validate:coverage`, CLI gate untouched. `validate:contract` 234/234; gofmt/vet/lint/typecheck clean; no stale `sources/replay`/`portier-replay` references outside historical changelog entries
- [x] Replay analyze command (v1.13 Slice 2) — added `replay [--json] analyze --from <file-or-dir> [--out <file>]`, the first real offline analysis command. Inspects a saved artifact and produces deterministic analysis **from the artifact contents only** — reusing Slice 1 detection then decoding richer structures. Per kind: **run report** (step tallies, failed/skipped step IDs, emitted-code distribution from embedded findings/explanations, result-based findings/insights, runtime-unreachable/input-failed detection); **plan report** (valid/invalid tallies, invalid step IDs, `workflow.step.*` codes, readiness); **history export** (run tallies, workflow distribution, code distribution counting **runs containing** each code, failed-run shortlist, most-common code, most-recent run, empty handling); **support-report bundle dir** (step summary + codes from the bundle's own `manifest.json`/`report.json`/`explanations.json` only — no other/referenced file read; malformed report.json tolerated → minimal analysis). Analysis model (`tools/replay/sources/core/analyze.go`, `schemaVersion:1`, a **local tool schema** separate from the replay-plan schema): `{source, workflow?, result?, summary{steps,runs}, codes[], workflows?, findings[], insights[]}` with deterministic sorting (codes/workflows count desc then name/code asc; IDs asc; stable insights), `codes`/`findings`/`insights` always `[]` not null. Human renderer + single JSON marshaller (`render_analysis.go`) so `--json --out` is byte-identical; `main.go` dispatches `analyze`. Exit: `0` analysis created; `1` output-write/JSON-encode failure; `2` usage error or unreadable/malformed/unsupported artifact; **no `3`**. **Strictly offline & read-only** — never executes workflows, contacts the runtime, reads referenced config/policy/baseline/report files (test-proven), mutates inputs (before/after byte-compare test), applies/imports, enforces policy, runs shells, uploads, or collects logs/env/process data. Covered by white-box `analyze_test.go`/`render_analysis_test.go` (passed/failed/skipped run, codes incl. embedded findings + dedupe + ordering, runtime-unreachable/input-failed, valid/invalid plan, empty/populated/all-passed history, history code-counts-runs, bundle dir + manifest-only + wrong-manifest + malformed-report-tolerated, `embeddedFindingCodes`/`dedupeStrings`/`firstNonEmpty`, deterministic, human/JSON shape, byte-not-null) + black-box `main_analyze_test.go` (run/history/bundle human+JSON, `--out`/`--json --out` byte parity, missing-`--from`→2, unreadable/malformed/unsupported→2, bad-flag→2, out-write-failure→1, no-input-mutation, does-not-read-referenced-files, **plan still works**). `replay plan` and the whole CLI unchanged. Replay package coverage **94.1% → 97.5%** (above the 95% target; remaining gaps are unreachable defensive/`json` re-decode/marshal/`main()` branches); `validate:contract` 234/234; gofmt/vet/lint/typecheck clean
- [x] Standalone replay tool scaffold (v1.13 Slice 1) — new **separate offline tool** `replay` (`tools/replay/`, module `portier/replay`, stdlib-only, **not** a `portier` subcommand; does not import `tools/cli` or any runtime client). `replay [--json] plan --from <file-or-dir> [--out <file>]` detects an existing workflow artifact by **JSON/manifest shape (not filename)** — run report (`workflow run --json --out`), plan report (`workflow plan --json --out`), history export (`workflow history export --out`), or support-report bundle **directory** (`workflow report --out`, only its `manifest.json` read) — and prints a deterministic **replay plan** (`schemaVersion:1`, a local tool schema) answering "what offline analysis can this artifact support?". It does **not** replay by re-executing anything. Each plan lists `available` analyses gated on the artifact's inputs (step timeline, step-status/validation summary, history distributions, failed-run shortlist, explanation lookup, summary/report inspection) and `unavailable` analyses — always including the four it never performs: workflow re-execution, runtime probing, referenced-file reread, mutation/enforcement. Human + JSON output; `--json --out` byte-identical. Exit: `0` plan produced; `1` output-write/JSON-encode failure; `2` usage error or unreadable/malformed/unsupported artifact; **no `3`** (never contacts a runtime). **Strictly offline & read-only** — never executes workflows, contacts the runtime, reads referenced config/policy/baseline/report files, mutates inputs, applies/imports, enforces policy, schedules, runs shells, uploads, or collects logs/env/process data. Detection/plan/render in `tools/replay/sources/core` (`detect.go`/`plan.go`/`render.go`); `main.go` is the dispatcher. Covered by white-box `detect_test.go`/`plan_test.go`/`render_test.go` (all source kinds, malformed/unsupported/empty rejection, available/unavailable gating, empty history export, deterministic + inputs-as-`[]`, byte parity) + `main_test.go` (run report/history export/bundle dir human+JSON, `--out`/`--json --out` byte parity, missing `--from`→2, unreadable/malformed/unsupported→2, output-write-failure→1, **no-input-mutation**, **does-not-read-referenced-files**, dispatch). Replay module tests pass (`replay` pkg 97.8%, `main` 90.7%); `validate:contract` 234/234 (no runtime/API/contract change); scripts `build:replay`/`test:replay`/`validate:replay`; `build:clean` removes `tools/replay/build`; gofmt/vet clean
- [x] v1.12 workflow history toolkit consistency audit (v1.12 Slice 6) — **PASS, ready for finalization**. Audited the history toolkit (Slices 1–5): command surface (built-binary battery against a redirected config dir + white-box suite — run `--record-history` passed/failed/invalid-plan, list/stats/show/export/prune/clear dispatch, all offline so a malformed `--url` is ignored), JSON schemas (list `{schemaVersion,filters?,shown,totalStored,runs}`, stats `{schemaVersion,filters?,totalStored,shown,results,steps,workflows[],stepTypes[],codes[]}`, export `{schemaVersion,createdAt,source,runCount,runs,safety}` w/ 8 all-false safety flags + `--json --out` byte parity, prune `{schemaVersion,removed,kept,totalBefore,totalAfter}`; arrays non-nil, filters only when active, no full reports/messages/raw configs/runtime URLs/tokens in stored/exported entries), exit codes (list/stats 0 incl. empty; show 0/unknown-1/missing-2; export 0/usage-2; prune 0 incl. removed-0/no-mode·keep+filter·missing-yes-2; clear 0/missing-yes-2; **no exit 3** anywhere — history never contacts runtime), safety (history.go imports only `output`; no client/commands/os/exec/net/http/os.Getenv; only `run --record-history` writes after a completed run; prune/clear require `--yes`; mutates only the compact store or explicit export `--out`; no workflow execution / referenced-file reads / raw-report scanning / logs-env-process collection / telemetry/upload/sync), and architecture (`commands` handlers-only; model/projection/filter/stats/prune/store in the `workflow` package; no import cycles; `go vet` clean). Fixes (4, small, no behavior change): stale `workflow history` usage line `<list|show|clear>`→`<list|stats|show|export|prune|clear>`; `--json`/`--yes` options notes now mention stats/prune; the `workflow run` `--record-history` help + glossary run-history row now list the full subcommand set. `validate:contract` 234/234; CLI coverage **97.8%** (gate 97 PASS, none lowered); validate:cli/lint/typecheck/gofmt/vet clean. No release blockers; no new durable rule (confirms Slice 1–5 rules)
- [x] CLI workflow history prune (v1.12 Slice 5) — `portier workflow history prune` removes entries from the local history in one of two explicit modes — retention (`--keep <n>` keeps newest N, n>=0, `--keep 0` removes all) or filter (`--result`/`--workflow`/`--code` remove matching, AND-combined). **Destructive, requires `--yes`**; mutates **only the compact local history store** (never the runtime, workflow execution, referenced files, logs/env/process, or configs/policies/reports). `--keep` and filters mutually exclusive; `--limit` NOT accepted. No/ambiguous mode, negative/non-numeric `--keep`, invalid filter, or missing `--yes` → exit 2 with **no mutation**. Empty match (or `--keep n >=` count) removes 0, exit 0; missing history is a no-op that **creates no file**; a no-op prune does not rewrite the store. Human output `Removed`/`Kept`/`Total before`/`Total after`; `--json` → `{schemaVersion, removed, kept, totalBefore, totalAfter}`. Exit: `0` success (incl. removed 0); `1` read/write failure; `2` usage error; no `3`. Prune model/helper in the `workflow` package (`workflow/history.go`: `HistoryPruneOptions`/`HistoryPruneResult`/`PruneHistory` + `HistoryStore.Save` + shared `runMatchesFilters` factored out of `ApplyHistoryFilters`); `commands` handlers-only (`runWorkflowHistoryPrune` validates mode+`--yes`, loads via `loadHistoryStore`, saves only when `removed>0`); never imports `client`/`commands`, never contacts runtime, mutates only the store. Covered by white-box `workflow/history_internal_test.go` (keep newest N, keep 0 removes all, keep>=count, negative-clamp, empty history, filter by result/workflow/code, AND, empty match, no-input-mutation, `Save` round-trip) + `commands/workflow_history_internal_test.go` (keep + keep-0 human, filter prune + AND no-op, empty-match no-rewrite, missing-`--yes`→2-no-mutation, mode-validation table→2, empty/missing history→0-no-file, read-failure→1, write-failure→1 read-only file, usage-error no-mutation, path-resolution→1, JSON-encode→1, flag edges). CLI coverage **97.8%** (gate 97 PASS, not lowered/raised — still short of a stable 98% buffer; new prune functions 100%); `validate:contract` 234/234; lint/typecheck/gofmt/vet clean
- [x] CLI workflow history stats (v1.12 Slice 4) — `portier workflow history stats` summarizes the (optionally filtered) local history by result, workflow name, step status, step type, and emitted code, computed **only from compact local history metadata** (never the runtime, workflow execution, referenced files, or raw reports). Reuses the Slice 3 filters (`--result`/`--workflow`/`--code`/`--limit`) applied **before** stats, AND-combined; no new filter types. Reports `totalStored`/`shown`, result counts (passed/failed), step status counts (passed/failed/skipped), and grouped workflow/step-type/code counts. **Code counts = runs containing the code** (codes are deduped per run), not occurrences. Grouped tables sorted **count desc, then key asc** (deterministic). `--json` → `{schemaVersion, filters?, totalStored, shown, results, steps, workflows[], stepTypes[], codes[]}` — `filters` only when active, grouped arrays always present (`[]` when empty). Human output: optional Filters block + Runs/Steps buckets + grouped tables (empty tables omitted); empty/missing history → short `Result: no history`. Empty history + empty match sets are **success** (exit 0, zero stats). Exit: `0` success; `1` history read failure; `2` invalid filter value; no `3`. Stats model/counting/sorting in the `workflow` package (`workflow/history.go`: `HistoryStats`/`HistoryResultCounts`/`HistoryStepCounts`/`WorkflowCount`/`StepTypeCount`/`CodeCount`/`BuildHistoryStats`/generic `sortCounts`); `commands` handlers-only (`runWorkflowHistoryStats`); list/stats flag parse+validate factored into shared `parseHistoryFilters`, store load into `loadHistoryForRead`, Filters block into `printHistoryFilterBlock`; never imports `client`/`commands`, never contacts runtime, never mutates the store. Covered by white-box `workflow/history_internal_test.go` (empty, all count types, code-counts-runs-not-occurrences, count-desc/key-asc sorting, filtered, empty-match zero stats, no-input-mutation) + `commands/workflow_history_internal_test.go` (empty human/JSON with `[]` arrays, JSON counts+sorting, human shape, per-filter + AND + limit-before-stats, empty-match→0, invalid filters→2, read-failure→1, path-resolution→1, JSON-encode→1, store-not-mutated, flag edges). CLI coverage **97.7%** (gate 97 PASS, not lowered/raised; new stats functions 100%); `validate:contract` 234/234; lint/typecheck/gofmt/vet clean
- [x] CLI workflow history filtering (v1.12 Slice 3) — `portier workflow history list` gains AND-combined filters operating only on the compact local history (never the runtime/referenced files): `--result <passed|failed>` (exact), `--workflow <name>` (exact, case-sensitive), `--code <code>` (run whose `codes` contains the exact code; older/no-longer-explainable codes allowed), `--limit <n>` (newest N after filtering, n>0). Empty match sets exit 0 (incl. unknown `--code`). Human output shows an active **Filters** block + `Summary` (`N shown`/`N total stored`); no-match prints "No workflow history entries matched the filters."; pristine first-run (no filters, nothing stored) keeps its original guidance message. `--json` emits `{schemaVersion, filters?, shown, totalStored, runs}` — `filters` present only when a filter is active, with only active fields (`omitempty`); `runs` always an array. Exit: `0` success (incl. empty match); `1` history read failure; `2` invalid filter value (unknown `--result`, empty `--workflow`/`--code`, non-positive/non-numeric `--limit`); no `3`. Filter model/matching/list-view in the `workflow` package (`workflow/history.go`: `HistoryFilters`/`ApplyHistoryFilters`/`HistoryListView`/`BuildHistoryListView`/`ValidHistoryResult`); `commands` handlers-only (`runWorkflowHistoryList` validates via `fs.Visit` set-vs-default, calls helpers); never imports `client`/`commands`, never contacts runtime, never mutates the store. Covered by white-box `workflow/history_internal_test.go` (no-filter pass-through, per-filter + AND + limit-after-filter + unmatched-code, no-input-mutation, `ValidHistoryResult` allowlist, list-view filters-omitted/included + non-nil empty runs) + `commands/workflow_history_internal_test.go` (result/workflow/code/limit, invalid result/empty workflow/empty code/invalid limits→2, AND composition, JSON shape incl. omitempty filter keys, human Filters+Summary + no-match, empty-history-with-filters, read-failure→1, store-not-mutated). CLI coverage **97.6%** (gate 97 PASS, not lowered/raised; new filter functions 100%); `validate:contract` 234/234; lint/typecheck/gofmt/vet clean
- [x] CLI workflow history export (v1.12 Slice 2) — `portier workflow history export --out <file>` writes a compact, deterministic JSON snapshot of the local workflow history for archiving/sharing. **Fully offline & read-only** — loads the store, builds the snapshot, writes it; never mutates the store, contacts the runtime, executes a workflow, or reads workflow-referenced files. Snapshot `{schemaVersion:1, createdAt (RFC3339 UTC), source:"workflow-history", runCount, runs[(compact, newest-first, codes as-stored)], safety{8 flags all false}}`; the `safety` object explicitly states the exclusions (no raw configs/policies, full reports, logs, env, process data, runtime URLs, tokens). Missing/empty history → valid empty snapshot (`runCount:0`, `runs:[]`), exit 0. Human output confirms file + run count + a fixed safety block; global `--json` prints the same snapshot to stdout **byte-identical** to the file. `--out` parent dir must exist (matches other single-file `--out` exports); existing file overwritten. Exit: `0` written (incl. empty); `1` history read failure / output write failure / JSON-encode failure; `2` missing `--out` value. Export model/projection in the `workflow` package (`workflow/history.go`: `HistoryExport`/`HistorySafety`/`BuildHistoryExport`, createdAt injected); `commands` handlers-only (`runWorkflowHistoryExport` loads store → `BuildHistoryExport` → `output.WritePrettyJSON`/`PrintJSON`); never imports `client`/`commands`, never contacts runtime; reuses stored `HistoryRun` verbatim (no mutation). Covered by white-box `workflow/history_internal_test.go` (empty emits `runs:[]`, populated preserves newest-first + codes, safety all-false + 8 keys present, deterministic createdAt, no-input-mutation) + `commands/workflow_history_internal_test.go` (empty/populated, run count, newest-first, no-sensitive-content, `--json`/file **byte parity**, missing `--out`/missing-value→2, read-failure→1 no file, write-failure→1, store-not-mutated, JSON-encode→1, path-resolution failure, flag edges). CLI coverage **97.6%** (gate 97 PASS, not lowered/raised; new export functions 100%); `validate:contract` 234/234; lint/typecheck/gofmt/vet clean
- [x] CLI local workflow run history (v1.12 Slice 1) — **opt-in** local observability for `portier workflow run`. `workflow run --file <workflow.json> --record-history` records a **compact** entry after a completed run; without the flag nothing is recorded (no background/always-on collection). Stored (compact metadata only): run id, `createdAt` (RFC3339 UTC), workflow name, result, summary counts, compact per-step metadata (`id`/`type`/`status`/`exitCode`), and deduped+sorted emitted explanation/finding codes. **Never** stores raw configs/policies, full embedded reports, file contents, logs, env vars, process data, secrets, runtime URLs, or tokens (step report/message dropped). Bounded retention: latest **100** runs (newest first). Run id `<YYYYMMDDThhmmssZ>-<sanitized-workflow>-<suffix>`. `workflow history list` (newest-first human or `--json {schemaVersion,runs}`), `workflow history show <run-id>` (human/`--json`), `workflow history clear --yes` (requires `--yes`; already-empty = success). Exit codes — run `--record-history`: history write failure is a **warning**, never hides the run (passed→1, failed run keeps 1/3, runtime-unreachable stays 3); an **invalid plan is never recorded**. history: `0` success (incl. empty list / already-empty clear), `1` unknown id (`show`) or read/write failure, `2` missing run id (`show`) or `clear` without `--yes`. Store at `<user-config-dir>/portier/workflow-history.json` (path injectable for tests). Model/projection/run-id/store in the `workflow` package (`workflow/history.go`: `History`/`HistoryRun`/`HistoryStep`/`ProjectRun`/`NewRunID`/`HistoryStore`), never imports `client`/`commands`, never contacts the runtime; `commands` handlers-only (`workflow_history.go`). Covered by white-box `workflow/history_internal_test.go` (compact projection, no-sensitive-data-in-JSON, code dedupe/sort, run-id shape, load missing/empty/malformed, append ordering + parent-dir creation, retention 100, schemaVersion default, clear incl. error) + `commands/workflow_history_internal_test.go` (record compact + no-sensitive-content, no-record-without-flag, failed-run records `failed`, invalid-plan no-record, write-failure exit incl. runtime-unreachable→3, list/show/clear human+JSON, unknown/missing id, read failures, flag-parse edges, path-resolution failures, defaults) + `main_test.go` offline dispatch. CLI coverage **97.5%** (gate 97 PASS, not lowered, not raised — ~0.5% buffer too thin for 98); `validate:contract` 234/234; lint/typecheck/gofmt/vet clean
- [x] v1.11 workflow toolkit consistency audit (v1.11 Slice 8) — **PASS, ready for finalization**. Audited the workflow toolkit (Slices 1–7): command surface + all flag combinations (built-binary battery across plan/run/runbook/report/template/explain), JSON schemas (plan/runbook/run/template/report-bundle shapes; `--json --out` byte parity for plan/runbook/run incl. `--explain`; `--explain` additive/omitempty; passed/valid steps un-explained; report.json normalized — embedded per-step reports dropped, no config/host/port leak; `explanations.json` `{}` when none; `explain --list` = 44 codes), exit codes (plan 0/1/2, template 0/1/2, runbook 0/1/2, run 0/1/2/**3**, report 0/1/2 + non-empty-dir→1, no report exit-3; all `--out` write failures → 1), safety (no `os/exec`/shell/syscall/`net/http`/telemetry/`os.Getenv` in `workflow` or handlers; referenced-file reads injected via `RunDeps.ReadFile` so plan/runbook/report never open referenced files; only `run` contacts the runtime, read-only; no apply/import/enforcement/scheduler/probing/`allowUdp`/mutation), and architecture (`commands` handlers-only; `workflow` imports only `config`/`policy`/`explain`/`output`, not `client`/`commands`; one canonical `explain` registry; no second schema; no cycles; `go vet` clean; offline subcommands ignore a malformed `--url`). Fixes (3, small): (1) top-level `workflow` help intro verb list now includes "report"; (2) stale readme runbook note "execution deferred to a later slice" → points to `workflow run`; (3) added a white-box test for the untested exported `RunExplanations()` accessor (0% → 100%; func coverage 99.2% → 99.6%). `validate:contract` 234/234; CLI coverage **97.5%** (gate 97 PASS, none lowered); lint/typecheck/gofmt/vet clean. No release blockers; no new durable rule (confirms Slice 1–7 rules); sub-100% workflow functions are the documented structurally-unreachable encode/write-failure branches
- [x] CLI workflow support report (v1.11 Slice 7) — `portier workflow report --from <report.json> --out <directory>` packages an **existing** workflow plan/run JSON report (from `workflow plan/run --json --out`) into a small local diagnostic bundle — **fully offline, read-only, packaging only**. NEVER executes a workflow, runs a shell command, contacts the runtime, reads the config/policy/baseline/report files a step refers to, collects logs/env/process data, mutates the input, or uploads. Bundle: `manifest.json` (schemaVersion, RFC3339 createdAt, source `workflow-run`/`workflow-plan`, workflow, result, files), `summary.txt` (source/workflow/result + step list with `[PASSED]`/`[FAILED]`/`[SKIPPED]`/`[VALID]`/`[INVALID]` + explained codes + a fixed **safety block**), `report.json` (**normalized** — step id/type/status/message + per-step explainable codes; **drops** each run step's embedded policy/compare report so no config/host/port payload is carried), `explanations.json` (canonical explanations for emitted codes, `{}` when none). Code extraction (canonical registry, deduped): invalid plan steps → `workflow.step.*` code; failed run steps → embedded `policy.*` finding codes (`findings`/`new`); skipped run steps → `workflow.run.dependency_failed`; plus any codes in a `--explain` report's `explanations` map (`workflow.run.runtime_unreachable`/`input_failed`); unknown codes dropped. Detection: plan = has `schemaVersion` + result valid/invalid; run = no `schemaVersion` + result passed/failed; both require ≥1 step (a bare policy report → unsupported). Exit: `0` written; `1` output dir create/write failure (non-empty `--out` refused, matching `support-bundle`, without touching unrelated files); `2` missing/invalid args (incl. missing `--from`/`--out` value) or unreadable/malformed/unsupported input report; **no `3`** (never contacts runtime). Model/parser/extractor/summary/writer in the `workflow` package (`workflow/report.go`: `SupportReport`/`SupportReportStep`/`SupportReportManifest`/`ParseSupportReport`/`ExplanationCodes`/`Explanations`/`WriteSupportSummary`/`WriteSupportReport` with injected clock; `reportExplainRegistry` merges workflow step+run+`policy`); does NOT import `client`/`commands`; `commands` handlers-only (`RunWorkflowReport` reuses `prepareBundleDir`). Added docs-only AI prompt `prompts/workflow.md` (paste summary.txt/report.json/explanations.json; warns against secrets; forbids inventing blind config changes; no AI integration/upload/telemetry). Covered by white-box `report_internal_test.go` (run/plan detection, rejection cases, per-step code extraction incl. failed-compare `new` + malformed embedded report, dedupe/canonical-only, status tags, summary shape/placeholders/empty `{}`, bundle write w/ fixed clock) + black-box `commands/workflow_report_test.go` (failed/passed run, invalid plan, `--explain` run code, JSON manifest, **no-referenced-file-reads + input-not-mutated**, missing-dir-created, empty-dir-OK, non-empty-dir→1, malformed/unsupported→2, missing-args→2, unreadable→2, JSON-encode→1, dispatch) + `main_test.go` offline dispatch. CLI coverage **97.5%** (gate 97 PASS, not lowered, not raised — ~0.5% buffer too thin for 98); `validate:contract` 234/234; lint/typecheck/vet clean
- [x] CLI workflow run explanations (v1.11 Slice 6) — `portier workflow run --file <workflow.json> --explain` adds **additive** explanations for failed/skipped steps: a failed `policy.check`/`policy.review` explains its **policy finding codes**; a failed `policy.baseline.compare` explains the **underlying new-finding codes**; a step skipped because its `reportFrom` dependency produced no report explains **`workflow.run.dependency_failed`**; a runtime step that can't reach the runtime explains **`workflow.run.runtime_unreachable`** (still exit 3); an unreadable/malformed referenced input (or the runtime config) explains **`workflow.run.input_failed`**. **Passed steps are never explained** (no noise). `--explain` must NOT change which steps run, their status, the summary, the result, or the **exit code** — human adds an indented `Code:`/`Meaning:`/`What to do:`/`Related:` block under each failed/skipped step; JSON adds a top-level `explanations` map (deduped) populated ONLY with `--explain` (omitted entirely without it AND when no explainable codes were emitted); `--json --out --explain` stdout/file byte-identical. Three stable `workflow.run.*` codes join the canonical explain registry → `portier explain <workflow.run.*>` works and `explain --list` covers **44** codes (8+12+6+15+3); doctor/policy/workflow-plan explain unchanged; support-bundle `explanations.json` stays doctor-scoped. Codes/registry/selection in the `workflow` package (`workflow/run_explain.go`: `RunExplanations`/`ExplanationCodesForRun`/`runExplainRegistry` + finding-code collectors; `Explanations()` now merges step+run); reuses the `explain` renderer (one schema); per-step explain codes stored unexported (not in JSON). `commands` handlers-only (`RunWorkflowRun` wires `--explain` into `EmitRun`). Covered by white-box `workflow/run_explain_internal_test.go` (locked 3-code set, registry parity, `Explanations()` merge, run+policy registry resolution) + black-box `commands/workflow_run_explain_test.go` (policy-check/review/baseline finding codes, the 3 run codes incl. runtime-unreachable→3, passed-run emits no map, `--explain` doesn't change run via decode+DeepEqual, `--json --out --explain` byte parity, `explain <code>`/`--list` integration); `explain --list` count test 41→44. CLI coverage **97.5%** (gate 97 PASS, not lowered, not raised — ~0.5% buffer too thin for 98); `validate:contract` 234/234; lint/typecheck/vet clean
- [x] CLI workflow run — read-only execution (v1.11 Slice 5) — `portier workflow run --file <workflow.json>` validates the workflow then executes its steps in order by calling the existing safe domain helpers directly (`policy.Evaluate`/`BuildReview`/`Compare`) — the first execution command, strictly **read-only**: **never** runs a shell command/the runbook display text, applies/imports configs, enforces policy, schedules, or mutates runtime/config/policy/baseline/report files. Only runtime contact = read-only `GET /api/config/export` for a `policy.check` runtime step (no new endpoint). Step status: `policy.check`/`policy.review` pass→passed, violation→failed; `policy.baseline.compare` no-new→passed, new→failed. **`reportFrom` consumes the producing step's IN-MEMORY report** (round-tripped through JSON so fingerprints match file-derived baselines; no intermediate file); a dependency that produced no report → dependent **skipped** (fails the run). Report `{workflow,steps:[{id,type,status(passed|failed|skipped),exitCode,message,report?}],summary:{total,passed,failed,skipped},result}` (no raw configs/secrets). Human `[PASSED]`/`[FAILED]`/`[SKIPPED]` + `Result:`; `--json`; `--out` (`--json --out` byte-identical). **Invalid workflow → prints plan + exit 1, NO step run, NO `--out` file**. Referenced files read **during run** (unlike plan/runbook). Exit: `0` all passed; `1` step failed/skipped, invalid plan, or `--out` write failure; `2` usage/unreadable/malformed; **`3`** runtime unreachable (matches `policy check --runtime`); a referenced-file error during run = step failure (1), not 2. Model/orchestration/render in the `workflow` package (`workflow/run.go`: `WorkflowRun`/`WorkflowRunStep`/`WorkflowRunSummary`/`RunDeps`/`Run`/`RunExitCode`/`PrintRunHuman`/`EmitRun`/`ErrRuntimeUnreachable`); reuses `Parse`+`BuildPlan`+`policy`/`config`; **does NOT import `client`/`commands`** — runtime fetch is **injected** (`commands.makeRuntimeRules` memoizes + wraps connection errors with `ErrRuntimeUnreachable`→exit 3); `commands` handlers-only. `--explain` deferred. Covered by white-box `run_internal_test.go` (per-type pass/fail, runtime pass/unreachable→3/generic→1, reportFrom new-findings + **round-trip matches file-derived baseline**, dependency-skip, all input-error branches, unknown-type fallback, human shape) + black-box `commands/workflow_run_test.go` (all types, reportFrom, skip, invalid→1-no-run-no-out, malformed→2, missing-file→step-fail, **runtime via httptest** + unreachable→3 + API-error→1 + bad-URL→1 + memoized-single-fetch, JSON/human, `--out`/`--json --out` byte parity + write-failure, no-mutation, dispatch) + `main_test.go` offline dispatch. CLI coverage 97.6% → 97.5% (gate 97 PASS, not lowered; orchestrator/executors/render covered incl. error branches, only unreachable concrete-type encode branches uncovered); `validate:contract` 234/234; lint/typecheck/vet clean
- [x] CLI workflow runbook preview (v1.11 Slice 4) — `portier workflow runbook --file <workflow.json>` turns a **valid** workflow plan into the ordered Portier CLI commands you'd run manually (a **preview, NOT execution**). Parses + validates, then maps each step → command: `policy.check`(config)→`policy check --config <f> --policy <f>`, `policy.check`(runtime)→`... --runtime --policy <f>`, `policy.review`→`policy review --current/--candidate/--policy`, `policy.baseline.compare`(report)→`policy baseline compare --baseline <f> --report <f>`, (reportFrom)→explicit `<report-from:step-id>` **placeholder** + note (never invents a path). Each step has a canonical argv `command` + best-effort `display` (Portier never shell-executes; whitespace/quote tokens single-quoted). Human numbered list ending `Result: ready`; `--json` → `{workflow,steps:[{id,type,command,display,notes}],summary:{total},result}`; `--out` writes that JSON (`--json --out` byte-identical). **Invalid workflow → prints the plan + exit 1, NO runbook and NO `--out` file** (like `workflow plan`). Never executes commands / contacts runtime / reads referenced files / mutates anything but `--out`. Exit: `0` runbook; `1` invalid plan or `--out` write failure; `2` usage/unreadable/malformed; **no `3`**. Model/builder/renderer in the `workflow` package (`workflow/runbook.go`: `Runbook`/`RunbookStep`/`RunbookSummary`/`BuildRunbook`/`PrintRunbookHuman`/`EmitRunbook`), reuses `Parse`+`BuildPlan`; file writing reuses `output.WritePrettyJSON`; `commands` handlers-only (`RunWorkflowRunbook`). Covered by white-box `runbook_internal_test.go` (per-type mapping, placeholder+note, ordering, `shellQuoteToken`, human shape, unknown-type fallback, all templates → runbooks) + black-box `commands/workflow_runbook_test.go` (all types, ordering, human/JSON, **invalid→plan+exit1+no `--out` file**, malformed/schema/missing-file/unreadable/missing-out→2, `--out`+`--json --out` byte parity + write-failure→1, **does-not-read-referenced-files**, no-mutation, templates→runbooks, dispatch) + `main_test.go` offline dispatch. CLI coverage 97.6% → 97.6% (gate 97 PASS, not lowered; builder/renderer/`RunWorkflowRunbook` 100%, only unreachable `EmitRunbook` encode branch uncovered); `validate:contract` 234/234; lint/typecheck/vet clean
- [x] CLI workflow templates (v1.11 Slice 3) — `portier workflow template --list` lists four deterministic built-in templates (sorted): `policy-baseline-check` (check + baseline compare via `reportFrom`), `policy-check-local`, `policy-check-runtime`, `policy-review`. Each renders to a complete `schemaVersion:1` workflow **directly usable by `workflow plan --file`** and composes only the existing validated step types — **no execution/scheduler/mutation/apply/import fields**. Output: `template <name>` prints bare workflow JSON; `--json` prints a `{name,title,description,workflow}` wrapper; `--list` human list / `--list --json` → `{templates:[{name,title,description}]}` (no embedded workflow body); `--out <file>` writes the **bare** workflow JSON regardless of `--json` (human confirms `Workflow written to <file>`). Fully offline (no runtime/probe/step-exec), modifies nothing but `--out`. Exit: `0` success; `1` `--out` write failure; `2` unknown/missing/too-many template, missing `--out` value, or `--list` with a name/`--out`. Flags work before or after the name. Registry/rendering in the `workflow` package (`workflow/template.go`: `Template`/`Templates`/`FindTemplate`/`TemplateNames`/`WorkflowValue`/`DetailValue`/`PrintTemplateList`); file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only (`RunWorkflowTemplate`). Covered by white-box `template_internal_test.go` (sorted/unique; **each renders → Parse → BuildPlan valid**; field allowlist forbids exec/scheduler/mutation fields + non-validated step types; `FindTemplate`; `DetailValue` shape) + black-box `commands/workflow_template_test.go` (list human/json/sorted, render each, json wrapper, `--out` bare + **end-to-end usable by `workflow plan`**, `--json --out` writes bare, flag-after-name, unknown/missing/too-many/missing-out-value/list+name/list+out/bad-flag→2, write-failure→1, no-file-mutation, dispatch) + `main_test.go` offline dispatch. CLI coverage 97.7% → 97.6% (gate 97 PASS, not lowered; `template.go` 100%, dip = unreachable `json.Marshal`-on-concrete-DTO encode branches in `RunWorkflowTemplate`, as in `RunPolicyTemplate`); `validate:contract` 234/234; lint/typecheck/vet clean
- [x] CLI workflow plan explanations (v1.11 Slice 2) — every planned step carries a stable, operator-facing validation `code`; there are exactly **15** (`workflow.step.valid` + 14 invalid-step codes: `duplicate_id`/`unknown_type`/`missing_id`/`missing_type`/`missing_config_source`/`conflicting_config_sources`/`missing_policy`/`missing_current`/`missing_candidate`/`missing_baseline`/`missing_report_source`/`conflicting_report_sources`/`unknown_report_from`/`future_report_from`), one per Slice 1 outcome, **locked by a code-set test**. `portier workflow plan --explain` adds an inline `Code:`/`Meaning:`/`What to do:`/`Related:` block for each **invalid** step (human) and an additive `explanations` map (`--json`, deduped, **invalid-step codes only** — a valid step needs no explanation). `--explain` never changes steps/summary/result/exit code (decode-and-deep-compare guard); without it JSON is byte-identical (`explanations` is `omitempty`); `--json --out --explain` stays byte-identical. `portier explain <workflow-code>` + `explain --list` now cover **41** codes (8 config doctor + 12 live doctor + 6 policy + 15 workflow), sorted; doctor/policy explanations unchanged; support-bundle `explanations.json` stays doctor-scoped. Registry owned by the `workflow` package (`workflow/explain.go`, `Explanations()`); explain handler composes via `explain.Merge(doctor, policy, workflow)`; inline reuses `explain.PrintInline`/`ForReport` (no second schema); `commands` stays handlers-only. Covered by white-box `workflow/explain_internal_test.go` (locked set + registry parity) + `workflow_internal_test.go` (per-outcome code mapping, `invalidCodesOf`, explain renders invalid-only) + black-box `commands/workflow_explain_test.go` (stable codes, human/JSON explain, omit-without/valid-plan-omit, no-plan-change, `--out`/`--json --out` byte parity, `explain <code>`/`--list`/unknown). CLI coverage 97.7% → 97.7% (gate 97 PASS, not raised; `workflow` pkg 98.7%, only unreachable encode branch uncovered); `validate:contract` 234/234; lint/typecheck/vet clean
- [x] CLI workflow plan dry-run (v1.11 Slice 1) — `portier workflow plan --file <workflow.json>` reads a local workflow file, validates its schema + step references, and prints a deterministic plan. **Fully offline & dry-run** — does **not execute** steps, never contacts the runtime, never reads the files a step refers to (a missing referenced file does NOT fail the plan), never applies/imports, never enforces, never mutates anything but `--out`. Workflow format `schemaVersion:1` + non-empty `steps`; step types `policy.check` (exactly one of `config`/`runtime` + `policy`), `policy.review` (`current`/`candidate`/`policy`), `policy.baseline.compare` (`baseline` + exactly one of `report`/`reportFrom`); unknown fields rejected; ids unique/non-empty; `reportFrom` must reference an **earlier** step (inherently acyclic). Plan `{schemaVersion,name,steps:[{id,type,status,message,inputs,dependsOn}],summary:{total,valid,invalid},result}` (separate from policy/doctor models); human `[VALID]`/`[INVALID]` + `Result:`. Exit: `0` valid; `1` invalid plan **or** `--out` write failure; `2` usage (missing `--file`/`--out` value) or unreadable/malformed workflow (missing/unsupported `schemaVersion`, no steps); **no `3`** (never contacts runtime). `--json` full plan; `--out` writes JSON (`--json --out` byte-identical). New domain package `tools/cli/sources/workflow` (schema/parser/validator/model/render); `commands` handlers-only (`RunWorkflow`/`RunWorkflowPlan`); dispatched offline from `main.go`. Covered by white-box `workflow_internal_test.go` + black-box `workflow_test.go` (incl. no-mutation, does-not-open-referenced-files, `--out` byte parity/write-failure) + `main_test.go` dispatch. CLI coverage 97.6% → 97.7% (gate 97 PASS, not raised — ~0.7% buffer too thin for 98; `workflow` pkg 98.6%, only unreachable encode branch uncovered); `validate:contract` 234/234; lint/typecheck/vet clean
- [x] CLI package layout refactor (v1.10) — moved general model/result types + pure offline logic out of the catch-all `commands` package into focused per-domain packages: `tools/cli/sources/config` (`Rule`/`ParseLocal`/`Validate`/`Summary`/`FindDuplicateBindings`/`RuleDetail`), `tools/cli/sources/doctor` (`Report`/`CheckResult`/`Severity`, `RunLiveChecks`/`RunConfigChecks`, `Explanation` registry, `Emit`/`PrintHuman`/`ReportPayload`), `tools/cli/sources/policy` (`Report`/`Finding`, `Parse`/`Evaluate`/`PrintHuman`), `tools/cli/sources/planview` (config plan/diff/apply response rendering: `PrintPlan`/`PrintDiff`/`PrintApply`/`PlanExitCode`/`ApplyExitCode`); shared `PluralWord`/`PluralRule`/`WritePrettyJSON` → `output`. `commands` now holds only `Run*` handlers + dispatch (request-DTO mapping, per-command printers, single-command output structs, and plumbing intentionally stay with the handlers). De-stuttered names (`doctor.Report`, `config.Rule`); import DAG has no cycles (`config`/`doctor`/`policy` never import `commands`). **Behavior-preserving** — JSON tags unchanged, output byte-identical, white-box tests moved to their packages, black-box tests reference new package types. `coverage-tools-cli.js` `coverpkg` extended to instrument the new packages; CLI coverage 97.8% (gate 97, not lowered); `validate:contract` 234/234; lint clean. Durable rule in `CLAUDE.md`/`AGENTS.md`
- [x] v1.10 policy toolkit consistency audit (v1.10 Slice 8) — **PASS WITH NOTES, ready for finalization**. Audited the full policy toolkit (Slices 1–7): command surface + all flag combinations (live battery incl. `--runtime --json --out --explain`), source selection (exactly one of `--config`/`--runtime`; both/neither → 2; offline ignores a malformed `--url`), JSON schemas (check `{source?,explanations?,findings,summary,result}`; review `{review,…}`; baseline `{schemaVersion,createdAt,source?,result,findings:[{fingerprint,code,severity,title,message}]}`; compare `{summary,result,new,resolved,unchanged}`; template list/single/`--out`-bare; `--json --out` byte parity across check/review/export), exit codes (check 0/1/2/3, review 0/1/2, template/baseline-create/baseline-compare 0/1/2), safety (offline never resolves runtime URL; runtime read-only; no input mutation; no probing/enforcement/scheduler/telemetry/env-collection; no `allowUdp`; baseline stores only finding snapshots), and architecture (`commands` handlers-only; `policy` imports only `config`/`explain`/`output`, no `client`/`commands`, no cycle; `go vet` clean). One fix: refreshed the stale top-level `portier policy` help intro (was "offline … check + templates"; now describes check/review/baseline/template, read-only/dry-run, `check (--config | --runtime)`) + added a `--runtime --json --explain --out` byte-parity test. `validate:contract` 234/234; CLI coverage 97.6% (gate 97 PASS, none lowered); lint/typecheck/vet clean. No new durable rule; no release blockers; no deferred non-blockers
- [x] CLI policy baseline snapshots (v1.10 Slice 7) — `portier policy baseline create --from-report <report.json> --out <baseline.json>` saves an accepted policy report's findings as a compact baseline; `portier policy baseline compare --baseline <baseline.json> --report <report.json> [--json]` reports new/resolved/unchanged findings. Works on report JSON from `policy check`/`policy review` (`--json`/`--out`). **Fully offline** — never contacts the runtime, never mutates inputs. Baseline = accepted **snapshot of findings, NOT a config copy** (`{schemaVersion:1, createdAt(RFC3339 UTC), source?, result, findings:[{fingerprint,code,severity,title,message}]}`; no raw config/secrets/runtime-host/details/explanations; `policy.valid` marker excluded). **Deterministic fingerprints**: rule-scoped `code|name|protocol|listenHost|listenPort`, duplicate-binding `code|protocol|listenHost|listenPort|<sorted names>`, fallback `code|message` — source-independent (offline `--config` vs `--runtime` identical), never uses volatile rule IDs. **New findings fail; resolved-only changes do not.** Exit: create 0 written / 1 write-failure / 2 usage or malformed report; compare 0 no-new / 1 new / 2 usage or malformed baseline-or-report (incl. unsupported baseline schemaVersion). Model/fingerprint/build/compare in `policy` (`policy/baseline.go`); `BuildBaseline` takes an injected clock (testable `createdAt`); file writing reuses `output.WritePrettyJSON`; `commands` handlers-only. Covered by white-box `baseline_internal_test.go` (fingerprint stability incl. dup-binding name-order independence + message fallback, tracked-finding exclusion, injected-clock UTC, new/resolved/unchanged + sorting, parse rejection, detailString/nestedMap/bindingRuleNames helpers) + black-box `policy_baseline_test.go` (create from check & review reports, schema/createdAt/no-raw-config, JSON summary, compare no-change/new/resolved-only/mixed, JSON & human shape, all error exits, no-input-mutation, dispatch/help/bad-flag). CLI coverage 97.6% → 97.6% (gate 97 PASS, not lowered; model/helpers fully covered, residual = unreachable encode branches); `validate:contract` 234/234; lint/typecheck clean
- [x] CLI runtime policy check (v1.10 Slice 6) — `portier policy check --runtime --policy <file>` evaluates the **live runtime config** (read read-only via the existing `GET /api/config/export` path — **no new endpoint**) against a policy, reusing the **same evaluator + report shape** as offline `policy check`. Exactly one config source: `--config` (offline) XOR `--runtime`; both or neither → exit 2; `--policy` required and validated **before** any runtime I/O. Offline `--config` mode stays fully offline (never resolves/contacts the runtime URL); runtime mode uses global `--url`/`--host`/`--port` and lazily builds the client only in runtime mode. Additive `source` field in human (`Source: runtime`/`Source: config file`) and JSON (`omitempty`) — offline `--json` without source byte-identical to before; `--json`/`--out`/`--explain` behave as in offline check (`--json --out` byte-identical, `--explain` additive map, no change to findings/summary/result/exit). **Read-only/dry-run**: no enforcement/remediation/probing/mutation; runtime-unreachable → **3** and config-export/API failure → **1** (standard live-CLI convention, **never** a fabricated policy finding/report); violations → 1, passes → 0, bad input/policy → 2. No new policy codes; policy package stays free of live-client behavior (`commands` owns `loadCheckRules`/`exportedRules` + lazy client; `commands` handlers-only). Covered by black-box `policy_runtime_test.go` (httptest export server: pass/violation, JSON `source:"runtime"`, `--out` byte parity, `--explain`, unreachable→3, export-failure→1, both-sources→2, neither→2, missing/invalid policy→2, invalid `--url`→2, policy-before-fetch, offline-never-contacts-runtime, read-only-asserted). CLI coverage 97.6% → 97.6% (gate 97 PASS, not lowered; `exportedRules`/`RunPolicyCheck` 100%, residual = unreachable encode branches); `validate:contract` 234/234; lint/typecheck clean
- [x] CLI policy-aware config review (v1.10 Slice 5) — `portier policy review --current <file> --candidate <file> --policy <file>` runs **fully offline and dry-run** (reads three files; never contacts the runtime, never probes targets, never modifies any file except `--out`; no import/apply). Compares current vs candidate via `config.BuildSummary` and evaluates **only the candidate** against the policy (reusing `policy.Evaluate` + the `policy check` finding rendering — no second schema). Output: human `Portier Policy Review` + `Config changes:` block (`Rules: 2 → 3 (+1)`, enabled/disabled/groups/ungrouped) + candidate findings/summary/`Result:`; `--json` → `{review:{current,candidate,delta}, findings, summary, result}` (current/candidate = config-doctor summary shape, delta = scalar candidate−current); `--explain` adds the same additive `explanations` map (+ inline human blocks) without changing review/findings/summary/result/exit; `--out` writes the JSON (byte-identical to `--json` stdout, human confirms `Review written to <file>`). Compact summary only — **not** a full config diff engine. Exit: `0` candidate passes; `1` candidate violates (or JSON-encode / `--out` write failure); `2` missing/invalid args or unreadable/malformed config or policy (incl. unsupported schemaVersion). Model/eval/render live in `policy` (`policy/review.go`: `Review`/`ReviewSummary`/`ReviewDelta`/`BuildReview`/`PrintReviewHuman`/`EmitReview`); `commands` handlers-only; policy-check + review share `printFindings`/`printSummaryAndResult` (policy check output byte-identical). Covered by black-box `policy_review_test.go` (pass/fail exit, exact current/candidate/delta incl. negative deltas, JSON/human shape, all input-error exit-2 paths, `--out` byte parity + write failure, `--explain` adds explanations without changing report, no input mutation, dispatch, help, bad-flag). CLI coverage 97.6% → 97.6% (gate 97 PASS, not lowered; `BuildReview`/`PrintReviewHuman` 100%, remaining gap = unreachable `json.Marshal`-on-concrete-DTO encode branches); `validate:contract` 234/234; lint/typecheck clean
- [x] CLI policy templates (v1.10 Slice 4) — `portier policy template --list` lists three deterministic built-in templates (sorted by name): `local-safe` (forbids LAN exposure + privileged ports, keeps autostart, no group requirement), `managed` (requires groups; forbids LAN exposure, privileged ports, autostart), `permissive` (only duplicate bindings forbidden). Each is `schemaVersion:1` with only the five standard boolean guardrails — **no protocol allowlist/denylist, no `allowUdp`** (UDP first-class). Output: `template <name>` prints bare policy JSON (usable directly by `policy check`); `--json` prints a `{name,title,description,policy}` wrapper; `--list` human list (name+title+indented description) / `--list --json` → `{templates:[{name,title,description}]}` (no embedded policy); `--out <file>` writes the **bare** policy JSON regardless of `--json` (human mode confirms `Policy written to <file>`, `--json` keeps stdout the pure wrapper). Fully offline (no runtime/probe), modifies nothing but `--out`. Exit: `0` success; `1` `--out` write failure; `2` unknown/missing template, too many names, missing `--out` value, or `--list` with a name/`--out`. Flags work before or after the name (`template managed --out f`, via interspersed flag parsing). Registry/rendering live in `policy` (`policy/template.go`); file writing reuses `output.WritePrettyJSON`; `commands` stays handlers-only. Covered by white-box `template_internal_test.go` (each renders to a schema-1 policy `Parse` accepts + `Evaluate` runs; sorted/unique; no udp/protocol token; guardrail booleans locked to purpose) + black-box `policy_template_test.go` (list human/json/sorted, render each, json wrapper, `--out`-usable-by-`check`, unknown/missing/too-many/missing-out-value/write-failure/list+name/list+out, dispatch, help). CLI coverage 97.8% → 97.6% (gate 97 PASS, not lowered; `template.go` 100%, dip = unreachable `json.Marshal`-on-concrete-DTO encode branches in `RunPolicyTemplate`); `validate:contract` 234/234 unchanged; lint/typecheck clean
- [x] CLI policy report export (v1.10 Slice 3) — `portier policy check --config <file> --policy <file> --out <file>` writes the policy report to a file as deterministic pretty JSON — **same shape as `--json`** (`findings` + `summary` + `result`, plus the additive `explanations` map under `--explain`), no second schema. Mirrors the v1.9 doctor `--out` convention: writes the file regardless of `--json`; human mode keeps human stdout + a `Report written to <file>` confirmation; `--json` mode keeps stdout pure JSON and **byte-identical to the file** (`--json --out` and `--json --explain --out` parity). A **file-write failure is an operation failure → exit 1** (stderr `Error writing`), overriding the report exit code; a **missing `--out` value → exit 2**; a malformed/unreadable config or policy still → exit 2 and **writes no file** (no success-looking report). File written only after a successful marshal (no partial writes); parent dirs not created; config/policy files never mutated; fully offline (no runtime/probe/telemetry). Report construction stays in `policy.Emit`/`EmitOptions.OutPath`, file writing reuses `output.WritePrettyJSON`, `commands` stays handlers-only. Covered by black-box `policy_export_test.go` (write+result/json-file byte parity/explain-file/write-failure/missing-out/malformed-no-file/no-mutation); CLI coverage held at 97.8% (gate 97, not lowered — ~0.8% buffer too tight to raise to 98); `validate:contract` 234/234 unchanged; lint/typecheck clean
- [x] CLI policy explanations (v1.10 Slice 2) — `portier explain <policy-code>` explains all 6 policy finding codes; `portier explain --list` now lists **26** codes (8 config-doctor + 12 live-doctor + 6 policy), sorted; doctor explanations unchanged. `portier policy check --explain` adds an inline `Code:`/`Meaning:`/`What to do:` block per emitted finding (human) and an additive deduplicated `explanations` map (`--json`); `--explain` does not change findings/summary/result/exit code (JSON byte-identical without it via `omitempty`). Architecture: shared `Explanation` type + render helpers moved to a neutral leaf package `tools/cli/sources/explain` (`For`/`Sorted`/`SortedCodes`/`ForReport`/`PrintInline`/`Merge`); each domain (`doctor`, `policy`) owns its explanation data keyed by its own code constants; the `explain` command merges them; `commands` stays handlers-only. Policy explanations frame each finding as a **policy choice** (not a defect) and never claim auto-enforcement/remediation. Registry parity guarded per-domain (`policy/explain_internal_test.go`, `doctor/explain_internal_test.go`). Added offline AI prompt `prompts/policy.md`. `validate:contract` 234/234 (doctor `--explain` byte-identical, bundle explanations.json stays doctor-scoped 20); CLI coverage 97.8% (gate 97, not lowered); lint/typecheck clean. Durable rules in `CLAUDE.md`/`AGENTS.md`
- [x] CLI policy dry-run (v1.10 Slice 1) — `portier policy check --config <file> --policy <file>` runs **fully offline** (no running service, never contacts the runtime, never probes targets, never modifies the config or policy file); evaluates a config against a small JSON policy (`schemaVersion:1` + `rules:{requireGroup,allowLanExposure,allowPrivilegedPorts,allowAutostart,forbidDuplicateBindings}`, omitted fields default permissive, **unknown fields rejected**); deterministic findings with stable codes (`policy.valid`/`policy.group_required`/`policy.lan_exposure_forbidden`/`policy.privileged_port_forbidden`/`policy.autostart_forbidden`/`policy.duplicate_binding_forbidden`); human output (`[INFO]`/`[ERROR]` + severity summary + `Result: passed/failed`) and `--json` (`{findings[], summary, result}`); ordering = per-rule file order (group→LAN→privileged→autostart) then duplicate bindings (sorted proto→host→port); reuses the v1.9 config-doctor LAN/privileged/duplicate-binding interpretations; exit codes `0` (no violations), `1` (≥1 violation), `2` (missing/invalid args or unreadable/malformed config/policy incl. unsupported schemaVersion). **No enforcement/mutation/automation; UDP is first-class (no `allowUdp`/protocol restriction).** Covered by black-box `policy_test.go` + white-box `policy_internal_test.go` + `main_test.go` dispatch; CLI coverage 97.6% → 97.8% (every policy function 100%); **opportunistic gate ratchet applied — CLI gate raised 95% → 97%** (actual 97.8%, a deliberately tighter ~0.8% buffer, deterministic; remaining uncovered lines are documented structurally-unreachable defensive/platform branches); no gate lowered; `validate:contract` 234/234 unchanged
- [x] CLI config doctor (v1.9 Slice 1) — `portier config doctor <file>` runs **offline** with no running service; reports deterministic doctor checks with stable codes (`config.read_failed`/`config.parse_failed`/`config.empty`/`config.validation_failed`/`config.duplicate_binding`/`config.lan_exposure`/`config.privileged_port`/`config.valid`); human output (`[INFO]`/`[WARN]`/`[ERROR]` + severity summary) and `--json` (`{checks[], summary}`); exit codes `0` (no errors, warnings still `0`), `1` (≥1 error-severity check), `2` (missing arg/usage); never modifies the file. Covered by black-box `config_doctor_test.go` + white-box `doctor_internal_test.go`; CLI coverage 97.2% → 97.4% (gate 95%, not lowered)
- [x] CLI live runtime doctor (v1.9 Slice 2) — `portier doctor` runs against the **live** runtime (read-only; no mutation, no target probing); reuses the Slice 1 doctor model; stable codes (`runtime.reachable`/`runtime.unreachable`/`runtime.version`/`runtime.status_read`/`runtime.status_failed`/`rules.none`/`rules.present`/`rules.health_ok`/`rules.health_warning`/`rules.health_error`/`config.export_read`/`config.export_failed`); reachability + version (warning, never fail, on CLI/runtime mismatch) + rule health read from the API `health` field (never re-derived) + read-only config-export check; `--json` emits the `DoctorReport`; exit `0` (no errors/warnings-only), `1` (≥1 error-severity check, **including an unreachable runtime — exits `1`, not `3`**), `2` (usage). Covered by black-box `doctor_runtime_test.go` + `main_test.go` dispatch; CLI coverage 97.4% → 97.6% (gate 95%, not lowered); `validate:contract` 234/234 unchanged (no new endpoints/DTOs)
- [x] CLI doctor explain (v1.9 Slice 3) — `portier explain <code>` prints a deterministic **offline** explanation (meaning + what to do + related codes) for every stable doctor/check code; `portier explain --list` lists all 20 codes (8 config + 12 live); `--json` for single + list; unknown/missing/too-many-args → exit 2 (clear error pointing to `--list`, never a generic explanation); no runtime contact (ignores `--url`), no external fetch, no AI, no remediation. Internal guard test asserts every doctor code constant is explainable + registry integrity (no extras/dups, `Code` matches key, required fields/severity present, no dangling `related`). Covered by `explain_test.go` + `explain_internal_test.go` + `main_test.go` dispatch; CLI coverage 97.6% → 97.7% (gate 95%, not lowered); `validate:contract` 234/234 unchanged
- [x] CLI doctor strict mode (v1.9 Slice 4) — `portier doctor --strict` and `portier config doctor --strict <file>` treat **warnings as failures** (warning-only report → exit 1; normal mode unchanged: info/warning → 0, error → 1). Strict changes only exit-code interpretation — same checks run, no runtime/config mutation. Human output gains a `Result: passed`/`Result: failed` line plus a `Strict mode: warnings are treated as failures.` note when strict flips a warning-only report; JSON gains additive `strict` + `result` fields (`checks`/`summary` shape preserved). `--strict` precedes the file for `config doctor` (flags-before-file). Shared `doctorExitCode(report, strict)`/`doctorResultLabel`. Covered by black-box strict tests in `config_doctor_test.go`/`doctor_runtime_test.go`, internal `doctor_internal_test.go`, and `main_test.go` dispatch; CLI coverage held at 97.7% (gate 95%, not lowered; small fully-covered change); `validate:contract` 234/234 unchanged
- [x] CLI doctor report export (v1.9 Slice 5) — `portier doctor --out <file>` and `portier config doctor --out <file> <config>` write the JSON report (**same `{checks, summary, strict, result}` shape as `--json`**, no second schema) for CI artifacts/support bundles. `--out` writes the file regardless of `--json`; stdout stays human output unless `--json` (then JSON also prints); human mode adds a `Report written to <file>` line, JSON mode stdout stays pure JSON. Reuses `writePrettyJSON` (pretty, create/truncate, no parent-dir creation, no partial write). Export does not change diagnostic semantics; a **file-write failure is an operation failure → exit 1** (stderr), and a missing `--out` value → exit 2. The report is still written when a warning fails the run under `--strict` (file shows `result: failed`). Covered by black-box `doctor_export_test.go` (both commands: write/strict/json+file/write-failure/missing-value) + `main_test.go` dispatch; CLI coverage held at 97.7% (gate 95%, not lowered; small fully-covered change — emit/RunDoctor/RunConfigDoctor 100%); `validate:contract` 234/234 unchanged
- [x] CLI doctor AI handoff prompt (v1.9 Slice 10, docs-only) — added `prompts/doctor.md`: a reusable copy-paste **AI handoff prompt** (full + short variants) for asking an AI assistant to interpret `portier doctor`/`config doctor` output or a support-bundle summary. **Fully offline & safe** — Portier adds no AI integration/upload/telemetry; the prompt tells the assistant to treat pasted output as the only source of truth, prefer JSON over human (flag conflicts), distinguish info/warning/error/strict-failure/operation-failure, explain stable codes via the `explanations` map, respect Portier semantics, and **never request secrets/tokens/keys/env dumps/process lists/logs**; it asks for verdict, prioritized findings (errors first), likely cause, risk level, and separated "safe now" vs "needs admin/network/security review" steps. Referenced from `tools/cli/readme.md` ("Using doctor output with an AI assistant") + glossary term added. No code change (a `--prompt`/`prompt` CLI helper was deferred to avoid prompt-text drift); `validate:contract` 234/234, CLI coverage unchanged (no code); lint/typecheck clean
- [x] v1.9 doctor toolkit release-readiness audit (v1.9 Slice 10) — **PASS, ready for finalization**. Audited the full doctor toolkit (Slices 1–9): command surface + all flag combinations (binary battery), JSON schema consistency (`checks`/`summary`/`strict`/`result` + `explanations` only with `--explain` + `config` only for config doctor after parse; live doctor never carries `config`; `--json --out` byte-identical), check-code/explanation coverage (20 codes all explainable, no extra/dangling/ad-hoc-literal codes, `explain --list` sorted), exit codes (0/1/2 incl. unreachable→1, write-failure→1, unknown-explain→2), and safety (read-only GETs only, no env/process/log/exec collection, support-bundle excludes unsafe data + refuses non-empty dir, offline config doctor, no target probing, flags never change which checks run). One tiny fix: `doctor` help exit-code text now mentions `--out` write-failure→1 (parity with `config doctor`). Deferred non-blockers: offline `config doctor` still routes through the URL-resolving `config` dispatch (pre-existing, == `config validate`); two platform-dependent defensive filesystem branches in `prepareBundleDir`/`RunSupportBundle` uncovered (command-level failure paths tested). `validate:contract` 234/234; CLI coverage 97.6% (gate 95% PASS, none lowered); lint/typecheck clean
- [x] CLI config doctor summary (v1.9 Slice 9) — `portier config doctor --json <file>` emits a deterministic top-level `config` summary: `{ruleCount, enabledRuleCount, disabledRuleCount, protocols:{tcp,udp}, groupCount, groups:[{name,ruleCount,enabledRuleCount,disabledRuleCount}], ungroupedRuleCount}`. Optional `config` field on the shared `doctorReportJSON` wrapper, populated **only by config doctor** (`omitempty`) — live `doctor` + support-bundle JSON unchanged; `checks` array shape unchanged; **no new check codes**. Deterministic + offline: groups sorted by trimmed name, whitespace/empty group → ungrouped, stable `{tcp,udp}` protocol shape (unknown protocols uncounted), no env/runtime/filesystem data, no full-rule duplication. Present when the file parses (incl. empty `ruleCount:0` and validation-failure configs); absent on read/parse failure. Flows through `--out` (`--json --out` byte-identical); `--explain` presentation-only (summary unchanged); `--strict` only changes exit (summary unchanged). Covered by black-box `config_doctor_summary_test.go` + internal `buildConfigSummary` tests; CLI coverage 97.5% → 97.6% (gate 95%, not lowered; builder 100%); `validate:contract` 234/234 unchanged
- [x] CLI config doctor advisory details (v1.9 Slice 8) — `portier config doctor --json <file>` attaches deterministic structured `details` to findings (no new codes): `config.duplicate_binding` → `bindings[]` (`{protocol,listenHost,listenPort,rules:[{id?,name,enabled,group?}]}`, sorted proto→host→port, rules file order) + existing `errors`; `config.lan_exposure`/`config.privileged_port` → **aggregated** one check each with `rules[]` (`{id?,name,protocol,listenHost,listenPort,enabled,group?}`, file order); `config.empty`→`{ruleCount:0}`; `config.validation_failed`→`{errors,ruleCount}`; `config.valid`→`{ruleCount,tcpCount,udpCount}`. Details derived **only from the offline config** (no env/process/log/filesystem/runtime/probe data; ids only when present; groups trimmed). LAN stays `0.0.0.0` (no `::`, consistent with shared advisory); privileged stays `<1024` (no `COMMON_PORTS` dup). Flows through `--out`; `--explain` stays presentation-only; **support-bundle unchanged** (runs live doctor). Exit semantics unchanged (normal warn→0, strict warn→1). Covered by black-box `config_doctor_details_test.go` + internal `configdetails_internal_test.go` (grouping/ordering/tie-breakers/aggregation/titles); CLI coverage held at 97.5% (gate 95%, not lowered; new helpers 100%); `validate:contract` 234/234 unchanged
- [x] CLI doctor inline explanations (v1.9 Slice 7) — `portier doctor --explain` and `portier config doctor --explain <file>` add an inline explanation (code + meaning + next action + related) for the checks **present in the report**, reusing the Slice 3 explanation registry (no duplicated strings). Human output: indented `Code:`/`Meaning:`/`What to do:`/`Related:` block per check; JSON: **additive** top-level `explanations` map (emitted codes only, deduped) — `checks`/`summary`/`strict`/`result` shape unchanged, omitted (`omitempty`) without `--explain` so non-explain JSON is byte-identical. Combines with `--strict`/`--json`/`--out` (`--out --explain` writes JSON with explanations; flags before the file for `config doctor`). `--explain` changes nothing about which checks run, the summary, result, or exit codes (normal info/warn→0, error→1; strict warn→1). Unknown codes degrade safely (`(no explanation available)`); an internal guard proves every emittable doctor code is explainable. Support-bundle output unchanged. Covered by black-box `doctor_explain_test.go` (both commands: human/json/out/strict/unreachable/no-change-to-report) + internal `explanationsForReport`/`printCheckExplanation` tests; CLI coverage 97.4% → 97.5% (gate 95%, not lowered — recovers part of the Slice 6 dip); `validate:contract` 234/234 unchanged
- [x] CLI doctor support bundle (v1.9 Slice 6) — `portier support-bundle --out <dir>` collects deterministic doctor artifacts into a directory (read-only; no runtime/config mutation): `manifest.json` (schemaVersion/generatedAt/cliVersion/source/generatedBy/runtimeUrl/strict/result/artifacts/warnings), `doctor.json` (**same schema as `doctor --json`**), `doctor.txt`, `explanations.json` (all 20 codes), and — when available — `runtime.json` + `config-export.json`. **Unreachable runtime still creates a useful bundle** (manifest + doctor.json with `runtime.unreachable` + doctor.txt + explanations.json; omitted artifacts recorded as manifest warnings) and exits 1. `--strict` applies strict to the bundled report; `--json` prints a bundle summary. Exit `0` (written + passed), `1` (written + doctor failed / filesystem failure), `2` (missing `--out`/usage). Existing **non-empty** dir or a file path is **refused** (exit 1, never overwritten); existing empty dir is used. **Excludes** env vars / process lists / logs / tokens / filesystem scans (may include rule names/hosts/ports). Reuses doctor builder/renderer + explanation registry + `writePrettyJSON`. Covered by black-box `support_bundle_test.go` (files/manifest/doctor schema/doctor.txt/explanations/unreachable/missing-out/non-empty/out-is-file/mkdir-failure/json-summary/strict/encode-failure) + `main_test.go` dispatch; CLI coverage 97.7% → 97.4% (gate 95%, **not lowered**; small dip = unreachable defensive filesystem branches, command-level failure paths covered); `validate:contract` 234/234 unchanged
- [x] Forward Rules row action menu — Start/Stop stays an inline button; the right-hand seamless "⋮" (More actions, styled like the drag handle) opens a menu with Edit, Duplicate, Diagnose, Activity, and a red Delete (in that order); menu closes on outside click / Escape; Delete still shows the inline Confirm/Cancel prompt; trigger disabled while the row is busy
- [x] Diagnose rule — panel opens, results shown, close button
- [x] Settings config import (merge — inline 1 TCP rule)
- [x] Settings config import (replace — v1-mixed fixture, 4 rules; verify Forward Rules view)
- [x] Settings config import (invalid JSON — parse error, state preserved)
- [x] Settings config export (download shape: version, exportedAt, rules array, rule present)
- [x] Settings Runtime/Environment section shows Node server runtime info
- [x] API Docs view — endpoint list, GET /api/connections listed
- [x] Live Connections view — title/tabs/empty states, tab switching, summary stats bar
- [x] Live Connections — protocol filter set/clear, auto-refresh toggle, footer counts
- [x] Live Connections — rule filter dropdown populated when rule is running
- [x] TCP real forwarding
- [x] UDP one-way, bidirectional-last-client, bidirectional-multi-client real forwarding
- [x] TCP and UDP activity event assertions

Config compatibility coverage (`validate:config` — not manual QA):

- [ ] Valid fixture config load (raw array — all protocols and UDP modes)
- [ ] Valid fixture config load (Go wrapper shape)
- [ ] Valid fixture import via HTTP API (all 8 valid fixtures)
- [ ] UDP default mode normalization (no udpMode → one-way)
- [ ] Export shape stability (version, exportedAt, rules[])
- [ ] Duplicate binding rejection (409)
- [ ] Invalid field rejection — port out of range, missing name, empty host, bad protocol, bad udpMode (400)
- [ ] Malformed JSON rejection (server exit)

Go service coverage:

- [ ] Config load/save and import/export
- [ ] Manager lifecycle, duplicate binding rejection, update/restart behavior, and reorder
- [ ] API routes and error shapes
- [ ] TCP real forwarding and activity
- [ ] UDP one-way, bidirectional-last-client, bidirectional-multi-client, stats, sessions, stop, and activity
- [ ] Port advisories, validation, options, static serving, and health endpoint

## Manual Platform QA Required Before Distribution

Manual QA is now limited to firewall behavior and production install paths. Core TCP/UDP protocol correctness and OS service install/uninstall flows are automated.

### Package Build and Smoke Test (Automated)

- [ ] `npm run validate:runtime:smoke` passes — builds `build/portier/`, validates layout and content, runs smoke test.
  - Validates: `service`/`service.exe`, `server.js`, `web/index.html`, `web/assets/`, `readme.txt`.
  - Validates: `readme.txt` mentions management URL and config path.
  - Validates: `node_modules`, `rules.json`, `sources/`, `client/`, `server/` are absent from the package.
  - Smoke test: starts the packaged binary, polls `/api/health`, GETs `/`, verifies HTML is served, stops cleanly.

### OS Service Install Validation (Automated — Run Explicitly)

Service install, start, health check, stop, and uninstall are validated by explicit commands on each platform:

- [ ] `npm run validate:service:windows:user` — Windows scheduled task, no Administrator required.
- [ ] `npm run validate:service:windows:machine` — Windows Service, Administrator required.
- [ ] `npm run validate:service:macos` — macOS LaunchAgent, no sudo required.
- [ ] `npm run validate:service:linux` — Linux systemd unit, root/sudo required.

Each script uses test-specific names and temp directories. Production Portier installs are not touched.

Pass `--no-build` to reuse an existing `build/portier/` and skip the package build step.

### Windows Firewall (Manual — On Real Production Install)

- [ ] Windows Firewall prompts or required inbound rules documented and observed for LAN-visible forwarded ports.
- [ ] Production install to `%ProgramFiles%\Portier` (Machine) or `%LOCALAPPDATA%\Portier` (User) verified.
- [ ] Config preserved at `%ProgramData%\Portier\rules.json` (Machine) or `%APPDATA%\Portier\rules.json` (User) after uninstall.

### macOS Firewall (Manual — On Real Production Install)

- [ ] macOS Firewall prompts or required settings documented and observed for LAN-visible forwarded ports.
- [ ] Production install to `~/Applications/Portier` with config at `~/Library/Application Support/Portier/rules.json` verified.
- [ ] Config preserved after `scripts/macos/service/uninstall-launch-agent.sh`.

### Linux Firewall (Manual — On Real Production Install)

- [ ] Firewall rules for LAN-visible forwarded ports documented and observed.
- [ ] Production install to `/opt/portier` with config at `/etc/portier/rules.json` verified.
- [ ] Config preserved after `scripts/linux/service/uninstall-service.sh` without `--remove-config`.

## Post-v1.0 Follow-Ups

- Drag-and-drop rule reorder testing, if drag-and-drop UI replaces the current Move Up/Down controls.
- macOS `.app` bundle or Homebrew formula.
- Linux hardening beyond the example systemd unit.

## Coverage Baseline (v1.5.0)

Updated at v1.5.0 release (2026-06-09).

| Component  | Statements | Branch | Functions |      Gate | Run command                      |
| ---------- | ---------: | -----: | --------: | --------: | -------------------------------- |
| tools/cli  |      93.2% |      — |     98.6% |       93% | `npm run coverage:cli`           |
| client     |     95.1%  | 90.1%  |    79.9%  |  94/90/79 | `npm run coverage:client`        |
| service    |      85.8% |      — |     93.2% |       85% | `npm run coverage:service`       |
| shared     |     100.0% |  100%  |    100%   | 100/100/100 | `npm run coverage:shared`      |
| server     |     88.7%  | 91.0%  |    99.1%  |  88/90/99 | `npm run coverage:server`        |
| scripts    |        N/M |      — |         — |      none | not yet measured                 |

Gate format: `stmts/branch/funcs` thresholds. All gates enforced at v1.4.0.

Run all (reporting): `npm run coverage:baseline`  
Run all with gates: `npm run validate:coverage` (exits 1 on gate failure)  
Per-component gate: `npm run validate:coverage:<component>`

All v1.4 new/changed modules reached 100% meaningful coverage:
- `server/sources/connections/tcp-connection-registry.ts`: 100%
- `server/sources/connections/udp-session-registry.ts`: 100%
- `server/sources/forwarders/tcp-forwarder.ts`: 100% stmts (up from 68.5%)
- `service/sources/connections/tcp_connection_registry.go`: 98.1% stmts, 100% public methods
- `service/sources/connections/udp_session_registry.go`: 98.4% stmts, 100% public methods
- `client/sources/features/connections/LiveConnectionsView.tsx`: 100% stmts
- `client/sources/utils/format.ts`: 100%

Coverage policy for v1.4 and v1.5: 100% meaningful coverage for all newly added or materially changed files. Existing baselines ratcheted incrementally; do not block unrelated work on legacy gaps.

---

## v1.6-pre Coverage Ratchet & Quality Hardening

Pre-release hardening pass to raise the test safety net toward ~95% meaningful coverage before starting the v1.6 architecture and quality audit. No new product features.

- [ ] v1.6-pre roadmap added to `docs/roadmap.md` ✓
- [ ] v1.6-pre changelog entry added
- [ ] Coverage baseline re-measured and documented under v1.6-pre
- [ ] Gap analysis performed; prioritized gap list produced
- **Service uplift** — meaningful Go tests for api, config, manager, validation, forwarder gaps → target ≥90%
- **Server uplift** — meaningful TS tests for api.ts, diagnose.ts, udp-forwarder.ts → target ≥95% stmts
- **CLI uplift** — meaningful CLI tests for remaining gaps → target ≥95%
- **Client branch/function uplift** — App.tsx, ActivityLogView, ForwardRuleList gaps → preserve ≥95% stmts
- [ ] Coverage gates ratcheted to stable achieved values; no gate lowered
- [ ] `validate:coverage` passes all gates at new values
- [ ] Full validation suite run: lint, typecheck, test, build, validate:contract, test:e2e, validate:cli, validate:runtime:smoke

---

## v1.6 Planning

v1.6 is a dedicated audit and hardening release targeting a structured multi-angle inspection of the codebase after v1.4 and v1.5 have raised test coverage. See `docs/roadmap.md` for audit dimensions, slice plan, and non-goals.

Planning checklist:

- [x] v1.6 roadmap added to `docs/roadmap.md` as Architecture, Quality & Maintainability Audit
- [x] Audit dimensions recorded (architecture boundaries, runtime parity, forwarding correctness, API contract, CLI quality, client/UI quality, test quality, complexity/maintainability, security/safety, packaging, documentation consistency)
- [x] Coverage prerequisite explained: v1.4/v1.5 100% meaningful coverage target is the safety net required before the v1.6 audit and hardening work
- [x] Tentative audit slice plan recorded (12 slices)
- [x] Non-goals recorded (no new large features, no server/ removal, no major architecture rewrite without audit-backed plan)
- [x] v1.4/v1.5 coverage target linked to v1.6 audit safety net in `docs/roadmap.md`

---

## v1.5 Planning

v1.5 targets declarative config and drift control: plan/diff/apply workflows for comparing desired config files with the running configuration and applying changes safely. See `docs/roadmap.md` for goals, CLI commands, UI direction, slices, and non-goals.

Planning checklist:

- [x] v1.5 roadmap added to `docs/roadmap.md`
- [x] Diff/plan/apply scope recorded
- [x] CLI commands recorded (`config diff`, `config plan`, `config apply --yes/--dry-run/--backup-out`, `--fail-on-drift`, `--json`)
- [x] Proposed API direction recorded (`POST /api/config/plan`)
- [x] UI import preview direction recorded (Settings / Config Import Preview: Add/Update/Remove/Unchanged counts before confirm)
- [x] Non-goals recorded (auth, remote management, cloud sync, team sharing, scheduled rules, firewall management, service install from CLI, full rollback/history, TUI, traffic graphs)
- [x] 100% meaningful coverage target recorded for v1.4 and v1.5

Checklist items to add per slice as work proceeds:

- [x] **Slice 1** — Config diff/plan strategy and contract: matching semantics (id-first, then identity key protocol+listenHost+listenPort), operation model (add/update/remove/unchanged, destructive flag, changes[]), plan summary (hasDrift, hasErrors, counts), plan/apply types added to `@portier/shared` (`shared/sources/plan.ts`: `ConfigPlanOperationType`, `ConfigPlanChange`, `ConfigPlanRuleSnapshot`, `ConfigPlanOperation`, `ConfigPlanSummary`, `ConfigPlanError`, `ConfigPlanWarning`, `ConfigPlanResponse`, `DesiredConfig`, `ConfigPlanRequest`, `ConfigApplyRequest`, `ConfigApplyResponse`). `plan.test.ts` added with 20+ shape tests covering all types. `POST /api/config/plan` and `POST /api/config/apply` added to `docs/api-contract.md` as Planned (v1.5). Client in-app API Docs updated with planned badges and response/request field docs. `ApiDocsView.test.tsx` updated with 6 new tests. `validate:contract` updated with skip notes for both endpoints. `tools/cli/readme.md` updated with planned v1.5 CLI commands. `docs/roadmap.md` v1.5 section expanded with matching semantics, operation model, and summary model.
- [x] **Slice 2** — Backend plan endpoint: `POST /api/config/plan` in TypeScript server; config comparison logic; 100% meaningful coverage. Pure plan engine (`server/sources/config-plan.ts`): `buildConfigPlan`, `extractRulesArray`, `validatedToSnapshot`, `ruleToSnapshot`, `diffMaterialFields`, `isDestructiveUpdate`, `detectDuplicateIds`, `detectDuplicateKeys`; id-first matching, identity key (protocol+listenHost+listenPort) fallback, ambiguous match error, `DUPLICATE_DESIRED_ID`/`DUPLICATE_DESIRED_IDENTITY_KEY` guards, 8 material field diff, destructive flag (remove always; update destructive when forwarding fields change), `REMOVE_EXISTING` / `LAN_EXPOSURE` warnings. `POST /api/config/plan` route added to `server/sources/api.ts`; 400 for missing desired, 200 with structured plan errors for invalid desired. 65 plan engine unit tests + 11 API integration tests. `validate:contract` TS skip removed (11 real assertions); Go skip kept with parity note. API Docs parity badge added (TypeScript server — Go service parity pending); `ApiDocsView.test.tsx` updated. `docs/api-contract.md` heading updated to Implemented (TypeScript server). `docs/changelog.md` updated.
- [x] **Slice 3** — Go service plan parity: `POST /api/config/plan` in Go service; parity with TypeScript behavior; `validate:contract` skip replaced with real checks. Pure plan engine added at `service/sources/configplan/plan.go` (`BuildConfigPlan`, `ExtractRulesRaw`): id-first matching, identity key fallback, 8 material field diff, destructive flag, `REMOVE_EXISTING`/`LAN_EXPOSURE` warnings, all error codes. Route and handler added to `service/sources/api/api.go`; 400 for missing `desired`, 200 with structured errors for invalid desired. 49 Go engine unit tests + 10 Go API integration tests. `validate:contract` Go skip removed; 11 real plan assertions now run against Go (138/2 passed/skipped); API Docs parity badge removed (both runtimes implement). Service coverage: 84.8% → 85.8% statements.
- [x] **Slice 4** — CLI `config plan` and `config diff` commands; `--json` output; `--fail-on-drift` with exit code 4; 100% meaningful coverage. `portier config plan <file>` and `portier config diff <file>` added. Both validate the file locally then call `POST /api/config/plan`. Plan: structured summary + per-operation listing with field-level changes and `[destructive]` flag. Diff: `+`/`~`/`-`/`=` prefixed visual output; `--show-unchanged` flag. `--fail-on-drift` exits 4 when drift and no plan errors; plan errors take priority (exit 1). `--json` prints raw `ConfigPlanResponse`. New client types: `ConfigPlanRuleSnapshot`, `ConfigPlanChange`, `ConfigPlanOperation`, `ConfigPlanSummary`, `ConfigPlanError`, `ConfigPlanWarning`, `ConfigPlanResponse`, `ConfigPlanDesired`, `ConfigPlanRequest`, `PlanConfig` method. `configcmd.go` extended with `RunConfigPlan`, `RunConfigDiff`, all formatting helpers. New test file `configplancmd_test.go` (35 tests) + 5 client tests. CLI coverage: 92.7% → 93.2% (gate 92%). All 5 coverage gates pass.
- [x] **Slice 5** — CLI `config apply` with `--yes`, `--dry-run`, `--backup-out`; `POST /api/config/apply` in TypeScript server and Go service. Handler logic: plan errors → 200 ok:false (no mutation); dryRun → 200 ok:true (no mutation, no yes required); destructive without yes → 400; drift → replace import with current ruleId injected for key-matched rules; no drift → 200 ok:true no import. Response: `ConfigApplyResponse { ok, dryRun, appliedAt, plan, applied }`. CLI: local file validation, backup export (skipped on dry-run), human output ("Nothing to apply" / "Dry run complete" / "Config applied"). 25 `configapplycmd_test.go` tests + 7 client tests + 11 Go API tests. `validate:contract` 156/156. API Docs updated, `docs/api-contract.md` updated. All 5 coverage gates pass.
- [x] **Slice 6** — Settings UI Plan & Apply: `planHelpers.ts` (4 helpers, 17 tests); `planConfig`/`applyConfig` API helpers with tests; Plan & Apply section in SettingsView with file picker, plan preview (summary counts, errors, warnings, operation list, destructive checkbox), apply (yes:true, ok:false handling, form clear on success); 23 new unit tests; E2E test in `settings.spec.ts`; E2E file label exact-match fix in portier.spec.ts/settings.spec.ts; client branch 90.1% (gate 90%); all 5 coverage gates pass; 32/32 E2E pass.
- [x] **Slice 7** — Contract/config validation and coverage gate hardening: `validate:contract` 156/156 assertions pass against both runtimes for all plan and apply scenarios. Coverage gates ratcheted to v1.5.0 values: cli ≥93%, server ≥88/90/99, client ≥94/90/79, service ≥85%, shared 100%. All new v1.5 code verified at 100% meaningful coverage.
- [x] **Slice 8** — v1.5 readiness audit, version bump to 1.5.0, changelog finalized. Full validation suite passed: lint, typecheck, 756 unit tests, all 5 coverage gates, 156/156 contract assertions, 32/32 E2E. `validate:runtime:smoke` passed. `build:release:current` produced release artifacts. Tag v1.5.0 ready.

---

## v1.4 Roadmap

v1.4 targets live connection and session visibility: a read-only Live Connection Inspector showing active TCP connections and UDP sessions with duration, bytes, and client address. See `docs/roadmap.md` for API direction, data model, UI direction, slices, and non-goals.

Quality target: all newly added or materially changed code in v1.4 should reach 100% meaningful test coverage, with explicit coverage gates where practical. This covers CLI additions, Go service changes, TypeScript server changes, shared types, contract validators, and client-side logic.

- [x] 100% meaningful coverage target/gate tracked for all new v1.4 Live Connection Inspector code (TCP tracking, UDP tracking, `GET /api/connections` handler, contract validator, UI helpers — both runtimes). CLI `connections` command and Slice 9 rule-row live summary deferred to v1.5.

Checklist items to add per slice as work proceeds:

- [x] **Slice 1** — Live Connection Inspector contract and coverage strategy: `GET /api/connections` response shape finalized (`tcpConnections`, `udpSessions`, `ruleSummaries`, `generatedAt`); coverage gate plan recorded; `docs/api-contract.md` draft updated; `docs/checklist.md` updated.
- [x] **Slice 2** — Shared types and API contract validation: `LiveConnectionsResponse`, `TcpConnectionInfo`, `UdpSessionInfo`, `RuleLiveSummary`, `LiveConnectionStatus`, `UdpSessionStatus` added to `@portier/shared` (`shared/sources/connections.ts`); `validate:contract` updated with skip note for planned endpoint; `docs/api-contract.md` finalized (field directions, `lastTrafficAt` null, Shared Shapes updated); client in-app API Docs view updated with planned badge; `ApiDocsView.test.tsx` updated with 5 new tests; `connections.test.ts` added with 14 shape tests. Implementation still pending (Slices 3–7).
- [x] **Slice 3** — TypeScript server TCP live tracking: `TcpConnectionRegistry` added (`server/sources/connections/tcp-connection-registry.ts`); `TcpForwarder` wired to open/track/close entries per connection; `ForwardManager` owns shared registry and exposes `getLiveTcpConnections()`; registry cleanup on error path (via `closeBoth`) and stop (via `closeConnectionsForRule`); no double-counting on error/close race; no payload capture. Registry: 100% stmts/branch/funcs. `tcp-forwarder.ts`: 100% stmts/funcs, 90% branch (uncovered branches = optional registry param when omitted). Server overall: 79.6% → 80.55% stmts. 28 registry tests + 7 forwarder integration tests added.
- [x] **Slice 4** — TypeScript server UDP session tracking: active session registry for all UDP modes; startedAt, lastSeenAt, packets/bytes; named idle/expiry constants (30s idle, 5min expire); 100% meaningful coverage including idle/expiry edge cases.
- [x] **Slice 5** — Go service TCP live tracking: `TcpConnectionRegistry` (`service/sources/connections/tcp_connection_registry.go`); `TCPForwarder` wired to open/close/track bytes; `Manager` owns shared registry and exposes `GetLiveTCPConnections()`; concurrency-safe (mutex for map ops, atomic byte counters); `CloseConnectionsForRule` in Stop as belt-and-suspenders. Registry: 98.1% stmts (100% public methods). 26 registry unit tests + 8 forwarder integration tests. Service overall: 79.7% → 80.6% stmts.
- [x] **Slice 6** — Go service UDP session tracking: `UdpSessionRegistry` (`service/sources/connections/udp_session_registry.go`); composite session keys (`ruleId:mode:clientAddress:clientPort`); `OpenOrTouchSession`/`RecordInbound`/`RecordOutbound`/`CloseSession`/`CloseSessionsForRule`/`PruneExpired`/`Snapshot`/`SnapshotForRule` API; `UDPSessionIdleDuration = 30s`, `UDPSessionExpireDuration = 5min`; status `active`/`idle` calculated at snapshot time; expired sessions hidden from snapshot without explicit prune; all three UDP modes wired in `UDPForwarder`; one-way/last-client modes call `OpenOrTouchSession` + `RecordInbound` per inbound packet; last-client closes previous session on client-endpoint change; multi-client stores `registryID` on session struct, calls `RecordOutbound` in `sessionReadLoop`, `CloseSession` in `expireSession`; `Manager` owns shared `UdpSessionRegistry`, injects it via `NewUDPForwarderWithRegistry`, exposes `GetLiveUDPSessions()`; `CloseSessionsForRule` in Stop as belt-and-suspenders. Registry: 98.4% stmts (100% public methods; 2 untestable defensive paths: stale-key path in `OpenOrTouchSession`, `rand.Read` fallback in `generateConnectionID`). 33 registry unit tests + 11 forwarder integration tests. Service overall: 80.6% → 82.1% stmts.
- [x] **Slice 7** — `GET /api/connections` parity: `GET /api/connections` implemented on TypeScript server (`server/sources/api.ts`) and Go service (`service/sources/api/api.go`); `RuleLiveSummary` + `LiveConnectionsResponse` types added to `service/sources/connections/live_connections.go`; both runtimes return identical JSON shape (`generatedAt`, `tcpConnections[]`, `udpSessions[]`, `ruleSummaries[]`); arrays always non-null; `lastTrafficAt` null for idle rules; `ruleSummaries` covers all configured rules; `fetchLiveConnections()` client API helper added; contract validator skip replaced with 8 real checks; API Docs view `Planned — v1.4` badge removed; 6 TypeScript + 6 Go tests.
- [x] **Slice 8** — Client API and Live Connections UI: `fetchLiveConnections()` API helper; Live Connections view (table with protocol, rule, client, target, duration/idle, bytes in/out, packets, status); rule/protocol/status filters; manual refresh and auto-refresh toggle; empty state; loading/error handling; 100% meaningful coverage of helpers and display logic.
- [ ] **Slice 9** — Rule row live activity summary: compact active connections/sessions count and last-traffic age per rule row, using `GET /api/connections` data; subtle display; tests added. *Deferred to v1.5.*
- [ ] **Slice 10** — CLI `portier connections`: calls `GET /api/connections`; human aligned table; `--rule`, `--protocol`, `--json` flags; safe rule resolver reused; 100% meaningful coverage; `validate:coverage` threshold maintained or raised. *Deferred to v1.5.*
- [ ] **Slice 11** — Diagnostics export integration: decide whether to include live session snapshot in CLI and UI support bundle; implement if promoted; update tests and contract if changed. *Deferred to v1.5.*
- [x] **Slice 12** — Coverage gates and readiness audit: all v1.4 coverage targets verified; all new/changed modules at 100% meaningful coverage; coverage gates added for all 5 components (cli ≥92%, client ≥90/89/76, server ≥82/86/97, service ≥82%, shared ≥82/54/90); vitest config corrected for Windows path-case issue; `validate:coverage` passes all gates; per-component `validate:coverage:*` scripts added.
- [x] **Slice 13** — v1.4 version bump, changelog finalized, tag created, full validation suite passed (`lint`, `typecheck`, `test`, `build`, `validate:cli`, `validate:contract`, `validate:runtime:smoke`, `validate:release:current`).
- [x] **Slice 14** — Docs update: `docs/roadmap.md`, `docs/api-contract.md`, `docs/changelog.md`, `README.md`, `AGENTS.md`, `CLAUDE.md` all reflect delivered v1.4 behavior.

---

## v1.3 Roadmap

v1.3 targets native CLI and automation: a Go-based `portier` CLI that talks to the existing management API. See `docs/roadmap.md` for principles, command set, implementation structure, packaging direction, slices, and non-goals.

Checklist items to add per slice as work proceeds:

- [x] **Slice 1** — CLI strategy and command design: command set, rule lookup behavior, output modes, exit code contract, and module layout confirmed. Documented in `docs/roadmap.md`.
- [x] **Slice 2** — Go CLI skeleton and API client: `tools/cli/` module scaffolded, HTTP client (`ConnectionError`/`APIError` types), `--url`/`--host`/`--port`/`PORTIER_URL` connection options, `--json` flag, `runtime` command (human + JSON output), structured error output, 22+ tests using `httptest`, `build:cli`/`test:cli`/`validate:cli` npm scripts.
- [x] **Slice 3** — Read-only commands: `portier list`, `portier status`, `portier activity`; human table and `--json` output; `--limit`/`--rule`/`--type`/`--severity` filters for activity; output helpers (`FormatBool`, `FormatBytes`, `FormatTimestamp`, `PrintTable`); 59 CLI tests total.
- [x] **Slice 4** — Lifecycle commands: `portier start <id|name>`, `portier stop <id|name>`, `portier diagnose <id|name>`; safe rule resolver (exact ID wins, unique name match, ambiguous-name exit 2, not-found exit 1); `DiagnosticCheck`/`DiagnosticSummary`/`RuleDiagnosticsResult` types; `StartForward`/`StopForward`/`DiagnoseForward` client methods; stable JSON objects for start/stop; raw `RuleDiagnosticsResult` for diagnose; 89 CLI tests total.
- [x] **Slice 5** — Config commands: `portier config validate <file>` (local-only, all three shapes, field/binding validation); `portier config export --out <file>` (calls `GET /api/config/export`, stdout JSON mode, no partial writes); `portier config import --mode merge|replace [--yes] <file>` (local validate before API, replace requires `--yes`); `ConfigRule`/`ConfigExportResponse`/`ConfigImportRequest`/`ImportResult`/`ConfigImportResponse` types; `doWithBody` helper; 132 CLI tests total.
- [x] **Slice 6** — Diagnostics export: `portier diagnostics export --out <file>` (bundle schema, partial failure, `--run-diagnostics`, `--activity-limit`, 153 tests total).
- [x] **Slice 7** — CLI packaging: `portier`/`portier.exe` built into `build/portier/` by all platform build scripts; runtime and release validation require CLI binary; Windows installer includes `portier.exe`; `readme.txt` documents CLI usage; no PATH integration in v1.3.
- [x] **Slice 8** — v1.3 readiness audit, version bump, changelog finalized, tag created. Coverage gate added (`validate:cli:coverage`, 88% threshold, 90.1% actual); version bumped to 1.3.0; all validation suites passed.
- [x] **Post-v1.3 coverage ratchet** — CLI coverage gate raised to 92% (actual: 92.7%); 10 targeted tests added; first ratchet step toward v1.4/v1.5 100% meaningful coverage target.

---

## v1.2 Roadmap

v1.2 focuses on diagnostics and operational polish. See `docs/roadmap.md` for goals, slices, and non-goals.

Checklist items to add per slice as work proceeds:

- [x] **Slice 1** — Runtime info endpoint and UI display: `GET /api/runtime` in both runtimes, Settings Runtime/Environment section, shared `RuntimeInfo` type, contract validator updated.
- [x] **Slice 2** — Rule diagnostics API: `POST /api/forwards/:id/diagnose` in both runtimes; `docs/api-contract.md` updated; client in-app API Docs updated; API Docs tests updated.
- [x] **Slice 3** — Rule diagnostics UI: Diagnose button per rule row, inline diagnostics panel, all check states, clear on close/delete; tests added; API Docs updated.
- [x] **Slice 4** — Activity Log polish: View Activity button per rule row, filter banner, type filter, clear filters, `DELETE /api/activity` on both runtimes, Export JSON, Clear Log, packet throttle note; `docs/api-contract.md` updated; client in-app API Docs updated; contract validator updated; tests added.
- [x] **Slice 5** — Settings / runtime / config polish: copy buttons (config path, static dir, management URL), datetime export filename, export note excludes Activity Log, export success/error feedback, import mode with descriptions above file picker, replace confirm backup export button, `PORTIER_APP_VERSION` constant, version in sidebar footer; 24 new tests.
- [x] **Slice 6** — Safer networking UX: listen host presets (Local only / LAN exposed), inline LAN warning in form, platform-aware firewall note, friendly conflict error copy, improved LAN_EXPOSURE advisory message; 17 new/updated ForwardRuleForm tests.
- [x] **Slice 7** — Diagnostics export: Download Diagnostics JSON button in Settings; client-side bundle (runtime, rules, statuses, activity, UI-session diagnostics); partial-failure errors array; no backend endpoint; 216 client tests passing.
- [x] **Slice 8** — v1.2 readiness audit, version bump, changelog, tag: all validation passed; version bumped to 1.2.0; changelog finalized; tag ready.

---

## v1.1 Installer Readiness

v1.1 focuses on distribution and native OS service installers. See `docs/installer.md` for scope and implementation slices.

Checklist items to add per slice as work proceeds:

- [x] **Slice 2** — Windows Inno Setup installer: machine-wide install (`%ProgramFiles%\Portier`), optional Windows Service task, uninstall preserves `rules.json`. Scripts in `scripts/windows/release/`. Build via `npm run release:current`. Requires Inno Setup 6.
- [x] **Slice 3** — macOS LaunchAgent polish: auto-copy from `build/portier/`, `--source-dir`/`--no-start`/`--runtime` options, label bug fixed, `--purge` on uninstall, `scripts/macos/release/build-release.sh` for portable tar.gz, signing/notarization docs. Service lifecycle scripts in `scripts/macos/service/`. Build via `npm run release:current` (on macOS). Validate via `npm run validate:service:macos`.
- [x] **Slice 4** — Linux install/uninstall/start/stop/status scripts complete; `--source-dir`/`--no-enable` added to `install-service.sh`; `scripts/linux/release/build-release.sh` for portable tar.gz; service lifecycle scripts in `scripts/linux/service/`; systemd unit examples and docs updated.
- [x] **Slice 5** — `validate:service:*` scripts unified: all support `--no-build`, `--keep-files`, `--port`; test-specific names/paths/ports on all platforms; `validate:service:current` dispatches by OS with unsupported-platform error. Windows user-scope validated on Windows host. macOS/Linux validation requires the respective OS.
- [x] **Slice 6** — `release:current` and `release:portable` produce `build/releases/<platform>/` with versioned portable archives. `validate:release:portable` validates layout, required files, forbidden files, and readme.txt content. Windows zip via `Compress-Archive`; macOS/Linux tar.gz via `scripts/macos/release/build-release.sh` and `scripts/linux/release/build-release.sh`. Windows installer non-fatal if Inno Setup absent. macOS .pkg and Linux .deb/.rpm deferred.
- [x] **Slice 7** — v1.1 readiness audit passed; version bumped to `1.1.0`; changelog entry finalized; tag created.
